<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 107]
- [stat.ML](#stat.ML) [Total: 11]
- [eess.SP](#eess.SP) [Total: 14]
- [cs.AI](#cs.AI) [Total: 119]
- [cs.RO](#cs.RO) [Total: 26]
- [cs.LG](#cs.LG) [Total: 114]
- [eess.IV](#eess.IV) [Total: 4]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [Theatre Chapbooks At Scale: A Statistical Comparative Analysis of Typography](https://arxiv.org/abs/2607.27266)
*Diego Belzarena,Seginus Mowlavi,Paula Casariego Castiñeira,Alejandra Ulla Lorenzo,Gregory Randall,Jean-Michel Morel*

Main category: cs.CV

TL;DR: 提出一种统计方法，通过字符原型聚类与对齐量化历史印刷书籍之间的字体相似度，并利用a contrario统计框架解释字体距离的显著性，用于加速17世纪西班牙戏剧小册子的文献学分析。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于数字人文/文献学领域的历史印刷书籍字体（typeface）相似性量化，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感场景，也不涉及object detection、目标检测、轻量化、pruning、quantization、knowledge distillation等任何与在轨目标检测或模型压缩相关的内容。

Reading priority: 1分。论文属于数字人文/文献学领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何模型压缩/加速技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：历史印刷书籍字体相似性量化与文献学分析（philological analysis）。应用场景：数字人文、数字文献学（digital bibliography）。传感器类型：不适用（处理的是扫描印刷书籍图像中的字符）。目标类别：Roman和Italic两种字体类型。摘要未说明具体图像采集设备。

Model architecture: 方法核心包括：(1) 从书籍中自动提取字符图像；(2) 通过clustering和aligning生成character prototypes（字符原型）；(3) 基于原型定义任意两本书之间的typeface distance（字体距离）；(4) 采用a contrario statistical framework（a contrario统计框架）解释字体距离的显著性。摘要未说明具体网络结构或深度学习模型。

Lightweight method: 摘要未涉及任何模型轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、轻量网络设计、实时推理、边缘部署、FPGA/NPU/TensorRT等任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。

Datasets and metrics: 数据集：17世纪西班牙印刷戏剧chapbooks（小册子）的扫描图像，数量超过人类专家系统视觉检查的能力范围。评价指标：typeface distance（字体距离）及其显著性（通过a contrario统计框架判定）。摘要未说明具体定量指标（如准确率、召回率等）。

Experiments: 主要实验：将方法应用于17世纪西班牙印刷戏剧chapbooks的文献学研究，自动比较不同书籍中提取的Roman和Italic字体。结果：经人类专家验证，方法发现了新的印刷商归属（printer attributions），并修正了此前的印刷商归属。摘要未说明与其他方法的对比实验或消融实验。

Limitations: (1) 与遥感目标检测及轻量化方向完全无关，无直接借鉴价值；(2) 摘要未说明方法的计算复杂度、运行效率；(3) 摘要未说明方法对噪声、图像质量退化（如历史书籍扫描件的磨损、墨迹污染）的鲁棒性；(4) 摘要未说明方法是否可扩展到其他语言或时期的书籍；(5) 摘要未说明字符提取、聚类、对齐的具体实现细节和失败模式。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但存在以下间接可借鉴点：(1) a contrario统计框架用于显著性判定的思想，可类比于遥感目标检测中检测结果的置信度校准或虚警控制（false alarm suppression）；(2) 字符原型聚类与对齐的思路，可类比于遥感小目标检测中的模板匹配或原型学习方法；(3) 大规模自动分析超出人类视觉检查能力的数据，这一思路与遥感大数据处理中自动化分析的需求有相通之处。但整体而言，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值很低。

Abstract: We propose a statistical methodology that quantifies the similarity of typefaces between printed historical books. This provides a tool that accelerates philological analysis. Using character prototypes derived from clustering and aligning automatically extracted character images, the method defines a typeface distance between any two books. To produce actionable outputs, we develop an a contrario statistical framework to interpret the significance of the computed typeface distances. We apply the method to the philological study of 17 th -century Spanish printed theatre chapbooks in a quantity that exceeds the capabilities of systematic visual inspection by human experts. Our method enables the automatic comparison of Roman and Italic types extracted from different books. After validation by human experts, our method has led to new printer attributions being discovered, and former printer attributions being revised. This success strongly suggests that our method has the potential to enable digital bibliography on a larger scale than was previously possible.

</details>


### [2] [OVEarth-Bench: Evaluating Category Breadth and Query Diversity for Open-Vocabulary Earth Observation](https://arxiv.org/abs/2607.27278)
*Kaiyu Li,Zepeng Xin,Zixuan Jiang,Jing Fu,Lanxuan Xue,Lingyu Zhang,Xiangyong Cao*

Main category: cs.CV

TL;DR: 提出 OVEarth-Bench，一个面向开放词汇地球观测（open-vocabulary EO）的基准测试，扩展了类别广度（层级化类别覆盖、正负表达）和查询多样性（词汇、指代、推理查询），并在统一 zero-shot 协议下评估多种通用与 EO 专用方法。

Research relevance: 2 分（弱相关）。论文属于遥感/Earth observation 场景，也涉及目标定位（localization，含 box 定位），但核心是开放词汇（open-vocabulary）理解与基准构建，不涉及目标检测模型的轻量化、剪枝、量化、蒸馏、实时推理或星上部署。与"在轨遥感目标检测模型轻量化"方向仅在地球观测场景和目标定位层面有间接关联，轻量化与部署信息完全缺失。

Reading priority: 2 分。论文与遥感场景和目标定位相关，但核心是开放词汇基准构建与评测，不涉及目标检测模型轻量化或星上部署，对本课题的直接借鉴价值有限。若关注开放词汇遥感理解前沿可略读，否则优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：开放词汇地球观测（open-vocabulary EO），包括 mask 和 box 两种定位形式；应用场景为遥感/Earth observation 场景理解；传感器类型摘要未说明（推测为光学遥感影像，但摘要未明确）；目标类别为自然语言描述的地理空间概念（geospatial concepts），非固定类别集，涵盖层级化类别。

Model architecture: 摘要未说明具体模型结构。论文评估了通用方法（general methods）和 EO 专用方法（EO-specific methods），并特别提到基于 MLLM（Multimodal Large Language Model）的方法表现最强，但未给出具体网络架构细节。

Lightweight method: 摘要未说明。论文不涉及任何轻量化技术（pruning、quantization、knowledge distillation、NAS、轻量网络设计等均未提及），也不涉及实时推理或边缘/星上部署。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关指标。且 MLLM-based 方法通常计算开销大，与在轨轻量化部署方向存在张力，但论文未讨论此问题。

Datasets and metrics: 提出新基准 OVEarth-Bench，扩展两个维度：(1) 类别广度——广泛的层级化类别覆盖，含正负表达（positive and negative expressions）；(2) 查询多样性——词汇查询（vocabulary）、指代查询（referring）、推理查询（reasoning）。采用统一的 zero-shot 协议，支持 mask 和 box 两种定位评估。具体数据集规模、评价指标细节（如 mAP 等）摘要未说明。

Experiments: 在 OVEarth-Bench 上评估了广泛的通用方法和 EO 专用方法，主要发现：(1) 现有方法性能有限，更广的类别覆盖带来更稳定的模型排名；(2) MLLM-based 方法整体性能最强；(3) EO 专用方法普遍不如通用模型，很少能匹配最强方法。具体数值结果摘要未说明。

Limitations: 摘要未说明。但可推断：(1) 论文聚焦基准构建与评测，不涉及模型压缩或部署；(2) MLLM-based 方法计算开销大，不适合在轨场景；(3) 未讨论轻量化与在轨部署可行性；(4) 未提供模型效率指标。

Ideas for my research: 尽管论文不涉及轻量化，但对本课题仍有间接借鉴：(1) 开放词汇/自然语言驱动的遥感目标定位是前沿方向，若未来在轨模型需支持灵活类别查询，可考虑将轻量化技术（如蒸馏、量化）应用于 MLLM-based 检测器；(2) OVEarth-Bench 的层级化类别与正负表达设计，可作为评估轻量化模型在复杂语义下鲁棒性的参考基准；(3) 论文指出 EO 专用方法普遍弱于通用方法，提示在轨轻量化模型设计时不应过度依赖手工特征，可借鉴通用模型的表征能力并通过蒸馏迁移到轻量网络。

Abstract: Open-vocabulary Earth observation (EO) aims to localize geospatial concepts specified in natural language rather than a fixed label set. Existing benchmarks, however, usually cover narrow category vocabularies or limited query forms. To fill this gap, we introduce OVEarth-Bench, which extends existing evaluation in two directions: category breadth, through broad hierarchical category coverage with positive and negative expressions, and query diversity, through vocabulary, referring, and reasoning queries. The benchmark supports mask and box localization under a unified zero-shot protocol. We evaluate a broad set of general and EO-specific methods. The evaluation reveals that: (1) the performance of current methods remains limited, while broader category coverage yields more stable model rankings; (2) MLLM-based methods achieve the strongest overall performance; and (3) EO-specific methods generally underperform general models and rarely match the strongest methods. These findings provide guidance for future open-vocabulary EO method design and highlight the importance of developing more realistic, diverse, high-quality, and large-scale benchmarks for reliable evaluation. Our data and evaluation package are released at https://earth-insights.github.io/OVEarth-bench.

</details>


### [3] [VETO: Towards Protecting Images From Frontier AI Editing](https://arxiv.org/abs/2607.27292)
*Jonas Grebe,Hossein Shakibania,Tobias Braun,Marcus Rohrbach,Anna Rohrbach*

Main category: cs.CV

TL;DR: 提出VETO，一种针对现代图像编辑模型（如FLUX.2）的防篡改"隐形斗篷"（anti-edit cloak），通过干扰joint-attention机制来阻止模型从参考图像中提取信息，并引入VetoBench基准来评估防御效果。

Research relevance: 1分，基本无关。该论文聚焦于生成式图像编辑模型的对抗性防御（anti-edit defense），属于生成式AI安全领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或加速方法。

Reading priority: 1分。论文主题为生成式图像编辑的对抗性防御，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像编辑防御（anti-edit defense）/ 对抗性扰动生成。应用场景：生成式AI安全与内容保护。传感器类型：不适用（自然图像）。目标类别：不适用（涉及图像编辑中的对象与身份重上下文化）。

Model architecture: 针对现代扩散模型（如FLUX.2）的joint-attention机制设计对抗性扰动（cloak），通过干扰prompt/generation tokens对reference-image tokens的注意力读取来阻断信息提取。摘要未说明具体网络结构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文与星上/边缘部署无关，未报告参数量、FLOPs、延迟、显存、能耗等部署指标。

Datasets and metrics: 引入VetoBench基准，评估传统局部编辑（localized edits）和更广泛的上下文偏移（contextual shifts）两类场景。在两种当代编辑模型和三个基准上评估。具体数据集名称和指标（如PSNR、SSIM、FID等）摘要未说明。

Experiments: 在两种当代编辑模型（含FLUX.2）和三个基准上，VETO在防御效果和保护-保真度权衡（protection-fidelity trade-off）上持续优于现有防御方法。具体数值结果摘要未说明。

Limitations: 与本研究方向基本无关。主要局限：1）属于生成式AI安全领域，与遥感目标检测和轻量化完全脱节；2）不涉及任何遥感数据、卫星场景或目标检测任务；3）不提供任何模型压缩、加速或部署相关信息；4）其对抗性扰动方法对遥感在轨智能的借鉴价值极为有限。

Ideas for my research: 借鉴价值极低。唯一可能的间接启发是：如果未来遥感在轨目标检测模型需要防御对抗性攻击（如对SAR/光学图像的对抗扰动），可参考其"干扰注意力机制"的思路，但这与本课题"轻量化"核心方向无关。不建议投入时间深入阅读。

Abstract: The rise of powerful, accessible image-editing models such as FLUX.2 has brought high-fidelity editing within broad reach. Their capabilities now extend beyond localized modifications to extracting and recontextualizing objects and identities in entirely new scenes. By allowing prompt and generation tokens to attend directly to reference-image tokens, modern models blur the boundary between conventional editing and text-to-image synthesis. This expanded generative freedom also broadens the space of potential misuse, as harmful transformations are no longer confined to a predictable set of localized edits. Existing anti-edit defenses are designed to disrupt the semantic bottleneck of the reference-image encoding in legacy diffusion pipelines. However, newer editors distill reference information through joint-attention blocks, thereby often circumventing these protections. We therefore introduce VETO, a subtle anti-edit cloak that disrupts this inner mechanism through which modern models read the source image. Additionally, as existing editing benchmarks leave comprehensive recontextualizations largely untested, we introduce VetoBench, which evaluates defenses not only on conventional localized edits but also on broader contextual shifts. Across two contemporary editing models and three benchmarks, VETO consistently outperforms existing defenses while providing a stronger protection-fidelity trade-off.

</details>


### [4] [Bunraku: Turning a Single Illustration into an Editable Live2D Character](https://arxiv.org/abs/2607.27348)
*Junhao Chen,Jingjia Mao,Dayong Li,Chenghai Li,Saining Zhang,Zhihao Li,Hao Zhao,Yufei Wang,Ruqi Huang*

Main category: cs.CV

TL;DR: 提出首个从单张插画端到端生成完整 Live2D 结构化动画资产（RGBA 图层、变形网格、关键帧位移）的系统，并构建了 Live2D-Bench 基准与 8,884 个模型的语料库。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于 2D 动漫角色动画（Live2D）的生成式建模，属于计算机图形学/生成模型领域，不涉及 remote sensing、object detection、卫星图像或任何模型压缩/部署技术。相关性评分：1 分。

Reading priority: 1 分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于 2D 动画生成领域。除非对生成式图层分解或联合预测机制有跨领域兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：从单张插画生成 Live2D 结构化动画资产（图层分解、网格生成、关键帧位移预测）。应用场景：虚拟直播、移动游戏、交互式角色。传感器类型：不适用（输入为 2D 插画）。目标类别：动漫角色/虚拟化身。

Model architecture: 两阶段生成框架：Stage 1 采用 Live2D-aware 器官级分类法下的分层扩散过程（layered diffusion process），生成有序 RGBA 图层堆叠并完成遮挡区域补全；Stage 2 从 alpha 通道构建内容一致的三角网格，并联合预测所有图层的关键帧位移场——每个顶点作为一个 token，self-attention 跨越图层边界，位移被分解为有界方向向量和对数幅度。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS 等模型压缩或加速方法。值得注意的是，作者提到将网络规模扩大 112 倍并未带来性能提升，这暗示模型规模并非关键因素，但并未从轻量化角度展开。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗或实时推理能力，与星上/边缘部署无关。

Datasets and metrics: 数据集：自建的 8,884 个模型 Live2D 语料库（含图层与动画监督），以及 50 个留出角色用于测试；提出 Live2D-Bench 标准化基准。评价指标：Stage 2 在无 teacher forcing 的真实生成条件下，每顶点方向余弦（direction cosine）达到 0.768（中位数 0.828）。

Experiments: 主要实验：在 50 个留出角色上评估 Stage 2 的位移预测质量，per-vertex direction cosine 为 0.768（中位数 0.828）。关键消融发现：联合预测（joint prediction）而非独立预测是获得连贯角色的最大增益来源；将网络规模扩大 112 倍无性能提升。另验证了基于自然语言指令对服装图层进行重纹理化，同时复用网格与预测动画。

Limitations: 摘要未说明具体局限。可推断的缺口包括：仅针对动漫/虚拟角色领域，不涉及遥感图像；未讨论模型效率、部署约束或实时性能；生成质量指标（direction cosine）与下游任务（如目标检测精度）无可比性；未涉及任何遥感传感器数据类型（光学、SAR、高光谱等）。

Ideas for my research: 尽管领域无关，仍有少量方法论层面的可借鉴点：(1) 联合预测（joint prediction）优于独立预测的结论，可类比到遥感小目标检测中多目标/多尺度联合建模；(2) "网络规模扩大 112 倍无增益"的发现提示在遥感检测中应关注架构设计而非单纯堆参数量，这与轻量化目标一致；(3) 将位移分解为有界方向+对数幅度的因子化表示，可类比旋转框检测中角度与尺寸的解耦回归。但整体借鉴价值有限。

Abstract: Live2D is the dominant 2D character-animation format for anime characters and virtual avatars, representing each character as a stack of RGBA layers driven by per-layer mesh deformation. Despite its wide use in virtual streaming, mobile games, and interactive characters, authoring a Live2D model still demands weeks of manual layer separation, occlusion completion, mesh placement, and keyframing, and no prior generative method produces such a structured asset end-to-end. We present the first system that, from a single illustration, generates all the structured information a Live2D runtime consumes: ordered RGBA layers, a deformation mesh per layer, and the parameter-driven keypose vertex offsets that make the character move. Stage 1 casts layered decomposition as a layered diffusion process under a Live2D-aware organ-level taxonomy, producing an ordered RGBA stack with hidden-region completion. Stage 2 builds a content-conforming triangle mesh for each layer from its alpha channel alone, then predicts the keypose displacement field of all layers jointly: every vertex of every layer is one token, self-attention spans layer boundaries, and each displacement is factorised into a bounded direction and a log-magnitude. Joint rather than independent prediction is what makes the result a coherent character instead of separately plausible parts, and is our largest gain; scaling the network 112x yields none. On 50 held-out characters, under true generation with no teacher forcing, Stage 2 attains a per-vertex direction cosine of 0.768 (median 0.828). Because a layer's mesh derives from its alpha channel, a clothing layer can be re-textured from a natural-language instruction while the mesh and predicted animation are reused byte-for-byte. We further contribute Live2D-Bench, the first standardized benchmark for the task, and an 8,884-model Live2D corpus with layer and animation supervision.

</details>


### [5] [Shared Semantic Codebook Distillation for Unpaired Cross-Modal Medical Classification](https://arxiv.org/abs/2607.27357)
*Dillan Imans,Phuoc-Nguyen Bui,Duc-Tai Le,Hyunseung Choo*

Main category: cs.CV

TL;DR: 提出Shared Semantic Codebook Distillation (SSCD)，通过共享离散码本在未配对跨模态医学图像之间进行知识蒸馏，将强教师模态的诊断知识迁移到更轻量、更易部署的学生模态。

Research relevance: 弱相关（2分）。论文属于泛机器学习/跨模态知识蒸馏方法，应用场景是医学图像分析（OCT-to-fundus视网膜疾病分类、CT-to-chest-X-ray肺炎分类），与遥感/卫星/在轨部署无直接关联。但其核心思想——通过共享离散码本实现无需配对样本的跨模态知识蒸馏，以及推理时丢弃教师侧和码本模块仅保留学生编码器的设计——对遥感领域跨传感器（如SAR到光学）知识蒸馏具有方法层面的借鉴意义。

Reading priority: 3分。方法本身（跨模态未配对知识蒸馏）对遥感跨传感器蒸馏有借鉴价值，但应用场景是医学图像分类而非遥感目标检测，且未提供任何轻量化部署量化指标，与在轨部署直接相关性有限。可作为方法参考阅读，但优先级中等。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：跨模态知识蒸馏（cross-modal knowledge distillation），用于医学图像分类。应用场景：OCT-to-fundus视网膜疾病分类、CT-to-chest-X-ray肺炎分类。传感器类型：OCT、眼底图像（fundus）、CT、胸部X光。目标类别：视网膜疾病、肺炎。摘要未说明具体类别数量。

Model architecture: 核心架构：Shared Semantic Codebook Distillation (SSCD)。每张图像被表示为一个共享的、模态无关的离散码本（codebook）上的分布，通过全局和类别条件（class-conditionally）对齐这些分布来实现知识迁移。码本通过指数移动平均（EMA）在线演化，并通过熵正则化（entropy regularization）和死码重启（dead-code restart）保持多样性。推理时丢弃所有教师侧和码本模块，仅保留学生编码器和分类器。摘要未说明学生/教师网络的具体结构（如ResNet、ViT等）。

Lightweight method: 采用知识蒸馏（knowledge distillation）作为模型压缩/轻量化手段。具体创新在于：针对未配对（unpaired）跨模态场景，提出通过共享离散码本（shared discrete codebook）进行蒸馏，避免实例级蒸馏（instance-level distillation）和直接特征匹配（direct feature matching）在几何不兼容特征空间中的失效问题。蒸馏在全局和类别条件两个层面进行分布对齐。推理阶段教师侧和码本模块全部丢弃，仅保留轻量学生编码器+分类器，实现部署端轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等具体部署指标。仅提及学生模态"cheaper and more deployable"（更便宜、更易部署），且推理时丢弃教师侧和码本模块，只保留学生编码器和分类器，说明部署端模型结构精简。但未给出任何量化数据。

Datasets and metrics: 数据集：两个异质未配对设置——OCT-to-fundus视网膜疾病分类、CT-to-chest-X-ray肺炎分类。评价指标：macro-F1。实验结果：OCT-to-fundus从64.5提升到70.2 macro-F1；CT-to-chest-X-ray从73.8提升到76.3 macro-F1，均优于所有评估的蒸馏基线方法。代码和预训练模型已开源（GitHub）。

Experiments: 在两个未配对跨模态医学图像分类任务上验证SSCD。学生基线分别为64.5和73.8 macro-F1，经SSCD蒸馏后提升至70.2和76.3 macro-F1，均超越所有对比的蒸馏基线。摘要未说明具体对比的蒸馏基线方法名称，也未提供消融实验细节。

Limitations: 1. 应用场景为医学图像，与遥感/卫星场景无关；2. 摘要未说明学生模型的轻量化程度（参数量、FLOPs等）；3. 未说明蒸馏后学生模型与教师模型的性能差距；4. 未涉及遥感中常见的旋转框检测、小目标检测等任务，仅针对分类任务；5. 未说明码本蒸馏的计算开销和训练成本；6. 未说明在目标检测任务上的适用性。

Ideas for my research: 1. 跨传感器知识蒸馏启发：遥感领域常面临SAR与光学影像、多光谱与高光谱影像之间的跨模态知识迁移问题，SSCD的共享离散码本思想可借鉴用于SAR-to-optical的遥感目标检测知识蒸馏，解决配对样本稀缺问题；2. 推理侧轻量化设计：推理时丢弃教师和码本模块、仅保留学生编码器的设计思路，与在轨部署中"训练在地面、推理在星上"的范式高度契合；3. 类别条件蒸馏：遥感目标检测中不同类别（舰船、飞机、车辆）特征差异大，类别条件（class-conditioned）的蒸馏对齐方式可能比全局对齐更有效；4. 码本在线演化机制（EMA+熵正则化+死码重启）可借鉴用于稳定遥感跨域蒸馏训练。

Abstract: Cross-modal knowledge distillation can transfer diagnostic knowledge from a strong but costly teacher modality to a cheaper and more deployable student modality. In medical image analysis, however, the two modalities are often unpaired: they are collected from different patient cohorts and occupy geometrically incompatible feature spaces. This makes instance-level distillation invalid and direct feature matching unreliable. To address these challenges, we propose Shared Semantic Codebook Distillation (SSCD), which compares teacher and student representations through a shared discrete codebook. Each image is represented as a distribution over a common, modality-agnostic vocabulary, and knowledge is transferred by aligning these distributions across modalities, both globally and class-conditionally, without requiring paired samples or directly comparable raw features. The codebook is evolved online by exponential moving average and kept diverse through entropy regularization and dead-code restart. At inference, all teacher-side and codebook modules are discarded, leaving only the student encoder and classifier. On two heterogeneous unpaired settings, OCT-to-fundus retinal disease classification and CT-to-chest-X-ray pneumonia classification, SSCD improves the student from 64.5 to 70.2 macro-F1 and from 73.8 to 76.3 macro-F1, respectively, outperforming all evaluated distillation baselines on both settings. Code and pretrained models are available at https://github.com/DillanImans/SSCD-unpaired-distillation

</details>


### [6] [PanDent: Toward Comprehensive Tooth-Level Structure-Language Consistency in Dental Radiology](https://arxiv.org/abs/2607.27378)
*Xiaohan Li,Xinyu Liu,Chang Liu,Sum Wing Au Yeung,Jun Liu,Yixuan Yuan,Hui Chen*

Main category: cs.CV

TL;DR: 提出PanDent，一个基于9,524张牙科全景X光片（OPG）的大规模临床基准数据集，用于评估多模态大语言模型（MLLMs）在牙齿级诊断和临床推理上的表现。

Research relevance: 与本课题（遥感卫星在轨目标检测与模型轻量化）基本无关。论文聚焦于牙科全景X光影像（dental panoramic radiography）上的多模态大语言模型（MLLM）临床推理评估，既非遥感/卫星/地球观测场景，也不涉及目标检测或模型轻量化/部署优化。属于医学影像+大语言模型评估方向，与本课题仅存在"视觉-语言模型评估"这一泛化方法层面的微弱关联。

Reading priority: 1分。论文主题为牙科影像上的MLLM临床推理评估，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何轻量化/部署技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态大语言模型（MLLM）在牙科全景X光影像上的临床推理与报告生成评估。应用场景：牙科影像诊断（dental panoramic radiography / orthopantomogram, OPG）。传感器类型：X光影像（非遥感传感器）。目标类别：牙齿（tooth-level）及其临床诊断发现。

Model architecture: 摘要未说明具体模型架构。实验对象为多种现有多模态大语言模型（MLLMs），包括SOTA专有模型（proprietary models）、通用开源模型（general-domain open-source models）和医学专用模型（medical-specific models），但未给出具体模型名称或结构细节。

Lightweight method: 摘要未涉及任何模型压缩或加速方法。未提及pruning、quantization、knowledge distillation、NAS、轻量网络设计、实时推理或边缘/星上部署等轻量化技术。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该任务为牙科影像诊断，与星上/边缘部署场景无关。

Datasets and metrics: 数据集：PanDent，包含9,524张高质量OPG（牙科全景X光片），由经验丰富的牙医进行细粒度牙齿级标注，并经口腔颌面放射科医生验证。同时构建了与结构化临床证据对应的临床一致性放射学报告。评价指标：摘要未明确列出具体指标名称，但强调评估MLLM生成的报告是否在语言流畅性之外还具备临床一致性（clinically consistent），涉及细粒度定位（fine-grained localization）和牙齿级诊断准确性。

Experiments: 在多种MLLM上进行了实验，包括SOTA专有模型、通用开源模型和医学专用模型。主要发现：当前MLLM能生成流畅的报告，但在细粒度定位和牙齿级诊断上存在显著错误，无法产生临床一致的描述。在PanDent上微调（fine-tuning）能显著提升结构-语言一致性，增强视觉定位准确性和诊断正确性，使模型输出更接近专家牙科解读。

Limitations: 1. 与本课题方向无关，属于医学影像领域。2. 摘要未说明具体MLLM模型名称、参数量、推理成本。3. 未涉及任何轻量化或部署优化技术。4. 未提供定量指标数值（如准确率、定位误差等）。5. 数据集为牙科X光，与遥感影像（光学/SAR/高光谱）在成像机理、目标尺度、场景复杂度上差异巨大。

Ideas for my research: 尽管方向不同，仍有少量可借鉴的泛化思路：1. 该工作强调"细粒度标注+专家验证"构建高质量基准数据集的方法论，可类比到遥感目标检测中构建专家级细粒度标注（如小目标、旋转框的精确标注）的基准构建思路。2. 其"结构化临床证据与自由文本报告对应"的设计，可启发遥感领域构建"检测结果-自然语言描述"的跨模态对齐评估框架。3. 其发现"模型能生成流畅输出但细粒度定位差"的结论，与遥感小目标检测中模型"能检测但定位精度不足"的问题有相似性，可借鉴其细粒度评估思路。但这些均为间接借鉴，对"在轨轻量化"本身无直接参考价值。

Abstract: Accurate evaluation of multimodal large language models (MLLMs) in dental panoramic radiography (orthopantomogram, OPG) is limited by the lack of fine-grained, clinically reliable benchmarks that reflect expert interpretation. This work introduces PanDent, a large-scale, clinically grounded OPG benchmark built upon fine-grained, expert-validated tooth-level annotations. The dataset comprises 9,524 high-quality OPGs, each associated with comprehensive structured annotations produced by experienced dentists and further validated by an oral and maxillofacial radiologist, providing clinically reliable supervision for tooth-level diagnosis and reasoning. Clinically consistent radiology reports are constructed from expert-validated findings using clinician-defined reporting logic, establishing explicit correspondence between structured clinical evidence and free-text descriptions. This design enables evaluation of whether MLLMs generate reports that are not only linguistically coherent but also clinically consistent with expert-validated tooth-level findings. Experiments are conducted on diverse MLLMs, including state-of-the-art (SOTA) proprietary models, general-domain open-source models, and medical-specific models. Results show that current MLLMs can generate fluent reports, yet fail to produce clinically consistent descriptions, exhibiting substantial errors in fine-grained localization and tooth-level diagnosis. Fine-tuning on PanDent significantly improves structure-language consistency, substantially enhancing visual localization accuracy and diagnostic correctness, and bringing model outputs closer to expert dental interpretation. These results establish PanDent as a rigorous benchmark for evaluating tooth-level clinical reasoning in MLLMs and a valuable resource for clinically grounded dental AI.

</details>


### [7] [VideoCoCo: Code-as-CoT for Physically-Consistent Video Generation via an Agentic Dual-Engine System](https://arxiv.org/abs/2607.27380)
*Haodong Li,Tianfei Ren,Xiaoxiao Ma,Chunmei Qing,Zhen Fang,Sipeng He,Ziyu Guo,Haoyu Wu,Juanxi Tian,Yihang Zou,Ruichuan An,Dongzhi Jiang,Boxue Yang,Ji Xie,Xu Huang,Wenhao Yan,Jialv Zou,Zhengrong Yue,Yaxin Luo,Xiaotong Li,Yuzhu Wang,Junyan Ye,Jinjing Zhao,Zehui Chen,Lin Chen,Renye Yan,Feng Zhao,Pheng-Ann Heng*

Main category: cs.CV

TL;DR: 提出VideoCoCo，一个基于可执行Blender代码作为过程级思维链的双引擎智能体框架，用于生成物理一致的文本到视频。

Research relevance: 弱相关（1-2分）。该论文属于文本到视频生成（text-to-video generation）领域，与遥感、卫星、Earth observation、目标检测、模型轻量化均无直接关联。既不是遥感场景，也不涉及目标检测，更不涉及任何模型压缩或部署优化方法。唯一可能的间接借鉴点是其"可执行代码作为中间表示"的agentic框架设计思路，但这与在轨遥感目标检测轻量化方向几乎无关。

Reading priority: 1分。该论文属于文本到视频生成领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：文本到视频生成（text-to-video generation），物理一致性视频合成。应用场景：通用视频生成，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: VideoCoCo采用双引擎（dual-engine）智能体框架：1) 编码智能体（coding agent）根据文本提示合成可执行的Blender程序，显式描述场景及其时间演化；2) 可执行仿真引擎（executable simulation engine）运行程序生成确定性的时空草稿（spatiotemporal draft）；3) 生成式视频引擎（generative video engine）通过草稿条件编辑（draft-conditioned editing）将草稿转化为逼真视频。该框架将过程级推理（process-level reasoning）与高保真视觉实现（high-fidelity visual realization）解耦。

Lightweight method: 摘要未说明。该论文不涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且视频生成任务本身与星上/边缘部署场景无关。

Datasets and metrics: 构建了VideoCoCo-3K数据集，包含草稿-指令-目标三元组（draft-instruction-target triplets）。评价指标：PhyGenBench（从0.475提升到0.558）和VBench-2.0（从52.18提升到77.88），对比基线为OmniWeaving。

Experiments: 在PhyGenBench上，VideoCoCo将OmniWeaving基线从0.475提升至0.558；在VBench-2.0上从52.18提升至77.88，在两个基准上均取得最佳平均分数。摘要未提供消融实验或更多对比细节。

Limitations: 1) 与遥感卫星在轨目标检测及轻量化方向基本无关；2) 摘要未说明模型推理效率、部署成本等关键信息；3) 依赖Blender仿真引擎，计算开销可能较大，不适合资源受限场景；4) 未讨论实时性、能耗等边缘部署指标。

Ideas for my research: 尽管该论文与本研究方向弱相关，但"可执行代码作为过程级中间表示"的agentic框架设计思路有一定启发：1) 在遥感目标检测中，可考虑将领域知识（如卫星轨道参数、传感器成像模型）编码为可执行规则或程序，辅助检测模型的推理过程；2) 其"分解过程级推理与高保真视觉实现"的思想，可类比于在轨检测中将"物理/几何先验推理"与"深度特征提取"解耦，从而降低模型复杂度。但总体而言，该论文对本课题的直接借鉴价值有限。

Abstract: Text-to-video models have achieved remarkable visual quality, yet they still struggle to generate physically consistent dynamics because the temporal evolution of a scene must be inferred implicitly from a highly compressed text prompt. Existing chain-of-thought approaches introduce intermediate plans or visual states, but these representations are typically non-executable or temporally sparse, limiting their ability to instantiate and control the complete spatiotemporal process. To address this limitation, we introduce VideoCoCo, an agentic dual-engine framework in which executable Blender code serves as a process-level chain of thought. Given a text prompt, a coding agent synthesizes a Blender program that explicitly specifies the scene and its temporal evolution. The executable simulation engine runs the program to produce a deterministic spatiotemporal draft, which is subsequently transformed into a photorealistic video by a generative video engine through draft-conditioned editing. This decomposition separates process-level reasoning from high-fidelity visual realization. To adapt the video editor to simulated drafts, we construct VideoCoCo-3K, a curated dataset of draft-instruction-target triplets. VideoCoCo improves the OmniWeaving baseline from 0.475 to 0.558 on PhyGenBench and from 52.18 to 77.88 on VBench-2.0, achieving the best average score on both benchmarks. These results demonstrate that executable code provides an effective, controllable, and inspectable intermediate representation for physically consistent video generation.

</details>


### [8] [IGME: Efficient Chained Method Ensemble for Transferable Semantic Segmentation Attacks](https://arxiv.org/abs/2607.27465)
*Mengqi He,Jing Zhang*

Main category: cs.CV

TL;DR: 提出IGME方法，通过链式计算和路径平均方向实现仅需单一源模型的语义分割模型可迁移对抗攻击，降低计算成本。

Research relevance: 弱相关（2分）。该论文属于对抗攻击（adversarial attack）领域，针对语义分割（semantic segmentation）模型，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然对抗攻击在遥感场景中也有研究价值，但本文未涉及遥感数据、遥感目标检测或任何模型压缩/加速方法，属于泛视觉/泛机器学习方法。

Reading priority: 1分。该论文聚焦语义分割的对抗攻击效率，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不推荐优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语义分割模型的迁移对抗攻击（transferable adversarial attack on semantic segmentation）。应用场景：通用计算机视觉，非遥感场景。传感器类型：摘要未说明（实验使用Pascal VOC和Cityscapes自然图像数据集）。目标类别：摘要未说明（语义分割的通用类别）。

Model architecture: 将可迁移攻击组合建模为可微攻击组件的链式计算（chained computation over differentiable attack components），共享源模型的梯度计算。使用integrated-gradient风格的路径平均方向（path-averaged direction）作为经验性稳定化启发式。实验覆盖CNN和Transformer架构的分割模型。

Lightweight method: 本文的"轻量化"体现在攻击计算效率上——通过单一源模型替代多模型集成（model ensemble）来降低计算成本，而非模型压缩/加速（pruning, quantization, knowledge distillation, NAS等）。与遥感在轨目标检测的模型轻量化方向无关。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。本文关注的是攻击生成的计算效率（runtime），而非模型在边缘/星上部署的可行性。

Datasets and metrics: 数据集：Pascal VOC和Cityscapes。评价指标：可迁移性（transferability）与效率（efficiency）之间的权衡（transferability-efficiency trade-off），具体指标名称摘要未说明。对比对象：single-source baselines和model-ensemble attacks。

Experiments: 在CNN和Transformer架构的语义分割模型上评估IGME。结果显示：与single-source基线相比达到有竞争力的可迁移性（competitive transferability），与model-ensemble攻击相比具有更优的运行时间（favorable runtime），且仅需访问一个源模型。

Limitations: 1. 仅针对语义分割任务，未涉及目标检测（object detection）或旋转框检测。2. 实验仅在自然图像数据集（Pascal VOC, Cityscapes）上验证，未涉及遥感图像、SAR、高光谱等数据。3. 摘要未说明方法的鲁棒性边界、对防御方法的抵抗能力。4. 未涉及任何模型压缩、加速或边缘部署相关内容。

Ideas for my research: 1. 本文的"链式计算共享梯度"思想在计算效率优化上有一定启发，但方向不同——若未来研究遥感模型对抗鲁棒性，可借鉴其单源模型降低计算开销的思路。2. 对抗攻击与模型轻量化结合（如轻量模型在对抗攻击下的鲁棒性）是一个潜在交叉方向，但本文未涉及。3. 总体而言，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Semantic segmentation models are vulnerable to transferable adversarial perturbations, yet evaluating transfer attacks on dense prediction models can be computationally expensive. Existing ensemble attacks often rely on multiple surrogate models, increasing the computation cost, even harder for segmentation. This paper studies an efficient single-source alternative for transferable attacks on semantic segmentation. We formulate transferable attack composition as a chained computation over differentiable attack components, allowing the expensive source-model gradient computation to be shared. To reduce the update instability introduced by chained composition, we further use an integrated-gradient-style path-averaged direction as an empirical stabilization heuristic. Experiments on Pascal VOC and Cityscapes evaluate the resulting transferability efficiency trade-off across CNN- and transformer-based segmentation models. IGME achieves competitive transferability compared with single-source baselines and favorable runtime compared with model-ensemble attacks, while requiring access to only one source model.

</details>


### [9] [ProgFormer: Hierarchical Voxel Diffusion Transformer for Longitudinal Brain MRI Prediction](https://arxiv.org/abs/2607.27537)
*Dexuan Ding,Yuankai Qi,Luping Zhou,Jian Yang,Quan Z. Sheng,Ming-Hsuan Yang*

Main category: cs.CV

TL;DR: 提出ProgFormer，一种层级式体素空间Diffusion Transformer，用于纵向脑部MRI预测，通过粗-细双通路联合估计速度场实现端到端预测。

Research relevance: 与本研究方向基本无关（1分）。该论文聚焦于医学影像领域中的纵向脑部MRI预测（brain MRI prediction），属于医学图像分析/神经影像学方向，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务，更不涉及模型轻量化或星上/边缘部署。

Reading priority: 1分。该论文属于医学影像预测方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：纵向脑部MRI预测（longitudinal brain MRI prediction），属于医学影像生成/预测任务。应用场景：疾病进展预测（如阿尔茨海默病），非遥感场景。传感器类型：structural MRI（医学磁共振成像）。目标类别：脑部结构区域，非遥感目标。

Model architecture: ProgFormer采用层级式体素空间Diffusion Transformer架构，包含两条通路：1）coarse pathway：从3D patch tokens进行主要体积预测，建模整体脑结构和纵向上下文；2）fine pathway：利用coarse表示作为时空grounding，在单个patch内进行体素级细化。两条通路联合通过conditional flow matching直接在体素空间估计速度场（velocity field），无需单独学习的图像自编码器。预测的未来扫描通过Euler steps积分速度场从高斯噪声生成。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。虽然提到"latent-space-based methods improve computational efficiency"，但这是对已有方法的描述，ProgFormer本身并未提出轻量化方案。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。作为医学影像预测模型，其目标场景是临床诊断而非星上/边缘部署。

Datasets and metrics: 数据集：ADNI、AIBL、OASIS三个公开医学影像基准数据集。实验设置：pairwise和trajectory两种设置。评价指标：摘要未说明具体指标名称。

Experiments: 在ADNI、AIBL、OASIS三个基准上，在pairwise和trajectory两种设置下，与多个state-of-the-art方法对比，ProgFormer表现出更优性能。摘要未给出具体数值结果。

Limitations: 1）与遥感卫星在轨目标检测方向完全无关，属于医学影像领域；2）摘要未报告计算开销、推理速度、模型规模等部署指标；3）未涉及目标检测任务；4）未涉及任何轻量化技术；5）医学MRI与遥感影像在数据模态、目标特性、任务定义上差异巨大，方法难以直接迁移。

Ideas for my research: 尽管该论文与本研究方向弱相关，但存在少量可借鉴的通用方法论思想：1）coarse-to-fine（粗到细）的层级化处理思路可类比遥感大场景中先全局定位再局部细化的检测范式；2）conditional flow matching作为生成式方法，若未来研究涉及遥感数据增强或合成样本生成，可参考其端到端设计；3）"保留全局一致性同时敏感于局部细粒度变化"的思想，与遥感小目标检测中"全局上下文+局部细节"的平衡问题有概念上的相似性。但这些借鉴点较为间接，整体参考价值有限。

Abstract: Predicting future structural MRI of a brain is challenging because longitudinal changes are often subtle and confined to specific anatomical regions, while most subject-specific brain structure remains stable over time. An effective model should therefore preserve global brain structural consistency while remaining sensitive to fine-grained disease progression. Existing latent-space-based methods improve computational efficiency, but suffer from information loss during their compression-reconstruction procedure. In contrast, direct voxel-space methods avoid latent reconstruction but commonly use a unified prediction pathway to model brain structure and progression-related changes. Subtle local changes may therefore be overshadowed by the dominant stable brain structure. To address these challenges, we propose ProgFormer, a hierarchical voxel-space Diffusion Transformer for longitudinal brain MRI prediction. ProgFormer uses a coarse pathway to perform the primary volumetric prediction from 3D patch tokens. This pathway models overall brain structure and longitudinal context. The fine pathway then uses the coarse representations as spatio-temporal grounding for voxel-level refinement within individual patches. The two pathways jointly estimate a velocity field directly in voxel space through conditional flow matching, enabling end-to-end prediction without a separately learned image autoencoder. The predicted future scan is then generated from Gaussian noise by integrating the estimated velocity field over a sequence of Euler steps. Extensive experimental results on three widely used benchmarks, ADNI, AIBL, and OASIS, under both pairwise and trajectory settings demonstrate favourable performance compared against several state-of-the-art methods.

</details>


### [10] [Drawing-Recode: Annotation Grounding for Parametric CAD Code Generation from Raster 2D CAD Drawings](https://arxiv.org/abs/2607.27558)
*Mingi Kim,Yongjun Kim,Hyungki Kim*

Main category: cs.CV

TL;DR: 提出 Drawing-Recode 框架，从栅格格式的 2D CAD 图纸中恢复参数化 CAD 序列（CAD code），通过图像编码器提取几何特征、文本识别模块识别标注，并用 cross-attention 和 Annotation Grounding Loss 将标注与几何信息显式关联，最终由 LLM 生成结构化参数化 CAD 代码。

Research relevance: 与本研究方向弱相关（2分）。论文属于工业制造领域的 CAD 图纸数字化与参数化重建任务，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测、小目标检测或旋转框检测。虽然使用了图像编码器、cross-attention、LLM 等通用视觉-语言技术，但既非遥感场景，也非目标检测任务，更不涉及轻量化/部署。属于泛视觉/泛机器学习方法，仅在某些通用技术（如 cross-attention 特征对齐、LLM 生成结构化输出）上可能有间接借鉴意义。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于工业 CAD 图纸数字化领域，不涉及遥感场景、目标检测或轻量化部署，仅个别通用技术（cross-attention 特征对齐、结构化输出生成）有间接参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：从栅格 2D CAD 图纸中恢复参数化 CAD 序列（Parametric CAD sequence recovery），生成 CAD code。应用场景：工业制造、零件复制（part reproduction）、制造流程自动化（manufacturing process automation）。传感器类型：不涉及遥感传感器，输入为扫描/栅格格式的 2D CAD 图纸。目标类别：不涉及目标检测，处理对象为 CAD 几何图元与尺寸标注。

Model architecture: Drawing-Recode 框架包含：1) 图像编码器（image encoder）提取几何特征；2) 独立的文本识别模块（text recognition module）识别尺寸标注；3) cross-attention 机制将标注显式关联（grounding）到几何信息；4) Annotation Grounding Loss (AGL) 作为训练约束；5) 大语言模型（LLM）接收融合特征并生成 Structured Parametric CAD Code (SPCC) 格式的 CAD 代码。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署等模型压缩与加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该方法依赖 LLM 生成 CAD 代码，计算开销较大，与在轨/边缘部署场景不匹配。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及实验表明 Drawing-Recode 优于现有基线（existing baselines），并在模拟工业条件的扫描图纸（scanned drawings resembling industrial conditions）上保持鲁棒性（robust）。

Experiments: 摘要仅说明 Drawing-Recode 在实验中优于现有基线方法，并在扫描图纸（模拟工业条件）上表现鲁棒。未给出具体对比方法、消融实验细节或量化结果。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于工业 CAD 数字化领域；2) 摘要未说明数据集规模、评价指标、计算开销；3) 依赖 LLM 生成代码，推理成本高，不适合资源受限的星上部署；4) 未讨论模型压缩或加速策略。

Ideas for my research: 尽管方向不同，仍有少量可借鉴的通用技术思路：1) cross-attention 将多模态信息（几何+文本标注）显式对齐的做法，可类比遥感目标检测中融合多光谱/SAR 多源特征时的跨模态对齐；2) Annotation Grounding Loss 这种"显式监督特征关联"的损失设计思路，可用于遥感小目标检测中强化目标区域与上下文特征的关联；3) 用 LLM 生成结构化输出（CAD code）的思路，可类比于遥感检测中生成结构化检测结果/场景描述，但需注意 LLM 推理开销不适合在轨部署。整体借鉴价值有限。

Abstract: Recovering Parametric CAD sequences from raster-format 2D Computer-Aided Design (CAD) drawings accumulated prior to digital transformation is important for part reproduction and manufacturing process automation. However, existing studies either process only vector drawings or are limited to specific domains, and fail to explicitly connect dimensional annotations to geometric information, limiting their use of dimensional information for 3D Parametric CAD sequences recovery. We propose Drawing-Recode, a framework that generates Parametric CAD sequences as CAD code from raster 2D CAD drawings. Drawing-Recode extracts geometric features via an image encoder and recognizes annotations through a separate text recognition module, then explicitly grounds annotations to geometric information using cross-attention and our proposed Annotation Grounding Loss (AGL). The resulting features are fed into a Large Language Model (LLM) to generate CAD code in the Structured Parametric CAD Code (SPCC) format. Experiments show that Drawing-Recode outperforms existing baselines and remains robust on scanned drawings resembling industrial conditions. We expect Drawing-Recode contributes to digitizing raster 2D CAD drawings in industrial settings and to part reproduction and manufacturing automation.

</details>


### [11] [Inference-Time Agentic Decision Rules Beat Longer Evolving Search for Multi-Image Medical Reasoning](https://arxiv.org/abs/2607.27564)
*Site Li,Jianyi Hao,Xiaofeng Liu*

Main category: cs.CV

TL;DR: 本文研究多图像医学视觉问答（MedFrameQA）中的智能体决策策略，比较五种推理时agentic策略，发现简单的order-vote聚合策略优于复杂变体，且增加进化搜索预算无泛化收益。

Research relevance: 弱相关（1分）。论文属于医学视觉问答（medical VQA）领域，涉及vision-language agent和推理时决策策略，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学VQA领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多图像医学视觉问答（multi-image medical VQA）中的agentic决策。应用场景：医学影像推理。传感器类型：医学图像（具体模态摘要未说明）。目标类别：医学视觉问答中的问题-图像对。

Model architecture: 摘要未说明具体模型架构。涉及vision-language agent框架，比较五种推理时agentic策略（order-vote、order-rerank等），使用ShinkaEvolve配置进行进化搜索优化。

Lightweight method: 摘要未涉及任何轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、模型压缩或推理加速技术。仅涉及推理时决策策略（inference-time agentic strategies）的对比，与模型轻量化无关。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。该工作面向医学VQA准确率优化，未考虑任何边缘或星上部署约束。

Datasets and metrics: 数据集：MedFrameQA（内部冻结划分：1,331 evolution、665 holdout、855 final test）。评价指标：final-test accuracy（最终测试准确率），使用paired bootstrap分析验证显著性。实验设置：五次独立重复运行，进化搜索预算从50代扩展到100代。

Experiments: 主要结果：order-vote策略达到57.89±0.65% final-test准确率，优于固定基线（52.73±0.42%）和order-rerank变体（55.79±0.43%）。将进化搜索预算从50代增至100代无泛化收益：holdout性能略增但final-test准确率从57.89%降至56.02%。

Limitations: 与本研究方向基本无关。摘要未说明模型架构细节、计算开销、部署约束。核心结论（简单聚合策略优于复杂变体、增加搜索预算无收益）虽有一定方法论启示，但场景完全偏离遥感目标检测。

Ideas for my research: 尽管场景无关，但有一个方法论层面的可借鉴点：论文发现"定义正确的决策规则比扩大优化搜索预算更重要"，这提示在遥感在轨目标检测轻量化中，应优先设计合理的模型结构和压缩策略，而非盲目增加训练/搜索预算。此外，order-vote这类鲁棒聚合思想或可类比到遥感多帧/多视角目标检测的时序融合中，但需自行验证。

Abstract: Multi-image medical VQA is not merely a prompt-length problem; it is a fundamental challenge of agentic decision-making. Medical vision-language agents must aggregate evidence across ordered images, remain robust to answer-order perturbations, and avoid overfitting to noisy search-time feedback. We study MedFrameQA through a controlled comparison of five inference-time agentic strategies, optimized using the same high-budget ShinkaEvolve configuration and evaluated on a reproducible internal frozen split (1,331 evolution, 665 holdout, 855 final test). Across five independent repeated runs, the strongest method emerges as the simplest robust aggregator: the \textbf{order-vote} policy achieves $57.89 \pm 0.65\%$ final-test accuracy, significantly outperforming the fixed baseline ($52.73 \pm 0.42\%$) and the more complex, albeit brittle, order-rerank variant ($55.79 \pm 0.43\%$). Paired bootstrap analysis confirms these significant gains. Extending the evolutionary search budget from 50 to 100 generations yields no generalization benefit: while holdout performance marginally increases, final-test accuracy drops from $57.89\%$ to $56.02\%$. Our findings suggest that for multi-image medical reasoning, defining the correct agentic decision rule is substantially more impactful than expanding the optimization search budget.

</details>


### [12] [Objective-Aligned Direct Answer SFT for Robust Multi-Frame Medical VQA](https://arxiv.org/abs/2607.27566)
*Site Li,Jianyi Hao,Xiaofeng Liu*

Main category: cs.CV

TL;DR: 本文研究多帧医学视觉问答（MedFrameQA）的鲁棒适应方法，发现直接面向答案目标的decoder-only SFT（MedGemma-1.5-4B）是最强且最鲁棒的适应策略，优于controller、scaffold evolution、hard-negative mixing等复杂机制。

Research relevance: 弱相关（2分）。本文属于医学视觉问答（medical VQA）领域，与遥感、卫星、Earth observation、SAR、高光谱等完全无关；不涉及目标检测、小目标检测或旋转框检测；也不涉及剪枝、量化、蒸馏、NAS等轻量化方法。唯一可借鉴的是其"面向最终目标的对齐式微调优于复杂架构"的方法论思想，以及鲁棒性评估的实验设计（固定splits、匹配预算、重复seeds、校准），但这属于泛机器学习方法论层面的借鉴，与遥感在轨检测轻量化方向弱相关。

Reading priority: 2分。与遥感在轨目标检测及轻量化方向弱相关，仅方法论层面的"目标对齐优于架构复杂度"和鲁棒性评估设计有参考价值。若时间紧张可跳过，仅作泛方法论参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多帧医学视觉问答（multi-frame medical VQA）；应用场景：医学影像报告生成与问答；传感器类型：医学影像（非遥感传感器）；目标类别：医学视觉问题（非遥感目标）。与遥感场景完全无关。

Model architecture: 对比了多种适应架构族：controller-style inference（控制器式推理）、localization-aware reranking（定位感知重排序）、static hard-negative mixing（静态难负样本混合）、staged continuation（分阶段延续训练）、scaffold evolution、以及直接decoder-only answer SFT。基础骨干为MedGemma-1.5-4B和Qwen2.5-VL-3B。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等模型压缩或加速方法。虽然使用了4B和3B规模的模型，但未讨论部署优化。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。模型规模为4B/3B，属于大模型范畴，与星上/边缘部署的轻量化需求不匹配。

Datasets and metrics: 数据集：MedFrameQA（多帧医学VQA基准）；评价指标：held-out report accuracy（留出报告准确率）、鲁棒性指标（跨固定splits、匹配预算、重复seeds、校准后的稳定性）；实验设置：固定数据划分、匹配计算预算、多次随机种子、post-hoc calibration。

Experiments: 主要结果：直接decoder-only answer SFT（MedGemma-1.5-4B）是MedFrameQA上最强且最鲁棒的适应族，显著优于frozen baselines，且在重复seeds和匹配控制下保持稳定；post-hoc calibration可修复置信度估计而不损失准确率；核心方法可迁移到Qwen2.5-VL-3B等次级骨干。结论是"面向目标对齐的直接SFT优于复杂辅助机制"。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关；2. 不涉及任何轻量化或部署优化方法；3. 模型规模（3B-4B）远超星上可部署范围；4. 医学VQA与遥感目标检测的任务本质差异大，方法迁移需谨慎；5. 摘要未说明推理速度、模型压缩等部署关键指标。

Ideas for my research: 1. 方法论借鉴：其"面向最终目标的对齐式微调优于复杂架构"的结论，可类比到遥感在轨检测——即"直接优化检测精度/轻量化目标，而非堆叠复杂模块"的设计哲学；2. 实验设计借鉴：固定splits、匹配预算、重复seeds、校准的鲁棒性评估框架，可用于评估轻量化遥感检测模型在不同随机种子下的稳定性；3. 若未来在遥感大模型（如遥感VLM）上做轻量化蒸馏，其"直接SFT优于复杂适应"的结论可作为蒸馏目标设计的参考；4. 其post-hoc calibration思路可迁移到在轨检测的置信度校准，提升星上决策可靠性。

Abstract: Multi-frame medical VQA appears to reward increasingly complex adaptation: controller-style inference, localization-aware reranking, static hard-negative mixing, and staged continuation all appear plausible from first principles. We test a simpler competing hypothesis on MedFrameQA: methods that remain tightly aligned with the benchmark's final answer objective should be the strongest \emph{robust} adaptation family once evaluation is controlled across fixed splits, matched budgets, repeated seeds, and calibration. We compare controller-based methods, scaffold evolution, static mixed supervision, continuation-heavy variants, and direct answer-only supervised fine-tuning (SFT). The strongest robust family is direct decoder-only answer SFT on MedGemma-1.5-4B. Empirically, this family yields substantial improvements in held-out report accuracy over frozen baselines while remaining remarkably stable across repeated seeds and matched controls, ensuring our claims reflect true family-level robustness rather than an isolated hyperparameter peak. Furthermore, post-hoc calibration effectively repairs confidence estimation without compromising accuracy, and the core approach transfers consistently to secondary backbones like Qwen2.5-VL-3B. The main result is therefore not that a complex auxiliary mechanism wins, but that objective-aligned direct answer SFT is the strongest robust adaptation family we found for MedFrameQA. By establishing this strong, minimalist baseline, we hope to redirect community focus toward fundamentally robust optimization rather than architectural complexity.

</details>


### [13] [ZMIS-SAM: Segment Anything Model Enhanced with Wavelet Transform for Zooplankton Microscopy Image Instance Segmentation](https://arxiv.org/abs/2607.27585)
*Dekun Yuan,Zhongwei Li,Zheng Qiao,Jie Zhang*

Main category: cs.CV

TL;DR: 提出基于SAM和wavelet transform的浮游动物显微图像实例分割模型ZMIS-SAM，通过轻量适配器、邻域特征聚合和基于小波的多尺度多方向特征增强模块，解决SAM在浮游动物显微图像分割中分类不准、细长附肢分割不连续和边界不完整的问题。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦于浮游动物显微图像的实例分割任务，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关。虽然使用了SAM（Segment Anything Model）这一基础模型，并涉及"lightweight adapters"轻量适配器概念，但整体属于生物显微图像分析领域，与遥感卫星在轨目标检测及轻量化方向关联度很低。

Reading priority: 1分。论文聚焦浮游动物显微图像实例分割，与遥感卫星在轨目标检测和轻量化方向基本无关。虽然SAM适配器和小波特征增强有一定方法学参考价值，但整体优先级很低，不建议作为重点阅读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：实例分割（instance segmentation）。应用场景：海洋生态监测中的浮游动物显微图像分析。传感器类型：显微成像（microscopic image），非遥感传感器。目标类别：浮游动物（zooplankton），包括其细长附肢等形态结构。

Model architecture: 基于SAM（Segment Anything Model）的实例分割框架，包含三个核心模块：1) ZM-ViT：通过两个轻量适配器（lightweight adapters）增强SAM对浮游动物形态和图像强度分布的建模能力；2) Neighboring Feature Aggregation Module (NFAM)：整合通用特征和领域特定特征，改善半透明细长附肢的连续分割；3) Wavelet-based Multi-scale Multi-directional Feature Enhancement (WM2FE)：基于小波变换的多尺度多方向特征增强模块，恢复高频细节以细化边界分割。

Lightweight method: 摘要提及使用"two lightweight adapters"（两个轻量适配器）来增强SAM的能力，属于参数高效微调（parameter-efficient fine-tuning）范畴，但未详细说明具体参数量、计算量或压缩策略。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等经典轻量化方法。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。由于基于SAM这一大规模基础模型，且面向显微图像而非遥感在轨场景，其星上/边缘部署可行性未讨论。

Datasets and metrics: 数据集：浮游动物数据集（zooplankton dataset）及多个公开跨域数据集（multiple public cross-domain datasets）。评价指标：摘要未明确列出具体指标名称，仅提及"state-of-the-art instance segmentation performance"，推测可能涉及mAP、IoU等实例分割常用指标，但摘要未说明。

Experiments: 在浮游动物数据集上达到state-of-the-art实例分割性能，并在多个公开跨域数据集上展示了较强的泛化能力。摘要未提供具体数值结果、对比方法列表或消融实验细节。

Limitations: 1) 与遥感卫星在轨目标检测方向基本无关，属于海洋生物显微图像分析领域；2) 基于SAM的模型通常参数量和计算量较大，摘要未讨论轻量化部署问题；3) 摘要未提供定量实验结果（如mAP、IoU数值）和计算开销数据；4) 未说明模型在资源受限设备上的可行性。

Ideas for my research: 尽管整体方向不相关，但仍有少量可借鉴的技术思路：1) "lightweight adapters"（轻量适配器）的参数高效微调思想可迁移到遥感目标检测中，用于在预训练大模型上以少量可训练参数适配遥感域；2) 基于小波变换的多尺度多方向特征增强（WM2FE）模块，其高频细节恢复思路可用于遥感小目标检测中的边界细化，小目标在遥感图像中往往缺乏高频细节；3) NFAM中通用特征与领域特定特征融合的思路，可类比遥感检测中通用backbone与遥感域适配特征的融合。但这些借鉴点属于方法层面的间接启发，而非直接可复用的方案。

Abstract: As primary consumers in the marine food chain, zooplankton play a crucial role in maintaining marine ecological balance. However, the Segment Anything Model (SAM) exhibits limited performance in microscopic image instance segmentation due to its lack of zooplankton-specific domain knowledge. To address these challenges, we propose a novel instance segmentation model based on SAM and wavelet transform (ZMIS-SAM), effectively tackling issues such as inaccurate classification, discontinuous segmentation of slender appendages, and incomplete boundary segmentation. Our framework incorporates three core innovations: ZM-ViT enhances SAM's capability to model zooplankton morphology and image intensity distributions through two lightweight adapters, the Neighboring Feature Aggregation Module (NFAM) improves continuous segmentation of semi-transparent slender appendages by integrating general-purpose and domain-specific features, and the Wavelet-based Multi-scale Multi-directional Feature Enhancement (WM2FE) module effectively recovers high-frequency details to refine boundary segmentation completeness. Extensive experiments demonstrate that ZMIS-SAM achieves state-of-the-art instance segmentation performance on the zooplankton dataset and exhibits strong generalization capability across multiple public cross-domain datasets.

</details>


### [14] [MeshFM: 2D Features Are All You Need for 3D Shape Understanding](https://arxiv.org/abs/2607.27592)
*Jinfan Zhou,Richard Liu,Itai Lang,Rana Hanocka*

Main category: cs.CV

TL;DR: MeshFM 提出一种高效的 3D 特征提取前馈框架，通过从 2D 视觉基础模型蒸馏特征到 3D，无需 3D 标注即可训练，可直接用于 part segmentation、dense correspondence 和 mesh deformation 等下游任务。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于 3D 几何/网格特征提取，属于 3D 视觉与基础模型蒸馏领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测任务，更不涉及剪枝、量化、知识蒸馏（用于模型压缩）、NAS 等轻量化部署技术。相关性评分：1 分。

Reading priority: 1 分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于 3D 视觉特征学习领域。仅"2D 基础模型特征蒸馏到轻量网络"这一思想有间接借鉴价值，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D 特征提取（feedforward feature extraction from 3D inputs）；应用场景：3D 物体理解，包括 part segmentation（部件分割）、dense correspondence（稠密对应）、mesh deformation（网格变形）；传感器类型：3D mesh / 3D 输入（非遥感传感器）；目标类别：3D 物体（具体类别摘要未说明）。

Model architecture: MeshFM 采用两阶段训练策略：(1) 仅用 2D feature supervision 在 3D 中优化一个 feature field（特征场）；(2) 训练一个前馈网络（feedforward network）回归该特征场。推理时无需优化过程，直接前向预测 3D 特征。模型被训练为对输入物体的极端旋转具有鲁棒性。

Lightweight method: 摘要中提到的"efficient feedforward framework"和"without requiring optimization during inference"体现的是推理效率方面的设计（避免推理时迭代优化），但并未涉及传统意义上的模型轻量化方法（pruning、quantization、knowledge distillation 用于压缩、NAS、轻量网络设计等）。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、延迟、能耗等部署相关信息。虽然"feedforward + 无推理优化"的设计有利于推理效率，但论文面向的是 3D 网格处理场景，与星上/边缘部署遥感目标检测模型无直接关联。

Datasets and metrics: 摘要未明确说明具体数据集名称和评价指标。仅提及下游任务包括 part segmentation、dense correspondence、mesh deformation，实验表明 MeshFM 仅用 2D supervision 训练即可与显式 3D supervision 训练的方法性能相当（on par），且无需 task-specific fine-tuning。

Experiments: 主要实验结论：MeshFM 仅用 2D supervision 训练，性能与用 3D supervision 显式训练的方法相当，即使不做任务特定微调也能直接应用；模型对输入物体的极端旋转具有鲁棒性。摘要未提供具体对比方法名称和量化结果。

Limitations: 摘要未说明：具体数据集、评价指标、模型规模、推理速度、与轻量化部署相关的量化指标。此外，论文面向 3D 网格/几何输入，与遥感图像（2D 光学/SAR/高光谱）在数据模态上差异巨大，方法难以直接迁移到遥感目标检测场景。

Ideas for my research: 尽管整体弱相关，但有两个概念可借鉴：(1) "从 2D 基础模型蒸馏特征到目标域"的思想——类似地，可考虑将 2D 视觉基础模型（如 DINOv2、CLIP）的特征蒸馏到轻量遥感目标检测骨干网络中，以提升小目标特征表达能力；(2) "两阶段训练：先优化特征场/特征表示，再训练前馈网络回归"的策略——可类比为先用大模型/教师模型生成高质量特征标签，再训练轻量学生网络直接回归，避免推理时依赖大模型。但需注意，这些只是方法论层面的启发，论文本身并非遥感或轻量化部署研究。

Abstract: We present MeshFM, an efficient feedforward framework for extracting rich features from 3D inputs. Our method distills 2D features from visual foundation models into 3D. We train a feedforward network to directly predict 3D features without requiring optimization during inference. The approach utilizes a two-stage training strategy. First, we optimize a feature field in 3D using only 2D feature supervision. Second, we train a network to regress this feature field. The entire procedure requires no 3D annotation, instead relying on the powerful information in 2D foundation models. We demonstrate that our learned features can be immediately applied to downstream tasks, including part segmentation, dense correspondence, and mesh deformation. Extensive experiments show that MeshFM, trained solely with 2D supervision, performs on par with methods trained explicitly with 3D supervision, even without task-specific fine-tuning. Moreover, our model is trained to be robust to extreme rotations of the input objects. Project page: https://threedle.github.io/MeshFM/

</details>


### [15] [MPIE-Bench: Benchmarking Anatomically Plausible Multi-Person Interaction Editing](https://arxiv.org/abs/2607.27616)
*Jiajia Lin,Mingxuan Du,Tuowen Zhou,Benfeng Xu,Hongtao Xie*

Main category: cs.CV

TL;DR: 提出MPIE-Bench基准和MPIE-Eval评估方法，用于评测文本到图像/个性化编辑模型在多人互动场景（拥抱、携带、格斗等）中的解剖学与几何学错误，发现现有VLM评估在这些场景下严重饱和而人类能明显看出错误。

Research relevance: 弱相关（1-2分）。该论文属于生成式AI/文本到图像编辑领域，研究多人互动图像合成中的解剖学与几何评估问题，与遥感、卫星、在轨目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于生成式AI图像编辑评估领域，不建议投入阅读时间。仅当对"评估指标设计"或"VLM评估局限性"有泛化兴趣时可作背景参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：文本到图像生成与个性化编辑的评估（text-to-image editing evaluation）。应用场景：生成式AI内容质量评估，非遥感场景。传感器类型：不适用（非遥感传感器）。目标类别：多人互动场景中的人体（embrace, carry, grapple等14种互动类别）。

Model architecture: 摘要未说明具体生成模型架构。评估方法MPIE-Eval使用冻结的公开多人网格重建模型（frozen public multi-person mesh reconstruction）来重建人体网格，并基于重建结果计算两个评估轴：Anatomy（解剖学完整性）和Interaction（互动几何匹配度）。

Lightweight method: 不涉及。摘要未提及任何剪枝、量化、知识蒸馏、NAS、轻量网络设计或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作为评估基准而非部署模型。

Datasets and metrics: 数据集：MPIE-Bench，2,500个视频挖掘的编辑三元组（video-mined editing triplets），覆盖405个场景、14个互动类别、4种接触密度（C0-C3）。评价指标：mesh Anatomy（最高0.65）、mesh Interaction（最高0.72），对比VLM-as-a-judge清单（评分>0.95），以及五名人工评审的一致性验证。

Experiments: 在10个编辑模型（ten editors）上评测，mesh Anatomy最高0.65、mesh Interaction最高0.72，且没有单一模型在两个轴上同时表现优异；VLM清单对同一批图像评分超过0.95，说明VLM评估严重饱和。五名评审员的研究确认两个评估轴比零样本VLM判断更贴近人类判断，且排名在消融所有权重和阈值后保持稳定。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向完全无关，属于生成式AI评估领域。2. 摘要未说明生成模型的具体架构、训练细节或计算开销。3. 评估对象为人体互动场景，不涉及遥感目标（舰船、飞机、车辆等）。4. 未提供任何部署相关指标。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但可借鉴的间接思路包括：1) 其"评估指标饱和"问题的发现——类似地，在遥感小目标检测中，现有mAP等指标可能在小目标或密集目标场景下饱和，需要设计更细粒度的评估维度；2) 其"多轴评估"思想（Anatomy + Interaction）可类比为遥感检测中"检测精度 + 定位几何质量"的多维度评估框架；3) 其"VLM评估与人类判断的偏差"提示在遥感领域使用大模型辅助评估时需谨慎验证。但这些借鉴点较为间接，整体参考价值有限。

Abstract: Text-to-image and personalized editing models now synthesize high-fidelity single-subject images with ease. Yet placing multiple named people into shared contact actions such as embrace, carry, or grapple still exposes major failures: fused limbs, invented extremities, and interpenetrating bodies. Existing evaluations largely overlook these anatomical and geometric issues, and VLM-as-a-judge checklists often saturate on Interaction while the errors remain obvious to humans. We introduce MPIE-Bench, a 2,500-sample benchmark of video-mined editing triplets spanning 405 scenes, 14 interaction categories, and four contact densities (C0-C3). We also propose MPIE-Eval, whose two new axes score contact-time geometry from a frozen public multi-person mesh reconstruction. Anatomy asks whether every human-like mass is explained by a complete set of reconstructed bodies, and Interaction asks whether the penetration and surface distance between those bodies match the contact the instruction asked for. Across ten editors, mesh Anatomy tops out at 0.65 and mesh Interaction at 0.72 on two different models, so no single editor is strong on both, while VLM checklists rate the same images above 0.95. A five-rater study confirms that both axes track human judgement more closely than a zero-shot VLM judge, and the rankings hold under ablation of every weight and threshold.

</details>


### [16] [MedXplore: Towards Reliable and Unbiased Generalized Category Discovery in Medical Imaging](https://arxiv.org/abs/2607.27620)
*Jianwei He,Kailin Lyu,Junhao Dong,Long Xiao,Wenjie Hou,Jingze Lu,Di Wu,Lin Shu,Jie Hao*

Main category: cs.CV

TL;DR: 提出MedXplore，一个面向医学影像的广义类别发现（GCD）统一框架，通过频域自适应注意力（FAAC）和自适应余弦角间隔（ACAM）分别从感知层和决策层优化，缓解旧类偏差并提升病变敏感表示学习。

Research relevance: 弱相关（1-2分）。该论文聚焦医学影像分析（medical image analysis）中的广义类别发现（Generalized Category Discovery, GCD），完全不涉及遥感（remote sensing）、卫星（satellite）、地球观测（Earth observation）、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测（object detection）或模型轻量化（pruning/quantization/distillation/NAS等）。其核心是医学影像中的开集类别发现与旧类偏差缓解，与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。该论文面向医学影像GCD任务，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感场景或任何轻量化/部署技术。除非对频域注意力或类别发现方法有跨领域兴趣，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：广义类别发现（Generalized Category Discovery, GCD），即同时识别已知类别并发现新类别。应用场景：医学影像分析（medical image analysis），非遥感场景。传感器类型：医学影像（如内窥镜图像，Kvasir数据集）。目标类别：医学病变/病灶类别，非遥感目标（舰船/飞机/车辆等）。

Model architecture: MedXplore统一框架，包含两个核心模块：(1) 感知层：Frequency-SNR Adaptive Attention and Consistency (FAAC)，采用可学习的全频谱滤波（learnable full-spectrum filtering）和全局-局部能量对比激活（global-local energy contrast activation），为patch一致性学习提供语义锚点；(2) 决策层：Adaptive Cosine-Angular Margin (ACAM)，利用语义难度和特征置信度自适应调整角度间隔，平衡类内紧凑性和类间可分性。摘要未说明具体骨干网络结构。

Lightweight method: 摘要未涉及任何模型轻量化方法。未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。未提供参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）或实时性（real-time）等任何部署相关信息。该工作面向医学影像分析，不涉及星上或边缘部署场景。

Datasets and metrics: 数据集：多个医学影像benchmark，其中明确提及Kvasir（内窥镜图像数据集）。评价指标：All accuracy（总体准确率），以及false-old errors（旧类误判错误率）。摘要未说明其他具体数据集名称和更多指标细节。

Experiments: 在多个benchmark上，MedXplore相比最强对比方法在All accuracy上平均提升8.5%。在Kvasir数据集上，将false-old errors从14.50%降至0.80%，在严重的旧-新类别模糊场景下展现出强鲁棒性。摘要未说明具体对比方法名称和消融实验细节。

Limitations: (1) 与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学影像领域；(2) 摘要未说明模型复杂度、推理速度等部署指标；(3) 未涉及目标检测任务，而是类别发现（分类性质）任务；(4) 未说明在真实临床或大规模数据上的泛化验证；(5) 频域注意力机制的计算开销未讨论。

Ideas for my research: 尽管该论文与遥感在轨目标检测方向弱相关，但仍有少量可借鉴的通用思路：(1) FAAC中的频域注意力（frequency domain attention）和全局-局部能量对比激活思想，可迁移到遥感图像中增强小目标/弱目标的特征显著性，因为遥感图像中舰船、飞机等小目标同样存在"局部信号弱于全局背景"的问题；(2) 自适应角度间隔（ACAM）中根据特征置信度动态调整决策边界的思路，可用于遥感目标检测中处理类别不平衡或难易样本；(3) 旧类偏差（old-class bias）缓解思想对遥感增量学习/持续学习场景有参考价值。但整体而言，该论文对"轻量化"课题的直接借鉴价值有限。

Abstract: Deep learning has shown strong potential in medical image analysis, but most existing methods rely on large-scale annotations and a closed-world assumption that rarely holds in clinical practice. Although Generalized Category Discovery (GCD) has advanced rapidly on natural images, it remains underexplored in medical imaging. To address this issue, we propose MedXplore, a unified framework for reliable and unbiased medical GCD, optimizing from both perceptual and decision levels. Specifically, at the perceptual level, taking a frequency domain perspective, Frequency-SNR Adaptive Attention and Consistency (FAAC) performs learnable full-spectrum filtering and global-local energy contrast activation to not only highlight local abnormal signals relative to the global context, but also provide reliable semantic anchors for patch consistency learning. At the decision level, Adaptive Cosine-Angular Margin (ACAM) adjusts angular margins using semantic difficulty and feature confidence to balance intra-class compactness and inter-class separability. Together, the two modules improve lesion-sensitive representation learning and mitigate old-class bias. Experiments on multiple benchmarks show an average \textbf{8.5\%} gain in \textit{All} accuracy over the strongest competing methods. On Kvasir, MedXplore reduces false-old errors from 14.50\% to 0.80\%, demonstrating strong robustness under severe old-new ambiguity.

</details>


### [17] [BlindPSNR: A No-Reference Fidelity Predictor for Low-Light Image Enhancement](https://arxiv.org/abs/2607.27628)
*Mingzhe Lyu,Jinqiang Cui,Hong Zhang*

Main category: cs.CV

TL;DR: 提出 BlindPSNR，一个轻量级无参考网络，通过窗口交叉注意力融合增强图像与低光输入，用异方差回归预测 PSNR，用于低光图像增强（LLIE）的参数自动选择。

Research relevance: 弱相关（2分）。本文属于泛视觉/图像增强领域，核心是 low-light image enhancement (LLIE) 和 no-reference PSNR prediction，不涉及遥感、卫星、Earth observation、SAR、高光谱等遥感场景，也不涉及目标检测或模型压缩/剪枝/量化/蒸馏等轻量化技术。虽然网络本身是 lightweight 的，但轻量化并非本文研究重点，而是作为方法设计的一部分。因此与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅可作为泛视觉方法参考。

Reading priority: 1分。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于通用低光图像增强领域，不涉及遥感、目标检测或模型压缩技术。仅当对无参考质量评估或交叉注意力融合机制感兴趣时可作泛视觉参考，优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：低光图像增强（LLIE）中的无参考 PSNR 预测，用于自动选择增强方法的可调参数。应用场景：通用图像增强，非遥感场景。传感器类型：通用 RGB 图像。目标类别：不涉及目标检测，无目标类别。

Model architecture: BlindPSNR 是一个轻量级无参考网络，核心模块为 windowed cross-attention（窗口交叉注意力），用于融合增强后的图像与原始低光输入；通过 heteroscedastic regression（异方差回归）估计 PSNR。摘要未说明具体网络深度、宽度、层数等结构细节。

Lightweight method: 摘要仅提及网络是 lightweight（轻量级），但未说明具体轻量化手段（如剪枝、量化、知识蒸馏、NAS 等）。轻量化是网络设计目标而非本文核心贡献，具体参数量、FLOPs 等指标摘要未说明。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。仅提及网络为 lightweight，但缺乏量化指标支撑星上/边缘部署可行性判断。

Datasets and metrics: 数据集：摘要未说明具体训练/测试数据集名称。评价指标：top-1 selection accuracy（参数选择准确率）、regret（dB）、SRCC（Spearman rank correlation coefficient，0.61-0.67）。对比了 7 个 NR-IQA 基线，均取得 0% top-1 准确率；标量回归基线 top-1 为 54.4%，BlindPSNR 达 89.5%，regret 从 1.62 dB 降至 0.026 dB。

Experiments: 主要实验：在自建 benchmark 上对比 7 个 NR-IQA 基线（均 0% top-1 准确率）和标量回归基线（54.4% top-1，regret 1.62 dB）。BlindPSNR 达到 89.5% top-1 准确率，regret 降至 0.026 dB，并在未见数据集上泛化（SRCC = 0.61-0.67）。摘要未说明消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于通用图像增强领域。2. 摘要未说明具体数据集、网络结构细节、参数量与计算量。3. 泛化性仅用 SRCC 衡量，未说明跨场景（如遥感影像）的适用性。4. 依赖 paired training data 计算 ground-truth PSNR，实际在轨场景中难以获得配对数据。

Ideas for my research: 1. 无参考质量/保真度预测思路可借鉴：在轨遥感目标检测中，若需自动评估增强或预处理模块对检测性能的影响，可借鉴 no-reference 预测思路，但需将预测目标从 PSNR 改为检测指标（如 mAP）。2. windowed cross-attention 融合多输入的设计思想可用于遥感多光谱/多时相特征融合。3. 异方差回归（heteroscedastic regression）输出不确定性估计，可用于在轨推理中对检测结果置信度建模。整体借鉴价值有限，需自行迁移到遥感目标检测场景。

Abstract: Low-light image enhancement (LLIE) methods involve tunable parameters that are typically fixed, often leading to performance degradation when applied across scenes. Manually selecting the best configuration, however, can be time-consuming and not always practical. Peak signal-to-noise ratio (PSNR) is the natural fidelity criterion for automating parameter selection, yet it requires a ground-truth reference that is typically unavailable. To our knowledge, no learning-based method addresses no-reference PSNR prediction for low-light image enhancement; the natural surrogate, no-reference image quality assessment (NR-IQA), targets perceptual quality rather than signal fidelity, and all seven baselines we test achieve 0% top-1 selection accuracy on our benchmark. With paired training data, the ground-truth PSNR is analytically computable, providing exact supervision without a separate teacher network. Building on this, we propose BlindPSNR, a lightweight no-reference network that fuses the enhanced image with the degraded low-light input via windowed cross-attention and estimates PSNR through heteroscedastic regression. While a scalar-regression baseline achieves top-1 accuracy of 54.4%, BlindPSNR raises this to 89.5% with regret dropping from 1.62 dB to 0.026 dB, and generalizes to unseen datasets (SRCC = 0.61-0.67).

</details>


### [18] [4DHumanDiff: Direct Text-to-4DGS Generation for Consistent 360-Degree Dynamic Humans](https://arxiv.org/abs/2607.27634)
*Renlong Wu,Haoran Chen,Yuxiang Wei,Xiaowei Jin,Wangmeng Zuo,Hui Li*

Main category: cs.CV

TL;DR: 4DHumanDiff 是一个从文本提示直接生成360度动态人体资产的扩散框架，基于4D Gaussian Splatting (4DGS) 表示，避免视频预生成和逐场景重建，可在1分钟内生成一致的多视角动态人体。

Research relevance: 弱相关（1-2分）。该论文属于3D生成/计算机图形学领域，核心任务是 text-to-4D 动态人体资产生成，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。也不涉及目标检测、小目标检测、旋转框检测、舰船/飞机/车辆检测等遥感目标检测任务。虽然论文涉及 diffusion 模型和推理加速（inference time 减少10x），但这是生成任务而非检测任务，且没有涉及任何模型压缩/轻量化技术（pruning, quantization, knowledge distillation, NAS 等均未提及）。

Reading priority: 1分。该论文属于3D生成/计算机图形学领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感、目标检测、模型压缩或边缘部署等任何相关技术，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：text-to-4D 动态人体资产生成（generative task）。应用场景：3D内容生成、数字人、虚拟现实/增强现实。传感器类型：不适用（非遥感传感器）。目标类别：动态人体（human assets）。与遥感目标检测完全无关。

Model architecture: 基于 diffusion 框架，直接生成由 4D Gaussian Splatting (4DGS) 表示的动态人体。骨干网络为 3D U-Net，并带有 temporal attention 模块用于运动感知生成。端到端建模结构化4D表示空间，避免视频预生成和逐场景重建。

Lightweight method: 摘要未说明。论文没有涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。仅提到 inference time 比已有方法减少10x以上，但这是通过端到端生成架构（避免视频预生成和逐场景重建）实现的，并非模型轻量化技术。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、显存、能耗等部署相关信息。虽然提到推理时间减少10x以上且可在1分钟内生成，但这是针对3D生成任务的推理效率，与星上/边缘部署（FPGA/NPU/TensorRT）完全无关。

Datasets and metrics: 构建了一个大规模 text-to-4DGS 数据集，包含60,000对高质量文本-4DGS数据对。评价指标包括：时间一致性（temporal consistency）、多视角一致性（multi-view consistency）、渲染质量、推理时间。具体指标名称摘要未说明。

Experiments: 实验表明 4DHumanDiff 能在1分钟内生成一致的360度动态人体，相比已有方法在时间一致性和多视角一致性上表现更好，推理时间减少10x以上。引入了2D正则化（2D regularization）和无训练4D插值（training-free 4D interpolation）来提升渲染质量和运动平滑度。具体对比方法和消融实验细节摘要未说明。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向完全无关，属于3D生成领域。2. 摘要未说明模型参数量、FLOPs、部署硬件要求等关键信息。3. 未涉及任何模型压缩或轻量化技术。4. 生成任务与检测任务在方法论上差异巨大，借鉴价值有限。

Ideas for my research: 借鉴价值非常有限。可能的间接启发包括：1) 论文中 diffusion 框架的端到端设计思路（避免中间步骤）在方法论上可类比于检测任务中"端到端"设计（如 DETR 系列），但这是泛化层面的启发；2) 推理效率优化（减少中间冗余步骤）的思路可迁移到在轨检测的实时性优化中，但具体技术不可复用。总体而言，不建议作为主要参考论文。

Abstract: Generating high-quality 360-degree dynamic human assets from text prompts is challenging. Existing methods usually synthesize monocular or multi-view videos first and then fit a 4D representation, which is expensive and often causes incomplete geometry or view-inconsistent renderings. We present 4DHumanDiff, a diffusion framework that directly generates dynamic humans represented by 4D Gaussian Splatting (4DGS) from text prompts. By modeling the structured 4D representation space end-to-end, 4DHumanDiff avoids video pre-generation and per-scene reconstruction, making it better suited for view-consistent and temporally coherent asset generation. The model uses a 3D U-Net backbone with temporal attention for motion-aware generation. We further construct a large-scale text-to-4DGS dataset with 60,000 high-quality pairs, and introduce 2D regularization and training-free 4D interpolation to improve rendering quality and motion smoothness. Experiments show that 4DHumanDiff generates consistent 360-degree dynamic humans within one minute, achieves better temporal and multi-view consistency, and reduces inference time by more than 10x.

</details>


### [19] [MMOOC: A Comprehensive Benchmark for Out-of-Context Evaluation in Multimodal Large Language Models](https://arxiv.org/abs/2607.27637)
*Wenjie Zhu,Yabin Zhang,Wenjun Zeng,Lei Zhang*

Main category: cs.CV

TL;DR: 提出MMOOC基准数据集，用于评估多模态大语言模型（MLLM）在上下文偏移场景下的拒答能力与稳健回答能力，包含41K+图像-问题对。

Research relevance: 弱相关（2分）。论文属于多模态大语言模型（MLLM）的视觉-语言理解与稳健性评估方向，与遥感、卫星、在轨目标检测、模型轻量化均无直接关联。虽然MLLM在遥感场景理解中可能有潜在应用，但本论文未涉及任何遥感数据、遥感目标检测或轻量化部署内容，属于泛视觉-语言领域的通用方法。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于MLLM稳健性评估的通用视觉-语言研究，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：MLLM的拒答能力（refusal）与稳健回答能力（robust answering）评估；应用场景：通用视觉-语言理解中的上下文偏移（context shift）场景；传感器类型：不适用（通用自然图像）；目标类别：不适用（通用图像-问题对）。

Model architecture: 摘要未说明具体模型架构。论文聚焦于评估基准而非提出新模型，评估对象为多种现有MLLM（具体模型名称摘要未说明）。

Lightweight method: 摘要未涉及任何轻量化方法（无剪枝、量化、蒸馏、NAS等）。仅提及"post-training can improve robustness"（后训练可提升稳健性），但这属于训练策略而非模型压缩/加速方法。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。MLLM本身通常参数量巨大，与星上/边缘部署的轻量化需求相悖。

Datasets and metrics: 数据集：MMOOC基准，包含41K+图像-问题对，涵盖可回答的Shifted IC（非主体上下文偏移）和不可回答的OOC（主体级上下文偏移）两类问题，三种问题格式、八种偏移类型、六种视觉场景；数据质量通过MLLM过滤和人工验证保证。评价指标：Accuracy（准确率）、Refusal Rate（拒答率），以及LLM-as-a-Judge指标评估推理正确性。

Experiments: 在多种MLLM上进行实验，结果显示当前模型在偏移上下文下难以平衡可回答性与拒答能力。进一步分析了关键失败模式，并表明post-training可以提升稳健性。具体对比模型名称和量化结果摘要未说明。

Limitations: 1. 与遥感/卫星/在轨目标检测/轻量化完全无关，属于通用视觉-语言领域；2. 未涉及任何目标检测任务；3. 未涉及任何模型压缩或加速方法；4. 未提供部署相关指标；5. 摘要未说明具体MLLM模型名称和详细实验结果。

Ideas for my research: 借鉴意义有限，但可考虑以下间接启发：1. MLLM在遥感场景理解中可能面临类似的上下文偏移问题（如不同传感器、不同分辨率下的遥感图像），其"拒答+稳健回答"的评估框架可迁移到遥感MLLM的稳健性评估；2. LLM-as-a-Judge的自动评估思路可用于遥感目标检测结果的自动质量评估；3. 若未来研究方向涉及遥感视觉-语言模型（如遥感图像描述、遥感问答），此基准的构建方法论（MLLM过滤+人工验证）有参考价值。但整体上对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值很低。

Abstract: Multimodal Large Language Models (MLLMs) have achieved strong performance on a wide range of vision-language tasks, but often fail under imperfect or shifted contexts. A reliable MLLM should refuse truly out-of-context (OOC) questions with subject-level context shifts while still answering shifted in-context (Shifted IC) questions with non-subject context shifts. Existing benchmarks mainly target OOC or visually unanswerable questions, but overlook answerable Shifted IC cases and cover limited OOC shifts. To fill this gap, we present MMOOC, a large-scale benchmark for evaluating refusal and robust answering abilities of MLLMs. MMOOC contains over 41K image-question pairs, including answerable Shifted IC cases and unanswerable OOC cases, spanning three question formats, eight shift types and six visual scenarios, with data quality ensured through MLLM-based filtering and human verification. We evaluate model responses using Accuracy and Refusal Rate, and further introduce an LLM-as-a-Judge metric to assess the correctness of model reasoning. Experiments on diverse MLLMs show that current models still struggle to balance answer-ability and refusal under shifted contexts. We further analyze key failure patterns and show that post-training can improve robustness. MMOOC will be made publicly available.

</details>


### [20] [Learning Color Grading, No Photo Sharing: Federated Aesthetic Preference Learning for Personalized Image Enhancement](https://arxiv.org/abs/2607.27659)
*Chuanzhi Xu,Ziyuan Tao,Jean Julien KNell,Yanrong Chen,Haolan Guo,Xuanhua Yin,Adnan Mahmood,Weidong Cai*

Main category: cs.CV

TL;DR: FedPAIE 是一个联邦个性化美学图像增强框架，通过轻量级双线索美学评分器校准为个性化评分器，引导轻量级 CLUT 增强器在资源受限设备上进行用户自适应的颜色分级，无需集中收集原始照片或评分。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，聚焦于个性化图像美学增强（color grading），并非遥感/卫星场景，也不涉及目标检测。虽然涉及轻量化（lightweight scorer、轻量CLUT增强器、参数仅0.293M）和资源受限设备部署，但轻量化对象是图像增强网络而非目标检测模型，且场景与遥感在轨目标检测无直接关联。其联邦学习、轻量化设计思路对边缘部署有一定借鉴意义，但整体相关性较弱。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，不涉及遥感场景和目标检测任务。但其联邦学习框架、轻量级模型参数控制策略和"辅助网络引导主网络"的正则化范式对边缘/在轨部署的分布式训练和轻量化设计有一定间接借鉴价值，可作为泛读参考而非精读对象。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：个性化美学图像增强（personalized aesthetic image enhancement），具体为颜色分级（color grading）。应用场景：资源受限的用户设备（resource-constrained user devices），非遥感/卫星场景。传感器类型：普通用户照片（unpaired local photographs）。目标类别：不涉及目标检测，处理对象为自然图像的美学增强。

Model architecture: FedPAIE 包含两个核心模块：(1) 轻量级双线索美学评分器（lightweight dual-cue aesthetic scorer），训练时更新最多0.787M参数；(2) 轻量级 CLUT 增强器（lightweight CLUT enhancer），基于颜色查找表（Color Look-Up Table）实现，适配阶段更新0.265M参数。评分器被校准为个性化评分器（personalized scorer）后冻结，用于引导增强器的正则化适配。采用保真度约束（fidelity constraints）和 excess-gap 惩罚项来正则化评分器引导的适配过程，防止代理分数过优化（proxy-score over-optimization）。

Lightweight method: 轻量化体现在模型参数规模控制上：评分器学习最多更新0.787M参数，增强器适配更新0.265M参数，推理时仅保留0.293M参数的个性化增强器。采用 CLUT（颜色查找表）作为增强器核心，天然轻量高效。训练全流程保持轻量化（training remains lightweight throughout the pipeline）。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或 NAS 等经典模型压缩方法。

Onboard deployability: 面向资源受限用户设备（resource-constrained user devices）设计，推理时仅保留0.293M参数的个性化增强器，参数规模极小，具备边缘部署潜力。但摘要未说明具体 FLOPs、延迟、显存、能耗等量化部署指标，也未涉及 FPGA/NPU/TensorRT 等硬件加速方案。由于场景是用户设备而非卫星在轨，未涉及星上部署相关约束（如辐射环境、功耗预算、实时推理要求）。

Datasets and metrics: 数据集：MIT-Adobe FiveK 和 Flickr-AES。评价指标：摘要未明确列出具体指标名称，仅提及"effective open-world personalization"和"favorable balance between user preference and image fidelity"，暗示同时评估用户偏好匹配度和图像保真度。实验设置细节（如联邦客户端数量、非独立同分布设置等）摘要未说明。

Experiments: 在 MIT-Adobe FiveK 和 Flickr-AES 两个数据集上验证了开放世界个性化（open-world personalization）效果，并展示了用户偏好与图像保真度之间的良好平衡。摘要未提供与基线方法的定量对比数据、消融实验细节或具体指标数值。

Limitations: (1) 与遥感/卫星/目标检测完全无关，属于图像美学增强领域；(2) 摘要未提供 FLOPs、延迟、能耗等部署量化指标；(3) 未涉及剪枝、量化、蒸馏等主流模型压缩技术；(4) 未说明联邦学习在非独立同分布数据下的收敛性分析；(5) 未提供与现有图像增强方法的定量对比结果；(6) 个性化评分器依赖用户反馈，在遥感场景中无直接对应需求。

Ideas for my research: (1) 联邦学习（Federated Learning）框架可借鉴：在遥感在轨场景中，多颗卫星/多个地面站可协同训练目标检测模型而无需共享原始遥感影像，保护数据隐私的同时实现分布式学习；(2) 轻量级评分器引导增强器适配的思路可迁移为"轻量级辅助网络引导主检测网络适配"的蒸馏/正则化范式；(3) CLUT 的轻量高效设计思想可启发遥感图像预处理/增强模块的轻量化实现；(4) 参数规模控制策略（评分器0.787M、增强器0.265M、推理0.293M）可作为在轨模型参数量预算的参考量级；(5) 保真度约束与 excess-gap 惩罚防止代理分数过优化的思路，可类比为在轨检测中防止代理损失/蒸馏损失过拟合的约束设计。

Abstract: Personalized image enhancement should reflect individual aesthetic taste, yet learning such preferences commonly depends on private photos and ratings that are unsuitable for centralized collection. The task must infer preference from sparse, heterogeneous feedback and translate it into natural-looking color transformations on resource-constrained user devices. We introduce FedPAIE, a federated personalized aesthetic image enhancement framework for user-adaptive color grading without centralizing raw photos or ratings. FedPAIE trains a lightweight dual-cue aesthetic scorer, calibrates it into a personalized scorer on a small local support set, and freezes it to guide regularized adaptation of a lightweight CLUT enhancer from unpaired local photographs. Fidelity constraints and an excess-gap penalty regularize scorer-guided adaptation to limit proxy-score over-optimization while preserving content and natural appearance. Training remains lightweight throughout the pipeline: scorer learning updates at most 0.787M parameters, enhancer adaptation updates 0.265M, and inference retains only a 0.293M-parameter personalized enhancer. Experiments on MIT-Adobe FiveK and Flickr-AES demonstrate effective open-world personalization and a favorable balance between user preference and image fidelity. FedPAIE thus connects decentralized preference learning with efficient personalized image transformation without requiring paired user retouches.

</details>


### [21] [Witness Evidence Portfolios: Single-Prefill Risk Detection for Closed Multimodal Answers](https://arxiv.org/abs/2607.27667)
*Fexiang Liu,Shiye Wang,Qiang Qiu,Zheng Wang*

Main category: cs.CV

TL;DR: 提出 Witness Evidence Portfolios (WEP)，一种用于多模态大语言模型 (MLLM) 推理时风险检测的方法，通过逐层分析视觉证据的贡献来判定闭式视觉答案是否可信、需复核或路由到更强系统。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦多模态大语言模型 (MLLM) 的推理时风险检测 (inference-time risk detection) 与置信度校准，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱场景，也不涉及目标检测任务，更不涉及任何模型轻量化或部署优化。仅"部署可靠性"这一概念与在轨部署有一定概念上的弱关联，但方法本身与遥感在轨目标检测及轻量化无直接关系。

Reading priority: 1分。论文聚焦 MLLM 推理时风险检测，与遥感卫星在轨目标检测及模型轻量化方向基本无关，既非遥感场景，也不涉及目标检测或轻量化方法。仅在置信度评估与路由决策的概念层面有极弱的参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态大语言模型 (MLLM) 的推理时风险检测 / 置信度评估。应用场景：通用视觉问答 (visual question answering) 的可靠性判断，决定答案应被信任、复核或路由到更强系统。传感器类型：不涉及遥感传感器，为通用视觉输入。目标类别：不涉及具体目标检测类别，针对闭式视觉答案 (closed visual answers) 的置信度判断。

Model architecture: WEP 方法基于白盒 (white-box) 预填充 (prefill) 路径，逐层 (layer by layer) 估计视觉贡献对预测候选的支持或反驳。通过两条可解释路由族 (route families) 汇总：question-related evidence provenance（问题相关证据来源）和 signed evidence concentration（带符号证据集中度）。采用嵌套分组验证 (nested grouped validation) 选择更可靠的路由族和稀疏 top-k 路由组合，并与候选置信度融合。无需图像扰动、解码改变、反向传播或外部验证器。

Lightweight method: 摘要未说明。论文不涉及剪枝 (pruning)、量化 (quantization)、知识蒸馏 (knowledge distillation)、NAS、轻量网络设计或任何模型压缩/加速方法。其"无需图像扰动、解码改变、反向传播或外部验证器"的描述属于推理效率层面的设计考量，但并非模型轻量化方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关指标。虽然方法避免了反向传播和外部验证器，降低了推理开销，但面向的是通用 MLLM 推理场景，未涉及星上 (onboard) 或边缘 (edge) 部署可行性。MLLM 本身参数量巨大，与在轨遥感目标检测的轻量化部署需求相去甚远。

Datasets and metrics: 数据集：四个二值答案基准 (four binary-answer benchmarks)，具体名称摘要未说明。模型：三个 MLLM (three MLLMs)，具体模型名称摘要未说明。评价指标：mean error AP（平均错误平均精度），提升 0.134；所有 12 个 model-dataset 组合增益为正；10 对组合的 image-cluster bootstrap 区间严格为正。使用带标签的校准切片 (labeled calibration slice)。

Experiments: 在三个 MLLM 和四个二值答案基准上，WEP 将 mean error AP 提升 0.134。所有 12 个 model-dataset 组合均获得正向增益，10 对组合的 image-cluster bootstrap 区间严格为正。摘要未提供与基线方法的详细对比表或消融实验细节。

Limitations: 1. 仅适用于白盒 (white-box) 闭式答案系统，对黑盒模型或开放式生成不适用；2. 需要带标签的校准切片 (labeled calibration slice)，实际部署中获取标注数据可能受限；3. 仅针对二值答案基准，未扩展到多类别或开放域场景；4. 与遥感、目标检测、轻量化完全无关，无法直接迁移到在轨遥感目标检测任务；5. 摘要未说明计算开销、推理延迟等部署指标。

Ideas for my research: 1. 概念借鉴：WEP 的"逐层证据归因"思想可类比到遥感目标检测模型中的特征图逐层分析，用于判断检测置信度的可靠性，但需大幅改造以适配 CNN/Transformer 检测头；2. 置信度校准思路：在遥感小目标检测中，可将"证据来源与集中度"思想用于区分真实目标与虚警 (false alarm)，提升检测结果的可信度判断；3. 路由思想：WEP 的"信任/复核/路由"决策框架可类比到星上资源受限场景——简单场景用轻量模型直接推理，复杂场景路由到更强模型，但论文本身未涉及此方向。总体而言，借鉴价值有限，仅概念层面。

Abstract: Reliable deployment of multimodal large language models (MLLMs) requires deciding whether a confident visual answer should be trusted, reviewed, or routed to a stronger system. Confidence scores capture candidate margins, but not where the estimated signed visual readouts associated with those margins come from or how they are distributed. We study inference-time risk detection for closed visual answers using the same white-box prefill path that produces the answer. Witness Evidence Portfolios (WEP) first estimates, layer by layer, which visual contributions support or contradict the predicted candidate. It summarizes these contributions through two interpretable route families: question-related evidence provenance and signed evidence concentration. Nested grouped validation chooses the more reliable family and a sparse top-k route portfolio, which is fused with candidate confidence. WEP needs no image perturbation, decoding change, backward pass, or external verifier. Across three MLLMs and four binary-answer benchmarks, WEP improves mean error AP by 0.134. All 12 model--dataset gains are positive, and image-cluster bootstrap intervals are strictly positive on 10 pairs. WEP targets white-box closed-answer systems and uses a labeled calibration slice.

</details>


### [22] [JigShape: Evaluating Visual-Geometric Reasoning in VLMs through Jigsaw Puzzles](https://arxiv.org/abs/2607.27670)
*Shawn Li,Wei Yang,Jike Zhong,Jiate Li,Jiawei Yang,You Qin,Ryan Rossi,Franck Dernoncourt,Roger Zimmermann,Yue Wang,Zhengzhong Tu,Vicente Ordonez,Mohit Bansal,Yue Zhao*

Main category: cs.CV

TL;DR: 提出一个基于榫卯互锁拼图块的拼图求解基准（Jigsaw puzzle benchmark），用于评估视觉语言模型（VLM）在几何推理与视觉内容联合推理上的能力，发现模型在拼图规模增大时出现"scaling cliff"性能崩塌。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，研究的是拼图求解（jigsaw puzzle solving）中的几何推理问题，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关系。不涉及目标检测，也不涉及轻量化/部署。仅作为泛视觉推理方法，对遥感场景理解中的几何约束建模可能有间接借鉴意义。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，不涉及遥感场景、目标检测或任何轻量化/部署技术。仅"scaling cliff"现象和几何约束推理思想可能有极间接的借鉴意义，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：拼图求解（jigsaw puzzle solving），属于视觉几何推理任务。应用场景：视觉语言模型（VLM）的几何推理能力评测。传感器类型：不适用（非遥感传感器）。目标类别：拼图块（puzzle pieces），非遥感目标。摘要未说明任何遥感相关场景。

Model architecture: 摘要未详细说明具体模型架构。涉及的是视觉语言模型（VLM），包括GPT-5.5等前沿模型，以及经过监督微调（supervised fine-tuning）的模型。摘要未说明具体网络结构、backbone或关键模块设计。

Lightweight method: 摘要未说明。不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计、实时推理或边缘部署等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。该论文关注的是模型推理能力而非部署效率。

Datasets and metrics: 数据集：提出的新基准（\ours{}），包含95K个实例，四种网格密度（4×4到16×16），使用榫卯互锁（tab-and-blank interlocking）拼图块设计。评价指标：拼图求解准确率（accuracy），以随机基线（random baseline）为对照。实验设置：零样本（zero-shot）VLM评测、监督微调（supervised fine-tuning）评测。

Experiments: 主要实验结果：1）零样本VLM缺乏几何推理能力——五个前沿模型中仅GPT-5.5在4×4拼图上超过随机基线，其余均处于随机水平；2）监督微调在4×4上可达>97%准确率；3）所有模型在更大网格上性能崩塌——GPT-5.5从8×8的70%降至接近随机，微调模型在12×12上低于5%。作者将这一现象称为"scaling cliff"（缩放悬崖）。

Limitations: 1）与遥感、卫星、目标检测、轻量化部署完全无关，属于纯视觉推理研究；2）摘要未说明模型参数量、计算开销等部署相关信息；3）摘要未说明该基准在真实遥感场景中的适用性；4）"scaling cliff"现象的分析仅针对拼图任务，未推广到其他视觉任务；5）摘要未说明与遥感场景理解、小目标检测等任务的关联。

Ideas for my research: 1）"scaling cliff"现象提示：在遥感大场景（如大幅面卫星影像）中，模型在输入规模增大时可能面临类似的性能崩塌，这为在轨目标检测的尺度鲁棒性研究提供了思路——需要验证轻量化模型在大幅面遥感影像上的性能保持能力；2）几何约束与视觉内容联合推理的思想，可借鉴到遥感目标检测中的空间上下文建模（如舰船排列、机场跑道等几何先验）；3）该论文的基准设计方法论（通过约束设计消除ground truth歧义）可借鉴到遥感小目标检测数据集的标注设计，减少纹理重复区域（如海洋、沙漠）的标注歧义；4）但整体借鉴价值有限，因为不涉及任何轻量化或部署技术。

Abstract: Jigsaw puzzle solving requires jointly reasoning about visual content and geometric constraints, yet existing benchmarks use rectangular cuts that create ambiguous ground truth in texture-repeated regions. We introduce \textit{\ours{}}, a benchmark with tab-and-blank interlocking pieces where geometric constraints provide strong local compatibility requirements that, combined with visual content, yield unambiguous ground truth. Across 95K instances at four grid densities (4$\times$4 to 16$\times$16), we find that \textbf{zero-shot VLMs largely lack geometric reasoning}: only one of five frontier models (GPT-5.5) exceeds random baseline on 4$\times$4 puzzles, while all others perform at chance level. While supervised fine-tuning achieves $>$97\% on 4$\times$4, \textbf{all models collapse on larger grids}: GPT-5.5 drops from 70\% to near-random on 8$\times$8, and even fine-tuned models fall below 5\% on 12$\times$12. This ``scaling cliff'' suggests current architectures cannot maintain consistent constraint satisfaction as the number of pieces increases. \ours{} establishes scalable geometric reasoning as an open challenge for vision-language models.

</details>


### [23] [RefineSVG: Visual Feedback-Driven Reinforcement Learning for Image-to-SVG Generation](https://arxiv.org/abs/2607.27699)
*Shaobo Liu,Feiqiao Mao,Shuaishuai Zhou,Yan Zhan,Weiqi Tan,Zhiqiong Lu,Zhengping Liang*

Main category: cs.CV

TL;DR: 提出 RefineSVG，一个单步闭环视觉反馈框架，让多模态大语言模型通过渲染-观察-纠正的自我修正机制生成高保真 SVG 矢量图。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于图像到 SVG 矢量图生成，属于多模态大语言模型（MLLM）与计算机图形学交叉领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务，更不涉及模型轻量化或边缘/星上部署。虽然论文涉及 token 压缩（压缩 52%）和代码效率，但这是针对 SVG 生成任务的 token 序列压缩，与模型压缩/轻量化（pruning, quantization, knowledge distillation）完全不同。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于 MLLM + SVG 生成领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像到 SVG 矢量图生成（image-to-SVG generation）。应用场景：通用图像矢量化，非遥感场景。传感器类型：不适用（输入为自然图像）。目标类别：不适用（无目标检测任务）。

Model architecture: 基于多模态大语言模型（MLLM）的框架，包含：1) 初始 SVG 生成 pass；2) 外部渲染引擎（rendering engine）用于渲染输出；3) Diff-Map 多维视觉残差图作为 ReAct 风格的纠正信号；4) SVG 语义词汇表（semantic vocabulary）压缩 token 序列；5) 渐进式训练管线（SFT + rejection-sampling 冷启动数据构建 + 端到端 agentic RL）。

Lightweight method: 摘要未说明传统模型压缩方法（pruning, quantization, knowledge distillation, NAS 均未涉及）。唯一与"轻量"相关的是 SVG-oriented semantic vocabulary 将 token 序列压缩超过 52%，但这属于输出 token 层面的压缩，而非模型参数/计算量的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。且该框架依赖外部渲染引擎进行渲染-观察-纠正的闭环交互，需要多次前向推理，计算开销大，不适合星上/边缘实时部署场景。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括：重建保真度（reconstruction fidelity）、结构精度（structural accuracy）、代码效率（code efficiency）。

Experiments: 实验表明 RefineSVG 在重建保真度、结构精度和代码效率上持续优于现有基线方法。具体对比方法、消融实验细节摘要未说明。

Limitations: 1) 与遥感目标检测和模型轻量化完全无关；2) 依赖外部渲染引擎，闭环推理需要多次前向传播，计算开销大；3) 摘要未提供模型规模、推理速度等关键部署信息；4) 仅针对 SVG 生成任务，泛化到其他视觉任务（如检测）的可行性未说明。

Ideas for my research: 尽管整体弱相关，但有两个概念可借鉴：1) "闭环视觉反馈/自我纠正"思想——在遥感在轨目标检测中，可借鉴类似的自校正机制，例如检测结果通过后处理验证后反馈给模型进行二次修正，提升小目标检测的鲁棒性；2) "语义词汇表压缩 token 序列"的思路——在遥感检测模型的输出端或特征表示层面，可通过语义化的紧凑编码减少冗余，间接降低计算和存储开销。但整体借鉴价值有限。

Abstract: We propose RefineSVG, a single-step closed-loop visual feedback framework that enables multimodal large language models (MLLMs) to perform high-fidelity image-to-SVG generation through self-correction. Existing MLLM-based approaches rely on single-pass open-loop inference, where the model receives visual input only once and must generate thousands of SVG code tokens without intermediate verification. This paradigm inevitably leads to geometric drift, error accumulation, and visual hallucination on complex images. RefineSVG overcomes this limitation by invoking an external rendering engine after an initial SVG generation pass to compare the rendered output against the target image. The comparison yields a multi-dimensional visual residual map (Diff-Map) that is fed back to the model as a ReAct-style correction signal, driving a targeted correction step. To support this render-observe-correct interaction, we further introduce an SVG-oriented semantic vocabulary that compresses token sequences by over 52%. A progressive training pipeline spanning supervised fine-tuning, rejection-sampling cold-start data construction, and end-to-end agentic reinforcement learning aligns the model with closed-loop visual correction. Extensive experiments show that RefineSVG consistently outperforms existing baselines in reconstruction fidelity, structural accuracy, and code efficiency.Code is available at https://github.com/liuxiaobo66/RefineSVG.

</details>


### [24] [Calibrate Before Reason: Robust Visual Token Reduction against Semantic Drift in VLMs](https://arxiv.org/abs/2607.27700)
*Jiasheng Li,Zhong Ji,Yan Zhang,Huihui Li*

Main category: cs.CV

TL;DR: 提出CaRe，一种无需训练的视觉token缩减框架，通过"先校准再推理"原则在压缩VLM视觉token序列时保持语义保真度，剪掉94.4%的token仍保留96.4%性能，推理加速2.30倍。

Research relevance: 弱相关（2分）。论文聚焦Large Vision-Language Models (VLMs)的视觉token缩减，属于泛视觉/泛机器学习方法，不涉及遥感、卫星、Earth observation、SAR、高光谱等遥感场景，也不涉及目标检测任务。虽然token缩减本质上是一种推理加速/轻量化手段，但其对象是VLM而非遥感目标检测模型，且方法为training-free的token剪枝/校准，与遥感在轨目标检测的轻量化（剪枝、量化、蒸馏等）技术路线不同。属于泛视觉方法，弱相关。

Reading priority: 2分。该论文是泛VLM领域的token缩减方法，与遥感卫星在轨目标检测及轻量化方向弱相关。虽然"先校准再推理"和confidence-gated校准的思想有一定迁移潜力，但方法本身不涉及遥感场景、目标检测任务或经典模型压缩技术（剪枝/量化/蒸馏/NAS），且未提供部署相关指标。可作为轻量化后性能补偿的灵感参考，但非核心文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视觉token缩减（visual token reduction），服务于Large Vision-Language Models (VLMs)的推理加速。应用场景：通用VLM推理，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（非特定目标检测任务）。

Model architecture: CaRe框架包含两个互补模块：1) Perturbation-Robust Calibration Anchoring——在多方向扰动下识别具有稳定模型侧影响的校准锚点（calibration anchors）；2) Confidence-Gated Token Calibration——从未被选中的token中提取可靠校准信号并注入锚点。整体为training-free的token缩减后处理框架，不修改VLM主干结构。

Lightweight method: 核心轻量化手段是visual token reduction（视觉token缩减），即通过剪枝（pruning）或压缩冗余token来缩短视觉token序列长度，从而降低推理开销。CaRe的独特之处在于：在token缩减之后、推理之前引入"Calibrate Before Reason"校准步骤，通过perturbation-robust anchoring和confidence-gated calibration来缓解token缩减导致的semantic drift。该方法为training-free，无需重新训练模型。但注意：这是针对VLM的token级压缩，不是针对遥感目标检测模型的剪枝/量化/蒸馏/NAS等经典轻量化方法。

Onboard deployability: 摘要提供了端到端推理加速数据：剪掉94.4%的visual tokens后，端到端推理速度提升最多2.30倍（相对未剪枝的vanilla模型）。但摘要未说明参数量、FLOPs、显存占用、能耗、延迟的具体数值，也未提及FPGA/NPU/TensorRT等边缘或星上部署硬件适配。由于该方法面向通用VLM而非遥感目标检测模型，且未涉及星上部署约束，其onboard deployability信息有限。

Datasets and metrics: 摘要提到"across diverse VLM architectures and benchmarks"，但未列出具体数据集名称和评价指标。性能指标包括：token缩减率（剪掉94.4%的visual tokens）、性能保持率（保留96.4%的full-token性能）、端到端推理加速比（最高2.30倍）。具体benchmark名称和指标（如准确率、F1等）摘要未说明。

Experiments: 摘要提到在多种VLM架构和benchmark上进行了广泛评估，CaRe优于state-of-the-art token reduction baselines。关键实验数据：剪掉94.4%的visual tokens时保留96.4%的原始full-token性能，端到端推理速度相对未剪枝模型提升最多2.30倍。但未给出具体对比方法名称、消融实验细节和不同架构下的详细结果。

Limitations: 1. 摘要未说明具体VLM架构、benchmark数据集和评价指标，可复现性信息不足。2. 方法针对VLM的token缩减，不直接适用于遥感目标检测模型（如CNN-based或transformer-based检测器）。3. 摘要未说明该方法在遥感图像（如SAR、高光谱、多光谱）上的适用性。4. 未提供参数量、FLOPs、显存、能耗等部署关键指标。5. 未说明token缩减对遥感小目标检测的影响——遥感场景中小目标信息本就稀疏，token缩减可能加剧小目标漏检，而摘要未讨论这一风险。

Ideas for my research: 1. "Calibrate Before Reason"思想可迁移：在遥感目标检测模型的轻量化（如剪枝、量化）之后，引入类似的校准/补偿机制来缓解压缩导致的语义漂移，尤其是对遥感小目标检测性能的退化。2. Confidence-Gated机制可借鉴：在token或特征通道缩减时，用置信度门控从未保留的部分提取补偿信号，注入保留部分，这一思路可用于遥感检测器的通道剪枝或token剪枝后的性能补偿。3. Perturbation-Robust Anchoring思想可用于遥感场景：在遥感图像存在噪声、遮挡、光照变化等扰动下，识别对模型输出影响稳定的关键特征锚点，辅助轻量化后的模型保持鲁棒性。4. 该方法为training-free，若能在遥感检测模型上实现无需重训的轻量化+校准，对星上部署（无法在线重训）有实际价值。

Abstract: Large Vision-Language Models (VLMs) suffer from prohibitive inference overhead due to long sequences of visual tokens. However, existing visual token reduction methods mainly improve efficiency by pruning or compressing redundant tokens without examining whether the resulting representation remains semantically consistent with the original representation. Mapping the original N-token visual sequence to K tokens may discard, dilute, or misassign critical visual cues, triggering severe semantic drift that deviates the VLM's understanding. In this paper, we first introduce the principle of 'Calibrate Before Reason' to visual token reduction and propose CaRe, a training-free robust framework that calibrates compact visual representations before reasoning to preserve semantic fidelity in VLMs. CaRe consists of two mutually complementary modules: 1) Perturbation-Robust Calibration Anchoring, which identifies calibration anchors with stable model-side influence under multi-directional perturbations; 2) Confidence-Gated Token Calibration, which extracts reliable calibration signals from unselected tokens and injects them into anchors. Extensive evaluations across diverse VLM architectures and benchmarks verify that CaRe outperforms state-of-the-art token reduction baselines. While pruning 94.4% of visual tokens, our method retains 96.4% of the original full-token performance, delivering up to 2.30 times faster end-to-end inference speed relative to unpruned vanilla models.

</details>


### [25] [PrintAnything: Learning an Intermediate Representation for 3D printing G-code Generation](https://arxiv.org/abs/2607.27729)
*Sangmin Hong,Daniel Sungho Jung,Heewon Kim,Kyoung Mu Lee*

Main category: cs.CV

TL;DR: 提出 PrintAnything 框架，直接从 3D 点云生成可执行的 3D 打印 G-code，无需网格重建，通过 slice-wise 点投影策略和 G-plan map 统一表示实现无网格打印流水线。

Research relevance: 弱相关（1-2 分）。论文主题是 3D 打印（3D printing）中的点云到 G-code 生成，属于 3D 几何处理与制造领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。仅有的弱关联在于其处理对象是 point clouds（点云），而遥感领域（如 LiDAR 点云、SAR）也涉及点云数据，但本论文处理的是 3D 打印制造场景，方法不可直接迁移。

Reading priority: 1 分。论文主题为 3D 打印 G-code 生成，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无直接借鉴价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D 打印 G-code 生成（toolpath synthesis），即从点云直接生成可执行的 3D 打印指令。应用场景：增材制造 / 3D 打印（fused deposition modeling, FDM）。传感器类型：3D 点云（point clouds，来源未说明，可能为扫描设备）。目标类别：无目标检测概念，处理对象为 3D 几何形状。

Model architecture: 摘要未说明具体网络结构。核心组件包括：(1) slice-wise point projection strategy——将非结构化 3D 点云转换为与 FDM 逐层打印特性一致的 slice-aligned 2D 表示；(2) Geometric plan (G-plan) map——由 occupancy、region、flow 三种 2D map 组成的紧凑表示，编码几何与挤出（extrusion）属性，用于桥接点云与 G-code。整体为学习式（learned）框架，直接从点云预测 G-code。

Lightweight method: 摘要未说明任何轻量化方法。不涉及 pruning、quantization、knowledge distillation、NAS、实时推理、边缘部署、FPGA/NPU/TensorRT 等模型压缩或加速技术。G-plan map 被称为 compact 2D representation，但这是针对数据表示紧凑性的设计，并非模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。论文面向 3D 打印制造场景，与星上/边缘部署无关。

Datasets and metrics: 摘要未说明具体数据集、评价指标和实验设置。仅提及代码已开源（GitHub 链接），未给出定量评估结果。

Experiments: 摘要未说明主要实验结果、对比方法或消融实验。仅声称方法能准确生成可打印的 G-code，实现无网格（mesh-free）打印流水线，但无定量数据支撑。

Limitations: 摘要未说明局限性。主要缺口包括：无定量实验结果、无数据集与指标、无网络结构细节、无计算开销信息。从方法本身看，slice-wise 投影和 G-plan map 针对 3D 打印逐层特性设计，泛化到其他任务（如遥感）的可行性存疑。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的借鉴点非常有限，但可提取以下泛化思路：(1) 将非结构化数据（点云/遥感图像）转换为与下游任务物理特性对齐的紧凑 2D 表示（类似 G-plan map 的 occupancy/region/flow 编码思想），可用于遥感旋转框检测中设计更紧凑的表示；(2) "compact representation" 的设计理念可启发在遥感检测中减少冗余特征、降低计算量的思路。但整体而言，本论文与遥感在轨检测轻量化方向基本无关，不建议作为主要参考。

Abstract: Point clouds are one of the most fundamental and widely used 3D representations, serving as the most basic geometric representation of 3D shapes. Nevertheless, most existing 3D printing pipelines require a watertight mesh as input, preventing the direct use of point clouds for fabrication. A common workaround is to reconstruct meshes from point clouds; however, the resulting meshes often contain geometric artifacts, such as incorrect faces or topological inconsistencies, that are difficult to repair and may lead to printing failures. To overcome these limitations, we propose PrintAnything, a novel framework that learns to produce executable 3D printing G-code directly from 3D point clouds without requiring mesh reconstruction. To enable point clouds to serve as direct input for slice-wise toolpath generation, we introduce a slice-wise point projection strategy that transforms unstructured 3D point clouds into slice-aligned 2D representations consistent with layer-by-layer nature of fused deposition modeling in 3D printing. To eliminate mesh dependency and provide a unified representation that bridges point clouds and G-code, we propose Geometric plan (G-plan) map, a compact 2D representation composed of occupancy, region, and flow maps that encode the geometric and extrusion properties required for toolpath synthesis in 3D printing. As a result, our proposed method accurately generates printable G-code directly from point clouds, enabling a practical and fully mesh-free pipeline for 3D printing. The code is publicly available at \href{https://github.com/Sangminhong/PrintAnything}{https://github.com/Sangminhong/PrintAnything}.

</details>


### [26] [Articulated Object Reconstruction from Rest-State Observation](https://arxiv.org/abs/2607.27749)
*Daeun Lee,Jaeah Lee,Woosung Kim,Haebeom Jung,Jaesik Park*

Main category: cs.CV

TL;DR: 提出一种从单一闭合构型重建可动关节物体的rest-state方法，利用显式网格作为中间表示融合视觉-语言与分割模型输出，并通过视频扩散模型合成关节运动假设来估计关节参数。

Research relevance: 弱相关（1分）。该论文聚焦于3D可动关节物体重建（articulated object reconstruction）与digital twin构建，属于3D视觉/几何重建领域，与遥感、卫星、在轨目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或边缘部署方法。

Reading priority: 1分。该论文属于3D几何重建与生成式方法领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对diffusion-based假设生成或跨模型融合策略有泛化兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D articulated object reconstruction（可动关节物体重建）与digital twin构建。应用场景：交互式数字孪生（interactive digital twins）。传感器类型：视觉图像（vision-language models的输入），非遥感传感器。目标类别：可动关节物体（articulated objects），非遥感目标。

Model architecture: 采用显式mesh作为中间表示（intermediate representation），用于跨模型验证与融合（cross-model verification and fusion），整合vision-language模型和segmentation模型的输出。使用video diffusion model合成articulation hypotheses，并通过geometric consistency进行验证。摘要未说明具体网络结构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其方法依赖video diffusion model和vision-language model，计算开销大，与星上/边缘部署场景不匹配。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及与motion-observing reconstruction-based、generation-based、modular pretrained-model三类baseline进行对比，评价维度包括part decomposition准确性和articulation物理合理性。

Experiments: 摘要未说明具体实验设置和量化结果。仅声明方法在part decomposition和physically plausible articulation上达到与三类baseline相当的性能（performing competitively）。

Limitations: 1. 该问题本身是ill-posed（病态）设定，从单一闭合构型估计关节参数存在固有歧义；2. 依赖video diffusion model合成运动假设，计算成本高，难以满足实时/边缘部署需求；3. 摘要未提供定量实验结果、数据集规模、泛化能力等关键信息；4. 与遥感在轨目标检测方向无任何交集。

Ideas for my research: 借鉴意义有限，但可提取以下思路：1. 其"explicit intermediate representation + cross-model verification"的思想可类比于遥感检测中多模型/多模态（如光学+SAR）融合时的中间特征对齐与一致性校验；2. 使用diffusion model生成假设再通过几何一致性验证的思路，可类比于遥感小目标检测中利用生成模型做数据增强或伪样本合成；3. 其"rest-state formulation"（从静态单帧推断动态结构）的思想，对遥感中从单帧影像推断目标运动状态或结构属性有一定启发。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点很少。

Abstract: Building interactive digital twins requires recovering both 3D geometry and the kinematic structures that govern how objects articulate. Yet existing methods for articulated object reconstruction require explicitly observable motion from multiple articulation states. We introduce a rest-state formulation that reconstructs articulated objects from a single closed configuration, an inherently ill-posed setting where geometry, semantics, and motion priors compensate for the absence of motion cues. Our framework adopts an explicit mesh as an intermediate representation for cross-model verification and fusion, reconciling noisy outputs from vision-language and segmentation models into spatially consistent part structures. To estimate joint parameters without observed motion, we use a video diffusion model to synthesize articulation hypotheses and validate them through geometric consistency. Our approach achieves accurate part decomposition and physically plausible articulation, performing competitively with motion-observing reconstruction-based, generation-based, and modular pretrained-model baselines.

</details>


### [27] [EgoGVAE: Ego-body Mesh Reconstruction via Guided Variational Autoencoder](https://arxiv.org/abs/2607.27755)
*Jaehun Jung,Wonjun Kim*

Main category: cs.CV

TL;DR: 提出一种基于VAE引导网络潜空间的头姿到全身姿态网格重建方法，通过分布对齐实现单步采样，推理速度比扩散方法快50倍以上。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文属于人体姿态估计/重建（human pose estimation, ego-body mesh reconstruction）领域，面向头戴设备或智能眼镜应用，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测任务。虽然论文关注推理速度优化（one-step sampling, 50x faster），但其轻量化思路是针对生成式扩散模型的采样加速，与模型压缩（pruning/quantization/distillation）或边缘/星上部署无直接关系。相关性评分：1分。

Reading priority: 1分。该论文属于人体姿态重建领域，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议投入阅读时间。仅当对"潜空间分布对齐加速生成式推理"这一通用技术感兴趣时，可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：从头部姿态（head pose）恢复全身姿态网格（full-body mesh reconstruction），属于人体姿态估计/重建任务。应用场景：头戴设备（head-mounted devices）、智能眼镜（smart glasses）。传感器类型：摘要未说明（推测为IMU或头戴相机，但摘要未明确）。目标类别：人体全身姿态（full-body pose），非遥感目标。

Model architecture: 提出方法包含两个网络：(1) guidance network——以全身姿态为输入的variational autoencoder (VAE)，用于学习全身姿态的潜空间分布；(2) head-to-motion network——以头部姿态为输入，通过强制其潜分布与guidance network的潜分布相似（latent distribution alignment），使仅从头姿采样的潜特征能可靠解码为自然全身姿态。采用one-step sampling方案替代diffusion-based迭代过程。

Lightweight method: 本文的"轻量化"体现在推理加速：通过潜空间分布对齐（latent distribution alignment）实现one-step sampling，相比diffusion-based方法推理速度快50倍以上。但这属于生成式模型的采样加速策略，并非传统意义上的模型压缩（pruning, quantization, knowledge distillation, NAS）或边缘/星上部署优化。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。仅提及推理速度比diffusion-based方法快50倍以上（more than 50 times faster），但未给出绝对延迟数值。该工作面向头戴设备/智能眼镜场景，与星上部署（FPGA/NPU/TensorRT）无直接关联。

Datasets and metrics: 摘要未说明具体数据集名称。仅在benchmark datasets上进行了实验，评价指标涉及ego-body mesh reconstruction性能（具体指标如MPJPE等摘要未说明）。

Experiments: 摘要仅说明在benchmark数据集上验证了方法能有效提升ego-body mesh reconstruction性能，且推理速度显著优于diffusion-based方法。具体对比方法、消融实验细节摘要未说明。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于人体姿态估计领域；2. 摘要未说明具体数据集、评价指标、参数量、FLOPs等关键信息；3. 潜空间分布对齐方法对姿态数据的有效性是否可迁移到遥感目标检测场景存疑；4. 未讨论实际部署（如边缘设备）的硬件约束。

Ideas for my research: 尽管该论文与遥感在轨目标检测无直接关系，但有两个思路可借鉴：(1) 潜空间分布对齐（latent distribution alignment）思想——如果未来在遥感目标检测中引入生成式或自监督预训练，可考虑用轻量teacher网络引导student网络潜空间对齐，替代昂贵的diffusion迭代过程；(2) one-step sampling加速策略——若在轨场景涉及生成式数据增强或超分，可参考其将迭代式生成改为单步采样的思路。但整体借鉴价值有限。

Abstract: We address the problem of recovering the full-body mesh from only the head pose. This task has become essential for various applications based on head-mounted devices or smart glasses. The challenge of this task lies in estimating the pose information of unobserved body parts based solely on a single joint (i.e., head) trajectory. Several studies have begun to adopt head-conditioned generative models, however, such previous methods are costly and time-consuming due to the diffusion-based iterative process. As an alternative, we propose a simple yet novel method that leverages the latent space of the guidance network, which is designed as a variational autoencoder taking full-body poses as inputs. By enforcing latent distributions of this guidance network and our head-to-motion network to be similar, latent features sampled from the 'guided' distribution, i.e., distribution learned in our head-to-motion network, can be reliably decoded for natural representations of full-body poses even only with the head pose. One important advantage of the proposed method is that one-step sampling scheme achieves remarkably fast inference (more than 50 times faster) compared to diffusion-based approaches. Experimental results on benchmark datasets show that the proposed method efficiently improves the performance of ego-body mesh reconstruction.

</details>


### [28] [Filling the Pareto-Optimal Front for Affordance Segmentation on Embedded Devices Using RGB-D Cameras](https://arxiv.org/abs/2607.28293)
*Edoardo Ragusa,Giovanni Paolo Canuti,Simone Lugani,Rodolfo Zunino,Paolo Gastaldo*

Main category: cs.CV

TL;DR: 面向可穿戴机器人affordance segmentation任务，提出硬件感知的神经架构搜索（hardware-aware NAS）和专用微调方法，将深度（D）信息融入小型深度网络，在Jetson Nano等便携硬件上实现实时且低能耗的RGB-D分割。

Research relevance: 弱相关（2分）。论文涉及轻量化网络设计（NAS、小型网络、硬件加速器、实时推理、能耗预算），与"模型轻量化"方向有方法学上的借鉴意义；但任务场景是wearable robots的affordance segmentation（RGB-D），并非遥感/卫星/目标检测场景，与遥感在轨目标检测无直接关联。

Reading priority: 2分。与遥感在轨目标检测无直接关联，但hardware-aware NAS和嵌入式平台部署的实验范式对轻量化方法设计有一定借鉴价值。若时间有限可仅阅读其NAS搜索空间设计和Pareto评估部分。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：affordance segmentation（可交互区域分割）；应用场景：wearable robots（可穿戴机器人）；传感器类型：RGB-D（RGB相机 + RealSense深度传感器）；目标类别：摘要未说明（affordance区域）。

Model architecture: 两种方案：(1) 重新公式化的hardware-aware neural architecture search（硬件感知NAS），配以新设计的搜索空间以将深度(D)信息集成到小型深度网络；(2) 专用fine-tuning方法，包含一个preprocessing layer将深度信息与RGB数据融合，使其兼容传统架构。整体面向小型网络（tiny-like approaches）的改进。

Lightweight method: 核心轻量化手段为hardware-aware NAS（硬件感知神经架构搜索），搜索空间专门设计以适配便携硬件加速器；同时通过专用微调（fine-tuning）和预处理层实现RGB-D融合，在保持小模型规模的同时提升性能。目标是生成能利用现代便携硬件加速器、克服tiny-like方法在硬件约束下性能不足的方案。

Onboard deployability: 部署平台：Jetson Nano开发板 + RealSense RGB-D相机。系统可在标准电池（如智能手机电池）能耗预算内实现实时性能（real-time performances）。摘要未明确给出具体参数量、FLOPs、延迟、显存占用等量化指标，但强调通过Pareto最优前沿平衡泛化性能与硬件需求。

Datasets and metrics: 在"一对真实世界数据集"（a pair of real-world datasets）上进行实验，摘要未说明数据集具体名称。评价指标：摘要未明确说明，但提及通过Pareto最优前沿（Pareto optimal front）平衡泛化性能与硬件需求。

Experiments: 在真实世界数据集上与现有方案（existing solutions）对比，证明所提方法有效性；在多数情况下生成的方案能识别Pareto最优前沿，兼顾泛化性能与硬件需求。摘要未提供具体数值结果、对比方法名称或消融实验细节。

Limitations: (1) 任务场景为wearable robots的affordance segmentation，与遥感/卫星目标检测无直接关系；(2) 摘要未提供具体参数量、FLOPs、延迟、能耗等量化部署指标；(3) 数据集规模与名称未说明，泛化性存疑；(4) 未涉及遥感图像特有的挑战（如小目标、旋转框、复杂背景、多光谱/SAR数据等）。

Ideas for my research: (1) hardware-aware NAS的搜索空间设计思路可借鉴——将硬件约束（如Jetson/FPGA/NPU的算子支持、内存带宽）直接编码进搜索空间，用于遥感在轨目标检测模型的轻量化搜索；(2) 多模态融合的preprocessing layer思路可迁移到遥感多光谱/高光谱+RGB融合场景；(3) Pareto最优前沿评估方法（泛化性能 vs 硬件需求）可用于在轨检测模型的权衡分析；(4) 在真实嵌入式平台（类似Jetson Nano）上验证能耗与实时性的实验范式值得参考，但需替换为遥感目标检测任务和星载硬件约束。

Abstract: While depth sensors have the potential to complement RGB data for affordance segmentation in wearable robots, their usage seems to remain underexplored. The paper proposes two approaches: a reformulated version of hardware-aware neural architecture search, endowed with a newly designed search space to integrate depth (D) information into small-sized deep networks, and a dedicated fine-tuning approach, including a preprocessing layer to merge depth information with RGB data and make it compatible with conventional architectures. In both cases, those methods aim to generate solutions that benefit from modern (portable) hardware accelerators and overcome existing tiny-like approaches, which often fail to tackle critical scenarios due to the severe constraints set by the supporting hardware. Extensive experiments on a pair of real-world datasets demonstrate the effectiveness of the proposed method as compared with existing solutions. The approach presented in the paper generates, in most cases, solutions that identify the Pareto optimal front to balance generalization performance and hardware requirements. The paper also describes the supporting prototype, including a Jetson Nano board and a RealSense RGB-D camera. When considering the energy profile of the device, the overall system can attain real-time performances within an energy budget that is compatible with standard batteries, such as those used in smartphones.

</details>


### [29] [DS@GT ARC at ImageCLEFmedical 2026: Architectural Diversity for Concept Detection and Foundation-Model Scaling for Caption Prediction in Medical Image Analysis](https://arxiv.org/abs/2607.27763)
*Bowen Wang,Youwen Zhang,Ritesh Mehta*

Main category: cs.CV

TL;DR: 本文是DS@GT团队参加ImageCLEFmedical Caption 2026医学影像挑战赛的技术报告，涉及放射影像的医学概念检测（UMLS CUI分类）和自然语言描述生成两个任务，采用ConvNeXt-V2、BiomedCLIP、DenseNet-169集成以及Gemma-3、BLIP等大模型。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文面向医学放射影像（radiology images）的医学概念检测与描述生成，属于医学影像分析+多模态大语言模型领域，不涉及遥感、卫星、地球观测、SAR、高光谱等任何遥感相关场景，也不涉及目标检测任务，更不涉及剪枝、量化、蒸馏等轻量化部署方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学影像分析+多模态大模型领域。仅"training-free KNN检索"和"阈值正则化调优"两个方法论细节可能有间接借鉴价值，但整体阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学影像概念检测（Concept Detection，UMLS CUI分类）和医学影像描述生成（Caption Prediction）。应用场景：医学放射影像分析（ImageCLEFmedical挑战赛）。传感器类型：放射影像（radiology images），非遥感传感器。目标类别：UMLS医学概念（CUIs），非舰船/飞机/车辆等遥感目标。

Model architecture: Task 1：ConvNeXt-V2、BiomedCLIP ViT-B/16、DenseNet-169三模型late-fusion集成，配合"Honest Threshold Tuning"正则化阈值调优；另有一套training-free的KNN检索pipeline基于冻结的BiomedCLIP embeddings。Task 2：Gemma-3 27B微调、BLIP全微调（含自定义Vizwins merging）、MedGemma-4B零样本推理。

Lightweight method: 摘要未明确涉及传统轻量化方法（剪枝/量化/蒸馏/NAS）。但Task 1中training-free KNN检索pipeline（基于冻结BiomedCLIP embeddings）相比微调集成模型大幅降低了训练成本，可视为一种"低成本推理"思路；Task 2中对比了27B、4B等不同规模模型，涉及模型规模与性能的权衡，但未涉及部署层面的压缩加速。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。Task 2使用Gemma-3 27B等大模型，显然不适合星上/边缘部署，但摘要未给出具体部署可行性分析。

Datasets and metrics: 数据集：ROCOv2（ImageCLEFmedical Caption 2026挑战赛）。评价指标：Task 1使用primary F1和secondary F1；Task 2使用overall指标。Task 1集成模型primary F1=0.5790，secondary F1=0.9657（官方排名第一）；KNN pipeline primary F1=0.5780，secondary F1=0.9599。Task 2：Gemma-3 27B overall=0.3571（排名第三），BLIP overall=0.3564，MedGemma-4B zero-shot overall=0.3186。

Experiments: Task 1：三模型late-fusion集成排名官方第一（primary F1=0.5790）；training-free KNN检索在primary track上几乎与微调集成持平（0.5780 vs 0.5790），但成本大幅降低。Task 2：Gemma-3 27B排名第三（0.3571），BLIP（0.3564）与MedGemma-4B零样本（0.3186）作为不同规模/成本方案的对比。摘要未提供消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于医学影像多模态领域。2. 摘要未提供任何部署相关指标（参数量、FLOPs、延迟、能耗）。3. 未涉及目标检测任务，仅涉及分类和生成。4. 大模型（27B）方案在资源受限场景下不具可行性，但摘要未讨论压缩策略。

Ideas for my research: 尽管领域不同，仍有少量可迁移的通用方法论借鉴：1. "training-free KNN检索"思路——在遥感目标检测中，可探索基于冻结backbone特征+近邻检索的轻量推理方案，避免全模型微调，降低在轨计算开销。2. "Honest Threshold Tuning"正则化调阈方法——在遥感小目标检测中，类别不平衡和稀有目标（如罕见舰船类型）的置信度阈值调优可借鉴此思路避免验证集过拟合。3. 多模型late-fusion集成与单模型轻量化的权衡——可启发在轨场景中"多模型集成 vs 单模型压缩"的取舍分析。4. 不同规模模型（27B vs 4B）的性能-成本权衡分析框架，可类比遥感检测中不同轻量化程度模型的权衡。

Abstract: We describe the DS@GT submissions to the ImageCLEFmedical Caption 2026 challenge, which continues a long-running benchmark on the ROCOv2 dataset with two tracks: Concept Detection (Task 1), assigning UMLS Concept Unique Identifiers (CUIs) to radiology images, and Caption Prediction (Task 2), generating natural-language captions. For Task 1, our primary submission was a three-way late-fusion ensemble of ConvNeXt-V2, BiomedCLIP ViT-B/16, and DenseNet-169 with a regularized ''Honest Threshold Tuning'' procedure designed to avoid validation overfitting on rare concepts; this submission ranked first on the official submission with a primary $F_1$ of $0.5790$ and a secondary $F_1$ of $0.9657$. In parallel, we submitted a training-free KNN retrieval pipeline over frozen BiomedCLIP embeddings, which reached a primary $F_1$ of $0.5780$ and a secondary $F_1$ of $0.9599$-essentially matching the fine-tuned ensemble on the primary track at a fraction of the cost. For Task 2, our submissions included a fine-tuned Gemma-3 27B model (overall $0.3571$, ranking third in the official submission), a fully fine-tuned BLIP pipeline with custom Vizwins merging ($0.3564$), and a zero-shot MedGemma-4B run with a PubMed-style prompt ($0.3186$), spanning a wide range of model scales and training costs. Code: https://github.com/dsgt-arc/imageclef-caption-2026.

</details>


### [30] [Private Face Recognition Training Dataset Publication via Identity-Decoupled and Geometry-Preserving Face Distillation](https://arxiv.org/abs/2607.27764)
*Shuhuan Chen,Xiangyu Zhu,Weisong Zhao,Siran Peng,Tianshuo Zhang,Haoyuan Zhang,Haichao Shi,Xiao-Yu Zhang,Zhen Lei*

Main category: cs.CV

TL;DR: 提出 Private Face Distillation 框架，通过身份解耦与几何保持来解决私有面部识别训练数据集发布中的"身份悖论"，在保护隐私的同时保持识别效用。

Research relevance: 弱相关（2分）。该论文属于人脸识别（face recognition）与隐私保护领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景；也不涉及目标检测（object detection）任务，更不涉及轻量化、剪枝、量化、知识蒸馏（knowledge distillation）用于模型压缩或边缘/星上部署。虽然标题和正文中出现"Distillation"一词，但这里的"Private Face Distillation"指的是隐私数据蒸馏/身份解耦，并非模型压缩意义上的知识蒸馏。因此与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅作为泛视觉/隐私学习方法具有极弱的参考价值。

Reading priority: 1分。该论文聚焦人脸识别隐私数据发布，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测任务或任何模型压缩/部署技术，阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：私有面部识别（private face recognition）训练数据集发布与隐私保护。应用场景：人脸识别模型训练数据的隐私安全发布，涉及 IJB-C surveillance 等监控场景。传感器类型：普通可见光人脸图像（非遥感传感器）。目标类别：人脸（face identity）。与遥感目标检测无关。

Model architecture: 提出 Private Face Distillation 框架，包含两个核心模块：(1) Orthogonal Geometry Preservation（正交几何保持），从私有身份表示中构造解耦的代理身份（proxy identities），同时维持超球面几何（hyperspherical geometry）；(2) Relational Topology Alignment（关系拓扑对齐），保持身份间关系以用于识别学习。摘要未说明具体骨干网络结构。

Lightweight method: 摘要未涉及任何模型轻量化方法。虽然方法名称含"Distillation"，但这里的"Private Face Distillation"是隐私数据层面的身份解耦与蒸馏，并非模型压缩中的 knowledge distillation（知识蒸馏），不涉及剪枝（pruning）、量化（quantization）、NAS、实时推理或边缘部署。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作聚焦于训练数据隐私发布，而非推理端模型压缩或星上/边缘部署，因此不具备在轨部署可行性分析。

Datasets and metrics: 实验在多个 domain-shifted FR 场景上进行，具体数据集包括 IJB-C surveillance（监控场景）。评价指标为 TAR@FAR=1e-3（在误报率 1e-3 下的真正率），在 IJB-C surveillance 上较基线提升 3.94%，同时降低 source-identity linkability（源身份可链接性）。摘要未说明其他数据集细节。

Experiments: 在多个跨域（domain-shifted）人脸识别场景下，Private Face Distillation 相比已评估的发布基线（publication baselines）取得了更强的效用（utility）。在 IJB-C surveillance 上，TAR@FAR=1e-3 较基线提升 3.94%，同时降低了源身份可链接性。摘要未提供消融实验、对比模型细节或更多定量结果。

Limitations: (1) 与遥感、卫星、目标检测、轻量化完全无关，属于人脸识别隐私保护方向；(2) 摘要未说明模型结构、计算开销、推理效率等部署相关信息；(3) 仅报告了 TAR@FAR 单一指标，缺乏对 linkability 降低幅度的定量细节；(4) 未说明方法在非人脸通用目标上的泛化能力；(5) 摘要未说明训练成本、收敛性等。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但可借鉴以下思路：(1) "身份解耦与几何保持"的思想——在遥感目标检测中，若涉及数据隐私（如高分辨率遥感影像中的敏感目标），可借鉴其"解耦敏感语义、保留识别几何"的框架设计；(2) Relational Topology Alignment 中"保持样本间关系"的思想，可迁移到知识蒸馏（knowledge distillation）中用于保持教师-学生模型间的特征拓扑关系，提升轻量化模型的表征一致性；(3) 超球面几何（hyperspherical geometry）约束在遥感旋转框检测的类别判别学习中可能有借鉴价值。但整体而言，该论文对本课题的直接借鉴价值有限。

Abstract: Publishing private face recognition~(FR) training datasets is privacy-sensitive because faces expose identity information. Private FR training dataset publication mitigates this risk by releasing protected proxies as substitutes for private training faces. However, training FR models with such data introduces an identity paradox: \emph{the identity cues that make released faces useful for recognition supervision are also the cues that make them linkable to real individuals.} A protected face should be decoupled from the original identity, yet still behave as a reliable identity sample for training. Removing these cues too aggressively may destroy the class structure needed for recognition learning, whereas preserving them too faithfully may increase source-identity linkability. We argue that this paradox stems from conflating source-aligned identity semantics with recognition-useful proxy identity geometry. The former should be suppressed to reduce linkage to private individuals, while the latter should be preserved for FR learning. Based on this insight, we propose \textbf{Private Face Distillation}, an identity-decoupling and geometry-preserving framework. It uses Orthogonal Geometry Preservation to construct decoupled proxy identities from private identity representations while maintaining hyperspherical geometry, and Relational Topology Alignment to preserve identity relations for recognition learning. Experiments across multiple domain-shifted FR scenarios show that Private Face Distillation achieves stronger utility than the evaluated publication baselines. On IJB-C surveillance, it improves $\mathrm{TAR}@\mathrm{FAR}{=}1\text{e-}{3}$ by 3.94\% over the baseline while reducing source-identity linkability. These results suggest that private FR training dataset publication should decouple source-identity correspondence while preserving proxy identity geometry.

</details>


### [31] [CXR-Retrieve: Compositional Text-to-Image Retrieval in Chest Radiography](https://arxiv.org/abs/2607.27779)
*Tomer Erez,Moshe Kimhi,Chaim Baskin,Ehud Rivlin*

Main category: cs.CV

TL;DR: 提出CXR-Retrieve基准和label-aware对比微调目标，用于胸部X光影像的组合式文本-图像检索，解决临床查询中连词和否定约束的匹配问题。

Research relevance: 弱相关（2分）。本文属于医学影像（chest X-ray）领域的vision-language检索任务，既非遥感/卫星场景，也不涉及目标检测或模型轻量化。虽然涉及对比学习、检索等通用技术，但对遥感在轨目标检测与轻量化方向借鉴价值有限。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学影像vision-language检索领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务：text-to-image retrieval（文本-图像检索），具体为组合式临床查询检索。场景：医学影像（chest X-ray，胸部X光）。传感器类型：X-ray影像（非遥感传感器）。目标类别：病理发现（atelectasis, pneumonia等），非遥感目标。

Model architecture: 基于CXR-CLIP（in-domain vision-language checkpoint）进行label-aware contrastive fine-tuning。核心是设计对比学习目标，吸引具有兼容病理约束的image-text对（包括共享的确认缺失），排斥矛盾对。摘要未说明具体网络backbone结构。

Lightweight method: 摘要未涉及任何轻量化方法（无pruning、quantization、knowledge distillation、NAS等）。

Onboard deployability: 摘要未说明。无参数量、FLOPs、延迟、显存、能耗等部署相关信息。

Datasets and metrics: 数据集：MIMIC-CXR-JPG官方测试集（5,159张测试图像），145条文本查询（涵盖单一/连词发现、阳性/阴性）。评价指标：Precision@5。对比基线：CXR-CLIP。

Experiments: 在CXR-CLIP基础上，label-aware对比微调在双病理连词查询上Precision@5提升8.5个百分点，在否定查询上提升22.0个百分点。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于医学影像检索领域。2. 摘要未说明模型规模、推理效率、部署可行性。3. 仅报告Precision@5，未报告其他检索指标（如Recall、mAP）。4. 基准规模有限（145条查询）。

Ideas for my research: 1. 对比学习中的"label-aware"思想可迁移：在遥感目标检测中，可设计考虑目标类别间语义约束（如"舰船且无飞机"）的对比损失，增强检测器对组合语义的理解。2. 否定/连词约束建模思路，可用于遥感场景中多目标共现/互斥关系的建模。3. 构建结构化基准（类似CXR-Retrieve）评估组合语义查询的思路，可借鉴到遥感图像检索或检测评估中。但整体借鉴价值有限。

Abstract: Large chest radiography archives are difficult to search because most studies are paired only with free-text reports rather than structured clinical annotations. Vision-language models offer a natural interface for text-to-image retrieval, but current biomedical models are primarily optimized for report-to-image matching rather than for satisfying short clinical search queries. This creates an objective mismatch: a model may retrieve images related to words in the query while failing to satisfy the full clinical constraint, especially for conjunctions and negations such as ``atelectasis and no pneumonia.''
  We introduce CXR-Retrieve, a structured benchmark for compositional chest X-ray text-to-image retrieval. The benchmark contains 5,159 test images from the official test-split of MIMIC-CXR-JPG and 145 textual queries spanning single and conjunction findings, both positive and negative. Relevance is defined by whether a retrieved image satisfies all asserted pathology constraints, rather than by whether it matches a paired report.
  We further propose a label-aware contrastive fine-tuning objective for clinical retrieval. Our method attracts image-text pairs with compatible asserted pathology constraints, including shared confirmed absences, while explicitly repelling contradictory pairs. Starting from the in-domain CXR-CLIP checkpoint, our method improves Precision@5 over CXR-CLIP by 8.5 percentage points on two-pathology conjunctions and by 22.0 percentage points on negation queries. These results show that reliable chest X-ray retrieval requires training objectives that model not only which findings are mentioned, but also how they are clinically asserted.

</details>


### [32] [FDDWAN: A Frequency-Decoupled Diffusion Network for Watermarking Attack](https://arxiv.org/abs/2607.27800)
*Chunpeng Wang,Yuxin Li,Xiaoyu Wang,Jidong Yang,Suo Gao,Qi Li*

Main category: cs.CV

TL;DR: 提出FDDWAN，一种基于小波域分解和残差扩散细化的频率解耦水印攻击网络，用于在保持视觉保真度的同时有效去除图像中的不可见水印。

Research relevance: 弱相关（2分）。该论文属于图像水印去除/攻击领域，与遥感、卫星、在轨部署、目标检测和模型轻量化均无直接关联。虽然其频率域分解和扩散模型方法在理论上可能对遥感图像处理有间接借鉴，但论文本身不涉及遥感场景、目标检测任务或任何轻量化/部署优化技术。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于图像水印攻击领域。虽然频率域分解思想有一定理论参考价值，但整体研究主题、任务类型和部署考量均与本人课题不匹配，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务：不可见水印去除（invisible watermark removal / watermark attack）。应用场景：图像版权保护对抗攻击。传感器类型：自然图像（CelebA人脸、ImageNet通用图像）。目标类别：不涉及目标检测，处理对象为含水印的自然图像。

Model architecture: FDDWAN采用coarse-to-fine两阶段框架：(1) Wavelet-based Frequency-domain Preliminary Attack Module (WFPAM) 通过小波分解将含水印图像分解为低频和高频子带，并针对不同频带采用频率特定的攻击策略；(2) Frequency-domain Residual Diffusion Attack Module (FRDAM) 在训练时分别建模初步攻击输出与无水印参考之间的残差分布，选择性细化频率域残差，引导扩散过程聚焦于剩余水印相关差异。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计。扩散模型本身计算开销较大，但论文未讨论推理效率或部署优化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存或能耗等部署相关指标。扩散模型（diffusion model）通常计算开销大、推理速度慢，与星上实时推理需求存在明显冲突，但论文未讨论此问题。

Datasets and metrics: 数据集：CelebA和ImageNet，覆盖四种代表性水印方案（watermarking schemes）。评价指标：水印去除效果与视觉保真度（perceptual fidelity）之间的权衡，具体指标名称摘要未说明。

Experiments: 在CelebA和ImageNet上对四种水印方案进行实验，FDDWAN相比传统方法和基于学习的方法在水印去除有效性与视觉保真度之间取得了更优的权衡。具体对比方法和数值结果摘要未说明。

Limitations: (1) 与遥感/卫星/目标检测/轻量化方向基本无关；(2) 扩散模型推理成本高，不适合实时或边缘部署场景；(3) 仅在自然图像（人脸、通用图像）上验证，未涉及遥感图像（SAR、多光谱、高光谱）的特殊性；(4) 摘要未报告计算开销、模型规模等关键工程指标。

Ideas for my research: 尽管整体相关性弱，但以下思路可间接借鉴：(1) 频率域分解（小波变换）思想可用于遥感目标检测中的特征增强或去噪预处理，例如在SAR图像中分离低频背景与高频目标特征；(2) 残差扩散细化的"选择性细化"策略可类比于在目标检测中只对困难样本或小目标区域进行精细化处理，减少不必要的计算量；(3) 频率特定策略（frequency-specific strategy）的思想可用于设计轻量化检测模型中对不同频带特征的分层处理，但需注意扩散模型本身不适合星上部署，若借鉴应替换为更轻量的频率域模块。

Abstract: Existing invisible watermark removal methods often struggle to accurately capture the watermark-bearing features, leading to an unfavorable trade-off between watermark suppression and perceptual fidelity. In this paper, we propose the Frequency-Decoupled Diffusion Watermark Attack Network (FDDWAN), a coarse-to-fine framework that performs watermark removal through wavelet-domain decomposition and residual diffusion refinement. In the initial stage, the Wavelet-based Frequency-domain Preliminary Attack Module (WFPAM) decomposes the watermarked image into low- and high-frequency subbands and applies frequency-specific attack strategies tailored to their respective contributions to watermark robustness and perceptual quality. In the next stage, the Frequency-domain Residual Diffusion Attack Module (FRDAM) separately models the residual distributions between the preliminarily attacked outputs and the corresponding watermark-free references during training. Rather than reconstructing the entire image, FRDAM selectively refines frequency-domain residuals, directing the diffusion process toward the remaining watermark related discrepancies while minimizing modifications to image content. Extensive experiments on CelebA and ImageNet across four representative watermarking schemes demonstrate that FDDWAN achieves a more favorable trade-off between watermark removal effectiveness and visual fidelity than conventional and learning-based attack methods.

</details>


### [33] [LoMeVQA: A Comprehensive Benchmark for Longitudinal Medical VQA](https://arxiv.org/abs/2607.27806)
*Zhilin Wu,Zhangkai Ni,Chengmei Yang,Longzhen Yang,Yihang Liu,Ying Wen,Lianghua He*

Main category: cs.CV

TL;DR: 提出LoMeVQA纵向医学视觉问答基准数据集（206K VQA对）和MedLong-8B多模态大语言模型，用于时序医学影像推理。

Research relevance: 弱相关（评分1分）。论文聚焦医学影像纵向视觉问答（longitudinal medical VQA），属于医疗多模态大语言模型领域，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学影像多模态大模型领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：纵向医学视觉问答（longitudinal VQA），包含5个子任务（progress classification, progress description, progress report generation, differential region grounding, differential region description）。应用场景：临床医学影像时序分析。传感器类型：医学影像（具体模态摘要未说明）。目标类别：医学病变/病灶区域。

Model architecture: MedLong-8B：一个8B参数规模的多模态大语言模型（MLLM），具体网络结构细节摘要未说明。数据构建采用自动化pipeline，包括：(1)按时间顺序组织患者记录；(2)通过医学知识图谱提取临床实体；(3)建模时序演化以引导LLM生成高质量纵向VQA对。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法。MedLong-8B为8B参数的大模型，反而属于大模型范畴，与轻量化方向相悖。

Onboard deployability: 摘要未说明。论文未报告参数量（除模型名中8B）、FLOPs、延迟、显存、能耗等部署指标。8B参数规模的大语言模型在星上/边缘部署可行性极低，且论文未讨论任何边缘或嵌入式部署场景。

Datasets and metrics: 数据集：LoMeVQA，包含206K纵向VQA对，覆盖5个任务。评价指标：摘要未说明具体指标名称。实验对比了通用MLLM和医学领域MLLM，MedLong-8B在所有任务上达到SOTA。

Experiments: 实验表明通用和医学领域MLLM在LoMeVQA上表现较差，暴露了时序推理能力的显著不足。MedLong-8B在所有任务上取得SOTA。论文还进行了详细分析，揭示了关键失败模式（failure modes）并给出改进纵向医学视觉推理的方向。

Limitations: 与本研究方向基本无关。主要局限：(1) 面向医学影像而非遥感影像；(2) 任务为VQA而非目标检测；(3) 模型为8B大语言模型，无任何轻量化设计；(4) 未涉及在轨/边缘部署；(5) 未报告计算效率相关指标。

Ideas for my research: 借鉴意义有限，但有以下间接启发：(1) 其"时序演化建模"思路可类比遥感多时相影像分析（如多时相变化检测），但需自行迁移；(2) 知识图谱引导数据构建的pipeline思路，可用于构建遥感领域高质量标注数据；(3) 其"failure modes分析"方法论可借鉴到遥感检测模型误差分析中。总体而言，直接可复用的技术点很少。

Abstract: In clinical practice, patients often undergo multiple imaging examinations over successive visits, yielding longitudinal data. Modeling such temporal information is crucial for reliable assessment of disease progression and treatment response. However, despite the rapid advancement of multimodal large language models (MLLMs), longitudinal medical visual reasoning remains largely underexplored. To fill this gap, we propose LoMeVQA, a comprehensive benchmark consisting of 206K longitudinal visual question answering (VQA) pairs for temporal medical image analysis. LoMeVQA covers five tasks: progress classification, progress description, progress report generation, differential region grounding, and differential region description. To construct the dataset, we develop an automated pipeline that (1) organizes patient records chronologically, (2) extracts clinically meaningful entities via a medical knowledge graph, and (3) models their temporal evolution to guide large language models in generating high-quality longitudinal VQA pairs. Extensive evaluations demonstrate that both general-purpose and medical-domain MLLMs perform poorly on LoMeVQA, revealing substantial limitations in temporal reasoning. To address these limitations, we introduce MedLong-8B, which achieves state-of-the-art performance across all tasks. Beyond benchmarking, we conduct detailed analyses that uncover key failure modes and shed light on how to improve longitudinal medical visual reasoning. Our data is available at: https://github.com/pepperbubble/LoMeVQA

</details>


### [34] [SPFM-Net: Semantic-Prior-Guided Frequency-Constrained Mamba for Invisible Watermark Attack](https://arxiv.org/abs/2607.27811)
*Chunpeng Wang,Yanan Shi,Zhiqiu Xia,Jidong Yang,Suo Gao,Qi Li*

Main category: cs.CV

TL;DR: 提出SPFM-Net，一种基于语义先验引导和频率约束的Mamba框架，用于不可见水印攻击，通过高比例掩码、部分微调的MAE重建、多尺度残差频率特征交互和轻量级Mamba全局状态空间建模，在去除水印与保持视觉保真度之间取得平衡。

Research relevance: 弱相关（2分）。该论文属于泛视觉/图像处理领域，核心任务是invisible watermark attack（不可见水印攻击），与遥感、卫星、在轨目标检测均无直接关系。虽然文中使用了"lightweight Mamba-based"模块，但轻量化仅是模型内部设计的一部分，并非面向部署优化的核心目标，且不涉及遥感场景或目标检测任务。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向相关性较弱，属于图像水印攻击领域。仅Mamba轻量级建模和多尺度频率特征交互两个技术点有间接借鉴价值，建议作为泛读材料，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：invisible watermark attack（不可见水印攻击）/ watermark removal。应用场景：图像水印去除，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：水印信号，非遥感目标。

Model architecture: SPFM-Net包含以下关键模块：(1) high-ratio masking（高比例掩码）破坏水印信号空间连贯性；(2) 部分微调的pretrained Masked Autoencoder (MAE) 从稀疏观测中重建语义一致图像并抑制水印信息；(3) Multi-scale Residual Frequency Feature Interaction (MRFFI) 模块聚合多感受野的水印残差特征；(4) lightweight Mamba-based Global State-space Feature Modeling (GSFM) 单元捕获全局长程依赖并分离水印特征与自然内容。整体采用多级目标函数联合优化。

Lightweight method: 文中提及"lightweight Mamba-based GSFM unit"，即采用Mamba（状态空间模型）作为轻量级全局特征建模单元，相比Transformer可降低计算复杂度。但摘要未说明具体的参数量、FLOPs或压缩手段（如pruning、quantization、knowledge distillation、NAS等），轻量化并非本文核心贡献。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。虽然Mamba架构本身具有线性复杂度优势，但本文未针对边缘/星上部署进行优化或评估。

Datasets and metrics: 实验覆盖四类水印方案：spatial-domain、transform-domain、orthogonal moment-based、deep learning-based watermarking schemes。具体数据集名称和评价指标（如PSNR、SSIM等）摘要未说明。

Experiments: 在代表性空间域、变换域、正交矩和深度学习水印方案上进行广泛实验，结果表明SPFM-Net在watermark attack effectiveness与perceptual fidelity之间取得了有利的权衡。具体对比方法和消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星/目标检测完全无关，属于图像水印攻击领域；(2) 摘要未提供任何部署相关指标（参数量、FLOPs、延迟等）；(3) 未说明在遥感图像上的适用性；(4) 轻量化仅体现在Mamba模块设计上，缺乏系统性的模型压缩评估。

Ideas for my research: 尽管整体方向不相关，但以下技术点对轻量化研究有间接借鉴价值：(1) Mamba/State Space Model作为轻量级全局特征建模单元，可用于替代遥感目标检测中的Transformer backbone，在保持长程依赖建模能力的同时降低计算复杂度；(2) 多尺度残差频率特征交互（MRFFI）思想可迁移到遥感小目标检测中，用于增强多尺度特征表达；(3) 部分微调pretrained MAE的策略可借鉴到遥感模型迁移学习中，减少在轨微调的计算开销。但需注意这些仅是方法层面的间接启发，需自行验证在遥感场景的适用性。

Abstract: Existing watermark attacks typically rely on predefined signal-processing operations or locally constrained restoration networks, making it difficult to capture the long-range dependencies of globally distributed watermark signals and resulting in an unfavorable trade-off between removal effectiveness and visual fidelity. In this paper, we propose SPFM-Net, a semantic-prior-guided and frequency-constrained Mamba framework for invisible watermark attack. SPFM-Net first employs high-ratio masking to disrupt the spatial coherence of invisible watermark signals, and then utilizes a partially fine-tuned pretrained Masked Autoencoder to reconstruct semantically consistent image from sparse observations while suppressing watermark-related information. A Multi-scale Residual Frequency Feature Interaction module subsequently aggregates watermark-related residual features across multiple receptive fields, while adaptively suppressing responses from watermark-irrelevant regions. To further capture the long-range dependencies of globally distributed watermark signals, a lightweight Mamba-based Global State-space Feature Modeling (GSFM) unit is introduced to separate watermark-related features from natural image content and suppress the remaining watermark traces. In addition, SPFM-Net is optimized using a multi-level objective that jointly imposes spatial-, frequency-, and edge-domain constraints, enabling effective watermark suppression while preserving perceptual quality. Extensive experiments on representative spatial-domain, transform-domain, orthogonal moment-based, and deep learning-based watermarking schemes demonstrate that SPFM-Net achieves a favorable trade-off between watermark attack effectiveness and perceptual fidelity.

</details>


### [35] [Hallucinations Leave a Grounding Signature:Verifier-Guided Decoding for Selective Object Correction](https://arxiv.org/abs/2607.27823)
*Lei Yang,Xinze Liu,Dayan Wu,Ding Wang,Hengjie Zhu,Zihao Zhang,Tianzhu Hu,Hanqi Wu,Peng Fu,Zheng Lin*

Main category: cs.CV

TL;DR: 提出基于内在接地签名（IGS）的验证器引导解码（VGD）框架，通过轻量验证器在生成过程中检测并回滚高风险的物体幻觉，选择性抑制幻觉物体以降低LVLM的object hallucination，同时保持视觉理解与真实物体覆盖率。

Research relevance: 弱相关（2分）。该论文属于泛视觉-语言模型（LVLM）领域的object hallucination缓解方法，不涉及遥感、卫星、Earth observation、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务本身（其"object"指图像描述中提及的物体，而非检测框），更不涉及剪枝、量化、蒸馏、NAS等模型压缩或星上部署。唯一与本研究方向的交叉点是其提出的"lightweight verifier"（轻量验证器）概念，以及KV cache回滚机制，在计算效率优化思路上有一定借鉴意义，但整体与遥感在轨目标检测轻量化方向弱相关。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，主要属于LVLM幻觉缓解领域。虽然"轻量验证器+选择性干预"的思路在计算效率优化上有一定启发，但缺乏遥感场景、目标检测任务和模型压缩方法等核心要素，阅读优先级较低。若对LVLM幻觉或解码策略感兴趣可略读，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大型视觉-语言模型（LVLM）的object hallucination缓解（图像描述生成中的幻觉物体抑制）。应用场景：通用图像captioning/视觉问答，非遥感场景。传感器类型：通用自然图像（MSCOCO等），非遥感传感器。目标类别：图像中出现的通用物体（如CHAIR/AMBER基准中的常见物体），非舰船/飞机/车辆等遥感目标。

Model architecture: 提出Intrinsic Grounding Signature (IGS)——一种分布式有符号注意力模式，编码了物体提及是否具有视觉证据支持的信息。基于IGS构建Verifier-Guided Decoding (VGD)框架：一个轻量验证器（lightweight verifier）在生成过程中检查每个新出现的物体提及，当判定为高风险时回滚KV cache，抑制该物体及其同义词，并重新生成受影响的后续文本。VGD仅对高风险物体提及进行干预，以保持模型原有视觉理解与真实物体覆盖率。

Lightweight method: 摘要中提到的轻量化元素有限：1) 使用"lightweight verifier"（轻量验证器）来检查物体提及，而非对整个模型进行压缩；2) 通过KV cache回滚机制实现选择性干预，避免对全部生成过程进行粗粒度干预，从而在计算上更高效。但摘要未说明该verifier的具体参数量、FLOPs、推理延迟等量化指标，也未涉及剪枝、量化、知识蒸馏、NAS等经典模型压缩方法。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。虽然"lightweight verifier"暗示了轻量设计，但缺乏具体量化数据，无法评估其在轨部署可行性。

Datasets and metrics: 数据集：CHAIR（CHAIR-MSCOCO）和AMBER-G。评价指标：CHAIR指标（CHAIR_i/CHAIR_s）、AMBER-G CHAIR、@rec90（召回率90%时的CHAIR值）、grounded-object coverage（真实物体覆盖率）。实验表明VGD在@rec90下将AMBER-G CHAIR降低43.6%，同时保留99.6%的真实物体覆盖率；在CHAIR-MSCOCO上将CHAIR_i/CHAIR_s分别降低37.0%/30.4%，且不缩短caption长度。

Experiments: 在CHAIR和AMBER-G两个基准上与现有object hallucination缓解方法对比，VGD达到state-of-the-art效果：@rec90下AMBER-G CHAIR降低43.6%，保留99.6% grounded-object coverage；CHAIR-MSCOCO上CHAIR_i/CHAIR_s分别降低37.0%/30.4%，且不缩短caption。摘要未提供与具体基线方法的逐项对比表、消融实验细节或计算开销对比。

Limitations: 1) 与遥感/卫星/在轨目标检测完全无关，属于通用视觉-语言模型领域；2) 摘要未说明lightweight verifier的具体架构、参数量与计算开销，无法评估其轻量化程度；3) 未涉及目标检测任务（无检测框、无IoU/mAP等检测指标）；4) 未涉及任何模型压缩方法（剪枝/量化/蒸馏/NAS）；5) 未提供推理延迟、吞吐量等效率数据；6) 仅在通用图像captioning基准上验证，未在遥感图像上测试。

Ideas for my research: 1) "选择性干预"思想可借鉴：在遥感在轨目标检测中，可设计轻量验证器对检测结果进行置信度校准，仅对高风险误检进行二次处理，避免全图重推理，降低计算开销；2) KV cache回滚/选择性抑制机制可类比为检测后处理中的"误检抑制"策略，在轻量化检测器中用轻量判别器过滤误检，替代重模型重推理；3) IGS（内在接地签名）的"注意力模式作为证据"思路，可用于遥感小目标检测中判断特征是否真正对应目标，辅助轻量模型提升检测可靠性；4) 其"保持覆盖率的同时降低误检"的权衡思路，对遥感目标检测中precision-recall平衡有参考价值。但整体而言，该论文方法迁移到遥感在轨检测需要大量改造，借鉴价值有限。

Abstract: Large vision-language models (LVLMs) often hallucinate objects that are absent from an image. Despite recent progress, existing mitigation methods still lack reliable object-level grounding diagnostics and therefore tend to apply coarse-grained interventions, which can impair visual understanding, shorten responses, and reduce coverage of genuinely grounded objects. The key challenge is thus to detect, during generation, whether each emerging object mention is supported by reliable visual evidence, so that hallucination can be mitigated selectively. Yet output confidence reflects next-token plausibility rather than visual support, allowing language priors to make absent objects appear certain. We show that the missing diagnostic evidence is encoded in an Intrinsic Grounding Signature (IGS), a distributed signed attention pattern that remains informative for such confident hallucinations. Based on IGS, we propose Verifier-Guided Decoding (VGD), a decoding framework in which a lightweight verifier examines each emerging object mention, rolls back the KV cache when the mention is identified as high risk, suppresses the object and its synonyms, and regenerates the affected continuation. Because VGD intervenes only on object mentions identified as high risk, it reduces object hallucination while preserving the model's original visual understanding and grounded object coverage. Experiments on CHAIR and AMBER-G show that VGD achieves state-of-the-art object hallucination reduction: at @rec90, it cuts AMBER-G CHAIR by 43.6\% while retaining 99.6\% of grounded-object coverage, and reduces CHAIR-MSCOCO CHAIR$_i$/CHAIR$_s$ by 37.0\%/30.4\% without shortening captions.

</details>


### [36] [Thinking Once Is Enough: Intermediate-Layer Evidence Routing for High-Resolution VQA](https://arxiv.org/abs/2607.27830)
*Zhongkuan Mao,Xianjie Liu,Tianyu Meng,Yidong Wang,Wenzhuo Zhao,Ronghao Xian,Yao Jiang,Fei Shen,Junfeng Fang,Yong Dai,Yi Zhang,Keren Fu*

Main category: cs.CV

TL;DR: 提出 Thinking-Once，一种免训练、单次视觉编码的证据路由方法，通过重构中间层注意力窗口来保留细粒度视觉证据，避免多轮裁剪/重编码，在 HR-VQA 任务上提升精度并大幅降低峰值内存和推理时间。

Research relevance: 弱相关（2分）。论文属于多模态大语言模型（MLLM）的高分辨率视觉问答（HR-VQA）任务，与遥感/卫星场景无关，也不涉及目标检测。但其"免训练、单次视觉编码、证据路由"的思路在降低计算开销和内存方面对轻量化部署有一定借鉴意义，属于泛视觉/泛机器学习方法。

Reading priority: 2分。论文与遥感、卫星、目标检测均无直接关系，属于多模态大模型推理优化方向。但其"免训练、单次编码、证据路由"的推理加速和内存优化思路对轻量化部署有一定启发，可作为泛化参考，不建议作为核心文献精读。

<details>
  <summary>Details</summary>

Task and scene: 任务：高分辨率视觉问答（HR-VQA），即多模态大语言模型在高分辨率图像上的问答。场景：通用视觉问答，非遥感场景。传感器类型：通用自然图像（如 V*Bench、HRBench 等基准数据集）。目标类别：不涉及目标检测，无具体目标类别。

Model architecture: 基于多模态大语言模型（MLLM），在 Qwen2.5-VL-7B、ZwZ-8B 等五个基础模型上验证。核心模块是"中间层路由窗口"（intermediate-layer routing window），通过重构 question-conditioned attention 来保留核心实体 token 和紧凑的背景上下文，并将证据路由到后续层，无需额外的视觉编码。

Lightweight method: 免训练（training-free）方法，核心是单次视觉编码（single-visual-pass），避免传统 HR-VQA 中反复裁剪、re-encoding、多轮搜索带来的额外计算。通过证据路由（evidence routing）在中间层保留细粒度信息，减少后续层的冗余计算。属于推理阶段的计算/内存优化，而非剪枝、量化、蒸馏或 NAS 等传统模型压缩方法。

Onboard deployability: 摘要提到平均峰值内存降低约 4GB；与 DeepScan 相比，V*Bench 推理时间降低 97.2%。但未说明具体参数量、FLOPs、延迟（绝对数值）、显存（绝对数值）、能耗等部署指标。由于是 7B/8B 级别的大模型，且面向通用视觉问答而非遥感在轨场景，直接星上部署可行性低。

Datasets and metrics: 数据集：V*Bench、HRBench-4K、HRBench-8K。指标：三个基准的平均分数（cross-benchmark mean）、峰值内存（peak memory）、推理时间（inference time）。对比了 11 个开源 HR-VQA baseline。

Experiments: 在五个基础模型上，Thinking-Once 一致提升或持平基线：V*Bench、HRBench-4K、HRBench-8K 平均提升 +3.1、+3.0、+2.7 分，峰值内存降低约 4GB。在 Qwen2.5-VL-7B 上三个基准分别提升 +9.9、+4.6、+5.5 分，cross-benchmark mean 从 72.5 提升到 79.1。在 ZwZ-8B 上达到 82.7 的均值。与 DeepScan 相比，V*Bench 推理时间降低 97.2%，cross-benchmark mean 从 77.8 提升到 79.1。

Limitations: 摘要未说明：具体参数量、FLOPs、绝对延迟数值、能耗、部署平台（FPGA/NPU/TensorRT）等。未涉及遥感图像、SAR、高光谱、多光谱等传感器类型。未涉及目标检测任务。方法依赖大语言模型的中间层注意力机制，对轻量级检测模型的可迁移性未验证。未说明在低算力边缘设备上的表现。

Ideas for my research: 1. "单次视觉编码 + 中间层证据路由"的思想可借鉴到遥感在轨目标检测：在轻量检测网络中，通过中间层特征路由保留小目标/细粒度特征，避免多次上采样或重复特征提取带来的计算开销。2. "免训练"的推理优化思路对星上部署有价值——在轨场景难以在线训练，免训练优化方法（如推理时特征路由、注意力重构）可直接部署。3. 峰值内存降低约 4GB 的思路提示：在轨硬件内存受限时，可通过中间层信息保留而非重复编码来降低内存峰值。4. 推理时间降低 97.2% 的对比说明"减少重复视觉处理"是显著加速方向，可类比到遥感图像中避免多尺度重复推理。

Abstract: High-resolution visual question answering (HR-VQA) is often treated as a problem of insufficient evidence acquisition, where failing multimodal large language models must inspect images again through cropping, re-encoding, or multi-round search. We show that this view is incomplete: in many cases, fine-grained evidence has already survived visual encoding and become identifiable and influential within an intermediate-layer routing window, but is later diluted before answer generation. We propose Thinking-Once, a \textbf{training-free, single-visual-pass} evidence-routing method that reconstructs question-conditioned attention at this window, preserves core entity tokens and compact background context, and routes this evidence to later layers without extra visual encoding. Across five base models, Thinking-Once consistently improves or matches the corresponding base setting, increasing the average scores on V$^*$Bench, HRBench-4K, and HRBench-8K by \textit{+3.1}, \textit{+3.0}, and \textit{+2.7} points while reducing the average peak memory by about 4,GB. On Qwen2.5-VL-7B, it improves the three benchmarks by \textit{+9.9}, \textit{+4.6}, and \textit{+5.5} points, raising the cross-benchmark mean from 72.5 to 79.1. With the ZwZ-8B base model, Thinking-Once reaches a mean score of 82.7. Against 11 open-source HR-VQA baselines, it obtains the best or tied-best score on all three benchmark averages and the best overall mean; for example, compared with DeepScan, it reduces V$^*$Bench inference time by \textbf{97.2\%} while improving the cross-benchmark mean from 77.8 to 79.1. These results show that HR-VQA can be improved by routing already encoded evidence rather than repeatedly acquiring new visual inputs. Code is available in the appendix.

</details>


### [37] [SAFViT: Spatial Attention Fusion Gating for Vision Transformer-Based Nucleus Segmentation and Classification](https://arxiv.org/abs/2607.27835)
*Harshit Mittal,Arash Rabbani*

Main category: cs.CV

TL;DR: 提出一种用于数字病理学细胞分割与分类的Spatial Attention Fusion (SAF) Gating模块，通过融合编码器与解码器特征生成逐像素"信任热图"，替代传统skip connection，在PanNuke数据集上提升多类panoptic质量（mPQ 0.471）。

Research relevance: 与本研究方向弱相关（2分）。论文属于数字病理学（digital pathology）细胞分割与分类任务，既非遥感/卫星场景，也非目标检测任务，更不涉及任何轻量化或部署方法。它属于泛视觉/医学图像分割方法，仅在"注意力门控机制"这一通用技术层面可能对遥感检测中的特征融合有间接借鉴意义。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，仅"注意力门控特征融合"这一通用技术思路有间接借鉴价值，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：细胞分割（cell segmentation）与多类分类（multi-class classification）；应用场景：数字病理学（digital pathology），用于诊断与治疗规划；传感器类型：病理切片显微图像（非遥感传感器）；目标类别：细胞多类（含minority "Dead"类）。

Model architecture: 基于CellViT（Vision Transformer + encoder-decoder）架构，提出Spatial Attention Fusion (SAF) Gating模块替代传统skip connection。每个SAF gate将encoder skip特征与上采样后的decoder特征拼接，通过两个pointwise convolution（中间含ReLU）压缩，再应用channel-wise softmax生成逐像素"信任热图"（per-pixel heatmap of trust），每个空间位置求和为1，让网络学习每个信息源的信任权重。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS或轻量网络设计等任何模型压缩/加速方法。SAF模块本身仅使用pointwise convolution，计算开销相对较低，但论文未给出FLOPs、参数量或延迟等量化数据。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息，也未讨论边缘或星上部署可行性。

Datasets and metrics: 数据集：PanNuke和MoNuSeg（均为病理学细胞分割数据集）；评价指标：multi-class panoptic quality (mPQ)、各类别F1 score（重点为Dead类F1）；对比方法：no gating、attention gates、squeeze-and-excitation、CBAM、cross-attention、attentional feature fusion共六种门控替代方案。

Experiments: SAF Gating在PanNuke上取得最高mPQ（0.471），相比无门控的CellViT基线，Dead类F1提升14.5个百分点。与六种门控替代方案（含SE、CBAM、cross-attention、AFF等）进行了系统对比。

Limitations: 1. 任务领域为数字病理学，与遥感/卫星/在轨目标检测完全无关；2. 摘要未提供计算开销、参数量、推理速度等部署指标；3. 未讨论模型在资源受限环境下的可行性；4. 仅验证了细胞分割场景，未在遥感图像或自然图像目标检测上验证；5. 提升主要来自minority类（Dead），对多数类性能影响未充分说明。

Ideas for my research: 1. SAF Gating的"逐像素信任热图"思想可迁移到遥感小目标检测的FPN特征融合中——遥感图像中背景占比极高、小目标稀疏，类似病理学中的minority类问题，可用类似门控机制让网络学习"哪里更信任哪层特征"；2. pointwise convolution + channel-wise softmax的轻量门控设计本身计算开销低，可作为轻量化特征融合模块的候选方案；3. 该工作对"多源特征融合中冗余/冲突信息"问题的处理思路，对遥感多光谱/多传感器特征融合有借鉴价值；4. 但需注意：该模块未做任何轻量化优化，若用于星上部署需额外评估其FLOPs与参数量。

Abstract: Accurate cell segmentation and classification are foundational to digital pathology, enabling quantitative tissue analysis for diagnosis and treatment planning. Encoder-decoder architectures that fuse multi-scale features through skip connections have become the dominant paradigm for this task, yet standard direct skip connections treat every spatial location equally, which leads to redundant and potentially conflicting information reaching the decoder. To overcome this problem, various gating mechanisms have been introduced, but most of them operate solely on filtering encoder information, neglecting the benefit of global contextual information from the decoder. This study proposes replacing conventional skip connections in a CellViT-based model with a novel Spatial Attention Fusion (SAF) Gating module. Each SAF gate concatenates the encoder skip and upsampled decoder features, compresses them through two pointwise convolutions with an intermediate ReLU, and applies a channel-wise softmax to produce a per-pixel "heatmap of trust" that sums to unity at every spatial location, allowing the network to learn where each source is most trustworthy. The resulting fused features improve the model's ability to detect the minority "Dead" class, which in turn enhances the multi-class panoptic quality (mPQ) on the PanNuke dataset. SAF Gating is compared against six gating alternatives including no gating, attention gates, squeeze-and-excitation, CBAM, cross-attention, and attentional feature fusion on PanNuke and MoNuSeg datasets. SAF Gating achieves the highest mPQ (0.471), a gain driven primarily by a 14.5-point improvement in Dead-class F1 score compared to ungated CellViT baseline.

</details>


### [38] [FeatFix: Reuse What You Verify through Local Exact-Feature Correction for Faster Cached Diffusion Inference](https://arxiv.org/abs/2607.27842)
*Hanshuai Cui,Zhiqing Tang,Zhi Yao,Qianli Ma,Fanshuai Meng,Weijia Jia*

Main category: cs.CV

TL;DR: 提出FeatFix，一种针对扩散模型缓存推理的局部精确特征校正方法，通过复用验证阶段已计算的精确块特征来重置局部草稿残差，在四个图像/视频骨干网络上实现最高6.70×加速。

Research relevance: 弱相关（2分）。论文聚焦扩散模型（diffusion models）的推理加速，属于生成模型加速领域，与遥感、卫星、目标检测均无直接关联。虽然其"缓存中间特征+精确特征校正"的思路在计算加速层面有一定借鉴意义，但场景完全不同，既非遥感目标检测，也非模型压缩/轻量化（剪枝、量化、蒸馏等）范畴。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，但"缓存特征+精确校正"的推理加速思路对特征复用类轻量化设计有一定启发。若时间有限可仅阅读方法部分了解其校正机制，不必深入复现。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：扩散模型（diffusion models）的图像/视频生成加速。应用场景：生成式AI推理加速，非遥感场景。传感器类型：不适用。目标类别：不适用。摘要未说明具体图像/视频生成任务细节。

Model architecture: FeatFix基于缓存扩散推理（cached diffusion inference）框架，在固定的稀疏layer-timestep站点上操作。核心机制：在验证站点将完整的草稿块输出（draft block output）替换为从相同输入状态计算得到的精确输出（exact output），避免token/channel级部分替换和全时间步重计算。涉及四个图像和视频骨干网络（具体架构名称摘要未说明）。

Lightweight method: 该方法属于training-free加速器（无需训练的推理加速），通过复用缓存中间特征（cached intermediate features）或预测未来特征来减少迭代去噪计算。FeatFix的独特贡献在于：将原本仅用于验证/度量差异的精确特征重新用于校正（correction），在验证站点重置局部草稿残差（draft residual），降低下游特征误差。这属于推理阶段计算复用/缓存策略，而非传统意义上的模型轻量化（剪枝、量化、蒸馏、NAS）。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅报告了相对Vanilla的加速比（最高6.70×），但未给出绝对延迟或硬件平台信息。由于面向扩散模型生成任务，与星上遥感目标检测部署场景差异较大。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括生成加速比（speedup，最高6.70× over Vanilla）和输出质量（output quality，具体指标如FID/IS等摘要未说明）。实验覆盖四个图像和视频骨干网络。

Experiments: 在四个图像和视频骨干网络上进行实验，FeatFix一致地加速生成过程，相对Vanilla实现最高6.70×加速，同时保持有竞争力的输出质量。摘要未提供与其他加速方法的详细对比结果、消融实验细节或具体质量指标数值。

Limitations: 1. 摘要未说明具体骨干网络名称、数据集、质量指标数值。2. 未报告绝对延迟、参数量、FLOPs等部署关键指标。3. 仅针对扩散模型生成任务，与遥感目标检测场景无直接关联。4. 未讨论该方法在检测/识别任务上的泛化性。5. 未涉及任何遥感数据（光学、SAR、高光谱等）或目标检测任务。

Ideas for my research: 1. "精确特征校正"思想可迁移：在遥感目标检测模型的轻量化推理中，若采用特征缓存/复用策略（如对相邻帧或相邻尺度复用特征），可借鉴FeatFix的思路——在关键层用精确计算的特征校正缓存特征的漂移（drift），而非仅丢弃或度量差异。2. 稀疏站点选择策略：FeatFix在固定的稀疏layer-timestep站点做校正，这种"稀疏关键点校正"思想可用于遥感检测网络的特征金字塔或骨干网络中的关键层轻量化设计。3. 但需注意：该方法面向生成模型，迁移到检测任务需要重新设计校正站点选择策略和误差度量方式，且遥感在轨场景更关注绝对延迟和能耗，而非相对加速比。

Abstract: Diffusion models are widely used to generate high-quality images and videos, but their iterative denoising process remains computationally intensive. A growing class of training-free accelerators reduces this cost by reusing cached intermediate features or forecasting future ones. To control draft drift, these methods sometimes compute an exact block feature for verification. Yet the resulting exact feature is typically used only to measure discrepancy or guide a later decision and is then discarded. We find that this previously computed feature can instead be reused for correction. Forwarding it at the verification site resets the local draft residual and reduces downstream feature error. Based on this observation, we introduce FeatFix, a local exact-feature correction method for cached diffusion inference. FeatFix operates at a fixed sparse set of layer--timestep sites. At each selected site, it replaces the complete draft block output with the exact output computed from the same incoming state, avoiding token- or channel-level partial replacement and full-timestep recomputation. Experiments across four image and video backbones show that FeatFix consistently accelerates generation, achieving a speedup of up to $6.70\times$ over Vanilla while maintaining competitive output quality.

</details>


### [39] [VCP-DCN: Beyond Visual Concealed Property via Depth Collaborative Network for Camouflaged Object Detection](https://arxiv.org/abs/2607.27843)
*Songsong Duan,Xi Yang,Nannan Wang*

Main category: cs.CV

TL;DR: 提出VCP-DCN深度协作网络，通过原型对比学习、多模态双注意力与深度自适应注入，挖掘RGB-D深度域中伪装目标的模态特有特征以提升COD检测性能。

Research relevance: 弱相关（2分）。该论文属于泛视觉领域中的Camouflaged Object Detection (COD)任务，不涉及遥感、卫星、Earth observation、SAR、高光谱或多光谱等遥感场景；也不涉及轻量化、剪枝、量化、知识蒸馏、NAS或边缘/星上部署。仅"目标检测/分割"这一任务层面与本研究方向有间接关联，但场景（RGB-D伪装目标）与遥感在轨目标检测差异显著。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向相关性较弱，属于通用RGB-D伪装目标检测任务。仅在多模态特征融合方法学层面有间接借鉴价值，且未涉及任何部署/轻量化内容。若时间有限，可跳过或仅快速浏览其多模态融合模块设计。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Camouflaged Object Detection (COD)，即伪装目标检测与分割。应用场景：复杂环境中识别与背景颜色/纹理相似的伪装目标（通用视觉场景，非遥感）。传感器类型：RGB-D（RGB图像+深度图）。目标类别：伪装目标（具体类别摘要未说明）。

Model architecture: VCP-DCN（Depth Collaborative Network），三阶段渐进式多模态对齐-交互-融合架构：(1) Alignment阶段：Separable Prototype Embedding (SPE)模块，通过prototype contrastive learning学习modality-consistency和modality-specific的RGB/depth prototype tokens；(2) Interaction阶段：Multi-modality Dual Attention (MDA)模块，利用modality-consistency prototype tokens与visual tokens之间的local response maps增强跨模态特征；(3) Fusion阶段：Depth Adaptive Injection (DAI)模块，通过决策机制计算RGB/depth modality-specific prototype tokens与modality-consistency tokens的相似度距离，自适应衡量RGB/depth特征贡献。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（无pruning、quantization、knowledge distillation、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该模型为多模态RGB-D网络，结构复杂（含prototype contrastive learning、dual attention、adaptive injection等模块），从设计上看计算开销较大，不具备星上/边缘部署可行性。

Datasets and metrics: 在三个权威数据集上进行了大量实验（具体数据集名称和评价指标摘要未说明）。

Experiments: 摘要仅说明"Extensive experiments demonstrate the effectiveness of our VCP-DCN on three authoritative datasets"，未给出具体对比方法、消融实验细节或定量结果。

Limitations: (1) 场景为通用RGB-D伪装目标检测，与遥感/卫星场景无关；(2) 未提供任何轻量化或部署相关信息；(3) 摘要未给出具体数据集名称、指标和定量结果；(4) 多模态prototype对比学习+双注意力+自适应注入的架构复杂度较高，推理开销可能较大；(5) 深度图在遥感在轨场景中通常不可用（遥感主要依赖光学/SAR/高光谱），该方法的输入模态假设难以直接迁移。

Ideas for my research: 尽管场景不相关，但以下方法学思想可借鉴：(1) prototype contrastive learning用于学习modality-consistency与modality-specific特征——在遥感多光谱/高光谱目标检测中，可借鉴此思路区分不同波段/通道的共享与特有特征；(2) 自适应特征贡献衡量机制（DAI模块的决策机制）——在遥感多传感器融合（如光学+SAR）中，可自适应评估各模态对检测的贡献权重；(3) 多模态对齐-交互-融合的三阶段渐进式设计——可迁移到遥感多源数据融合框架。但需注意：这些借鉴点属于方法学层面的间接启发，且需自行补充轻量化设计（如剪枝、蒸馏、量化）以满足在轨部署约束。

Abstract: Camouflaged Object Detection (COD) aims to identify and segment camouflaged objects in complex environments, which are often concealed because their color and texture are similar to the background. Several existing COD methods introduce depth maps to boost detection performance via learning complementary RGB-D features, ignoring modality-specific characteristics of concealed objects in the depth domain. To address this issue, we propose a depth collaborative network, called VCP-DCN, to mine distinguishable multi-modality features beyond visual concealed prototype in depth domain. Specifically, VCP-DCN progressively performs multi-modality alignment, interaction, and fusion for the COD task. In the \textbf{alignment} stage, we propose a Separable Prototype Embedding (SPE) module to learn modality-consistency and modality-specific RGB/depth prototype tokens through prototype contrastive learning. Furthermore, we develop a Multi-modality Dual Attention (MDA) module to enhance the cross-modal feature representation through local response maps between modality-consistency RGB/depth prototype tokens and visual tokens on the \textbf{interaction} stage. Finally, we design a Depth Adaptive Injection (DAI) module to adaptively measure contribution of RGB/depth features with a decision-making mechanism, which calculates similarity distance between RGB/depth modality-specific prototype tokens and modality-consistency ones on the \textbf{fusion} stage. Extensive experiments demonstrate the effectiveness of our VCP-DCN on three authoritative datasets.

</details>


### [40] [Benchmarking Foundation and Large Language Models for Few-Shot Medical Image Segmentation](https://arxiv.org/abs/2607.27856)
*Jinghong Liu,Yuchuan Deng,Fanping Liu,Meng Huang,Xirong Li*

Main category: cs.CV

TL;DR: 提出FAME，一个统一的few-shot医学图像分割基准，用于系统评估不同范式的FS-MIS方法（专家模型、SAM-based、CLIP-based、MLLM-based），涵盖14,958个测试样本、7个解剖部位、9种成像模态和14个ROI类别。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于医学图像few-shot分割（FS-MIS），属于医学影像分析领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测或模型轻量化/部署。唯一可能的间接关联是few-shot学习范式，但应用场景和任务类型完全不同。

Reading priority: 1分。论文聚焦医学图像few-shot分割基准，与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及遥感数据、目标检测或模型压缩/部署。除非对few-shot评估方法论有特殊兴趣，否则不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：few-shot医学图像分割（FS-MIS）；应用场景：医学影像分析，覆盖7个解剖部位、9种成像模态、14个ROI类别；传感器类型：医学成像模态（摘要未具体说明，可能包括MRI、CT、超声等）；目标类别：医学感兴趣区域（ROI），非遥感目标。

Model architecture: 摘要未说明具体模型架构。论文是基准（benchmark）工作，评估四类方法：专家模型（specialists）、SAM-based方法、CLIP-based方法、MLLM-based方法。核心关注点在于不同方法如何利用support examples（直接视觉适应 vs. prompt-based策略）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：FAME基准，包含14,958个测试样本，覆盖7个解剖部位、9种成像模态、14个ROI类别。实验设置：zero-shot和ten-shot两种设置，额外评估target-absence recognition（目标缺失识别）以及covariate和semantic shifts下的泛化能力。评价指标：摘要未具体说明指标名称。

Experiments: 主要发现：（1）有效few-shot分割取决于模型如何利用support examples，直接视觉适应（direct visual adaptation）通常优于prompt-based策略；（2）增加support examples仅在模型能有效利用它们时才提升性能；（3）语义迁移（semantic transfer）比成像域适应（imaging-domain adaptation）更具挑战性，且强定位能力不必然意味着可靠的目标缺失识别。

Limitations: 摘要未说明局限性。但作为医学图像分割基准，其数据分布、任务设定、评估协议均面向医学影像，与遥感场景差异巨大。此外，摘要未提供任何模型效率、部署或轻量化相关信息。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴的间接思路：（1）few-shot学习范式——遥感在轨场景中标注样本稀缺，可借鉴其"如何有效利用少量support examples"的评估思路，设计遥感小样本目标检测的基准评估；（2）target-absence recognition（目标缺失识别）——在轨遥感场景中，模型需要判断"当前帧是否包含目标"，这一评估维度对星上智能决策有参考价值；（3）covariate/semantic shift下的泛化评估——遥感图像存在传感器差异、光照变化、季节变化等域偏移，可借鉴其系统评估域泛化的方法论。但整体借鉴价值有限。

Abstract: Few-shot medical image segmentation (FS-MIS) aims to segment novel regions of interest (ROIs) from a few annotated support examples. Despite rapid progress, existing FS-MIS solutions span diverse paradigms but are evaluated under inconsistent settings, leaving their relative effectiveness unclear. We introduce FAME, a unified benchmark for evaluating FS-MIS solutions, covering specialists, SAM-based methods, CLIP-based methods, and MLLM-based methods. FAME contains 14,958 test samples across 7 anatomical sites, 9 imaging modalities, and 14 ROI categories, and evaluates models under zero-shot and ten-shot settings with additional assessment of target-absence recognition and generalization under covariate and semantic shifts. Our evaluation reveals several findings. First, effective few-shot segmentation depends on how models exploit support examples: direct visual adaptation generally outperforms prompt-based strategies. Second, increasing support examples improves performance only when models can effectively utilize them. Third, semantic transfer remains substantially more challenging than imaging-domain adaptation, and strong localization ability does not necessarily imply reliable target-absence recognition. We hope FAME provides a comprehensive understanding of current FS-MIS solutions and facilitates the development of more effective and reliable few-shot medical segmentation methods.

</details>


### [41] [EEG-EditBench: Probing Visual Information in EEG-Image Retrieval Models with Controlled Image Edits](https://arxiv.org/abs/2607.27857)
*Kaifan Zhang,Lihuo He,Yuqi Ji,Junjie Ke,Lukun Wu,Tianhao You,Xinbo Gao*

Main category: cs.CV

TL;DR: 提出EEG-EditBench诊断基准，通过受控编辑（物体身份、属性、背景、存在性）来评估EEG-to-image检索模型究竟保留了哪些视觉信息，发现强检索性能并不一致地迁移到编辑式评估，细粒度属性变化最具挑战性。

Research relevance: 弱相关（2分）。该论文属于脑电信号（EEG）到图像检索的神经解码方向，属于泛视觉/泛机器学习方法，与遥感、卫星、在轨目标检测、轻量化部署均无直接关联。其核心贡献是构建诊断性基准来评估模型保留的视觉信息，而非遥感场景下的检测或压缩方法。

Reading priority: 1分。该论文属于EEG神经解码领域，与遥感卫星在轨目标检测及轻量化方向基本无关，仅"受控编辑诊断"的方法论思想有极有限的间接借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：EEG-to-image retrieval（脑电-图像检索）与视觉解码诊断。应用场景：认知神经科学、脑机接口（BCI）。传感器类型：EEG（脑电信号），非遥感传感器。目标类别：THINGS-EEG2数据集中的200类日常物体（如cheetah、tools、plants、vehicles等），与遥感目标（舰船、飞机、车辆）无关。

Model architecture: 摘要未说明具体模型架构。论文评估了八个代表性的EEG视觉解码模型（eight representative EEG visual decoding models），但未给出这些模型的网络结构细节。

Lightweight method: 摘要未说明。该论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息，且EEG解码任务本身与星上/边缘部署场景无关。

Datasets and metrics: 数据集：基于200 THINGS-EEG2测试图像构建的EEG-EditBench，包含2,137个质量受控的编辑样本，编辑维度包括object identity（物体身份）、attributes（属性）、background（背景）、object presence（物体存在性）。评价指标：摘要未明确给出具体指标名称，但涉及standard retrieval accuracy与edit-based evaluation的对比。

Experiments: 评估了八个代表性EEG视觉解码模型。主要发现：强标准检索性能（standard retrieval）并不一致地迁移到编辑式评估（edit-based evaluation），其中细粒度属性变化（fine-grained attribute changes）对模型最具挑战性。EEG-EditBench揭示了聚合检索准确率所隐藏的模型行为。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，属于脑电信号与视觉解码交叉领域；2. 不涉及任何轻量化或部署方法；3. 摘要未说明模型架构、计算开销、推理速度等关键信息；4. 数据集基于日常物体（THINGS-EEG2），非遥感场景数据。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但其"受控编辑诊断基准"（controlled-edit diagnostic benchmark）的方法论思想有一定借鉴价值：1. 可类比构建遥感场景下的"受控编辑诊断"——例如对遥感图像中的舰船/飞机目标进行属性（颜色、朝向、尺寸）、背景（海面/陆地/云层）、存在性等受控编辑，评估轻量化检测模型在细粒度变化下的鲁棒性，避免仅用mAP等聚合指标掩盖模型缺陷；2. 其"强聚合性能不一定迁移到细粒度评估"的发现，提示在轻量化压缩（剪枝/量化/蒸馏）后，除了关注FLOPs和mAP，还应评估模型对细粒度目标属性变化的保持能力。

Abstract: Recent EEG-to-image retrieval models have achieved strong performance in identifying viewed images from semantically diverse candidates. Yet such success does not reveal what visual information supports the match. A model may readily identify a cheetah among tools, plants, and vehicles, but can it still distinguish the viewed cheetah from the same scene with the cheetah replaced by a dog? Motivated by this question, we introduce EEG-EditBench, a diagnostic benchmark that examines this question through controlled edits of object identity, attributes, background, and object presence. Built from the 200 THINGS-EEG2 test images, EEG-EditBench contains 2,137 quality-controlled edits and evaluates eight representative EEG visual decoding models. Our results show that strong standard retrieval does not consistently transfer to edit-based evaluation, with fine-grained attribute changes presenting the greatest challenge. EEG-EditBench reveals model behavior hidden by aggregate retrieval accuracy and provides a controlled basis for studying what visual information EEG-image models preserve. The code and complete dataset are publicly available.

</details>


### [42] [Learning to Understand Body Language from Flight through Robust 3D Avatar Placing](https://arxiv.org/abs/2607.27865)
*Dragos Costea,Alina Marcu,Cristina Lazar,Marius Leordeanu*

Main category: cs.CV

TL;DR: 提出 Drones2BodyLanguage 数据集，通过在真实无人机航拍视频中放置具有十种交流意图的虚拟化身，并利用轻量级几何世界模型实现度量精确的放置与重渲染，用于训练长距离人体运动与意图感知的空中机器人。

Research relevance: 弱相关（2-3分）。论文涉及无人机（UAV）航拍视频和轻量级几何模型，但核心任务是"人体运动与意图感知"（human motion and intent perception），并非遥感目标检测。虽然使用了无人机航拍场景和轻量化几何建模，但既不是遥感卫星场景，也不涉及目标检测或模型压缩/部署。轻量化仅体现在"lightweight geometric world model"这一几何建模层面，与模型压缩（pruning/quantization/distillation）无关。

Reading priority: 2 分。论文核心是无人机人体意图感知与数据合成，与遥感卫星在轨目标检测及轻量化方向弱相关。仅数据合成方法论和几何建模思路有少量可借鉴之处，不建议作为重点阅读对象。

<details>
  <summary>Details</summary>

Task and scene: 任务：长距离人体运动与意图感知（human motion and intent perception）；场景：无人机（UAV）航拍视频；传感器：4K 无人机摄像头；目标类别：十种交流意图（communicative intents）的虚拟化身（avatars）。非遥感卫星场景，非目标检测任务。

Model architecture: 核心是一个轻量级几何世界模型（lightweight geometric world model）：通过流式单目深度（streaming monocular depth）将语义选定的锚点（semantically selected anchors）提升到 3D，预测放置点作为仿射锚点组合（affine anchor combination），权重具有刚性不变性（rigid-invariant weights），并通过 SVD 拟合的地面旋转（SVD-fitted ground rotation）进行重渲染。评估了十二种架构（twelve architectures）。

Lightweight method: 摘要中"lightweight"仅指几何世界模型本身的轻量设计（轻量级几何建模），并非模型压缩/加速方法。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS 等轻量化技术。摘要未说明。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及"lightweight geometric world model"，但未给出具体计算开销或实时性数据。与星上部署（onboard deployment）无关。

Datasets and metrics: 数据集：Drones2BodyLanguage，基于真实无人机 4K 航拍视频，放置具有十种交流意图的虚拟化身，保持度量精确的位置、尺度、朝向，跨数百帧相机运动。评估指标：mean intent accuracy（平均意图准确率）。实验设置：scene- and motion-disjoint splits（场景与运动不相交划分），在真实（real）、重定向（retargeted）和生成（generated）运动上评估，并在两个 in-the-wild 场景上验证。

Experiments: 在十二种架构上，使用放置数据训练后，真实、重定向和生成运动的平均意图准确率均有大幅提升，且在两个野外场景上得到验证。摘要未给出具体数值和与基线方法的详细对比。

Limitations: 1. 与遥感卫星目标检测及轻量化部署方向基本无关，借鉴价值有限；2. 摘要未说明模型计算开销、参数量、推理速度等部署关键指标；3. 任务为人体意图感知而非目标检测，场景为低空无人机而非卫星遥感；4. 未涉及任何模型压缩/加速技术。

Ideas for my research: 1. 其"轻量级几何世界模型"中利用单目深度将 2D 锚点提升到 3D 并保持刚性不变性的思路，可借鉴到遥感目标检测中利用几何先验（如地物空间关系）增强小目标定位；2. 数据合成/放置（placing virtual objects into real scenes）的方法论，可迁移到遥感领域合成带标注的舰船/飞机目标数据，缓解遥感标注稀缺问题；3. 场景与运动不相交划分（scene- and motion-disjoint splits）的评估策略，对遥感跨场景泛化评估有参考意义。但整体借鉴价值有限。

Abstract: Perceiving human motion and intent at long range is a prerequisite for socially intelligent aerial robots, yet the data to learn it barely exists. We introduce Drones2BodyLanguage, a dataset grounding human motion in real UAV footage: avatars manifesting ten communicative intents are placed into unmodified 4K drone scenes with metrically correct position, scale and orientation, maintained over hundreds of frames of camera motion. Enabling it is a lightweight geometric world model of the local scene - semantically selected anchors lifted to 3D through streaming monocular depth - in which a placement point is predicted as an affine anchor combination with provably rigid-invariant weights, and re-rendered under an SVD-fitted ground rotation. Across twelve architectures on scene- and motion-disjoint splits, training on placed data lifts mean intent accuracy by a wide margin for real, retargeted and generated motion alike, with gains confirmed on two in-the-wild scenes.

</details>


### [43] [DECODE: Tackling Representation and Decision Degradation in Continual AI-Generated Image Detection](https://arxiv.org/abs/2607.27882)
*Zihao Cai,Xinghan Li,Ruiyan Yang,Xue Song,Haijun Shan,Jingjing Chen*

Main category: cs.CV

TL;DR: 提出DECODE解耦持续检测框架，通过子空间多样性正则化（SDR）和闭式决策对齐（CDA）同时缓解AI生成图像检测中的表征级和决策级遗忘问题。

Research relevance: 弱相关（2分）。论文聚焦AI生成图像检测（AI-generated image detection）的持续学习问题，属于泛视觉/泛机器学习范畴，与遥感、卫星、在轨部署、目标检测均无直接关联。虽然涉及"detection"和"continual learning"概念，但检测对象是AI生成图像而非遥感目标，场景和任务均不匹配。

Reading priority: 2分。与遥感在轨目标检测及轻量化方向弱相关，但持续学习中"双重退化"的视角和闭式决策对齐方法对遥感增量学习场景有一定启发，可作为边缘参考阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI生成图像检测（AI-generated image detection）的持续学习（continual learning）。应用场景：数字取证（digital forensics），非遥感场景。传感器类型：不适用（处理的是自然图像/生成图像）。目标类别：区分真实图像与AI生成图像（19个生成域）。

Model architecture: DECODE解耦持续检测框架，包含两个核心模块：Subspace Diversity Regularization (SDR)用于保持多样化的取证表征，Closed-Form Decision Alignment (CDA)用于在每次adapter合并后重新校准共享分类头。整体采用adapter-based持续学习范式。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。虽然adapter机制本身具有参数高效特性，但论文并未从轻量化角度阐述。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。论文面向的是数字取证场景而非星上/边缘部署。

Datasets and metrics: 数据集：19个生成域（generative domains）用于持续学习训练，11个未见生成器（unseen generators）用于泛化测试。评价指标：平均准确率（average accuracy）、遗忘率（forgetting rate）。实验结果：19个生成域上平均准确率99.36%，遗忘率仅0.39%；11个未见生成器上泛化准确率95.36%。

Experiments: 在19个生成域上验证DECODE框架，达到99.36%平均准确率和0.39%遗忘率，并在11个未见生成器上达到95.36%准确率。论文揭示了"双重退化"（Dual Degradation）现象：即使特征表征保持判别性，分类头在新域持续优化时决策边界仍会漂移。

Limitations: 与遥感在轨目标检测方向基本无关。摘要未说明模型复杂度、推理速度、部署可行性等关键信息。持续学习框架虽有一定方法论价值，但未在遥感场景验证，也未涉及轻量化设计。

Ideas for my research: 1. 持续学习思想可借鉴：在轨遥感目标检测模型面临新传感器/新场景数据不断到来的情况，DECODE中"表征级+决策级"双重遗忘的视角可迁移到遥感目标检测的持续学习场景。2. CDA（闭式决策对齐）无需手动调参的特性，对星上自适应更新模型有一定参考价值。3. SDR（子空间多样性正则化）可用于缓解遥感目标检测模型在增量学习时的特征退化。4. 但需注意，论文未涉及轻量化，需自行结合剪枝/量化/蒸馏等方法。

Abstract: As generative models continue to evolve, AI-generated image detectors must incrementally adapt to emerging generative domains while preserving knowledge acquired from previous ones. This continual learning setting is particularly challenging because forensic traces are often subtle and generator-specific, making detectors highly vulnerable to catastrophic forgetting. Existing methods primarily address this problem by stabilizing feature representations, implicitly treating forgetting as a representation-level issue. In this paper, we show that this perspective is incomplete. We demonstrate that even when feature representations remain discriminative, the decision boundary can progressively drift as the classification head is continually optimized on new domains. These two effects jointly give rise to a compound failure mode, termed Dual Degradation. To overcome this challenge, we propose DECODE, a decoupled continual detection framework that jointly mitigates representation- and decision-level forgetting. Specifically, we introduce Subspace Diversity Regularization (SDR) to preserve diverse forensic representations and Closed-Form Decision Alignment (CDA) to recalibrate the shared classification head after each adapter merge without manual hyperparameter tuning. Extensive experiments on 19 generative domains show that DECODE achieves an average accuracy of 99.36% with only 0.39% forgetting, while further generalizing to 11 unseen generators with 95.36% accuracy.

</details>


### [44] [Unifying Adversarially Robust Model Experts in Vision-Language Models](https://arxiv.org/abs/2607.27897)
*Nguyen Duc Thai,Junhao Dong,Sua Qi Rong,Hua Yu,Yew-Soon Ong*

Main category: cs.CV

TL;DR: 提出CARE框架，通过嵌入空间对齐实现多个对抗微调专家模型的协同训练，将互补的鲁棒性知识整合到单一统一模型中，提升视觉语言模型（如CLIP）的对抗鲁棒性。

Research relevance: 弱相关（2分）。论文聚焦于视觉语言模型（VLM/CLIP）的对抗鲁棒性防御，属于泛视觉/泛机器学习方法。虽然涉及模型微调与知识整合，但既非遥感/卫星场景，也非目标检测任务，更不涉及轻量化或边缘/星上部署。仅"adversarial fine-tuning"和"knowledge exchange"在方法论层面可能对模型训练策略有间接借鉴意义。

Reading priority: 2分。论文与遥感/卫星/目标检测/轻量化方向基本无关，属于对抗鲁棒性领域的泛视觉方法。仅在"多专家协同训练+知识整合"的方法论层面有间接借鉴价值，且其鲁棒性增强思路对在轨模型抗干扰有一定启发。但整体优先级低，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：对抗鲁棒性防御（adversarial robustness defense），具体为对抗微调（adversarial fine-tuning）。应用场景：通用图像分类（classic image classification）和下游视觉语言任务（downstream vision-language tasks）。传感器类型：摘要未说明（非遥感传感器）。目标类别：摘要未说明（非遥感目标）。

Model architecture: 基于视觉语言模型（VLM，如CLIP）架构。核心是CARE框架：训练期间维护多个"robust model experts"（鲁棒模型专家），通过embedding-space harmonization（嵌入空间对齐/协调）实现专家间知识交换，最终将多个专家的知识整合（consolidate）为单一统一鲁棒模型。摘要未给出具体网络层结构细节。

Lightweight method: 摘要未涉及轻量化方法。论文不涉及pruning、quantization、knowledge distillation、NAS、实时推理或边缘部署。虽然"knowledge consolidation into a single unified model"在概念上类似模型集成压缩，但并非以轻量化为目标，而是以鲁棒性整合为目标。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。论文关注的是对抗鲁棒性而非部署效率，且面向通用VLM（如CLIP），其计算开销通常较大，与星上/边缘部署需求不匹配。

Datasets and metrics: 摘要提到在"classic image classification"和"downstream vision-language tasks"上进行实验，但未给出具体数据集名称（如ImageNet、CIFAR等均未提及）。评价指标方面，摘要未明确列出具体指标名称，仅提及"outperform individually learned model experts"，暗示以鲁棒性/准确率类指标衡量。

Experiments: 在两种具有互补鲁棒性行为的对抗微调策略上验证了CARE的有效性。实验表明CARE能够超越单独训练的模型专家（individually learned model experts）。摘要未提供具体数值结果、对比基线细节或消融实验信息。

Limitations: 1. 摘要未说明CARE在遥感/卫星图像上的适用性，VLM在遥感场景的对抗鲁棒性尚未验证。2. 摘要未提供计算开销、训练成本等信息，多专家协同训练可能带来额外训练负担。3. 摘要未说明最终统一模型相对单专家的参数量/推理开销变化。4. 摘要未涉及轻量化或部署约束，与在轨场景的实时性、资源受限需求脱节。

Ideas for my research: 1. 多专家协同训练思想可借鉴：在遥感目标检测中，可训练多个针对不同场景（如不同传感器、不同天气/光照条件）的专家模型，通过嵌入空间对齐整合为单一鲁棒模型，提升在轨检测模型对复杂遥感环境的适应性。2. "knowledge consolidation into a single model"的思路与模型集成压缩/蒸馏有相通之处，可探索将多专家知识蒸馏到轻量检测网络中，同时兼顾鲁棒性与轻量化。3. 对抗鲁棒性对在轨部署有实际意义——卫星在轨模型可能面临对抗扰动或噪声干扰，CARE的鲁棒性增强思路可迁移到遥感检测模型的鲁棒训练中。4. 但需注意：CARE本身不解决轻量化问题，需结合pruning/quantization/distillation等手段才能满足星上部署约束。

Abstract: Vision-language models (VLMs), such as CLIP, are vulnerable to adversarial attacks, posing a serious problem for real-life applications and deployment. Adversarial fine-tuning emerges as a prominent defense method; however, different fine-tuning strategies often produce specialized models with distinct robustness characteristics. Each fine-tuned model in turn thrives in some evaluation settings but falters on others, limiting their defensive capabilities. We refer to these specialized fine-tuned models as robust model experts and propose a collaborative adversarial fine-tuning framework: CARE - Collaborative Adversarial Robustness fine-tuning using Embedding alignment. CARE maintains multiple experts during training, enables knowledge exchange through embedding-space harmonization, and consolidates the learned knowledge into a single unified robust model. Experts benefit from one another while preserving their individual specializations, enabling the final model to inherit complementary robustness properties. In this paper, we demonstrate CARE on two different adversarial fine-tuning strategies with complementary robustness behaviors. Extensive experiments on classic image classification and downstream vision-language tasks display the effectiveness of our approach, with CARE being able to outperform individually learned model experts. The results suggest that collaborative learning across model experts is a promising direction for improving adversarial robustness.

</details>


### [45] [CoRE-UIR: Prior-guided common and residual experts for efficient all-in-one remote sensing image restoration](https://arxiv.org/abs/2607.27898)
*Zaiyan Zhang,Qiangqiang Yuan,Jie Li,Ziyang Lihe,Yu Wan,Yuzeng Chen,Xin Su,Liangpei Zhang*

Main category: cs.CV

TL;DR: 提出CoRE-UIR，一种基于"公共+残差专家"分解的先验引导全局-局部图像复原框架，通过低秩残差专家和冻结CLIP先验嵌入实现多退化图像复原，在提升PSNR的同时大幅降低计算开销和峰值内存。

Research relevance: 弱相关（2-3分）。论文面向UAV和卫星遥感图像，属于遥感/卫星场景，且涉及图像复原（image restoration）而非目标检测（object detection）。虽然论文强调效率（11.83× faster、峰值内存降低85.3%），但轻量化手段是低秩专家分解，并非剪枝/量化/蒸馏等典型模型压缩方法，且任务本身是图像复原而非目标检测。因此与"遥感卫星在轨目标检测与模型轻量化"方向相关性较弱，但低秩分解和效率-质量权衡思路对轻量化设计有借鉴意义。

Reading priority: 2分。论文面向遥感图像复原且强调效率优化，但任务（图像复原）与目标检测不直接相关，轻量化手段（低秩专家分解）也非典型模型压缩方法。可作为效率-质量权衡设计的参考阅读，但对在轨目标检测轻量化课题的直接借鉴价值有限。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：All-in-One图像复原（Universal Image Restoration），处理UAV和卫星遥感图像中的恶劣天气、光照变化、成像伪影等复合退化。传感器类型：UAV和卫星光学遥感图像。目标类别：不涉及目标检测，属于图像级复原任务。数据集：构建了MDVD-108K（Multi-Degradation VisDrone）大规模UAV复原数据集，含单退化和复合退化，另有真实世界测试集。

Model architecture: 核心模块为Common-and-Residual Expert Block (CoRE)，将复原能力显式分解为：1) 公共稠密专家（common dense expert）负责退化不变复原；2) 低秩残差专家（low-rank residual experts）负责退化特定补偿，避免冗余专家复制。此外包含：Degradation Prior Embedding (DPE)将冻结的CLIP特征适配为显式复原先验；Global Feature Modulation (GFM)在局部残差补偿前对齐全局特征统计量。整体为prior-guided global-local框架。

Lightweight method: 轻量化手段为低秩专家分解（low-rank residual experts），通过将多专家设计从full-rank改为低秩残差补偿，减少参数冗余和计算开销。摘要未说明是否使用剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS。摘要未说明具体参数量（parameters）和FLOPs数值，仅报告了相对加速比（11.83× faster）和峰值内存降低（85.3%）。

Onboard deployability: 摘要报告了效率指标：相对最强基线BaryIR运行速度快11.83×，峰值内存降低85.3%。但摘要未说明绝对延迟（latency）、参数量（parameters）、FLOPs、能耗（energy consumption）或是否适配FPGA/NPU/TensorRT等边缘/星上部署平台。由于任务为图像复原而非目标检测，且未给出绝对部署指标，星上部署可行性无法直接判断。

Datasets and metrics: 数据集：构建MDVD-108K（Multi-Degradation VisDrone）大规模UAV复原数据集，覆盖单退化和复合退化；另有真实世界测试集。评价指标：PSNR（峰值信噪比），报告整体平均PSNR提升1.05 dB。此外在downstream tasks和unseen degradation上验证了泛化性。摘要未说明是否使用SSIM等其他指标。

Experiments: 在多个数据集上与最强基线BaryIR对比：平均PSNR提升1.05 dB，运行速度快11.83×，峰值内存降低85.3%。在downstream tasks和unseen degradation上验证了泛化性。摘要未说明具体对比方法列表、消融实验细节及各模块（CoRE/DPE/GFM）的独立贡献。

Limitations: 1) 任务为图像复原而非目标检测，与在轨目标检测方向不直接相关；2) 摘要未报告绝对参数量、FLOPs、延迟等部署关键指标，仅给出相对加速比；3) 未说明是否在真实星载硬件（FPGA/NPU）上验证；4) 未说明低秩残差专家在极端退化或未知退化下的鲁棒性边界；5) 依赖CLIP特征作为先验，可能引入额外计算和存储开销，且CLIP在星上部署的可行性未讨论；6) 摘要未说明模型在UAV/卫星图像上的复原是否对后续目标检测精度有定量增益。

Ideas for my research: 1) 低秩专家分解思想可迁移到目标检测头的轻量化设计：将检测头中多个任务分支（分类/回归/旋转框）分解为公共稠密分支+低秩残差分支，减少参数冗余；2) "公共+残差"分解思路可用于轻量化检测骨干网络，将退化不变特征与退化特定特征分离，提升在轨复杂成像条件下的鲁棒性；3) 效率-质量权衡的评估方式（相对加速比+峰值内存+PSNR）值得借鉴，但需补充绝对FLOPs/参数量/延迟指标；4) 构建复合退化数据集（如MDVD-108K）的思路可迁移到遥感目标检测的域适应/退化鲁棒性评测；5) 若将图像复原作为在轨目标检测的前处理模块，可借鉴其低秩设计降低前处理开销，但需评估端到端检测精度增益。

Abstract: Remote sensing images acquired by unmanned aerial vehicles (UAVs) and satellites are often degraded by adverse weather, illumination variation, and imaging artifacts, which may co-occur and jointly induce global distribution shifts and local structural corruption. Although All-in-One image restoration offers an appealing unified alternative to task-specific pipelines, existing methods still suffer from weak or implicit degradation cues and parameter redundancy caused by full-rank multi-expert designs with overlapping restoration behaviors. We propose CoRE-UIR (Common and Residual Experts for Universal Image Restoration), a prior-guided global-local framework centered on the Common-and-Residual Expert Block (CoRE). CoRE explicitly decomposes restoration capacity into a common dense expert for degradation-invariant restoration and low-rank residual experts for degradation-specific compensation, enabling adaptive specialization without redundant expert replication. Built on this design, Degradation Prior Embedding (DPE) adapts frozen CLIP features into an explicit restoration-oriented prior, while Global Feature Modulation (GFM) aligns global feature statistics before local residual compensation. We also construct MDVD-108K (Multi-Degradation VisDrone), a large-scale UAV restoration dataset covering both single and compound degradations, together with a real-world test set. Extensive experiments on multiple datasets show that CoRE-UIR improves the overall average PSNR by 1.05 dB while running 11.83$\times$ faster and reducing peak memory by 85.3% relative to the strongest baseline, BaryIR, thereby maintaining a favorable quality-efficiency trade-off. Evaluations on downstream tasks and unseen degradation also validate the generalizability of CoRE-UIR. The code and dataset will be released at https://github.com/zzaiyan/CoRE-UIR.

</details>


### [46] [One Patch Is Enough: Reinforcement-Optimized Visual Token Grounding for MLLM-Based Scene Text Spotting](https://arxiv.org/abs/2607.27902)
*Rui Tang,Wentao Yang,Peirong Zhang,Yongxin Shi,Shun Zhang,Huiguo He,Lianwen Jin*

Main category: cs.CV

TL;DR: 提出SPaTS，一种基于单锚点视觉token路由的视觉中心场景文本检测框架，通过强化学习优化token选择、方向嵌入对齐和全图增强解码，提升MLLM在密集/小文本实例上的定位与识别精度。

Research relevance: 弱相关（2分）。该论文聚焦于scene text spotting（场景文本检测与识别），属于OCR/MLLM方向，并非遥感/卫星/地球观测场景。虽然涉及object detection中的小目标与密集目标定位问题，但目标类别是文本而非遥感地物（舰船、飞机、车辆等），且完全不涉及轻量化、剪枝、量化、蒸馏、边缘/星上部署等模型压缩与加速内容。其方法（视觉token路由、强化学习优化、方向嵌入对齐）属于泛视觉/多模态大模型方法，对遥感目标检测的借鉴价值有限。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关。虽然其小目标/密集目标定位和token路由思想有一定借鉴价值，但场景（OCR文本）、架构（MLLM）和部署约束（无轻量化考虑）均与本研究方向差异较大，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：scene text spotting（场景文本检测与识别），属于OCR与MLLM交叉任务。应用场景：自然图像中的文本定位与识别，非遥感场景。传感器类型：普通RGB自然图像。目标类别：文本实例（text instances），包括密集和小尺寸文本。

Model architecture: SPaTS框架：基于Multimodal Large Language Model (MLLM)的视觉中心架构。核心模块包括：(1) Single-Patch routing——将每个文本实例路由到单一anchor visual token；(2) Single-Patch Selective Optimization (SPaSO)——强化学习框架，用patch-level rewards优化离散视觉token选择；(3) Directional Embedding Alignment (DEA)——解耦特征幅度与方向，抑制不稳定范数偏差；(4) Patch-Enhanced Decoding (PED)——将路由的anchor与语言语义融合，并在全图特征图上cross-attend进行几何感知的边界回归。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（无pruning、quantization、knowledge distillation、NAS、轻量网络设计等）。相反，其基于MLLM的架构通常计算开销较大，与轻量化方向相悖。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。基于MLLM的框架通常参数量大、推理开销高，不具备星上/边缘部署可行性。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及与"frontier closed-source MLLMs"和"OCR MLLMs"进行对比实验，具体benchmark（如ICDAR系列、Total-Text等）未在摘要中给出。

Experiments: 摘要仅说明"Extensive experiments demonstrate that SPaTS consistently and significantly outperforms both frontier closed-source MLLMs and OCR MLLMs"，即实验表明SPaTS在场景文本检测任务上显著优于前沿闭源MLLM和OCR MLLM。具体数值结果、消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星/地球观测场景完全无关，目标类别（文本）与遥感地物目标差异巨大；(2) 基于MLLM的框架计算开销大，未考虑轻量化与部署约束；(3) 摘要未提供任何部署相关指标（参数量、FLOPs、延迟等）；(4) 未说明在遥感图像上的泛化能力；(5) 强化学习训练（SPaSO）可能带来训练不稳定性和收敛问题，摘要未讨论。

Ideas for my research: 尽管整体弱相关，但以下思路可迁移到遥感小目标检测：(1) "Single-Patch routing"思想——将每个目标路由到单一anchor token，可类比遥感小目标检测中减少冗余anchor/query的设计，降低计算冗余；(2) Directional Embedding Alignment (DEA)——解耦特征幅度与方向来抑制范数偏差，对遥感图像中光照/尺度变化导致的特征不稳定可能有借鉴意义；(3) Patch-level reward的强化学习优化思路，可用于遥感目标检测中优化检测框选择策略；(4) 但需注意：MLLM架构本身不适合星上部署，若借鉴应将其思想迁移到轻量CNN/Transformer检测器中。

Abstract: Scene text spotting requires high-precision alignment between textual recognition and spatial localization. While visual-token grounding has emerged as a promising formulation for Multimodal Large Language Models (MLLMs), the previous multi-patch paradigm often introduces redundant noise and localization ambiguity, particularly for dense or small text instances. To address this, we propose Single-Patch Text Spotting (SPaTS), a vision-centric framework that routes each text instance through a single anchor visual token and then recovers geometry via full-image refinement. To accurately identify this anchor without oracle labels, we introduce Single-Patch Selective Optimization (SPaSO), a reinforcement learning framework that optimizes discrete visual-token selection using patch-level rewards. To further improve representation robustness and localization precision, we introduce Directional Embedding Alignment (DEA) to suppress unstable norm bias by decoupling feature magnitude and direction, and Patch-Enhanced Decoding (PED) to fuse the routed anchor with language semantics and cross-attend over the full-image feature map for geometry-aware boundary regression beyond coordinate-space surrogates. Extensive experiments demonstrate that SPaTS consistently and significantly outperforms both frontier closed-source MLLMs and OCR MLLMs. Code will be released soon.

</details>


### [47] [ARD-REFSM: Enhancing Reflection Symmetry Detection with Asymmetric Denoising and Rotation Equivariance](https://arxiv.org/abs/2607.27927)
*Dongfu Yin,Rourou Su,Cong Zhao,Fei Yu*

Main category: cs.CV

TL;DR: 提出ARD模块和REFSM模块用于反射对称性检测，通过抑制非对称区域干扰和增强旋转等变性特征匹配，并构建了新的GMSYM基准数据集。

Research relevance: 弱相关（2分）。该论文聚焦于反射对称性检测（reflection symmetry detection），属于通用计算机视觉任务，并非遥感/卫星场景。虽然对称性检测在遥感场景中可能有潜在应用（如建筑物、飞机等目标的结构对称性），但论文本身未涉及遥感图像、卫星数据或在轨部署。同时，论文未涉及任何轻量化方法（pruning, quantization, distillation等），也未讨论部署相关指标。因此属于泛视觉方法，与本课题方向弱相关。

Reading priority: 2分。该论文与遥感在轨目标检测及轻量化方向弱相关，不涉及遥感数据、目标检测或模型压缩。仅ARD的背景抑制思想和REFSM的旋转等变性对遥感旋转目标检测有间接借鉴价值，可作为补充阅读而非核心参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：反射对称性检测（reflection symmetry detection）。应用场景：通用图像对称性分析，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：对称图案/结构，非特定遥感目标。

Model architecture: 包含两个核心模块：(1) Asymmetric Region Denoising (ARD)模块——抑制非对称区域干扰以细化对称图案；(2) Rotation Equivariant Feature Similarity Matching (REFSM)模块——通过原始图像与旋转图像之间的特征相似性匹配增强旋转等变性。采用双输入（dual-input）框架，利用rotation loss最大化原始与旋转图像score map的一致性，实现旋转等变的对称轴预测。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（如剪枝、量化、知识蒸馏、NAS等），也未讨论轻量化网络设计。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息，也未讨论实时推理或边缘/星上部署可行性。

Datasets and metrics: 在四个标准数据集（DENDI, NYU, LDRS, SDRW）以及自建的GMSYM基准数据集上进行实验。GMSYM数据集将图像按不同场景分类并引入多种干扰因素。评价指标为准确率（accuracy）和鲁棒性（robustness），具体指标名称摘要未说明。

Experiments: 在DENDI、NYU、LDRS、SDRW四个标准数据集及GMSYM数据集上，所提方法在准确率和鲁棒性上均达到state-of-the-art性能。消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星场景完全无关，未使用任何遥感数据（optical, SAR, hyperspectral等）；(2) 未涉及目标检测任务，而是对称性检测这一更细分的视觉任务；(3) 未讨论任何轻量化或部署问题；(4) 未提供计算复杂度、推理速度等工程指标；(5) 旋转等变性增强方法在计算开销上可能较大，但摘要未说明。

Ideas for my research: 尽管整体弱相关，但有两个技术点可借鉴：(1) ARD模块的"非对称区域抑制"思想——在遥感小目标检测中，背景杂波（如海面、复杂地物）可类比为"非对称干扰区域"，类似的自适应背景抑制机制可能有助于提升小目标检测精度；(2) REFSM模块的旋转等变性增强——遥感目标（如飞机、舰船）常呈任意朝向，旋转等变特征表示对旋转框检测（rotated object detection）有潜在借鉴价值，可考虑在轻量检测网络中引入旋转等变特征增强模块。但需注意这些借鉴需要重新设计以适配遥感场景和轻量化约束。

Abstract: Reflection symmetry detection remains challenging due to interference from asymmetric regions and arbitrary orientations of symmetric patterns. Asymmetric regions introduce background clutter that disrupts symmetric pattern matching, whereas conventional convolutional neural networks lack rotation equivariance, leading to inconsistent feature representations under rotational transformations. To address these issues, we propose an Asymmetric Region Denoising (ARD) module and a Rotation Equivariant Feature Similarity Matching (REFSM) module. The ARD module suppresses asymmetric interference to refine symmetric patterns, while the REFSM module enhances rotation equivariance through feature similarity matching between original and rotated images. Specifically, our dual-input REFSM framework leverages rotation loss to maximize consistency between the score maps of original and rotated images, thereby enabling precise prediction of rotation-equivariant symmetry axes. Furthermore, we introduce GMSYM, a new benchmark dataset that categorizes images into diverse scenarios and incorporates various interferences to address the limitations of existing reflection symmetry detection benchmarks. Extensive experiments on four standard datasets (DENDI, NYU, LDRS, SDRW) and our proposed GMSYM dataset demonstrate that our method achieves state-of-the-art performance in both accuracy and robustness.

</details>


### [48] [LAST: The Last Query Token Guides Visual Token Pruning for Edge-Cloud Collaborative MLLM Inference](https://arxiv.org/abs/2607.27952)
*Feng Yang,Xinrui Ju,Keyang Zhang,Xiandong Meng,Rongqun Lin,Howard Leung,Shiqi Wang,Haoliang Li,Chris Xing Tian*

Main category: cs.CV

TL;DR: 提出LAST，一种面向边缘-云协同MLLM推理的免训练查询相关视觉token剪枝框架，利用边缘端紧凑VLM作为引导代理，基于因果注意力下最后一个查询token对视觉token的注意力信号实现查询感知的token剪枝，在保留12.5%视觉token的情况下保持95.4%的完整token精度。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，聚焦于边缘-云协同的多模态大语言模型（MLLM）推理中的视觉token剪枝，并非遥感/卫星场景，也不涉及目标检测任务。虽然涉及"边缘部署"和"token剪枝"（一种轻量化/压缩思路），但其对象是MLLM的视觉token序列而非遥感目标检测模型，且不涉及遥感图像、卫星数据或目标检测。轻量化思想（token pruning）对遥感大模型或视觉Transformer类检测器的token稀疏化有一定借鉴意义，但整体与"遥感卫星在轨目标检测模型轻量化"方向弱相关。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，属于泛视觉MLLM推理优化。其token剪枝和边缘-云协同思想有一定借鉴价值，但并非遥感场景，也不涉及目标检测任务，优先级较低。若课题涉及遥感大模型或Transformer检测器的token稀疏化，可作参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：边缘-云协同MLLM推理中的视觉token剪枝（visual token pruning）。应用场景：边缘设备编码视觉输入为token，云端通用MLLM进行推理。传感器类型：通用视觉输入（非遥感专用）。目标类别：不涉及具体目标检测类别。摘要未说明遥感、SAR、高光谱、多光谱等遥感相关数据。

Model architecture: LAST框架包含两个核心组件：(1) 边缘端紧凑VLM（compact edge-side VLM）作为引导代理（guidance proxy），用于生成轻量级重要性信号；(2) 基于因果注意力（causal attention）机制，利用最后一个查询token（last query token）对视觉token的注意力作为重要性度量。该设计无需访问云端模型内部状态、无需自回归生成、无需对多个查询位置进行昂贵聚合。在固定token预算下保留一组多样化的查询相关视觉token。

Lightweight method: 核心方法是token剪枝（token pruning），属于模型输入侧的稀疏化/压缩手段。具体特点：(1) 免训练（training-free），无需额外训练；(2) 查询感知（query-aware）剪枝，利用因果注意力下最后一个查询token的注意力信号；(3) 边缘端紧凑VLM作为引导代理，避免依赖云端MLLM内部状态；(4) 在固定token预算下保留多样化查询相关token。该方法属于推理时动态token稀疏化，而非传统的剪枝/量化/蒸馏/NAS等模型压缩方法。

Onboard deployability: 摘要提及"低边缘端选择开销"（low edge-side selection overhead）和"减少云端计算"（reduced cloud-side computation），并给出token保留比例（12.5%）。但摘要未说明具体的参数量、FLOPs、延迟、显存、能耗等量化部署指标。由于该方法面向边缘-云协同而非星上在轨部署，且未给出硬件平台（FPGA/NPU/TensorRT）相关信息，星上部署可行性无法直接评估。

Datasets and metrics: 在11个多模态基准（11 multimodal benchmarks）上评估，采用多个token预算（multiple token budgets）设置，与不同引导策略的剪枝方法对比。主要指标为精度保持率（保留95.4%的完整token精度）和token保留比例（12.5%）。摘要未说明具体数据集名称和具体指标细节。

Experiments: 在11个多模态基准上，与不同引导策略（vision-driven、query-guided、compact guidance models）的剪枝方法对比。LAST在多个token预算下一致取得最强性能，保留12.5%视觉token时保持95.4%的完整token精度，且边缘端选择开销低、云端计算减少。摘要未提供具体对比数值和消融实验细节。

Limitations: (1) 与遥感场景无关，未涉及遥感图像特性（如小目标、旋转目标、复杂背景）；(2) 面向MLLM的token剪枝，而非目标检测模型；(3) 摘要未说明边缘端紧凑VLM的具体规模、训练方式及部署成本；(4) 未给出量化部署指标（FLOPs、延迟、能耗等）；(5) 依赖因果注意力结构，对非因果注意力或非Transformer架构的适用性未知；(6) 未讨论在轨/星上硬件约束下的可行性。

Ideas for my research: (1) token剪枝思想可迁移到遥感目标检测中的视觉Transformer骨干（如DETR类检测器），通过查询感知的token稀疏化降低计算量；(2) "免训练+引导代理"的设计思路可借鉴：在星上部署时，可用一个轻量引导网络决定哪些特征/区域需要被云端或更强模型处理，实现边缘-云协同；(3) 因果注意力下"最后一个查询token注意力"作为重要性信号的低开销设计，可启发遥感检测中基于注意力图的自适应区域选择；(4) 固定token预算下的多样性保留策略，可类比遥感图像中在有限计算预算下保留小目标/稀疏目标区域的思路；(5) 但需注意，该方法面向MLLM而非检测器，迁移到遥感目标检测需要重新设计重要性信号与任务对齐。

Abstract: Multimodal foundation models are reshaping edge-cloud visual intelligence from task-specific feature pipelines into token-based interfaces, where edge devices encode visual inputs into tokens for a general-purpose cloud MLLM. However, dense visual-token sequences increase cloud-side inference costs. Existing pruning methods mainly target centralized inference: vision-driven methods can operate before cloud execution but are typically query-agnostic, whereas query-guided methods often rely on internal states of the target MLLM and cannot determine token relevance before transmission. Compact guidance models offer an alternative, but existing designs may require costly attention aggregation or auxiliary generation. We propose LAST, a training-free framework for query-dependent visual token pruning in edge-cloud collaborative MLLM inference. LAST uses a compact edge-side VLM as a guidance proxy and derives a lightweight importance signal from the last query token's attention to visual tokens. Under causal attention, the last query token can attend to the full visual sequence and the entire query context, enabling query-aware pruning without cloud-model access, autoregressive generation, or costly aggregation over multiple query positions. LAST then retains a diverse set of query-relevant visual tokens under a fixed token budget. We evaluate LAST on 11 multimodal benchmarks under multiple token budgets against pruning methods with different guidance strategies. Experiments show that LAST consistently achieves the strongest performance, preserving 95.4% of the full-token accuracy while retaining only 12.5% of the visual tokens, with low edge-side selection overhead and reduced cloud-side computation.

</details>


### [49] [FiRE: Enhancing MLLMs with Fine-Grained Context Learning for Complex Image Retrieval](https://arxiv.org/abs/2607.27959)
*Bohan Hou,Haoqiang Lin,Xuemeng Song,Haokun Wen,Meng Liu,Yupeng Hu,Xiangyu Zhao*

Main category: cs.CV

TL;DR: 提出一种自动化的细粒度多模态五元组数据集构建流程和两阶段细粒度多模态微调策略，用于提升多模态大语言模型（MLLM）在长文本-图像检索、视觉对话检索和组合图像检索（CIR）等复杂任务上的零样本检索性能。

Research relevance: 弱相关（2分）。该论文属于多模态大语言模型（MLLM）在图像检索任务上的方法研究，属于泛视觉/泛机器学习方法。虽然摘要中提到了"lightweight MLLM backbone"，但这只是作为对比实验中的一个设置，并非论文的核心贡献，且完全不涉及遥感、卫星、在轨部署或目标检测。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅在两阶段微调策略和细粒度上下文建模方面可能对多模态遥感理解有间接借鉴意义。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于通用多模态图像检索领域。虽然两阶段微调策略有一定方法论上的参考价值，但对本课题的直接帮助很小，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态图像检索（image retrieval），包括long-text-to-image retrieval、visual dialog retrieval、composed image retrieval (CIR)。应用场景：通用视觉检索，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（通用图像检索，非特定目标类别）。

Model architecture: 基于Multimodal Large Language Models (MLLMs)作为universal image retriever。核心设计包括：(1) 自动化细粒度多模态五元组数据集构建pipeline；(2) 两阶段细粒度微调策略——阶段一为fine-grained context reasoning-oriented fine-tuning（增强上下文理解），阶段二为fine-grained retrieval-oriented fine-tuning（增强查询-目标对齐）。具体backbone结构摘要未说明。

Lightweight method: 摘要中仅提及"a more lightweight MLLM backbone"作为对比实验中的设置，表明作者在实验中使用了比对比方法更轻量的MLLM backbone，但并未提出任何具体的轻量化方法（如剪枝、量化、蒸馏、NAS等）。轻量化不是本文的核心贡献。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。仅提到使用了更轻量的MLLM backbone，但未给出具体数值。

Datasets and metrics: 在五个数据集上进行了实验，涵盖多种复杂图像检索任务。具体数据集名称、评价指标（如Recall@K等）摘要未说明。实验设置为zero-shot retrieval settings。

Experiments: 在五个数据集上的零样本检索实验中，该方法显著优于现有方法（"remarkable superiority"），即使使用更轻量的MLLM backbone也能取得更好效果。具体对比方法、消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星/目标检测完全无关，属于通用多模态检索领域；(2) 轻量化仅作为实验设置提及，未提出任何模型压缩或加速方法；(3) 未提供任何部署相关指标（参数量、FLOPs、延迟等）；(4) 数据集和评价指标细节未在摘要中给出；(5) 两阶段微调策略的泛化性是否适用于遥感场景未经验证。

Ideas for my research: (1) 两阶段微调策略（先上下文理解再检索/对齐）的思想可借鉴到遥感目标检测的fine-tuning流程中，例如先进行遥感场景理解预训练，再进行特定目标检测微调；(2) 细粒度上下文建模对于遥感小目标检测可能有启发——遥感图像中上下文信息（如舰船周围的海面、机场跑道等）对检测小目标至关重要；(3) 如果未来研究涉及遥感多模态大模型（如遥感图像-文本检索、遥感视觉问答），本文的CIR数据集构建pipeline和微调策略可作参考；(4) 但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Due to their strong generalizable multimodal processing and reasoning capabilities, Multimodal Large Language Models (MLLMs) have demonstrated significant potential as universal image retrievers, effectively addressing diverse real-world image retrieval tasks. Nevertheless, pioneering studies, while promising, overlook the potential of fine-grained context modeling and disentangled fine-tuning objectives in enhancing MLLMs' retrieval performance, particularly for complex tasks such as long-text-to-image retrieval, visual dialog retrieval, and composed image retrieval (CIR). Therefore, in this work, we propose an automated fine-grained multimodal quintuple dataset construction pipeline and a novel two-stage fine-grained multimodal fine-tuning strategy. The dataset generation pipeline produces a comprehensive CIR dataset with fine-grained image captions and modification text, facilitating fine-grained context modeling. Beyond the previously entangled fine-tuning paradigm, our approach separates the fine-tuning process into two distinct stages: (1) fine-grained context reasoning-oriented fine-tuning and (2) fine-grained retrieval-oriented fine-tuning. These stages aim to sequentially enhance the model's context understanding and query-target alignment capabilities, thereby improving retrieval performance. Extensive experiments across five datasets encompassing diverse and complex image retrieval tasks demonstrate the remarkable superiority of our method over existing approaches in zero-shot retrieval settings, even with a more lightweight MLLM backbone compared to those methods.

</details>


### [50] [FootprintNet: State-Transition-Guided Dynamic Footprint Learning for Multi-temporal Remote Sensing Change Detection](https://arxiv.org/abs/2607.27969)
*Haotian Zhang,Hao Chen,Han Guo,Zhengxia Zou,Zhenwei Shi*

Main category: cs.CV

TL;DR: 提出Urban Building Dynamics Detection (UBDD)任务，用于从多时相遥感影像中检测建筑变化的动态足迹（变化发生的时间区间），并设计了FootprintNet网络，通过状态-动作交互建模和变化边界线索增强特征对比，配合新的Building Change Dynamics Score (BCDS)指标来评估预测与标签的时间接近度。

Research relevance: 3分。论文属于遥感场景（remote sensing multi-temporal change detection），且涉及遥感影像中的建筑变化检测（building change detection），与遥感目标检测有一定相关性。但核心任务是"变化检测"（change detection）而非"目标检测"（object detection），且完全不涉及轻量化、模型压缩、在轨部署等方向。因此与"遥感卫星在轨目标检测与模型轻量化"课题的相关性为中等偏弱——遥感场景相关，但任务类型（变化检测vs目标检测）和轻量化维度均不匹配。

Reading priority: 2分。论文与遥感场景相关，但核心任务是多时相变化检测而非目标检测，且完全不涉及轻量化与在轨部署。对课题的借鉴价值有限，仅在时序建模和评价指标设计上有一定启发意义，建议低优先级阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多时相变化检测（multi-temporal change detection, MTCD），具体为建筑变化动态足迹检测（Urban Building Dynamics Detection, UBDD），输出像素级分类掩码（pixel-wise classification masks）。应用场景：城市建筑动态变化监测，与人类活动相关的重复变化区域识别。传感器类型：多时相遥感影像（multi-temporal imagery），具体传感器类型摘要未说明。目标类别：建筑变化（building-change），包括单次变化和多次变化（multi-change）类别。

Model architecture: FootprintNet：将建筑变化过程抽象为潜在状态（latent states）与动作（actions）之间的交互，施加状态-动作转移约束（state-action transition constraints）以引导因果一致的变化轨迹学习。利用时间变化边界线索（temporal change-boundary cues）增强边界两侧的特征对比，提升不同动态足迹的判别能力。具体网络结构细节（backbone、层数、模块设计）摘要未说明。

Lightweight method: 摘要未说明。论文未涉及任何轻量化方法（如pruning、quantization、knowledge distillation、NAS、轻量网络设计等），也未讨论模型压缩或加速策略。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关信息，也未讨论星上或边缘部署可行性。由于是多时相变化检测任务，通常需要处理多帧影像，计算开销可能较大，但摘要未提供任何量化数据。

Datasets and metrics: 数据集：TSCD、MUDS、WUSU三个多时相变化检测数据集。评价指标：除常规指标外，提出Building Change Dynamics Score (BCDS)，用于评估预测结果对变化语义的保持程度以及与标签的时间偏移（temporal offsets）。常规指标具体名称（如IoU、F1等）摘要未说明。

Experiments: 在TSCD、MUDS、WUSU三个数据集上进行了广泛实验，FootprintNet优于当前state-of-the-art方法。具体对比方法、消融实验细节、定量结果数值摘要未说明。

Limitations: 1. 任务为变化检测而非目标检测，与课题核心任务不直接匹配；2. 完全未涉及轻量化、模型压缩或部署优化；3. 未报告计算复杂度、参数量、推理速度等部署关键指标；4. 多时相处理可能带来较高的计算和存储开销，但未讨论；5. 仅针对建筑变化场景，泛化到其他目标类别（舰船、飞机、车辆等）未验证。

Ideas for my research: 1. 状态-动作交互建模（state-action transition constraints）思想可借鉴：在遥感目标检测中，可考虑将时序变化建模为状态转移过程，增强对动态目标的检测能力；2. 时间变化边界线索（temporal change-boundary cues）增强特征对比的思路，可用于多帧遥感影像中运动目标（如舰船、车辆）的检测，提升边界判别力；3. BCDS指标的设计思路（考虑预测与标签的时间接近度）可启发在轨检测中设计考虑时序一致性的评价指标；4. 多变化类别统一表示（independent multi-change class）的思想可用于处理遥感场景中目标多次出现/消失的复杂情况。

Abstract: Despite substantial progress in remote sensing multi-temporal change detection (MTCD), most existing MTCD methods still represent the dynamic process at each spatial location over the entire observation period using a single change category associated with the final observation. This implicit single-change assumption limits their ability to characterize regions of recurrent change closely related to human activities. To address this limitation, we introduce Urban Building Dynamics Detection (UBDD), which identifies building-change dynamic footprints, i.e., the temporal intervals in which changes occur, from multi-temporal imagery and produces pixel-wise classification masks. For regions undergoing two or more changes, UBDD introduces an independent multi-change class for unified representation, thereby enabling unified modeling of single- and multi-change processes. Furthermore, we propose FootprintNet, which abstracts building-change processes as interactions between latent states and actions, and imposes state-action transition constraints to guide the learning of causally coherent change trajectories. It further exploits temporal change-boundary cues to enhance feature contrast across boundary sides, thereby improving the discrimination among different dynamic footprints and enabling accurate detection of dynamic footprints. Moreover, we introduce the Building Change Dynamics Score (BCDS) to address the inability of conventional metrics to reflect the temporal proximity between predicted footprints and labels. It evaluates predictions according to their preservation of change semantics and temporal offsets from the corresponding labels. Extensive experiments on TSCD, MUDS, and WUSU demonstrate that FootprintNet outperforms current state-of-the-art methods. The code is available at https://github.com/zmoka-zht/FootprintNet.

</details>


### [51] [Now You Have My Healthy Attention: A U-DiT for Brain-MRI Inpainting](https://arxiv.org/abs/2607.27974)
*Danilo Danese,Angela Lombardi,Tommaso Di Noia*

Main category: cs.CV

TL;DR: 本文提出一种基于U-DiT架构的确定性回归模型，用于脑部MRI图像的区域修复（inpainting），通过约束注意力机制和利用对侧对称性先验来提升修复的解剖合理性。

Research relevance: 弱相关（1-2分）。本文是医学影像（脑部MRI）修复任务，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。唯一可借鉴的是其U-DiT架构中通过下采样token网格进行全局self-attention的设计思路，以及注意力约束方法，但这些属于泛视觉/泛机器学习方法，与遥感目标检测及轻量化方向关联很弱。

Reading priority: 1分。本文是医学影像修复任务，与遥感卫星在轨目标检测及轻量化方向基本无关，仅架构层面的U-DiT设计思路有极有限的参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学图像修复（inpainting）/ 局部合成（Local Synthesis）；应用场景：脑部MRI肿瘤区域健康组织补全；传感器类型：T1-weighted MRI（医学影像，非遥感传感器）；目标类别：健康脑组织区域补全，非目标检测任务。

Model architecture: 基于U-DiT（U-Net + Diffusion Transformer）原理的确定性回归模型：体素编码器-解码器结构，通过下采样全局self-attention block引入长程上下文，使用三维旋转位置编码（3D rotary position embeddings）；卷积和skip connections保留高频细节。关键创新：1) 约束注意力使被遮挡（void）token只关注同一volume中已知健康token，并加入对侧同源（contralateral homologue）的learned bias；2) 加入对侧对称性输入（contralateral-symmetry input）作为患者特定先验。

Lightweight method: 摘要未说明。本文未涉及剪枝、量化、知识蒸馏、NAS等轻量化方法，也未讨论模型压缩或加速策略。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗等部署相关信息，也未讨论实时推理或边缘/星上部署可行性。

Datasets and metrics: 数据集：ASNR-MICCAI BraTS Local Synthesis (Inpainting) task，官方BraTS-2026 validation leaderboard，219个病例；评价指标：SSIM（健康区域均值0.864）、PSNR（24.7 dB）、MSE（4.6×10⁻³）。

Experiments: 在官方BraTS-2026验证集上达到健康区域SSIM 0.864、PSNR 24.7 dB、MSE 4.6×10⁻³。进一步分析了distortion-optimal回归固有的残差平滑性及其对解剖真实性的影响。摘要未说明与其他方法的对比实验或消融实验细节。

Limitations: 1) 任务为医学图像修复，与遥感目标检测完全无关；2) 未涉及任何轻量化或部署优化；3) 未报告模型复杂度、推理速度等工程指标；4) 作者指出distortion-optimal回归会导致残差平滑，可能影响解剖真实性，说明该方法在生成质量上存在固有局限。

Ideas for my research: 1) U-DiT架构中"下采样token网格上进行全局self-attention"的设计思路，可借鉴到遥感大图目标检测中降低attention计算复杂度，配合轻量化部署；2) 注意力约束思想（只关注已知/有效区域）可类比遥感图像中遮挡、云覆盖区域的检测处理；3) 对称性先验思路在遥感中可类比为某些地物（如道路、建筑）的结构先验，但需谨慎评估适用性；4) 整体而言，本文对"遥感在轨目标检测轻量化"课题的直接借鉴价值有限。

Abstract: The ASNR-MICCAI BraTS Local Synthesis (Inpainting) task asks for the anatomically plausible completion of healthy brain tissue within a masked region of a T1-weighted MRI, providing a tumor-free anatomical reference for downstream analysis. As the task is scored by distortion metrics (SSIM, PSNR, MSE), we build a deterministic regression model and focus on giving it inductive biases tailored to inpainting. Our network follows the U-DiT principle of performing self-attention on a downsampled token grid: a volumetric encoder-decoder imports long-range context through a downsampled global self-attention block with three-dimensional rotary position embeddings, while convolutions and skip connections preserve high-frequency detail. Two ideas drive our results. First, we constrain the attention so that occluded ("void") tokens attend only to known-healthy tokens of the same volume, with a learned bias toward each query's contralateral homologue, forcing the completion to be inferred from observed anatomy rather than from other unknown regions. Second, we add a contralateral-symmetry input that supplies the mirrored healthy hemisphere as a patient-specific prior; since the brain is approximately bilaterally symmetric and lesions are typically unilateral, this prior improves the distortion metrics at matched structural similarity. On the official BraTS-2026 validation leaderboard our submission reaches a mean healthy-region SSIM of $0.864$, PSNR of $24.7$\,dB and MSE of $4.6{\times}10^{-3}$ over $219$ cases. We further analyse the residual smoothness inherent to distortion-optimal regression and discuss its implications for anatomical realism.

</details>


### [52] [ViP-Rig: Visual-Prompted Controllable Rigging](https://arxiv.org/abs/2607.27982)
*Zihan Qin,Mingze Sun,Yifan Mao,Jialei Xu,Jingfeng Guo,Changrong Hu,Wenbo Zhao,Junjun Jiang,Xianming Liu*

Main category: cs.CV

TL;DR: ViP-Rig是一个基于视觉提示的3D网格绑定（rigging）框架，通过将用户绘制的2D骨骼和刚性提示特征注入冻结的预训练骨干网络，实现提示优先的绑定和结果引导的编辑。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于3D动画网格绑定（mesh rigging）任务，属于计算机图形学/动画领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测或模型轻量化/部署。相关性评分：1分。

Reading priority: 1分。该论文属于3D动画绑定领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。仅当对"冻结骨干+适配器"的参数高效微调范式感兴趣时，可作泛化参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D网格绑定（mesh rigging），包括骨骼生成（Skeleton Generation）和蒙皮预测（Skinning Prediction）。应用场景：3D动画制作、角色绑定。传感器类型：不适用（非遥感传感器）。目标类别：3D网格、骨骼结构、蒙皮权重。

Model architecture: ViP-Rig采用两阶段架构：第一阶段Skeleton Generation使用Dense-to-Compact Visual Prompt Encoding将骨骼草图编码为紧凑的固定长度条件token，通过gated adapters注入冻结的预训练自回归生成器；第二阶段Skinning Prediction使用相同的视觉编码设计处理刚性图，将token对称注入点流和关节流以调节点-关节兼容性和蒙皮权重。整体基于冻结的预训练骨干网络（frozen pretrained backbones）。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计。虽然使用了冻结的预训练骨干网络（frozen pretrained backbones）和gated adapters，但这更多是参数高效微调（parameter-efficient fine-tuning）的思路，而非面向部署的模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该任务本身是3D动画绑定，与星上/边缘部署场景无关。

Datasets and metrics: 数据集：Articulation-XL2.0（训练/评估）和ModelsResource（零样本评估）。评价指标：骨骼和蒙皮权重的恢复准确度（recovery accuracy），在提示引导评估（prompt-guided evaluation）下与几何条件基线（geometry-conditioned baselines）对比。摘要未说明具体指标名称（如IoU、Chamfer distance等）。

Experiments: 在Articulation-XL2.0上训练并评估，在ModelsResource上进行零样本评估。结果显示ViP-Rig在提示引导评估下比几何条件基线更准确地恢复目标骨骼和蒙皮权重。定性结果展示了在提示优先绑定和结果引导编辑两种模式下均能实现显式且局部的控制。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于计算机图形学领域。2. 摘要未说明模型规模、推理速度、计算开销等部署相关信息。3. 摘要未说明在复杂网格、大规模场景下的泛化能力。4. 摘要未说明与现有绑定工具链的集成方式。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但存在少量可借鉴的通用技术思路：1. "冻结预训练骨干 + gated adapters注入条件token"的参数高效微调范式，可类比迁移到遥感检测模型中，在冻结大模型骨干的情况下通过轻量适配器注入任务特定信息，减少可训练参数量。2. "Dense-to-Compact Visual Prompt Encoding"将稠密输入压缩为紧凑token的思路，可类比用于遥感图像中多尺度特征的高效编码。但这些借鉴点较为间接，整体参考价值有限。

Abstract: Rigging is inherently task-dependent because the same mesh may require different skeletons and deformation behaviors across animation tasks. In practice, artists often inspect an initial rig and repeatedly edit its skeletal structure and deformation behavior to meet specific animation requirements. Existing automatic methods primarily generate a plausible rig from geometry, offering limited explicit control over the resulting skeleton and deformation behavior. In this work, we present ViP-Rig, a visual-prompted framework that supports both prompt-first rigging and result-guided editing by injecting features extracted from user-drawn or edited 2D skeletal and rigidity prompts into frozen pretrained backbones. Specifically, ViP-Rig consists of two stages, Skeleton Generation and Skinning Prediction. In the first stage, the skeletal sketch is processed by the Dense-to-Compact Visual Prompt Encoding to produce compact, fixed-length conditioning tokens. The resulting tokens are injected into a frozen pretrained autoregressive generator through gated adapters to control joint placement and branching structure while preserving the generator's geometric prior. In the second stage, the rigidity map is processed using the same visual encoding design, while the pretrained skinning backbone remains frozen. The resulting tokens are symmetrically injected into the point and joint streams to modulate point-joint compatibility and the resulting skinning weights. Experiments on Articulation-XL2.0 and zero-shot evaluation on ModelsResource show that ViP-Rig more accurately recovers target skeletons and skinning weights than geometry-conditioned baselines under prompt-guided evaluation. Qualitative results further demonstrate explicit and localized control in both prompt-first rigging and result-guided editing.

</details>


### [53] [Deep learning-based hierarchical insect classification using camera trap imagery](https://arxiv.org/abs/2607.28005)
*Zaki Mahfoud,Juan A. Chiavassa,Simon Walther,Florian Haselbeck,Ehsan Yaghoubi*

Main category: cs.CV

TL;DR: 提出一种基于深度学习的层级分类模型，用于从自动相机陷阱图像中对昆虫进行五级生物分类学层级识别，并构建了约一百万张图像的长尾数据集。

Research relevance: 弱相关（2分）。论文属于泛视觉/生态监测领域的图像分类任务，与遥感卫星、在轨部署、目标检测、轻量化均无直接关联。唯一可借鉴的是层级分类思想（hierarchical classification）和长尾数据（long-tailed data）处理，但这些与遥感目标检测场景差异较大。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于生态监测图像分类任务，不涉及遥感场景、目标检测或模型压缩。仅层级分类和长尾处理思想有极有限的间接参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：层级图像分类（hierarchical image classification）；应用场景：昆虫生物多样性监测（insect biodiversity monitoring），使用自动非致命相机陷阱（camera traps）；传感器类型：相机陷阱视频图像（非遥感卫星传感器）；目标类别：昆虫，五级34类生物分类学层级。

Model architecture: 摘要未完整说明具体网络结构，仅提及"adapt a hierarchical classification model architecture to a five-level variable-depth hierarchy"，即采用层级分类模型架构，支持五级可变深度层级，并利用生物分类学（biological taxonomy）提取粒度特定的视觉特征（granularity-specific visual features）。采用置信度阈值（T=0.6）决定预测到最深的分类层级。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等模型压缩或加速方法，也未涉及实时推理或边缘部署。

Onboard deployability: 摘要未说明。未提供参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）等部署相关信息，也未讨论星上或边缘部署可行性。

Datasets and metrics: 数据集：自建昆虫数据集，约一百万张图像，来自1,801个相机陷阱视频记录，标注为五级34类层级结构，属于长尾分布（long-tailed）。评价指标：per-level accuracy（各层级准确率），测试集上达到80-99%。

Experiments: 模型在测试集上五个层级均达到80-99%的准确率，优于非层级分类器（non-hierarchical classifiers），通过利用生物分类学提取粒度特定特征并做出层级一致的预测。摘要未提供更多对比实验和消融实验细节。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，属于生态监测领域；2. 未涉及任何轻量化或部署相关内容；3. 未提供模型复杂度、推理速度等工程指标；4. 层级分类的置信度阈值策略（T=0.6）虽有一定借鉴意义，但应用场景差异大。

Ideas for my research: 1. 层级分类思想（hierarchical classification）可借鉴到遥感目标检测的类别层级设计（如"舰船→军用舰船→航母"），在细粒度类别不确定时回退到粗粒度预测，提升预测可靠性；2. 长尾数据处理（class-balanced weighting）对遥感小目标/稀有目标类别（如特定型号飞机、舰船）有参考价值；3. 置信度阈值回退策略（confidence threshold-based fallback）可用于在轨推理时对低置信度检测结果进行保守处理，减少误报。但整体借鉴价值有限。

Abstract: Declining insect populations make reliable biodiversity monitoring increasingly urgent, yet monitoring of insect biodiversity is hampered by a lack of standardised data and by costly and time-consuming manual identification by expert entomologists. Deep learning-based image classifiers, processing data from automated non-lethal camera traps, have the potential to transform and scale insect biodiversity monitoring. However, challenges remain in acquiring expert-annotated datasets, developing model architectures that generalise well across diverse taxonomic levels and training models on highly imbalanced data. Hierarchical data also benefits from designing models that default to higher-confidence, coarser-level predictions, when uncertain about finer taxonomic levels. In this paper we address these challenges with a deep learning-based hierarchical classification model. First, we present a manually curated, long-tailed dataset of around one million images of insects, extracted from 1,801 camera-trap video recordings and annotated with a five-level, 34-class hierarchy. Further, we adapt a hierarchical classification model architecture to a five-level variable-depth hierarchy, with class-balanced weighting. Our model improves on non-hierarchical classifiers by leveraging biological taxonomy to extract granularity-specific visual features and makes hierarchy-consistent predictions to the deepest taxonomic level that meets a confidence threshold (T = 0.6). Our model achieved a per-level accuracy of 80-99% on test data, across five levels of hierarchy. Furthermore ...

</details>


### [54] [Beyond Classification: Pathology Foundation Models as Detection Encoders for Mitotic Figures](https://arxiv.org/abs/2607.28007)
*Sweta Banerjee,Alireza Teimoury,Nils Porsche,Alexandra K. Stoll,Viktoria Weiss,Niklas Hargarter,Jonas Ammeling,Thomas Conrad,Christoph Stroblberger,Christopher Kaltnecker,Robert Klopfleisch,Christof A. Bertram,Katharina Breininger,Marc Aubreville*

Main category: cs.CV

TL;DR: 本文研究病理学基础模型（Foundation Models）的潜在特征空间是否适合作为密集目标检测（mitotic figure detection）的骨干网络，对比了多个病理FM与端到端ResNet50基线，并搭配单阶段、双阶段和自注意力检测器。

Research relevance: 弱相关（2分）。本文属于医学病理图像（病理切片）领域的目标检测研究，与遥感/卫星/在轨部署无关。虽然涉及object detection和foundation model backbone的迁移，但场景是病理显微镜图像而非遥感影像，且完全不涉及轻量化/部署。其借鉴意义仅在于"预训练基础模型作为检测骨干"这一通用方法论，与遥感在轨目标检测及轻量化方向关联较弱。

Reading priority: 2分。与遥感在轨目标检测及轻量化方向弱相关，仅"FM作为检测骨干"的评估方法论有一定借鉴价值，但场景差异大且无轻量化内容，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务：密集目标检测（dense object detection），具体为有丝分裂细胞（mitotic figures）检测。场景：病理切片显微图像（pathology microscopy）。传感器类型：病理显微成像（非遥感传感器）。目标类别：有丝分裂细胞 vs 其他细胞。数据集：MIDOG++（多域）、TUPAC16（域外测试）。

Model architecture: 将病理Foundation Models（UNI, UNI2-h, Virchow, Virchow2, H-optimus-0, H-optimus-1）作为backbone，与三类检测器结合：单阶段RetinaNet、双阶段Faster R-CNN、自注意力Deformable DETR。对比基线为端到端训练的ResNet50 backbone。

Lightweight method: 摘要未说明。本文未涉及pruning、quantization、knowledge distillation、NAS等任何轻量化或模型压缩方法，也未讨论实时推理或边缘部署。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标。且病理FM通常参数量巨大（如Virchow等），与星上/边缘部署的轻量化需求相悖。

Datasets and metrics: 数据集：MIDOG++（多域数据集，用于训练与评估）、TUPAC16（作为out-of-domain域外测试集）。评价指标：摘要未明确说明具体指标（如AP/mAP等未提及）。实验设置：对比6个病理FM backbone × 3类检测器，与ResNet50端到端基线对比。

Experiments: 主要发现：H-optimus-0和Virchow模型取得有竞争力的性能，表明当前基于图像级自监督训练的病理FM潜在空间适合直接用于有丝分裂检测，且在域外测试集上可能更鲁棒。代码已开源（https://github.com/DeepMicroscopy/FM4MFdet）。

Limitations: 1. 场景为病理显微图像，与遥感/卫星影像差异大；2. 未涉及任何轻量化或部署优化；3. 未报告计算开销（FLOPs/参数量/延迟），无法评估部署可行性；4. 病理FM参数量庞大，直接用于在轨场景不现实；5. 摘要未说明具体评价指标和定量结果数值。

Ideas for my research: 1. 方法论借鉴：可参考"预训练基础模型作为检测backbone + 多种检测头组合"的评估范式，在遥感场景下对比不同预训练遥感FM（如基于自监督的遥感基础模型）作为检测骨干的适用性；2. 域外鲁棒性评估思路：借鉴其使用out-of-domain测试集（TUPAC16）评估模型泛化性的做法，可在遥感目标检测中引入跨传感器/跨区域域外测试；3. 但需注意：该论文未做轻量化，若要在轨部署，需额外结合pruning/quantization/distillation等手段压缩FM backbone，这恰是本人课题可切入的空白点。

Abstract: Pathology foundation models (FMs) are models trained on vast amounts of typically unlabeled data and have been shown to yield regularized latent spaces that can be used effectively in downstream classification tasks. This is also true for the classification of mitotic figures vs. other cells. However, it is so far unclear if the latent space of current FMs provides features that are discriminant and spatially suitably resolved to also serve as a backbone for dense object detection paradigms. In this work, we investigate this question for common current pathology FMs (UNI, UNI2-h, Virchow, Virchow2, H-optimus-0, H-optimus-1) and compare their performance against a fully end-to-end trained baseline based on a ResNet50 architecture. We combine FM backbones with representatives of single stage, dual stage and self-attention-based detectors (RetinaNet, Faster R-CNN, Deformable DETR respectively) on the multi-domain MIDOG++ dataset, and on the TUPAC16 dataset as an out-of-domain case. We show that the H-optimus-0 and Virchow models yielded competitive performance, indicating that the latent spaces of current FMs, all trained on image-level self-supervision, are suitable for direct mitotic figure detection and may be slightly more robust on our out-of-domain test case. All code is made available publicly at https://github.com/DeepMicroscopy/FM4MFdet.

</details>


### [55] [ENCORE: Event-Assisted Complementary Motion Refinement for Learned Video Compression](https://arxiv.org/abs/2607.28020)
*Shuhan Ye,Hongbin Yu,Chenqi Kong,Pingchuan Ma,Chong Wang,Jun Wan,Qixin Zhang*

Main category: cs.CV

TL;DR: 提出ENCORE框架，利用事件相机(event camera)作为辅助模态，通过互补运动表示(CMR)、空间能量与冗余校准(SERIC)和能量感知路由(EAR)来细化学习型视频压缩中的运动估计，提升快速运动、遮挡、弱纹理等场景下的压缩效率。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。该论文聚焦于学习型视频压缩(learned video compression)，利用事件相机(event camera)辅助运动估计，属于视频编码领域，不涉及遥感、卫星、目标检测或模型轻量化/部署。虽然视频压缩与遥感视频数据有一定关联，但论文未涉及遥感场景、目标检测任务或任何轻量化/边缘部署方法。

Reading priority: 1分。该论文聚焦于学习型视频压缩与事件相机辅助运动估计，与遥感卫星在轨目标检测及模型轻量化方向基本无关。除非课题涉及遥感视频压缩或事件相机在遥感中的应用，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：学习型视频压缩(learned video compression)中的运动估计细化。应用场景：通用视频压缩，特别针对快速运动、模糊、遮挡、弱纹理、低光照和亮度突变等挑战性场景。传感器类型：RGB相机 + 事件相机(event camera)双模态。目标类别：不涉及目标检测，无目标类别。

Model architecture: ENCORE框架包含三个核心模块：(1) Complementary Motion Representation (CMR)——将对齐的RGB-event特征分解为公共运动表示和模态特有运动表示；(2) Spatial Energy and Redundancy-Informed Calibration (SERIC)——识别相对RGB而言活跃且新颖的事件特有响应，抑制弱或冗余证据，预测候选光流修正(candidate flow correction)；(3) Energy-Aware Routing (EAR)——决定修正应在何处以及以多强程度细化RGB光流。事件仅作为运动建模的辅助模态，RGB仍是唯一编码和重建目标。

Lightweight method: 摘要未说明。论文未涉及剪枝(pruning)、量化(quantization)、知识蒸馏(knowledge distillation)、NAS或轻量网络设计等模型压缩/加速方法。该论文的核心贡献是运动估计精度提升，而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。事件相机辅助的双模态架构通常需要额外的传感器和计算开销，且事件数据处理本身具有较高计算复杂度，对星上/边缘部署的可行性未作讨论。

Datasets and metrics: 数据集：BS-ERGB、HQ-EVFI、CED三个事件-RGB视频数据集。评价指标：PSNR-RGB和MS-SSIM-RGB的BD-rate节省率。在BS-ERGB上达到最高20.80% PSNR-RGB和22.14% MS-SSIM-RGB的BD-rate节省，另外两个数据集上也有持续改进。实验覆盖不同GOP长度。

Experiments: 在BS-ERGB、HQ-EVFI、CED三个数据集上验证了ENCORE的持续增益，且在不同GOP长度下均有效。在BS-ERGB上取得最佳效果（20.80% PSNR-RGB和22.14% MS-SSIM-RGB BD-rate节省）。摘要未提供与具体基线方法的对比细节、消融实验或计算开销对比。

Limitations: (1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于视频压缩领域；(2) 依赖事件相机(event camera)作为额外传感器，在遥感卫星场景中事件相机并不常见，部署可行性存疑；(3) 摘要未报告计算复杂度、参数量、推理速度等关键部署指标；(4) 双模态架构增加了系统复杂度和硬件需求；(5) 未讨论在低功耗、资源受限的边缘/星上环境中的适用性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴思路：(1) 模态互补思想——如果未来在轨平台配备多光谱/高光谱+全色等多传感器，可借鉴CMR中"公共表示+模态特有表示"的分解思路，用于多模态遥感目标检测的特征融合；(2) SERIC中的"能量感知校准"思想可类比于注意力机制，用于遥感小目标检测中抑制背景冗余、突出目标响应；(3) 事件相机在快速运动场景的优势，对遥感视频中运动目标（如飞机、舰船）的时序建模有一定启发。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Learned video compression relies on accurate temporal modeling to remove redundancy between adjacent frames. However, most existing codecs infer motion solely from discretely sampled RGB frames, making their estimates vulnerable to fast motion, blur, occlusion, weak texture, low illumination, and abrupt brightness changes. Event cameras asynchronously capture fine-grained intensity changes between RGB timestamps and therefore provide complementary evidence about inter-frame dynamics. We propose ENCORE, an Event-Assisted Complementary Motion Refinement framework for learned video compression. ENCORE first employs Complementary Motion Representation (CMR) to decompose aligned RGB-event features into common and modality-specific motion representations. Spatial Energy and Redundancy-Informed Calibration (SERIC) then identifies event-specific responses that are active and novel relative to RGB, suppresses weak or redundant evidence, and predicts a candidate flow correction. Finally, Energy-Aware Routing (EAR) determines where and how strongly the correction should refine the RGB flow. Events serve solely as an auxiliary modality for motion modeling, while RGB remains the only coding and reconstruction target. Experiments on BS-ERGB, HQ-EVFI, and CED demonstrate consistent gains across datasets and GOP lengths. On BS-ERGB, ENCORE achieves up to 20.80% PSNR-RGB and 22.14% MS-SSIM-RGB BD-rate savings, while retaining clear improvements on the other two datasets.

</details>


### [56] [Split and Drive: Dual-Axis Disentanglement for Real-Time Gaussian Head Avatars](https://arxiv.org/abs/2607.28032)
*MD Wahiduzzaman Khan,Mingshan Jia,Xiaolin Zhang,En Yu,Kaska Musial-Gabrys*

Main category: cs.CV

TL;DR: 提出 SpiD（Split and Drive）框架，基于 3D Gaussian Splatting 从单张图像创建可动画的逼真人头头像，通过计算轴内化逐帧驱动、特征轴将头像分解为三个专用高斯分支，在单 GPU 上实现最快的完整推理速度。

Research relevance: 弱相关（2分）。该论文属于数字人合成/3D 视觉领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关系。虽然涉及目标（人脸）建模与推理速度优化，但既非遥感场景，也非目标检测任务，轻量化方法（如分支分解、内化驱动）对遥感在轨检测的借鉴价值有限。

Reading priority: 1分。该论文属于数字人/3D 高斯渲染领域，与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及遥感场景、目标检测任务或经典模型压缩方法，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：单图像可动画人头头像合成（digital human synthesis / head avatar generation）。应用场景：数字人、虚拟形象、3D 重建与渲染。传感器类型：单目 RGB 图像。目标类别：人脸/头部。与遥感目标检测无关。

Model architecture: 基于 3D Gaussian Splatting（3DGS）的框架。核心设计为两条解耦轴：(1) 计算轴（compute axis）——将逐帧驱动（per-frame driving）内化到模型中，消除对外部 tracking pipeline 的依赖；(2) 特征轴（feature axis）——将头像分解为三个专用 Gaussian 分支（specialized Gaussian branches），每个分支建模一个几何上不同的面部区域（facial domain），以提升表达力和渲染保真度。

Lightweight method: 摘要未明确提及剪枝、量化、知识蒸馏或 NAS 等经典轻量化方法。其"轻量化"体现在推理效率层面：通过内化驱动逻辑（internalizing driving）消除外部 tracking 管线在推理时的额外延迟，以及通过分支解耦（branch disentanglement）避免统一表示带来的冗余计算。但未给出参数量、FLOPs 等量化指标。

Onboard deployability: 摘要仅说明在单 GPU 上，包含完整 driving pipeline 的情况下，SpiD 在所有对比方法中推理速度最快。但未说明具体延迟数值、参数量、FLOPs、显存占用、能耗或实时帧率。由于面向的是数字人渲染而非遥感在轨推理，且依赖 3D Gaussian Splatting 渲染管线，与星上/边缘部署（FPGA/NPU/TensorRT）场景不匹配。具体部署指标：摘要未说明。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面，摘要提到"consistently strong performance"和"fastest inference speed"，但未给出具体指标名称（如 PSNR、SSIM、LPIPS、FPS 等）。实验设置细节：摘要未说明。

Experiments: 摘要提到与 state-of-the-art 方法进行了广泛对比实验，SpiD 在性能上保持一致强表现，同时在单 GPU 上（包含完整 driving pipeline）取得最快的推理速度。但未列出具体对比方法名称、消融实验细节或定量结果。

Limitations: (1) 与遥感/卫星/在轨目标检测方向基本无关，属于数字人合成领域；(2) 摘要未提供任何量化部署指标（参数量、FLOPs、延迟、显存、能耗）；(3) 未说明三个 Gaussian 分支的具体划分方式与计算开销；(4) 未讨论在低算力/边缘设备上的可行性；(5) 单图像输入的限制、泛化到不同光照/姿态的鲁棒性未说明。

Ideas for my research: 尽管场景不同，仍有少量可借鉴的通用思路：(1) "计算轴内化驱动"的思想——将外部依赖（如 tracking pipeline）整合进模型内部以减少推理时额外开销，类比于在轨检测中可将预处理/后处理步骤融合进网络以降低端到端延迟；(2) "特征轴分支解耦"——将复杂目标按几何/语义区域分解为多个专用分支，可类比遥感大场景中按目标类别（舰船/飞机/车辆）或尺度（大/中/小目标）进行分支化设计，提升小目标检测的表达力；(3) 强调"包含完整管线"的推理速度对比，提示在轨检测评估时应包含前后处理全流程延迟而非仅网络前向时间。但整体借鉴价值有限。

Abstract: Creating photorealistic animatable head avatars from a single image remains a fundamental challenge in digital human synthesis. While recent 3D Gaussian Splatting methods have achieved promising results, they rely on external tracking pipelines whose latency is excluded from inference measurements. Furthermore, they adopt unified representations that entangle geometrically distinct facial regions, limiting both expressiveness and rendering fidelity. We propose SpiD (Split and Drive), a single-image Gaussian head avatar framework built on two disentanglement axes. The compute axis internalizes per-frame driving, eliminating external tracking dependency at inference. The feature axis decomposes the avatar into three specialized Gaussian branches, each modeling a geometrically distinct facial domain. Extensive experiments demonstrate consistently strong performance against state-of-the-art methods while achieving the fastest inference speed among all compared methods on a single GPU with the complete driving pipeline included.

</details>


### [57] [TongueReenact: Geometry-Anchored Tongue Synthesis for Face Reenactment](https://arxiv.org/abs/2607.28039)
*MD Wahiduzzaman Khan,Mingshan Jia,Xiaolin Zhang,En Yu,Kaska Musial-Gabrys*

Main category: cs.CV

TL;DR: 提出首个跨身份舌头动态迁移的人脸重演框架，通过基础模型辅助的引导式pipeline训练舌头分割模型，并引入空间约束的潜在掩码扩散模型实现逼真舌头合成。

Research relevance: 弱相关（1分）。该论文聚焦人脸重演（face reenactment）中的舌头动态迁移与合成，属于计算机视觉/图形学中的人脸生成与编辑方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。不涉及任何目标检测任务，也不涉及模型轻量化、剪枝、量化、知识蒸馏、NAS、边缘部署等压缩加速方法。仅有的间接关联是使用了diffusion model和foundation model，属于泛视觉生成方法，对遥感目标检测及轻量化课题基本无可借鉴性。

Reading priority: 1分。该论文与人脸重演/生成方向强相关，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不涉及目标检测、遥感场景或任何模型压缩加速技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：人脸重演（face reenactment）中的跨身份舌头动态迁移与合成；应用场景：数字人、视频生成、人脸编辑；传感器类型：不适用（RGB人脸视频/图像）；目标类别：舌头（tongue）、嘴部区域（mouth interior）。

Model architecture: 包含三个核心组件：(1) foundation-model-assisted bootstrapping pipeline，用于生成in-the-wild场景下的专用舌头分割模型（tongue segmentation model）；(2) 空间约束的潜在掩码扩散模型（spatially constrained latent masked diffusion model），用于逼真舌头合成；(3) 自适应掩码膨胀（adaptive mask dilation）机制，实现嘴部边界的无缝过渡。此外提出VLM-based evaluation protocol用于大规模专家级标注复现。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（无pruning、quantization、knowledge distillation、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该任务（人脸重演/舌头合成）与星上部署场景无关。

Datasets and metrics: 摘要未明确说明具体数据集名称。评价指标为tongue-specific metrics（舌头专用指标），以及基于VLM的评估协议。实验对比了所有baseline，声称在每项舌头指标上提升超过两倍（improvements of more than two times over all baselines）。

Experiments: 摘要提到在全部舌头专用指标上相对所有baseline提升超过两倍，并通过VLM-based evaluation protocol在所有消融变体上确认了感知优越性（perceptual superiority across all ablation variants）。但未给出具体数值、对比方法名称或消融细节。

Limitations: 摘要未说明局限性。主要缺口包括：未报告模型规模与推理效率；未涉及实时性；未讨论跨数据集泛化；未提供定量指标数值；未说明舌头分割模型的训练数据规模与标注成本。此外，该方向本身与遥感目标检测/轻量化无直接关联。

Ideas for my research: 借鉴意义有限。可能的间接启发：(1) foundation-model-assisted bootstrapping pipeline的思路——利用基础模型辅助生成训练数据/伪标签，可类比于遥感领域利用预训练大模型辅助小样本目标检测的标注或蒸馏；(2) latent masked diffusion model的生成思路或可迁移到遥感数据增强（如SAR/光学图像合成），但需谨慎评估计算开销；(3) VLM-based evaluation protocol可类比为遥感检测中利用VLM辅助评估或标注。总体而言，这些均为间接启发，不涉及轻量化核心问题。

Abstract: Modern face reenactment systems achieve impressive pose and expression transfer using geometry-driven representations. However, they largely ignore tongue dynamics, leading to anatomically inconsistent mouth interiors during speech and expressive motions. We introduce the first framework for cross-identity tongue dynamics transfer in face reenactment. We propose a foundation-model-assisted bootstrapping pipeline that produces a dedicated tongue segmentation model for in-the-wild reenactment without curated annotations. We further introduce a spatially constrained latent masked diffusion model for realistic tongue synthesis, with adaptive mask dilation for seamless mouth boundary transitions. Extensive experiments demonstrate improvements of more than two times over all baselines on every tongue-specific metric. We additionally propose a VLM-based evaluation protocol that replicates expert annotation at scale, confirming perceptual superiority across all ablation variants.

</details>


### [58] [Temporal Concentration from Rollout Errors: Implicit Preference Optimization for Text-to-Video Diffusion](https://arxiv.org/abs/2607.28058)
*Henglin Liu,Fangyuan Kong,Jing Wang,Yizhou Lin,Nisha Huang,Chang Liu,Xintao Wang,Pengfei Wan,Kun Gai,Xiu Li*

Main category: cs.CV

TL;DR: 提出cIPO（concentrated Implicit Preference Optimization），一种面向视频扩散模型的后训练框架，通过从去噪过程直接推导隐式偏好信号并聚焦高误差时间片段进行优化，解决视频生成中的时间稀疏伪影（运动崩溃、物体闪烁、颜色过饱和）问题。

Research relevance: 弱相关（2分）。该论文属于视频生成（video generation）与扩散模型（diffusion model）领域，核心是偏好优化（preference optimization）与时间一致性（temporal coherence），与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署加速方法。仅从方法论层面（如隐式信号提取、时间维度聚焦优化）可能对时序数据处理有间接启发，但整体与本课题方向基本无关。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于视频生成/扩散模型偏好优化领域。除非对视频扩散模型或偏好优化有独立兴趣，否则无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视频扩散模型的后训练偏好优化（post-training preference optimization for video diffusion models）；应用场景：视频生成质量提升；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 基于视频扩散模型（video diffusion model），采用DPO（Direct Preference Optimization）框架的变体。核心设计：cIPO通过前向加噪+迭代去噪重建过程，将原始视频视为preferred sample、重建结果视为dispreferred sample，从而从去噪过程直接推导隐式偏好信号（implicit preference signals），无需人工标注或外部reward model。同时计算帧级时间重建误差（temporal reconstruction errors），将优化集中在高误差片段上。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法。其"efficiency"指优化训练的效率（无需人工标注和外部奖励模型），而非推理部署效率。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗等任何部署相关指标。且视频扩散模型本身计算量巨大，与星上/边缘实时推理场景不匹配。

Datasets and metrics: 摘要提到在多个数据集（multiple datasets）上验证，但未具体列出数据集名称。评价指标涉及视频真实性（video authenticity）和时间一致性（temporal coherence），但未给出具体指标名称（如FVD、CLIP score等）。摘要未说明具体实验设置。

Experiments: 摘要仅概述性地说明"extensive experiments"表明cIPO在多个数据集上持续提升视频真实性和时间一致性，并强调隐式偏好+时间集中优化的有效性和效率。未给出具体数值结果、对比基线或消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于视频生成领域；2. 摘要未提供任何量化实验数据（指标数值、数据集名称、对比方法）；3. 未讨论推理效率或部署可行性；4. 隐式偏好信号依赖重建质量，若重建本身存在系统性偏差可能引入噪声；5. 时间集中优化策略在长视频或高分辨率场景下的计算开销未说明。

Ideas for my research: 1. 方法论层面的间接启发：cIPO中"时间维度聚焦优化"的思想——即识别高误差/高信息量的时间片段并集中优化——可类比于遥感视频或时序遥感数据中聚焦关键帧/关键区域进行检测或压缩的思路；2. 隐式信号提取（从模型自身前向/反向过程推导监督信号）的思想，在无标注遥感数据场景下可能对自监督预训练或知识蒸馏有启发；3. 但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点非常有限，不建议深入精读。

Abstract: Recent advances in preference alignment for diffusion-based video generation, particularly via Direct Preference Optimization (DPO), have significantly improved visual quality. However, temporally sparse artifacts such as motion collapse, object flickering, and color oversaturation remain a major barrier to perceptual realism. Existing methods struggle with these issues due to two key limitations: (1) the preference attribution bottleneck, where offline human annotations are costly and fail to accurately capture learning dynamics, while online reward signals are rollout-aware but often unstable and biased; and (2) temporal credit misallocation, where uniformly applied supervision cannot effectively target the brief segments in which artifacts occur. To address these challenges, we propose concentrated Implicit Preference Optimization (cIPO), a post-training framework for video diffusion models. cIPO derives implicit preference signals directly from the denoising process: given a real video, the model adds forward noise and reconstructs it via iterative denoising, treating the original as the preferred sample and the reconstruction as the dispreferred one. This formulation captures inference-time errors without requiring human annotations or external reward models. Moreover, frame-level discrepancies between original and reconstructed videos reveal when failures occur. cIPO leverages this by computing temporal reconstruction errors and concentrating optimization on high-error segments, enabling more precise correction of failure-prone regions. Extensive experiments demonstrate that cIPO consistently enhances video authenticity and temporal coherence across multiple datasets, highlighting the effectiveness and efficiency of implicit preference with temporally concentrated optimization.

</details>


### [59] [Landmark shape spaces with induced metrics](https://arxiv.org/abs/2607.28064)
*Sarang Joshi,Peter W. Michor,Stefan Sommer*

Main category: cs.CV

TL;DR: 本文提出一种将Kendall地标形状空间与微分同胚群上的Sobolev度量统一的新几何框架，通过"屏蔽弹性算子"（screened elasticity operator）构造具有刚性运动不变性、尺度固定且防止地标碰撞的黎曼度量形状空间。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于计算几何/形状分析/微分几何领域，讨论的是地标配置空间（landmark configuration spaces）上的黎曼几何与形状匹配，不涉及遥感图像、目标检测、神经网络或模型压缩。相关性评分：1分。

Reading priority: 1分。论文属于计算几何/微分几何理论方向，与遥感卫星在轨目标检测及轻量化研究完全无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：形状空间几何构造与地标匹配（landmark matching）的数学理论问题。应用场景：计算形状分析、图像配准、医学影像等几何数据处理。传感器类型：不涉及遥感传感器（非SAR、光学、高光谱等）。目标类别：地标点（landmarks），非遥感目标（舰船/飞机/车辆等）。

Model architecture: 摘要未说明具体神经网络模型结构。核心数学构造为：定义在微分同胚群上的右不变Sobolev度量，通过"屏蔽弹性算子"（screened elasticity operator）下降（descend）到地标配置空间，其零空间恰好为刚性运动（rigid motions），从而构造新的地标形状空间。

Lightweight method: 不涉及任何模型轻量化方法。摘要中未出现剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理、边缘部署、FPGA/NPU/TensorRT等任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集和评价指标。论文聚焦于理论几何构造与数值求解方法（matching problems和geodesics计算），未提及任何遥感数据集或检测评价指标（如mAP、IoU等）。

Experiments: 摘要仅提及"present approaches to solving matching problems and computing geodesics numerically"，即提出了数值求解匹配问题和测地线的方法，但未给出具体实验结果、对比方法或消融实验。

Limitations: 摘要未说明局限性。从研究内容看，该工作属于纯数学/几何理论贡献，与深度学习目标检测、遥感图像理解、模型压缩等工程问题完全脱节，缺乏可迁移到遥感在轨检测场景的工程方法。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的可借鉴点极为有限。唯一可能的间接联系是：形状空间理论中关于刚性运动不变性（rotation/translation invariance）的数学处理，在旋转框检测（oriented object detection）中旋转等变性设计或可参考其数学思想，但这一关联非常牵强且工程转化成本极高。总体而言，不建议从本文提取研究思路。

Abstract: We present a unification of Kendall's landmark shape spaces, where rigid motions are factored out and scale fixed on landmark configurations equipped with Euclidean geometry, with landmark configuration spaces carrying Riemannian metrics descending from right-invariant Sobolev metrics on the diffeomorphism group. The resulting new landmark shape spaces achieve the defining properties of both approaches: The regularity of the descending metric prevents landmarks from colliding, the metric is defined in the ambient space independent of the number of landmarks, local rigid transformations are preserved, global rigid motions are removed, and scale fixed. To achieve this, we define a particular Sobolev-type operator, the screened elasticity operator, whose null-space consists exactly of the rigid motions, we show how this operator descends to achieve the desired geometry, and we present approaches to solving matching problems and computing geodesics numerically. The resulting construction allows the use of landmark configuration spaces with sufficiently regular metrics in applications while retaining the shape invariances that are a hallmark of Kendall's shape spaces.

</details>


### [60] [BladeYOLO: Wind Turbine Blade Defect Detection with Limited Annotations and Weak-Saliency Awareness](https://arxiv.org/abs/2607.28065)
*Yabin Xu,Fangtao Zhang,Fan Wang,Zhan Wang,Honghua Chen,Mingqiang Wei,Haoran Xie,Sam Kwong*

Main category: cs.CV

TL;DR: 提出BladeYOLO，一种用于风机叶片缺陷检测的框架，结合DINOv3自监督预训练的ViT骨干、Mamba引导的弱缺陷增强模块和轻量级Style-Injector模块，在WTBlade-Defect和Wind Surface Defect数据集上取得SOTA性能。

Research relevance: 弱相关（2分）。该论文是工业场景下的风机叶片缺陷检测，属于泛视觉目标检测任务，与遥感/卫星场景无直接关联。虽然涉及小目标、低对比度缺陷检测和轻量级模块设计，但并非遥感影像、SAR、高光谱或多光谱数据，也未涉及星上部署、在轨推理等轻量化部署问题。其小目标检测和轻量化模块设计思路对遥感目标检测有一定借鉴意义，但整体相关性较弱。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向相关性较弱，核心场景是工业风机叶片缺陷检测。虽然小目标检测模块设计和自监督预训练迁移思路有一定参考价值，但缺乏遥感场景验证和模型压缩/部署相关内容，阅读优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：风机叶片缺陷检测（wind turbine blade defect detection）。应用场景：工业巡检（real-world inspection），非遥感/卫星场景。传感器类型：摘要未说明（推测为可见光工业相机图像）。目标类别：风机叶片缺陷（blade defects），具有小尺度、低对比度、背景复杂的特点。

Model architecture: 基于YOLOv12-L框架，集成ViT（Vision Transformer）骨干，使用DINOv3自监督预训练权重初始化。包含三个关键模块：(1) Mamba-guided Weak-Defect Enhancement module，由Detail-Enhanced Multi-scale Branch（保留高频结构线索）和Cross-Mamba module（将高层语义引导传播到浅层特征）组成；(2) 轻量级Style-Injector模块，通过Fourier分解捕获环境相关风格信息并注入到选定的ViT self-attention层。

Lightweight method: 摘要中仅提到"lightweight Style-Injector module"这一轻量级模块设计，但未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等模型压缩方法。整体模型基于YOLOv12-L，属于较大模型，未说明参数量、FLOPs、推理延迟等轻量化部署指标。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、推理延迟、显存占用、能耗等部署相关信息。基于YOLOv12-L和ViT骨干的架构通常计算量较大，且未提及任何边缘部署、实时推理或硬件加速（FPGA/NPU/TensorRT）相关内容。

Datasets and metrics: 数据集：WTBlade-Defect（自建/私有数据集）和公开的Wind Surface Defect数据集。评价指标：mAP50和mAP50-95。实验设置包括annotation-budget experiments（减少标注样本下的性能评估）和cross-dataset robustness评估。在Wind Surface Defect数据集上，BladeYOLO比最佳对比方法在mAP50上提升3.5%，在mAP50-95上提升2.5%。

Experiments: 在WTBlade-Defect数据集上取得优越性能；通过annotation-budget实验验证了在减少训练标注下的良好表现；在公开Wind Surface Defect数据集上验证了跨数据集鲁棒性，mAP50提升3.5%，mAP50-95提升2.5%。摘要未提供与其他方法的具体对比列表和消融实验细节。

Limitations: (1) 与遥感/卫星场景完全无关，数据来源为工业巡检图像；(2) 摘要未说明模型参数量、FLOPs、推理速度等部署指标，无法判断其轻量化程度；(3) 基于YOLOv12-L + ViT的架构计算开销可能较大，与在轨部署需求相悖；(4) 未涉及任何模型压缩技术（剪枝、量化、蒸馏等）；(5) 摘要未说明缺陷检测的具体类别和数量。

Ideas for my research: (1) DINOv3自监督预训练权重的迁移思路可借鉴到遥感小样本场景，遥感标注数据稀缺，自监督预训练+微调范式值得探索；(2) Mamba-guided Weak-Defect Enhancement模块中"高层语义引导传播到浅层特征"的设计思路，可迁移到遥感小目标检测中增强浅层特征表达；(3) Style-Injector通过Fourier分解注入环境风格信息，对遥感影像中不同光照、季节、传感器差异的鲁棒性提升有借鉴意义；(4) annotation-budget实验设计思路可参考，遥感标注成本高，验证低标注下的性能很有价值；(5) 但需注意，该工作未涉及真正的模型轻量化（压缩/加速），仅有一个轻量模块，对在轨部署的直接借鉴有限。

Abstract: Wind turbine blade defect detection remains highly challenging in real-world inspection scenarios due to limited on-site data and the subtle visual characteristics of defects. In practice, blade defects are often small-scale, low-contrast, and difficult to distinguish from complex backgrounds, which significantly limits the robustness of existing detectors. To address these challenges, we propose BladeYOLO, a defect detection framework for wind turbine blades. Specifically, we integrate a Vision Transformer (ViT) backbone initialized with DINOv3 self-supervised pre-trained weights into YOLOv12-L, enabling the transfer of large-scale generic visual priors to blade defect detection and improving feature representation under limited training annotations. To enhance the perception of subtle defects, we further develop a Mamba-guided Weak-Defect Enhancement module, which consists of a Detail-Enhanced Multi-scale Branch for preserving high-frequency structural cues and a Cross-Mamba module for progressively propagating high-level semantic guidance to shallow features. In addition, we introduce a lightweight Style-Injector module that captures environment-related style information via Fourier decomposition and injects it into selected ViT self-attention layers, thereby improving robustness against environment-induced appearance variations. Extensive experiments demonstrate that BladeYOLO achieves superior performance on the WTBlade-Defect dataset, with additional annotation-budget experiments showing its favorable performance under reduced training annotations. Evaluation on the public Wind Surface Defect dataset further provides supportive evidence for the cross-dataset robustness of BladeYOLO. In particular, on this public dataset, BladeYOLO outperforms the best competing method by 3.5\% in mAP$_{50}$ and 2.5\% in mAP$_{50-95}$.

</details>


### [61] [mmRadarTwin: A Measurement-Calibrated Signal-Level Digital Twin Platform for Indoor mmWave Radar](https://arxiv.org/abs/2607.28108)
*Jianyi Zhou,Chenghao Zhang,Yanli Li,Dong Yuan*

Main category: cs.CV

TL;DR: mmRadarTwin是一个面向室内毫米波雷达感知的信号级、路径可归因的数字孪生平台，通过Unreal Engine场景仿真与真实FMCW雷达测量分支共享接收通道和距离-角度处理接口，实现室内雷达数字孪生的构建、比较与诊断。

Research relevance: 弱相关（2分）。该论文聚焦室内毫米波雷达（indoor mmWave radar）感知的数字孪生仿真平台，属于泛雷达感知/仿真领域，与遥感卫星、Earth observation、aerial image、SAR、高光谱/多光谱等遥感场景完全无关，也不涉及目标检测或模型轻量化。唯一可借鉴的是其"信号级仿真-真实测量对比"的系统性工作流思想，但与本课题方向关联度很低。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于室内毫米波雷达数字孪生仿真领域，不涉及遥感场景、目标检测或任何轻量化技术。除非对"仿真-真实对比诊断"方法论有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：室内毫米波雷达感知的数字孪生仿真与诊断（digital twin for indoor mmWave radar perception）。应用场景：室内办公环境（office deployment）的雷达感知仿真。传感器类型：commodity monostatic mmWave radar（商用单站毫米波雷达）+ Unreal Engine场景仿真。目标类别：室内场景中的actor（人员/物体），非遥感目标。摘要未说明具体目标检测任务。

Model architecture: mmRadarTwin平台架构：包含两条分支——(1) 真实雷达测量分支（real radar measurement branch）；(2) Unreal Engine场景仿真分支（scene-simulation branch）。两条分支通过共享的接收通道（receive-channel）和距离-角度处理接口（range-angle processing interface）连接。仿真器输出复数多通道接收网格（complex multi-channel receive grids），并导出逐路径贡献记录（per-path contribution records），标识每个仿真回波的actor、材质标签（material tag）、传播事件（propagation event）和输出bin支持。摘要未说明具体神经网络或检测模型架构。

Lightweight method: 摘要未说明。该论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或任何模型压缩/加速方法。其核心贡献是仿真平台而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时推理等部署指标。该平台面向室内雷达仿真与诊断，与星上部署（onboard deployment）、边缘部署（edge deployment）、FPGA/NPU/TensorRT等均无关联。

Datasets and metrics: 数据集：在办公环境中使用商用单站毫米波雷达和移动场景采集硬件采集数据，覆盖22个雷达位置、154个测量位姿（measured poses）。评价指标：recall（召回率），具体为"measurement-active geometry-supported response regions"的召回率，在中心可用视场（central usable field of view）中达到70.8%。摘要未说明其他指标（如precision、mAP等）。

Experiments: 主要实验：在办公部署场景中评估mmRadarTwin，物理驱动的路径基仿真器（physics-only path-basis simulator）在中心可用视场中召回70.8%的测量活跃且几何支撑的响应区域。同时暴露了残差来源：弱路径或缺失路径支撑（weak or missing path support）、响应偏移（shifted responses）、不支持的锚点（unsupported anchors）、缺失的物理机制（missing physical mechanisms）。论文明确不声称实现完整的雷达地图重建或跨房间泛化。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关，属于室内毫米波雷达仿真领域。2. 论文自身也承认局限：不声称完整雷达地图重建或跨房间泛化，仅建立系统工作流。3. 召回率仅70.8%，存在多种残差来源（弱路径、响应偏移、缺失物理机制等）。4. 摘要未说明计算开销、实时性、模型复杂度等部署相关信息。5. 未涉及任何目标检测模型或轻量化技术。

Ideas for my research: 尽管与本课题弱相关，但可借鉴以下几点：1. "仿真-真实测量共享处理接口"的系统性对比工作流思想，可类比用于遥感仿真数据（如合成SAR/光学图像）与真实遥感图像的域对齐与诊断；2. 逐路径贡献记录（per-path contribution records）的"可归因诊断"思路，可迁移到遥感目标检测中分析模型对特定目标/背景成分的响应来源；3. 明确暴露残差来源（weak path support、shifted responses等）的诊断方法论，可用于分析轻量化模型相对原始模型的性能退化来源。但这些借鉴点较为间接，整体参考价值有限。

Abstract: Indoor mmWave radar perception is difficult to reproduce because measured range-angle responses depend on scene geometry, material response, multipath, hardware conventions, and signal processing. Existing ray-tracing and digital-twin tools often expose rendering, channel, or path-level quantities, while radar sensing requires complex signal products that can be processed and compared in the same domain as real FMCW measurements. We present mmRadarTwin, a signal-level and path-attributed digital-twin platform for indoor mmWave radar. mmRadarTwin links a real radar measurement branch with an Unreal Engine scene-simulation branch through a shared receive-channel and range-angle processing interface. The simulator writes complex multi-channel receive grids and exports per-path contribution records that identify the actor, material tag, propagation event, and output-bin support of each simulated return. We evaluate mmRadarTwin in an office deployment using a commodity monostatic mmWave radar and mobile scene-capture hardware. Across 154 measured poses spanning 22 radar locations, the current physics-only path-basis simulator recalls 70.8% of measurement-active geometry-supported response regions in the central usable field of view while exposing residuals caused by weak or missing path support, shifted responses, unsupported anchors, and missing physical mechanisms. Rather than claiming complete radar-map reconstruction or cross-room generalization, mmRadarTwin establishes a practical systems workflow for constructing, comparing, and diagnosing indoor radar digital twins.

</details>


### [62] [Towards Practical Algorithm Selection for Unsupervised Domain Adaptation in Medical Imaging](https://arxiv.org/abs/2607.28125)
*Yiheng Xiong,Luisa Gallée,Daniel Santak Wolf,Heiko Hillenhagen,Michael Götz*

Main category: cs.CV

TL;DR: 提出一种无需标签的UDA算法与超参数联合选择准则，通过两级一致性参考（agreement reference）在无标签目标域上选出最适合部署的候选模型。

Research relevance: 弱相关（2分）。论文属于泛机器学习/医学影像领域，涉及unsupervised domain adaptation (UDA)的模型选择与部署，但场景是brain MRI和chest X-ray医学影像，与遥感、卫星、Earth observation、SAR、高光谱等完全无关，也不涉及object detection或轻量化方法。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，主要价值在于UDA场景下的无标签模型选择思想，可作为在轨模型自适应选择的参考，但非核心文献。

<details>
  <summary>Details</summary>

Task and scene: 任务：UDA算法与超参数的无标签选择（label-free model selection）。场景：临床医学影像部署。传感器/数据类型：brain MRI、chest X-ray。目标类别：摘要未说明（医学影像分割/分类任务，具体未明确）。

Model architecture: 摘要未说明具体网络结构。方法核心是构建两级agreement reference：第一级利用多个label-free selection signals在每个算法内提名一个模型；第二级跨算法聚合提名模型形成参考预测，选择与参考最一致的候选模型用于部署。

Lightweight method: 不涉及。摘要未提及pruning、quantization、knowledge distillation、NAS、轻量网络、FLOPs、参数量等任何模型压缩或加速方法。论文关注的是UDA模型选择而非模型轻量化。

Onboard deployability: 摘要未说明。论文讨论的是临床部署（clinical deployment）场景下的模型选择，不涉及星上/边缘部署、FPGA/NPU/TensorRT、延迟、能耗、显存等部署硬件指标。

Datasets and metrics: 数据集：4个brain MRI数据集和4个chest X-ray数据集，覆盖7个临床相关迁移场景（transfer scenarios）。评价指标：摘要未明确说明具体指标名称，仅提及"selection performance"。

Experiments: 在7个临床迁移场景上验证，结果表明所提方法在模型选择性能上优于其他方法，且在不同算法池（algorithm pools）下保持有效性。

Limitations: 1. 仅针对UDA模型选择，不涉及目标检测或遥感场景；2. 摘要未说明方法对目标域分布偏移的鲁棒性边界；3. 未提供计算开销、推理速度等部署相关数据；4. 医学影像与遥感影像在数据分布、目标尺度、传感器特性上差异巨大，方法可迁移性存疑。

Ideas for my research: 1. 虽然场景不同，但其"label-free selection criterion"思想可借鉴到遥感在轨场景：当星上部署多个候选轻量化检测模型时，可在无标注目标域（如新轨道/新传感器数据）上通过一致性参考自动选择最优模型，避免人工调参；2. 两级agreement reference的"多信号提名+跨模型聚合"思路可用于在轨模型自适应选择，与domain adaptation结合；3. 但需注意，该方法本身不解决轻量化问题，仅可作为模型选择/部署决策的辅助策略。

Abstract: Numerous unsupervised domain adaptation (UDA) algori-thms exist, but for clinical practice, selecting the best-suited one along with proper hyperparameters often remains unclear, as the unlabeled deployment (target) domain prevents direct evaluation. We propose a label-free criterion that jointly selects the algorithm and hyperparameters for UDA. Given a pool of candidate models from multiple algorithms trained with different hyperparameters, our approach scores each candidate against an agreement reference, and selects the one with the highest score. The agreement reference is constructed in two levels without using target labels. First, we leverage multiple label-free selection signals, using each to nominate a model within every algorithm. Second, the nominated models are aggregated across algorithms to form a reference prediction for each unlabeled target sample. The candidate whose predictions agree most with this reference is then selected for deployment. Experimental results on four brain MRI and four chest X-ray datasets across seven clinically relevant transfer scenarios show that our method achieves better selection performance than other methods and remains effective across different algorithm pools. Our approach takes a step towards practical, label-free algorithm selection for clinical deployment of UDA.

</details>


### [63] [Face and Voice Cross-modal Association with Learning Convex Feature Embedding](https://arxiv.org/abs/2607.28129)
*Taewan Kim,Jiwoo Kang*

Main category: cs.CV

TL;DR: 提出一种基于凸包嵌入（convex embedding）和跨模态注意力机制的语音-人脸关联学习方法，在VoxCeleb数据集上实现跨模态验证、匹配和检索任务的性能提升。

Research relevance: 1分，与本研究方向基本无关。该论文研究的是语音（voice）与人脸（face）的跨模态关联学习，属于多模态深度学习领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景，也不涉及目标检测或模型轻量化/部署。

Reading priority: 1分。该论文研究语音-人脸跨模态关联，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何模型压缩/加速技术，阅读优先级极低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：跨模态关联学习（cross-modal association learning），具体为语音-人脸验证（verification）、匹配（matching）和检索（retrieval）。应用场景为说话人识别/人脸识别关联，传感器类型为音频和视频（RGB人脸图像），目标类别为人物身份。与遥感目标检测无关。

Model architecture: 提出跨模态特征嵌入方法，核心思想是通过在跨模态特征之间构造"另一个特征"（making another feature exist between cross-modal features），使同一人的语音特征和人脸特征嵌入到凸包（convex hull）中。同时引入跨模态注意力机制（cross-modal attention）与凸嵌入技术结合，通过最小化类间差异（inter-class discrepancies）来抑制假阳性和假阴性。摘要未说明具体网络骨干结构。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法，未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等轻量化技术。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息，也未讨论边缘部署或星上部署可行性。

Datasets and metrics: 数据集：VoxCeleb（大规模音视频数据集）。任务：跨模态验证（verification）、匹配（matching）、检索（retrieval）。摘要未说明具体评价指标（如准确率、召回率、mAP等）的具体数值。

Experiments: 在VoxCeleb数据集上进行了跨模态验证、匹配和检索三类任务的全面评估，实验结果表明所提方法相比现有state-of-the-art方法有显著改进。摘要未提供具体对比数值和消融实验细节。

Limitations: 1. 与遥感在轨目标检测方向完全无关，无可借鉴的遥感场景信息；2. 摘要未提供模型复杂度、推理速度等部署相关信息；3. 摘要未说明方法在跨模态特征异构性（heterogeneity）处理上的具体技术细节；4. 仅在VoxCeleb单一数据集上验证，泛化性未知。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但"凸包嵌入（convex embedding）"和"跨模态注意力"的思想在方法论层面有一定抽象借鉴价值：1. 若未来研究涉及多模态遥感数据（如光学+SAR、多光谱+高光谱）的融合检测，跨模态特征对齐和注意力机制的思想可参考；2. 凸嵌入思想可用于缓解遥感多模态特征异构性导致的误检问题。但整体而言，该论文对本课题的直接借鉴价值很低。

Abstract: Face-and-voice association learning is one of the most challenging tasks in deep learning. In this paper, we propose a simple but powerful cross-modal feature embedding method for the association of faces and voices. Previous work has studied cross-modal association tasks to establish the correlation between voice clips and facial images. These works have addressed cross-modal discrimination but underestimate the importance of handling heterogeneity in inter-modal features between audio and video, resulting in a lot of false positives and false negatives. To tackle the problem, the proposed method learns the embeddings of cross-modal features by making another feature exist between cross-modal features, facilitating the voice and face features of the same person to be embedded in a convex hull. Moreover, the incorporation of cross-modal attention mechanisms with convex embedding techniques represents a highly effective strategy for the attenuation of false positives and false negatives, accomplished via the minimization of inter-class discrepancies. We exhaustively evaluated our method for cross-modal verification, matching, and retrieval tasks on the large-scale VoxCeleb dataset. Extensive experimental results demonstrate that the proposed method achieves notable improvements over existing state-of-the-art methods.

</details>


### [64] [Collaborative Feature Aggregation for Face Super-Resolution and Robust Re-Identification](https://arxiv.org/abs/2607.28130)
*Juheon Hwang,Taewan Kim,Jiwoo Kang*

Main category: cs.CV

TL;DR: 提出一种基于transformer的多视角/多时序人脸图像协同超分辨率与行人重识别联合方法，通过协作特征聚合和级联SR网络逐步恢复高分辨率人脸并统一身份特征。

Research relevance: 弱相关（1-2分）。论文聚焦人脸超分辨率（face super-resolution）和行人重识别（person re-identification），属于泛视觉/人脸识别领域，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。该论文聚焦人脸超分与行人重识别，与遥感卫星在轨目标检测及轻量化方向基本无关，不推荐优先阅读。仅当对"多帧协同特征聚合"或"渐进式超分辅助检测"这类跨域方法论感兴趣时，可作为泛视觉参考略读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：人脸超分辨率（face SR）与行人重识别（person re-identification）联合任务。应用场景：多时序（sequential）或多视角（multi-view）人脸图像。传感器类型：普通可见光摄像头（非遥感传感器）。目标类别：人脸（human face）。

Model architecture: 核心结构包括：(1) transformer-based collaborative feature aggregation module，用于从多序列或多视角数据中统一身份特征；(2) cascade SR network，逐步渐进式恢复目标人脸高分辨率图像，并伴随渐进式人脸特征统一。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。

Onboard deployability: 摘要未说明。未提供参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）或实时性等部署相关信息。且该任务面向人脸识别场景，与星上部署无关。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及"exhaustive experimental results and comparisons"以及"outperforms other state-of-the-art methods"，未给出PSNR、SSIM、Rank-1、mAP等具体指标数值。

Experiments: 摘要仅说明实验结果表明方法在face super-resolution和re-identification两个任务上均优于现有state-of-the-art方法，但未给出具体对比方法、消融实验细节或定量结果。

Limitations: (1) 与遥感/卫星/在轨目标检测方向基本无关；(2) 摘要未提供任何轻量化或部署相关信息；(3) 未说明具体数据集、指标和定量结果；(4) 未讨论计算复杂度或实时性；(5) 多视角/多时序人脸数据假设在遥感场景中难以直接迁移。

Ideas for my research: 尽管整体弱相关，但以下思路可借鉴：(1) transformer-based collaborative feature aggregation思想——在遥感多时相（multi-temporal）或多视角观测中，可借鉴"多帧协同特征聚合"来增强小目标检测的时空一致性特征；(2) cascade progressive restoration思想——在遥感低分辨率小目标检测中，可借鉴渐进式特征恢复/增强策略来提升小目标的可检测性；(3) 联合身份重建与下游识别任务的联合优化思路，可类比为"联合超分+检测"的多任务学习范式，这在遥感小目标检测中也有一定价值。但需注意，这些借鉴属于方法论层面的间接启发，而非直接可复现方案。

Abstract: We propose a novel collaborative approach for face super-resolution (SR) and robust person re-identification from sequential or multi-view facial images. Traditional SR methods often suffer from blurring and distortion in faces recovered from poor-quality images due to low resolution. Image- and video-based facial SR methods using facial landmarks or segmentation also have similar challenges. To overcome these limitations, we leverage multiple correlated facial observations, across time or viewpoints, by introducing a transformer-based collaborative feature aggregation method that unifies identity features from multi-sequence or multi-view data. This allows faces in multiple sequences of an individual to contribute to accurately estimating common facial features. Furthermore, we propose a cascade SR network to progressively restore the high-resolution image of the target's face with gradual facial feature unification. The unified identity representation is further utilized in person re-identification scenarios, enabling accurate matching even under severe image degradation. The exhaustive experimental results and comparisons show that our method outperforms other state-of-the-art methods, demonstrating consistent improvements in both face super-resolution and re-identification performance. Our work highlights the effectiveness of joint identity reconstruction and progressive image restoration from multiple facial inputs in enhancing downstream visual recognition tasks.

</details>


### [65] [Convolutional Neural Shading for High-Quality 3D Reconstruction from Multi-View Images](https://arxiv.org/abs/2607.28132)
*Juheon Hwang,Taewan Kim,Heeseok Oh,Jiwoo Kang*

Main category: cs.CV

TL;DR: 提出卷积神经着色（CNS）管线，利用卷积神经着色器和细粒度位移网络从多视角图像重建高质量3D形状，解决单点几何信息导致的局部细节缺失问题。

Research relevance: 弱相关（2分）。该论文属于3D重建/神经渲染领域（neural radiance fields, differentiable rendering），与遥感卫星在轨目标检测及模型轻量化方向基本无关。既不是遥感场景，也不涉及目标检测，更不涉及任何轻量化/部署方法。仅作为泛视觉/泛机器学习方法，与本课题相关性极低。

Reading priority: 1分。论文属于3D重建/神经渲染领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何轻量化/部署方法，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D形状重建（3D shape reconstruction）与神经渲染（neural rendering）。应用场景：多视角图像（multi-view images）的3D几何重建。传感器类型：普通RGB相机图像（摘要未明确说明，推断为多视角RGB图像）。目标类别：3D物体表面几何，非遥感目标。

Model architecture: 包含两个核心模块：(1) 卷积神经着色器（convolutional neural shader），用于捕捉暗区和无纹理区域的几何变化；(2) 细粒度位移网络（fine-detail displacement network），利用表面几何的空间信息，通过关联渲染坐标中的邻近值学习细粒度位移细节，缓解图像边界处的表面不规则性。整体基于neural radiance fields等神经可微渲染框架。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（pruning, quantization, knowledge distillation, NAS等均未提及）。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。且3D重建任务本身计算开销大，与星上实时推理场景不匹配。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提到通过extensive experiments验证了重建形状和渲染图像质量相比SOTA方法有显著提升。

Experiments: 摘要仅说明实验结果表明所提方法在重建形状和渲染图像质量上优于当前SOTA方法，未给出具体对比方法、消融实验细节或量化结果。

Limitations: 摘要未说明局限性。但可推断：(1) 3D重建任务计算复杂度高，不适合实时/在轨场景；(2) 未涉及遥感图像特性（如SAR、多光谱、高光谱）；(3) 未提供任何轻量化或部署可行性分析；(4) 多视角输入要求与遥感单帧/少帧观测场景不匹配。

Ideas for my research: 借鉴意义有限。可能的间接启发：(1) 卷积神经着色器利用邻域空间信息增强特征表达的思想，可类比于在遥感小目标检测中利用上下文/邻域信息增强小目标特征；(2) 细粒度位移网络通过关联邻近值学习细节，其"局部相关性建模"思路对遥感小目标特征增强有一定参考价值。但整体而言，该论文与本课题方向关联度很低，不建议作为主要参考。

Abstract: We propose a convolutional neural shading (CNS), a novel pipeline to reconstruct high-quality 3D shapes from multi-view images. Several recent studies have used neural radiance fields and other neural differentiable rendering methods to understand 3D geometry. However, these approaches rely on single-point geometric information, such as positions and normals of the surface, leading to a lack of detailed local geometry. Our approach addresses the inherent limitations of single-point information by leveraging a neural shader to capture variations even in dark and textureless regions with a convolutional neural shader, resulting in far more accurate geometry predictions. Additionally, our method mitigates surface irregularities at image boundaries by introducing a fine-detail displacement network, which utilizes spatial information of surface geometry and learns fine displacement details by correlating neighboring values in the rendering coordinates. Through extensive experiments, our proposed method has demonstrated significant quality improvements in the reconstructed shapes and rendered images over current state-of-the-art methods.

</details>


### [66] [What Makes Deep Learning Work for Traditional Chinese Medicine Tongue Diagnosis? A Comprehensive Ablation Study](https://arxiv.org/abs/2607.28148)
*Longxia Gao,Linan Wang,Yuhe Han,Junze Geng,Meng Zhang,Hanqing Zhao*

Main category: cs.CV

TL;DR: 本文针对中医舌诊自动分类任务，通过系统性消融实验（20+模型版本、5折交叉验证）探索了骨干网络、损失函数、数据增强和训练策略的设计空间，得出六条可泛化的多标签医学图像分类设计原则。

Research relevance: 弱相关（2分）。论文属于泛视觉/医学图像分类方向，与遥感、卫星、在轨部署、目标检测均无直接关联。但其系统性的消融实验方法论、骨干网络参数效率对比（ConvNeXt-Tiny）、损失函数选择（BCE vs Asymmetric Loss）、数据增强策略、集成策略等设计原则，对轻量化模型设计具有方法论层面的借鉴意义，但并非遥感场景。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，属于医学图像分类领域。但其系统消融方法论和参数效率对比思路有一定借鉴价值，可作为方法论参考而非核心文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多标签医学图像分类（multi-label medical image classification）；应用场景：中医舌诊（tongue diagnosis in TCM）；传感器类型：普通医学图像（非遥感传感器）；目标类别：舌象多标签（13至45个标签维度）。与遥感目标检测无关。

Model architecture: 对比了六种骨干网络（backbone architectures），最优为ConvNeXt-Tiny；采用weak-group ensemble（弱组集成）策略替代概率平均；涉及四种损失函数和五种数据增强策略。摘要未说明具体网络结构细节。

Lightweight method: 摘要未明确涉及剪枝、量化、知识蒸馏或NAS等模型压缩方法。但通过骨干网络选择（ConvNeXt-Tiny）体现了参数效率（parameter efficiency）的考量，属于轻量网络架构选择层面的探索，而非显式的轻量化技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署相关信息。仅提及ConvNeXt-Tiny"offers optimal parameter efficiency"，但未给出具体数值。与星上/边缘部署无直接关联。

Datasets and metrics: 数据集：TongueDx2（5,109张图像，976张专家标注）及合并数据集（11,101样本）；评价指标：weighted-F1；实验设置：5折交叉验证（5-fold cross-validation），20+模型版本系统消融。

Experiments: 最佳976样本模型weighted-F1=0.6625（ConvNeXt-Tiny + restrained augmentation + weak-group ensemble）；最佳11,101样本模型weighted-F1=0.7761。六条关键发现：(1) ConvNeXt-Tiny参数效率最优；(2) BCE比Asymmetric Loss高+2.7%；(3) 受限颜色增强至关重要；(4) weak-group ensemble替换概率平均提升+2.1%；(5) 数据规模扩展带来+20.6%提升；(6) 标签维度从13扩到45导致灾难性崩塌（0.78→0.22）。

Limitations: 摘要未说明：模型参数量/FLOPs/推理延迟等部署指标；与遥感场景无关；标签维度扩展导致的性能崩塌（0.78→0.22）问题未给出解决方案；未涉及实时推理或边缘部署可行性。

Ideas for my research: 尽管非遥感场景，其方法论可借鉴：(1) 系统化消融实验设计（20+版本、5折交叉验证）可作为轻量化遥感检测模型设计空间探索的范式；(2) 骨干网络参数效率对比思路可迁移到遥感小目标检测骨干选择；(3) BCE vs Asymmetric Loss的对比结论提示在遥感目标检测中损失函数选择需系统验证；(4) weak-group ensemble策略可借鉴到遥感检测模型集成轻量化；(5) 标签维度扩展导致性能崩塌的发现，对遥感多类别目标检测的类别扩展有警示意义。

Abstract: Deep learning has shown promise for automated tongue diagnosis in traditional Chinese medicine (TCM), yet the design space remains underexplored. We conducted a systematic ablation study spanning 20+ model versions under rigorous 5-fold cross-validation on TongueDx2 (5,109 images, 976 expert-annotated) and a merged dataset of 11,101 samples. We compared six backbone architectures, four loss functions, five augmentation strategies, and six training strategies. The best 976-sample model achieved weighted-F1 of 0.6625 using ConvNeXt-Tiny with restrained augmentation and weak-group ensemble, while the best 11,101-sample model reached weighted-F1 of 0.7761. Six key design principles emerged: (1) ConvNeXt-Tiny offers optimal parameter efficiency; (2) BCE substantially outperforms Asymmetric Loss (+2.7%); (3) restrained color augmentation is critical; (4) weak-group ensemble replacement (+2.1%) outperforms probability averaging; (5) data scaling yielded +20.6% improvement; (6) expanding from 13 to 45 label dimensions caused catastrophic collapse (0.78 to 0.22). These principles are generalizable to multi-label medical image classification with class imbalance.

</details>


### [67] [OPLD: On-Policy Latent Distillation for Multimodal Reasoning](https://arxiv.org/abs/2607.28154)
*Shoutai Zhu,Tianyang Xu,Bin Sun,Mingyuan Xu,Yu Liu,Qinzhen Guo*

Main category: cs.CV

TL;DR: 提出OPLD（On-Policy Latent Distillation）框架，通过策略内潜在蒸馏将多模态Chain-of-Thought推理能力迁移到潜在推理表征中，在多个多模态基准上达到SOTA。

Research relevance: 弱相关（2分）。该论文属于多模态视觉推理（multimodal visual reasoning）领域，核心是latent reasoning与Chain-of-Thought蒸馏，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然涉及"distillation"概念，但这里的蒸馏对象是推理能力而非模型压缩，与知识蒸馏用于轻量化模型的目的完全不同。

Reading priority: 1分。该论文属于多模态推理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，阅读优先级极低。除非对"过程级蒸馏"思想有跨领域借鉴兴趣，否则不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态视觉推理（multimodal visual reasoning）；应用场景：通用视觉问答与推理基准；传感器类型：不涉及遥感传感器；目标类别：不涉及具体目标检测类别。摘要未说明具体数据集细节。

Model architecture: 摘要未说明具体网络结构。核心思想是将多模态CoT推理过程内化为连续潜在表征（latent representations），通过OPLD框架在推理过程层面监督潜在状态，而非传统的特征级对齐。

Lightweight method: 摘要中的"distillation"指潜在推理能力蒸馏（latent reasoning distillation），即把privileged multimodal CoT的推理能力迁移到潜在推理表征中，属于推理范式层面的蒸馏，并非模型压缩意义上的knowledge distillation（如teacher-student网络压缩、特征蒸馏用于轻量化）。不涉及剪枝、量化、NAS等轻量化方法。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。该工作面向通用多模态推理，与星上/边缘部署无关。

Datasets and metrics: 摘要提到"diverse multimodal benchmarks"和"multiple benchmarks"，但未具体列出数据集名称和评价指标。摘要未说明。

Experiments: 摘要提到OPLD在多个多模态基准上一致优于现有latent reasoning方法，并在多个基准上达到SOTA。但未给出具体对比方法名称、消融实验细节和量化结果。摘要未说明。

Limitations: 摘要未说明局限性。从研究角度，该工作聚焦通用多模态推理，未涉及遥感场景、目标检测任务或模型压缩/部署约束，与在轨遥感目标检测轻量化方向基本无交集。

Ideas for my research: 借鉴价值有限，但有两个潜在思路：1）"在推理过程层面监督而非特征级对齐"的思想，若未来在遥感目标检测中引入推理蒸馏（如将大模型的检测推理逻辑蒸馏到轻量检测头），可参考其"过程级监督"而非"输出级监督"的思路；2）OPLD的on-policy策略内蒸馏框架，若类比到遥感检测模型的知识蒸馏中，可考虑在训练过程中动态调整蒸馏目标而非静态对齐，可能提升轻量模型对复杂遥感场景的适应能力。但这些仅是间接类比，需谨慎评估。

Abstract: Interleaved multimodal Chain-of-Thought (CoT) improves visual reasoning by incorporating auxiliary visual evidence into intermediate reasoning. However, existing approaches remain constrained by externally defined reasoning traces and visual operations, limiting their ability to develop flexible and abstract visual thinking. Reasoning with latent has recently offered a promising direction by internalizing intermediate computation into continuous representations. Nevertheless, existing visual-latent methods mainly supervise latent states through alignment with compressed auxiliary visual features, treating them as proxies for visual observations rather than active reasoning states. Consequently, they capture the provided evidence but fail to fully internalize the abstract reasoning process induced by multimodal CoT. In this paper, we propose OPLD (On-Policy Latent Distillation), a simple framework that transfers the reasoning capability induced by privileged multimodal CoT into latent reasoning representations. Extensive experiments on diverse multimodal benchmarks demonstrate that OPLD consistently outperforms existing latent reasoning methods and achieves state-of-the-art performance on multiple benchmarks. The results suggest that supervising latent representations at the reasoning-process level provides a more effective paradigm for multimodal latent reasoning than conventional feature-level alignment.

</details>


### [68] [S-Avatar: Diffusion-Guided Gaussian Head Avatars from a Single Image](https://arxiv.org/abs/2607.28164)
*Hail Song,Seokhwan Yang,Jiwon Yang,Woojin Cho,Woontack Woo*

Main category: cs.CV

TL;DR: 提出 S-Avatar，一种从单张图像生成逼真 3D 头部虚拟形象的方法，结合 diffusion 引导的 3D Gaussian Splatting (3DGS) 生成与 FLAME 参数化头部模型控制，实现实时动态头部渲染。

Research relevance: 弱相关（2 分）。论文属于泛视觉/泛机器学习方法，聚焦 3D 头部虚拟形象重建与 VR/AR 应用，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。唯一可借鉴点在于其 diffusion 引导的 3DGS 生成与实时渲染思路，以及 FLAME 参数化控制策略，但这些与遥感在轨目标检测场景差异极大。

Reading priority: 1 分。论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于 3D 头部虚拟形象重建领域，无遥感场景、无目标检测、无轻量化方法，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：单图像 3D 头部虚拟形象重建与动画生成（head avatar reconstruction and animation）。应用场景：Virtual Reality (VR) / Augmented Reality (AR)。传感器类型：普通 RGB 单目图像。目标类别：人类头部/面部。与遥感目标检测无关。

Model architecture: 三阶段 pipeline：(1) diffusion-based Gaussian splat generation module，从单张图像直接合成高分辨率 3D Gaussian Splatting (3DGS)；(2) 将参数化头部模型 FLAME 与生成的 3DGS 对齐，通过优化其参数和空间变换；(3) 构建 binding template 编码初始 splats 与 FLAME 之间的空间关系，通过 binding template 变形 3DGS 实现动态头部渲染。

Lightweight method: 摘要未说明。论文未涉及剪枝 (pruning)、量化 (quantization)、知识蒸馏 (knowledge distillation)、NAS 等模型压缩方法。其"实时渲染"（real-time rendering）能力主要来自 3DGS 本身的高效渲染特性，而非针对网络模型的轻量化设计。

Onboard deployability: 摘要未说明。论文提到可"实时渲染"（render in real time）动态 3D 头部虚拟形象，但未给出具体参数量、FLOPs、延迟、显存、能耗等部署指标。且该实时性针对的是 VR/AR 渲染场景，与星上/边缘部署（FPGA/NPU/TensorRT）无关。

Datasets and metrics: 摘要未说明具体数据集名称。仅在公开数据集（public datasets）上评估，评价指标涉及 novel-view generation 和 expression generation 的 realism 与 consistency，与 state-of-the-art 方法对比。未涉及遥感数据集（如 DOTA、HRSC2016、DIOR 等）或目标检测指标（mAP、FPS 等）。

Experiments: 摘要未说明详细实验设置。仅在公开数据集上与 state-of-the-art 方法对比，声称在 novel-view 和 expression generation 上取得更优的 realism 和 consistency。无消融实验、无部署相关实验。

Limitations: (1) 与遥感/卫星/在轨目标检测方向完全无关，属于 3D 视觉/图形学领域；(2) 摘要未提供任何模型压缩、轻量化或部署相关信息；(3) 未给出定量指标（如 PSNR、SSIM、FID 等）和具体数据集名称；(4) 未讨论在低算力、受限资源（如星载平台）上的可行性；(5) 3DGS 渲染通常需要较高 GPU 资源，与星上边缘部署的算力约束可能冲突。

Ideas for my research: (1) diffusion-guided 生成模块的思路可类比用于遥感数据增强或合成训练样本，缓解小目标样本不足问题；(2) 3DGS 的实时渲染策略与 binding template 参数化控制思想，可启发遥感目标检测中利用 3D 几何先验辅助旋转框检测（oriented object detection）的建模；(3) 但整体借鉴价值有限，建议仅作泛读，不投入深入研读时间。

Abstract: We propose S-Avatar, a novel method for generating photorealistic 3D head avatars from a single image using a diffusion-guided 3D model generation module and strategies for animating 3D Gaussian Splatting (3DGS). While single-image head avatar reconstruction is crucial for lifelike Virtual Reality (VR) applications, existing approaches often struggle to preserve 3D consistency under unseen viewpoints. S-Avatar addresses this limitation through a three-stage pipeline. First, a high-resolution 3DGS is synthesized directly from a single image using a diffusion-based Gaussian splat generation module. Next, the parametric head model FLAME is aligned with the generated 3DGS by optimizing its parameters and spatial transformations. Finally, to adapt the 3DGS to FLAME variations, we construct a binding template that encodes the spatial relationship between the initial splats and FLAME. The dynamic 3D head avatar can then be rendered in real time by deforming the 3DGS with the binding template. By combining diffusion-guided canonical 3DGS generation with FLAME-based control, our method achieves efficient and accurate reconstruction with enhanced 3D consistency. Evaluations on public datasets demonstrate that S-Avatar outperforms state-of-the-art methods in novel-view and expression generation, achieving superior realism and consistency. Consequently, our approach represents a significant advance in accessible avatar creation, applicable to a wide range of VR/AR applications. The project page is available at https://github.com/hailsong/savatar.

</details>


### [69] [Think with Extra-Image: A Farmland Segmentation Agent Driven by Spatio-Temporal Information Gain](https://arxiv.org/abs/2607.28186)
*Haiyang Wu,Weiliang Mu,Zhuofei Du,Dandan Zhong,Kaijie Shi,Haifeng Li,Chao Tao*

Main category: cs.CV

TL;DR: 提出FarmSeeker，一个动态农田遥感图像分割智能体，从信息瓶颈视角将分割重新定义为由任务相关的额外时空信息增益驱动的动态决策过程，并构建了首个支持推理-查询的全球尺度高分辨率农田分割基准GSFS-Bench。

Research relevance: 弱相关（2分）。论文涉及remote sensing和遥感图像分割（farmland segmentation），属于遥感场景理解范畴，但任务类型是语义分割而非目标检测，且完全不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理、星上部署等任何模型压缩或加速手段。与"遥感卫星在轨目标检测与模型轻量化"方向仅在"遥感"这一维度有交集，在"目标检测"和"轻量化/部署"两个核心维度上均无关联。

Reading priority: 2分。论文与遥感相关，但任务为分割而非目标检测，且完全不涉及轻量化/部署。仅"动态决策/按需处理"的思想对降低计算开销有一定启发，但整体与课题方向匹配度低，可作为泛读参考而非精读对象。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：遥感图像语义分割（farmland remote sensing image segmentation, FRSI segmentation）。应用场景：农田/耕地遥感图像分割，关注物候（phenology）和空间上下文导致的农田外观变化。传感器类型：摘要未说明（推测为光学遥感影像）。目标类别：农田/耕地（farmland）与其他土地覆盖类型（land-cover）的区分。

Model architecture: FarmSeeker是一个动态分割智能体（dynamic segmentation agent），核心设计包括：1）识别模糊区域（identifies ambiguous regions）；2）推理模糊原因（reasons about their causes）；3）按需查询额外时空信息（queries extra spatio-temporal information on demand）。整体采用"Think with Intra-Image"范式的替代方案，从信息瓶颈（information bottleneck）视角将分割建模为动态决策过程。具体网络结构细节摘要未说明。

Lightweight method: 摘要未说明。论文不涉及任何模型轻量化方法（pruning, quantization, knowledge distillation, NAS等均未提及），也未讨论实时推理或边缘/星上部署。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。且该方法的"按需查询额外时空信息"机制需要外部信息源支持，与在轨卫星的自主、受限计算和通信环境存在天然冲突，部署可行性存疑。

Datasets and metrics: 构建了GSFS-Bench，据称是首个全球尺度（global-scale）、高分辨率（high-resolution）的FRSI分割基准，支持推理-查询（reasoning-querying）评估。具体数据集规模、分辨率、标注方式及评价指标（如mIoU等）摘要未说明。

Experiments: 实验表明FarmSeeker相比现有方法（existing methods）在分割性能上更稳定（more stable segmentation performance）。具体对比方法、消融实验、定量结果摘要未说明。

Limitations: 1）任务为语义分割而非目标检测，与课题核心任务不匹配；2）完全不涉及轻量化/压缩/加速方法；3）"按需查询额外时空信息"依赖外部数据源，不适合在轨自主推理场景；4）未提供任何部署相关指标（参数量、FLOPs、延迟等）；5）GSFS-Bench基准的具体细节和公开性需进一步确认。

Ideas for my research: 1）"信息瓶颈视角"和"动态决策"思想可借鉴：在轨目标检测中，可考虑对模糊/低置信度区域进行动态增强处理或选择性推理，而非对所有区域统一计算，这有助于降低平均计算开销；2）"识别模糊区域→推理原因→按需处理"的pipeline思想可迁移到小目标检测中，对难以判别的目标区域进行针对性处理；3）GSFS-Bench的全球尺度高分辨率基准构建思路，对构建遥感目标检测评测基准有参考价值；4）但整体借鉴价值有限，因为核心任务（分割vs检测）和核心诉求（轻量化）均不匹配。

Abstract: Existing farmland remote sensing image (FRSI) segmentation follows a "Think with Intra-Image" paradigm, assuming that the current image contains sufficient visual evidence for reliable segmentation. Yet farmland appearance varies with phenology and spatial context and is often confused with other land-cover, making instantaneous, local observations inadequate. Thus, segmentation ambiguity stems not only from limited model representation, but more fundamentally from the required spatio-temporal information lying beyond the current image. Based on this insight, we redefine FRSI segmentation from an information bottleneck perspective as a dynamic decision process driven by task-relevant extra spatio-temporal information gain. We further propose FarmSeeker, a dynamic FRSI segmentation agent that identifies ambiguous regions, reasons about their causes, and queries extra spatio-temporal information on demand for accurate segmentation. To evaluate FarmSeeker, we construct GSFS-Bench, the first global-scale, high-resolution FRSI segmentation benchmark that supports reasoning-querying. Experiments show that FarmSeeker achieves more stable segmentation performance than existing methods. The project is publicly available at: https://withoutocean.github.io/FarmSeeker/

</details>


### [70] [Scaling Vision-Language Models Is Not Enough to Mitigate Bias](https://arxiv.org/abs/2607.28211)
*Ioannis Sarridis,Ioannis Kompatsiaris,Symeon Papadopoulos*

Main category: cs.CV

TL;DR: 对194个公开VLM（视觉-语言模型）进行大规模实证研究，分析模型规模、训练数据属性与架构选择对虚假相关性鲁棒性的影响。

Research relevance: 与本研究方向基本无关（1分）。论文研究的是Vision-Language Models（如CLIP）在虚假相关性（spurious correlations）下的鲁棒性，属于多模态学习与模型鲁棒性研究范畴。不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景；不涉及目标检测、小目标检测或旋转框检测；也不涉及剪枝、量化、知识蒸馏、NAS等轻量化方法或星上/边缘部署。唯一可能的间接关联是论文涉及"model scale"（模型规模）与性能的关系，但这与轻量化/模型压缩的工程目标完全不同。

Reading priority: 1分。论文主题为VLM鲁棒性实证研究，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何模型压缩/部署方法，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：VLM鲁棒性实证分析（虚假相关性下的最差组准确率评估）。应用场景：通用多模态系统，非遥感场景。传感器类型：不适用（使用自然图像数据集ImageNet、CelebA、UrbanCars）。目标类别：人脸属性（CelebA）、车辆/城市场景（UrbanCars）、通用图像分类（ImageNet）。

Model architecture: 覆盖16个模型家族、194个公开VLM，包括不同模型规模（model sizes）、patch size、image resolution等架构选择。具体架构细节（如具体网络结构、注意力机制等）摘要未说明。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等轻量化或模型压缩方法。仅讨论了"model scale"（模型规模）与鲁棒性性能之间的Spearman相关性，属于分析性研究而非压缩方法研究。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。论文关注的是模型鲁棒性而非部署效率。

Datasets and metrics: 数据集：ImageNet（整体性能）、CelebA（单一属性偏差）、UrbanCars（复杂多属性偏差），共24个训练数据集。评价指标：Spearman相关系数（ρ）、worst-group accuracy（最差组准确率）。实验设置：194个VLM、16个模型家族、3个评估基准。

Experiments: 主要发现：(1) 模型规模与性能的Spearman相关性随评估基准从ImageNet（ρ=0.68）到单一属性偏差（ρ=0.48）再到多属性偏差（ρ=0.05）逐渐减弱；(2) 训练数据的属性（大小和质量）与worst-group accuracy的关系更一致；(3) curated数据集在相近规模下比uncurated数据集提升高达25%；(4) 架构选择（patch size、image resolution）的影响高度依赖基准和偏差类型。

Limitations: 摘要未说明局限性。但可指出：论文聚焦于VLM鲁棒性而非遥感目标检测或模型轻量化，与本研究方向无直接关联；研究基于自然图像数据集，未涉及遥感影像特性（如复杂背景、小目标、多尺度等）。

Ideas for my research: 借鉴意义有限，但可提取两点间接启示：(1) 论文揭示"模型规模与性能的相关性会随任务复杂度/偏差类型而变化"，提示在遥感目标检测轻量化中，不能仅以模型规模作为性能预测指标，需结合具体遥感任务特性评估；(2) 训练数据质量（curated vs uncurated）对worst-group accuracy影响显著，提示在遥感在轨检测中，训练数据的质量与分布（如不同传感器、不同天气条件）可能比单纯增大模型更重要。但整体上该论文对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点很少。

Abstract: Vision-Language Models (VLMs) such as CLIP are now foundational to multimodal systems, yet their robustness to spurious correlations remains poorly understood at scale. We present the first large-scale empirical study of 194 publicly available VLMs, including 16 model families, covering a wide range of model sizes, 24 training datasets, and three evaluation benchmarks, namely ImageNet (overall performance), CelebA (typical single-attribute bias), and UrbanCars (complex multi-attribute biases). Across these settings, the Spearman correlation between model scale and performance weakens as evaluation shifts from ImageNet ($ρ{=}0.68$) to single-attribute ($ρ{=}0.48$) and further to multi-attribute ($ρ{=}0.05$) bias benchmarks. In contrast, properties of the training data (size and quality) show more consistent relationships with worst-group accuracy across both bias benchmarks. Notably, curated datasets yield improvements of up to 25% over uncurated alternatives at a comparable scale. Finally, the effect of architectural choices (e.g., patch size, image resolution) is highly context-dependent, varying with the nature of the benchmark, including the type of bias and its spatial distribution within images.

</details>


### [71] [FaithEyes: Towards Faithful Tool Use via Multi-Agent Process-Image Verification](https://arxiv.org/abs/2607.28225)
*Haoqing Wang,Xingrun Xing,Wei Xia,Ziheng Li,Yehui Tang*

Main category: cs.CV

TL;DR: 提出FaithEyes，一个多智能体自评判框架，用于提升Agentic视觉语言模型（VLM）工具调用的忠实度，通过VLM判断过程图像是否有助于回答问题，并将判断注入推理上下文和工具奖励中，抑制reward hacking。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦于Agentic VLM的工具调用忠实度（tool faithfulness）问题，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。唯一可能的间接联系是：论文涉及"工具调用浪费计算"（waste computation）这一效率问题，以及多智能体自评判框架的推理设计，但这些与遥感目标检测轻量化方向关联甚微。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于通用Agentic VLM领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态视觉推理（visual perception and reasoning）中的工具调用忠实度评估与改进。应用场景：通用视觉问答与推理基准，非遥感场景。传感器类型：自然图像（通用视觉），非遥感/卫星/航空影像。目标类别：摘要未说明（非检测任务）。

Model architecture: FaithEyes采用多智能体（multi-agent）框架：主智能体（main agent）负责推理和工具调用（如cropping、code-based image manipulation），子智能体（subagent）由模型自身充当，用于评判主智能体的工具调用是否有用。训练采用两阶段SFT + RL pipeline，基于改编的开源数据。判断结果注入推理上下文作为tool observation的一部分，同时用于按helpful-tool ratio缩放工具奖励。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS、轻量网络等模型压缩或加速方法。虽然提到"waste computation"（工具调用浪费计算），但这是关于工具调用效率/忠实度的问题，而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且多智能体框架在推理时需要模型自身作为子智能体进行评判，增加了推理开销，与星上/边缘部署的轻量化需求相悖。

Datasets and metrics: 数据集：改编的开源数据（adapted open-source data），具体名称摘要未说明。评价指标：视觉感知与推理基准上的准确率（accuracy）以及工具忠实度（tool faithfulness）。具体基准名称和指标细节摘要未说明。

Experiments: 摘要仅说明FaithEyes在视觉感知和推理基准上取得竞争性或更优的准确率，同时显著提升工具忠实度。具体对比方法、消融实验细节摘要未说明。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于通用多模态推理领域。2. 多智能体自评判框架在推理时依赖模型自身作为子智能体，增加推理计算开销，不利于在轨部署。3. 摘要未报告任何部署相关指标（参数量、FLOPs、延迟等）。4. 摘要未说明具体数据集、基准名称和实验细节。

Ideas for my research: 1. 借鉴意义有限，但"工具调用忠实度"思想可类比到遥感目标检测中的"注意力/特征是否真正聚焦于目标区域"问题——即检测模型是否真正利用了目标区域特征而非背景先验。2. "helpful-tool ratio"奖励缩放的思想可类比到检测训练中，对无效特征/无效分支的贡献进行抑制。3. 多智能体自评判框架中"模型自身作为评判者、避免外部依赖"的设计思路，在星上推理场景中可借鉴为"自包含、无外部依赖"的模型设计原则。总体而言，借鉴价值较低。

Abstract: Agentic vision-language models (VLMs), which interleave textual reasoning with explicit tool calls such as cropping and code-based image manipulation, have emerged as a compelling paradigm for reliable and interpretable multimodal reasoning. However, recent studies have revealed that such models often use tools unfaithfully. Many process images are irrelevant to the question (e.g., the tool crops the wrong region or misses the queried target), yet the call still receives full credit and the model still answers correctly. Such decorative or misaligned tool calls waste computation and reveal that the model leans on prior knowledge or the original image rather than the evidence it retrieves. This may stem from two limitations of prevailing methods: the tool reward fails to distinguish useful from useless calls, and tool feedback carries no signal of usefulness. To this end, we introduce FaithEyes, a multi-agent self-judging framework. Concretely, we use a VLM to judge whether each process image helps answer the question. The judgement is injected into the reasoning context as part of the tool observation to help subsequent reasoning, and meanwhile is used to scale the tool reward by the helpful-tool ratio to suppress reward hacking. To keep judgement available at evaluation and thus ensure train-test consistency, we further design a multi-agent framework where the model itself serves as a subagent to judge the tool calls from main agent, eliminating any dependence on an external model at inference. Training via a two-stage SFT + RL pipeline on adapted open-source data, FaithEyes attains competitive or superior accuracy across visual perception and reasoning benchmarks, while markedly improving tool faithfulness. The homepage is at https://github.com/Mosi-AI/FaithEyes.

</details>


### [72] [EgoGenesis: Egocentric World-Action Modeling with Online Anchored Projective Memory and Action-3D RoPE](https://arxiv.org/abs/2607.28243)
*Zexuan Yan,Yuzhou Wu,Yue Ma,Zonghang He,Kaibo Yin,Xiaobing Tu,Yinggui Wang,Jinkui Ren,Xiantao Zhang,Shijian Wang,Jinghong Liu,Linfeng Zhang*

Main category: cs.CV

TL;DR: 提出一个基于视频生成的自我中心世界-动作模拟器（egocentric world-action simulator），通过几何感知条件机制合成可控的高质量操作视频，用于扩充真实训练数据以提升具身智能体的泛化能力。

Research relevance: 弱相关（2分）。该论文属于具身智能（embodied AI）与视频生成领域，聚焦于自我中心（egocentric）操作视频合成与数据增强，完全不涉及遥感、卫星、地球观测、航空图像、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测或模型轻量化。仅从"数据增强提升下游模型泛化"这一方法论角度对遥感小样本场景可能有间接借鉴意义，但整体与本研究方向基本无关。

Reading priority: 1分。该论文属于具身智能与视频生成方向，与遥感卫星在轨目标检测及模型轻量化基本无关，不建议优先阅读。仅当对"数据增强提升泛化"这一通用方法论感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：自我中心视频生成与数据增强（egocentric video generation & data augmentation）；应用场景：具身智能操作（embodied manipulation）、机器人操作任务；传感器类型：自我中心相机（egocentric camera）；目标类别：操作动作、末端执行器运动、场景物体。

Model architecture: 基于预训练视频生成先验（pretrained video generation prior），引入两种几何感知条件机制：(1) Online Anchored Projective Memory (OAPM)——保留首帧3D场景锚点并周期性刷新近期状态，用于自回归生成；(2) Action-3D Rotary Position Embedding (A3D-RoPE)——将末端执行器运动编码为相机感知的3D旋转坐标，注入skeleton-to-video交叉注意力实现精确动作控制。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其应用场景为机器人操作而非星上/边缘部署。

Datasets and metrics: 数据集：真实机器人操作轨迹数据（400条真实轨迹 + 400条生成轨迹）；评价指标：out-of-distribution real-robot success rate（单臂任务从77%提升到84%，双臂任务从53%提升到70%）。具体数据集名称摘要未说明。

Experiments: 主要实验：用400条生成轨迹增强400条真实轨迹，在单臂任务上将OOD真实机器人成功率从77%提升到84%，在双臂任务上从53%提升到70%，证明合成数据能显著提升下游WAM（world-action model）泛化能力。消融实验细节摘要未说明。

Limitations: 摘要未说明局限性。但可推断：该方法依赖预训练视频生成先验，计算开销可能较大；生成视频与真实物理世界的一致性、长序列生成的稳定性等潜在问题未在摘要中讨论；与遥感/卫星/目标检测/轻量化方向无直接关联。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但"合成数据增强提升下游模型泛化"的思想可迁移到遥感小样本目标检测场景：例如利用生成模型合成遥感小目标/旋转框样本（如舰船、飞机）来扩充稀缺标注数据，从而在保持模型轻量化的同时提升检测精度。此外，其"几何感知条件机制"（如3D锚点、旋转位置编码）对遥感旋转框检测中几何信息建模有一定启发，但整体借鉴价值有限。

Abstract: Egocentric video offers rich manipulation experience for embodied AI, yet collecting diverse egocentric data across scenes, objects, motions, and embodiments remains costly. We present \method, an egocentric world-action simulator that synthesizes controllable, high-quality manipulation videos to expand scarce real-world training data. \method{} builds on a pretrained video generation prior and introduces two geometry-aware conditioning mechanisms. Online Anchored Projective Memory (OAPM) preserves a first-frame 3D scene anchor while periodically refreshing a recent state during autoregressive generation. Action-3D Rotary Position Embedding (A3D-RoPE) encodes end-effector motion with camera-aware 3D rotary coordinates, injecting action geometry into skeleton-to-video cross-attention for precise control. Together, these components improve visual fidelity, geometric stability, and action alignment in long egocentric rollouts. Moreover, augmenting 400 real trajectories with 400 \method-generated trajectories improves out-of-distribution real-robot success from 77\% to 84\% on single-arm tasks and from 53\% to 70\% on dual-arm tasks, demonstrating that the synthesized data substantially improve downstream WAM generalization.

</details>


### [73] [Space2Ground 2.0: A Multi-Source Dataset and Framework for Agricultural Monitoring through Fusion of Street-Level and Satellite Imagery](https://arxiv.org/abs/2607.28247)
*Iason Tsardanidis,Alkiviadis Koukos,George Choumos,Vasileios Sitokontantinou,Charalampos Kontoes*

Main category: cs.CV

TL;DR: Space2Ground 2.0 是一个多源农业监测框架，将 Sentinel-1 SAR、Sentinel-2 多光谱时序数据与 Mapillary 众包街景图像融合，用于地块级作物分类，并在塞浦路斯构建了包含 46,050 张标注街景图像和 8,581 个农业地块的公开基准数据集。

Research relevance: 弱相关（2分）。论文涉及遥感（Sentinel-1 SAR、Sentinel-2 多光谱）和 Earth observation，但核心任务是地块级作物分类（crop classification），并非目标检测（object detection），也不涉及任何轻量化/在轨部署方法。属于遥感场景理解/分类方向，与"遥感卫星在轨目标检测与模型轻量化"课题仅存在遥感数据层面的弱关联。

Reading priority: 2分（低优先级）。该论文与遥感数据相关，但核心任务是作物分类而非目标检测，且完全不涉及轻量化/在轨部署。仅当需要了解多源遥感数据融合或大规模遥感数据集构建方法时可作为背景参考，对"在轨目标检测模型轻量化"课题的直接借鉴价值有限。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：地块级作物分类（parcel-level crop classification）与多模态农业监测；应用场景：农业监测、作物类型识别、农业政策支持；传感器类型：Sentinel-1 SAR、Sentinel-2 多光谱、车载相机街景图像（Mapillary 平台）；目标类别：农业作物类型（具体类别摘要未说明）。

Model architecture: 摘要未说明具体模型结构。论文重点在于数据处理流水线（semantic filtering, image quality assessment, viewpoint-based parcel association, dataset refinement）和多源数据融合框架，而非提出新的检测/分类网络架构。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS 或轻量网络设计等任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时推理等部署相关信息，也未涉及星上/边缘部署（onboard/edge deployment）场景。

Datasets and metrics: 数据集：Space2Ground 2.0 基准数据集，包含 46,050 张标注街景图像（从超过 900,000 张初始图像中筛选），关联 8,581 个农业地块，覆盖塞浦路斯 2022 生长季；数据源包括 Sentinel-1 SAR、Sentinel-2 多光谱时序和 Mapillary 街景图像。评价指标：摘要未明确说明具体指标（推测为分类准确率等，但摘要未给出）。

Experiments: 实验包括单源（single-source）和多源（multi-source）观测下的地块级作物分类实验。结果表明街景图像提供的细粒度视觉信息与卫星时序数据融合后能增强分类性能。具体对比方法、消融实验和量化结果摘要未说明。

Limitations: 1. 论文聚焦于作物分类而非目标检测，与在轨目标检测任务不直接相关；2. 未涉及任何模型轻量化或部署优化方法；3. 摘要未报告模型性能指标、计算开销等关键量化信息；4. 依赖众包街景数据（Mapillary），数据获取依赖地面车辆采集，与卫星在轨自主处理场景差异较大；5. 实验仅覆盖塞浦路斯单一区域和单一生长季，泛化性待验证。

Ideas for my research: 1. 多源数据融合思路可借鉴：SAR + 多光谱 + 地面图像的跨模态互补策略，对遥感目标检测中多传感器融合有参考价值；2. 数据流水线中的语义过滤和质量评估（image quality assessment）思想可用于在轨目标检测的数据预处理环节；3. 大规模众包数据筛选与标注的自动化流程（从 90 万张筛选到 4.6 万张）对构建高质量遥感训练数据集有方法论借鉴；4. 但该论文不提供任何轻量化模型设计或部署加速的参考，对"在轨轻量化"核心诉求帮助有限。

Abstract: Accurate and scalable parcel-level agricultural monitoring remains challenging because satellite Earth Observation alone provides only an overhead perspective of agricultural parcels, while optical observations are further affected by cloud-induced temporal gaps. This paper presents Space2Ground 2.0, a multi-source framework integrating Sentinel-1 SAR and Sentinel-2 multispectral time series with geo-tagged street-level imagery acquired using vehicle-mounted cameras and shared through the Mapillary platform. A largely automated processing pipeline performs semantic filtering, image quality assessment, viewpoint-based parcel association, and dataset refinement, transforming large volumes of crowdsourced imagery into parcel-linked, analysis-ready data. Applied over Cyprus during the 2022 growing season, the pipeline produced a curated dataset of 46,050 annotated street-level images, selected from an initial collection exceeding 900,000 images and linked with satellite information for 8,581 agricultural parcels. The practical value of the dataset was assessed through parcel-level crop classification experiments using both single- and multi-source observations. The results demonstrate that street-level imagery provides complementary fine-scale visual information that enhances classification when integrated with satellite time series. Overall, Space2Ground 2.0 provides an openly available benchmark dataset and a reproducible methodology for multimodal agricultural monitoring, with potential applications in visual verification, reduced reliance on costly field inspections, and data-driven agricultural policy implementation.

</details>


### [74] [TARS: Timestep-Aware Data Scaling for 3D-Free Video Re-Shooting](https://arxiv.org/abs/2607.28261)
*Jiwen Liu,Shujuan Li,Xiaohan Li,Zijie Meng,Xinyue Liu,Yulong Xu,Yan Zhou,Guoxin Zhang*

Main category: cs.CV

TL;DR: 提出TARS，一种无需3D先验的文本驱动视频重拍（video re-shooting）范式，通过时间步敏感性分析发现相机运动主要在高噪声阶段建立，并采用自监督训练实现可控相机运动与视角切换。

Research relevance: 弱相关（1-2分）。该论文属于视频生成/视频重拍（video re-shooting）领域，核心是文本驱动的相机运动与视角控制，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。也不涉及目标检测、小目标检测、旋转框检测、舰船/飞机/车辆检测等遥感目标检测任务。同时不涉及任何模型轻量化方法（剪枝、量化、蒸馏、NAS、实时推理、边缘部署等）。属于泛视觉/生成式AI方法，与本研究方向基本无关。

Reading priority: 1分。该论文属于视频生成/重拍领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感场景或任何模型压缩/加速方法，阅读优先级极低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视频重拍（video re-shooting），即通过文本语义指定相机运动与视角来重新生成视频。应用场景：视频生成、相机运动控制、视角切换（第一/第三人称视角）。传感器类型：普通RGB视频。目标类别：不涉及目标检测，无具体目标类别。

Model architecture: TARS范式，基于扩散模型（diffusion-based）的视频生成框架。核心设计包括：1) 时间步敏感性分析（timestep-wise sensitivity analysis），发现相机运动主要在高噪声阶段建立；2) 自监督训练（self-supervised training）学习相机动力学与基础视觉表征；3) 联合文本-相机条件（joint textual-camera conditioning）机制。整体为3D-free范式，不依赖显式3D重建。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法，包括剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等均未提及。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。且视频生成任务本身计算开销大，与星上/边缘部署场景不匹配。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面提到"更准确和时间一致的相机控制"（more accurate and temporally consistent camera control），但未给出具体指标名称（如FVD、LPIPS等）。

Experiments: 摘要提到"Extensive experiments show that TARS provides more accurate and temporally consistent camera control than prior methods"，即通过大量实验证明TARS相比先前方法在相机控制的准确性和时间一致性上更优。但摘要未给出具体对比方法名称、定量结果或消融实验细节。

Limitations: 1) 与遥感卫星在轨目标检测及轻量化方向完全无关，属于视频生成领域；2) 摘要未说明模型规模、推理速度、计算资源需求，无法评估部署可行性；3) 未提供具体数据集、定量指标和对比基线细节；4) 自监督训练的数据规模与泛化边界未说明；5) 生成式方法固有的时间一致性、伪影等问题在摘要中未充分讨论。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但可借鉴的间接思路有限：1) 时间步敏感性分析（timestep-wise sensitivity analysis）的思想——在遥感目标检测中，可类比分析不同网络层/不同特征分辨率对检测精度的敏感性，从而指导剪枝（pruning）时优先保留关键层；2) 自监督训练策略——在遥感数据标注稀缺的场景下，可借鉴自监督预训练思路提升小样本检测性能。但整体借鉴价值较低，不建议作为核心参考。

Abstract: Video re-shooting aims to regenerate videos with controllable camera motion and viewpoint. Existing methods rely on explicit 3D priors, which are limited by reconstruction quality and often perform poorly when synthesizing previously unseen regions, or on paired videos with different camera trajectories, whose scarcity hinders generalization. We revisit video re-shooting through text-driven semantic viewpoint specification, enabling control over shot scale, viewing angle, and first-/third-person perspective. To this end, we propose TARS, a 3D-free video re-shooting paradigm. Timestep-wise sensitivity analysis reveals that camera motion is primarily established during high-noise stages, where coarse spatiotemporal structures are formed. Based on this insight, we introduce self-supervised training to learn camera dynamics and fundamental visual representations without paired re-shooting data or 3D reconstruction. Through data scaling and joint textual-camera conditioning, TARS supports robust camera and viewpoint control, plausibly synthesizing regions beyond the source view under large camera motions while enabling reverse-angle re-shooting and perspective switching. Extensive experiments show that TARS provides more accurate and temporally consistent camera control than prior methods. Project Page: https://ymlinfeng.github.io/TARS.github.io/

</details>


### [75] [Theia: Large-Scale Multimodal Captioning and Automated Validation of the Incidents1M Dataset for Data-Free Distillation](https://arxiv.org/abs/2607.28269)
*Simone Giano,Lorenzo Severini,Alessandro Galdelli,Adriano Mancini*

Main category: cs.CV

TL;DR: 提出一种利用LLM自动构建并验证大规模多模态灾害响应数据集的方法，通过Qwen3.5生成高质量文本描述，并引入image-blind LLM-as-a-Judge验证流程，服务于Data-Free Knowledge Distillation (DFKD)。

Research relevance: 弱相关（2分）。论文聚焦于Vision-Language Models (VLMs)在灾害管理领域的多模态数据集构建与Data-Free Knowledge Distillation (DFKD)，属于泛视觉-语言多模态与知识蒸馏方法研究。虽然涉及knowledge distillation这一轻量化相关技术，但场景是disaster management而非遥感/卫星，目标也不是遥感目标检测，与"遥感卫星在轨目标检测模型轻量化"方向关联度低。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向关联度低，主要价值在于DFKD数据构建与验证方法论上的间接启发，可作为知识蒸馏方向的泛读参考，不建议作为核心文献精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态数据集构建与自动验证，服务于Data-Free Knowledge Distillation (DFKD)。应用场景：disaster management（灾害管理/灾害响应）。传感器类型：不涉及遥感传感器，基于自然图像数据集Incidents1M和CrisisMMD。目标类别：灾害事件相关图像，非遥感目标（舰船/飞机/车辆等）。

Model architecture: 使用两种Qwen3.5架构生成图像文本描述：4B dense模型和35B Mixture-of-Experts (MoE)模型；引入Qwen3.5-9B作为image-blind LLM-as-a-Judge验证器，通过遮蔽原始图像模拟student model在data-free distillation中的modality gap。

Lightweight method: 涉及Data-Free Knowledge Distillation (DFKD)作为核心知识迁移方法，但论文本身不提出新的模型压缩/加速算法，而是聚焦于为DFKD提供高质量多模态训练数据。未涉及pruning、quantization、NAS、FPGA/NPU/TensorRT等具体轻量化或部署技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。使用的Qwen3.5-35B MoE模型体量巨大，与在轨轻量化部署方向相悖。

Datasets and metrics: 数据集：Incidents1M（vision-only，无文本描述）、CrisisMMD（存在text-image语义错配）。从Incidents1M恢复100,000张图像并生成文本描述。评估：173,179个label pairs上的语义一致性评分（78.65/100），Precision 77.6%，Recall 46.0%。

Experiments: 对比两种Qwen3.5架构（4B dense vs 35B MoE）生成caption的语义一致性；通过LLM-as-a-Judge自动评估揭示模型保守caption行为（高Precision低Recall），并暴露原始ground truth中的人类标注不一致问题。

Limitations: 1. 与遥感/卫星场景完全无关，不涉及遥感图像、SAR、高光谱或多光谱数据；2. 不涉及目标检测任务；3. 不涉及模型轻量化/在轨部署；4. 使用35B MoE等超大模型，计算开销巨大，与轻量化方向相悖；5. 摘要未说明DFKD在student model上的实际压缩效果或部署收益。

Ideas for my research: 1. Data-Free Knowledge Distillation (DFKD)思想可借鉴：在遥感在轨场景中，教师模型（大模型）与student模型（轻量模型）之间的知识蒸馏可考虑data-free方式，减少对大量标注遥感数据的依赖；2. LLM-as-a-Judge的自动验证思路可迁移到遥感caption/标注质量评估中，用于验证遥感图像-文本语义对齐；3. 高Precision低Recall的保守caption策略对遥感目标检测蒸馏中减少false positive噪声有参考价值；4. 但需注意：该工作面向自然图像和灾害场景，迁移到遥感需重新设计数据构建与验证流程。

Abstract: The deployment of Vision-Language Models (VLMs) in critical domains like disaster management requires high-quality multimodal datasets, especially for transferring knowledge via Data-Free Knowledge Distillation (DFKD). However, existing datasets in this domain either entirely lack descriptive text, such as Incidents1M, or suffer from severe text-image semantic misalignment, such as CrisisMMD. In this work, we present a novel methodology to construct and automatically validate a large-scale multimodal dataset for disaster response. Starting from the vision-only Incidents1M, we successfully recovered 100,000 images and generated high-fidelity textual descriptions using two distinct Qwen3.5 architectures: a 4B dense model and a 35B Mixture-of-Experts (MoE) model. To ensure the generated captions provide reliable semantic anchoring for DFKD, we introduce an image-blind LLM-as-a-Judge validation pipeline leveraging Qwen3.5-9B. By intentionally obscuring the original image from the judge, this evaluator accurately simulates the modality gap of the student model during data-free distillation. Our evaluation across 173,179 label pairs demonstrates a high semantic agreement (78.65/100) between the two architectures. Furthermore, the automated evaluation reveals a conservative captioning behaviour, characterized by a high Precision (77.6%) and low Recall (46.0%). This minimizes the false positive noise, while simultaneously exposing underlying human annotation inconsistencies in the original ground truth. This work provides a scalable, LLM-validated multimodal dataset and a reproducible framework to advance cross-modal knowledge distillation.

</details>


### [76] [MSCM-net: A hyperspectral image classiffcation method based on multi-scale convolution and Mamba](https://arxiv.org/abs/2607.28277)
*Jianjun Chen,Linlin Wang,Lifang Chang,Limin Huo,Shujiang Song,Yanjia Zhao,Mingwei Shao*

Main category: cs.CV

TL;DR: 提出MSCM-net，一种结合多尺度CNN与Mamba的高光谱图像分类模型，通过MCSE模块和双分支特征聚合模块在降低计算复杂度的同时提升分类性能。

Research relevance: 弱相关（2-3分）。论文涉及遥感（hyperspectral imaging）和降低计算复杂度，但核心任务是高光谱图像分类（classification），而非目标检测（object detection），也未涉及轻量化部署（pruning/quantization/distillation等）。与"在轨遥感目标检测模型轻量化"方向相关性有限。

Reading priority: 2分。论文与遥感相关且涉及计算复杂度优化，但核心是高光谱分类而非目标检测，且缺乏轻量化部署的具体指标。可作为Mamba架构在遥感任务中应用的参考阅读，但对"在轨目标检测轻量化"课题的直接借鉴价值有限。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：高光谱图像分类（hyperspectral image classification）。应用场景：遥感与工程应用。传感器类型：高光谱成像（hyperspectral imaging）。目标类别：摘要未说明具体类别，属于像素级分类任务而非目标检测。

Model architecture: MSCM-net由三部分组成：(1) 多尺度特征提取模块MCSE，包含多尺度卷积（不同卷积核提取不同感受野的局部信息）和SENet（自动学习多尺度特征中各通道的重要性）；(2) 多个堆叠的Mamba blocks，利用Mamba的长序列建模能力；(3) 双分支特征聚合模块，分别提取中心像素的光谱信息和周围像素的空间信息并进行融合。

Lightweight method: 摘要未明确采用剪枝、量化、知识蒸馏、NAS等标准轻量化方法。论文声称"降低计算复杂度"（reducing computational complexity），主要通过Mamba架构替代Transformer来降低计算开销，但这属于架构层面的效率优化，而非专门的模型压缩/加速技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署相关信息。仅提到"降低计算复杂度"，但未给出具体量化指标，无法判断其星上/边缘部署可行性。

Datasets and metrics: 在三个广泛使用的基准数据集上进行了大量实验（摘要未说明具体数据集名称）。评价指标：摘要未明确说明，通常高光谱分类使用Overall Accuracy (OA)、Average Accuracy (AA)、Kappa系数等，但摘要未给出。

Experiments: 在三个基准数据集上验证了MSCM-net，实验结果表明其能在降低计算复杂度的同时达到先进的分类性能（advanced classification performance）。摘要未提供具体对比方法、消融实验细节和数值结果。

Limitations: (1) 核心任务是高光谱图像分类而非目标检测，与在轨目标检测方向不直接相关；(2) 摘要未提供任何轻量化量化指标（参数量、FLOPs、推理延迟等）；(3) 未说明是否针对边缘/星上部署场景设计；(4) 未提供具体数据集名称和数值结果，难以评估方法实际效果；(5) Mamba在遥感高光谱分类中的应用尚属较新方向，其实际部署可行性有待验证。

Ideas for my research: (1) Mamba（状态空间模型）作为Transformer的轻量替代，其长序列建模能力可借鉴用于遥感目标检测中的特征提取，特别是处理高分辨率遥感图像时降低计算复杂度；(2) 多尺度卷积+SENet的MCSE模块设计思路可迁移到遥感小目标检测中，增强多尺度特征融合能力；(3) 双分支特征聚合（中心像素光谱+周围像素空间）的思想可类比于遥感检测中的局部-全局特征融合策略；(4) 若后续论文补充FLOPs和推理速度数据，可评估其作为轻量骨干网络用于在轨检测的潜力。

Abstract: Hyperspectral imaging is widely used in remote sensing and engineering. Therefore, research on its classification methods is crucial. While CNN and Transformer-based methods have advanced, they still face locality constraints and high computational complexity. To address these issues, we propose an innovative hyperspectral image classification model, MSCM-net. Specifically, first of all, a model architecture combining multi-scale CNN and Mamba is proposed. It consists of a multi-scale feature extraction module (MCSE) and multiple stacked Mamba blocks, which integrates the local feature extraction capability of multi-scale CNN and the long sequence modeling advantage of Mamba. Secondly, the proposed MCSE module consists of multi-scale convolution and SENet. Convolution kernels of different scales extract local information with different receptive fields, enhancing the fusion of spatial and spectral information. Meanwhile, the SENet enables the model to automatically learn the importance of each channel in the multi-scale features. Furthermore, we also propose a dual-branch feature aggregation module, which further effectively extracts and integrates the spectral information contained in the central pixel and the spatial information in the surrounding pixels. Our model has undergone numerous experiments on three widely used benchmark datasets. The experimental results show that MSCM-net can achieve advanced classification performance while reducing computational complexity.

</details>


### [77] [Beyond Visual Ambiguity: Guiding Robust Monocular Depth Estimation in Challenging Scenarios via Detailed Long Captions](https://arxiv.org/abs/2607.28285)
*Junrui Zhang,Jiaqi Li,Yiran Wang,Liao Shen,Zhiguo Cao*

Main category: cs.CV

TL;DR: 提出CapDepth框架，利用详细长文本描述作为辅助模态，通过动态文本编码器和文本自适应解码器增强单目深度估计在非朗伯表面和恶劣天气下的鲁棒性。

Research relevance: 与本研究方向弱相关（2分）。论文聚焦于单目深度估计（Monocular Depth Estimation, MDE），属于泛视觉/三维感知领域，不涉及遥感、卫星、在轨部署或目标检测。虽然深度估计与遥感三维重建有一定关联，但论文既未涉及遥感场景，也未涉及目标检测或任何轻量化/部署方法，属于泛机器学习方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于单目深度估计+多模态（文本-视觉）领域。虽然多模态特征融合和自适应归一化有一定方法论参考价值，但缺乏遥感场景、目标检测和轻量化部署的任何关联，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：单目深度估计（Monocular Depth Estimation, MDE）。应用场景：非朗伯表面（non-Lambertian surfaces）和恶劣天气（adverse weather）下的深度感知。传感器类型：单目RGB相机。目标类别：不涉及目标检测，主要面向场景深度预测。

Model architecture: CapDepth框架包含三个核心组件：(1) 详细长文本输入模板（detailed long caption input template），通过多个原子句显式传达丰富的空间关系；(2) 动态文本编码器（dynamic caption encoder），通过渐进式掩码注意力（progressive masked attention）提取细粒度的深度相关文本特征；(3) 文本自适应解码器（text-adaptive decoder），通过稳定的自适应层归一化（adaptive layer normalization）用文本特征引导深度解码。整体属于vision-language model (VLM) 架构。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等均未提及。

Onboard deployability: 摘要未说明。论文未报告参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）等部署相关信息，也未讨论实时推理或边缘/星上部署可行性。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为深度估计误差（depth error），报告了非朗伯表面深度误差降低25.0%、恶劣天气条件下深度误差降低22.0%的结果。

Experiments: 摘要提到"Extensive experiments validate the efficacy of CapDepth"，并声称在非朗伯表面和恶劣天气条件下优于state-of-the-art方法，分别实现25.0%和22.0%的深度误差降低。但摘要未给出具体对比方法名称、消融实验细节或更多定量结果。

Limitations: (1) 论文完全聚焦于单目深度估计，与遥感目标检测无直接关联；(2) 未涉及任何轻量化或部署优化方法；(3) 依赖详细长文本描述作为输入，这在实际遥感在轨场景中难以获取（卫星图像通常没有配套的详细文本标注）；(4) 摘要未说明推理速度、模型规模等部署关键指标；(5) 未提及在遥感影像（如SAR、多光谱、高光谱）上的适用性。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴思路：(1) 文本-视觉多模态融合中的"渐进式掩码注意力"（progressive masked attention）思想，可类比用于遥感多光谱/高光谱波段间的特征交互；(2) "自适应层归一化"（adaptive layer normalization）作为特征调制手段，可借鉴到轻量检测头设计中以提升小目标特征表达；(3) 长文本模板中"原子句表达空间关系"的结构化思想，可类比用于遥感场景中目标间空间关系的显式建模。但整体借鉴价值有限，因为该工作依赖文本模态，而遥感在轨场景通常缺乏文本信息。

Abstract: Monocular depth estimation (MDE) faces challenges with non-Lambertian surfaces and adverse weather conditions due to the visual ambiguities inherent in single-image limited information. Existing works address them in isolation via image inpainting or augmentation, yielding limited robustness gains. Language, as a powerful complementary modality to vision, is demonstrated to enhance the visual perception capabilities of vision-language models (VLMs) via detailed long captions. However, prior language-integrated MDE methods fail to fully harness this potential due to short text input with limited information, coarse global text feature learning, and limited language guidance during depth decoding. To address these limitations, we propose CapDepth, a novel framework for robust MDE that leverages guidance from detailed long captions to alleviate visual ambiguities in both challenging scenarios. First, we design a detailed long caption input template that explicitly conveys rich spatial relationships among multiple atom sentences. Second, a dynamic caption encoder is introduced to extract fine-grained depth-relevant text features via progressive masked attention. Finally, we propose a text-adaptive decoder that guides enhanced depth decoding with text features via stable adaptive layer normalization. Extensive experiments validate the efficacy of CapDepth, which outperforms state-of-the-art methods, achieving depth error reductions of 25.0% on non-Lambertian surfaces and 22.0% under adverse weather conditions.

</details>


### [78] [MonoVoc: Decoupling Geometry and Semantics for Lightweight Monocular Open-Vocabulary 3D Gaussians](https://arxiv.org/abs/2607.28300)
*Pouya Ardekhani,Zahra Dehghanian,Morteza Abolghasemi,Hamid R. Rabiee*

Main category: cs.CV

TL;DR: 提出一种无需训练的开放词汇3D场景理解管线，将3D几何重建与语义集成解耦，用模块化对象级语义嵌入替代稠密逐高斯存储，在Replica数据集上实现数量级的内存压缩。

Research relevance: 弱相关（2分）。该论文属于3D场景理解/3D Gaussian Splatting领域，面向的是室内场景的开放词汇语义检索与问答，既非遥感/卫星场景，也非目标检测任务，更不涉及模型轻量化/在轨部署。虽然其"解耦几何重建与语义集成"和"模块化对象级语义嵌入替代稠密特征存储"在内存压缩思路上有一定借鉴意义，但整体与遥感卫星在轨目标检测及轻量化方向关联度很低。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于3D场景理解/3D Gaussian Splatting领域，仅在"解耦+模块化存储压缩"的思路上有极有限的间接启发，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务：开放词汇3D场景理解（open-vocabulary 3D scene understanding），包括3D检索和问答。场景：室内重建环境（Replica数据集），输入为单目视频序列。传感器：单目RGB相机。目标类别：开放词汇语义对象（非特定遥感目标）。

Model architecture: 基于3D Gaussian Splatting的框架，核心创新是显式解耦3D几何重建与语义集成：几何独立提取，语义通过轻量级模块化后处理框架（post-processing framework）接地（grounding），输出紧凑、可解释、可搜索的对象级语义Gaussian map。

Lightweight method: 摘要未明确提及剪枝/量化/蒸馏/NAS等经典轻量化方法。其轻量化体现在：用模块化对象级语义嵌入（modular, object-level semantic embeddings）替代稠密逐Gaussian语言特征存储（dense per-Gaussian storage），实现数量级内存缩减。属于存储/内存层面的压缩策略，而非计算/模型层面的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及相比SOTA基线实现order-of-magnitude（数量级）的内存使用降低，但未给出具体数值。该工作面向交互式室内系统，非星上/边缘部署场景。

Datasets and metrics: 数据集：Replica dataset。评价指标：渲染保真度（rendering fidelity）、分割精度（segmentation accuracy）、内存使用量（memory usage）。摘要未给出具体数值。

Experiments: 在Replica数据集上验证了解耦架构保持较强的渲染保真度和有竞争力的分割精度，并在内存使用上相比SOTA基线实现数量级缩减。摘要未提供详细对比方法和消融实验细节。

Limitations: 1. 面向室内单目视频场景，与遥感卫星成像（俯视、多光谱/SAR、大场景）差异巨大；2. 未涉及目标检测任务，而是3D语义检索与问答；3. 未提供计算量、延迟、参数量等部署关键指标；4. 未涉及任何遥感数据或卫星在轨约束（功耗、算力、带宽）；5. 摘要未说明方法在复杂开放场景下的泛化能力。

Ideas for my research: 1. "解耦几何/特征提取与语义集成"的思想可借鉴：在遥感在轨场景中，可将目标检测的特征提取与后处理语义标注解耦，降低星上实时计算负担；2. "模块化对象级嵌入替代稠密特征存储"的思路可迁移到遥感目标检测中，用紧凑的对象级表示替代逐像素/逐anchor的稠密特征，减少星上存储与下传带宽；3. 其"training-free"后处理框架提示可考虑在轨部署时采用轻量后处理而非重训练模型，降低在轨更新成本。但整体借鉴价值有限，需结合遥感场景重新设计。

Abstract: Open vocabulary 3D scene understanding is essential for next-generation interactive systems, empowering users to intuitively query and navigate reconstructed environments using natural language. However, current 3D Gaussian frameworks are often bottlenecked by restrictive multiview capture requirements, costly scene-specific optimization, and the massive memory overhead of storing dense language features. We present a novel, training-free pipeline that fundamentally reimagines this paradigm by explicitly decoupling 3D geometric reconstruction from semantic integration. Given a standard monocular video sequence as input, our method efficiently outputs a compact, highly interpretable, and fully searchable object-level semantic Gaussian map. Rather than entangling heavy language embeddings within the mapping loop, we extract geometry independently and ground semantics through a lightweight, modular post-processing framework. Extensive evaluations on the Replica dataset demonstrate that this decoupled architecture preserves strong rendering fidelity and competitive segmentation accuracy. Crucially, by replacing dense per-Gaussian storage with modular, object-level semantic embeddings, our approach delivers an order-of-magnitude reduction in memory usage compared to SOTA baselines. This provides a highly efficient, scalable, and practical solution for open-vocabulary 3D retrieval and question answering directly from everyday monocular video.

</details>


### [79] [ObjectStream: Latent Objects as Memory Anchors for Streaming Video Understanding](https://arxiv.org/abs/2607.28312)
*Mingkang Dong,Muxin Pu,Jie Li,Bohan Guo,Songruo Chen,Bin Ren,Xu Zheng,Chen Zhao,Tianwen Qian,Mohamed Elhoseiny,Yuqian Fu*

Main category: cs.CV

TL;DR: 提出ObjectStream，一种无需训练的框架，将潜在对象作为流式视频理解的记忆锚点，通过从冻结的Video-LLM表示中诱导空间连贯的潜在对象并跨帧链接为持久锚点，在有限记忆预算下保留对象历史、瞬态变化和近期视觉上下文，显著提升视频理解效率。

Research relevance: 弱相关（2分）。该论文属于流式视频理解（streaming video understanding）领域，面向的是通用视频大语言模型（Video-LLM），并非遥感/卫星场景。虽然其核心思想——以对象为锚点进行记忆压缩、token丢弃（discarding 82.5% visual tokens）、降低GPU显存和TTFT——在"轻量化/高效推理"维度上对遥感在轨目标检测的模型压缩与高效部署有一定方法论借鉴意义，但论文不涉及remote sensing、satellite、aerial image、SAR、高光谱等遥感内容，也不涉及目标检测（object detection）任务本身，更不涉及星上部署。因此属于"泛视觉/泛机器学习方法，弱相关"。

Reading priority: 2分。该论文与遥感/卫星/在轨目标检测方向弱相关，但其"以对象为锚点的记忆压缩"和"training-free token丢弃降低显存与延迟"思想对遥感视频流实时检测的轻量化有一定启发。若课题涉及遥感视频/时序目标检测的高效推理，可略读借鉴其记忆组织策略；若课题聚焦静态遥感图像检测或传统模型压缩（剪枝/量化/蒸馏），则参考价值有限。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：流式视频理解（streaming video understanding）与长视频理解（long-video understanding），属于视频大语言模型（Video-LLM）的推理与记忆管理任务。应用场景：在线流式视频（online streaming）与离线长视频（offline long-video）基准测试。传感器类型：普通RGB视频。目标类别：视频中的潜在对象（latent objects），非遥感目标。摘要未说明具体目标类别。

Model architecture: ObjectStream框架构建在冻结的Video-LLM（如Qwen2.5-VL-7B）之上，无需训练（training-free）。核心结构包括：(1) 从Video-LLM表示中诱导空间连贯的潜在对象（spatially coherent latent objects）；(2) 跨帧链接潜在对象为持久锚点（persistent anchors）；(3) 在有限记忆预算（bounded memory budget）下维护对象历史。保留三种互补证据：持久对象历史（persistent object histories）、瞬态对象变化（transient object changes）、近期视觉上下文（recent visual context）。无需外部object detector或segmentation model。

Lightweight method: 该论文的"轻量化"主要体现在记忆/上下文压缩而非模型压缩：通过以潜在对象为锚点组织视觉记忆，在有限记忆预算下丢弃大量视觉token（discarding 82.5% of visual tokens），从而降低峰值GPU显存和TTFT（time-to-first-token）约50%。这是一种token级上下文压缩/记忆管理策略，而非剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS等传统模型压缩方法。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明星上/边缘部署相关内容。论文报告了在线流式评估中峰值GPU显存和TTFT降低约50%，以及离线长视频基准中丢弃82.5%视觉token仍超越full-token基线，体现了计算与显存效率优势。但未给出具体参数量、FLOPs、延迟、能耗等部署指标，也未涉及FPGA/NPU/TensorRT等边缘硬件适配。

Datasets and metrics: 在线流式基准：OVO-Bench Real-Time Visual Perception（Qwen2.5-VL-7B上提升10.0 points）。离线长视频基准：摘要未说明具体数据集名称。评价指标：在线流式评估中的感知准确率（points）、峰值GPU显存（peak GPU memory）、TTFT（time-to-first-token）；离线长视频基准中的准确率及token丢弃率（82.5%）。

Experiments: 主要实验：(1) 在线流式评估中，ObjectStream使Qwen2.5-VL-7B在OVO-Bench Real-Time Visual Perception上提升10.0 points，同时峰值GPU显存和TTFT降低约50%；(2) 离线长视频基准上，超越full-token基线同时丢弃82.5%视觉token。摘要未说明与具体对比方法的详细消融实验。

Limitations: 局限性：(1) 面向通用视频理解，非遥感场景，未验证在遥感影像/卫星视频上的适用性；(2) 依赖Video-LLM的表示质量，潜在对象诱导的鲁棒性在遥感小目标、密集目标场景下存疑；(3) 摘要未说明记忆预算的具体设置、对象链接的失败模式、以及长时漂移问题；(4) 未报告模型压缩（参数量/FLOPs）层面的指标，仅涉及token级记忆压缩；(5) 未涉及星上硬件部署。

Ideas for my research: (1) 借鉴"以对象为锚点组织记忆"的思想：在遥感在轨目标检测中，可将检测到的目标（舰船/飞机/车辆）作为记忆锚点，跨帧维护目标历史，用于时序目标跟踪与状态变化推理，同时压缩背景token以降低计算开销。(2) token丢弃/记忆压缩策略可迁移到遥感视频流（如卫星视频、航空视频）的实时检测中，在有限星上算力下通过选择性保留目标相关特征来降低显存与延迟。(3) "training-free"思路值得借鉴——在已有检测模型上通过推理时的上下文组织实现效率提升，避免星上重训练成本。(4) 其"三种互补证据"（持久历史+瞬态变化+近期上下文）的设计可用于遥感时序目标检测中的多尺度时间上下文建模。但需注意：遥感小目标密集、背景复杂，潜在对象诱导方法需针对遥感特性重新设计。

Abstract: Streaming video understanding requires models to continuously retain useful visual evidence before future questions are known. Existing approaches primarily manage the growing visual context according to token importance, temporal redundancy, or segment-level relevance, but rarely organize evidence around objects that persist and evolve over time. Thus, in this paper, we introduce ObjectStream, a training-free framework that treats latent objects as memory anchors for streaming video understanding. ObjectStream induces spatially coherent latent objects directly from frozen Video-LLM representations, links them across frames into persistent anchors, and maintains their histories under a bounded memory budget, without requiring external object detectors or segmentation models. Built on these anchors, ObjectStream preserves three complementary forms of evidence: persistent object histories, transient object changes, and recent visual context. This design enables existing Video Large Language Models (Video-LLMs) to reason over object identities, interactions, and state changes while leaving the underlying model unchanged. Extensive experiments on online streaming and offline long-video benchmarks demonstrate both effectiveness and efficiency. In online streaming evaluation, ObjectStream improves Qwen2.5-VL-7B by 10.0 points on OVO-Bench Real-Time Visual Perception, while reducing peak GPU mem-ory and TTFT by approximately 50%. On offline long-video benchmarks, it surpasses the full-token baseline while discarding 82.5% of visual tokens. These results highlight latent objects as a practical and effective organizing principle for compact streaming video memory.

</details>


### [80] [AdaAnchor4D: Anchor-Conditioned Spatiotemporal Feature Aggregation for Monocular UAV 4D Reconstruction](https://arxiv.org/abs/2607.28320)
*Peiyi Xu,Junpeng Zhang,Guanbin Li,Ronghua Shang,Mingtao Feng,Le Dong,Weisheng Dong,Guangming Shi,Jie Feng*

Main category: cs.CV

TL;DR: 提出AdaAnchor4D，一种用于单目UAV视频动态城市场景重建的自适应锚点变形框架，通过Anchor-Conditioned Feature Aggregation、解耦局部几何变形和密度自适应坐标扭曲，提升动态高斯重建质量并保持实时渲染。

Research relevance: 弱相关（2分）。论文使用UAV视频和VisDrone、UAVDT等遥感/无人机数据集，但核心任务是动态城市场景的3D重建与渲染（novel view synthesis / dynamic scene reconstruction），并非目标检测，也不涉及模型轻量化或星上部署。属于泛视觉/三维重建方向，与遥感目标检测及轻量化课题关联度低。

Reading priority: 2分。该论文属于动态场景3D重建方向，与遥感在轨目标检测及模型轻量化课题关联度低。虽然使用了UAV/遥感相关数据集，但任务本质不同，且不含轻量化技术。仅在需要了解UAV数据集特性或借鉴自适应特征聚合思想时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：单目UAV视频动态城市场景的3D重建与实时渲染（dynamic scene reconstruction / novel view synthesis）。应用场景：无人机航拍城市动态场景。传感器类型：单目RGB相机（UAV视频）。目标类别：非目标检测任务，涉及动态区域（车辆、行人等运动物体）的时空建模。数据集：UAV-Arc4D、VisDrone、UAVDT。

Model architecture: 基于动态Gaussian Splatting（3D Gaussian Splatting）框架。核心模块包括：(1) Anchor-Conditioned Feature Aggregation (ACFA)：用anchor-specific聚合嵌入和时间信息自适应聚合共享时空特征；(2) Decoupled Local Geometry Deformation (DLGD)：将anchor状态变形与局部Gaussian几何变形解耦；(3) Density-Adaptive Coordinate Warping (DACW)：根据轴向上的anchor分布对特征查询坐标进行重参数化，缓解非均匀几何采样与均匀网格参数化之间的不匹配。

Lightweight method: 摘要未说明明确的模型压缩/轻量化方法。论文强调"real-time rendering performance"（实时渲染），但这是通过Gaussian Splatting的高效渲染管线实现的，并非针对目标检测模型的剪枝、量化、蒸馏或NAS等轻量化手段。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。仅提及"maintaining real-time rendering performance"（保持实时渲染性能），但未给出具体帧率、延迟或硬件平台。该工作面向的是3D重建渲染而非在轨目标检测，与星上部署（FPGA/NPU/TensorRT）无直接关联。

Datasets and metrics: 数据集：UAV-Arc4D、VisDrone、UAVDT。评价指标：摘要未明确列出具体指标名称，但提到"higher rendering quality"（更高的渲染质量），通常此类工作使用PSNR、SSIM、LPIPS等图像质量指标。摘要未说明具体数值。

Experiments: 在UAV-Arc4D、VisDrone、UAVDT三个数据集上与代表性动态Gaussian方法对比，AdaAnchor4D取得更高的渲染质量，同时保持实时渲染性能。摘要未提供具体对比数值和消融实验细节。

Limitations: (1) 该工作面向3D重建/渲染，与目标检测任务无关；(2) 摘要未提供参数量、FLOPs、延迟等部署关键指标；(3) 未涉及任何模型压缩、剪枝、量化、蒸馏等轻量化技术；(4) 未讨论星上/边缘部署可行性；(5) 动态场景重建的实时渲染与在轨目标检测的实时推理是不同技术栈。

Ideas for my research: (1) 该工作使用VisDrone和UAVDT数据集，这些数据集本身包含遥感/无人机目标检测标注，可作为本课题目标检测实验的补充数据来源；(2) ACFA中"anchor-specific自适应聚合"的思想可类比借鉴到轻量化检测模型的特征融合模块设计中，用自适应权重替代固定融合策略以提升小目标检测精度；(3) DACW中处理非均匀采样与均匀网格不匹配的思路，对遥感图像中目标尺度分布极不均匀的小目标检测有一定启发；(4) 但整体而言，该论文对"在轨目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Monocular UAV videos provide valuable observations for dynamic reconstruction of complex urban scenes. However, such scenes exhibit pronounced spatiotemporal heterogeneity: different regions follow distinct temporal activity patterns, while the motion states of some dynamic regions may further evolve over time. Although dynamic Gaussian methods based on decomposed shared spatiotemporal feature fields have achieved efficient and accurate reconstruction in object-centric or relatively compact scenes, their commonly adopted fixed plane-wise feature combination mechanisms are less suited to the heterogeneous local dynamics of UAV scenes, often leading to ghosting artifacts and blurred dynamic details. To address this challenge, we propose AdaAnchor4D, an adaptive anchor deformation framework for monocular UAV dynamic scene reconstruction. At its core, Anchor-Conditioned Feature Aggregation (ACFA) adaptively aggregates shared spatiotemporal features using anchor-specific aggregation embeddings and temporal information, allowing different local units to obtain dynamic representations tailored to their local and temporal states. Decoupled Local Geometry Deformation (DLGD) separates anchor-state deformation from local Gaussian geometry deformation, while Density-Adaptive Coordinate Warping (DACW) reparameterizes feature-query coordinates according to the axis-wise anchor distributions, alleviating the mismatch between non-uniform geometric sampling and uniform grid parameterization. Experiments on UAV-Arc4D, VisDrone, and UAVDT show that AdaAnchor4D achieves higher rendering quality than representative dynamic Gaussian methods while maintaining real-time rendering performance. The code will be made publicly available.

</details>


### [81] [Same Branches, Different Trees: A Bifurcation Connectedness Metric for Coronary Artery Segmentation and FFR-CT Decision Agreement](https://arxiv.org/abs/2607.28327)
*Maame Owusu-Ansah,Kelvin Lee,Dr Vinod Venugopal,Muhammad Moazzam Jawaid,Wenting Duan,James Brown*

Main category: cs.CV

TL;DR: 本文研究CT血管造影（FFR-CT）中血管分割的连通性评估指标BCS（Bifurcation Connectedness Score）及其可微训练代理soft-BCS，用于衡量分割结果在分叉点处的连通性，弥补Dice等标准指标无法捕捉局部断裂的缺陷。

Research relevance: 与本研究方向基本无关（1分）。论文属于医学影像（CT血管造影/FFR-CT）中的血管分割质量评估，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测或模型轻量化/部署。仅有的间接关联是"分割质量评估指标"这一方法论层面，与遥感分割/检测中的评估指标设计可能有微弱借鉴，但整体弱相关。

Reading priority: 1分。论文属于医学影像分割评估领域，与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及遥感场景、目标检测或模型压缩部署，仅方法论层面有极微弱的评估指标设计借鉴，阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学图像分割质量评估（血管树分割的连通性评分）。应用场景：CT血管造影衍生的血流储备分数（FFR-CT）计算。传感器类型：CT（医学影像），非遥感传感器。目标类别：血管树、分叉点（bifurcation）。

Model architecture: 摘要未说明具体分割网络结构。核心贡献是评估指标BCS（Bifurcation Connectedness Score）及其可微训练代理soft-BCS，作用于分割结果的分叉点连通性评分，而非网络架构本身。

Lightweight method: 摘要未涉及任何轻量化方法（pruning、quantization、knowledge distillation、NAS、轻量网络等均未提及）。论文聚焦于分割质量评估指标设计，与模型压缩/加速无关。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：摘要未说明具体数据集名称。评价指标：BCS（Bifurcation Connectedness Score）、soft-BCS（可微训练代理）、clDice、Skeleton Recall、Dice；以及FFR-CT决策一致性（OR 2.16, CI [1.23, 4.18]）。

Experiments: 主要实验发现：(1) BCS能捕捉标准指标（如Dice）遗漏的连通性断裂问题，对连通性破坏响应强烈，而对保持连通性的血管变窄几乎不变；(2) 更高的BCS对应FFR-CT决策在预测与真实几何之间更接近的一致性，尤其在严重病变中（OR 2.16）；(3) soft-BCS与Skeleton Recall在训练中能恢复相同的分支但构建不同的树，说明"恢复分支"与"保持连通"是可分离的性质，建议分别报告。

Limitations: 摘要未说明局限性。但可推断：(1) 该研究完全面向医学影像场景，与遥感/卫星目标检测无直接关联；(2) 未涉及任何轻量化或部署问题；(3) BCS/soft-BCS是针对血管分叉结构设计的，迁移到遥感目标检测（如旋转框、小目标）需要重新设计；(4) 摘要未提供网络结构、训练细节、计算开销等关键信息。

Ideas for my research: 尽管弱相关，仍有两点方法论层面的借鉴：(1) "标准指标（如Dice/mAP）可能遗漏关键结构性质"这一思想可迁移到遥感目标检测——例如在遥感小目标检测中，mAP可能无法反映检测框的连通性/完整性（如舰船目标被分割成多段），可考虑设计类似"结构感知"的评估指标；(2) "可微训练代理（soft-BCS）"的思路提示：若遥感检测需要优化某种难以直接微分的结构指标（如旋转框IoU、目标连通性），可设计其可微代理作为训练损失。但整体借鉴价值有限，不建议作为主要参考。

Abstract: Fractional flow reserve derived from CT angiography (FFR-CT) simulates flow through a patient-specific vessel model, so its accuracy depends on the connectedness of the segmented tree, not only on volumetric overlap: a segmentation can reach high Dice yet sever a bifurcation, dropping the downstream subtree and reversing the treatment decision. Topology-aware losses such as clDice and Skeleton Recall act on the global centreline and can miss localised breaks. We study the Bifurcation Connectedness Score (BCS), which scores connectedness at each ground-truth bifurcation, and soft-BCS, its differentiable training surrogate. BCS captures a property of segmentation quality the standard metrics miss: it responds strongly to breaks in connectedness while staying largely unchanged under connectedness-preserving narrowing. Higher BCS accompanies closer agreement between the FFR-CT decisions a solver makes on predicted versus ground-truth geometry, most clearly in severe disease (OR 2.16, CI [1.23, 4.18]). Both decisions come from the same solver, so this reflects geometric, not clinical, fidelity. In training, soft-BCS and Skeleton Recall recover the same branches but build different trees. Recovering branches and keeping them connected are separable properties, so we recommend reporting a measure of each.

</details>


### [82] [Capturing Token Tendencies for Training-Free Token Pruning in Multimodal Large Language Models](https://arxiv.org/abs/2607.28341)
*Jie Ma,Zhike Qiu,Jie Gao,Jiayi Ji,Qian Chen,Xiaoshuai Sun,Rongrong Ji*

Main category: cs.CV

TL;DR: 提出Trend-aware Pruning框架，将MLLM中的视觉token剪枝从静态局部快照决策提升为时间轨迹建模问题，通过捕捉注意力流动量并动态恢复"晚开花"token，实现高效且可逆的多模态推理。

Research relevance: 弱相关（2分）。论文聚焦Multimodal Large Language Models (MLLMs)中的visual token pruning，属于泛视觉/泛机器学习方法。虽然"pruning"与轻量化方向相关，但对象是视觉token而非模型参数/通道，且场景是通用多模态任务而非遥感/卫星目标检测。与遥感卫星在轨目标检测无直接关联，但token剪枝的"动态可逆"思想对轻量化设计有一定借鉴意义。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关。虽然token剪枝的"动态可逆"思想有一定借鉴价值，但场景（MLLM）、剪枝对象（视觉token）、评估指标均与本研究方向差异较大。可作为轻量化方法设计的灵感参考，但非核心必读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Multimodal Large Language Models (MLLMs)中的视觉token剪枝（visual token pruning）。应用场景：通用多模态推理任务（multimodal tasks）。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（非特定目标检测任务）。

Model architecture: 基于MLLM的分层Transformer架构。核心创新是Trend-aware Pruning框架，将token重要性评估从静态快照（static, instantaneous heuristics）提升为时间轨迹建模（temporal trajectory modeling），通过捕捉attention flow的动量（momentum）实现动态纠偏机制（dynamic rectification mechanism），选择性重新激活"late-blooming" tokens（初始被低估但语义重要性上升的token）。

Lightweight method: 采用token-level pruning（视觉token剪枝），属于推理加速/计算压缩方法。与传统的参数剪枝（weight pruning）、通道剪枝（channel pruning）、量化（quantization）、知识蒸馏（knowledge distillation）不同，本文剪枝对象是MLLM中的视觉token数量。关键改进是：从不可逆（irreversible）的静态剪枝变为可逆（reversible）的动态剪枝，通过轨迹建模避免浅层误剪深层关键token。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及可减少77.8%以上的视觉token，最终层仅保留约23个token，同时保持competitive performance。由于是MLLM场景，模型规模通常较大，与星上部署的轻量化需求差距较大。摘要未说明是否适用于边缘/星上部署。

Datasets and metrics: 摘要未说明具体数据集名称。提及"diverse multimodal tasks"（多样多模态任务），但未列出具体benchmark。评价指标方面仅提及efficiency-performance trade-off，未给出具体指标名称（如accuracy、FLOPs等）。

Experiments: 摘要未给出详细实验对比。核心结果：视觉token减少超过77.8%，最终层仅保留约23个token，同时保持competitive performance。未给出与具体baseline方法的定量对比数据。

Limitations: 1. 摘要未说明具体数据集、指标和baseline对比，实验细节缺失。2. 场景为MLLM通用多模态任务，与遥感/卫星目标检测无直接关联。3. 未提供FLOPs、延迟、参数量等部署关键指标。4. token剪枝针对的是MLLM的视觉编码器输出，与遥感目标检测的backbone/neck/head结构差异较大。5. "可逆剪枝"的额外计算开销（轨迹建模、动态恢复机制）未在摘要中量化。

Ideas for my research: 1. 借鉴"动态可逆剪枝"思想：在遥感目标检测中，可探索对浅层特征图/候选框进行动态剪枝，并在深层根据注意力或梯度信息恢复关键区域，避免小目标（如舰船、飞机）在浅层被误删。2. "注意力流动量"（momentum of attention flow）概念可迁移到遥感检测中，用于判断哪些特征通道/空间位置在深层推理中会变得重要，辅助通道剪枝或token选择。3. 对遥感大场景图像，可借鉴token-level pruning思路对高分辨率输入进行动态区域选择，减少计算量同时保留小目标关键区域。4. 该方法的"轨迹建模"思想可用于设计更智能的剪枝调度策略，在训练后剪枝（post-training pruning）中考虑层间重要性演化。

Abstract: While visual token pruning is essential for efficient Multimodal Large Language Models (MLLMs), existing training-free methods suffer from a critical limitation: they rely on static, instantaneous heuristics to perform irreversible filtering. This approach ignores the hierarchical nature of MLLMs, where token importance often evolves dynamically rather than remaining fixed across layers. Consequently, tokens essential for deep-layer reasoning are often prematurely discarded by shallow-layer estimates. To address this, we propose Trend-aware Pruning, a novel framework that elevates pruning from a local snapshot decision to a temporal trajectory modeling problem. Instead of relying on isolated scores, our method captures the momentum of attention flow. This enables a dynamic rectification mechanism that selectively reactivates "late-blooming" tokens, those initially undervalued but exhibiting rising semantic importance, thereby preventing the loss of critical visual cues. Extensive experiments demonstrate that our approach achieves a superior efficiency-performance trade-off across diverse multimodal tasks. Notably, it reduces visual tokens by over 77.8%, retaining only approximately 23 tokens in the final layer while maintaining competitive performance, offering a robust and reversible solution for high-efficiency multimodal inference.

</details>


### [83] [ShadowDancer: Teaching Video World Models Any Action by Learning Unified Dynamics Representations from a Video and Its Shadow](https://arxiv.org/abs/2607.28362)
*Jin Cao,Zian Meng,Kaipeng Zhang*

Main category: cs.CV

TL;DR: ShadowDancer 提出一种基于"shadow pairs"和"cross-shadow prediction"的交互式视频世界模型动作控制方法，通过演示视频实现任意动作的帧级精确控制，无需动作标签或微调即可在新场景中复用动作。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，聚焦于交互式视频世界模型（interactive video world models）中的动作控制（action control）问题，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。它既不是遥感场景，也不涉及目标检测，更不涉及模型压缩或边缘部署。仅作为泛机器学习方法，对动作表征学习有一定参考价值，但与本研究方向基本无关。

Reading priority: 1分。该论文属于视频世界模型/动作控制方向，与遥感卫星在轨目标检测及模型轻量化基本无关，不涉及遥感场景、目标检测或任何模型压缩/部署技术，阅读优先级极低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：交互式视频世界模型中的任意动作（any-action）帧级控制。应用场景：视频生成/世界模型（world model）中的动作控制与动作迁移。传感器类型：视频（RGB）。目标类别：不涉及具体目标检测类别。摘要未说明具体数据集。

Model architecture: 核心架构包括：(1) Shadow Library——大规模构建"shadow pairs"（在同一动力学下、独立重采样外观的视频对）；(2) cross-shadow prediction——通过从一个shadow预测另一个shadow来学习动作表征，丢弃被重采样的外观信息、保留被保留的动力学信息作为动作；(3) block-causal world model——由统一动力学表征驱动的块因果世界模型。整体属于视频生成/世界模型框架，非检测网络。

Lightweight method: 摘要未涉及任何轻量化方法。不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理或边缘部署。该论文关注的是动作表征学习与迁移，而非模型压缩或加速。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。该工作面向视频世界模型，计算开销通常较大，与星上/边缘部署场景不匹配。

Datasets and metrics: 摘要未明确说明具体数据集名称。评价指标包括：动作迁移效果（action transfer）、长动作展开（long action rollout）、以及平均盲测胜率（average blinded win rate，86%）。对比基线为强潜在动作（latent-action）和交互式世界模型基线。

Experiments: 实验表明ShadowDancer在多个动力学族（diverse dynamics families）上相比强潜在动作基线和交互式世界模型基线，在动作迁移和长动作展开上均有改进，rollout对比中平均盲测胜率为86%。摘要未提供消融实验细节。

Limitations: (1) 与遥感目标检测和轻量化方向完全无关，属于视频生成/世界模型领域；(2) 摘要未说明计算开销、推理速度等部署指标；(3) 摘要未说明在真实遥感/卫星数据上的适用性；(4) 依赖大规模shadow pairs的构建（Shadow Library），数据构建成本可能较高；(5) 摘要未说明对目标检测任务的适用性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但可借鉴的间接思路包括：(1) "cross-shadow prediction"中"丢弃被重采样信息、保留本质信息"的表征解耦思想，可类比于遥感目标检测中解耦外观（光照、云雾、季节变化）与语义特征，提升模型对遥感影像域差异的鲁棒性；(2) 动作/动力学表征的迁移思想可类比于遥感检测模型在不同传感器（光学/SAR）或不同分辨率间的特征迁移；(3) 但整体而言，该论文对"轻量化"课题的直接借鉴价值有限，不建议作为核心参考。

Abstract: We present ShadowDancer, a novel approach to any-action, frame-level control of interactive video world models. The obstacle is representational: existing interfaces either encode an action loosely, leaving how it unfolds for the model to improvise, or encode it exactly through structured signals that serve one family and are hard to acquire, so precise control across diverse dynamics remains impractical. Demonstration videos are the natural remedy, specifying any dynamics frame by frame; yet a video shows its dynamics only through one particular appearance, a single shadow of the underlying dynamics, so actions learned from demonstrations transfer poorly to new scenes. ShadowDancer addresses this with two key innovations: (1) shadow pairs, video pairs that replay the same dynamics under independently resampled appearance, constructed at scale by our Shadow Library, so that a dynamics family becomes controllable exactly when such pairs can be constructed for it; and (2) cross-shadow prediction, which learns actions by predicting one shadow from the other, so that whatever the pairing resamples is discarded by construction and whatever it preserves becomes the action, yielding a unified dynamics representation that drives a block-causal world model. Any demonstrated clip thus becomes a reusable action asset, replayed in new environments without action labels, motion estimators, or fine-tuning. Experiments demonstrate improved action transfer and long action rollout over strong latent-action and interactive world model baselines across diverse dynamics families, with an average blinded win rate of 86% in rollout comparisons. We show video results at https://ShadowDancer-1.github.io

</details>


### [84] [Explaining Image Similarity with Automatically Extracted Concept Activation Vectors](https://arxiv.org/abs/2607.28386)
*Isaac Roberts,Petra Bevandic,Alexander Schulz,Barbara Hammer*

Main category: cs.CV

TL;DR: 提出一种基于Sparse Autoencoders自动提取Concept Activation Vectors的模型无关可解释性框架，用于解释图像相似性评分背后的概念驱动因素。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方向，聚焦于图像相似性的可解释性（explainability），与遥感、卫星、目标检测、轻量化部署均无直接关联。既未涉及遥感场景，也未涉及目标检测或模型压缩/加速方法。

Reading priority: 1分。与"遥感卫星在轨目标检测模型轻量化"研究方向基本无关，属于可解释AI领域的方法论文，不涉及遥感、目标检测或模型压缩/部署，阅读价值很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像相似性解释（image similarity explanation），属于可解释AI（XAI）领域。应用场景为通用计算机视觉，非遥感场景。传感器类型：摘要未说明。目标类别：摘要未说明（涉及texture、shape、color等通用视觉概念）。

Model architecture: 框架由两部分组成：1) Sparse Autoencoders (SAEs) 用于自动提取Concept Activation Vectors (CAVs)；2) 一个model-agnostic的扰动-测量机制，沿发现的概念方向扰动embedding并测量相似性函数的变化。框架不依赖特定模型或特定相似性度量。

Lightweight method: 摘要未涉及任何轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、实时推理或边缘部署等模型压缩/加速技术。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该框架本身是分析工具而非部署模型，且依赖SAE训练，计算开销可能较大，与星上/边缘部署场景不匹配。

Datasets and metrics: 摘要未明确说明具体数据集。评价指标方面，使用"faithfulness"（潜在扰动对数据分布的忠实度）和"concept importances线性恢复真实相似性分数"作为评估标准。实验包含定性结果（qualitative results）验证个体和群体相似性判断的可解释性。

Experiments: 实验表明：1) 潜在空间扰动比pixel-space基线更忠实于底层数据分布；2) concept importances能够线性恢复真实相似性分数；3) 定性结果验证了方法在理解模型个体和群体相似性判断方面的有效性。摘要未提供具体数值结果或对比方法细节。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向基本无关；2) 摘要未说明计算开销和可扩展性，SAE训练本身可能计算成本较高；3) 摘要未说明该方法在特定领域（如遥感影像）的适用性；4) 摘要未提供定量对比实验的详细结果。

Ideas for my research: 借鉴意义有限，但可考虑以下间接启发：1) 若在遥感目标检测中需要解释模型决策（如舰船/飞机检测的误检原因），CAV+SAE的可解释性思路可用于分析检测模型在嵌入空间中的决策依据；2) 概念归因图（concept attribution maps）的思想可迁移到遥感小目标检测中，帮助定位模型关注的特征区域；3) 但该框架本身不提供任何轻量化手段，对在轨部署的模型压缩需求无直接帮助。

Abstract: Image similarity underlies many computer vision applications, yet it is often unclear why two images receive a high or low similarity score. Existing explainability methods often rely on gradient-based attribution maps to provide local justifications for similarity. These approaches struggle to provide global insights into what specifically drives similarity in regions of an embedding space, such as texture, shape, or color. We introduce a model- and metric-agnostic framework that explains image similarity using Concept Activation Vectors (CAVs) extracted automatically via Sparse Autoencoders (SAEs). Given a pair of images, we perturb their embeddings along discovered concept directions and measure the resulting change in a chosen similarity function, yielding concept importances. For image pairs, we provide localization with concept attribution maps. We extend this procedure to group-level settings, explaining what drives similarity across a cluster of images rather than a single pair, and further, we introduce Exemplar Retrieval, aiming to recover samples with similar reasons contributing to similarity. Our experiments show that our latent perturbations are more faithful to the underlying data distribution than pixel-space baselines, and that concept importances linearly recover the true similarity score. Qualitative results further confirm the usefulness of our methods in understanding a model's individual and group similarity judgments.

</details>


### [85] [Hand-Object Interaction in the Age of Large Foundation Models:Reconstruction, Generation, and Embodied Transfer](https://arxiv.org/abs/2607.28394)
*Weiquan Lin,Yu Deng,Shiyang Liu,Luping Xiao,Xu Tang,Junzhi Yu,Jiaolong Yang,Lei Zhang,Xingyu Chen*

Main category: cs.CV

TL;DR: 一篇关于基础模型先验知识（foundation-model priors）在手-物交互（HOI）建模中的系统性综述，提出八类子先验的分类体系并覆盖重建与生成六大任务。

Research relevance: 弱相关（2分）。该论文聚焦 hand-object interaction (HOI) 建模与 foundation model 先验知识，属于三维重建/生成与机器人学习领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。仅"foundation model"这一概念与遥感大模型有一定方法学上的间接借鉴意义，但整体与本研究方向基本无关。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于HOI三维重建/生成与机器人学习领域，无直接可借鉴的检测方法或压缩技术，仅"foundation model先验分类"思路有极弱的间接启发，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：手-物交互（HOI）重建与生成，涵盖六类HOI任务；应用场景：三维重建、内容生成、机器人学习（human-data pretraining、human-to-robot skill transfer、HOI-to-robot data generation）；传感器类型：非遥感，涉及视觉图像/视频；目标类别：手部、物体及其交互关系。摘要未说明具体传感器。

Model architecture: 摘要未说明具体模型结构。论文提出的是概念性分类体系：将 foundation-model 先验分为几何（shape retrieval、shape reconstruction、spatial reconstruction）、语义（semantic grounding、language reasoning）、视觉（visual representation、image generation、video generation）三大族共八类子先验，并分析这些先验如何被表示、注入和适配到HOI pipeline中。

Lightweight method: 不涉及。摘要未提及剪枝、量化、知识蒸馏、NAS、轻量网络、实时推理、边缘部署等任何模型压缩或加速方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该论文面向通用HOI建模与机器人学习，与星上/边缘部署无关。

Datasets and metrics: 摘要仅提及"summarize datasets and evaluation protocols"，并维护一个持续更新的 live repository 聚合新兴方法与基准，但未列出具体数据集名称和评价指标。摘要未说明。

Experiments: 作为综述论文，无实验对比或消融实验。主要贡献是系统梳理HOI文献、建立八类foundation-model子先验的分类学（taxonomy），并分析先验在HOI pipeline中的表示、注入与适配方式，以及HOI知识在机器人学习中的应用。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，无任何可迁移的检测或压缩方法；2. 作为综述，未提供具体模型实现或量化结果；3. 摘要未说明HOI任务在视觉不确定性下的具体评测基准细节；4. 摘要未说明foundation model先验在计算开销、推理效率方面的考量。

Ideas for my research: 1. 该论文的"foundation-model priors"分类学思路（几何/语义/视觉三大族）可类比借鉴到遥感大模型（如遥感基础模型）在目标检测中的先验知识组织方式，但仅具方法论启发；2. "先验知识如何被表示、注入和适配"的分析框架可迁移到遥感检测中如何利用预训练大模型知识辅助小模型训练（如知识蒸馏的变体），但需自行设计；3. 整体借鉴价值有限，不建议作为核心参考。

Abstract: Hand-object interaction (HOI) modeling remains challenging because it requires joint reasoning about hand articulation, object geometry, contact, semantics, and dynamics under severe visual uncertainty. Foundation models introduce transferable prior knowledge learned from large-scale cross-domain data, offering new ways to address these challenges beyond task-specific data and models. However, the rapidly growing literature remains fragmented, and existing studies typically describe these methods simply as ``using large models'' without systematically characterizing what knowledge is introduced, where it enters the HOI pipeline, or which HOI uncertainty it helps reduce. This survey presents the first systematic review of foundation-model priors for HOI. We organize the literature into six HOI tasks spanning reconstruction and generation. More importantly, we establish a taxonomy of eight foundation-model sub-priors grouped into geometric, semantic, and visual families. Geometric priors encompass shape retrieval, shape reconstruction, and spatial reconstruction; semantic priors include semantic grounding and language reasoning; and visual priors cover visual representation, image generation, and video generation. Based on this taxonomy, we systematically analyze how different priors are represented, injected, and adapted across HOI pipelines and tasks. Beyond how foundation models empower HOI, we further examine how HOI-derived knowledge is used in robot learning, including human-data pretraining, human-to-robot skill transfer, and HOI-to-robot data generation. Finally, we summarize datasets and evaluation protocols, and discuss limitations and future directions toward more generalizable HOI systems. To support long-term progress, we curate a live repository that continuously aggregates emerging methods and benchmarks.

</details>


### [86] [Large scale cross-regional remote sensing flood monitoring framework for operative mapping and impact analysis](https://arxiv.org/abs/2607.28401)
*Ilya Novikov,Svetlana Illarionova,Ruslan Dzharkinov,Maria Smirnova,Ayrat Abdullin,Anna Korotkova,Mariia Ulianova,Dmitrii Shadrin,Evgeny Burnaev*

Main category: cs.CV

TL;DR: 提出一个基于SAR、多光谱和DEM多模态数据的端到端洪水监测与灾害评估框架，对比了监督U-Net++与自监督AnySat两种分割策略，并应用于俄罗斯领土的洪水监测。

Research relevance: 该论文与遥感卫星强相关（使用SAR、多光谱、DEM等卫星遥感数据），也涉及遥感图像分割任务，但**不涉及目标检测**（是语义分割/水表提取），且**不涉及模型轻量化、剪枝、量化、蒸馏、边缘/星上部署**等任何压缩或加速方法。因此相关性评分为3分：遥感+分割方法有借鉴意义，但非目标检测且无轻量化内容。

Reading priority: 2分。该论文与遥感强相关且涉及分割任务，但既非目标检测也非轻量化方向，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。仅多模态融合和自监督训练策略可作间接参考，阅读优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：洪水监测中的水面分割（water surface segmentation）与灾害影响评估（damage assessment）。应用场景：俄罗斯联邦七个地区的洪水监测。传感器类型：SAR（synthetic aperture radar）、多光谱影像（multispectral imagery）、数字高程模型（DEM）及其衍生数据，共21通道输入。目标类别：水面/洪水区域（非目标检测，无舰船/飞机/车辆等目标类别）。

Model architecture: 对比了两种分割架构：(1) 监督式 U-Net++ 模型；(2) 自监督预训练的 AnySat 架构（pre-trained and fine-tuned for segmentation task）。输入为21通道多模态数据（SAR + multispectral + DEM及其衍生）。摘要未说明具体网络深度、模块设计细节。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法，如pruning、quantization、knowledge distillation、NAS、轻量网络设计等均未提及。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关指标。该工作聚焦于洪水监测的准确性和泛化能力，而非在轨/边缘部署。

Datasets and metrics: 使用自收集的多模态数据集（self-collected multimodal dataset），覆盖俄罗斯七个地区。评价指标摘要未明确列出（未说明IoU、F1、mAP等具体指标）。灾害评估遵循俄罗斯紧急情况部（Russian Ministry of Emergency Situations）官方方法论，评估维度包括受灾面积、物质损失、伤亡、生态与农业影响。

Experiments: 主要实验对比了监督式U-Net++与自监督AnySat两种策略在有限标注数据条件下的水面分割效果。结论：在本文数据条件下监督学习更有效，而AnySat方法稳定性更好，在更大规模无标注数据或推理时模态缺失场景下更具优势。将最佳洪水面积预测应用于2019年Tulun洪水灾害评估，结果与官方评估高度吻合（除物质损失因使用开源数据库而存在偏差）。

Limitations: (1) 不涉及目标检测，仅做语义分割；(2) 无任何轻量化/部署相关内容；(3) 摘要未说明模型参数量、计算量、推理速度等关键部署指标；(4) 数据集仅覆盖俄罗斯七个地区，泛化到其他地理区域的能力未充分验证；(5) 物质损失评估因依赖开源数据库存在偏差；(6) 摘要未说明具体分割精度指标（如IoU）。

Ideas for my research: (1) 多模态融合思路可借鉴：SAR+多光谱+DEM的21通道输入设计，对遥感在轨目标检测中多传感器融合有参考价值；(2) 自监督预训练（AnySat）与监督学习的对比实验设计值得参考，可迁移到在轨目标检测的少样本/域适应场景；(3) 数据稀缺条件下的训练策略（监督 vs 自监督）对比思路可用于遥感目标检测小样本场景；(4) 但该论文无轻量化内容，对"模型压缩/加速"课题的直接借鉴有限，更多是数据融合与训练策略层面的启发。

Abstract: Effective flood monitoring is critical for minimizing the impacts of flood disasters on populations and infrastructure. Yet reliable remote sensing across extensive and environmentally diverse regions remains challenging, as most segmentation algorithms lack the generalisation capacity required for large-scale application, while annotated flood data are scarce and unevenly distributed.
  This study presents an end-to-end multimodal framework for Russian Federation territories sustainable flood monitoring and damage assessment based on synthetic aperture radar data, multispectral imagery, and digital elevation models with their derivatives, forming a 21-channel input. Using a self-collected multimodal dataset covering seven Russian regions, two strategies for water surface detection under limited data conditions were compared: a supervised U-Net++ model and the self-supervised AnySat architecture pre-trained and fine-tuned for the segmentation task. Under the data conditions of this study, supervised learning proved more effective, while the AnySat-based approach offered greater stability and retains advantages for settings where larger unlabelled data or missing modalities at inference are expected.
  The best flood area predictions were used to estimate flood impact in urban areas in terms of the area affected, material damage, casualties, and ecological and agricultural impact. The estimations were conducted following the official methodology of the Russian Ministry of Emergency Situations. Applied to the 2019 Tulun flood, the obtained results closely matched official assessments, except for material damage, due to the open-source databases usage. The results demonstrate the potential of deep learning and multimodal satellite data integration for scalable, reliable flood monitoring across diverse environmental and data-limited conditions.

</details>


### [87] [Negative controls reveal volume-driven confounding in radiomics and imaging foundation model features](https://arxiv.org/abs/2607.28423)
*Katy L. Scott,Sejin Kim,Joshua Siraj,Caryn Geady,Matthew Boccalon,Mattea Welch,Mogtaba Alim,Andrew J. Hope,Benjamin Haibe-Kains*

Main category: cs.CV

TL;DR: 提出READII-2-ROQC开源框架，通过体积保持的负对照扰动图像来评估影像组学与深度学习影像特征是否捕获独立的生物学空间信号，用于癌症影像生物标志物的质量控制。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于医学影像（cancer imaging）中的radiomics特征质量控制，属于医学影像分析领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测或模型轻量化/部署。唯一可能的弱关联是"imaging foundation models"和"deep imaging features"涉及深度学习特征提取，但应用场景和任务目标与遥感在轨目标检测完全不同。

Reading priority: 1分。论文属于医学影像radiomics领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对"特征质量控制/负对照验证"方法论感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：影像组学（radiomics）特征与深度学习影像特征的质量控制与可解释性评估。应用场景：癌症影像生物标志物开发（肿瘤体积、HPV状态预测）。传感器类型：医学影像（CT/MRI等，摘要未明确说明具体模态）。目标类别：肿瘤（tumour）。

Model architecture: 摘要未说明具体模型架构。提及使用PyRadiomics提取影像组学特征，以及imaging foundation models提取深度特征。框架核心是生成voxel-perturbed images（体素扰动图像）作为负对照，包括tumour、background和whole-image三个区域的随机化扰动策略。

Lightweight method: 摘要未涉及任何轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、轻量网络设计、实时推理或边缘/星上部署。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性指标。该框架是离线医学影像分析工具，与在轨部署无关。

Datasets and metrics: 数据集：三个公开癌症影像队列（three public cancer imaging cohorts），共处理3,552个肿瘤体积（tumour volumes）。特征提取：PyRadiomics特征和foundation-model特征，从原始图像和9个匹配对照图像中提取。评价指标：模型性能对比（原始图像vs对照图像），复现已发表的survival和HPV-status预测签名。具体指标名称（如AUC、C-index等）摘要未说明。

Experiments: 在三个公开癌症影像队列上，对3,552个肿瘤体积提取PyRadiomics和foundation-model特征，生成9个匹配的对照图像。实验发现：多个模型在空间结构被破坏后仍保持性能，揭示存在volume-driven或contextual confounding（体积驱动或上下文混杂）；另一些模型则表现出对扰动的敏感性信号。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于医学影像分析领域。2. 摘要未说明具体影像模态、模型架构细节、计算资源需求。3. 该框架的"负对照扰动"思想虽有一定方法论借鉴意义，但应用场景差异巨大。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但其"负对照扰动验证特征是否捕获真实信号"的方法论思想有一定可迁移性：在遥感目标检测中，可通过类似的空间扰动/置换实验来验证模型是否真正学习到目标的空间结构特征，而非依赖背景上下文或尺度伪影。这可用于评估轻量化模型在压缩后是否仍保持对真实目标特征的敏感性，而非仅靠捷径学习。但整体借鉴价值有限。

Abstract: Radiomics and imaging foundation models promise non-invasive biomarkers of tumour biology, yet predictive signatures may reflect tumour volume or acquisition artifacts rather than meaningful image structure. We introduce READII-2-ROQC, an open-source framework that uses volume-preserving negative controls to assess whether radiomic and deep imaging features capture independent spatial signals. READII-2-ROQC generates voxel-perturbed images across tumour, background and whole-image regions using configurable randomization strategies, then compares feature behaviour and model performance between original and control images. Applied to three public cancer imaging cohorts, the framework processed 3,552 tumour volumes and extracted PyRadiomics and foundation-model features from original images and nine matched controls. Reproducing published survival and HPV-status signatures, we show that multiple models retain performance after spatial structure is destroyed, revealing volume-driven or contextual confounding, whereas others show perturbation-sensitive signal. READII-2-ROQC provides a scalable quality-control strategy for developing interpretable, biologically grounded imaging biomarkers and reproducible radiomics workflows.

</details>


### [88] [ViewMind3D: Modular View-Aware Inference for Training-Free 3D-QA](https://arxiv.org/abs/2607.28442)
*Ping-Kun Chiang,Kun-Ru Wu,Po-han Li,Sandeep Chinchali,Ufuk Topcu,Yu-Chee Tseng*

Main category: cs.CV

TL;DR: 提出ViewMind3D，一个完全无需训练的模块化框架，通过编排通用LLM和VLM实现多视角场景的3D空间推理与问答，无需3D重建和模型微调。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦3D问答（3D-QA）与具身智能/机器人感知，与遥感卫星、在轨目标检测、轻量化部署均无直接关联。虽然其"training-free"模块化设计思想在推理效率上有一定借鉴意义，但场景、任务、数据均非遥感领域。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于泛视觉/具身智能领域。仅其"training-free模块化编排"和"BEV空间编码"思想有微弱借鉴价值，不建议作为核心参考文献，可作为跨领域方法参考快速浏览。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D question answering (3D-QA)，即基于多视角场景观测的3D空间推理与问答。应用场景：embodied AI、robotic perception（具身智能与机器人感知）。传感器类型：多视角RGB图像（multi-view observations），非遥感传感器。目标类别：场景中的物体（object cues），非遥感目标。

Model architecture: 模块化框架，包含四个可解释组件：(1) question-driven multi-view selection（问题驱动的多视角选择）；(2) guided visual grounding with language-conditioned object cues（语言条件目标线索引导的视觉定位）；(3) spatial context encoding via bird's-eye-view (BEV) viewpoint indicator（基于BEV视角指示器的空间上下文编码）；(4) structured answer generation through role-based reasoning（基于角色推理的结构化答案生成）。底层依赖通用LLM和VLM，无需3D专用训练或微调。

Lightweight method: 摘要未明确提及传统轻量化方法（pruning/quantization/distillation/NAS）。但框架本身是"training-free"（无需训练/微调），通过模块化编排通用LLM/VLM实现推理，避免了3D专用模型的训练开销，在计算资源利用上有一定"轻量"思想。然而未报告参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗等部署指标。且依赖通用LLM/VLM，这类模型通常参数量巨大，实际在星上/边缘部署可行性存疑。虽然training-free避免了训练开销，但推理阶段仍需运行大型LLM/VLM，与在轨轻量化部署需求不符。

Datasets and metrics: 数据集：ScanQA和SQA3D（均为室内3D场景问答基准，非遥感数据集）。评价指标：SQA3D上整体准确率50.8%，其中"What"类空间接地问题提升明显；ScanQA上CIDEr达到73.4。对比对象：prior training-free和fine-tuned 3D-LLMs。

Experiments: 在ScanQA和SQA3D上验证，相比先前的training-free和fine-tuned 3D-LLM方法取得有竞争力的性能。特别在SQA3D的"What"类空间接地问题上表现提升，ScanQA上CIDEr 73.4。摘要未提供消融实验细节和具体对比数值表。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，场景为室内3D问答；2. 依赖大型通用LLM/VLM，推理开销大，未报告任何部署效率指标；3. 未涉及目标检测任务本身，更偏向场景理解与问答；4. 未报告参数量、FLOPs、延迟等关键轻量化指标；5. 数据集为ScanQA/SQA3D，非遥感数据。

Ideas for my research: 1. "training-free"模块化设计思想可借鉴——在遥感在轨场景中，若能将复杂检测任务分解为多个可解释子模块并复用预训练模型，可减少在轨训练/微调负担；2. BEV视角指示器用于空间上下文编码的思路，可迁移到遥感旋转框检测中，帮助模型理解目标的空间方位关系；3. question-driven多视角选择机制，可类比遥感多时相/多角度影像的智能选择策略，减少冗余计算；4. 但需注意，该框架依赖大模型，与在轨轻量化（FPGA/NPU/TensorRT）需求差距较大，借鉴时应聚焦其模块化分解思想而非直接套用。

Abstract: Recent advances in large language models (LLMs) and vision-language models (VLMs) have enabled new possibilities for 3D question answering (3D-QA), a key capability for embodied AI and robotic perception. However, most existing methods rely on 3D-specific training or fine-tuning with costly annotations, limiting their scalability and real-world applicability. We present \textbf{ViewMind3D}, a fully training-free and modular framework for 3D spatial reasoning over multi-view observations of a scene without requiring complete 3D reconstruction. The framework decomposes the 3D-QA task into four interpretable components: (1) question-driven multi-view selection, (2) guided visual grounding with language-conditioned object cues, (3) spatial context encoding via a bird's-eye-view (BEV) viewpoint indicator, and (4) structured answer generation through role-based reasoning. This design enables structured, robust, and interpretable reasoning without requiring model tuning. Experimental results on ScanQA and SQA3D show that ViewMind3D achieves competitive performance compared to prior training-free and fine-tuned 3D-LLMs. In particular, our method improves performance on spatially grounded question types, such as ``What'' questions in SQA3D, while maintaining strong overall accuracy (50.8\%) and achieving 73.4 CIDEr on ScanQA. These results demonstrate that effective 3D reasoning can be achieved through modular orchestration of general-purpose LLMs and VLMs for robotic perception in real-world environments.

</details>


### [89] [VisualRouter: Query-Grounded Visual Sampling for Long Video Understanding](https://arxiv.org/abs/2607.28463)
*Haiyue Zhang,Yi Bin,Xun Jiang,Zeyu Ma,Duo Peng,Guoqing Wang,Yang Yang,Heng Tao Shen*

Main category: cs.CV

TL;DR: 提出VisualRouter，一种无需训练、即插即用的查询引导视觉采样框架，通过将查询分类为全局/局部并采用对应采样策略，提升大视觉语言模型在长视频理解中的表现。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，聚焦长视频理解中的视觉token采样问题，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然"减少视觉token数量"在概念上与轻量化有间接相似性，但研究对象是视频帧采样而非模型压缩或推理加速，且不涉及遥感场景。

Reading priority: 1分。该论文聚焦长视频理解中的视觉采样，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在"输入侧数据精简"概念上有极弱的间接启发，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长视频理解中的视觉采样（visual sampling）；应用场景：视频问答、视频理解；传感器类型：普通视频帧（非遥感/卫星影像）；目标类别：不涉及具体目标检测类别。

Model architecture: VisualRouter框架包含两个核心模块：(1) 查询分类器，将query分为global或local两类；(2) 针对global query采用relevance-coverage混合采样策略，针对local query采用event-aware帧选择策略（包含event partitioning、segment-level frame allocation、intra-event frame selection三个步骤）。整体为training-free、plug-and-play设计，可嵌入现有LVLM。

Lightweight method: 摘要未明确涉及传统模型压缩方法（pruning/quantization/distillation/NAS）。但其核心思想是通过减少输入视觉token数量（帧采样）来降低计算开销，属于输入侧的数据精简策略，与轻量化在"减少计算量"目标上有间接相似性，但并非模型层面的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。由于是training-free方法，无需额外训练开销，但未提供推理加速的具体量化数据。不涉及星上/边缘部署场景。

Datasets and metrics: 数据集：Video-MME、LongVideoBench、MLVU；评价指标：视频理解准确率（具体指标名称摘要未说明，推测为accuracy或相关QA指标）；基座模型：Qwen2.5-VL-7B；对比方法：uniform sampling及现有training-free visual sampling方法。

Experiments: 在Video-MME、LongVideoBench、MLVU三个数据集上，VisualRouter相对uniform sampling分别提升5.2%、7.7%、11.6%（使用Qwen2.5-VL-7B），并优于同设置下现有training-free视觉采样方法。摘要未提供消融实验细节。

Limitations: 摘要未说明：(1) 未涉及遥感/卫星影像场景；(2) 未提供计算开销、推理速度等部署指标；(3) 未说明对local query的event-aware策略在复杂场景下的鲁棒性；(4) 未讨论采样帧数对性能-效率权衡的影响；(5) 未涉及模型压缩或推理加速。

Ideas for my research: 尽管该论文与遥感在轨目标检测无直接关联，但其"查询引导的输入精简"思想有一定借鉴价值：(1) 在遥感在轨场景中，可借鉴"query分类+自适应采样"的思路，根据检测任务需求（如全局场景理解 vs 局部小目标搜索）动态选择输入影像块或波段，减少无效计算；(2) "relevance-coverage混合策略"可类比于遥感影像中兼顾"目标相关区域"与"全局覆盖"的裁剪/分块策略；(3) training-free、plug-and-play的设计理念适合星上部署场景，因为星上资源受限且难以进行大规模训练。但整体借鉴价值有限，因为核心问题域差异较大。

Abstract: Large vision-language models (LVLMs) have achieved significant progress in video understanding, yet understanding long videos remains challenging due to the large number of visual tokens and limited context windows. Visual sampling provides a practical solution by selecting an informative subset of frames. However, existing methods typically either rely on relevance-aware sampling, leading to redundant frame selection and insufficient temporal coverage, or adopt a fixed sampling strategy regardless of query type. In this paper, we propose VisualRouter, a training-free and plug-and-play framework for query-grounded visual sampling. VisualRouter first classifies each query as either global or local and then applies the corresponding sampling strategy. For global queries, it employs a relevance-coverage hybrid strategy that preserves temporal coverage while retaining query-relevant visual evidence. For local queries, it adopts an event-aware frame selection strategy that performs event partitioning, segment-level frame allocation, and intra-event frame selection, jointly balancing relevance, coverage, and diversity with a limited number of input frames. Experiments show that VisualRouter consistently improves multiple LVLMs over uniform sampling, achieving gains of 5.2%, 7.7%, and 11.6% on Video-MME, LongVideoBench, and MLVU with Qwen2.5-VL-7B, and outperforming existing training-free visual sampling methods under the same setting.

</details>


### [90] [Can Vision-Language Models Reason about AI Edits in Images?](https://arxiv.org/abs/2607.28464)
*Darsha Udayanga,Pin-Yu Chen,Payel Das,Qiang Ji*

Main category: cs.CV

TL;DR: 提出基于GRPO强化学习的VLM训练框架，让视觉语言模型通过推理轨迹检测和定位AI篡改图像，并用轻量分割模型生成像素级定位掩码。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦AI图像伪造检测与定位，与遥感、卫星、在轨部署、目标检测均无直接关联。虽然提到"lightweight segmentation model"，但轻量化并非论文核心贡献，只是辅助模块，且不涉及遥感场景。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向基本无关，仅"轻量分割模型"和"RL训练范式"有微弱借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI篡改图像检测与定位（image forgery detection and localization）。应用场景：可信AI、图像取证。传感器类型：普通自然图像（非遥感/卫星影像）。目标类别：AI生成/篡改图像区域。

Model architecture: 核心架构：Vision-Language Model (VLM) 作为推理主体，采用GRPO（Group Relative Policy Optimization）强化学习训练，模型先生成结构化推理轨迹（reasoning trace）再输出篡改判定；随后由轻量分割模型（lightweight segmentation model）根据推理结果生成像素级定位掩码。

Lightweight method: 摘要仅提及使用"lightweight segmentation model"生成像素级定位掩码，但未说明该轻量模型的具体结构、压缩方法（剪枝/量化/蒸馏等）或部署指标。轻量化并非本文核心贡献。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。VLM本身通常计算开销较大，且本文未讨论边缘/星上部署可行性。

Datasets and metrics: 数据集：多个图像篡改数据集（multiple image manipulation datasets），摘要未列出具体名称。评价指标：提出统一指标 effective intersection over union (eff-IoU) 联合评估检测与定位性能；同时对比SOTA图像伪造检测器。

Experiments: 实验表明该方法在多个篡改数据集上达到与SOTA图像伪造检测器相当的检测与定位性能，但所需监督显著更弱（substantially weaker supervision）。摘要未提供具体数值结果或消融实验细节。

Limitations: 1. 与遥感/卫星场景完全无关，未涉及遥感影像特性（多光谱、SAR、复杂背景、小目标等）；2. 未提供轻量分割模型的具体压缩手段和部署指标；3. VLM+RL训练框架计算成本高，实际部署可行性存疑；4. 摘要未说明推理速度、模型规模等关键工程指标。

Ideas for my research: 1. GRPO/RL训练范式可借鉴：在遥感目标检测中，可尝试用强化学习奖励机制引导模型生成推理轨迹，提升检测可解释性；2. "轻量分割模型+推理引导"的两阶段思路可迁移到遥感小目标检测中，用轻量网络做粗定位再精修；3. eff-IoU这种统一评估检测与定位的指标设计思路，对遥感旋转框检测的评估指标设计有参考价值；4. 但整体借鉴价值有限，因为核心场景（AI篡改图像）与遥感目标检测差异较大。

Abstract: Detection and localization of AI-tampered images are critical for trustworthy AI, yet modern generative models have made such manipulations increasingly difficult to identify. While traditional binary classifiers can detect image tampering, they lack interpretability and generalization. Vision-Language Models (VLMs) offer a promising alternative due to their strong visual understanding and reasoning capabilities; however, existing approaches typically rely on supervised finetuning with curated explanations rather than exploiting their inherent reasoning capabilities. In this work, we investigate whether VLMs can be trained to reason about AI-generated image edits using reinforcement learning (RL) rather than explicit reasoning supervision. Motivated by the success in Group Relative Policy Optimization (GRPO), an RL technique that incentivizes the model to reason by generating thinking traces prior to giving the final answer, we propose a GRPO-based training framework that utilizes simple accuracy and format rewards. Given an input image, the model produces a structured reasoning trace and predicts whether the image has been tampered with. A lightweight segmentation model is then guided by the reasoning output to generate pixel-level localization masks. Experiments across multiple image manipulation datasets demonstrate that our approach achieves competitive detection and localization performance compared to state-of-the-art image forgery detectors, despite requiring substantially weaker supervision. We introduce effective intersection over union (eff-IoU), a unified metric to jointly evaluate detection and localization. These results suggest that reinforcement learning provides an effective and scalable mechanism for teaching VLMs to reason about AI-generated content.

</details>


### [91] [Towards Real-Time PixOOD: Efficient Anomaly Segmentation for Autonomous Vehicles](https://arxiv.org/abs/2607.28483)
*Luca de Martino,Federico Aromolo,Federico Nesti,Giorgio Buttazzo*

Main category: cs.CV

TL;DR: 提出一种面向自动驾驶和铁路场景的实时异常分割加速流水线，通过重构PixOOD的Neyman-Pearson评分阶段并利用TensorRT硬件优化编译，在桌面GPU和嵌入式平台（Jetson AGX Orin）上分别达到182 FPS和75 FPS，比原始基线快20倍和18倍。

Research relevance: 弱相关（2-3分）。该论文聚焦于自动驾驶和铁路领域的异常分割（anomaly segmentation）与嵌入式部署，属于泛视觉/边缘部署方法。它不涉及遥感、卫星、Earth observation、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测（object detection）任务本身。但其"通过TensorRT硬件优化编译实现嵌入式平台高效部署"的思路对星上部署（onboard deployment）具有方法论上的借鉴意义，因此介于2分（泛视觉弱相关）与3分（部署方法有借鉴意义但非遥感场景）之间，综合判定为2分。

Reading priority: 2分。该论文与遥感卫星在轨目标检测方向弱相关，主要价值在于TensorRT嵌入式部署和算法-硬件协同优化的方法论借鉴，但任务类型（异常分割vs目标检测）和场景（自动驾驶/铁路vs遥感卫星）均不匹配，阅读优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：实时异常分割（real-time anomaly segmentation），属于out-of-distribution detection范畴。应用场景：自动驾驶（autonomous driving）和铁路（railway）领域。传感器类型：摘要未说明（推测为车载相机/铁路监控相机）。目标类别：异常区域/异常对象，非特定类别目标检测。

Model architecture: 基于PixOOD（state-of-the-art out-of-distribution detection方法）的流水线，核心改进是重构其Neyman-Pearson评分阶段（Neyman-Pearson scoring stage）。具体网络结构细节摘要未说明。

Lightweight method: 主要轻量化/加速手段为：1) 算法层面重构Neyman-Pearson评分阶段以降低计算开销；2) 硬件层面通过TensorRT编译优化（hardware-optimized TensorRT compilation）实现全流水线部署加速。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS。

Onboard deployability: 部署信息明确：桌面平台NVIDIA RTX 4060 GPU上达到182 FPS，嵌入式平台NVIDIA Jetson AGX Orin上达到75 FPS，分别比原始基线快20x和18x。摘要未说明参数量（parameters）、FLOPs、显存占用、能耗等具体指标。该工作证明了异常分割流水线可在嵌入式平台（Jetson AGX Orin）上实现实时处理，对边缘/车载部署有直接参考价值。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及面向自动驾驶和铁路领域，具体benchmark（如可能为相关异常分割数据集）未给出。

Experiments: 主要实验结果为：桌面RTX 4060 GPU上182 FPS（20x加速），Jetson AGX Orin嵌入式平台上75 FPS（18x加速）。摘要未说明精度对比、消融实验或与其他方法的详细对比。

Limitations: 1) 非遥感场景，未涉及遥感图像特性（如小目标、旋转目标、复杂背景）；2) 任务为异常分割而非目标检测，与遥感在轨目标检测任务类型不同；3) 摘要未提供精度指标，无法判断加速是否以精度损失为代价；4) 未说明参数量、FLOPs、能耗等部署关键指标；5) 未涉及剪枝、量化等模型压缩方法，仅依赖TensorRT编译优化。

Ideas for my research: 1) TensorRT硬件优化编译思路可直接借鉴：在遥感在轨目标检测模型部署中，可考虑将轻量化后的检测模型通过TensorRT/FPGA/NPU等硬件加速编译进一步压缩推理延迟；2) 算法-硬件协同优化（algorithm-hardware co-design）的理念值得借鉴，即不仅做模型压缩，还要针对目标硬件平台做算子级优化；3) 该工作展示了嵌入式平台（Jetson AGX Orin）上实现实时推理的可行性，可作为遥感在轨部署硬件选型的参考；4) 重构评分/后处理阶段以降低计算开销的思路，可类比到遥感检测中NMS、ROI提取等后处理环节的轻量化。

Abstract: Real-time anomaly segmentation is essential for the safety of autonomous systems. Although recent approaches offer high accuracy, their computational cost limits their deployment on embedded hardware. This work presents an efficient and accelerated pipeline designed for both embedded and desktop platforms, targeting the autonomous driving and railway domains. The proposed approach reformulates the Neyman-Pearson scoring stage of PixOOD, a state-of-the-art out-of-distribution detection method, and deploys the full pipeline through hardware-optimized TensorRT compilation, reaching up to 182 FPS on a desktop NVIDIA RTX 4060 GPU and 75 FPS on the NVIDIA Jetson AGX Orin embedded platform, respectively 20x and 18x faster than the original baseline. The achieved results demonstrate that advanced anomaly segmentation can be efficiently deployed for onboard processing in autonomous driving and railway applications.

</details>


### [92] [AuricularWorld: Hierarchical Action-Guided World Modeling for Fine-Grained Auricular Structure Segmentation from CT Scans](https://arxiv.org/abs/2607.28487)
*Jingwen Yang,Senmao Wang,Luoyao Kang,Runmeng Cui,Keying Zhang,Yunjia Bao,Haifan Gong,Lin Lin,Haiyue Jiang*

Main category: cs.CV

TL;DR: 提出一种基于世界模型（world-model）的CT耳廓结构细粒度分割框架，通过确定性循环状态空间模型在潜在空间中进行迭代解剖推理，显著提升小尺寸、不规则、重叠耳廓结构的分割精度并降低HD95超过43%。

Research relevance: 弱相关（2分）。该论文属于医学图像分割（medical image segmentation）领域，针对CT影像中的耳廓（auricular）结构，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。其核心贡献在于潜在空间中的世界模型推理机制，属于泛视觉/泛机器学习方法，对遥感目标检测的借鉴意义有限。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，属于医学图像分割领域。虽然潜在空间迭代推理和前景稀疏处理思想有一定借鉴价值，但整体方向偏离，且未涉及任何轻量化或部署内容，阅读优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学图像细粒度分割（fine-grained segmentation）；应用场景：CT影像中的耳廓结构解剖分析；传感器类型：CT（Computed Tomography）；目标类别：耳廓软骨结构（auricular cartilage）及其复合结构（含软骨与相邻皮肤）。

Model architecture: 基于encoder-decoder架构，在中间潜在空间引入确定性循环状态空间模型（deterministic recurrent state-space model）。多尺度encoder特征与部分解码表示融合形成结构观测（structural observation）以初始化潜在动力学；推理时执行三步潜在rollout（latent rollout），通过分层解剖动作（hierarchical anatomical actions）更新循环状态并逐步细化潜在表示；最终潜在轨迹投影回decoder并与高分辨率特征结合产生分割结果。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法，也未讨论实时推理或边缘部署。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。且该框架在推理时需执行三步潜在rollout，属于迭代推理机制，计算开销可能较大，不利于资源受限的星上部署场景。

Datasets and metrics: 数据集：摘要未说明具体数据集名称（仅提及CT影像中的耳廓结构）；评价指标：分割精度（segmentation accuracy）和HD95（Hausdorff Distance 95th percentile），HD95降低超过43%。

Experiments: 实验表明所提框架在小尺寸、不规则、重叠的耳廓CT结构上持续提升分割精度，HD95降低超过43%。摘要未提供与其他方法的详细对比、消融实验细节或具体数值。

Limitations: 1. 与遥感/卫星/在轨目标检测方向基本无关，属于医学影像领域；2. 推理阶段的三步潜在rollout属于迭代过程，计算开销和延迟可能较高，未讨论实时性；3. 摘要未提供参数量、FLOPs等轻量化关键指标；4. 未讨论该框架在非医学场景（如遥感图像）的泛化能力。

Ideas for my research: 1. 潜在空间中的迭代推理（latent rollout）思想可借鉴：遥感小目标检测中，可通过循环状态更新逐步细化目标特征表示，类似"由粗到精"的迭代检测策略；2. 平衡的分层动作目标（balanced hierarchical action objective）处理前景稀疏问题的思路，可迁移到遥感小目标检测中正负样本极度不平衡的场景；3. 多尺度特征与部分解码表示融合形成结构观测的机制，与遥感目标检测中FPN/多尺度特征融合思想相通，可参考其融合方式；4. 但需注意：该框架推理开销较大，若借鉴需考虑轻量化改造（如减少rollout步数、蒸馏为单步推理等）。

Abstract: Fine-grained segmentation of auricular structures in CT is challenging because the ear occupies a small image region, cartilage boundaries are highly irregular, and interfaces between cartilage and surrounding soft tissues are often ambiguous. Clinical annotations may also include both composite structures containing cartilage and adjacent skin and their corresponding cartilage-only regions, producing nested and overlapping labels. We propose a world-model-based segmentation framework that enables iterative anatomical reasoning beyond conventional feed-forward prediction. Built on an encoder-decoder architecture, the framework introduces a deterministic recurrent state-space model into the intermediate latent space. Multi-scale encoder features and partially decoded representations are fused to form a structural observation that initializes the latent dynamics. During inference, the model performs a three-step latent rollout without ground-truth guidance. Hierarchical anatomical actions update the recurrent state and progressively refine the latent representation. The resulting latent trajectory is projected back into the decoder and combined with high-resolution features to produce the final segmentation. To learn reliable latent transitions, we introduce a balanced hierarchical action objective that addresses foreground sparsity, missing anatomical groups, and imbalance between add and remove operations. Extensive experiments show that the proposed framework consistently improves segmentation accuracy and reduces HD95 by more than 43% for small, irregular, and overlapping auricular structures in CT. These results demonstrate the effectiveness of latent world-model reasoning for challenging medical image segmentation.

</details>


### [93] [RefCaptioner: Multi-Reference Image-Grounded Video Captioning](https://arxiv.org/abs/2607.28509)
*Tengfei Liu,Yang Shi,Yuran Wang,Xiaohan Zhang,Yuqing Wen,Yuqi Tang,Qixun Wang,Zhuoran Zhang,Xuanyu Zhu,Weihong Lin,Xinlei Yu,Yujie Wei,Xinwei Long,Fengxiang Wang,Xinlong Chen,Yue Ding,Jialu Chen,Haotian Wang,Yuanxing Zhang*

Main category: cs.CV

TL;DR: 提出多参考图像锚定的视频描述生成新任务，并构建RefCaptioner两阶段后训练框架，通过混合数据SFT与分层覆盖折扣GRPO提升参考选择、短语级绑定与干扰项拒绝能力。

Research relevance: 弱相关（1-2分）。该论文聚焦视频描述生成（video captioning）与多参考图像锚定，属于多模态自然语言处理与视频理解领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关联。论文不涉及目标检测、小目标检测、旋转框检测或遥感场景理解，也不涉及剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署等轻量化或部署技术。其核心贡献在于多模态对齐与生成式任务，与"遥感卫星在轨目标检测模型轻量化"方向基本无关。

Reading priority: 1分。该论文属于多模态视频描述生成领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感场景或任何模型压缩/部署技术。除非对多模态对齐或强化学习训练策略有跨领域兴趣，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多参考图像锚定的视频描述生成（multi-reference image-grounded video captioning），属于多模态视频理解与自然语言生成任务。应用场景为通用视频内容描述与跨模态引用锚定，非遥感场景。传感器类型：摘要未说明（涉及视频与参考图像，非遥感传感器）。目标类别：视频中的局部视觉元素（local visual elements），非遥感目标。

Model architecture: RefCaptioner采用两阶段后训练（two-stage post-training）框架，包含：(1) 混合数据监督微调（mixed-data SFT）；(2) 分层覆盖折扣GRPO（Hierarchical Coverage-Discounted GRPO）强化学习。模型联合优化参考选择（reference selection）、短语级绑定（phrase-level binding）、干扰项拒绝（distractor rejection）与跨参考一致性（cross-reference consistency），同时保持通用视频描述能力。具体骨干网络结构摘要未说明。

Lightweight method: 摘要未涉及任何轻量化方法。不包含剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、神经架构搜索（NAS）、模型压缩或加速技术。该论文关注的是多模态生成任务的训练策略与对齐能力，而非模型效率或部署优化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该论文面向通用视频描述生成任务，未考虑星上或边缘部署场景，不具备在轨遥感目标检测的部署可行性分析。

Datasets and metrics: 构建了包含20,000个视频和171,354张参考图像的训练语料库；引入MRVBench基准，用于评估视频描述事实性（caption factuality）与多参考锚定能力，覆盖真实世界与AI生成视频。同时使用标准视频描述基准（standard video captioning benchmarks）进行对比评估。具体指标名称（如BLEU、CIDEr、ROUGE等）摘要未明确列出。

Experiments: 实验表明RefCaptioner在开源模型中取得最佳综合性能，并在标准视频描述基准上保持竞争力。人工评估确认其生成的描述更受标注者偏好，且能实现更忠实于源信息的视频重建（source-faithful video reconstruction），兼容开源与专有视频生成器。未提供与遥感或目标检测相关的实验。

Limitations: 摘要未说明模型规模、推理效率、训练成本等关键信息；未讨论在资源受限设备上的可行性；未涉及遥感或卫星场景的任何验证；未提供消融实验细节（如各组件对性能的具体贡献）；未说明GRPO训练的计算开销与收敛稳定性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但以下思路可作间接借鉴：(1) 分层覆盖折扣GRPO中的"覆盖度"与"干扰项拒绝"思想，可类比于遥感目标检测中抑制背景干扰与误检（如虚警抑制）的奖励设计；(2) 两阶段后训练框架（SFT + 强化学习）的思路，可迁移到遥感检测模型的领域自适应微调中；(3) 多参考一致性约束可类比于多视角/多时相遥感图像的一致性正则化。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Existing video captioning models generate natural descriptions of video content but cannot explicitly ground local visual elements to multiple reference images. We introduce multi-reference image-grounded video captioning, a new task requiring factual video descriptions with phrase-level reference grounding, and propose RefCaptioner, a two-stage post-training framework for this task. RefCaptioner combines mixed-data SFT with Hierarchical Coverage-Discounted GRPO to jointly improve reference selection, phrase-level binding, distractor rejection, and cross-reference consistency while preserving general video-captioning ability. To support training, we construct a corpus containing $20,000$ videos and 171,354 reference images. We further introduce MRVBench, a benchmark for evaluating caption factuality and multi-reference grounding on both real-world and AI-generated videos. Experiments show that RefCaptioner achieves the best overall performance among the open-source models while remaining competitive on standard video captioning benchmarks. Human evaluation further confirms that its captions are preferred by annotators and enable more source-faithful video reconstruction with both open-source and proprietary video generators.

</details>


### [94] [Beyond Frame Selection: Generative Latent Evidence Aggregation for Long-Video Understanding](https://arxiv.org/abs/2607.28516)
*Bowen Liu,Shuning Wang,Xinpeng Ding,Zhiheng Wu,Bodong Du,Xiaomeng Li*

Main category: cs.CV

TL;DR: GenEvA提出一种生成式潜在证据聚合框架，通过查询条件化的证据分布将长视频中选定的帧组织为跨帧潜在证据，以极低的token开销提升Video-MLLM的长视频理解能力。

Research relevance: 弱相关（2分）。该论文属于泛视觉/多模态大模型（Video-MLLM）领域，聚焦长视频理解中的跨帧证据聚合，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然涉及"compact pipelines"和"token overhead"等效率概念，但并非面向遥感场景，也未涉及目标检测或模型压缩/加速方法。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于视频多模态大模型领域。虽然"自适应证据调用"和"低token开销"思想有一定借鉴价值，但整体相关性极低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长视频理解（long-video understanding）中的问答生成；应用场景：视频多模态大模型（Video-MLLM）；传感器类型：普通视频帧；目标类别：不涉及具体目标检测类别。摘要未说明具体视频数据集内容。

Model architecture: GenEvA框架包含：查询条件化证据分布（query-conditioned evidence distribution）、潜在证据聚合（latent evidence aggregation）、自适应证据调用（Adaptive Evidence Invocation）。基于两个Video-MLLM骨干：LLaVA-Video和Qwen2.5-VL。核心思想是在帧选择后增加一个"post-selection"阶段，将选定的帧组织为查询相关的跨帧潜在证据。

Lightweight method: 并非传统模型压缩方法（无剪枝/量化/蒸馏/NAS）。其效率优化体现在：通过潜在证据聚合减少视频token开销，仅增加0.11%-0.40%的平均视频token开销；通过Adaptive Evidence Invocation按需决定是否插入潜在补充证据，实现任务感知的token分配。属于"紧凑管线"（compact pipeline）设计，而非模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及token开销为0.11%-0.40%，但这属于推理时的token效率而非模型部署指标。不涉及星上/边缘部署可行性分析。

Datasets and metrics: 四个基准数据集（摘要未列出具体名称，仅提及LVBench）；两个Video-MLLM骨干：LLaVA-Video和Qwen2.5-VL；评价指标：LLaVA-Video四基准平均分（+5.2点）、Qwen2.5-VL在LVBench上的准确率（+10.1点）。

Experiments: 在8帧设置下，GenEvA在四个基准上提升LLaVA-Video平均分+5.2点，在LVBench上提升Qwen2.5-VL准确率+10.1点。消融/分析实验表明：任务感知分配（task-aware allocation）和Adaptive Evidence Invocation带来收益。摘要未说明与更多基线方法的详细对比。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于视频理解领域；2. 摘要未说明模型参数量、计算量等部署关键指标；3. 未涉及任何遥感数据集或遥感目标检测任务；4. 未涉及模型压缩方法（剪枝/量化/蒸馏/NAS）；5. 摘要未说明在真实边缘/星上硬件上的推理性能。

Ideas for my research: 1. "查询条件化证据分布"的思想可借鉴：在遥感目标检测中，可设计任务/类别条件化的特征聚合机制，让模型聚焦于与检测目标相关的特征通道或区域；2. "Adaptive Evidence Invocation"（按需调用）思想可迁移到轻量化设计：在推理时根据输入复杂度动态决定是否启用深层/重模块，实现自适应计算分配，类似动态推理/early-exit思路；3. token效率优化思路（以极低开销换取性能提升）对星上有限带宽/算力场景有启发，但需结合具体检测任务重新设计。

Abstract: Long-video understanding commonly compresses videos into a small set of frames or visual tokens for answer generation. Existing compact pipelines focus on retaining relevant visual content as explicit evidence. Yet making evidence available does not ensure that complementary cues across moments are integrated for answering. Our key idea is to organize selected frames into query-relevant cross-frame evidence before generation. We formulate this post-selection stage as a latent evidence interface and instantiate it with GenEvA ($\textbf{Gen}erative$ $Latent$ $\textbf{Ev}idence$ $\textbf{A}ggregation$), a distribution-guided latent evidence aggregation framework. Specifically, GenEvA uses a query-conditioned evidence distribution to focus aggregation on relevant frames, forming compact cross-frame latent evidence from their frame-specific information. Since cross-frame integration is not always needed, the same distribution determines whether to insert this latent complement. Across four benchmarks and two Video-MLLM backbones, GenEvA consistently improves matched-frame baselines. At 8 frames, it raises the four-benchmark LLaVA-Video average by $+5.2$ points and Qwen2.5-VL accuracy on LVBench by $+10.1$ points. These gains require only $0.11\%$--$0.40\%$ average video-token overhead; analyses further show task-aware allocation and benefits from Adaptive Evidence Invocation.

</details>


### [95] [What to Remove, What to Preserve: Dual-Ambiguity Rectification for All-in-One Image Restoration](https://arxiv.org/abs/2607.28526)
*Cencen Liu,Wen Yin,Dongyang Zhang,Dongmin Li,Shan Zhao,Bing Su,Tao He,Jielei Wang,Guoming Lu*

Main category: cs.CV

TL;DR: 提出DAR-Net，一种面向全合一图像复原（all-in-one image restoration）的双重歧义校正网络，通过退化原型表示（DAR）、语义歧义校正（SeAR）和空间歧义校正（SpAR）模块，在统一框架内处理多种退化类型，在标准复原基准上取得最优性能。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛图像复原方向，不涉及遥感、卫星、目标检测或轻量化部署。虽然图像复原（image restoration）在遥感图像预处理中有潜在应用价值（如去云、去雾、去噪），但论文本身未针对遥感场景设计，也未涉及目标检测或模型压缩。属于泛机器学习/泛视觉方法，与"遥感卫星在轨目标检测与模型轻量化"方向弱相关。

Reading priority: 2分。论文与遥感、卫星、目标检测、轻量化均无直接关联，属于通用图像复原方法。仅在"退化感知特征解耦"这一思想层面有间接借鉴价值，但优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：all-in-one image restoration（全合一图像复原），即在统一框架内处理多种退化类型（如去雨、去雾、去噪等）。应用场景：通用图像复原，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：不涉及目标检测，无目标类别。

Model architecture: DAR-Net包含三个核心模块：1) Degradation Archetype Representation (DAR)模块——通过simplex-constrained archetype mixture modeling构建结构化的退化状态表示；2) Semantic Ambiguity Rectification (SeAR)模块——生成degradation-aware prompts以改进解码器中的channel-wise conditioning；3) Spatial Ambiguity Rectification (SpAR)模块——将degradation-aware和complementary特征正则化到正交响应子空间，减少移除与保留线索之间的空间干扰。整体为encoder-decoder架构。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等任何轻量化或模型压缩方法，也未讨论实时推理或边缘部署。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。从方法复杂度看（包含archetype mixture modeling、prompt生成、正交子空间正则化等），模型可能较重，但摘要未给出量化数据。

Datasets and metrics: 数据集：标准all-in-one restoration benchmarks（三退化与五退化设置），以及CDD-11和WeatherBench。评价指标：PSNR（峰值信噪比）。在three-degradation设置下平均PSNR较最强对手提升0.14 dB，在five-degradation设置下提升0.34 dB。

Experiments: 在标准all-in-one复原基准上，DAR-Net在three-degradation和five-degradation两种设置下均取得最优平均PSNR，分别较最强竞争者提升0.14 dB和0.34 dB；在CDD-11和WeatherBench上也表现更优。摘要未提供消融实验细节。

Limitations: 1) 论文面向通用图像复原，未针对遥感影像的退化特性（如云遮挡、薄雾、传感器噪声、条带噪声等）进行专门设计；2) 未涉及目标检测任务，复原结果对下游检测性能的影响未验证；3) 未提供任何轻量化或部署相关信息，模型复杂度未知；4) 摘要未说明推理速度、参数量等关键工程指标。

Ideas for my research: 1) DAR模块的"退化原型表示"思想可借鉴到遥感场景——遥感影像退化类型多样（云、雾、噪声、条带），若在轨检测前做轻量化的退化感知预处理，可借鉴archetype mixture modeling来建模退化状态；2) SeAR模块的degradation-aware prompts思想可用于遥感目标检测中的域自适应或退化鲁棒性增强；3) 但需注意，该论文方法较重，若要在轨应用需配合轻量化手段（如蒸馏、剪枝）压缩，这本身可作为一个研究方向；4) 正交子空间正则化思想可用于解耦遥感检测中的"目标特征"与"背景/退化特征"，提升小目标检测鲁棒性。

Abstract: All-in-one image restoration aims to handle diverse degradations within a unified framework. Existing methods commonly encode heterogeneous degradation conditions in a shared latent space, where degradation-related cues and scene content can remain entangled. We characterize the resulting challenge as dual ambiguity: semantic ambiguity in channel-wise modulation and spatial ambiguity in restoration responses, which can lead to content corruption and residual artifacts. To mitigate this issue, we propose DAR-Net, a Dual-Ambiguity Rectification Network for all-in-one image restoration. DAR-Net first introduces a Degradation Archetype Representation (DAR) module to construct a structured degradation state through simplex-constrained archetype mixture modeling. Based on this state, a Semantic Ambiguity Rectification (SeAR) module generates degradation-aware prompts to improve channel-wise conditioning in the decoder. A Spatial Ambiguity Rectification (SpAR) module further regularizes degradation-aware and complementary features toward orthogonal response subspaces, reducing spatial interference between removal and preservation cues. Extensive experiments on standard all-in-one restoration benchmarks show that DAR-Net achieves the best overall performance under both three-degradation and five-degradation settings, improving the average PSNR over the strongest competitor by 0.14 dB and 0.34 dB, respectively; it additionally shows superior performance on CDD-11 and WeatherBench.

</details>


### [96] [MarkushGlyph and OCSRGlyph: Improved Chemical Structure Recognition](https://arxiv.org/abs/2607.28532)
*Alex Andonian,Samuel G Rodriques,Andrew D White,Siddharth M Narayanan*

Main category: cs.CV

TL;DR: 提出OCSRGlyph和MarkushGlyph两个视觉-语言模型，将化学结构图像（包括单个分子和Markush结构）转化为文本行符号表示，属于光学化学结构识别（OCSR）任务。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于化学结构图像识别（OCSR）和Markush结构解析，属于化学信息学与文档图像理解领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测或模型轻量化/部署。相关性评分为1分。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于化学信息学领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：光学化学结构识别（OCSR）和Markush结构图像到文本翻译（image-to-text translation）。应用场景：专利与科学文献中的化学结构图像解析、数据库索引、机器学习训练集构建。传感器类型：不适用（输入为化学结构图像）。目标类别：单个分子结构、Markush结构（分子家族）。

Model architecture: OCSRGlyph：基于图像到文本翻译框架的OCSR模型，特别关注立体化学（stereochemistry）信息的处理。MarkushGlyph：视觉-语言模型（vision-language model），将整个Markush结构作为图像整体读取，与以往多阶段分别处理视觉和文本输入的系统形成对比。摘要未说明具体网络架构（如Transformer、CNN等细节）。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息，且该任务本身与星上/边缘部署场景无关。

Datasets and metrics: 摘要未说明具体数据集名称。论文提出一个新的评估指标用于衡量Markush结构翻译的准确性，以处理先前指标中存在的失败模式（failure modes）。摘要未说明具体评价指标细节。

Experiments: 摘要未说明具体实验结果数值。论文声称OCSRGlyph在OCSR任务上达到state-of-the-art水平，优于先前方法，改进点在于对立体化学信息的仔细处理；MarkushGlyph采用端到端的视觉-语言模型方式，与多阶段系统形成对比。摘要未提供消融实验或对比实验细节。

Limitations: 摘要未说明局限性。但可推断：该工作面向化学文档图像解析，与遥感目标检测在数据模态、任务目标、部署约束上均无交集；未涉及任何轻量化或实时推理考量；Markush结构解析本身仍具挑战性，新指标也仅针对该特定任务设计。

Ideas for my research: 借鉴意义有限。唯一可能间接相关的点是：MarkushGlyph采用端到端视觉-语言模型整体读取图像、避免多阶段级联误差的思路，与遥感目标检测中"端到端一体化"设计思想有一定类比价值；以及论文提出新评估指标以处理先前指标失败模式的做法，提示我们在轻量化模型评估中也可设计更贴合在轨部署约束的指标（如能耗-精度联合指标）。但整体上该论文对本课题的直接可借鉴性很低。

Abstract: Chemical structures appear in patents and the scientific literature as images. For programmatic usage, such as indexing in databases or constructing machine learning model training sets, they must be transformed into line notations. The two common forms of this task are translating an image of a single molecule (optical chemical structure recognition - OCSR) and translating a Markush structure that represents a family of molecules. While prior work in the former case is quite mature, Markush structure parsing remains a challenging task. In this work, we treat both tasks as an image-to-text translation problem. We then propose OCSRGlyph, a state-of-the-art OCSR model, improving performance over prior methods by carefully considering stereochemistry. For the Markush task, we introduce MarkushGlyph, a vision-language model that reads the entire Markush structure as an image. This contrasts with prior systems, which often use multiple stages to separately process visual and text input content. Finally, we introduce a new metric for determining the accuracy of Markush structure translations, handling failure modes present in prior metrics.

</details>


### [97] [ScaFE: Data-Efficient Scar Classification with LLM-Generated Clinical Feature Programs](https://arxiv.org/abs/2607.28538)
*Ruman Wang,Hangting Ye*

Main category: cs.CV

TL;DR: 提出ScaFE框架，利用LLM将临床知识转化为可执行的确定性特征程序，在本地环境运行轻量级Random Forest分类器，用于病理性疤痕（瘢痕疙瘩与增生性疤痕）的医学图像分类，兼顾数据治理、可审计性与跨院泛化。

Research relevance: 弱相关（2分）。该论文属于医学图像分类（病理性疤痕识别），与遥感/卫星/在轨部署完全无关，也不涉及目标检测。虽然涉及"轻量级Random Forest"和"本地受限环境执行"等轻量化/边缘部署思想，但场景是医疗影像而非遥感，方法也非目标检测。其核心价值在于"LLM知识转化为可执行特征程序+本地轻量推理"的范式，对轻量化推理思路有一定借鉴意义，但整体与本研究方向关联度低。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于医学图像分类领域。虽然"LLM生成特征程序+轻量分类器"的范式有一定新颖性，但对本课题的直接借鉴价值很低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学图像分类（二分类：瘢痕疙瘩 vs 增生性疤痕）；应用场景：跨医院临床照片分类，受限于本地数据治理要求；传感器类型：临床照片（RGB）；目标类别：瘢痕疙瘩（keloids）、增生性疤痕（hypertrophic scars）。与遥感目标检测无关。

Model architecture: ScaFE（Scar Feature Engineering）框架：1) 利用web-enabled LLM检索临床证据并合成可执行的"特征程序"（feature programs），用于测量视觉可评估的疤痕属性；2) 候选程序在受限本地环境中执行；3) 仅返回聚合验证统计量和特征级SHAP摘要用于迭代修复；4) 最终使用轻量级Random Forest对结构化特征表示进行分类。核心思想是用确定性程序替代端到端图像模型或VLM直接诊断。

Lightweight method: 轻量化体现在：1) 采用轻量级Random Forest而非深度神经网络作为最终分类器；2) 特征程序在受限本地环境执行，避免将图像发送到云端VLM；3) 通过LLM生成的特征程序替代大规模端到端模型，降低计算和数据依赖。但摘要未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等经典模型压缩方法。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及"受限本地环境执行"和"轻量级Random Forest"，暗示计算开销较低，但未给出量化数据。该工作面向医疗本地部署场景，而非星上/边缘实时推理，未涉及FPGA/NPU/TensorRT等硬件加速。

Datasets and metrics: 数据集：600张来自三家医院的临床疤痕照片，采用leave-one-site-out（留一站点）跨院评估。评价指标：site-macro balanced accuracy（站点宏平均平衡准确率）。对比基线：BiomedCLIP（最强基线）。额外实验：仅用10%开发数据时的性能评估；迭代修复后可执行程序率从66.7%提升至95.0%，91.7%的最终特征有验证证据。

Experiments: 主要结果：ScaFE在leave-one-site-out评估下达到81.0%的site-macro balanced accuracy，超过最强基线BiomedCLIP达10.0个百分点；仅用10%开发数据时仍保持72.0%的平衡准确率，领先11.8个百分点。迭代修复机制将可执行程序率从66.7%提升至95.0%。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于医学图像分类领域；2) 摘要未说明模型复杂度、推理速度、参数量等部署关键指标；3) 特征程序依赖LLM生成，其可扩展性和对遥感复杂场景的适用性未知；4) 仅针对二分类疤痕识别，未涉及目标检测任务；5) 摘要未说明Random Forest的具体规模、特征维度等实现细节。

Ideas for my research: 尽管场景不同，ScaFE的"LLM知识→确定性特征程序→轻量分类器"范式对在轨遥感目标检测轻量化有间接启发：1) 可探索用LLM/知识驱动方式生成针对遥感小目标的可解释特征提取规则，替代部分深度网络层，降低在轨计算负担；2) "本地受限环境执行+仅返回聚合统计量"的数据治理思想，与星上数据不出星、仅回传检测结果的在轨处理理念有相似之处；3) 数据高效性（10%数据仍保持性能）对遥感标注稀缺场景有参考价值。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Classifying pathological scars from clinical photographs requires distinguishing keloids from hypertrophic scars despite limited expert-labeled data and substantial acquisition variation across hospitals. End-to-end image models remain data-dependent, whereas sending photographs to a hosted vision-language model (VLM) may conflict with local data-governance requirements and yields decisions that are difficult to reproduce and audit. We introduce ScaFE (Scar Feature Engineering), which transfers clinical knowledge from a large language model (LLM) into deterministic, executable feature programs instead of asking the model to diagnose images. A web-enabled LLM retrieves clinical evidence and synthesizes programs that measure visually assessable scar attributes. Candidate programs execute in a restricted local environment, and only aggregate validation statistics and feature-level SHAP summaries are returned for iterative repair and refinement; raw images and patient-level outputs remain local. A lightweight Random Forest then operates on the resulting structured representation. On 600 photographs from three hospitals under leave-one-site-out evaluation, ScaFE achieves 81.0% site-macro balanced accuracy, exceeding the strongest baseline, BiomedCLIP, by 10.0 percentage points. With only 10% of the development data, ScaFE retains 72.0% balanced accuracy and an 11.8-point lead. Iterative refinement also raises the executable-program rate from 66.7% to 95.0%, with verified evidence for 91.7% of the final features. These results show that LLM knowledge can support data-efficient, cross-site medical image classification through local and auditable feature programs rather than direct VLM decisions.

</details>


### [98] [MIND: Multimodal Intent-Driven Network via Diffusion Transformers for Medical Image Fusion](https://arxiv.org/abs/2607.28565)
*Yunzhan Fu,Xiangyu Shen,Yifei Sun,Yuhan Chen,Jian Wu,Hongxia Xu*

Main category: cs.CV

TL;DR: 提出MIND，一种基于Diffusion Transformers (DiTs)的多模态意图驱动医学图像融合网络，利用BioMedGPT生成诊断意图文本引导融合过程，并设计多尺度潜在适配器和医学语义一致性损失来提升融合质量与下游脑肿瘤分割精度。

Research relevance: 弱相关（1-2分）。该论文聚焦于医学图像融合（medical image fusion），属于医学影像分析领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关。论文不涉及目标检测（object detection），也不涉及任何模型轻量化方法（pruning, quantization, knowledge distillation, NAS等）。虽然Diffusion Transformer架构和意图驱动融合思路在方法论上可能对多模态遥感数据融合有一定启发，但整体与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。该论文属于医学图像融合领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感场景或任何轻量化/部署技术。除非对多模态融合的意图驱动机制有特殊兴趣，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学图像融合（medical image fusion），即多模态医学影像的互补信息整合。应用场景：临床诊断支持、脑肿瘤分割辅助。传感器类型：医学影像模态（MRI、CT、PET等，摘要未明确列出具体模态）。目标类别：脑肿瘤（brain tumor）。

Model architecture: MIND (Multimodal Intent-Driven Network)，基于Diffusion Transformers (DiTs)架构。关键模块包括：(1) BioMedGPT用于从源图像生成意图驱动的融合文本（intent-driven fusion texts）；(2) Multi-scale Latent Adapter，在序列化之前显式提取源图像特征，通过严格维度对齐注入网络，以缓解DiTs中1D序列展平导致的2D空间连续性丢失；(3) medical semantic consistency loss，确保融合图像与融合文本之间的深层语义锁定，同时维持底层物理流形重建的稳定性。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（如剪枝、量化、知识蒸馏、NAS、轻量网络设计等）。相反，基于Diffusion Transformers的架构通常计算开销较大，与轻量化方向相悖。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。考虑到DiTs和BioMedGPT等大模型的引入，该方法的计算复杂度预计较高，不具备星上或边缘部署的可行性。

Datasets and metrics: 数据集：Harvard、BraTS、GFP三个医学影像数据集。评价指标：摘要未明确列出具体指标名称，仅提及"superior fusion quality"和"significantly improves downstream brain tumor segmentation accuracy"，推测使用了融合质量指标（如PSNR、SSIM、MI等）和分割精度指标（如Dice等），但摘要未具体说明。

Experiments: 在Harvard、BraTS和GFP三个数据集上进行了综合实验，结果显示MIND在融合质量上优于现有方法，并显著提升了下游脑肿瘤分割精度，同时支持灵活的交互式融合（flexible interactive fusion）。摘要未提供与具体基线方法的对比细节和消融实验结果。

Limitations: (1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于医学影像领域；(2) 摘要未报告计算开销、推理速度、参数量等部署关键指标；(3) 依赖BioMedGPT等大型预训练模型，实际工程部署成本高；(4) 未说明方法在非医学场景（如遥感多模态数据）的泛化能力；(5) 摘要未提供定量对比表格和消融实验细节。

Ideas for my research: 尽管该论文与遥感在轨目标检测方向基本无关，但仍有少量方法论层面的可借鉴点：(1) "意图驱动"（intent-driven）的融合思路——在遥感多模态数据（如光学+SAR、多光谱+高光谱）融合中，可借鉴用语义/文本引导融合过程的思想，但需替换为遥感场景的语义标签或场景描述；(2) Multi-scale Latent Adapter中"在序列化前显式提取空间特征并维度对齐注入"的设计，对处理遥感图像中空间连续性保持有参考价值，尤其对Transformer类检测器在遥感大尺寸图像上的应用；(3) 语义一致性损失的设计思路可迁移到遥感多模态融合任务中。但整体借鉴价值有限，不建议作为主要参考。

Abstract: Medical image fusion aims to integrate complementary information from diverse imaging modalities to support clinical diagnosis. Existing methods typically apply uniform fusion rules globally, lacking a deep understanding of diagnostic intents and pathological structures. To address these limitations, we propose MIND, a Multimodal Intent-Driven Network via Diffusion Transformers (DiTs) for medical image fusion. Specifically, we utilize BioMedGPT to generate intent-driven fusion texts from source images, guiding the fusion process with pathology-aware diagnostic intents. To combat the loss of 2D spatial continuity caused by 1D sequence flattening in DiTs, we design a Multi-scale Latent Adapter. This module explicitly extracts source image features before serialization, injecting them into the network via strict dimensional alignment to effectively supplement image features. To resolve the semantic shift caused by decoupling image outputs from diagnostic intents, we design a medical semantic consistency loss. This loss ensures deep semantic locking between fused images and fusion texts while maintaining the stability of the underlying physical manifold reconstruction. Comprehensive experiments on the Harvard, BraTS, and GFP datasets reveal that MIND delivers superior fusion quality, significantly improves downstream brain tumor segmentation accuracy, and enables flexible interactive fusion, holding significant promise for intent-driven intelligent clinical decision support systems.

</details>


### [99] [Finding Change in Satellite Archives from Text: How to Combine Before-and-After Images Efficiently](https://arxiv.org/abs/2607.28571)
*Simon Roy,Mark Bong,Giovanni Beltrame*

Main category: cs.CV

TL;DR: 本文系统比较了双时相遥感图像变化检索中融合模块的八种设计（attention、Mamba、learned compression三类），提出Temporal Bottleneck Fusion (TBF)压缩融合表示，并发现两阶段搜索（廉价差分模型初筛+attention重排）可在保持召回率的同时将查询成本降低10-15倍。

Research relevance: 与遥感卫星相关（bi-temporal satellite image pairs、Earth observation），但任务不是目标检测而是变化检索/变化描述匹配（change retrieval）。不涉及目标检测，也不涉及轻量化部署到星上/边缘设备，但涉及推理速度优化和模型压缩（TBF），对轻量化有一定间接借鉴意义。综合评分：3分——检测或轻量化方法有借鉴意义，但不是遥感目标检测场景。

Reading priority: 3

<details>
  <summary>Details</summary>

Task and scene: 任务类型：双时相遥感图像变化检索（bi-temporal change retrieval），即根据自然语言描述在前后时相卫星图像对中检索匹配的变化。传感器类型：卫星光学图像（satellite imagery）。目标类别：建筑物变化（new building appeared）。应用场景：Earth observation archive search，面向运营级遥感数据检索。

Model architecture: 固定图像编码器为冻结的CLIP模型（frozen CLIP model）。融合模块（fusion module）比较了八种设计，来自三个家族：(1) attention-based fusion；(2) state-space models (Mamba)；(3) learned compression，即本文提出的Temporal Bottleneck Fusion (TBF)。此外还提出两阶段搜索策略：廉价差分模型（cheap difference model）先shortlist候选，再用attention fusion重排（re-rank）。

Lightweight method: 涉及两类轻量化/加速手段：(1) 两阶段级联搜索——用廉价差分模型初筛候选对，减少需要全量attention fusion处理的候选数量，查询成本降低10-15倍；(2) Temporal Bottleneck Fusion (TBF)——对融合表示做learned compression（bottleneck），参数量减少2.3倍，延迟降低1.6倍。但未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等经典模型压缩方法。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等具体部署指标。仅提及查询成本（query cost）降低10-15倍、TBF参数量减少2.3倍、延迟降低1.6倍。该工作面向地面数据中心archive search场景（query time across many candidate pairs），而非星上在轨推理，未涉及FPGA/NPU/TensorRT等边缘部署。Mamba在L=196 patch数下因memory bandwidth限制无速度优势的结论对在轨轻量化硬件选型有参考价值。

Datasets and metrics: 数据集：LEVIR-CC和Dubai-CC两个双时相变化描述基准。评价指标：R@1/R@5（检索召回率）、BLEU-1（变化描述质量）、query cost（查询成本）、参数量、延迟。实验设置：每个设计用10个随机种子测试，差异具有统计显著性。

Experiments: 三个主要发现：(1) 两阶段搜索（cheap difference model shortlist + attention re-rank）在LEVIR-CC上匹配或超过全量fusion的召回率，查询成本降低10-15倍，在Dubai-CC上R@1/R@5相当；(2) Mamba的线性扫描在L=196 patch数下无速度优势，受memory bandwidth限制，而attention能很好地映射到并行硬件；(3) TBF压缩融合表示使参数量减少2.3倍、延迟降低1.6倍，仅损失0.007的BLEU-1，但更激进的压缩会静默丢弃变化相关细节，而聚合指标无法揭示这一点。

Limitations: 摘要未说明：具体模型结构细节、训练策略、硬件平台、FLOPs/显存/能耗等部署指标。该工作面向地面archive search而非星上在轨推理，未涉及目标检测任务。此外，TBF激进压缩会丢失变化相关细节（aggregate metrics无法揭示），说明压缩与任务保真度之间存在未充分量化的权衡。Mamba在更大patch数或不同硬件下的表现未说明。

Ideas for my research: (1) 两阶段级联搜索思想可迁移到在轨目标检测：用轻量backbone快速初筛候选区域，再用高精度模型精修，降低整体推理成本；(2) TBF的bottleneck压缩思路可借鉴到检测模型特征压缩，但需注意"聚合指标掩盖细节丢失"的教训——在轨检测中需用mAP等细粒度指标而非单一指标评估压缩效果；(3) Mamba在L=196下受memory bandwidth限制的结论提示：在轨硬件选型时，attention类算子可能比SSM类算子更适合并行硬件（如FPGA/NPU）；(4) 冻结CLIP作为固定编码器的做法提示可探索"冻结大模型+轻量任务头"的范式，减少在轨需要更新的参数量。

Abstract: Operational Earth observation increasingly calls for answering queries such as ``find the image pairs where a new building appeared.'' This means searching an archive of before-and-after (bi-temporal) satellite image pairs and ranking each pair by how well it matches a natural-language description of the change. The component that performs this match, the fusion module that combines the ``before'' and ``after'' views, must be run at query time across many candidate pairs, so its speed largely sets the cost of every search. We present a controlled comparison of how to build that module. Using one fixed image encoder (a frozen CLIP model) and one training recipe for all variants, we evaluate eight designs drawn from three families: attention, state-space models (Mamba), and learned compression (our Temporal Bottleneck Fusion, TBF). Each design is tested on two benchmarks (LEVIR-CC and Dubai-CC) with ten random seeds, so the reported differences are statistically grounded. We outline three findings: first, a training-free two-stage search (a cheap difference model that shortlists candidates, followed by attention fusion that re-ranks them) matches or exceeds full-fusion recall on LEVIR-CC while cutting query cost $10$-$15\times$, with comparable R@1/R@5 on Dubai-CC; second, the linear-time scan of Mamba, attractive on paper, gives no speed benefit at the patch counts typical of vision transformers ($L{=}196$): the scan is limited by memory bandwidth, whereas attention maps cleanly onto parallel hardware; and third, compressing the fused representation (TBF) reduces parameters by $2.3\times$ and latency by $1.6\times$ for a change-only BLEU-1 cost of $0.007$, although more aggressive compression quietly discards change-relevant detail that aggregate metrics fail to reveal.

</details>


### [100] [ROAD: Reciprocal-Objective Alignment of Discriminative Semantics for 3D Shape Generation](https://arxiv.org/abs/2607.28581)
*Xiao Luo,Mingyang Du,Xin Zhou,Tianrui Feng,Xiwu Chen,Xiaofan Li,Jiangning Zhang,Dingkang Liang*

Main category: cs.CV

TL;DR: 提出ROAD框架，通过将判别式3D基础模型的先验知识迁移到diffusion transformer中，以极少的训练数据（1.5%）和显著降低的训练成本实现高保真3D生成。

Research relevance: 弱相关（1分）。该论文聚焦于3D生成（3D generation），属于生成式AI/3D视觉领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等方向完全无关。论文不涉及object detection、remote sensing、卫星影像或任何遥感场景理解任务。虽然论文涉及"降低训练成本"和"知识迁移"等概念，但其核心是3D生成模型的训练效率优化，而非目标检测模型的轻量化或部署优化，与"遥感卫星在轨目标检测模型轻量化"课题基本无关。

Reading priority: 1分。该论文属于3D生成领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。仅当对"知识迁移降低训练成本"这一通用范式感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：高保真3D生成（High-fidelity 3D generation）。应用场景：通用3D内容生成，非遥感场景。传感器类型：不适用（非遥感传感器）。目标类别：3D物体/场景生成，非遥感目标检测。摘要未说明具体3D生成对象类别。

Model architecture: 基于diffusion transformer（DiT）的生成架构。核心组件包括：(1) Holistic Semantic Condensing（整体语义凝聚），用于强制全局语义一致性；(2) Structural Optimal Alignment（结构最优对齐），形式化为bipartite matching问题，用于对齐不同潜在空间之间的微观几何细节。判别式3D基础模型仅用于训练阶段的监督对齐，推理阶段不参与。

Lightweight method: 并非传统意义上的模型轻量化（pruning/quantization/distillation/NAS）。其核心思想是"知识迁移"——将判别式3D基础模型（discriminative 3D foundation models）中已有的语义和结构先验迁移到生成模型中，从而避免从零学习几何，降低训练成本。这类似于knowledge distillation的思想，但目标是降低训练成本而非推理成本。摘要明确说明判别式模型"only used for training-time supervision...not used at inference, incurring no additional inference cost"。

Onboard deployability: 摘要未说明。论文关注的是训练阶段的计算开销降低，而非推理阶段的部署优化。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。不涉及星上部署、边缘部署、FPGA/NPU/TensorRT等硬件加速方案。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面，论文以工业基线Step1X-3D作为对比基准，声称在仅使用1.5%训练数据的情况下达到具有竞争力的生成性能。具体定量指标（如FID、PSNR等）摘要未说明。

Experiments: 与工业基线Step1X-3D对比，ROAD在仅使用1.5%的训练数据下取得高度竞争力的生成性能，显著降低训练成本。摘要未提供更多实验细节、消融实验或对比方法。

Limitations: (1) 与遥感/卫星/目标检测方向完全无关，属于3D生成领域；(2) 摘要未提供具体性能指标、数据集规模、训练时间等量化数据；(3) 仅降低训练成本，不涉及推理阶段的计算优化；(4) 未讨论模型在边缘设备或星上平台的部署可行性；(5) 判别式3D基础模型的依赖可能带来额外的训练阶段资源需求。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但以下思路可作间接参考：(1) "知识迁移降低训练成本"的思想——在遥感目标检测中，可以利用预训练的基础模型（如SAM、DINOv2等）作为teacher，通过knowledge distillation将先验知识迁移到轻量化的检测网络中，降低训练数据需求和训练成本；(2) "训练阶段使用大模型、推理阶段不依赖大模型"的设计范式——这与knowledge distillation中teacher仅用于训练、student独立推理的思路一致，可借鉴到遥感小目标检测模型的蒸馏训练中；(3) 对齐策略（bipartite matching）的思想——在遥感旋转框检测中，可考虑类似的对齐机制来匹配不同特征空间的信息。总体而言，借鉴价值有限。

Abstract: High-fidelity 3D generation predominantly relies on scaling model capacity and data, which incurs prohibitive computational costs. This paradigm typically requires learning geometry from scratch and overlooks the rich semantic and structural priors already encapsulated in discriminative 3D foundation models. We contend that leveraging the profound understanding of the 3D world possessed by these discriminative models can significantly reduce generative cost. To this end, we propose ROAD, a framework that reduces the training cost of 3D generation by transferring these rich discriminative priors into diffusion transformers. To address the inherent semantic-structural heterogeneity between generative and discriminative latents, we introduce a reciprocal-objective alignment strategy. This method synergizes Holistic Semantic Condensing to enforce global semantic coherence and Structural Optimal Alignment, which is formulated as a bipartite matching problem to rigorously align microscopic geometric details between disparate latent spaces. The 3D foundation model is only used for training-time supervision of alignment and is not used at inference, incurring no additional inference cost. Compared with the industrial baseline Step1X-3D, the proposed ROAD achieves highly competitive generation performance with only 1.5% of the training data and significantly reduces training costs, effectively reducing the computational overhead of high-fidelity 3D generation. Code is available at https://github.com/H-EmbodVis/ROAD.

</details>


### [101] [MixFrag: Fragility-Guided Mixed-Precision Post-Training Quantization for Vision Transformers](https://arxiv.org/abs/2607.28589)
*Md. Mehrab Hossain Opi,Robiul Islam Ryad,Md. Umar Faruk*

Main category: cs.CV

TL;DR: 提出MixFrag，一个基于脆弱性引导的Vision Transformer混合精度后训练量化（PTQ）框架，通过KL散度估计组件级量化脆弱性，并将比特分配建模为多选择背包问题（MCKP），在ImageNet-1K分类和COCO检测/分割任务上取得SOTA性能。

Research relevance: 弱相关（2-3分）。论文核心是Vision Transformer的混合精度后训练量化（PTQ），属于模型压缩/轻量化范畴，与"轻量化"方向相关；但任务场景是通用图像分类（ImageNet-1K）和通用目标检测（COCO），并非遥感/卫星场景。量化方法本身对星上部署有潜在借鉴意义，但论文未涉及遥感数据、在轨部署或遥感目标检测。

Reading priority: 3分。论文的混合精度PTQ方法对轻量化方向有较强借鉴价值，且PTQ无需重训练的特性契合星上部署需求；但场景为通用视觉而非遥感，且未提供部署硬件验证数据，因此优先级中等。若课题聚焦ViT类遥感检测模型的量化压缩，可重点精读其脆弱性度量和MCKP分配方法。

<details>
  <summary>Details</summary>

Task and scene: 任务：图像分类（ImageNet-1K）、目标检测与实例分割（COCO）。场景：通用视觉任务，非遥感/卫星场景。传感器类型：自然图像（RGB）。目标类别：通用物体类别（COCO 80类），非遥感特定目标（舰船/飞机/车辆等）。

Model architecture: 针对Vision Transformer（ViT）架构的量化框架。核心组件：1) 组件级量化脆弱性估计模块——通过KL散度度量全精度与孤立量化输出分布之间的差异；2) 比特分配模块——将逐层精度分配建模为Multiple-Choice Knapsack Problem（MCKP），在目标比特预算下实现自适应逐层精度分配。适用于多种ViT架构。

Lightweight method: 混合精度后训练量化（Mixed-Precision Post-Training Quantization, PTQ）。关键创新：1) 用KL散度估计每个transformer组件（层）对量化的脆弱性（fragility），而非统一比特宽度；2) 将比特分配形式化为MCKP优化问题，在给定比特预算下寻找最优逐层精度配置。属于量化（quantization）范畴，不涉及剪枝、蒸馏或NAS。

Onboard deployability: 摘要未说明具体部署指标（参数量、FLOPs、延迟、显存、能耗等均未给出）。但作为PTQ方法，其目标就是资源受限设备（resource-constrained devices）上的部署，混合精度量化可显著降低模型存储和计算开销。对星上部署的可行性：量化方法本身适用于边缘/星上推理，但摘要未提供实际部署硬件（FPGA/NPU/TensorRT）验证或能耗数据。

Datasets and metrics: 数据集：ImageNet-1K（分类）、COCO（目标检测与实例分割）。指标：分类准确率（具体数值摘要未说明）、检测/分割AP（在MP3/MP3设置下较此前最优方法提升最高9.6 AP）。实验覆盖多种ViT架构。摘要未说明具体比特配置细节和完整指标数值。

Experiments: 1) ImageNet-1K上多种ViT架构的分类实验，验证混合精度设置下的竞争力；2) COCO目标检测和实例分割实验，在MP3/MP3设置下达到SOTA，较此前最优方法提升最高9.6 AP；3) 消融/分析实验验证脆弱性度量与学习到的比特分配之间的强相关性。摘要未给出具体对比方法名称和完整数值。

Limitations: 1) 场景局限：仅在通用视觉任务（ImageNet/COCO）上验证，未涉及遥感图像、SAR、高光谱等数据；2) 摘要未说明实际部署硬件验证（FPGA/NPU/TensorRT）和能耗/延迟数据；3) 未涉及遥感小目标、旋转框检测等遥感特有挑战；4) 未说明量化后模型在检测任务上的具体精度损失和比特配置细节；5) 脆弱性估计依赖校准集，遥感场景下校准集选择策略未讨论。

Ideas for my research: 1) 将MixFrag的脆弱性引导混合精度量化思路迁移到遥感目标检测模型（如基于ViT的检测器）上，针对遥感场景重新设计脆弱性度量；2) MCKP比特分配框架可直接复用于遥感检测模型的逐层/逐模块精度分配，在星上算力预算约束下优化精度-资源权衡；3) 可结合遥感小目标检测特性，将脆弱性度量与检测精度（如AP_small）关联，而非仅用KL散度；4) 该方法的PTQ特性（无需重训练）非常适合星上模型快速部署场景，可探索在遥感检测骨干网络上应用；5) 可扩展为对遥感检测头、FPN等结构进行组件级混合精度分配。

Abstract: Post-training quantization (PTQ) has emerged as an effective solution for deploying Vision Transformers (ViTs) on resource-constrained devices. However, existing PTQ methods typically employ uniform bit-widths across transformer components, overlooking their heterogeneous sensitivity to quantization and leading to inefficient precision allocation. In this paper, we propose {MixFrag, a fragility-guided mixed-precision PTQ framework for Vision Transformers. MixFrag first estimates component-level quantization fragility by measuring the Kullback--Leibler (KL) divergence between full-precision and isolated quantized output distributions using a small calibration set. It then formulates bit allocation as a Multiple-Choice Knapsack Problem (MCKP), enabling adaptive layer-wise precision assignment under a target bit budget. Extensive experiments on ImageNet-1K across multiple Vision Transformer architectures demonstrate that MixFrag achieves competitive classification performance under practical mixed-precision settings. Furthermore, evaluations on COCO object detection and instance segmentation show that MixFrag achieves state-of-the-art performance among existing mixed-precision PTQ methods, improving the previous best method by up to 9.6 AP under the challenging MP3/MP3 setting. Additional analyses validate the proposed fragility metric and demonstrate its strong correlation with the learned bit allocation. These results establish MixFrag as an effective framework for mixed-precision post-training quantization of Vision Transformers.

</details>


### [102] [VAD: Attributing Visual Evidence for Target Reconstruction in Multimodal On-Policy Distillation](https://arxiv.org/abs/2607.28590)
*Kangning Zhang,Yixing Li,Shuai Shao,Qingyao Li,Zhengxi Lu,Zhiyuan Yao,Jianghao Lin,Wenxiang Jiao,Yuan Lu,Weiwen Liu,Weinan Zhang,Yong Yu*

Main category: cs.CV

TL;DR: 提出 Visual Attribution Distillation (VAD)，一种反事实目标重建算法，用于多模态 on-policy distillation 中估计教师纠正中由视觉证据支持的部分，从而改进视觉语言模型在细粒度视觉基准上的蒸馏效果。

Research relevance: 弱相关（2分）。论文属于多模态视觉语言模型（VLM）的知识蒸馏方法，核心是视觉证据归因与反事实目标重建，属于泛视觉/泛机器学习方法。虽然涉及"distillation"和"visual"概念，但既不针对遥感/卫星场景，也不涉及目标检测任务，更未涉及模型轻量化/在轨部署。其蒸馏思想（区分视觉证据支持的纠正与语言先验）对遥感知识蒸馏可能有方法论借鉴，但整体与本研究方向弱相关。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，属于多模态 VLM 蒸馏方法。其反事实归因思想有一定方法论启发，但模型规模（4B/9B）、任务类型（细粒度视觉理解而非目标检测）和场景（非遥感）均与本研究方向不匹配，阅读优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态视觉语言模型（VLM）的 on-policy distillation 训练。应用场景：细粒度视觉理解（fine-grained visual benchmarks），非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（细粒度视觉分类/理解任务）。

Model architecture: 采用 privileged-view teacher 与 student 的蒸馏架构。核心模块是 VAD 反事实目标重建算法：在 student 生成的每个前缀处，用同一固定 teacher 分别在有/无相关视觉证据的情况下评估，计算 centered log-probabilities 的变化得到 ut（视觉证据方向的 signed proxy），将原始纠正投影到该 proxy 上得到 intervention-aligned component 和 proxy-unexplained residual，再从前者重建 student-anchored target。训练时以重建目标为主监督信号，privileged teacher 仅作弱正则化。模型规模为 4B 和 9B。

Lightweight method: 涉及知识蒸馏（knowledge distillation）概念，但并非用于模型压缩/轻量化目的，而是用于提升 student 的视觉知识迁移质量。未涉及剪枝（pruning）、量化（quantization）、NAS、实时推理、边缘部署等轻量化手段。蒸馏在这里是训练策略而非部署加速手段。

Onboard deployability: 摘要未说明。模型规模为 4B 和 9B 参数，属于大模型范畴，与星上/边缘部署的轻量化需求（通常需远小于 1B 参数）差距很大。未报告参数量、FLOPs、延迟、显存、能耗等部署相关信息。

Datasets and metrics: 使用六个细粒度视觉基准（six fine-grained visual benchmarks），具体数据集名称摘要未说明。评估在 4B 和 9B 两种模型规模下进行。对比方法包括直接 privileged-view distillation 和 visual-advantage weighting。还进行了 token-level 分析和 controlled-target 分析。

Experiments: VAD 在六个细粒度视觉基准上优于直接 privileged-view distillation 和 visual-advantage weighting。Token-level 和 controlled-target 分析表明：proxy-aligned component 富含任务相关的视觉纠正，能产生更强的 target shifts，尤其在证据反驳错误答案时效果更显著。

Limitations: 1. 摘要未说明具体数据集、任务类型和评估指标细节；2. 模型规模为 4B/9B，属于大模型，与轻量化/在轨部署场景严重不匹配；3. 未涉及遥感、卫星、目标检测等本研究方向核心要素；4. 未报告计算开销、推理速度等部署指标；5. 反事实评估需要多次前向传播，训练成本可能较高（摘要未说明）。

Ideas for my research: 1. 反事实归因思想可借鉴：在遥感目标检测的知识蒸馏中，可以区分"教师纠正中哪些由视觉特征（如舰船轮廓、纹理）支持，哪些由上下文先验支持"，从而更精准地蒸馏视觉相关的知识；2. "student-anchored target" 概念可迁移到遥感检测蒸馏中，避免教师输出与 student 能力不匹配导致的蒸馏噪声；3. 将"证据存在/移除"的反事实评估思路用于遥感小目标检测，可分析哪些视觉线索（如高光谱波段、多尺度特征）真正驱动检测决策；4. 但需注意：该方法面向生成式 VLM 的 token 级蒸馏，与检测模型的 logit/feature 蒸馏机制差异较大，直接迁移需重新设计。

Abstract: Multimodal on-policy distillation (OPD) transfers fine-grained visual knowledge by supervising student-generated trajectories with a privileged-view teacher. Yet its next-token corrections are source-mixed, combining visual signals with linguistic priors and teacher-specific effects. The key challenge is to estimate which corrections are supported by visual evidence, not merely where or how strongly to distill. We introduce Visual Attribution Distillation (VAD), a counterfactual target-reconstruction algorithm that estimates the visually attributable part of a teacher correction. At each student-generated prefix, VAD evaluates the same fixed teacher with the relevant evidence present and removed. The corresponding change in centered log-probabilities defines ut, a signed proxy for the visual evidence direction that estimates how revealing the evidence supports or refutes candidate tokens. VAD projects the original correction onto this proxy to obtain an intervention-aligned component and a proxy-unexplained residual, then reconstructs a student-anchored target from the former. During training, this reconstructed target supplies the primary supervision signal, while the privileged teacher contributes a weak regularizer. Across six fine-grained visual benchmarks at 4B and 9B scales, VAD outperforms direct privileged-view distillation and visual-advantage weighting. Token- level and controlled-target analyses show that the proxy-aligned component is enriched in task-relevant visual corrections and yields stronger target shifts, especially when evidence refutes a mistaken answer. These results support counterfactual target reconstruction as an effective alternative to source-mixed supervision.

</details>


### [103] [Beacon: Knowing When and How to Perform Agentic Visual Reasoning](https://arxiv.org/abs/2607.28595)
*Qixun Wang,Yang Shi,Letian Cheng,Zhuoran Zhang,Yan He,Yuqi Tang,Qi Zhang,Xinlei Yu,Ruizhe Chen,Tianrun Xu,Yuanxing Zhang,Pengfei Wan,Haotian Wang,Xianghua Ying*

Main category: cs.CV

TL;DR: 提出Beacon智能体视觉推理模型，通过Necessity-Aware Adaptive Reward和Hint-Guided Capability Expansion机制，提升多模态大语言模型在工具调用上的模式自适应性和工具效果。

Research relevance: 弱相关（2分）。论文属于泛视觉/泛机器学习方法，聚焦多模态大语言模型（MLLM）的agentic visual reasoning与工具调用策略，与遥感、卫星、在轨目标检测、模型轻量化均无直接关联。虽然涉及"避免不必要计算开销"（avoiding unnecessary computational overhead）这一效率思想，但并非针对遥感场景或目标检测任务，也未涉及剪枝、量化、蒸馏等轻量化技术。

Reading priority: 1分。论文聚焦多模态大语言模型的agentic visual reasoning与工具调用策略，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然"自适应计算开销"思想有一定启发，但整体借鉴价值很低，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：agentic visual reasoning（智能体视觉推理），即多模态大语言模型在复杂视觉任务中判断是否需要调用外部工具。应用场景：通用视觉推理benchmark，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明。

Model architecture: Beacon模型，核心为两个机制：(1) Necessity-Aware Adaptive Reward——基于任务必要性鼓励模型自适应地决定是否调用工具；(2) Hint-Guided Capability Expansion——在强化学习阶段增强模型在最具挑战性问题上的工具使用能力。底层为多模态大语言模型（MLLM），具体骨干网络摘要未说明。

Lightweight method: 摘要未涉及传统模型压缩/加速方法（剪枝、量化、蒸馏、NAS等）。但论文的核心思想之一是"Mode Adaptiveness"——让模型识别何时真正需要工具，避免不必要的计算开销（avoiding unnecessary computational overhead），这属于推理阶段的动态计算/条件计算思想，与轻量化中的"动态推理"或"early exit"思路有概念上的相似性，但并非针对遥感在轨部署。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。论文关注的是工具调用的计算开销优化，而非模型本身的轻量化或边缘/星上部署。MLLM本身计算量巨大，与在轨边缘部署的现实约束（功耗、算力、存储受限）存在显著差距。

Datasets and metrics: 摘要未具体说明数据集名称，仅提及"diverse benchmarks"。评价指标包括：整体性能（overall performance）、Mode Adaptiveness（模式自适应性）、Tool Effect（工具效果）。具体benchmark名称和指标数值摘要未说明。

Experiments: 摘要提到在多个benchmark上进行了广泛实验，验证了Beacon在整体性能、Mode Adaptiveness和Tool Effect上的显著提升。实验揭示现有agentic visual reasoning模型存在Mode Adaptiveness有限、工具使用在困难样本上的收益被简单样本上的损害抵消等问题。具体对比方法和消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星/在轨目标检测完全无关，属于通用视觉推理方向；(2) 未涉及任何模型压缩或轻量化技术；(3) MLLM架构本身计算开销巨大，与在轨部署的功耗和算力约束严重不匹配；(4) 摘要未提供具体数据集、指标数值和部署相关参数；(5) 工具调用机制依赖外部工具环境，在星上受限环境中难以直接应用。

Ideas for my research: (1) "Mode Adaptiveness"思想可借鉴到在轨目标检测的"动态推理"设计中——即模型能判断当前输入是否需要高精度推理（如复杂场景）或可走快速通道（如简单背景），从而在保证精度的同时降低平均计算开销，这与轻量化中的conditional computation/early exit思路一致；(2) "避免不必要计算开销"的理念可启发设计自适应推理策略，在星上算力受限时优先处理关键目标；(3) 但整体而言，该论文与遥感在轨目标检测轻量化课题的直接借鉴价值有限，更多是方法论层面的启发。

Abstract: The fundamental goal of agentic visual reasoning is to improve the success rate of multimodal large language models (MLLMs) on complex tasks, rather than merely equipping them with a sophisticated yet inefficient reasoning paradigm. In this work, we rethink agentic visual reasoning through two key dimensions of tool use: Mode Adaptiveness (MA) and Tool Effect (TE). Mode Adaptiveness characterizes whether an MLLM can recognize when tools are truly necessary and invoke them accordingly, thereby avoiding unnecessary computational overhead while improving performance on challenging problems that require tool assistance. Tool Effect characterizes the actual impact of tool use: tools should extend the model's capabilities on problems unsolvable through text-only reasoning, while avoiding additional errors on problems that the model can already solve without tools. We conduct a comprehensive analysis to quantify these two properties and empirically reveal that existing agentic visual reasoning models exhibit limited Mode Adaptiveness, while the gains produced by tool use on hard examples are largely offset by the harm introduced on easy examples that the models can already solve. Motivated by these observations, we propose Beacon, a novel agentic visual reasoning model that achieves stronger overall performance, improved Mode Adaptiveness, and genuine tool-induced performance gains. At the core of Beacon are the Necessity-Aware Adaptive Reward and the Hint-Guided Capability Expansion mechanism in the reinforcement learning stage, which respectively encourage adaptive tool invocation based on task necessity and strengthen the model's tool-use capability on the most challenging problems. Extensive experiments across diverse benchmarks demonstrate the strong overall performance of Beacon and its substantial improvements in both Mode Adaptiveness and Tool Effect.

</details>


### [104] [Chimera: Designing and Chinchilla-Scaling Hybrid Visual Diffusion Transformers](https://arxiv.org/abs/2607.28611)
*Chongjian Ge,Hanwen Jiang,Tianyu Wang,Jiuxiang Gu,Yiran Xu,Ziwen Chen,Shaoteng Liu,Jing Shi,Yicong Hong,Zefan Cai,Hailin Jin,Hao Tan*

Main category: cs.CV

TL;DR: Chimera是一个混合视觉扩散骨干网络，通过Kimi Delta Attention (KDA)、多头潜在注意力(MLA)、短卷积和稀疏MoE的组合，配合HeteroP模块级缩放方案，实现长上下文视觉生成的高效计算，训练了11B参数(2B激活)的模型。

Research relevance: 弱相关（2分）。该论文属于泛视觉生成/扩散模型领域，与遥感、卫星、在轨部署、目标检测均无直接关联。它关注的是视觉内容生成（图像/视频生成）而非遥感目标检测，也不涉及遥感场景理解。虽然涉及计算效率优化（O(N)注意力、MoE稀疏激活），但这是面向生成任务的架构效率优化，而非面向边缘/星上部署的模型轻量化。其轻量化思路（稀疏激活、混合注意力）对遥感检测模型的部署优化仅有间接借鉴意义。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向相关性较弱，属于视觉生成领域。虽然其线性注意力、稀疏MoE等计算效率优化思路有一定架构设计参考价值，但整体研究场景、任务类型和部署目标与本研究方向差异较大，建议作为泛读材料了解前沿效率优化趋势即可，无需深入精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视觉内容生成（图像/视频生成），属于diffusion-based generative model。应用场景：高分辨率图像生成、长视频生成、多模态上下文生成。传感器类型：不涉及遥感传感器（非SAR、非高光谱、非多光谱）。目标类别：不涉及目标检测，无遥感目标类别。

Model architecture: Chimera混合视觉扩散骨干网络，包含：1) Kimi Delta Attention (KDA)用于长上下文状态跟踪，复杂度O(N)；2) 交错的多头潜在注意力(Multi-head Latent Attention, MLA)用于直接全局交互；3) 模态感知的短卷积(short convolutions)用于局部时空上下文；4) 稀疏Mixture-of-Experts (MoE)层扩展容量同时控制激活计算量。整体采用raster-ordered流处理文本、图像和视频token，无需位置嵌入。

Lightweight method: 轻量化主要体现在计算效率优化而非模型压缩：1) KDA注意力将复杂度从O(N²)降至O(N)；2) 稀疏MoE控制激活参数（11B总参数仅2B激活）；3) HeteroP模块级超参数迁移方案，按张量的functional fan-in和模型深度跨宽度和深度迁移超参数；4) 基于Chinchilla风格的计算最优定律拟合，指导模型规模与训练数据配比。但未涉及剪枝(pruning)、量化(quantization)、知识蒸馏(knowledge distillation)等经典模型压缩方法。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、FPGA/NPU/TensorRT等硬件加速方案。虽然模型有2B激活参数，但11B总参数规模对于星上部署而言仍然过大。未报告参数量、FLOPs、延迟、显存、能耗等具体部署指标。该工作面向云端大规模训练与推理场景，而非资源受限的边缘/星上环境。

Datasets and metrics: 摘要未明确说明具体数据集名称。评价指标包括：1) pretraining diffusion loss（预训练扩散损失）；2) FID（Fréchet Inception Distance）用于视频生成质量评估（30秒视频最后5秒仅6.5% FID退化）；3) 计算效率对比（dense backbone比full-attention Wan-2.1 2B基线计算效率高1.7x，完整系统达7.3x）。

Experiments: 主要实验发现：1) 计算效率方面，dense backbone比matched full-attention Wan-2.1 2B基线计算效率高1.7x，完整系统达7.3x；2) 零样本外推能力，无需长度特定微调即可从5秒训练片段外推到30秒视频，最后5秒仅6.5% FID退化；3) 计算最优定律显示，图像预训练在激活模型大小和训练token数之间几乎均匀分配计算，而视频预训练在高预算下略微偏向模型大小。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于视觉生成领域；2) 模型规模（11B参数）不适合星上部署；3) 摘要未说明推理延迟、能耗、硬件适配等部署关键指标；4) 未涉及目标检测任务，无法直接评估检测精度；5) 摘要未说明训练数据来源和具体数据集。

Ideas for my research: 1) KDA的O(N)复杂度注意力设计思路可借鉴到遥感大图目标检测中，遥感图像分辨率高、token数量大，线性复杂度注意力可降低计算开销；2) 稀疏MoE控制激活参数的思想可用于遥感检测模型的动态路由，在推理时只激活必要专家；3) HeteroP模块级超参数迁移方案对设计轻量化遥感检测网络的分层缩放有参考价值；4) 混合注意力（全局+局部）的架构模式与遥感图像中"全局上下文+局部细节"的检测需求有相似性，可探索在检测backbone中融合不同感受野的注意力；5) 但需注意，这些借鉴属于架构设计层面的间接启发，而非直接的轻量化方法。

Abstract: Visual generation increasingly requires high-resolution images, long videos, and multimodal context, making the quadratic cost of full attention prohibitive. We introduce Chimera, a hybrid visual diffusion backbone with a principled scaling recipe. Chimera processes text, image, and video tokens in one raster-ordered stream without positional embeddings. It combines Kimi Delta Attention (KDA) for long-context state tracking with O(N) complexity, interleaved Multi-head Latent Attention (MLA) for direct global interaction, and modality-aware short convolutions for local spatiotemporal context. Sparse Mixture-of-Experts (MoE) layers expand capacity while controlling activated compute. To scale this heterogeneous architecture, we introduce HeteroP, a module-wise scheme that transfers hyperparameters across width and depth according to each tensor's functional fan-in and model depth. HeteroP yields a consistently tuned family used to fit Chinchilla-style compute-optimal laws for activated model size, training-token count, and image-video data ratio. Guided by these laws, we train an 11B-parameter Chimera with 2B activated parameters. Experiments show three results. First, measured by pretraining diffusion loss, the dense backbone is 1.7x as compute-efficient as a matched full-attention Wan-2.1 2B baseline, while the complete system reaches 7.3x. Second, without length-specific fine-tuning, Chimera extrapolates zero-shot from 5-second training clips to 30-second videos, with only 6.5% FID degradation in the last five seconds. Third, the fitted laws show that compute-optimal image pretraining divides compute nearly evenly between activated model size and training-token count, whereas video pretraining modestly favors model size at higher budgets. These results establish a foundation for designing and scaling efficient long-context diffusion architectures.

</details>


### [105] [PhiZero: A World Model Built Around Physical Language](https://arxiv.org/abs/2607.28624)
*Shuyao Shang,Yuqi Wang,Ruopeng Gao,Xu Chen,Tieniu Tan,Lue Fan,Zhaoxiang Zhang*

Main category: cs.CV

TL;DR: 提出PhiZero，一种基于"物理语言"（physical language）的物理世界模型，采用"先推理后渲染"（reason-then-render）范式，将世界状态转移编码为紧凑离散表示，再渲染为视频，用于物理一致的世界演化建模与交互式模拟。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习领域的物理世界模型（physical world model）与视频生成方向，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。它既不是遥感场景，也不涉及目标检测，更不涉及模型压缩或边缘部署。仅从"世界状态建模"的抽象层面可能对遥感时序场景理解有间接启发，但整体相关性很弱。

Reading priority: 1分。该论文属于物理世界模型/视频生成方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不涉及遥感场景、目标检测或模型压缩部署，阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：物理世界建模（physical world modeling）、视频生成（video generation）、动作条件模拟（action-conditioned simulation）、零样本运动迁移（zero-shot motion transfer）。应用场景为通用视频/物理场景，非遥感场景。传感器类型：摘要未说明（推测为普通RGB视频）。目标类别：无特定目标检测类别。

Model architecture: PhiZero采用"reason-then-render"（先推理后渲染）两阶段架构：第一阶段将世界状态转移编码为紧凑的离散表示——"physical language"（物理语言），通过自监督从in-the-wild视频中学习；第二阶段将推理出的物理语言序列渲染为视频。该架构将显式的世界动力学推理从高维像素预测中解耦出来。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等模型压缩或加速方法。虽然"物理语言"作为紧凑离散表示在概念上具有某种"压缩"意味，但这是对世界状态表征的抽象压缩，而非面向部署的模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）等部署相关信息，也未讨论星上（onboard）或边缘（edge）部署可行性。该工作面向通用视频生成与理解，与在轨实时推理需求无关。

Datasets and metrics: 摘要未说明具体数据集名称。仅提及"in-the-wild videos"用于自监督学习，以及"generation and understanding benchmarks"用于评估。具体数据集、评价指标（如FVD、PSNR、SSIM等）均未在摘要中给出。

Experiments: 摘要提及在生成（generation）和理解（understanding）基准上验证了PhiZero建模物理一致世界演化的能力，并展示了其在真实交互式世界建模、细粒度动作条件模拟和零样本运动迁移方面的潜力。但未给出具体数值结果、对比方法或消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于视频生成与物理世界模型领域；2. 摘要未提供任何部署相关指标（参数量、FLOPs、延迟等）；3. 未说明物理语言的离散化粒度、词表大小、自监督训练细节；4. 未讨论在真实物理约束（如遥感成像物理过程）下的泛化能力；5. 摘要未说明推理速度与实时性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但有两个概念层面可借鉴的点：1) "reason-then-render"范式启发——在遥感时序目标检测中，可考虑将场景状态转移（如舰船运动、云层遮挡变化）编码为紧凑的离散状态表示，再进行检测推理，减少对高维时序特征的依赖；2) "物理语言"作为紧凑离散表征的思想，与轻量化目标检测中"用紧凑中间表示替代高维特征图"的思路有相通之处，可启发设计更高效的特征压缩模块。但整体而言，该论文对本课题的直接借鉴价值有限。

Abstract: We introduce PhiZero, a physical world model built around physical language, a compact discrete representation of world-state transitions. Existing physical world models typically predict future videos directly in pixel space, leaving the underlying world dynamics implicit within high-dimensional visual predictors. Motivated by humans' ability to abstract predictive structure from visual experience and organize it in natural language for explicit reasoning, we learn physical language from in-the-wild videos through self-supervision and use it to explicitly reason about how the physical world evolves. Accordingly, PhiZero adopts a reason-then-render paradigm: it first infers future world evolution as a physical-language sequence and then renders the inferred transitions into videos. Extensive experiments across generation and understanding benchmarks validate the ability of PhiZero to model physically coherent world evolution. We further show its potential for realistic and interactive world modeling, fine-grained action-conditioned simulation, and zero-shot motion transfer.

</details>


### [106] [ACE-Data-0: Human-Centric Ambient Capture as Embodied Data Engine](https://arxiv.org/abs/2607.28625)
*Yukang Cao,Haozhe Xie,Beichen Wen,Runmao Yao,Yinghao Liu,Yue Huang,Zhichao Liao,Yunxiang Wang,Haiheng Liu,Xingshun Tian,Dawei Su,Long Zhuo,Dacheng Tao,Xiaogang Wang,Liang Pan,Ziwei Liu*

Main category: cs.CV

TL;DR: 提出ACE数据引擎和ACE-Data-0数据集，用于构建多模态同步的具身智能（embodied AI）人类行为数据，覆盖第一人称感知、全身运动、灵巧操作、物体状态、声音和触觉等多模态信号。

Research relevance: 弱相关（2分）。该论文属于具身智能（embodied AI）和人类行为数据采集方向，与遥感卫星、Earth observation、aerial image、SAR、高光谱/多光谱等遥感场景完全无关；不涉及目标检测、小目标检测、旋转框检测等遥感目标检测任务；也不涉及模型轻量化、剪枝、量化、知识蒸馏、NAS、实时推理或星上部署。仅作为泛机器学习/多模态数据构建方法，对遥感领域借鉴意义有限。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于具身智能数据构建工作，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：具身智能多模态数据采集与基准构建；应用场景：真实家庭环境（home environments）中的人类行为理解；传感器类型：第一人称（egocentric）和多视角（multi-view exocentric）视频、全身与手部运动捕捉、物体几何与6-DoF轨迹、音频、触觉信号；目标类别：人类行为/交互（200个任务类别，75,000个交互片段）。与遥感目标检测无关。

Model architecture: 摘要未说明具体模型架构。论文主要贡献是数据采集引擎ACE（Ambient Capture Engine），包含table-scale（桌面尺度，解析手-物操作）和room-scale（房间尺度，捕捉全身运动与交互）两种配置，以及配套的层级化基准（hierarchical benchmark，从signals到scene components再到interactions）。

Lightweight method: 摘要未涉及任何模型轻量化方法。不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作聚焦数据采集而非模型部署。

Datasets and metrics: 数据集：ACE-Data-0，包含150小时、1700万视频帧、200个任务类别、50名参与者、2个环境、75,000个交互片段。覆盖原子操作（atomic manipulation）、长时序家庭活动链（long-horizon chains of household activities）和人-场景交互（human-scene interaction）。评价指标：摘要未说明具体指标，仅提出层级化基准（从signals到scene components再到interactions），并评估了SOTA方法在接触（contact）、遮挡（occlusion）、自我运动（egomotion）和长时间跨度（long temporal horizons）下的表现差距。

Experiments: 摘要仅提及对state-of-the-art方法在接触、遮挡、自我运动和长时间跨度场景下进行了评估，暴露了显著性能差距。未给出具体数值结果、对比方法列表或消融实验细节。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，属于具身智能领域；2. 摘要未涉及任何模型压缩或轻量化技术；3. 数据采集环境为家庭场景，与遥感图像（卫星、航空、SAR、高光谱）在成像机理、目标尺度、背景复杂度上差异巨大；4. 未提供模型部署相关指标。

Ideas for my research: 1. 该论文的多模态同步数据采集与对齐思路（spatially calibrated, temporally synchronized）可类比借鉴到遥感多传感器（光学+SAR+高光谱）数据融合标注的构建；2. 层级化基准设计（signals→scene components→interactions）的思路可迁移到遥感场景理解中"像素级→目标级→场景级"的递进式评测框架；3. 其"goal-level而非step-by-step指令"的数据标注策略，对遥感大规模弱监督标注有一定启发。但整体借鉴价值有限。

Abstract: Embodied intelligence faces a fundamental data bottleneck. Models must capture how first-person perception, whole-body motion, dexterous manipulation, object state, sound, and touch evolve together as humans pursue goals over time. Existing datasets fragment this experience across viewpoints, modalities, or spatial scales, leaving the full perception-action loop only partially observed. We introduce the Ambient Capture Engine (ACE), a human-centric data engine that transforms real home environments into spatially calibrated, temporally synchronized recording studios. ACE operates at two complementary scales: a table-scale configuration resolves hand-object manipulation, while a room-scale configuration captures whole-body motion, locomotion, and interactions across a furnished home. ACE records egocentric and multi-view exocentric video, full-body and articulated hand motion, object geometry and 6-DoF trajectories, audio, and tactile signals as a unified multisensory stream. Using ACE, we build ACE-Data-0, comprising 150 hours and 17M video frames across 200 task categories, performed by 50 participants in 2 environments, for a total of 75,000 interaction episodes. The dataset spans atomic manipulation, long-horizon chains of household activities, and human-scene interaction, while preserving natural behavioral variation through goal-level rather than step-by-step instructions. We further introduce a hierarchical benchmark that progresses from signals to scene components and then to interactions. Evaluations of state-of-the-art methods expose substantial gaps under contact, occlusion, egomotion, and long temporal horizons. ACE-Data-0 provides synchronized human demonstrations with aligned perceptual, kinematic, and contact supervision, offering a scalable foundation for imitation learning, world models, vision-language-action systems, and embodied AI.

</details>


### [107] [ReToken: One Token to Improve Vision-Language Models for Visual Retrieval](https://arxiv.org/abs/2607.28627)
*Yao Xiao,Reuben Tan,Zhen Zhu,Yuqun Wu,Jianfeng Gao,Derek Hoiem*

Main category: cs.CV

TL;DR: ReToken 提出一个可学习的检索嵌入（retrieval token），从预填充的视觉 KV cache 中稀疏选择与查询相关的视觉 token，以解决长视觉上下文中 vision-language model 性能下降和 GPU 内存受限的问题。

Research relevance: 弱相关（2分）。该论文属于泛视觉-语言模型（vision-language model）领域，聚焦长视觉上下文的 token 稀疏化与 KV cache 检索，并非遥感/卫星场景，也不涉及目标检测或遥感图像理解。但其"轻量设计 + 稀疏 token 选择 + 单卡可训练/推理"的思路对在轨部署中的计算资源受限问题有一定借鉴意义。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，核心贡献在通用 vision-language 的长上下文 token 稀疏化，不涉及遥感场景、目标检测或星上部署。仅其"轻量设计 + 稀疏 token 检索"的宏观思路有一定借鉴价值，但优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：vision-language model 的长视觉上下文理解（long visual context understanding），具体为从大量视觉 token 中检索与 query 相关的稀疏子集。应用场景：图像问答（image-QA）和长视频理解。传感器类型：摘要未说明（非遥感传感器）。目标类别：摘要未说明（非遥感目标）。

Model architecture: ReToken 是一个单一的可学习嵌入（single learnable embedding），作为显式的检索目标（explicit retrieval target），从预填充的视觉 KV cache 中选择与 query 相关的稀疏视觉 token 集合。基于 Qwen3VL-8B 和 InternVL3.5 等 vision-language backbone 进行集成。

Lightweight method: 核心轻量化手段是 token 稀疏化（token sparsification）：通过可学习检索嵌入从视觉 KV cache 中筛选出 query 相关的少量视觉 token，避免对所有 token 进行全量处理，从而降低计算和内存开销。训练数据量小（仅 small image-QA dataset），训练和长视频推理均可运行在单张 H100 上，体现了轻量设计（lightweight design）。

Onboard deployability: 摘要提到"lightweight design"使得训练和长视频推理都能在单张 H100 上完成，但未给出具体参数量、FLOPs、延迟、显存占用、能耗等量化指标。H100 属于数据中心级 GPU，与星上/边缘部署的硬件约束（FPGA/NPU/低功耗）差距较大，因此直接星上部署可行性有限。摘要未说明。

Datasets and metrics: 数据集：Visual Haystacks（图像基准）、LVBench（长视频基准）。评价指标：在 Visual Haystacks 上 Qwen3VL-8B 提升 13.4 点、InternVL3.5 提升 12.4 点（>20% 相对提升）；在 LVBench 上 Qwen3VL-8B 零样本迁移获得 8.0 点提升。训练数据为 small image-QA dataset。

Experiments: 主要实验：在 Visual Haystacks 上验证 ReToken 对 Qwen3VL-8B 和 InternVL3.5 的改进（分别 +13.4 和 +12.4 点）；在 LVBench 上验证零样本迁移到长视频场景（Qwen3VL-8B +8.0 点）。摘要未提供消融实验、对比方法细节或与 baseline 的完整对比表。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于通用 vision-language 领域。2. 摘要未提供参数量、FLOPs、延迟、显存等部署量化指标。3. 仅在 H100 上验证，未涉及边缘/星上硬件。4. 未说明 token 稀疏化对检测类任务（如小目标检测）的适用性，因为检索目标面向 QA 而非空间定位。5. 摘要未说明稀疏 token 选择是否会丢失小目标/弱信号信息，这对遥感小目标检测是潜在风险。

Ideas for my research: 1. 借鉴"可学习检索嵌入 + KV cache 稀疏选择"的思路，可探索在遥感大场景图像中，用可学习 token 从全局特征中稀疏检索目标相关区域，减少冗余背景计算，服务于在轨实时推理。2. 其"轻量设计 + 单卡可训练"的理念可迁移到遥感模型的训练/推理效率优化。3. 稀疏 token 选择机制可类比于遥感图像中的 region proposal / 显著性区域筛选，用于降低大场景遥感图像目标检测的计算量。4. 但需注意：遥感目标检测更关注空间定位精度和小目标召回，token 稀疏化可能损失小目标信息，需结合多尺度或注意力增强来弥补。

Abstract: Long visual context poses a challenge for vision-language models: performance degrades as the number of distractors grows, and processing all tokens at once is computationally infeasible under GPU memory constraints. We present ReToken, a single learnable embedding trained as an explicit retrieval target that selects a sparse set of query-relevant visual tokens from a pre-filled visual KV cache. Trained on only a small image-QA dataset, ReToken yields consistent gains across image and video benchmarks: on Visual Haystacks it improves Qwen3VL-8B by 13.4 points and InternVL3.5 by 12.4 points (>20% relative), and on LVBench it transfers zero-shot to long video for an 8.0-point gain with Qwen3VL-8B. Thanks to its lightweight design, both training and long-video inference fit on a single H100. Code is available at: https://github.com/avaxiao/ReToken

</details>


<div id='stat.ML'></div>

# stat.ML [[Back]](#toc)

### [108] [More Data, Worse Decisions? Preference Reversals in Neural Networks under Gram Incompatibility](https://arxiv.org/abs/2607.27255)
*Yanli Yan,Yuanzheng Li,Yong Zhao,Hongbo Guo,Shoudong Han*

Main category: stat.ML

TL;DR: 本文研究神经网络在合并多源数据（pooled data）重新拟合后，是否保持各来源支持的偏好排序（composition axiom），提出基于Case-Based Decision Theory的几何分析与审计框架。

Research relevance: 弱相关（2分）。论文属于泛机器学习/决策理论方向，讨论多源数据合并后模型偏好一致性的可靠性问题，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也不涉及目标检测或模型压缩加速方法。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于决策理论/机器学习可靠性研究，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多源数据合并（pooling）后神经网络偏好一致性的可靠性分析。应用场景：负载竞价代理（load-based bidding proxy）、医疗决策代理、金融决策代理。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 固定表示（fixed-representation）神经网络，输出头采用普通最小二乘（OLS, ordinary least squares）。核心分析基于inverse-Gram几何结构，引入scale-invariant Gram mismatch度量与geometry-oriented正则化。

Lightweight method: 不涉及。本文未提出任何剪枝、量化、知识蒸馏、NAS或轻量网络设计方法，也未讨论实时推理、边缘/星上部署等压缩加速手段。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且研究目标并非部署优化。

Datasets and metrics: 实验基于负载竞价代理（load-based bidding proxy）、医疗决策代理、金融决策代理三类决策代理数据。评价指标涉及描述性准确率（descriptive accuracy）、一致性（consistency）、几何（geometry）与效用损失（utility loss）等。

Experiments: 实验揭示稳定与易反转的pooling regime：load audit在代理效用下识别出可测量的非零类source-consensus-relative有害决策；跨域审计显示相近的Gram mismatch可能对应显著不同的preservation rates。Geometry-oriented目标占据不同的accuracy-consistency-geometry-harm操作点。

Limitations: 摘要未说明。但明显局限包括：与遥感/卫星/目标检测场景完全无关；未涉及任何轻量化或部署优化内容；实验场景为决策代理而非视觉感知任务；方法基于OLS输出头的固定表示网络，适用范围有限。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是：在遥感多源数据（如多时相、多传感器SAR/光学影像）合并训练时，模型偏好一致性（compositional reliability）是一个值得关注的可靠性问题——即合并不同来源遥感数据重新训练后，模型对目标的检测偏好是否保持一致。但本文方法（Gram几何分析、偏好审计）与目标检测轻量化课题的直接关联很弱，不建议作为核心参考。

Abstract: Neural networks increasingly combine data across populations, time periods, and operating conditions to improve generalization. This raises a reliability question: whether a model refitted on pooled data preserves an action ordering supported by both sources. Case-Based Decision Theory (CBDT) formalizes this requirement through its composition axiom, which requires source-supported preferences to survive their union. We study when this property holds for fixed-representation neural networks with ordinary least squares (OLS) output heads. First, we show that pooled refitting recomputes the inverse-Gram geometry used to weight source evidence, which can reverse shared preferences, and derive exact and approximate preservation conditions. Next, we introduce a scale-invariant Gram mismatch measure for prioritizing candidate pools and geometry-oriented regularization for shaping source geometry during training. Finally, we develop a three-stage audit that traces strict pairwise reversals through decision changes to task-defined utility loss. Experiments spanning a load-based bidding proxy and medical and financial decision proxies reveal stable and reversal-prone pooling regimes: the load audit identifies a measurable nonzero class of source-consensus-relative harmful decisions under the proxy utility, while cross-domain audits show that comparable mismatch can correspond to sharply different preservation rates. Geometry-oriented objectives occupy distinct descriptive accuracy-consistency-geometry-harm operating points. Together, the framework makes compositional reliability measurable and operational through screening, analytic certification, geometry-oriented training, and decision-consequence auditing.

</details>


### [109] [Expected Survival-Time Bounds for Robust Optimization Over Time under Isotropic Gaussian Dynamics](https://arxiv.org/abs/2607.27280)
*Pavel Novoa-Hernández*

Main category: stat.ML

TL;DR: 本文研究进化动态优化中"跨时间鲁棒优化"(ROOT)范式下解的期望存活时间，在各项同性高斯环境动态下推导出下界与多步上界，并给出理论分析与蒙特卡洛验证。

Research relevance: 与本研究方向基本无关（1分）。论文属于进化计算/动态优化理论领域，研究对象是"解在多个连续环境中的存活时间"，完全不涉及遥感、卫星、目标检测或模型轻量化。虽然文中出现"deployment"（部署）一词，但指的是优化解在动态环境中的部署，与星上/边缘部署无关。

Reading priority: 1分。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于进化动态优化理论，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：进化动态优化中的鲁棒优化（Robust Optimization Over Time, ROOT）理论分析。应用场景：动态环境下的优化解部署决策。传感器类型：不适用。目标类别：不适用。摘要未说明具体应用场景。

Model architecture: 不适用。本文不是深度学习/神经网络模型，而是对进化优化算法解的理论性质分析，将存活建模为离散首次退出问题（discrete first-exit problem）。

Lightweight method: 不涉及。摘要中未出现剪枝、量化、知识蒸馏、NAS、轻量网络等任何模型压缩或加速方法。

Onboard deployability: 不适用。摘要中的"deployment"指优化解在动态环境中的部署，而非星上/边缘硬件部署。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署指标。

Datasets and metrics: 数据集：不适用，本文为理论分析+蒙特卡洛仿真（Monte Carlo study），无真实数据集。评价指标：期望存活时间（expected survival time）、存活时间下界与上界、环境变化次数。

Experiments: 主要实验为蒙特卡洛仿真，验证理论预测，检验对建模假设和参数不确定性的敏感性，并展示边界如何支持部署决策。摘要未给出具体数值结果。

Limitations: 1. 与遥感、目标检测、模型轻量化完全无关，属于纯理论性进化计算研究。2. 分析限定于各项同性高斯环境动态，对真实复杂环境动态的适用性有限。3. 摘要未说明如何扩展到非高斯或各向异性环境。4. 未涉及任何实际工程应用或深度学习模型。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：本文"解在动态环境中存活时间"的建模思想，在概念上可类比于"轻量化模型在轨长期运行时的性能退化/漂移分析"，即星上模型在持续运行中面对数据分布漂移时，其检测精度能维持多久。但这种类比非常间接，且本文是纯理论分析，不提供任何可复现的深度学习或遥感方法。

Abstract: Robust Optimization Over Time (ROOT) is a recent branch of evolutionary dynamic optimization that seeks solutions capable of remaining effective across multiple consecutive environments. Unlike the traditional track-the-moving-optimum (TMO) paradigm, which reoptimizes after every environmental change, ROOT explicitly values persistence. Although the field has grown considerably, most contributions remain algorithmic and empirical, leaving several fundamental properties poorly understood from a theoretical perspective. One such property is survival time, defined as the number of future environments in which a deployed solution continues to satisfy a prescribed quality threshold. While survival time is widely used as a measure of temporal robustness, little is known about how its expected value depends on environmental dynamics, deployment quality, or problem characteristics. This paper studies expected survival time for a fixed deployed solution under isotropic Gaussian environmental dynamics. Modeling survival as a discrete first-exit problem, we derive a rigorous lower bound and a computable multi-step upper bound. The analysis shows that expected survival scales as $Θ(σ^-{2})$ in slowly varying environments and approaches its minimum value of one future change in high dimensions. A comprehensive Monte Carlo study validates the theoretical predictions, examines sensitivity to modeling assumptions and parameter uncertainty, and illustrates how the bounds can support deployment decisions after optimization. The resulting framework provides an analytical characterization of deployment lifetime and identifies when a required deployment horizon can be guaranteed, ruled out, or remains analytically unresolved.

</details>


### [110] [An analysis of binary isotonic regression: degrees of freedom and implications for calibration](https://arxiv.org/abs/2607.27301)
*Raphael Rossellini,Rina Foygel Barber,Zhimei Ren,Jake A. Soloff*

Main category: stat.ML

TL;DR: 本文从解析数论角度给出了等渗回归（isotonic regression）在二值样本上最坏情况自由度的尖锐有限样本刻画，并据此推导出等渗回归校准后Expected Calibration Error (ECE)的首个非平凡、无分布假设的保证。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文属于统计学习理论/概率校准领域，研究对象是等渗回归的自由度界与校准误差保证，不涉及遥感、卫星、目标检测或任何模型压缩/加速方法。

Reading priority: 1分。论文属于统计学习理论，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：统计推断与概率校准（probabilistic calibration）的理论分析。应用场景：概率预测器的后处理校准（post-processing calibration）。传感器类型：不适用。目标类别：二值标签 Y∈{0,1}，非视觉目标检测场景。

Model architecture: 研究对象为等渗回归（isotonic regression），一种用于估计单调函数和校准概率预测器的经典非参数工具。论文不涉及神经网络或深度学习模型架构。

Lightweight method: 不涉及。论文不讨论剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文为纯理论分析，不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。

Datasets and metrics: 无实验数据集。评价指标为等渗回归的degrees of freedom（自由度）以及Expected Calibration Error (ECE)。理论设定为二值样本 Y∈{0,1}，无分布假设（distribution-free）。

Experiments: 摘要未说明具体实验。核心理论贡献为：(1) 给出等渗回归在二值样本上最坏情况自由度的尖锐有限样本界，主项为 (3/(4π²)^(1/3))·n^(2/3)，改进了此前上界；(2) 基于确定性自由度界推导出等渗回归ECE的首个非平凡、无分布保证。

Limitations: 论文仅针对二值标签（Y∈{0,1}）场景，未扩展到多分类或回归任务；为纯理论结果，缺乏实际数据集验证；与遥感目标检测、模型轻量化、在轨部署完全无关。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接联系是：在轨目标检测模型的置信度校准（calibration）是一个值得关注的工程问题，等渗回归作为后处理校准方法可提升检测器输出概率的可靠性，但其理论分析本身对本课题的轻量化方向无直接帮助。若关注检测器校准，可参考其ECE度量思想，但无需深入本文理论推导。

Abstract: Isotonic regression is a canonical tool for estimating monotone functions and calibrating probabilistic predictors. We provide a fully sharp finite-sample characterization of its worst-case degrees of freedom on binary samples. Specifically, we identify the binary sequences that maximize the number of distinct fitted values produced by isotonic regression. We develop a sharp bound on the degrees of freedom with a leading term of $\frac{3}{(4π^2)^{1/3}} n^{2/3}$ using analytic number theory, improving on previous bounds.
  We then apply this result to calibration. Calibration is a central requirement for probabilistic prediction, and isotonic regression is a widely used post-processing method for improving calibration. Building on deterministic degrees-of-freedom bounds, we derive, to our knowledge, the first nontrivial distribution-free guarantee on the Expected Calibration Error (ECE) of isotonic regression. This ECE bound is fully model-free and distribution-free, only assuming $Y \in \{0,1\}$.

</details>


### [111] [HOMER: Huber-of-Means for Efficient and Robust Estimation in Hilbert Spaces](https://arxiv.org/abs/2607.27532)
*Kisung You,Boram Cho*

Main category: stat.ML

TL;DR: 提出HOMER（Huber-of-Means for Efficient and Robust Estimation），一种基于径向Huber中心的稳健均值估计方法，用于在重尾数据下实现高效且稳健的统计推断。

Research relevance: 1分，基本无关。该论文是统计学/稳健估计方向的纯理论工作，讨论重尾数据下的均值估计（Huber-of-Means），完全不涉及遥感、卫星、目标检测或模型轻量化。与"遥感卫星在轨目标检测与模型轻量化"研究方向无任何直接或间接关联。

Reading priority: 1分。该论文为纯统计学稳健估计理论工作，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：稳健统计估计（robust mean estimation）。应用场景：重尾数据下的均值推断，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: HOMER通过径向Huber中心（radial Huber center）聚合块均值（block means），包含canonical和pseudo-Huber两种形式，分别对每个块得分进行有界约束，在类中位数稳健性与经验均值效率之间插值。

Lightweight method: 摘要未说明。该论文不涉及任何模型压缩或加速方法（pruning, quantization, knowledge distillation, NAS等均不涉及）。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 使用重尾模拟数据（heavy-tailed simulations）和干净高斯数据（clean Gaussian data）进行实验验证，评估指标为估计效率（efficiency）和覆盖概率（coverage）。未使用任何遥感数据集。

Experiments: 实验表明：HOMER在少数块摘要被位移时保持稳定；在干净高斯数据上两种版本均接近经验均值的效率；有限块sandwich区间在偏斜函数型数据上覆盖不足；当污染影响大多数块或破坏普通块内均值时方法失效。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于纯统计学理论论文；2. 方法在污染影响大多数块或块内均值被破坏时失效；3. 有限块sandwich区间存在覆盖不足问题，尤其对偏斜函数型数据；4. 需要块大小和块数量同时增长且块大小增长更快，实际应用条件较严格。

Ideas for my research: 该论文与本人课题基本无直接借鉴价值。唯一可能的间接启发是：HOMER中"稳健估计"的思想或可类比于在轨目标检测中对抗噪声/异常数据（如传感器噪声、云遮挡导致的异常特征）的鲁棒性设计，但这一关联非常牵强，且论文本身不涉及任何视觉或遥感内容，不建议投入时间深入阅读。

Abstract: Heavy tails weaken high-confidence control for the empirical mean. Geometric median-of-means (MOM) also lacks a threshold that moves toward mean efficiency. We propose \emph{HOMER}, or Huber-of-Means for Efficient and Robust Estimation. HOMER aggregates block means through a radial Huber center. Its canonical and pseudo-Huber forms bound each block score and interpolate between median-like robustness and the empirical mean. We establish a Hilbert-space majority theorem and a MOM-order deviation bound under a finite second moment. Canonical HOMER recovers the sample mean inside its quadratic region. Pseudo-HOMER approaches the sample mean as the threshold grows. It also admits asymptotic linearity and consistent sandwich covariance estimation around the population block-Huber target. Under a finite third moment, fixed finite-dimensional projections support mean inference at the usual parametric rate. This result requires growing block sizes and counts, with block sizes increasing faster. Heavy-tailed simulations show that HOMER remains stable when a minority of block summaries is displaced. On clean Gaussian data, both versions closely approach the empirical mean's efficiency. Finite-block sandwich intervals undercovered, especially for skewed functional data. Further studies show failure when contamination affects most blocks or compromises ordinary within-block means.

</details>


### [112] [Robust Wavelength Selection for Partial Least Squares Sugar Content Estimation Using Combinatorial Bayesian Optimization](https://arxiv.org/abs/2607.27645)
*Mitsunobu Kanebako,Ami S. Koshikawa,Masaru Hitomi,Takuro Tanaka,Mahito Chiba,Maiko Mori,Masayuki Ohzeki*

Main category: stat.ML

TL;DR: 提出一种基于贝叶斯优化的波长区域选择方法，将近红外光谱糖含量预测中的波长选择建模为二元黑箱优化问题，通过稀疏二次代理模型和Thompson采样结合模拟/量子退火求解，提升偏最小二乘回归的预测精度和波长选择一致性。

Research relevance: 弱相关（1分）。该论文聚焦近红外光谱（NIR spectroscopy）的波长选择与糖含量预测，属于光谱分析/化学计量学领域，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。唯一的间接联系是"特征选择"这一通用思想，以及"量子退火/模拟退火"作为组合优化求解手段，但这些与遥感目标检测模型轻量化方向关联甚微。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于近红外光谱特征选择研究，不建议优先阅读。除非对QUBO/量子退火在特征选择中的应用有特殊兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务：近红外光谱波长区域选择（wavelength-region selection），用于糖含量（sugar content）预测。应用场景为光谱分析/化学计量学，非遥感场景。传感器类型为近红外光谱仪（NIR spectrometer），目标类别为糖含量回归预测，非目标检测任务。

Model architecture: 方法核心为组合贝叶斯优化（combinatorial Bayesian optimization）：构建稀疏二次代理模型（sparse quadratic surrogate model），通过Thompson sampling（汤普森采样）顺序提取感兴趣波长区域，将采集函数最小化建模为二次无约束二元优化问题（QUBO），用模拟退火（simulated annealing）或量子退火（quantum annealing）求解。下游预测模型为偏最小二乘回归（partial least squares regression, PLS）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。其"稀疏"代理模型和QUBO求解属于特征选择范畴，与深度学习模型轻量化无关。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。虽然涉及量子退火（quantum annealing），但这是用于求解QUBO优化问题，并非面向星上/边缘部署的推理加速方案。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为预测精度（prediction accuracy）、均方根误差（root mean square error, RMSE），以及波长区域选择的一致性（consistency）。对比方法包括基于遗传算法的选择（genetic-algorithm-based selection）和模拟退火（simulated annealing）。

Experiments: 实验表明：所提方法相比遗传算法和模拟退火，能提升PLS回归的预测精度，并得到更一致的波长区域。在一比特局部扰动（one-bit local perturbations）下，所选波长区域的验证集RMSE波动极小，说明方法收敛到更平滑的误差曲面，避免孤立过拟合解。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于光谱特征选择方法研究。2. 摘要未说明数据集规模、光谱波段数量、计算开销等关键细节。3. 未讨论该方法在深度学习模型或大规模高维特征上的可扩展性。4. 量子退火的实际硬件依赖和成本未提及。

Ideas for my research: 借鉴意义有限，但可提取两点通用思想：1) 将特征/波段选择建模为组合优化问题（QUBO）并用贝叶斯优化求解的思路，理论上可迁移到遥感多光谱/高光谱波段的轻量化特征选择，减少输入通道从而降低模型计算量；2) "局部稳定性/平滑误差曲面"的评估视角可用于衡量轻量化模型（如剪枝后模型）的鲁棒性。但整体上该论文对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点很少。

Abstract: Wavelength selection is one of the important preprocessing methods in near-infrared spectroscopy to improve prediction accuracy and interpretability of spectral data. We formulate wavelength-region selection for sugar content estimation as a binary black-box optimization problem and propose a method based on Bayesian optimization. The proposed method constructs a sparse quadratic surrogate model and sequentially extracts interested wavelength regions by Thompson sampling. Minimizing an acquisition function is performed as a quadratic unconstrained binary optimization problem by simulated or quantum annealing. Experiments show that the proposed method improves the prediction accuracy of partial least squares regression and yields more consistent wavelength regions than genetic-algorithm-based selection and simulated annealing. Under one-bit local perturbations, the selected wavelength regions show minimal fluctuations in root mean square errors between observed and predicted values of a validation set. This local stability suggests that our method converges to a smoother error landscape and avoids isolated overfitted solutions. These results indicate that combinatorial Bayesian optimization is a useful framework for robust feature selection in spectroscopic prediction tasks.

</details>


### [113] [Error Analysis of Neural-Network-Based Engression](https://arxiv.org/abs/2607.27723)
*Juntong Chen,Zijian Guo,Xinwei Shen*

Main category: stat.ML

TL;DR: 本文对Engression方法（一种基于能量分数拟合条件分布的生成式深度学习方法）进行了理论误差分析，将超额风险分解为近似误差、随机误差和蒙特卡洛误差三部分，并在目标条件生成器具有组合光滑结构的假设下建立了收敛速率。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文属于统计机器学习/生成式建模的理论分析范畴，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感场景，也不涉及目标检测任务，更不涉及任何模型压缩或轻量化方法。属于泛机器学习理论方法，相关性评分应为1分。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于统计学习理论范畴，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：条件分布估计/生成式建模（conditional distribution estimation / generative modeling）。应用场景：通用统计学习与深度学习的理论分析，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 采用深度神经网络（deep neural networks）实现Engression生成式模型，形式为 Y = f(X, ε)，其中ε为随机噪声输入。论文重点在于理论分析而非具体网络结构设计，摘要未说明具体网络架构细节。

Lightweight method: 不涉及。摘要中未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文为纯理论分析，未涉及参数量、FLOPs、延迟、显存、能耗、实时推理或边缘/星上部署等任何部署相关信息。

Datasets and metrics: 摘要未说明。论文为理论误差分析，未提及任何实验数据集或评价指标。

Experiments: 摘要未说明。论文聚焦于理论收敛速率推导（excess risk分解为approximation error、stochastic error、Monte Carlo error），未提及实验对比或消融实验。

Limitations: 摘要未说明具体局限性。但作为纯理论分析论文，其假设条件（目标条件生成器具有组合光滑结构）在实际遥感数据中是否成立未知；且该工作不涉及任何遥感场景验证，也未讨论计算效率或部署可行性。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：Engression作为一种生成式条件分布学习方法，其误差分解框架（近似误差、随机误差、蒙特卡洛误差）在理论上可用于分析生成式数据增强方法在遥感小样本目标检测中的误差来源，但这一联系非常间接。对于在轨遥感目标检测模型轻量化课题，该论文几乎无可直接借鉴的内容。

Abstract: Engression (Shen and Meinshausen, 2024) learns a conditional distribution by fitting a generative model $Y = f(X,\varepsilon)$ under the energy score, a strictly proper scoring rule. We provide a theoretical error analysis of engression implemented with deep neural networks. We decompose the excess risk into three components: the approximation error, the stochastic error, and the Monte Carlo error. Based on this decomposition, we establish convergence rates under the assumption that the target conditional generator admits a compositional smoothness structure.

</details>


### [114] [Robust Estimation of Sparse Numerical Vectors under Local Differential Privacy](https://arxiv.org/abs/2607.27815)
*Puning Zhao,Zhikun Zhang,Shaowei Wang,Sheng Yue,Bangzhou Xin,Tianhang Zheng,Pengfei Zhang,Xiaochun Cao*

Main category: stat.ML

TL;DR: 提出Randomized Projection with Clipping (RPC)方法，解决本地差分隐私（LDP）下多条目用户的鲁棒稀疏向量均值估计问题，通过随机投影与裁剪限制攻击者能力，并基于精确偏差分析消除偏差-方差权衡。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于本地差分隐私（LDP）协议下的投毒攻击防御与鲁棒稀疏向量均值估计问题，属于隐私保护与鲁棒统计估计领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感场景，也不涉及object detection、小目标检测、旋转框检测等目标检测任务，更不涉及pruning、quantization、knowledge distillation、NAS等轻量化方法。唯一可能的弱关联是"轻量化"概念在数学意义上的"轻量"（稀疏向量），但与本课题的模型轻量化完全无关。

Reading priority: 1分。论文属于差分隐私与鲁棒统计估计领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：鲁棒稀疏向量均值估计（robust sparse vector mean estimation），属于差分隐私与鲁棒统计领域。应用场景：本地差分隐私（LDP）协议下的数据聚合。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 提出Randomized Projection with Clipping (RPC)方法：服务器向每个用户发送随机二值向量，用户将本地数据投影到该向量上并进行裁剪（clipping）以限制攻击者能力；针对裁剪偏差提出基于精确偏差表达式分析的校正方法。属于统计估计方法而非深度学习模型架构。

Lightweight method: 摘要未说明。本文不涉及模型轻量化方法（pruning、quantization、knowledge distillation、NAS等均未提及）。文中"轻量"仅指稀疏向量（每个用户向量有m个非零坐标），与深度学习模型压缩无关。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集名称。实验包括数值实验（numerical experiments），在可信环境（trusted environments）下与现有方法对比性能，在不可信环境（untrusted environments）下评估对投毒攻击的鲁棒性。具体评价指标摘要未说明。

Experiments: 数值实验表明：1）在可信环境下，RPC方法达到与现有方法相当或更优的性能，说明其本身就是一个高效的估计器；2）在不可信环境下，RPC方法对投毒攻击显著更鲁棒。提供了在所有可能攻击下的估计误差严格理论保证。

Limitations: 摘要未说明。但可推断：1）论文完全面向差分隐私与鲁棒统计场景，与遥感目标检测和模型轻量化无直接关联；2）未涉及深度学习模型、遥感图像处理或边缘部署；3）方法基于数学统计估计框架，难以直接迁移到遥感目标检测任务。

Ideas for my research: 对本课题（遥感卫星在轨目标检测模型轻量化）的可借鉴点非常有限。可能的间接启发：1）RPC中"随机投影+裁剪"的思想在数学上是一种降维与压缩手段，可类比思考在遥感目标检测中通过随机投影或特征裁剪实现特征维度压缩，但需谨慎评估其与深度学习特征提取的兼容性；2）偏差校正的精确分析思路可启发在模型量化或剪枝后对精度损失的偏差补偿设计。总体而言，本文与本课题关联度极低，不建议作为主要参考。

Abstract: Local differential privacy (LDP) protocols are vulnerable to poisoning attacks. Existing research have proposed efficient defense strategies for single-item users. However, in practice, a user may possess multiple items. The defense against poisoning attacks for multi-item users is challenging, because due to larger output spaces, the adversary can conduct more powerful attacks without being detected. In this paper, we address the robust sparse vector mean estimation problem, in which each user has a vector with $m$ nonzero coordinates. We propose Randomized Projection with Clipping (RPC). Firstly, the server sends a random binary vector to each user. The user then projects its local data on the vector, and clip the value to restrict the attacker's capability. To handle clipping bias, we propose a correction method based on a careful analysis that gives an exact expression of the bias. As a result, bias-variance tradeoff is no longer needed, thus the clipping threshold can be further reduced to shrink the output space and enhance robustness. We provide a rigorous theoretical guarantee of the estimation error under all possible attacks. Numerical experiments show that under trusted environments, our new method achieves comparable or better performance than existing methods, indicating that our method is already an efficient estimator in its own right. Under untrusted environments, our method is also significantly more robust to poisoning attacks.

</details>


### [115] [Generalization and Trade-off in Adversarial Training: An RKHS Perspective via Kernel Integral Operators](https://arxiv.org/abs/2607.27995)
*Yiling Xie,Xiaoming Huo*

Main category: stat.ML

TL;DR: 本文从RKHS（再生核希尔伯特空间）理论框架出发，研究对抗训练（adversarial training）的泛化误差界，提出两阶段噪声去偏方法以缓解对抗鲁棒性与统计精度之间的权衡。

Research relevance: 弱相关（2分）。本文属于泛机器学习/统计学习理论方向，研究对抗训练的泛化误差界与噪声去偏，不涉及遥感、卫星、目标检测或模型轻量化/部署。虽然对抗训练（adversarial training）在遥感目标检测模型鲁棒性方面可能有间接借鉴意义，但论文本身与遥感场景、目标检测任务、轻量化部署均无直接关联。

Reading priority: 2分。本文是统计学习理论方向的对抗训练泛化分析，与遥感卫星在轨目标检测及轻量化方向基本无关。仅当课题涉及对抗鲁棒性理论或遥感模型鲁棒性分析时，可作为理论背景参考，否则优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：对抗训练（adversarial training）的统计学习理论分析。应用场景：泛化机器学习鲁棒性研究，非遥感场景。传感器类型：摘要未说明。目标类别：摘要未说明（非目标检测任务）。

Model architecture: 在RKHS（reproducing kernel Hilbert space）框架下，通过核积分算子（kernel integral operator）研究对抗训练估计器。提出两阶段噪声去偏（two-stage noise-debiased）过程，估计并移除混合鲁棒项中的噪声贡献。摘要未说明具体网络结构或深度学习模型架构。

Lightweight method: 摘要未说明。本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时推理或星上/边缘部署等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为泛化误差界（generalization error bounds）、minimax预测基准（minimax prediction benchmark）、泛化率（generalization rate）。数值实验用于验证理论发现，但摘要未说明实验所用数据集。

Experiments: 理论结果：推导了RKHS对抗训练估计器的source-uniform泛化误差界；在固定多项式谱模型上建立匹配下界，揭示对抗训练相对minimax预测基准存在统计精度损失；提出两阶段噪声去偏方法，使估计器达到minimax多项式速率（至多差对数因子）。数值实验支持理论发现，但摘要未说明具体对比方法和消融实验细节。

Limitations: 1. 纯理论分析论文，缺乏实际遥感/目标检测场景验证；2. 摘要未说明方法在真实深度学习模型（如CNN、Transformer）上的可迁移性；3. 未涉及任何轻量化或部署相关内容；4. 对抗训练与遥感在轨目标检测的关联需要额外推理，论文本身未建立该联系。

Ideas for my research: 1. 对抗训练（adversarial training）可用于提升遥感目标检测模型在恶劣成像条件（噪声、遮挡、光照变化）下的鲁棒性，本文关于"对抗鲁棒性与泛化权衡"的理论分析可作为设计鲁棒轻量化模型的参考；2. 两阶段噪声去偏思想可借鉴到遥感图像去噪/增强预处理中，提升小目标检测精度；3. 但本文不提供任何轻量化方法或部署方案，对"在轨轻量化"课题的直接借鉴价值有限。

Abstract: Adversarial training has emerged as a powerful approach for protecting models against adversarial attacks in a broad range of real-world applications. In this paper, we study adversarial training in the reproducing kernel Hilbert space (RKHS) framework through the associated kernel integral operator. We first derive source-uniform generalization error bounds for the RKHS adversarial training estimator in terms of the robustness level, sample size, source smoothness, and kernel spectrum. On a fixed polynomial-spectrum model, we further establish a matching lower bound showing that the optimally balanced generalization rate can be slower than the minimax prediction benchmark. This result reveals a loss of statistical accuracy in adversarial training. Our analysis shows that this loss arises from the interaction between adversarial robustness and observation noise: the noise contribution in the mixed robustness term slows the approximation rate, although the same term reduces the estimation complexity. To address this limitation, we propose a two-stage noise-debiased procedure that estimates and removes the noise contribution from the mixed term. The resulting estimator improves the generalization rate and attains the minimax polynomial rate, up to a logarithmic factor, when the robustness level is selected at the stated sample-dependent order. Our results characterize the generalization behavior of adversarial training in a nonparametric framework and provide a new interpretation and a principled solution for the trade-off between adversarial robustness and generalization. Numerical experiments support the theoretical findings and demonstrate the effectiveness of the proposed method.

</details>


### [116] [On a joint simultaneous learning of relevant feature subsets and subspaces in regression-like problems](https://arxiv.org/abs/2607.28080)
*Illia Horenko*

Main category: stat.ML

TL;DR: 提出熵最优流形回归（EOMR）方法，将熵最优流形聚类扩展为可同时识别非平稳非线性回归问题中相关特征子集与子空间的方法，在混沌动力学和托卡马克等离子体预测任务上以极低模型复杂度超越多种SOTA机器学习工具。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于非线性回归/时间序列预测问题（Lorenz-96混沌系统、托卡马克等离子体Hasegawa-Wakatani模型），属于流体动力学与等离子体物理领域的机器学习方法研究。不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据；不涉及object detection、小目标检测、旋转框检测或遥感场景理解；也不涉及pruning、quantization、knowledge distillation、NAS、实时推理、边缘部署或星上部署等轻量化技术。唯一的弱关联在于其强调"线性扩展的迭代与内存复杂度"以及"极低参数量的模型描述"，在模型复杂度控制理念上对轻量化研究有间接启发，但场景与方法均不相关。

Reading priority: 1分。该论文属于流体动力学/等离子体物理领域的非线性回归方法研究，与遥感卫星在轨目标检测及模型轻量化方向几乎无直接关联。既非遥感场景，也非目标检测任务，更未涉及部署相关的轻量化技术。仅在"极低模型复杂度"和"线性复杂度扩展"的设计理念上有间接启发，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：非线性/非平稳回归与时间序列预测。应用场景：混沌动力学系统预测（Lorenz-96，F=8和F=12强混沌与极强混沌状态）、托卡马克等离子体边缘湍流建模（Hasegawa-Wakatani模型）。传感器类型：无（非遥感传感器）。目标类别：无（非目标检测任务）。

Model architecture: EOMR（Entropy-Optimal Manifold Regression）基于EOMC（Entropy-Optimal Manifold Clustering）扩展，联合识别相关特征子集与子空间，在非平稳非线性回归中实现鲁棒学习。对Hasegawa-Wakatani数据，EOMR蒸馏出仅含8个参数的线性、因果、弱平稳自回归过程来描述主导EOF（Essential Orthogonal Function）动力学。摘要未说明具体网络结构细节。

Lightweight method: 摘要未明确采用剪枝、量化、蒸馏、NAS等标准轻量化技术。但其核心卖点是"linearly-scaling iteration and memory complexities"（线性扩展的迭代与内存复杂度）以及"极简模型描述"（仅8个参数的自回归过程），体现了极低模型复杂度的设计理念，可视为一种"模型固有轻量化"思路，但并非面向部署的压缩/加速方法。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。仅提及迭代与内存复杂度为线性扩展，以及EOMR相比对比方法具有"orders of magnitude smaller model complexities"（数量级更小的模型复杂度）。但未涉及FPGA/NPU/TensorRT等边缘或星上部署相关内容。

Datasets and metrics: 数据集：(i) Lorenz-96混沌系统（forcing parameter F=8和F=12）；(ii) Hasegawa-Wakatani模型托卡马克等离子体边缘数据。评价指标：root mean squared prediction errors（RMSE预测误差）、model complexity（模型复杂度）。对比方法：梯度提升随机森林（gradient boosted random forests）、深度神经网络（deep neuronal networks）、TabPFN v0.3（transformer-based AI工具）。

Experiments: 在Lorenz-96强混沌（F=8）与极强混沌（F=12）状态下，以及Hasegawa-Wakatani托卡马克等离子体数据上，EOMR相比梯度提升随机森林、深度神经网络和TabPFN v0.3，在RMSE预测误差上取得数量级优势，同时模型复杂度低数量级。对Hasegawa-Wakatani数据，EOMR蒸馏出仅8个参数的线性因果弱平稳自回归过程描述主导EOF动力学。摘要未说明消融实验。

Limitations: 1. 摘要未说明EOMR在图像/视觉任务上的适用性，无法判断能否迁移到遥感目标检测。2. 摘要未提供具体参数量、FLOPs、推理延迟等部署指标。3. 摘要未说明EOMR对高维图像数据（如遥感影像）的可扩展性。4. 摘要未说明EOMR是否支持分类/检测任务，仅针对回归预测。5. 对比方法中未包含轻量化CNN或检测模型，缺乏与目标检测SOTA的对比。

Ideas for my research: 1. EOMR的"线性扩展迭代与内存复杂度"设计理念可借鉴：在遥感在轨目标检测的轻量化设计中，可关注算法整体复杂度随输入规模线性增长的特性，而非仅关注模型参数量。2. "以极简参数描述复杂动力学"的思路可启发：在星上部署场景中，探索用极低参数量的轻量模型（如少量参数的自回归/线性结构）近似复杂检测特征分布的可能性。3. 其"联合识别特征子集与子空间"的思想可迁移到遥感特征选择/通道剪枝中，用于在保持检测精度的前提下自动筛选关键特征通道。4. 但整体而言，该论文与遥感目标检测及轻量化部署的直接关联极弱，借鉴价值有限。

Abstract: We extend a recently introduced Entropy-Optimal Manifold Clustering (EOMC) to allow for a joint simultaneous identification of subsets and subspaces of relevant features in nonstationary and nonlinear regression problems. It is shown that the proposed extension - that we coin as Entropy-Optimal Manifold Regression (EOMR) - allows a robust learning with linearly-scaling iteration and memory complexities. EOMR is compared to the most complete set of state-of-the-art tools from the Artificial Intelligence (AI) and Machine Learning (ML) that is available to the author, on the very challenging problems from chaotic and fluid dynamics: (i) on predicting the Lorenz-96 systems dynamics in strongly- and very-strongly chaotic regimes (with forcing parameter being $F=8$ and $F=12$, respectively); and, (ii) on a data from the Hasegawa-Wakatani model on the edge of the tokamak plasma. It is demonstrated that the proposed benchmarks (i) and (ii), indeed, are the very challenging problems for the state of the art ML and AI tools - since both the general-purpose gradient boosted random forests and deep neuronal networks, as well as transformer-based AI tools like TabPFN v.03 (more spezialised for large-dimensional small data learning problems) - result in orders of magnitude inferior root mean squared prediction errors, and orders of magnitude larger model complexities, when compared to the EOMR. For a Hasegawa-Wakatani example, EOMR distills a very simple entropy-optimal and skilful description of the leading Essential Orthogonal Function (EOF) dynamics, given by linear, causal and weakly-stationary autoregressive process described by just 8 parameters.

</details>


### [117] [Uncertainty quantification for trustworthy deep learning: Methods and measures](https://arxiv.org/abs/2607.28248)
*H. Martin Gillis,Thomas Trappenberg*

Main category: stat.ML

TL;DR: 一篇关于深度学习不确定性量化（UQ）方法的系统性综述，聚焦集成方法和近似贝叶斯方法，以及用于汇总其输出的度量标准。

Research relevance: 弱相关（2分）。该论文是泛机器学习/深度学习领域的不确定性量化综述，不涉及遥感、卫星、目标检测或轻量化部署。虽然不确定性量化（UQ）对安全关键场景（如星上推理）有潜在价值，但论文本身并未针对遥感或目标检测场景，也未涉及模型压缩/加速方法。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，作为泛机器学习综述，仅在"不确定性量化"这一交叉维度上有间接参考价值。若课题不涉及不确定性估计，则无需精读；若未来考虑在轨推理的置信度可靠性问题，可选择性阅读其中efficient ensemble和single-pass方法章节。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：深度学习中的不确定性量化（Uncertainty Quantification, UQ）综述。应用场景：安全关键领域（safety-critical domains）的深度神经网络部署，非遥感/卫星场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 综述覆盖五类方法家族：1) Bayesian neural networks（贝叶斯神经网络）；2) Monte Carlo Dropout；3) deep ensembles（深度集成）；4) efficient ensemble approximations（高效集成近似）；5) last-layer or single-pass approaches（末层或单次前向方法）。同时涉及evidential/prior networks、conformal prediction、post-hoc calibration、out-of-distribution detection和selective prediction等相邻工作。

Lightweight method: 论文并非轻量化方法综述，但其中"efficient ensemble approximations"和"single-pass methods"（单次前向方法）在计算效率上具有轻量化思想——即通过减少多次前向传播的开销来获得不确定性估计。这属于计算效率优化，而非剪枝/量化/蒸馏等传统模型压缩手段。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。仅提及"efficient ensemble approximations"和"single-pass methods"在计算效率上的优势，但未给出具体量化数据。

Datasets and metrics: 摘要未说明具体数据集。论文讨论了不确定性度量（uncertainty measures）及其分解，包括entropy decomposition与pairwise divergence measures的对比，并整合了评估方法论（evaluation methodology），但未给出具体数据集或指标名称。

Experiments: 摘要未说明具体实验结果。作为综述论文，其"实验"部分主要是对各类UQ方法的理论动机、实现方式、经验性能和局限性的定性比较与批判性评述，而非提出新方法并进行实验验证。

Limitations: 1) 与遥感/卫星/目标检测场景完全无关，未涉及任何遥感数据或检测任务；2) 未涉及模型压缩（剪枝/量化/蒸馏）等轻量化手段；3) 未提供任何部署相关的量化指标（FLOPs、延迟、能耗等）；4) 作为综述，缺乏针对特定下游任务（如目标检测）的UQ方法适配讨论。

Ideas for my research: 1) 尽管论文不直接相关，但"efficient ensemble approximations"和"single-pass methods"的思想可借鉴到星上目标检测模型的不确定性估计中——在轨推理时，模型不仅需要输出检测框，还需要输出置信度可靠性，这对安全关键的在轨决策有价值；2) 轻量化模型（如剪枝/量化后的检测器）往往存在校准退化问题，可借鉴本文的post-hoc calibration方法对轻量化模型进行置信度校准；3) 若未来课题涉及在轨模型的不确定性感知（uncertainty-aware）推理，本文的UQ方法分类框架可作为理论参考。

Abstract: The deployment of deep neural networks in safety-critical domains demands reliable estimates of predictive confidence, yet conventional architectures lack principled uncertainty quantification. This survey provides a structured, critical review of methods for Uncertainty Quantification (UQ) in deep learning, scoped to ensemble-based and approximate Bayesian approaches and the measures used to summarize their outputs. Relative to existing UQ surveys, our contribution is depth on efficient ensemble approximations and single-pass methods, and a unified treatment that separates the method producing a predictive distribution from the measure that summarizes its uncertainty. We organize methods into five families: Bayesian neural networks, Monte Carlo Dropout, deep ensembles, efficient ensemble approximations, and last-layer or single-pass approaches. We situate adjacent work on evidential and prior networks, conformal prediction, and post-hoc calibration, together with the decision-time tasks of out-of-distribution detection and selective prediction. For each, we examine theoretical motivation, implementation, empirical performance, and limitations. We then review ensemble diversity theory and uncertainty measures and their decompositions, contrasting the entropy decomposition with pairwise divergence measures, and consolidate evaluation methodology so that our qualitative comparisons share a common basis. We close with a brief treatment of uncertainty in large language models and open research directions, including efficient epistemic measures for classification, last-layer diversity, diversity and calibration under shift, and hybrid architectures.

</details>


### [118] [Doubly Robust Functional Representation Learning for Longitudinal Causal Inference with Irregular Histories](https://arxiv.org/abs/2607.28567)
*Mengfei Ran,Yifeng Shen,Ruijie Guan*

Main category: stat.ML

TL;DR: 提出 Doubly Robust Functional Representation Learning (DR-FRL)，一种将不规则纵向历史数据（如实验室值、生理信号）转化为目标导向状态表示的交叉拟合工作流，用于因果推断中的观测历史方案估计。

Research relevance: 与本研究方向基本无关（1分）。该论文聚焦于纵向因果推断（longitudinal causal studies）、双重稳健估计（doubly robust estimation）、有效影响函数（EIF）等统计因果推断方法，应用于ICU临床终点预测（VitalDB数据集）。完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能，也不涉及目标检测、小目标检测、旋转框检测、舰船/飞机/车辆检测等遥感任务，更不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署等模型压缩或部署技术。属于泛统计/泛机器学习方法，与遥感在轨目标检测及轻量化方向无直接关联。

Reading priority: 1分。该论文属于纵向因果推断/统计方法领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：纵向因果推断（longitudinal causal inference）中的观测历史方案（observed-history regimes）估计。应用场景：ICU临床终点预测（VitalDB审计）。传感器类型：生理信号、实验室值点云（laboratory point clouds）、图像衍生摘要。目标类别：不涉及目标检测，目标为临床结局（ICU disposition endpoint）。

Model architecture: DR-FRL包含：功能编码器（functional encoder）和时间编码器（temporal encoder）将点云和先验历史映射为状态（states）；nuisance heads估计结局（outcome）、治疗（treatment）和删失（censoring）函数；EIF-targeted验证、校准、重叠、尾部、消融诊断模块。采用cross-fitted工作流。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（无剪枝、量化、蒸馏、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性指标。

Datasets and metrics: 数据集：VitalDB（ICU临床数据库）。评价指标：摘要未明确列出具体指标，但涉及EIF-targeted验证、校准（calibration）、重叠（overlap）、尾部（tail）和消融（ablation）诊断；模拟实验考察功能混杂高维、测量信息性、支持弱、伪结局重尾等场景。

Experiments: 模拟实验显示在高维功能混杂、信息性测量、弱支持或重尾伪结局场景下DR-FRL有增益。VitalDB审计表明：对于ICU处置终点，标量实验室摘要已携带大量与终点相关的信息（一个有用的阴性发现）。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于临床因果推断领域。2. 摘要未说明模型复杂度、推理效率等部署相关信息。3. 摘要未说明与现有目标检测或轻量化方法的对比。4. 摘要未说明方法在视觉/图像任务上的泛化能力。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向基本无关，但可借鉴的间接思路有限：1. "cross-fitted工作流"和"EIF-targeted验证/诊断"思想可类比为在模型压缩过程中引入目标导向的验证诊断，确保轻量化后的模型在关键指标（如小目标AP、旋转框AP）上不退化；2. "representation error进入二阶乘积余项"的误差分析思路，可类比于量化/剪枝误差对最终检测精度影响的理论分析框架。但整体借鉴价值很低。

Abstract: Longitudinal causal studies often record histories as irregular functional fragments: laboratory values, physiologic signals, sensor streams, and image-derived summaries measured at unequal and informative times. Standard doubly robust estimators usually require scalar summaries, whereas sequence learners optimize prediction losses that need not stabilize the efficient influence function. We propose Doubly Robust Functional Representation Learning (DR-FRL), a cross-fitted workflow that turns irregular histories into estimand-targeted states for observed-history regimes. Functional and temporal encoders map point clouds and prior histories into states; nuisance heads estimate outcome, treatment, and censoring functions; and EIF-targeted validation, calibration, overlap, tail, and ablation diagnostics assess whether the state supports the estimating equation. If the selected state preserves the nuisance information needed by the EIF, representation error enters the same second-order product remainder as ordinary nuisance error, and the mean estimator is asymptotically linear under explicit rate, overlap, calibration, and stability conditions. Catoni aggregation is treated separately as a bounded-influence point estimator, not a replacement for Wald inference. Simulations show gains when functional confounding is high-dimensional, measurement is informative, support is weak, or pseudo-outcomes are heavy-tailed. A VitalDB audit shows that DR-FRL can use irregular laboratory point clouds and deliver a useful negative finding: for this ICU-disposition endpoint, scalar laboratory summaries already carry much endpoint-relevant information.

</details>


<div id='eess.SP'></div>

# eess.SP [[Back]](#toc)

### [119] [Characterization of Continuous Electromagnetic Manifolds via Calculus of Variations](https://arxiv.org/abs/2607.27396)
*Kuranage Roche Rayan Ranasinghe,Miguel Rodrigo Castellanos,Giuseppe Thadeu Freitas de Abreu*

Main category: eess.SP

TL;DR: 提出一种基于变分法(calculus of variations)的框架，用于任意MIMO阵列几何的连续电磁流形表征与波束成形，通过连续馈电函数将优化提升到硬件解耦的高维电流子空间。

Research relevance: 弱相关（1分）。论文主题是MIMO天线阵列的电磁波束成形与近场辐射建模，属于无线通信/电磁学领域，与遥感、卫星、目标检测、模型轻量化均无直接关联。虽然"beamforming"在遥感SAR成像中也有应用，但本文聚焦于通信MIMO阵列的电磁场理论，不涉及遥感图像处理或目标检测。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于MIMO通信天线波束成形领域，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务：MIMO阵列的连续电磁流形表征与波束成形优化。应用场景：无线通信天线阵列设计。传感器类型：MIMO天线阵列（非遥感传感器）。目标类别：不涉及目标检测，无目标类别。

Model architecture: 基于变分法(CoV)的框架：将每个网格元素建模为二维平面贴片，通过Gauss-Legendre (GL)求积评估空间平均Green函数；引入L^2(S_T)中的连续馈电函数w(p)作为N端口网络的无限维极限，将优化提升到维度K>>N的硬件解耦电流子空间。

Lightweight method: 摘要未说明。本文不涉及模型轻量化方法（无剪枝、量化、蒸馏、NAS等）。虽然提到"negligible additional cost"和"comparable computational cost"，但这指的是电磁数值计算的开销，而非深度学习模型的压缩或加速。

Onboard deployability: 摘要未说明。未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。仅提及MATLAB Antenna Toolbox全波验证的计算成本与SotA基线相当。

Datasets and metrics: 数据集：无遥感或视觉数据集。验证方式：MATLAB Antenna Toolbox全波仿真验证，对比线性与平面几何下的近场精度。评价指标：近场精度(near-field accuracy gains)相对SotA基线的提升。

Experiments: 通过MATLAB Antenna Toolbox全波仿真验证，在可比计算成本下，对线性和平面几何均获得一致的近场精度提升。推导了无约束场强最大化和近场方向图综合（含功率密度PD与区域约束）的闭式最优波束成形器，建立了与离散和广义匹配滤波器的精确类比。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于电磁学与通信领域。2. 摘要未说明实际硬件实验验证，仅基于MATLAB仿真。3. 未讨论计算复杂度、实时性等部署相关指标。4. 未涉及任何深度学习或计算机视觉内容。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接联系：1) 若未来研究涉及SAR在轨成像的波束成形或天线阵列设计，本文的连续流形建模方法可能有参考价值；2) 本文"硬件解耦"的思想（将优化从硬件端口数N提升到更高维子空间K）在概念上类似于模型压缩中"解耦表示能力与硬件约束"的思路，但方法论完全不同。总体而言，对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点极少。

Abstract: We present a novel calculus of variations (CoV)-based framework for the characterizing of, and beamforming over, continuous electromagnetic manifolds of arbitrary multiple-input multiple-output (MIMO) array geometries. Building upon the discrete moment-matrix formulation of the state-of-the-art (SotA), the proposed framework simultaneously overcomes three of its fundamental limitations: (i) the point-source approximation error incurred by the near-field radiation operator; (ii) the confinement of the beamforming space to the N-dimensional subspace dictated by the hardware port count; and (iii) the generalization to arbitrary array geometries. To this end, each mesh element is modeled as a two-dimensional planar patch whose spatially averaged Green's function is evaluated via Gauss-Legendre (GL) quadrature, yielding a strictly more accurate near-field representation at negligible additional cost, while a continuous feeding function w(p) in L^2(S_T), introduced as the infinite-dimensional limit of the N-port network, lifts the optimization onto a hardware-decoupled current subspace of dimension K >> N. As an application example, we employ the proposed CoV-based framework to derive closed-form optimal beamformers for both unconstrained field-strength maximization, and a near-field pattern synthesis under a power density (PD) and region constraints, establishing their exact analogy to the discrete and generalized matched filters. Full-wave MATLAB Antenna Toolbox validation confirms consistent near-field accuracy gains over the SotA baseline for both linear and planar geometries at comparable computational cost.

</details>


### [120] [Radar-Aided Near-Field Beam Prediction via Beam Map Learning for XL-MIMO V2I Communications](https://arxiv.org/abs/2607.27643)
*Jiali Nie,Yu Han,Yuanhao Cui,Xiaojie Li,Shi Jin,Chao-Kai Wen*

Main category: eess.SP

TL;DR: 提出一种基于被动雷达辅助的近场波束预测框架，利用轻量级encoder-decoder CNN将雷达Bartlett谱映射为通信波束图，以降低XL-MIMO V2I系统中近场波束训练开销。

Research relevance: 弱相关（2分）。该论文属于无线通信领域（XL-MIMO V2I系统），核心任务是波束预测（beam prediction），并非遥感/卫星目标检测。虽然使用了轻量级CNN（lightweight encoder-decoder），但其轻量化动机是降低通信波束训练开销，而非面向遥感在轨部署。与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在"轻量级CNN设计"和"雷达-通信跨模态映射"方面有一定方法学借鉴意义。

Reading priority: 1分。该论文属于无线通信波束预测方向，与遥感卫星在轨目标检测及轻量化课题基本无关。仅在轻量级CNN架构和跨模态映射方法上有极有限的间接借鉴意义，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：近场波束预测（near-field beam prediction）；应用场景：XL-MIMO车联网（V2I）通信系统；传感器类型：被动雷达（passive radar）与通信信号；目标类别：不涉及目标检测，目标是预测通信波束方向。

Model architecture: 轻量级encoder-decoder卷积神经网络（lightweight encoder-decoder CNN），用于将雷达Bartlett谱（radar Bartlett spectra）映射为通信波束图（communication beam maps）。引入Gaussian soft supervision以保持波束空间连续性。

Lightweight method: 采用轻量级encoder-decoder CNN架构实现雷达到波束的映射，属于轻量网络设计（lightweight network design）范畴。摘要未说明具体参数量、FLOPs或压缩手段（如剪枝、量化、蒸馏等）。

Onboard deployability: 摘要未说明具体部署指标（参数量、FLOPs、延迟、显存、能耗等）。仅提及"lightweight"这一定性描述，未给出量化数据。该工作面向车联网通信场景，非星上部署。

Datasets and metrics: 数据集：基于Sionna ray tracing生成的雷达-通信同步仿真数据集（synchronized Sionna ray tracing radar-communication dataset）；评价指标：Top-k accuracy、distance-based accuracy、beam loss、spectral efficiency。

Experiments: 仿真实验表明所提方法在Top-k accuracy、distance-based accuracy、beam loss和spectral efficiency四个指标上均一致优于基线方法。摘要未说明具体对比方法及数值结果。

Limitations: 1. 摘要未说明轻量级CNN的具体参数量、FLOPs等部署指标；2. 仅基于仿真数据验证，缺乏真实场景验证；3. 面向V2I通信场景，与遥感/卫星在轨目标检测无直接关联；4. 未涉及目标检测任务本身。

Ideas for my research: 1. "跨模态映射"思想可借鉴：类似地，遥感中可将SAR/多光谱等模态特征映射到目标检测特征空间，实现模态间信息互补；2. "Gaussian soft supervision"保持输出空间连续性的思想，可用于旋转框检测中角度回归的连续性约束；3. 轻量级encoder-decoder架构设计思路可迁移到遥感小目标检测的轻量化骨干网络设计中；4. 但整体而言，该论文与在轨遥感目标检测轻量化课题关联度较低，借鉴价值有限。

Abstract: Near-field beam training in extremely large-scale multiple-input multiple-output (XL-MIMO) vehicle-to-infrastructure (V2I) systems incurs high overhead due to large range-angle codebooks and rapid channel variation. This paper proposes a passive radar-aided framework for near-field beam prediction based on radar-to-beam map learning. By exploiting the spatial correlation between radar observations and communication signals, the proposed method maps radar Bartlett spectra to communication beam maps using a lightweight encoder-decoder convolutional neural network. Gaussian soft supervision is further introduced to preserve beam-space continuity. Simulations on a synchronized Sionna ray tracing radar-communication dataset show that the proposed method consistently improves Top-k accuracy, distance-based accuracy, beam loss, and spectral efficiency.

</details>


### [121] [V-RIS: Virtual-Aperture DoA Estimation with Sparse RIS](https://arxiv.org/abs/2607.27716)
*Fenghao Zheng,Fuhai Wang,Zihan Jin,Gui Zhou,Robert Caiming Qiu,Zenan Ling*

Main category: eess.SP

TL;DR: 提出V-RIS框架，利用仅四个角子阵和单天线接收机，通过多个RIS相位配置下的观测重构虚拟孔径表面场，实现与全孔径相当的2D DoA估计精度，将有效感知孔径与物理RIS单元数量解耦。

Research relevance: 弱相关（2分）。该论文属于无线通信/电磁感知领域，研究可重构智能表面（RIS）的到达方向（DoA）估计，与遥感卫星在轨目标检测及模型轻量化基本无关。虽然"虚拟孔径重构"与"用少量物理单元实现大孔径性能"在思想层面与轻量化有类比性，但任务场景（通信感知 vs 遥感目标检测）、数据模态（射频/电磁 vs 光学/SAR图像）、方法体系（信号处理 vs 深度学习）均不匹配，属于泛信号处理方法，仅具有方法论层面的弱借鉴意义。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于RIS通信感知领域。仅在"用少量资源逼近大孔径性能"的抽象思想层面有微弱类比价值，不建议作为核心参考文献，可作为跨领域思维拓展的泛读材料。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：2D direction-of-arrival (DoA) estimation（到达方向估计）；应用场景：可重构智能表面（RIS）辅助的无线感知/通信；传感器类型：RIS（可重构智能表面）+ 单天线接收机；目标类别：信号源方向（非视觉目标）。与遥感目标检测（舰船/飞机/车辆）无关。

Model architecture: V-RIS框架：采用四个角子阵（four-corner subarrays）部署几何，结合单天线接收机；通过多个RIS相位配置下的接收观测重构虚拟孔径表面场；利用远场照明下离散RIS表面场沿两个孔径轴满足有限阶空间递推（finite-order spatial recurrences）这一关键观测；通过RIS编码接收观测实现数据级一致性，通过远场空间递推实现传播一致性。

Lightweight method: 摘要未明确使用剪枝/量化/蒸馏/NAS等深度学习轻量化方法。其"轻量化"体现在硬件层面：仅用25%可编程RIS单元（四个角子阵）即可逼近全孔径DoA精度，将有效感知孔径与物理部署单元数量解耦，降低硬件成本和控制开销。这是一种"虚拟孔径"信号重构思想，而非模型压缩方法。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及硬件成本和控制开销与孔径尺寸解耦（decouple effective sensing aperture from number of physically deployed RIS elements），以及室外原型验证中仅用25%可编程单元即可将仰角和方位角误差控制在1°以内。该部署可行性讨论针对RIS硬件，而非星上深度学习模型部署。

Datasets and metrics: 数据集：仿真数据（extensive simulations）+ 室外原型（outdoor prototype）。评价指标：DoA估计精度（elevation and azimuth errors，仰角和方位角误差，以度为单位）；对比基线：full-aperture benchmark（全孔径基准）、matrix-completion（矩阵补全）、least-squares（最小二乘）。摘要未说明具体仿真数据集名称。

Experiments: 主要结果：1) 仿真中V-RIS逼近全孔径基准的DoA精度，且比矩阵补全和最小二乘基线产生更干净的频谱；2) 室外原型验证：仅用25%可编程RIS单元，仰角和方位角误差均保持在真值1°以内。摘要未说明消融实验细节。

Limitations: 1) 与遥感目标检测、图像处理、深度学习完全无关，属于无线通信/电磁感知领域；2) 摘要未说明方法在非远场、近场或复杂多径环境下的鲁棒性；3) 摘要未说明计算复杂度、实时性等部署指标；4) 仅验证了DoA估计任务，未涉及目标检测/分类/识别；5) 仿真和原型规模有限，未说明大规模场景泛化能力。

Ideas for my research: 尽管场景不同，V-RIS在方法论层面有可借鉴点：1) "虚拟孔径/虚拟感受野"思想——用少量物理单元（四个角子阵）重构大孔径性能，类比于轻量化检测网络中"用少量计算资源逼近大模型精度"的蒸馏/剪枝思想；2) 空间递推（finite-order spatial recurrences）约束——在重构过程中引入物理先验约束，类比于遥感检测中利用目标的空间结构先验辅助小目标检测；3) 偏置不变损失（bias-invariant loss）抑制硬件失真——类比于星上部署时对传感器噪声/辐射畸变的鲁棒性设计。但这些均为间接类比，直接借鉴价值有限。

Abstract: Large-aperture reconfigurable intelligent surfaces (RISs) enable high-resolution 2D direction-of-arrival (DoA) estimation, but existing approaches still tie hardware cost and control overhead to aperture size. To decouple the effective sensing aperture from the number of physically deployed RIS elements, we present V-RIS, a framework for virtual-aperture surface-field reconstruction and DoA estimation. V-RIS uses only four corner subarrays and a single-antenna receiver to reconstruct the virtual-aperture surface field from receiver observations collected under multiple RIS phase configurations, and then performs DoA estimation on the reconstructed virtual-aperture surface field. Our key observation is that, under far-field illumination, the discretized RIS surface field satisfies finite-order spatial recurrences along both aperture axes. We enforce data-level consistency through the RIS-coded receiver observations and propagation consistency through the far-field spatial recurrence, while using a four-corner deployment geometry that retains both contiguous local elements and long aperture baselines. To improve robustness in practical receiver observations, we adopt a bias-invariant receiver-domain loss that suppresses quasi-static hardware distortions and configuration-invariant multipath contributions. Extensive simulations show that V-RIS approaches the DoA accuracy of a full-aperture benchmark while producing cleaner spectra than matrix-completion and least-squares baselines. An outdoor prototype further validates the design: with only 25\% programmable elements, V-RIS keeps both elevation and azimuth errors within $1^\circ$ of the ground truth.

</details>


### [122] [Multi-Chirp AFDM for Rydberg Atomic Quantum Receivers: Waveform and Algorithm Design](https://arxiv.org/abs/2607.27903)
*Hanvit Kim,Hyeon Seok Rou,Kihong Min,Giuseppe Thadeu Freitas de Abreu,Sunwoo Kim*

Main category: eess.SP

TL;DR: 提出一种基于Rydberg原子量子接收机(RAQR)的多chirp仿射频分复用(MC-AFDM)方案，用于双弥散信道下的联合时延-多普勒估计，通过多后chirp信号解决经典单chirp AFDM的秩亏问题，将距离和速度估计精度提升两个数量级。

Research relevance: 弱相关（2分）。该论文属于无线通信/量子传感领域，核心是Rydberg原子量子接收机(RAQR)下的MC-AFDM波形设计与时延-多普勒估计，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然"目标检测"在广义上可关联到多目标参数估计，但本文不涉及遥感图像、卫星平台、目标检测网络或任何模型压缩/轻量化技术。唯一可能的间接关联是"多目标时延-多普勒估计"与雷达/遥感中的目标参数估计有概念上的相似性，但技术栈完全不同。

Reading priority: 1分。该论文属于量子无线通信/传感领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。除非对量子传感与雷达目标参数估计的交叉方向感兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：无线通信中的联合时延-多普勒估计（delay-Doppler estimation）；应用场景：量子无线传感（quantum wireless sensing），双弥散信道（doubly-dispersive channel）；传感器类型：Rydberg原子量子接收机（RAQR）；目标类别：多目标（multiple targets）的时延-多普勒参数估计。不涉及遥感图像、SAR、高光谱或多光谱数据。

Model architecture: 提出MC-AFDM（multi-chirp affine frequency division multiplexing）波形方案，采用多个不同的AFDM后chirp信号（post-chirp signals）来克服经典单chirp AFDM（SC-AFDM）的秩亏问题（rank-deficiency problem）。信号处理算法基于正交匹配追踪（OMP）和最小二乘（LS）的序贯处理流程。推导了时延和Doppler估计的理论下界。

Lightweight method: 摘要未说明。本文不涉及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等。其"轻量化"诉求体现在信号处理算法的计算效率（OMP+LS序贯算法），但并非面向深度学习模型或边缘/星上部署的轻量化设计。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关指标。虽然OMP+LS算法相对简单，但本文面向的是量子无线传感场景，而非卫星在轨计算或边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为时延估计精度（range estimation accuracy）和Doppler/速度估计精度（velocity estimation accuracy），通过数值仿真（numerical results）验证。对比方法为经典SC-AFDM（single-chirp AFDM）。

Experiments: 数值结果表明：与经典SC-AFDM相比，MC-AFDM将距离和速度估计精度提升最多两个数量级（up to two orders of magnitude）；通过后chirp优化（post-chirp optimization）可逼近理论下界。分析还表明多个后chirp参数的边缘分布（edge distribution）可通过最小化条件数（condition number）进一步提升估计精度。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关，属于无线通信/量子传感领域；2. 摘要未说明计算复杂度、实时性、硬件实现可行性等部署相关信息；3. 摘要未说明在真实遥感/雷达场景中的验证情况，仅提供数值仿真结果；4. 未涉及任何深度学习模型或模型压缩技术。

Ideas for my research: 借鉴意义有限，但有以下间接启发：1. 多chirp/多波形设计解决秩亏问题的思路，可类比于多尺度/多特征融合策略在遥感小目标检测中的应用——通过多分支或多尺度特征来缓解单一特征表达不足导致的检测性能瓶颈；2. OMP+LS的序贯处理思想可类比于检测中的两阶段/级联策略，先粗定位再精估计；3. "最小化条件数"的优化思想可类比于在轻量化模型设计中通过条件数/谱分析来指导网络结构或量化位宽的选择。总体而言，本文对遥感在轨目标检测轻量化课题的直接可借鉴点很少。

Abstract: We propose a multi-chirp affine frequency division multiplexing (MC-AFDM) scheme for joint delay-Doppler estimation with Rydberg atomic quantum receivers (RAQRs). The work is motivated by the fact that RAQRs, while offering superior sensitivity and advantageous sensing capabilities, suffer from an optical ambiguity due to Doppler shifts in doubly-dispersive (DD) channel caused by target mobility, which precludes the reliable estimation of delay-Doppler parameters. To resolve this optical ambiguity and unleash the potential of RAQRs in DD channel, the proposed MC-AFDM employs multiple distinct AFDM post-chirp signals to overcome the rank-deficiency problem of the classical single-chirp AFDM (SC-AFDM), thereby enabling accurate delay-Doppler estimation of multiple targets. Our analysis reveals that the edge distribution of the multiple post-chirp parameters can further improve estimation accuracy by minimizing the condition number. Building on the proposed MC-AFDM waveform, we design a sequential signal processing algorithm based on orthogonal matching pursuit (OMP) and least squares (LS), and we derive the theoretical lower bounds for delay and Doppler estimation. Numerical results show that the proposed MC-AFDM improves range and velocity estimation accuracy by up to two orders of magnitude compared to the classical SC-AFDM, and approaches its theoretical bounds through post-chirp optimization, validating the quantum-induced advantage of RAQRs for high-resolution quantum wireless sensing.

</details>


### [123] [Power-Efficient XL-MIMO Design for Mixed Near- and Far-Field SWIPT Systems](https://arxiv.org/abs/2607.27992)
*Muhammad Zeeshan Mumtaz,Mohammadali Mohammadi,Hien Quoc Ngo,Hyundong Shin,Michail Matthaiou*

Main category: eess.SP

TL;DR: 本文研究混合近场/远场SWIPT系统中基于混合波束成形的模块化超大规模MIMO阵列的功耗效率优化，提出基于Frobenius范数频率相关的用户分类决策方法，并通过联合优化功率分配与子阵列激活将总功耗降低最高93%。

Research relevance: 与本研究方向基本无关（1分）。论文属于无线通信领域，研究SWIPT（无线信息与功率同传）系统中的XL-MIMO阵列功耗优化，不涉及遥感、卫星对地观测、目标检测或模型轻量化。虽然涉及"功耗优化"和"功率分配"，但这是通信物理层资源分配问题，与深度学习模型压缩/加速完全无关。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于无线通信物理层研究。除非对"动态资源激活降低功耗"这一概念性思想感兴趣，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：无线通信系统的功耗效率优化（power consumption minimization）。应用场景：混合近场/远场SWIPT系统，基于混合波束成形的模块化XL-MIMO阵列。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明深度学习模型结构。涉及的系统架构为：混合波束成形（hybrid beamforming, HB）的模块化超大规模MIMO（XL-MIMO）阵列，包含多个子阵列（subarrays），服务多个信息解码（ID）和能量采集（EH）用户。提出基于最小二乘（LS）信道估计的Frobenius范数频率相关性的用户场区分类决策方法。

Lightweight method: 摘要未涉及模型轻量化方法（无剪枝、量化、知识蒸馏、NAS等）。论文中的"功耗优化"是通过通信层面的功率分配（power allocation, PA）和子阵列激活（subarray activation, SA）实现的，属于通信资源调度优化，而非神经网络模型压缩或加速。

Onboard deployability: 摘要未说明。论文讨论的是通信系统功耗（power consumption），与星上深度学习推理的部署指标（参数量、FLOPs、延迟、显存、能耗）无关。虽然"功耗降低93%"看似与部署相关，但这是通信系统层面的功耗，不是AI推理能耗。

Datasets and metrics: 数据集：不适用（无深度学习数据集）。评价指标：总功耗（overall power consumption, PC）降低百分比。实验设置：仿真实验（simulation results），对比了所提PA-SA-HB方案与等功率全阵列（equal PA with full array, FA）以及PA-FA-HB方案。

Experiments: 仿真结果显示：所提PA-SA-HB方案相比等功率全阵列（equal PA with FA）总功耗降低最高93%，相比PA-FA-HB方案降低最高18%。摘要未提供更多实验细节。

Limitations: 摘要未说明局限性。但明显与本研究方向（遥感卫星在轨目标检测与模型轻量化）无直接关联。该论文属于通信物理层优化，不涉及任何视觉感知、目标检测或深度学习模型压缩内容。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：论文中"子阵列激活（subarray activation）"的思想——即根据任务需求动态激活部分计算/感知资源以降低功耗——在概念上类似于动态推理（dynamic inference）或条件计算（conditional computation），可类比为在轨场景下根据目标稀疏性动态跳过部分网络层或通道以节省能耗。但这种类比非常间接，不建议作为主要参考。

Abstract: This paper examines the power consumption (PC) efficiency of a mixed near- and far-field (MF) simultaneous wireless information and power transfer (SWIPT) system underpinned by a hybrid beamforming (HB)-based modular extra-large multiple-input-multiple output (XL-MIMO) array. Multiple information decoding (ID) and energy harvesting (EH) users are served by multiple constituent subarrays in both the near-field (NF) and far-field (FF) region of the transmit array. A novel decision method is proposed for accurate classification of different field users using Frobenius norm-based frequency correlation of the least square (LS) channel estimates. The NF spatial non-stationarities (SnS) effects entail distinct electromagnetic (EM) visibility regions (VRs), which can be customized to employ strategic activation of the constituent XL-MIMO subarrays. We formulate a two-tier joint optimization problem to minimize the overall PC, considering the power allocation (PA) for both ID and EH users in addition to the subarray activation (SA). This challenging mixed-integer problem is transformed into computationally tractable formulations, accompanied by the development of well-optimized algorithms. Our simulation results demonstrate an overall PC reduction for our proposed PA-SA-HB scheme by up to 93% against the equal PA with full array (FA) and up to 18% with respect to the PA-FA-HB case.

</details>


### [124] [Multi-Agent Reinforcement Learning for Base Station Placement in TDOA-Based Localization](https://arxiv.org/abs/2607.28002)
*Bastian Perner,Pratik Gajanan Raut,Maximilian Lübke,Norman Franchi*

Main category: eess.SP

TL;DR: 提出一种基于ray-tracing辅助的Multi-Agent Reinforcement Learning (MARL)框架，用于5G/6G网络中基站(BS)的布局优化，以提升TDOA定位精度。

Research relevance: 弱相关（2分）。该论文属于无线通信/定位领域，涉及基站布局优化和强化学习，与遥感卫星在轨目标检测及模型轻量化方向基本无关。既不是遥感/卫星场景，也不涉及目标检测，更不涉及模型轻量化或边缘部署。唯一可能的借鉴点是其使用了PPO等强化学习方法和ray-tracing仿真数据生成，但这属于泛机器学习方法，与本课题方向弱相关。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于无线通信定位领域，不建议深入阅读。仅当对强化学习在资源调度中的应用感兴趣时可略作参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：基站(Base Station)布局优化，用于TDOA定位系统。应用场景：5G/6G无线网络、室内外定位。传感器类型：无线通信信道（Channel Impulse Response, CIR）。目标类别：不涉及目标检测，目标是优化基站位置以提升设备定位精度。

Model architecture: 采用Multi-Agent Reinforcement Learning (MARL)框架，每个agent使用Proximal Policy Optimization (PPO)算法，各agent协同放置一个基站。奖励函数为定位精度与覆盖率的加权组合。输入为ray-tracing生成的Channel Impulse Responses (CIRs)。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，不报告参数量、FLOPs、延迟、显存或能耗等部署指标。

Datasets and metrics: 数据集：基于某大学校园的详细3D模型，通过ray-tracing生成CIR数据，在5个具有不同传播特性的校园区域段上评估。评价指标：定位Mean Absolute Error (MAE)、覆盖率，对比基线为基于GDOP的几何方法（mean-optimized geometric baseline）。

Experiments: 实验结果显示：所学习的策略在定位精度上与基于GDOP的几何方法相当，平均MAE相对更强的几何基线降低约3%；在个别区域段上提升明显（最高约14%），在其他区域段上误差相当或略高。行为具有区域依赖性。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于无线通信定位领域。2. 方法性能提升不稳定，呈区域依赖性，部分区域误差反而略高。3. 摘要未说明模型复杂度、训练成本、推理效率等关键信息。4. 仅在小范围校园场景验证，未涉及大规模或真实部署。

Ideas for my research: 1. 尽管场景不同，其"环境感知+仿真数据驱动"的思路可借鉴——类似地，遥感在轨目标检测可利用仿真/合成遥感数据（如合成SAR图像）增强训练。2. Multi-Agent协同优化的框架思想，可类比多卫星协同在轨推理或分布式边缘计算中的资源分配问题。3. PPO等强化学习方法可用于在轨模型的动态资源调度或自适应推理策略选择。但整体借鉴价值有限。

Abstract: Accurate localization of devices is a key capability for emerging 5G and 6G networks and depends on effective base station (BS) placement. Conventional geometry-based approaches such as Geometric Dilution of Precision (GDOP) ignore realistic propagation effects such as Non-Line of Sight (NLOS) shadowing and multipath-induced Time of Arrival (TOA) bias caused by buildings. This paper proposes a ray-tracing-assisted Multi-Agent Reinforcement Learning (MARL) framework for environment-aware BS placement in Time Difference of Arrival (TDOA) localization systems. Proximal Policy Optimization (PPO) agents are trained on Channel Impulse Responses (CIRs) generated from a detailed 3D model of a university campus. Each agent cooperatively places one BS while optimizing a shared reward that combines localization accuracy and coverage. The approach is evaluated on five campus segments with varying propagation characteristics. Results show that the learned policy achieves localization accuracy comparable to conventional GDOP-based placement, lowering the average localization Mean Absolute Error (MAE) by about 3 % relative to the stronger (mean-optimized) geometric baseline. The behavior is segment-dependent, with a clear improvement on individual segments (up to about 14 %) and comparable or slightly higher error on the others. These findings indicate that incorporating site-specific propagation data into the placement process can match and selectively improve upon purely geometric strategies, motivating further work toward consistent gains.

</details>


### [125] [A Stochastic Optimization Framework for RIS-Aided Wireless Network Design](https://arxiv.org/abs/2607.28018)
*Davide Gagliardi,Alessio Zappone,Domenico Ciuonzo,Marco Di Renzo*

Main category: eess.SP

TL;DR: 提出基于连续交叉熵和Metropolis-Hastings的随机优化框架，用于可重构智能表面(RIS)辅助无线网络的配置优化，在保持性能的同时将执行时间最多降低10倍。

Research relevance: 弱相关（2分）。论文主题是RIS辅助无线通信网络的随机优化，属于无线通信/信号处理领域，与遥感、卫星在轨目标检测、轻量化模型均无直接关联。唯一的间接联系是"降低执行时间"这一计算效率优化目标，以及优化方法本身可能对大规模非凸优化问题有方法论上的借鉴意义，但并非遥感场景，也不涉及目标检测或模型压缩。

Reading priority: 1分。论文主题为RIS无线通信优化，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在执行时间优化的方法论上有极弱的间接借鉴意义，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：RIS辅助无线网络的配置优化（achievable-rate maximization和energy-efficiency maximization）。应用场景：未来无线通信网络（6G等）。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体网络结构。核心是随机优化框架，包含两种算法：(a) 连续版本的cross-entropy (CE)方法；(b) 连续版本的Metropolis-Hastings (MH)方法。框架直接处理连续变量，可通过relaxation和projection扩展到离散设置。应用于近无源RIS（nearly-passive RISs）和有源RIS（active RISs）两种架构。

Lightweight method: 摘要未涉及模型压缩方法（pruning, quantization, knowledge distillation, NAS等均未提及）。论文的"轻量化"体现在算法层面：通过随机优化替代确定性优化，将执行时间（execution time）最多降低10倍，属于计算效率优化而非模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。仅提及执行时间最多降低10倍，但未给出具体硬件平台、延迟数值或实时性分析。不涉及星上部署、FPGA/NPU/TensorRT等边缘部署方案。

Datasets and metrics: 摘要未说明具体数据集。评价指标包括：achievable rate（可达速率）和energy efficiency（能量效率），以及执行时间（execution time）。实验为数值仿真（numerical results），非真实遥感数据。

Experiments: 数值结果表明：所提出的CE和MH随机优化方法在achievable-rate和energy-efficiency两个任务上，性能与state-of-the-art确定性算法相当或更优，同时执行时间最多降低10倍。摘要未给出具体对比方法名称和详细数值。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于无线通信领域。2. 摘要未说明算法在更大规模问题上的可扩展性。3. 未提供具体硬件实现细节和实际部署验证。4. 随机优化方法的收敛速度与问题规模的关系未详细说明。5. 未涉及任何视觉或遥感数据处理。

Ideas for my research: 1. 方法论借鉴：CE和MH这类随机优化方法在处理大规模非凸优化问题时具有计算效率优势，如果未来在轨目标检测涉及模型结构搜索（NAS）或超参数优化等非凸问题，可考虑引入随机优化策略。2. 计算效率思路：论文展示了"以随机近似替代确定性精确求解"来换取10倍速度提升的思路，这与轻量化中"以精度换速度"的trade-off哲学有相通之处。3. 但整体而言，该论文对遥感在轨目标检测轻量化课题的直接借鉴价值有限，不建议作为核心参考。

Abstract: Reconfigurable intelligent surfaces (RISs) are a promising technology for improving the spectral and energy efficiency of future wireless networks, which make use of metasurfaces. However, optimizing RIS configurations typically leads to large-scale, non-convex problems whose complexity grows significantly with the number of scattering elements and the adoption of advanced metasurface architectures. In this paper, we develop a stochastic optimization framework for RIS-aided wireless networks based on continuous versions of the ($a$) cross-entropy (CE) and ($b$) Metropolis-Hastings (MH) methods. Unlike existing stochastic approaches that mainly focus on discrete optimization, the proposed framework directly handles continuous variables and can be readily applied to discrete settings through relaxation and projection. We provide a theoretical characterization of the proposed algorithms, including convergence guarantees and efficiency analysis. The framework is applied to ($i$) achievable-rate maximization with nearly-passive RISs and ($ii$) energy-efficiency maximization with active RISs. Numerical results show that the proposed methods achieve performance comparable to, or better than, state-of-the-art deterministic algorithms, while reducing execution times up to 10 times in representative scenarios.

</details>


### [126] [Fractional Doppler Effects on OTFS-NOMA HetNets with Mixed-Mobility Users](https://arxiv.org/abs/2607.28059)
*Wafa Hedhly,Leila Musavian,Nikolaos Thomos*

Main category: eess.SP

TL;DR: 本文研究6G异构网络中OTFS-NOMA系统在混合移动性条件下的分数多普勒效应及其对检测性能的影响，并分析NOMA功率分配优化对缓解多普勒间干扰的作用。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于6G车载异构网络的OTFS-NOMA无线通信系统，涉及分数多普勒效应、多普勒间干扰（IDI）和功率分配优化，属于无线通信物理层领域，与遥感、卫星在轨目标检测、轻量化部署均无直接关联。

Reading priority: 1分。论文属于无线通信物理层方向，与遥感卫星在轨目标检测和模型轻量化课题无直接关联，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务：分析OTFS-NOMA系统在混合移动性（high-mobility/low-mobility）条件下的分数多普勒效应及功率分配优化。场景：6G车载异构网络（HetNets）。传感器类型：不适用（无线通信）。目标类别：不适用。

Model architecture: 系统采用OTFS调制（用于高移动性用户）与NOMA（非正交多址）结合，低移动性用户符号嵌入时频（TF）域。检测采用最小均方误差（MMSE）检测器，在多种关于IDI参数已知性的假设下进行评估。

Lightweight method: 摘要未说明。本文不涉及模型压缩或加速方法，无剪枝、量化、知识蒸馏、NAS等轻量化技术。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集。评价指标涉及频谱效率（spectral efficiency）和服务的用户数量（number of users served）。

Experiments: 摘要未说明具体实验细节。研究验证了分数多普勒对系统性能的显著影响，并展示了功率分配在缓解IDI效应方面的作用。

Limitations: 本文明确说明其目的是分析分数多普勒对OTFS-NOMA系统的影响，而非提出新的OTFS检测算法。摘要未提供具体数值结果、复杂度分析或与现有方法的定量对比。

Ideas for my research: 与遥感在轨目标检测及轻量化课题基本无可借鉴点。唯一可能的间接联系是：OTFS调制在卫星通信中也有应用前景，但本文聚焦地面车载网络，且不涉及目标检测或模型压缩，借鉴价值极低。

Abstract: Heterogeneous networks (HetNets) are considered a promising approach to meet the increasing throughput requirements of 6G vehicular networks. The integration of orthogonal time frequency space (OTFS) modulation and non-orthogonal multiple access (NOMA) has demonstrated significant improvements in the reliability of wireless networks under mixed-mobility. Applying this combination in HetNets helps accommodate high-mobility (HM) and low-mobility (LM) users while effectively addressing high Doppler shifts. However, in practical scenarios, fractional Doppler arises from the mismatch between the actual Doppler frequency and its discrete representation on the DD grid. This effect leads to inter-Doppler interference (IDI) that can significantly degrade detection performance. In our work, we investigate the effect of fractional Doppler on OTFS-NOMA systems under mixed-mobility conditions and analyze how NOMA power allocation can be optimized to mitigate the resulting performance degradation. OTFS modulation is used for the HM user, while the LM users' symbols are embedded in the time-frequency (TF) domain. Minimum mean square error (MMSE) detection is utilized under multiple assumptions regarding the knowledge of the IDI parameters. We investigate the impact of optimizing NOMA power allocation on the overall system performance. The purpose of this work is to analyze the impact of fractional Doppler in OTFS-NOMA systems in HetNets rather than proposing new detection algorithms for OTFS modulation. The results validate the significant performance impact of fractional Doppler on the system and demonstrate the role of power allocation in mitigating IDI effects in terms of spectral efficiency and number of users served.

</details>


### [127] [Beamforming and Phase Shift Design for STAR-RIS Assisted Secure Sensing and Communication in ISAC Systems](https://arxiv.org/abs/2607.28081)
*Haijun Zhang,Shuqing Wu,Xiaoqi Zhang,Zijun Wu,Xu Ma,Yuzheng Ren*

Main category: eess.SP

TL;DR: 提出一种基于STAR-RIS的ISAC框架，通过联合优化发射波束成形和STAR-RIS相移，在感知点目标（窃听者）与安全通信之间取得平衡，采用BCD混合算法求解。

Research relevance: 弱相关（1-2分）。论文属于无线通信领域，研究ISAC（通感一体化）与STAR-RIS（可重构智能超表面）的联合优化，目标是安全通信与点目标感知的折中。虽然涉及"目标检测"（point target detection）和"感知"（sensing），但这里的"目标"是通信场景中的窃听者，并非遥感/卫星图像中的地物目标；完全不涉及遥感图像、卫星观测、aerial image、SAR、高光谱等遥感数据；也不涉及任何模型轻量化、剪枝、量化、知识蒸馏、边缘部署等压缩加速方法。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。论文属于无线通信/ISAC领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感数据、目标检测模型或任何轻量化技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：ISAC（Integrated Sensing and Communication）通感一体化中的安全通信与点目标感知联合优化。应用场景：未来无线网络（future wireless networks），非遥感/卫星场景。传感器类型：多天线基站（base station with multiple antennas）与STAR-RIS，非遥感成像传感器。目标类别：点目标（point target），被视作窃听者（eavesdropper），非遥感地物目标。

Model architecture: 系统架构：多天线基站 + STAR-RIS（同时透射和反射可重构智能超表面）+ 单天线用户。优化变量：发射波束成形（transmission beamforming）和STAR-RIS相移（phase shifts）。算法框架：混合BCD（Block Coordinate Descent）算法，交替更新波束成形和STAR-RIS相移，结合SCA（successive convex approximation）、PDD（penalty dual decomposition）和PGM（projected gradient method）。

Lightweight method: 摘要未说明。论文不涉及任何模型轻量化方法（pruning, quantization, knowledge distillation, NAS等），也不涉及神经网络模型压缩或加速。其"优化"是针对通信系统参数的数学优化，而非深度学习模型的轻量化。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。该工作属于通信系统层面的资源分配优化，与在轨部署无关。

Datasets and metrics: 评价指标：CRB（Cramer-Rao bound）用于评估点目标（窃听者）的感知精度；secrecy rate（保密速率）用于量化通信链路的安全水平。数据集：摘要未说明（该工作为通信系统优化问题，通常不涉及标准数据集）。

Experiments: 摘要未说明具体实验结果。仅描述了所提出的联合优化问题及求解算法（BCD + SCA + PDD + PGM），未给出数值仿真结果、对比方法或消融实验。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，属于无线通信与信号处理领域。2. 摘要未说明算法复杂度、收敛性分析、仿真场景设置等关键细节。3. 点目标感知与遥感图像中的目标检测（object detection）在问题定义、数据形式、方法范式上完全不同。4. 不涉及任何深度学习模型，因此对模型轻量化方向无直接借鉴。

Ideas for my research: 1. 该论文与本人课题方向基本无关，不建议作为核心参考。2. 唯一可能的间接启发：ISAC中"感知-通信折中"的联合优化思想，若未来研究涉及"遥感感知与星地通信一体化"（如卫星在轨感知与下行传输的联合优化），可借鉴其多目标权衡与交替优化框架。3. STAR-RIS在通信中的相移优化思路，若未来涉及"可重构智能表面辅助的遥感信号增强"（如SAR信号增强），可能有微弱参考价值，但属于通信物理层范畴，与深度学习目标检测模型轻量化无直接关系。

Abstract: Integrated sensing and communication(ISAC), as a rapidly advancing technique, introduces a fresh approach for achieving secure communication and intelligent sensing for future wireless networks. An ISAC framework empowered by simultaneously transmitting and reflecting reconfigurable intelligent surfaces(STAR-RIS) is explored in this paper, where a base station equipped with multiple antennas establishes wireless links to users each with a single antenna during the detection of a point target. The point target, regarded as an eavesdropper, trying to intercept users' information. Cramer-Rao bound(CRB) serves as evaluation criterion to assess sensing accuracy of point eavesdropper, whereas the secrecy rate is employed to quantify the security level of the communication link. To optimize sensing-communication tradeoff, a joint optimization problem is constructed. To approach the formulated problem, a hybrid Block Coordinate Descent(BCD)-based algorithm is developed, which alternately updates the transmission beamforming and STAR-RIS phase shifts, using successive convex approximation(SCA) technique, penalty dual decomposition (PDD) framework and projected gradient method(PGM).

</details>


### [128] [When Linear RUL Labels Disagree with Vibration Degradation: A Stage-Aware Target and Dual-Scale Predictor Evaluated on XJTU-SY and IMS](https://arxiv.org/abs/2607.28115)
*Behrad Mousaei Shir-Mohammad,Seyed Reza Tavakoli,Mohammad Mohammadi,Siavash Ahmadi,Babak Hossein Khalaj,Mohammad Hossein Rohban*

Main category: eess.SP

TL;DR: 本文提出一种面向轴承剩余使用寿命(RUL)预测的"目标设计-预测分离"框架，通过构建有向振动健康指标和分段线性-二次-指数退化目标，结合CNN-LSTM与Transformer的OWA融合预测器，在XJTU-SY和IMS数据集上验证了阶段感知目标优于固定线性标签。

Research relevance: 弱相关（2分）。本文属于工业设备健康管理与剩余寿命预测（prognostics and health management, PHM）领域，研究对象是轴承振动信号，与遥感卫星、Earth observation、aerial image、SAR、高光谱/多光谱等遥感场景完全无关。虽然使用了CNN-LSTM和Transformer等深度学习模型，但属于泛时序预测方法，与目标检测（object detection）和模型轻量化（pruning/quantization/distillation）均无直接关联。其借鉴价值仅在于"目标设计（target design）与预测分离"的方法论思想，以及轻量级CNN-LSTM+Transformer的融合架构设计。

Reading priority: 1分。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于工业轴承RUL预测领域。仅方法论层面的"目标设计分离"和"紧凑CNN-LSTM+Transformer融合"思想有微弱借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：剩余使用寿命（Remaining Useful Life, RUL）回归预测；应用场景：工业轴承退化监测与预测性维护；传感器类型：振动传感器（vibration sensor）；目标类别：轴承退化状态（非目标检测任务）。

Model architecture: 采用紧凑型CNN-LSTM与Transformer双分支架构，通过验证集拟合的Ordered Weighted Averaging (OWA)融合两个分支的输出。CNN-LSTM用于提取因果特征序列的局部时序模式，Transformer用于捕获长程依赖关系。目标侧采用分段线性-二次-指数（linear-quadratic-exponential）连续退化状态目标函数。

Lightweight method: 摘要提及"compact CNN-LSTM"（紧凑型CNN-LSTM），暗示模型规模较小，但未说明具体的轻量化手段（如pruning、quantization、knowledge distillation、NAS等）。摘要未说明参数量、FLOPs、推理延迟等部署指标。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。虽然模型标注为"compact"，但缺乏量化证据支持其在边缘或星上部署的可行性。

Datasets and metrics: 数据集：XJTU-SY轴承数据集（留出所有以5结尾的轴承作为hold-out验证）、IMS轴承数据集（3条官方失效轨迹）。评价指标：RMSE（0.0608）、MAE（0.0392）、R-squared（0.9617）、BIC差异（128.8-368.1）、moving-block bootstrap置信区间。

Experiments: 在XJTU-SY轴承级hold-out上，融合预测器取得RMSE 0.0608、MAE 0.0392、R² 0.9617，其中Transformer贡献了大部分精度。在IMS三条失效轨迹上，阶段感知曲线相比最佳锚定线性拟合将RMSE降低3.6-18.2%、MAE降低3.1-31.1%，平均降低10.2%和15.0%。BIC差异支持阶段感知表示，但moving-block bootstrap区间跨越零，证据仍属描述性。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于工业PHM领域；2. 摘要明确承认"证据仍属描述性"，仅3条官方IMS轨迹可用，缺乏统计显著性；3. 未提供模型复杂度、推理速度等部署指标；4. 作者声明该框架是"面向测量的目标有效性框架"，而非普适的物理失效时间规律或跨域鲁棒预测方法。

Ideas for my research: 1. "目标设计（target design）与预测分离"的方法论可迁移到遥感目标检测中——例如在旋转框检测中，可将角度回归目标设计为阶段感知的连续表示，而非固定线性标签，可能提升小目标/密集目标的回归精度；2. CNN-LSTM与Transformer的OWA加权融合策略可借鉴到遥感轻量化检测中，用轻量CNN提取局部特征、Transformer捕获全局上下文，再通过可学习权重融合，兼顾精度与效率；3. 其"开发-验证分离"的严格数据划分（hold-out）思想值得借鉴，避免遥感数据集上的数据泄漏问题。

Abstract: Remaining useful life (RUL) studies commonly treat the label as fixed, although clock-linear labels may decline while measured vibration remains nearly stable and then changes rapidly near failure. We separate target design from prediction. A development-only pipeline constructs an oriented vibration health indicator, identifies chronological early, middle, and late stages, and fits a continuous linear-quadratic-exponential degradation-state target. A compact CNN-LSTM and Transformer learn the target from causal feature sequences, and validation-fitted Ordered Weighted Averaging combines their outputs. In a bearing-wise XJTU-SY hold-out, all bearings ending in 5 are excluded from fitted preprocessing, training, early stopping, and fusion. The fused predictor obtains an RMSE of 0.0608, an MAE of 0.0392, and an R-squared value of 0.9617, with the Transformer providing most of the accuracy. Target shape is assessed independently on three documented IMS failed-bearing trajectories. Against the best anchored linear fit to the same vibration-derived reference, the stage-aware curve reduces RMSE by 3.6-18.2% and MAE by 3.1-31.1%; the mean reductions are 10.2% and 15.0%, respectively. Conservative BIC differences of 128.8-368.1 favor the stage-aware representation, whereas moving-block bootstrap intervals cross zero. Thus, stage-dependent targets better describe the evaluated vibration-derived degradation states, but the evidence remains descriptive because only three official IMS runs are available. The study establishes a measurement-oriented target-validity framework, not a universal nonlinear law for physical time-to-failure or robust cross-domain prediction.

</details>


### [129] [Reduced-Observation Approximation of Near-Field Gaussian Covariance Matrices](https://arxiv.org/abs/2607.28201)
*Marco Moretti*

Main category: eess.SP

TL;DR: 提出一种低复杂度的二维近场高斯协方差近似框架，通过降维观测表示避免全矩阵特征分解，并引入自校准无参考谱误差估计器，实现复杂度大幅降低。

Research relevance: 弱相关（1-2分）。论文主题是近场阵列信号处理中的协方差矩阵近似与低复杂度计算，属于通信/阵列信号处理领域，与遥感、卫星、目标检测、轻量化模型均无直接关联。虽然涉及"低复杂度"和"复杂度降低"的概念，但这是数值计算层面的复杂度优化，而非深度学习模型压缩或边缘部署。与"遥感卫星在轨目标检测模型轻量化"方向基本无关。

Reading priority: 1分。论文属于阵列信号处理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：近场高斯协方差矩阵近似（near-field Gaussian covariance approximation）。应用场景：大孔径阵列（large-aperture arrays）中的定位（localization）、协方差感知估计（covariance-aware estimation）、线性MMSE滤波。传感器类型：阵列天线（非遥感传感器）。目标类别：不涉及目标检测。

Model architecture: 摘要未说明具体网络结构。核心方法为：将正交协方差写为 RQ = HHH 形式，从降维观测表示（reduced observation representation）获取主导协方差谱（dominant covariance spectrum），避免全 M×M 特征分解（eigendecomposition）。属于信号处理/数值线性代数方法，非深度学习模型。

Lightweight method: 摘要中的"低复杂度"（low-complexity）指数值计算层面的复杂度降低——通过降维表示避免全矩阵特征分解，以及自校准无参考谱误差估计器（self-calibrated non-reference spectral-error estimator）。这与深度学习模型压缩中的剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）等轻量化方法完全不同。

Onboard deployability: 摘要未说明部署相关信息。未提及参数量、FLOPs、延迟、显存、能耗、实时性等指标。虽然声称"substantial complexity reduction"，但未给出具体数值。

Datasets and metrics: 摘要未说明具体数据集。评价指标为收敛跟踪精度（accurate convergence tracking）和复杂度降低程度（complexity reduction）。无标准目标检测数据集（如DOTA、DIOR、NWPU VHR-10等）或指标（mAP、FPS等）。

Experiments: 摘要仅提及"数值结果表明准确的收敛跟踪和显著的复杂度降低"（Numerical results show accurate convergence tracking and substantial complexity reduction），未给出具体实验对比方法、消融实验或定量结果。

Limitations: 摘要未说明局限性。主要缺口包括：1）未给出复杂度降低的具体量化数值；2）未与现有近场协方差近似方法进行详细对比；3）未讨论该方法在遥感或目标检测场景中的适用性；4）属于信号处理领域，与深度学习目标检测模型轻量化无直接关联。

Ideas for my research: 借鉴意义有限。可能的间接启发：1）"降维表示避免全矩阵分解"的思想在数学上与模型压缩中的低秩分解（low-rank factorization）有相似之处，可类比思考如何对遥感检测模型中的权重矩阵或特征图做低秩近似；2）"自校准无参考估计器"的思路可类比无标签/无GT条件下的模型评估，但与本课题关联较弱。总体而言，不建议作为主要参考。

Abstract: Near-field covariance matrices are central to local- ization, covariance-aware estimation, and linear MMSE filtering in large-aperture arrays, but Gaussian position uncertainty requires costly numerical averaging of nonlinear spherical-wave steering vectors. This letter proposes a low-complexity framework for two-dimensional near-field Gaussian covariance approxima- tion. By writing the quadrature covariance as RQ = HHH ,
the dominant covariance spectrum is obtained from a reduced observation representation, avoiding full MxM eigendecom- position. A self-calibrated non-reference spectral-error estimator is further introduced using only grid-to-grid dominant-spectrum differences. Numerical results show accurate convergence track- ing and substantial complexity reduction.

</details>


### [130] [CRLB-Driven Beamforming and Power Allocation for Multi-BS Cooperative ISAC Networks](https://arxiv.org/abs/2607.28224)
*Yanpeng Su,Maximilian Lübke,Mengyu Zhang,Norman Franchi*

Main category: eess.SP

TL;DR: 提出一种基于Cramér-Rao下界(CRLB)驱动的波束成形(BF)与功率分配(PA)框架，用于协作式集成感知与通信(ISAC)网络中的多站目标估计。

Research relevance: 弱相关（2分）。论文属于无线通信与感知一体化(ISAC)领域，涉及多天线基站协作进行目标估计，与遥感卫星在轨目标检测和模型轻量化方向基本无关。虽然涉及"目标估计"和"感知"，但这是通信信号处理层面的目标定位/测速，而非遥感图像中的目标检测。论文中的"轻量化"体现在算法复杂度优化（两阶段PA算法降低计算复杂度），但并非深度学习模型压缩/加速范畴。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于ISAC无线通信信号处理领域。除非对CRLB理论界或复杂度-性能权衡方法论有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：集成感知与通信(ISAC)网络中的多站目标估计（位置与速度估计）。应用场景：多天线基站(BS)协作通信与感知一体化网络。传感器类型：多天线无线通信基站（非遥感/卫星传感器）。目标类别：多用户通信目标及多站感知目标（非遥感图像中的舰船/飞机/车辆等）。

Model architecture: 摘要未说明深度学习模型结构。方法框架包括：(1) 基于半定规划(SDP)的波束成形(BF)设计，利用PEB和VEB约束，通过双重Schur补处理，采用半定松弛(SDR)放松秩1约束；(2) 两阶段功率分配(PA)算法，通信波束用正则化迫零(RZF)生成，感知波束用零空间投影(NSP)生成，通信与感知PA通过二阶锥规划(SOCP)顺序求解。

Lightweight method: 论文中的"轻量化"并非深度学习模型压缩（剪枝/量化/蒸馏/NAS），而是信号处理算法层面的复杂度优化：提出两阶段PA算法替代联合BF优化，牺牲部分自由度以降低计算复杂度，执行时间约25ms。这与遥感目标检测模型的轻量化（参数量、FLOPs、延迟优化）完全不同范畴。

Onboard deployability: 摘要未说明星上/边缘部署相关信息。仅提及两阶段PA算法执行时间约25ms，适用于动态环境。未涉及参数量、FLOPs、显存、能耗等部署指标。

Datasets and metrics: 摘要未说明具体数据集。评价指标为位置误差界(PEB)和速度误差界(VEB)，以及BF与PA算法的性能对比和仿真执行时间。实验为仿真验证，未涉及真实遥感数据集。

Experiments: 摘要未说明详细实验设置。主要结论：两阶段PA算法相比BF在性能上仅有微小差距（slight gap），但执行时间约25ms，表明其在动态环境中的适用性。未提供与现有方法的详细对比表格或消融实验。

Limitations: (1) 与遥感/卫星/在轨目标检测完全无关，属于无线通信信号处理领域；(2) 摘要未说明具体仿真场景参数、基站数量、用户数量等关键实验设置；(3) 未涉及任何深度学习模型或遥感图像处理；(4) 两阶段PA算法牺牲自由度，性能与BF存在差距，但差距大小未量化说明；(5) 摘要未说明算法在真实硬件平台上的部署验证。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的直接借鉴意义有限，但有以下间接启发：(1) CRLB/PEB/VEB作为感知精度的理论下界度量思想，可类比用于遥感检测任务中评估检测精度的理论极限，辅助判断轻量化带来的精度损失是否在可接受范围内；(2) 两阶段"先优化再简化"的思路——先求解高精度复杂方案，再设计低复杂度近似方案并量化性能差距，可类比于"先训练大模型再蒸馏/剪枝到轻量模型"的范式；(3) 在复杂度与性能之间做权衡分析的方法论值得借鉴，但需将度量指标从PEB/VEB替换为遥感检测的mAP、IoU等。

Abstract: This paper presents a Cramér-Rao lower bound (CRLB)-driven beamforming (BF) and power allocation (PA) framework for cooperative integrated sensing and communication (ISAC) networks, where a set of multi-antenna base stations (BSs) jointly serve multiple users and simultaneously perform multi-static target estimation. In our design, we investigate how the position error bound (PEB) and velocity error bound (VEB) can be exploited and incorporated into BF and PA optimization. The design leveraging PEB and VEB as metrics directly characterizes sensing accuracy. First, we propose a semidefinite programming (SDP)-based BF, where the PEB and VEB constraints are handled by double Schur complements, and the rank-1 constraints of the BF covariance matrices are relaxed by semidefinite relaxation (SDR), whose tightness is proved using the Karush-Kuhn-Tucker (KKT) conditions. Addressing the complexity, we further develop a two-stage PA algorithm, where the communication and sensing beams are formed by the regularized zero-forcing (RZF) and null-space projection (NSP) methods, respectively, and the communication and sensing PAs are solved sequentially by second-order cone programming (SOCP). Although the PA algorithm sacrifices the degrees of freedom, its performance shows a slight gap compared to BF in the simulation, while the execution time of around 25 ms demonstrates its applicability in dynamic environments.

</details>


### [131] [Improved Frequency Tracking with Adaptive Moments for Narrowband Interference Mitigation in GNSS](https://arxiv.org/abs/2607.28395)
*Burak Soner,Abdulkadir Uzun,Ekin Uzun*

Main category: eess.SP

TL;DR: 提出一种受Adam优化算法启发的自适应陷波滤波器（ANF）频率跟踪算法，用于抑制GNSS接收机中的个人隐私设备（PPD）干扰信号，在动态跟踪性能与稳态估计抖动之间取得更优权衡。

Research relevance: 弱相关（2分）。论文涉及信号处理领域的GNSS干扰抑制，与遥感卫星目标检测及轻量化方向基本无关。虽然文中提到了quantization、resource utilization、latency等部署相关概念，但应用场景是GNSS接收机干扰消除，而非遥感图像目标检测。唯一可借鉴的点在于其受Adam优化启发的算法设计思路以及量化/延迟分析的方法论，但这些与遥感目标检测模型轻量化没有直接关联。

Reading priority: 1分。该论文属于GNSS信号处理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然涉及quantization和资源分析，但应用场景和任务类型完全不同，对课题的直接借鉴价值很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：GNSS接收机干扰抑制（interference mitigation），具体为对个人隐私设备（PPD）发射的扫频窄带干扰信号进行频率跟踪与切除。应用场景：GNSS接收机抗干扰。传感器类型：GNSS射频信号（非遥感图像）。目标类别：干扰信号（非目标检测任务）。

Model architecture: 提出一种基于Adam优化思想的自适应陷波滤波器（ANF）频率跟踪算法，利用复数一阶IIR陷波滤波器在输出功率最小化目标下的梯度一阶矩和二阶矩来更新频率估计。与现有ANF更新规则和基于频率锁定环（FLL）的方法进行对比。

Lightweight method: 摘要提及对资源利用（resource utilization）和延迟（latency）进行了分析，并量化了量化（quantization）和流水线延迟（pipeline delays）对方法的影响。但这不是针对神经网络模型的轻量化，而是针对信号处理算法的硬件实现资源分析。与剪枝、蒸馏、NAS等模型压缩方法无关。

Onboard deployability: 摘要提到分析了资源利用率和延迟，并量化了量化和流水线延迟的影响。提出的Adam-based方法相比vanilla ANF资源占用略高（marginally higher resource usage），但提供了更好的干扰抑制性能。然而，这是针对GNSS接收机硬件实现的分析，而非星上遥感目标检测模型的部署分析。具体参数量、FLOPs、能耗等指标摘要未说明。

Datasets and metrics: 使用了模拟和记录的干扰事件数据，包括公开数据集。评价指标包括：动态跟踪性能（尤其针对快速扫频干扰）、稳态估计抖动（steady-state estimation jitter）、干扰抑制效果、资源利用率、延迟。具体数据集名称和指标数值摘要未说明。

Experiments: 在广泛的模拟和记录干扰事件上进行了性能分析，与现有方法（包括FLL-based设计）进行了基准对比。结果显示Adam-based方法在大多数场景下提供更好的干扰抑制，但资源占用略高于vanilla ANF。具体数值结果摘要未说明。

Limitations: 1. 与遥感卫星目标检测完全无关，属于GNSS信号处理领域；2. 摘要未提供具体性能数值、资源占用数据、延迟数据；3. 未涉及任何目标检测模型、遥感图像或轻量化网络设计；4. 虽然提到quantization，但针对的是信号处理算法而非深度学习模型。

Ideas for my research: 1. 借鉴其"受Adam优化启发"的算法设计思路——在轻量化目标检测模型的训练或推理优化中，可以考虑将优化器思想引入网络结构设计；2. 其量化与延迟分析的方法论（评估量化对性能的影响、流水线延迟分析）可以迁移到星上目标检测模型的部署评估中；3. 其"跟踪速度与方差权衡"的分析框架，可类比为目标检测中"推理速度与精度"的权衡分析。但整体借鉴价值有限。

Abstract: Personal privacy devices (PPDs) typically emit strong tones or swept narrowband signals to jam nearby GNSS receivers and deliberately cause loss of lock. Excision methods deployed on receivers mitigate such interferers by tracking their instantaneous frequency and removing those components in either the time domain (e.g., notch filtering) or a transform domain (e.g., Fourier-domain excision). For effective mitigation without degrading the GNSS signal, the excision location must be precise; misplacement can sometimes harm performance even more than leaving the interferer unmitigated. Trackers are therefore evaluated for both dynamic tracking performance, especially against fast-sweeping jammers, as well as steady-state estimation jitter, reflecting a fundamental tracking speed versus variance trade-off. We propose a new frequency tracking algorithm that provides a better trade-off than existing methods using first and second moments of the gradient for a complex first-order IIR notch filter under a standard output power minimization objective. We first describe the algorithm, which is inspired by the Adam optimization method widely used for optimizing neural network parameters, and its relation to existing adaptive notch filter (ANF) update rules. Next, we analyze performance over a wide range of simulated and recorded interference events, including publicly available datasets, and benchmark against state-of-the-art methods including frequency-locked-loop (FLL) based designs. Finally, we analyze resource utilization and latency, and quantify the effects of quantization and pipeline delays on the proposed method and on representative state-of-the-art baselines. Our proposed Adam-based method shows marginally higher resource usage than a vanilla ANF while providing better suppression under most scenarios.

</details>


### [132] [Real-time Pre-Correlation GNSS Interference Classification with Lightweight Learned Algorithms](https://arxiv.org/abs/2607.28404)
*Burak Soner,Can Aksoy,Erkin Halaçlı*

Main category: eess.SP

TL;DR: 本文提出面向GNSS射频干扰(RFI)分类的轻量级算法，利用虚拟自适应陷波滤波器(ANF)的瞬时频率预测构造轻量输入特征，并结合梯度提升决策树等紧凑分类器，在资源受限的嵌入式平台上实现实时RFI分类。

Research relevance: 弱相关（2分）。本文涉及"轻量化算法"和"资源受限嵌入式平台实时推理"，与轻量化部署方向有一定方法学借鉴意义；但任务场景是GNSS射频干扰分类，并非遥感/卫星目标检测，不涉及object detection、remote sensing、aerial image、SAR、高光谱等遥感内容。属于泛信号处理/泛机器学习方法，与"遥感卫星在轨目标检测模型轻量化"课题相关性较弱。

Reading priority: 2分。本文与遥感卫星在轨目标检测方向相关性弱，核心价值在于"资源受限平台上的轻量算法设计"这一方法论层面的借鉴，但任务场景（GNSS RFI分类）和模型类型（GBDT等传统ML）与遥感目标检测差异较大，仅建议快速浏览以获取轻量化部署评估思路。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：GNSS射频干扰(RFI)分类（pre-correlation RFI classification），用于在运行中选择合适的干扰抑制对策。应用场景：安全关键应用中的GNSS接收机，部署在资源受限的嵌入式平台（CRPA平台，EDGE Microwave HEDGE8008）。传感器类型：GNSS射频信号（非遥感成像传感器）。目标类别：窄带和宽带非平稳RFI干扰类型。摘要未说明具体类别数量。

Model architecture: 核心方法：1) 从虚拟自适应陷波滤波器(ANF)的瞬时频率预测中提取新的轻量输入特征；2) 将这些新特征与文献中已有的频谱特征结合；3) 使用紧凑的learned classifiers，如gradient-boosted decision trees (GBDT)进行RFI分类。摘要未说明具体网络结构或深度模型细节。

Lightweight method: 轻量化主要体现在：1) 特征层面轻量化——设计轻量输入特征（基于ANF瞬时频率预测），而非高维原始信号特征；2) 模型层面轻量化——采用紧凑的learned classifiers（如gradient-boosted decision trees）而非深度神经网络，以满足tight compute and memory budgets；3) 部署层面——在紧凑CRPA平台(EDGE Microwave HEDGE8008)上实测资源利用率。未涉及pruning、quantization、knowledge distillation、NAS等经典模型压缩方法。

Onboard deployability: 摘要明确强调实时运行和资源受限嵌入式平台约束，并在紧凑CRPA平台(EDGE Microwave HEDGE8008)上测量了模型及文献代表性方法的资源利用率。但摘要未给出具体的参数量、FLOPs、延迟、显存、能耗等量化数值。

Datasets and metrics: 数据集：涵盖大量模拟和实测RFI事件，包括近期研究中的公开数据集。评价指标：摘要未明确说明具体指标（如accuracy、precision、recall、F1等）。实验设置：在紧凑CRPA平台(EDGE Microwave HEDGE8008)上测量资源利用率。

Experiments: 主要实验结论：1) 将ANF瞬时频率预测特征与文献中已有频谱特征结合，可提升窄带和宽带非平稳RFI的分类精度；2) 紧凑learned classifiers（如GBDT）在tight compute和memory budgets下仍能保持高精度；3) 在CRPA平台上对比了所提模型与文献代表性方法的资源利用率。摘要未提供具体数值结果和消融实验细节。

Limitations: 1) 任务场景为GNSS信号处理，与遥感成像目标检测完全不同，方法迁移需重新适配；2) 摘要未说明具体分类精度、模型大小、推理延迟等关键量化指标；3) 未涉及遥感图像特征（空间/光谱/多尺度等），特征设计思路难以直接迁移到遥感目标检测；4) 采用GBDT等传统ML方法，与深度目标检测模型在架构上差异较大。

Ideas for my research: 1) 特征层面的轻量化思路可借鉴——在遥感目标检测中，可考虑设计轻量输入特征（如基于信号/频谱先验的紧凑特征）来降低模型输入维度，减少计算开销；2) "在tight compute and memory budgets下选择紧凑模型"的benchmark方法论可借鉴——在星上部署场景中，可系统对比不同轻量化方案在真实硬件平台上的资源利用率；3) 在真实受限硬件平台（如FPGA/NPU）上实测资源利用率的评估范式值得学习；4) 但需注意，本文方法基于GNSS信号特征，与遥感图像目标检测的CNN/Transformer架构差异大，直接借鉴价值有限。

Abstract: Radio-frequency interference (RFI) remains a significant threat to GNSS in safety-critical applications. Since no single mitigation method is effective for all interferers, reliable classification is needed to select appropriate countermeasures during operation. To prevent loss of lock, RFI classification must run in real time, typically on resource-constrained embedded platforms, necessitating lightweight algorithms. While prior works realize this with simple rule-based algorithms for detecting and characterizing certain types of interferers, this approach does not scale to the broad space of all possible RFI techniques, and data-driven learned algorithms are a better fit. To satisfy these constraints, this work considers pre-correlation RFI classification with an emphasis on compact algorithms that still provide high accuracy. We first introduce a new set of lightweight input features derived from the instantaneous frequency predictions of a virtual adaptive notch filter (ANF). We observe improved classification accuracy for both narrowband and broadband non-stationary RFI by combining these new features with other spectral features from prior literature. Next, we benchmark compact learned classifiers such as gradient-boosted decision trees for accurate prediction under tight compute and memory budgets. The evaluation spans a broad set of simulated and recorded RFI events, including publicly available datasets from recent studies. Finally, we measure resource utilization for our models and for representative methods from the literature, on a compact CRPA platform (EDGE Microwave HEDGE8008).

</details>


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [133] [Multi-Head Attention Residuals](https://arxiv.org/abs/2607.27230)
*Cheng Luo,Zefan Cai,Junjie Hu*

Main category: cs.AI

TL;DR: 提出 Multi-Head Attention Residuals (MHAR)，将注意力残差中的路由查询重塑为 H 个逐子空间头，每个头在深度历史上拥有独立的 softmax，以缓解不同特征子空间对读取层数不一致导致的性能损失，在 100M-1B 规模上优于标准 Transformer 及多种基线。

Research relevance: 弱相关（2 分）。这是一篇关于 Transformer 架构改进的泛机器学习/NLP 论文，聚焦于 attention residual 机制中多子空间读取不一致的问题。论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务，更不涉及剪枝、量化、知识蒸馏、NAS 等轻量化方法。其相关性仅在于：作为 Transformer 架构层面的通用改进，可能对遥感目标检测 backbone 的注意力机制设计有间接借鉴意义，但并非遥感或轻量化方向的工作。

Reading priority: 2 分。论文与遥感卫星在轨目标检测及轻量化方向弱相关，属于泛机器学习/NLP 架构改进。其价值主要在于跨层特征路由的架构思想（逐子空间独立读取）可能对遥感检测 backbone 的跨尺度特征融合有间接启发，但缺乏视觉验证、缺乏部署指标，且不涉及任何轻量化方法。建议作为泛读材料，快速浏览核心思想即可，无需精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型预训练（language model pretraining），属于 NLP 领域。应用场景：大规模 Transformer 语言模型训练与推理。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 基于标准 Transformer 架构，核心创新是 Multi-Head Attention Residuals (MHAR)：将 attention residual 中的单一路由查询（routing query）重塑为 H 个逐子空间头，每个头在深度历史上拥有独立的 softmax 分布，使读取变为 block-diagonal 结构。该 reshape 操作零参数增加、计算开销可忽略，H=1 时退化为标准 attention residuals。采用 fused Triton routing kernels 提升训练吞吐。还提出 identity-preserving conversion 使用 delta attention residuals 支持 8B 规模的 mid-training。

Lightweight method: 摘要未说明传统意义上的模型压缩方法（剪枝、量化、蒸馏、NAS 均未涉及）。但有两处与"轻量化/高效化"间接相关：(1) MHAR 的 reshape 操作零参数增加、计算开销可忽略，属于"免费"的架构改进；(2) fused Triton routing kernels 将 attention-residual 训练吞吐从 baseline 的 0.2-0.5x 提升到 0.55-0.88x，同时保持接近 baseline 的峰值内存，属于推理/训练效率优化。但整体上不构成面向部署的轻量化方法。

Onboard deployability: 摘要未说明。论文面向大规模语言模型（100M-1B，以及 8B mid-training），未涉及星上/边缘部署、FPGA/NPU/TensorRT、延迟、能耗等部署指标。虽然 fused Triton kernels 提升了训练吞吐并保持近 baseline 峰值内存，但这是训练效率而非推理部署效率，且 Triton kernel 依赖 GPU 生态，与星上部署场景不匹配。

Datasets and metrics: 数据集：基于 Nemotron 的去重 anneal corpus，经过质量过滤，偏重 STEM 和代码内容。评价指标：validation loss（在 100M、350M、1B 三个规模上分别提升 -0.061、-0.149、-0.140）；8B mid-training 阶段使用 GSM8K（+3.2）和 GPQA（+3.1）作为下游评测指标。此外通过直接探测 trained queries 验证子空间不一致是性能提升的驱动因素。

Experiments: 在 100M、350M、1B 三个规模上从零训练，MHAR 在所有设置中均取得四种方法中的最佳 validation loss，且增益随规模增大而增大。head count H 是真实的设计轴：validation loss 对 H 呈 U 形，在 H=4 或 H=8 处有平坦最优，大规模模型采用 H=8，过度拆分（H=16）会回吐部分增益。fused Triton routing kernels 将训练吞吐从 0.2-0.5x 提升到 0.55-0.88x（相对 baseline），峰值内存接近 baseline。8B mid-training 通过 identity-preserving conversion 实现，GSM8K +3.2、GPQA +3.1。

Limitations: (1) 论文完全面向 NLP/语言模型，未涉及任何视觉或遥感任务，其结论（如 H=8 最优）是否适用于视觉 Transformer 或遥感目标检测 backbone 尚不明确；(2) 摘要未说明推理阶段的延迟、参数量、FLOPs 等部署关键指标；(3) 摘要未说明与轻量化方法（剪枝、量化、蒸馏）的结合可能性；(4) 摘要未说明在视觉任务上的验证，MHAR 的 block-diagonal 读取机制对视觉特征（尤其是遥感小目标的高频细节）是否有效未知；(5) 摘要未说明计算开销的具体数值（仅说"negligible compute"），也未给出推理吞吐对比。

Ideas for my research: (1) 思路迁移：MHAR 的核心洞察是"不同特征子空间对深度历史读取的偏好不一致"，这一思想可迁移到遥感目标检测 backbone 的跨层特征融合设计中——遥感小目标依赖浅层高分辨率特征，而语义信息依赖深层特征，不同通道子空间对跨层读取的需求天然不一致，可借鉴 MHAR 的逐子空间独立 softmax 路由机制设计跨尺度特征融合模块。(2) 零参数开销的架构改进思路：MHAR 通过 reshape 实现零参数增加、近零计算开销的改进，这种"免费"架构优化思路值得借鉴，尤其适合在轨部署场景下不增加参数量和 FLOPs 的约束。(3) 设计轴而非自由旋钮的启示：论文强调 head count 是真实设计轴（U 形曲线），提示在遥感检测模型中，类似的路由头数/分支数也应作为超参数系统搜索而非随意设定。(4) 效率优化：fused kernel 的思路可类比到遥感检测模型在 NPU/FPGA 上的算子融合优化，但需注意 Triton 依赖 GPU 生态，星上部署需重新实现。

Abstract: Transformers propagate information across depth through a single additive residual stream: every sublayer reads only the most recent state. Attention residuals relax this by letting each sublayer attend, through a learned softmax. However, that read uses a single query shared across the entire width, so every feature subspace must read the depth history through one distribution. The cost of this forced compromise grows with how much the subspaces disagree about which layers to read, and disagreement grows with model width. We introduce Multi-Head Attention Residuals (MHAR): the routing query is reshaped into H per-subspace heads, each with its own softmax over the depth history. The read becomes block-diagonal, the reshape adds zero parameters and negligible compute, and H = 1 recovers attention residuals exactly. Trained from scratch on a deduplicated Nemotron-based anneal corpus that is quality-filtered and STEM- and code-heavy, MHAR improves validation loss over a standard Transformer at 100M, 350M, and 1B (-0.061, -0.149, and -0.140). It achieves the best result among four methods in every setting, with the gain increasing from 100M to the larger scales. The head count is a real design axis rather than a free knob: validation loss is U-shaped with respect to H, with a flat optimum at H = 4 or H = 8 across scales. We adopt H = 8 for large-scale models; over-splitting beyond this point (H = 16) consistently gives back part of the gain. A direct probe of the trained queries confirms that learned subspace disagreement is the underlying driver. Fused Triton routing kernels increase attention-residual training throughput from 0.2-0.5x to 0.55-0.88x of the baseline while maintaining near-baseline peak memory. An identity-preserving conversion using delta attention residuals supports 8B mid-training, yielding improvements of +3.2 on GSM8K and +3.1 on GPQA.

</details>


### [134] [KernelGenBench: A Multi-Source and Multi-Chip Benchmark for LLM-based Kernel Generation](https://arxiv.org/abs/2607.27231)
*Peiyu Zang,Jian Tao,Jialing Zhang,Yichen Yuan,Wentao Zhang,Guang Liu,Yonghua Lin*

Main category: cs.AI

TL;DR: 提出KernelGenBench基准，用于系统评估LLM和agent自动生成的Triton kernel在多种算子来源和异构硬件平台上的性能与可移植性。

Research relevance: 弱相关（2分）。该论文聚焦于LLM自动生成高性能计算kernel（Triton kernel）的基准评测，属于泛机器学习/高性能计算领域，不涉及遥感、卫星、目标检测或模型轻量化。但其对异构硬件平台（多芯片）kernel性能可移植性的评测思路，对星上NPU/FPGA等异构平台的算子部署优化有一定间接借鉴意义。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及模型轻量化方向弱相关，主要价值在于跨异构硬件平台kernel性能评测的方法论参考，对星上算子部署优化有间接启发，但非核心文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM/agent自动生成Triton kernel的基准评测。应用场景：高性能计算kernel开发自动化。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构。核心是KernelGenBench基准框架，包含两个子基准：KernelGenBench-MS（多源，210个算子，来自三个非PyTorch标准来源）和KernelGenBench-MC（多芯片，110个算子子集，覆盖6个异构硬件平台）。评测对象包括纯LLM采样方法和agent-based方法（如AutoKernel、AKO4all）。

Lightweight method: 不涉及传统模型轻量化方法（剪枝、量化、蒸馏、NAS等）。但涉及kernel级性能优化与跨硬件平台可移植性，属于底层算子加速范畴，与部署优化相关。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。但论文评测了跨6个异构硬件平台的kernel性能可移植性，发现agent方法在跨平台时性能严重退化（如AutoKernel从NVIDIA的87%降至Platform E的25%），说明跨平台kernel部署是当前重大挑战。此外，agent方法生成每个成功算子平均消耗511万token，成本极高。

Datasets and metrics: 数据集：KernelGenBench-MS（210个算子，3个来源）和KernelGenBench-MC（110个算子，6个异构硬件平台）。评价指标：kernel生成成功率（如AutoKernel在NVIDIA上87%）、token消耗量（每个成功算子平均511万token）、跨平台性能保持率。实验规模：总消耗超过150亿token。

Experiments: 主要发现：(1) agent-based方法一致优于纯LLM采样方法，cuBLAS算子对所有方法最具挑战性；(2) 生成性能在不同硬件平台间差异显著，即使是近期kernel专用agent也面临严重跨平台退化；(3) 自主kernel生成成本极高，专用agent方法每个成功算子平均消耗511万token（AKO4all达519万），比简单LLM采样高数个数量级。

Limitations: 摘要未说明局限性。但可推断：该基准仅覆盖Triton kernel，未覆盖其他kernel语言（如CUDA、OpenCL）；评测成本极高（150亿token）；未涉及遥感/卫星场景下的算子；未讨论kernel生成结果在真实部署中的能耗和实时性指标。

Ideas for my research: 1. 借鉴其"跨异构硬件平台性能可移植性"的评测思路，在星上NPU/FPGA/DSP等异构平台上系统评估轻量化检测模型的算子级性能退化情况；2. 其揭示的"跨平台性能严重退化"问题提示：在轨部署时不能仅依赖单一平台（如GPU）的优化结果，需针对星载硬件做专门的算子适配与验证；3. 其"agent自动生成kernel"的思路可启发未来探索用LLM辅助生成星上目标检测模型的定制化算子（如NMS、ROI Align等），但需注意当前token成本极高，实际部署可行性有限；4. 其多源算子基准设计方法可参考，用于构建遥感目标检测轻量化模型的算子级基准测试集。

Abstract: Large language models (LLMs) have significantly increased the demand for efficient accelerator kernels, but kernel development remains a highly specialized and labor-intensive task. The recent rise of LLMs and agentic frameworks offers a promising pathway toward automatic kernel generation. However, despite rapid progress, there is still no comprehensive benchmark to rigorously evaluate LLM-generated kernels across diverse operator sources or heterogeneous hardware platforms. We present KernelGenBench, a unified benchmark for systematically evaluating LLM- and agent-generated Triton kernels across diverse operator sources and heterogeneous hardware platforms. It comprises two complementary sub-benchmarks: KernelGenBench-MS (Multi-Source), evaluating 210 operators from three sources beyond standard PyTorch-centric tasks, and KernelGenBench-MC (Multi-Chip), measuring performance portability across six heterogeneous hardware platforms using a 110-operator subset. Our large-scale evaluation, consuming over 15 billion tokens, shows: (1) agent-based methods consistently outperform pure LLM sampling methods, while cuBLAS operators are the most challenging across all methods; (2) generation performance varies significantly across hardware platforms, with even recent kernel-specialized agents experiencing severe cross-platform degradation (e.g., AutoKernel drops from 87% on NVIDIA to 25% on Platform E); (3) autonomous kernel generation remains highly cost-intensive, with specialized agent methods averaging 5.11 million tokens per successful operator (AKO4all reaches 5.19 million), orders of magnitude higher than simple LLM sampling approaches.

</details>


### [135] [RadHarmony: Radiological Data Handling in the Era of Agentic AI](https://arxiv.org/abs/2607.27235)
*Frank Li,Bardia Khosravi,Mohammadreza Chavoshi,Theo Dapamede,YoungSeok Jeon,Janice Newsome,Hari Trivedi,Judy Gichoya*

Main category: cs.AI

TL;DR: RadHarmony 是一个面向放射影像（胸片、CT、MRI）的开源 Python 库，提供统一 API 来加载、标准化和增强来自 24 个公开数据集的异构医学影像数据，并配套一个 AI-agent 技能辅助新数据集集成，同时提供参考 ViT 预训练基线。

Research relevance: 与本研究方向弱相关（2分）。论文聚焦医学影像（radiological images）数据标准化与多源数据集整合，属于泛视觉/医学影像领域，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。仅有的间接关联是：其"多源异构数据集统一加载与标准化"思路对遥感多源数据（光学/SAR/多光谱）整合有一定方法论借鉴，但并非遥感场景。

Reading priority: 1分。论文与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联，属于医学影像数据工程方向。仅当需要参考多源异构数据统一加载框架设计时，可作次要阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学影像数据集加载、标准化、增强与深度学习训练管线构建。应用场景：胸片（chest radiographs）为主，早期支持 CT 和 MRI。传感器类型：医学影像（X光、CT、MRI），非遥感传感器。目标类别：分类标签、分割掩膜、边界框、放射报告文本，非遥感目标检测。

Model architecture: RadHarmony-ViT：一个参考 vision transformer 基线模型，用于结合三个异构胸片数据集进行预训练，无需数据集特定代码。底层依赖 MONAI 的 map-style datasets 进行深度学习样本交付，支持可选 on-disk caching。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS 或轻量网络设计等模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且面向医学影像而非星上/边缘部署场景。

Datasets and metrics: 数据集：24 个公开放射影像数据集（胸片为主，含 CT、MRI），其中预训练实验使用 3 个异构胸片数据集。评价指标：摘要未说明具体指标（如准确率、AUC 等均未提及）。

Experiments: 主要实验为使用 RadHarmony 库对 3 个异构胸片数据集进行 ViT 预训练，验证无需数据集特定代码即可完成多源数据整合训练。摘要未给出具体数值结果、对比方法或消融实验。

Limitations: 1. 论文面向医学影像，与遥感/卫星/在轨目标检测完全无关；2. 摘要未提供任何模型性能指标、速度或资源占用数据；3. 未涉及任何轻量化技术；4. 早期仅支持胸片，CT/MRI 支持尚不完善；5. 无遥感数据（光学、SAR、高光谱、多光谱）支持。

Ideas for my research: 1. 其"多源异构数据集统一 API 与标准化"设计思路可借鉴到遥感领域——遥感数据同样存在多源（光学/SAR/多光谱）、多格式（GeoTIFF、HDF5）、多标注类型（水平框/旋转框/分割掩膜）的异构问题，可构建类似 RadHarmony 的遥感数据统一加载库；2. AI-agent 辅助数据集集成工作流（从原始数据检查到代码生成与测试）的思路可用于遥感标注数据集的自动化整合；3. 但该方法本身不提供任何轻量化或目标检测技术，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Training deep learning models on radiological images requires integrating heterogeneous datasets across different sources, file formats, directory layouts, label schemas, and annotation types. We present RadHarmony, an open-source Python library that provides a unified API for loading, harmonizing, and augmenting radiological datasets, with a primary focus on chest radiographs and early support for computed tomography (CT) and magnetic resonance imaging (MRI). RadHarmony standardizes metadata from 24 public datasets into a single tabular format, wraps MONAI's map-style datasets for deep-learning-ready sample delivery with optional on-disk caching, and supports classification labels, segmentation masks, bounding boxes, and radiology report text through a single interface, with an interactive visualization tool for dataset exploration and verification. To lower the barrier for integrating new datasets, RadHarmony introduces an AI-agent skill that guides the full integration workflow from raw data inspection through code generation and testing. We demonstrate the library's utility by pretraining RadHarmony-ViT, a reference vision transformer baseline that combines three heterogeneous chest radiograph datasets with no dataset-specific code. The code and pretrained model weights are available at https://github.com/f10409/RadHarmony.

</details>


### [136] [Asymmetric Collapse in Model Merging: When Refusal Over- writes Recognition](https://arxiv.org/abs/2607.27240)
*Aarnav Choudhary,Matheus Fonseca Rocha,Jiwon Seo,Vasu Sharma,Maheep Chaudhary*

Main category: cs.AI

TL;DR: 本文研究模型合并（model merging）在同时保留多个安全相关行为时的表现，发现攻击抵抗能力比分类精度更容易迁移，原因是拒绝微调产生的任务向量幅值更大，导致幅度敏感型合并方法偏向拒绝更新。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。本文属于大语言模型（LLM）安全对齐与模型合并（model merging）研究，涉及的是Gemma-3-1B-IT语言模型的安全行为，不涉及遥感、卫星、Earth observation、目标检测或任何模型压缩/轻量化技术。相关性评分：1分。

Reading priority: 1分。本文属于LLM安全对齐与模型合并研究，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：模型合并（model merging）下的多安全行为保持研究；应用场景：大语言模型安全对齐（safety alignment）；传感器类型：不适用（非遥感传感器）；目标类别：不适用（非目标检测任务）。

Model architecture: 基础模型为Gemma-3-1B-IT，两个微调版本分别针对CARES harm-level classification和WildJailbreak adversarial refusal两个安全目标。合并方法包括Linear、SLERP、TIES、DARE-TIES四种标准model merging方法。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS等轻量化技术。虽然模型合并（model merging）在广义上可被视为一种无需额外训练的模型能力整合手段，但本文并未将其用于模型压缩或部署加速，而是用于安全行为保持研究。

Onboard deployability: 摘要未说明。本文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。虽然Gemma-3-1B是一个较小规模的模型，但论文未从部署角度讨论其可行性。

Datasets and metrics: 数据集：CARES（harm-level classification）和WildJailbreak（adversarial refusal）两个安全相关数据集/任务。评价指标：分类准确率（classification accuracy）、攻击抵抗率（attack resistance，具体为jailbreak refusal rates）、良性合规性（benign compliance）。

Experiments: 四种合并方法（Linear、SLERP、TIES、DARE-TIES）下，攻击抵抗迁移显著优于分类精度：合并模型保留81-85%的jailbreak拒绝率，而CARES分类准确率最多仅12.9%。权重空间测量显示两个任务向量几乎正交（cosine similarity 0.011），说明不对称性并非由任务向量方向强烈对立导致，而是拒绝微调产生更大的逐层任务向量幅值，使幅度敏感型合并方法偏向拒绝更新。

Limitations: 1. 仅使用单一基础模型（Gemma-3-1B-IT）和两个安全任务，结论的泛化性有限；2. 仅研究安全相关行为，未涉及通用能力保持；3. 未讨论合并后模型在更广泛任务上的表现；4. 与遥感目标检测和轻量化方向完全无关，无直接可借鉴的部署或压缩技术。

Ideas for my research: 虽然本文与遥感在轨目标检测轻量化方向基本无关，但存在一个间接可借鉴的视角：model merging作为一种无需额外训练的能力整合手段，在理论上可被用于将多个轻量化检测模型（如不同传感器或不同目标类别的专用检测头）合并为单一模型，从而减少星上存储和推理开销。然而本文并未提供任何面向视觉/检测任务的合并方案，且其核心发现（任务向量幅值不对称导致合并偏向）是否适用于CNN/Transformer检测模型尚需验证，借鉴价值有限。

Abstract: Model merging is often used to combine capabilities from separately fine-tuned models without additional training, but it is unclear whether standard merging methods preserve multiple safety-relevant behaviors simultaneously. We study this question through a controlled case study using two Gemma-3-1B-IT finetunes on two complementary safety objectives: CARES harm-level classification and WildJailbreak adversarial refusal. We merge the two fine-tunes using Linear, SLERP, TIES, and DARE-TIES, and evaluate the merged models on classification accuracy, attack resistance, and benign compliance. Across all four methods, attack resistance transfers significantly more than classification accuracy: merged models retain 81-85% jailbreak refusal rates while CARES accuracy falls to at most 12.9%. Weight-space measurements suggest that this asymmetry is not caused by strongly opposing task-vector directions: the two task vectors are nearly orthogonal (cosine similarity 0.011). Instead, the refusal fine-tune induces consistently larger per-layer task-vector magnitudes, causing magnitude-sensitive methods to favor refusal updates. These results show that standard model merging can collapse safety recognition into broad refusal when safety-relevant task vectors differ substantially in scale.

</details>


### [137] [Divergence Decoding: Training-Free Capability Fusion](https://arxiv.org/abs/2607.27248)
*Yimi Wang,Hao Li,Shuo Yang,He Cao,Dechen Zhang,Ziang Wu,Zhiyuan Yan,Fanyang Mo,Li Yuan*

Main category: cs.AI

TL;DR: 提出Divergence Decoding，一种无需训练的推理时框架，通过Jensen-Shannon散度监控专家模型与通用大模型在逐token上的分布分歧，动态路由推理控制权，实现通用推理能力与领域专业知识的自适应融合。

Research relevance: 弱相关（1-2分）。论文聚焦于大语言模型（LLM）的推理时能力融合，属于自然语言处理/大模型领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。唯一可能的间接借鉴点是"training-free"的推理时策略组合思想，但这与遥感在轨目标检测轻量化方向相距甚远。

Reading priority: 1分。论文属于LLM推理时能力融合方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不涉及目标检测、遥感图像、模型压缩或边缘部署等核心关切点，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型（LLM）推理时能力融合（capability fusion）。应用场景：科学领域问答与推理（GPQA、ChemBench、ChemCoTBench化学与科学基准）。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用（非目标检测）。

Model architecture: 基于speculative decoding的"draft-and-verify"骨架，重构为自适应路由机制。核心组件：1) 通用模型（generalist，如Qwen/Llama系列）；2) 领域专家模型（specialist）；3) Jensen-Shannon散度（JSD）作为分布分歧监控器，逐token计算两个模型的输出分布差异；4) 自适应路由控制器，当专家模型出现显著分歧时判定为推理风险并切换控制权给通用模型。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等传统模型压缩方法。其"training-free"特性指的是无需额外训练即可实现能力融合，而非模型轻量化。推理时开销（额外运行两个模型）反而可能增加计算负担，与轻量化目标相悖。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存或能耗等部署指标。且该方法需要同时运行通用大模型和领域专家模型两个模型，计算开销较大，与星上/边缘部署的轻量化需求方向相反。

Datasets and metrics: 数据集：GPQA（科学问答）、ChemBench、ChemCoTBench（化学推理基准）。模型家族：Qwen系列和Llama系列。评价指标：摘要未明确列出具体指标名称，但为科学基准上的准确率/推理性能对比。实验设置：对比Divergence Decoding与领域专家模型、通用模型及多数单模型基线的性能。

Experiments: 实验结果表明：1) Divergence Decoding在GPQA、ChemBench、ChemCoTBench上同时优于领域专家模型和通用模型；2) 性能超过大多数单模型基线；3) 验证了该框架作为通用、无需训练的大模型能力融合范式的有效性。

Limitations: 1) 与遥感/目标检测/轻量化方向完全无关，属于NLP/LLM领域；2) 需要同时部署两个大模型，推理开销大，不适合资源受限的边缘/星上场景；3) 摘要未说明JSD阈值如何设定、路由切换的延迟开销、以及在不同模型规模下的扩展性；4) 仅在科学问答基准上验证，未涉及视觉或遥感任务。

Ideas for my research: 尽管整体弱相关，但"training-free"的推理时动态路由思想有一定启发：1) 可借鉴"分布分歧监控+动态路由"的思路，在遥感目标检测中设计轻量级"专家-通用"双分支结构，当检测置信度或特征分布出现异常时动态切换推理路径；2) Jensen-Shannon散度作为特征分布差异度量，可用于遥感图像域偏移检测或模型鲁棒性评估；3) 但需注意，该方法本身不轻量，直接迁移到星上部署不现实，仅可作为算法设计层面的思想参考。

Abstract: While large language models excel in reasoning, these generalists often lack knowledge for specialized scientific domains. Conversely, domain models~(specialists), while knowledgeable, suffer from specialization side-effects including diminished logic and reduced robustness.To address this dilemma, we introduce Divergence Decoding, a training-free framework for capability fusion. It reconstructs the "draft-and-verify" skeleton of speculative decoding into an adaptive routing mechanism. The core is using Jensen-Shannon divergence to monitor the distributional disagreement between the two models at each token. When the specialist exhibits significant divergence, our method identifies it as a potential reasoning risk and instantaneously routes control to the generalist. This allows the dynamic injection of general reasoning while preserving domain expertise, achieving inference-time policy composition of the generalist and the specialist.We evaluate Divergence Decoding across diverse model families (Qwen and Llama series) on challenging scientific benchmarks (GPQA, ChemBench, and ChemCoTBench). Experimental results demonstrate that Divergence Decoding outperforms both the domain-specialized and general-purpose models, effectively surpassing the performance of most single-model baseline. This suggests that Divergence Decoding provides a general, training-free paradigm for fusing diverse LLM capabilities through adaptive inference-time collaboration.

</details>


### [138] [PIE-APT: A Unified Framework for Temporal Planning and Contradiction Hunting via Incremental Direct-Derivation Abduction](https://arxiv.org/abs/2607.27287)
*Amir Hossein Sharafi,Alireza Shahbazi*

Main category: cs.AI

TL;DR: 提出一个基于描述逻辑（Description Logic）的统一框架，包含PIE-Abducer（增量直接推导溯因）和PIE-APT（时序知识图谱溯因规划）两个模块，用于在开放世界、信息不完备的动态知识图谱上进行推理与规划。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于知识图谱推理、描述逻辑、溯因规划（abductive planning）等知识表示与推理（KR&R）领域，属于符号AI/逻辑推理方向，不涉及任何视觉、图像、遥感、目标检测或模型压缩内容。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于知识图谱推理与符号AI领域，对本人课题无直接借鉴价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：动态知识图谱（Dynamic Knowledge Graphs）上的推理与规划；应用场景：开放世界环境下的不完备信息推理；传感器类型：不适用（非视觉任务）；目标类别：不适用。摘要未涉及任何遥感、卫星、图像或目标检测相关内容。

Model architecture: 提出统一框架包含两个集成模块：PIE-Abducer（增量直接推导溯因模块）和PIE-APT（时序知识图谱溯因规划模块）。基于描述逻辑（Description Logic, DL）和OWL表示，将状态转移建模为对演绎封闭DL理论的非单调更新，采用递归Generate-and-Test架构，交织后向链A*搜索与PIE-Abducer，并通过前向链时序投影（Temporal Projection）进行严格验证。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计方法。其"轻量"体现在逻辑推理层面（避免最小碰集MHS枚举的组合搜索），而非深度学习模型压缩。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。该工作属于符号推理框架，与深度学习推理加速和边缘部署无关。

Datasets and metrics: 使用四个OWL基准测试集，重点考察经典规划中缺失的语义能力：参数化目标与见证搜索（parameterized goals with witness search）、搜索中DL蕴含（mid-search DL entailment）、开放世界假设注入（open-world assumption injection）、对抗性矛盾猎取（adversarial contradiction hunting）。评价指标为与经典规划器的定性对比，以及与MHS忠实基线的定量对比。

Experiments: 实验结果表明该方法在定性上优于经典规划器（classical planners），且直接推导方法（direct-derivation approach）在溯因增强（abductive enrichment）过程中定量优于MHS忠实基线（MHS-faithful baseline）。摘要未提供具体数值结果。

Limitations: 摘要未说明。但可推断：1）该工作完全面向符号逻辑推理，与视觉感知、目标检测、遥感图像处理无任何交集；2）未涉及深度学习模型，因此不存在模型压缩或部署问题；3）未提供计算复杂度、运行时间等工程性能指标；4）未讨论与任何视觉或遥感任务的结合。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：其"避免组合搜索、采用直接推导"的思想在方法论层面与轻量化中"避免冗余计算、直接提取关键信息"的理念有抽象相似性，但无法直接迁移到遥感目标检测或模型压缩。不建议作为主要参考论文。

Abstract: Reasoning and planning over Dynamic Knowledge Graphs (DKGs) present significant challenges, especially in open-world environments with incomplete information. Existing action formalisms often face decidability issues and the Ramification Problem, while managing incomplete knowledge via structural abduction requires expansive combinatorial search. This paper introduces a unified framework with two integrated modules---\textbf{PIE-Abducer} (incremental direct-derivation abduction) and \textbf{PIE-APT} (Abductive Planning for Temporal KGs)---operating natively on the highly expressive Description Logic. We model state transitions along a linear timeline as non-monotonic updates to deductively closed DL theories. Treating the incremental reasoner as a black-box and representing actions natively in OWL without external modal operators preserves logical decidability. To address incomplete knowledge, \textbf{PIE-Abducer} circumvents traditional Minimal Hitting Set (MHS) enumeration. Instead of combinatorial syntactic search, it injects the logical negation of a target goal into a consistent branch and extracts missing premises via direct refutation consequences. \textbf{PIE-APT} then employs a recursive \textit{Generate-and-Test} architecture, interleaving backward-chaining A* search with \textbf{PIE-Abducer} up to a bounded causal depth, followed by strict validation via forward-chaining Temporal Projection. We evaluate four OWL benchmarks stressing semantic abilities absent in classical planning: parameterized goals with witness search, mid-search DL entailment, open-world assumption injection, and adversarial contradiction hunting. Results demonstrate qualitative superiority over classical planners and prove our direct-derivation approach quantitatively outperforms an MHS-faithful baseline during abductive enrichment.

</details>


### [139] [PAUSE: A User-Centric Benchmark for Personal AI Assistants in Unified Service Environments](https://arxiv.org/abs/2607.27354)
*Haoyu Chen,Xirui Shi,Yuyao Wang,Jerry Chen,Di Niu*

Main category: cs.AI

TL;DR: 提出PAUSE基准，用于评估在状态化、服务集成环境中部署的个人AI助手（tool-augmented agents）的用户中心化行为。

Research relevance: 弱相关（1-2分）。论文聚焦于个人AI助手（personal AI assistants）在服务环境中的基准评测，涉及agent、tool-augmented、user state、multi-turn interactions等概念，与遥感、卫星、Earth observation、目标检测、模型轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。仅有的微弱关联在于"deployment"一词，但指的是AI助手在服务环境中的部署，而非边缘/星上部署。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于AI Agent评测领域。除非对评测框架设计方法论感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：个人AI助手（personal AI assistants）作为task-oriented, tool-augmented agents的基准评测。应用场景：统一服务环境中的日常用户活动支持，涉及多服务协调、用户状态推理、权限约束、长时程交互。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。论文提出的是评测基准PAUSE（benchmark），而非检测或视觉模型。涉及agent-based系统，包含用户模拟（user simulation）、多机制评测框架（multi-regime evaluation framework），但未给出agent的具体网络结构或算法设计。

Lightweight method: 不涉及。摘要中未提及pruning、quantization、knowledge distillation、NAS、轻量网络、实时推理、边缘部署、FPGA/NPU/TensorRT等任何模型压缩或加速方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。论文中的"deployment"指AI助手在服务环境中的部署，与星上/边缘部署无关。

Datasets and metrics: 提出PAUSE基准，包含：语义级和轨迹级行为指标（semantic and trajectory-level behavioral metrics）用于开放式服务管理任务；确定性、基于状态的验证（deterministic, state-based verification）用于约束密集型任务。评测了state-of-the-art proprietary models，结果显示在需要状态推理和配置感知的场景下任务完成率低于70%。摘要未说明具体数据集规模、任务数量或对比模型名称。

Experiments: 基准结果显示即使最先进的专有模型（state-of-the-art proprietary models）在需要状态推理（stateful reasoning）和配置感知（configuration awareness）的场景下任务完成率（task completion）低于70%，并揭示了一致且可解释的失败模式（consistent and interpretable failure patterns）。摘要未给出具体数值、消融实验或与基线方法的详细对比。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于NLP/Agent评测领域。2. 摘要未说明评测模型的具体架构、参数量或推理成本。3. 摘要未说明PAUSE基准的规模（任务数量、服务环境数量）及与现有基准的定量对比。4. 用户模拟（user simulation）的真实性和可扩展性有待验证。

Ideas for my research: 借鉴价值有限，但可从方法论层面提取两点：1. "multi-regime evaluation framework"（多机制评测框架）的思想——针对不同任务特性采用不同评测指标（语义/轨迹级 vs 确定性状态验证），可类比到遥感目标检测中针对不同场景（如小目标 vs 旋转框）设计差异化评测协议。2. "user-centric synthesis pipeline"（用户中心化合成管线）用于可扩展生成评测环境——可类比为遥感领域合成数据生成管线（如合成SAR/光学遥感图像用于训练和评测），但需自行迁移设计。整体而言，对"在轨遥感目标检测模型轻量化"课题的直接借鉴点很少。

Abstract: Personal AI assistants are increasingly deployed as task-oriented, tool-augmented agents that operate within unified service environments to support everyday user activities. In realistic settings, such assistants must reason over persistent user state, respect user-specific configurations and permissions, and sustain long-horizon, constraint-aware interactions across multiple services. Existing benchmarks, however, often fragment service contexts or abstract away user state, limiting their ability to evaluate user-centric personal assistant behavior in realistic service settings. We introduce PAUSE, a user-centric benchmark for evaluating personal AI assistants in stateful, service-integrated environments. PAUSE captures core challenges of real-world assistant deployment by requiring agents to coordinate actions across heterogeneous user-owned resources while maintaining consistency with environment state, authorization constraints over multi-turn interactions. The benchmark incorporates explicit user-agent interaction via realistic user simulation, enabling evaluation beyond static tool execution. To support principled and reproducible evaluation, PAUSE adopts a multi-regime evaluation framework aligned with task characteristics. Open-ended service management tasks are assessed using semantic and trajectory-level behavioral metrics, while constraint-intensive tasks admit deterministic, state-based verification. Benchmark results show that even state-of-the-art proprietary models fail to reach 70% task completion on scenarios requiring stateful reasoning and configuration awareness, revealing consistent and interpretable failure patterns. Finally, we present a user-centric synthesis pipeline that enables scalable generation of coherent service environments, user configurations, and reliably annotated tasks, supporting benchmark extensibility and future research.

</details>


### [140] [SkillMentor: LLM Agent Self-Evolution via Learning Blind-Spot Diagnosis](https://arxiv.org/abs/2607.27360)
*Xiaoyi Bao,Yuanzhen Xie,Yunzhi Tan,Jinghang Gu,Zhongqing Wang,Chu-Ren Huang,Bo Hu,Zang Li*

Main category: cs.AI

TL;DR: 提出SkillMentor，通过强化学习训练一个Mentor策略来生成诊断任务、识别智能体反复出现的失败模式，并将其整理为可复用的纠正技能，实现无需更新执行器权重或人工标注的智能体自我进化。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文研究的是AI agent（智能体）的自我进化与盲点诊断能力，属于大语言模型/智能体领域，不涉及遥感、卫星、Earth observation、目标检测或任何模型压缩/轻量化技术。相关性评分：1分。

Reading priority: 1分。论文主题为AI agent自我进化与盲点诊断，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI agent的盲点诊断（blind-spot diagnosis）与自我进化能力学习。应用场景：通用智能体任务执行（AppWorld、BFCLv3基准）。传感器类型：不适用（非遥感/视觉传感器）。目标类别：不适用。

Model architecture: 提出SkillMentor框架，包含一个Mentor策略（通过reinforcement learning训练），用于生成诊断任务、识别失败模式并整理为可复用的纠正技能（corrective skills）。执行器（executor）权重保持不变。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：AppWorld和BFCLv3（均为AI agent/工具调用基准，非遥感数据集）。评价指标：executor performance（执行器性能），SkillMentor平均提升44.2%。

Experiments: 在AppWorld和BFCLv3两个基准上验证SkillMentor，平均提升执行器性能44.2%。实验排除了executor adaptation和human supervision两种替代改进来源，证明改进全部来自学习的诊断能力。

Limitations: 摘要未说明局限性。但明显与遥感目标检测和轻量化方向完全无关，属于LLM agent领域的方法研究，无法直接迁移到遥感在轨目标检测场景。

Ideas for my research: 借鉴意义非常有限。唯一可能的概念性启发是"诊断能力可学习"这一思想——在遥感在轨目标检测中，可以考虑让模型具备自我诊断能力（如检测自身在特定场景下的失败模式），但这与本课题的轻量化核心目标关联极弱。不建议作为主要参考。

Abstract: Agent self-evolution has primarily focused on learning how to act, while overlooking an equally important capability: learning to discover what an agent does not know. Existing approaches typically assume that failure discovery is given, focusing on how to repair failures once they are identified. We ask whether blind-spot diagnosis itself can be learned. We thus study diagnosis as an agent capability separate from execution, and exclude two alternative sources of progress: executor adaptation and human supervision. Under these constraints, performance cannot improve through executor updates or annotated examples, forcing all improvements to originate from the learned diagnostic capability. We propose SkillMentor, which trains a Mentor policy via reinforcement learning to generate diagnostic tasks, identify recurrent failure modes, and curate them into reusable corrective skills. Across AppWorld and BFCLv3, SkillMentor improves executor performance by an average of 44.2%. These results suggest that blind-spot diagnosis is a learnable capability, enabling self-evolution without updating executor weights or relying on human-curated data.

</details>


### [141] [Beyond the Bidirectional Promise: Re-evaluating the Robustness of Diffusion Language Models](https://arxiv.org/abs/2607.27386)
*Saurabh Yadav,Badri Narayana Patro,Vijay Srinivas Agneeswaran*

Main category: cs.AI

TL;DR: 系统评估扩散语言模型（DLM）在自然噪声和对抗攻击下的鲁棒性与校准特性，发现其鲁棒性主要依赖权重而非架构，且存在系统性过度自信问题，行为脆弱性源于解码器路由失败而非输入编码。

Research relevance: 弱相关（2分）。论文研究对象是Diffusion Language Models（扩散语言模型），属于自然语言处理/大语言模型领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可借鉴的是其对模型鲁棒性、校准性和部署风险的评估方法论，以及"鲁棒性必须内建于迭代解码循环而非表面修补"的结论，这对在轨模型面对噪声/对抗干扰时的鲁棒性设计有一定方法论启示，但场景完全不同。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于大语言模型鲁棒性评估领域。仅方法论层面的鲁棒性评估思路有微弱借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：扩散语言模型（DLM）的鲁棒性与校准性评估。应用场景：自然语言生成、大语言模型部署。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 对比了扩散语言模型（DLM）与自回归（AR）语言模型，使用两对参数匹配的模型：LLaDA-8B vs. LLaMA-3-8B，以及 Dream-7B vs. Qwen2.5-7B。通过配对设计隔离架构固有属性与权重依赖行为。采用机制性隐藏状态分析（mechanistic hidden-state analyses）进行诊断。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。其关注点是鲁棒性与校准性评估，而非轻量化。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。但指出DLM存在系统性过度自信（overconfidence），构成实际部署风险（practical deployment hazard），这对任何部署场景（包括星上部署）都有警示意义。

Datasets and metrics: 使用了32种自然扰动条件（natural perturbation conditions）、对抗梯度探针（adversarial gradient probes）和机制性隐藏状态分析。评价指标涉及鲁棒性（robustness）和校准性（calibration）。具体数据集名称摘要未说明。

Experiments: 主要发现：(1) 高度随机的DLM损失景观天然抵抗基于梯度的对抗后缀，但对自然噪声无保证防御；(2) 日常鲁棒性是权重依赖的而非架构固有的；(3) DLM表现出系统性过度自信；(4) 机制性探针显示所有模型都能完美编码输入损坏，行为脆弱性完全源于解码器路由失败（decoder routing failure）；(5) 表面级prompt修补无法改善噪声基线。

Limitations: 摘要未说明具体局限性。但明显局限包括：研究对象为语言模型而非视觉/遥感模型；未涉及目标检测任务；未提供任何轻量化或部署优化方案；结论是否可迁移到视觉扩散模型或遥感场景尚不明确。

Ideas for my research: 尽管场景不同，以下方法论可借鉴：(1) 配对模型设计（parameter-matched pairs）来隔离架构与权重影响的方法论，可用于对比轻量化前后模型的鲁棒性变化；(2) "鲁棒性必须内建于迭代解码循环而非表面修补"的结论，提示在轨目标检测模型的鲁棒性应内建于网络结构而非后处理修补；(3) 系统性过度自信的发现，提示在轨模型在低置信度场景下应谨慎输出，可结合不确定性估计；(4) 机制性探针诊断方法可用于分析轻量化剪枝/量化后模型性能退化的根因。

Abstract: Diffusion Language Models (DLMs) offer a compelling alternative to autoregressive (AR) generation by enabling bidirectional context and iterative refinement. However, their reliability under natural input noise and adversarial attacks remains under-explored. To address this, we systematically evaluate DLM robustness and calibration against AR baselines, using two parameter-matched pairs (LLaDA-8B vs. LLaMA-3-8B and Dream-7B vs. Qwen2.5-7B) across 32 natural perturbation conditions, adversarial gradient probes, and mechanistic hidden-state analyses. This paired design effectively isolates architecture-intrinsic properties from weight-dependent behaviors. We find a nuanced robustness profile: while highly stochastic DLM loss landscapes naturally resist gradient-based adversarial suffixes, they provide no guaranteed defense against natural noise, proving that everyday robustness is weight-dependent rather than inherently architectural. Furthermore, DLMs exhibit systematic overconfidence, presenting a practical deployment hazard. Most crucially, mechanistic probing reveals that all models perfectly encode input corruption, isolating behavioral fragility entirely to a decoder routing failure. Consistent with this diagnosis, we show that surface-level prompt patching fails to improve over noisy baselines. Ultimately, DLM robustness cannot be patched on; it must be fundamentally integrated into the iterative decoding loop.

</details>


### [142] [Bridging Inference-Time Scaling and Episodic Memory with Action-Centric Graphs](https://arxiv.org/abs/2607.27415)
*Xu Zheng,Chaohao Lin,Zhuomin Chen,Weijieying Ren,Haifeng Chen,Wei Cheng,Dongsheng Luo*

Main category: cs.AI

TL;DR: 提出GAMER框架，通过基于图的动作中心记忆与情景推理，将推理时扩展与情景记忆结合，解耦记忆机制与LLM以降低token消耗，提升智能体推理效率。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于Large Language Models (LLMs)的推理时扩展（inference-time scaling）与智能体（agents）的记忆机制优化，属于自然语言处理/强化学习/智能体决策领域，不涉及遥感、卫星、Earth observation、目标检测或模型压缩部署。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM智能体记忆与推理效率优化领域，技术路线（图结构记忆、TD学习、推理时扩展）与遥感目标检测轻量化没有直接交集，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体的推理效率优化与情景记忆建模。应用场景：智能体决策、多步推理任务。传感器类型：不适用。目标类别：不适用。摘要未说明具体benchmark场景。

Model architecture: GAMER框架：将历史推理建模为动态Action-Centric Graph（动作中心图）；采用dual-stream Temporal Difference (TD) learning机制估计动作节点的正（suggestion）负（avoidance）价值；推理阶段通过图上的高效搜索进行双向决策优化。

Lightweight method: 论文的"轻量化"体现在token/money消耗的节省——通过将记忆机制与LLM解耦（decoupling the memory mechanism from LLMs），提供比记忆机制baseline更少的记忆上下文来降低计算成本。但这并非传统意义上的模型压缩（pruning/quantization/distillation/NAS），而是推理阶段的上下文token优化，与遥感模型轻量化无关。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。其优化目标是LLM推理的token/money消耗，而非硬件部署效率。

Datasets and metrics: 在多个benchmark上进行实验，主要指标为success rate和progress rate，相比vanilla baselines分别提升20.81%和6.17%。摘要未说明具体数据集名称。

Experiments: 在多个benchmark上验证GAMER，相比vanilla baselines在success/progress rate上分别提升20.81%/6.17%。摘要未说明对比方法细节、消融实验或具体实验设置。

Limitations: 摘要未说明局限性。整体上该工作与遥感目标检测和模型轻量化方向无直接关联，属于LLM智能体推理效率优化领域。

Ideas for my research: 借鉴意义有限。可能的间接启发包括：(1) 将历史推理建模为图结构并解耦记忆机制以降低计算开销的思想，在概念层面与"减少冗余计算"的轻量化目标有相通之处，但技术路线完全不同；(2) 通过价值函数（正/负）引导决策、避免高风险的冗余搜索，这一"避免无效计算"的思路可类比于检测模型中的稀疏推理或动态推理（dynamic inference），但需要自行迁移到遥感目标检测场景。总体而言，直接可复现或可借鉴的技术点很少。

Abstract: Recent advancements in inference-time scaling have significantly unlocked the complex reasoning capabilities of Large Language Models~(LLMs). However, for agents, these approaches suffer from a critical inefficiency, operating in a stateless manner and engaging in redundant search processes. Existing memory mechanisms largely rely on the reasoning capabilities of LLMs, leading to prohibitive computational costs. In this paper, we propose a novel framework, \textit{GAMER}~(Graph-based Action-centric Memory with Episodic Reasoning), that bridges the gap between inference scaling and episodic memory. Our approach models historical reasoning as a dynamic \textit{Action-Centric Graph}. By decoupling the memory mechanism from LLMs, our method can save token/money usage by providing less memory context than memory mechanism baselines. To extract knowledge from the graph effectively, we use a dual-stream Temporal Difference learning mechanism to estimate the positive~(suggestion) and negative~(avoidance) value of action nodes based on past successes and failures. During the inference phase, this learned value function optimizes decision-making bi-directionally, so that positive values provide action suggestions, while negative values indicate high-risk actions. By performing efficient searches on the graph, our method significantly improves the efficiency of inference scaling. Experiments on multiple benchmarks demonstrate that \textit{GAMER} achieves superior performance by \textbf{20.81\%/6.17\%} for success/progress rate compared to vanilla baselines.

</details>


### [143] [Dimensionality and Measurement Precision in HLE's Multiple-Choice Subset](https://arxiv.org/abs/2607.27420)
*Mayank Sharma,Savira Nadela,Tyler Matteson*

Main category: cs.AI

TL;DR: 本文使用心理测量学方法（IRT模型）评估 Humanity's Last Exam (HLE) 基准测试的维度结构和测量精度，发现HLE主要测量单一通用推理因子，领域子分数不构成独立能力维度，且对最强模型的区分能力有限。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文研究的是大语言模型（LLM）基准测试的心理测量学性质，属于自然语言处理与教育测量交叉领域，不涉及遥感、卫星、目标检测或模型压缩/部署。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM评估与心理测量学领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务：对LLM基准测试HLE进行心理测量学评估（IRT建模、维度分析、信息函数分析）。应用场景：大语言模型能力评估。传感器类型：不适用（文本数据）。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。评估对象为29个LLM，使用两参数逻辑斯蒂克IRT模型（two-parameter logistic IRT model）对428个多项选择题进行建模。

Lightweight method: 不涉及。本文不讨论任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化方法。

Onboard deployability: 不涉及。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：Humanity's Last Exam (HLE) 的文本版多项选择子集（J=428个题目），评估29个LLM。指标：McDonald's ω_h（0.998）、域标签解释方差比例（3.5%）、Cohen's d（0.016）、域特定能力与总分相关性（r≥0.81）、测试信息函数（test information function）。

Experiments: 主要发现：(1) HLE测量单一通用推理因子（ω_h=0.998），领域标签仅解释3.5%的题目响应方差；(2) 域内与域间残差相关性几乎相同（d=0.016），域特定能力估计与总分近乎冗余（r≥0.81）；(3) 测量精度集中在中等能力水平，在θ>0（前沿模型所在区域）急剧下降，说明HLE对最强模型的区分能力有限。

Limitations: 与本研究方向完全无关。摘要未说明任何与遥感、目标检测、轻量化或边缘部署相关的信息。该论文属于LLM评估与心理测量学领域，对遥感在轨目标检测课题无直接借鉴价值。

Ideas for my research: 几乎无可借鉴点。唯一可能的间接启发是：如果未来需要评估遥感目标检测基准测试（如DOTA、DIOR等）的维度结构或难度分布，可参考其使用IRT模型分析基准测试测量精度分布的方法论思路，但这与本课题核心方向（在轨目标检测模型轻量化）关联极弱。

Abstract: Humanity's Last Exam (HLE) is widely used to evaluate frontier language models. HLE organizes its questions into eight subject-domain categories, whose subscores are often interpreted as evidence of distinct capabilities. However, no study has assessed whether these labels correspond to empirically separable latent constructs, nor whether the benchmark effectively differentiates between models of similar ability. We evaluate 29 LLMs on the text-only multiple-choice subset of HLE ($J = 428$ items) and apply psychometric methods to assess both the dimensionality of the benchmark and the distribution of its measurement precision. Fitting a two-parameter logistic IRT model, we find convergent evidence that HLE measures a single general reasoning factor: McDonald's $ω_h = 0.998$, domain labels explain only 3.5\% of item response variance, within- and between-domain residual correlations are nearly identical (Cohen's $d = 0.016$), and domain-specific ability estimates are near-redundant with the total score ($r \geq 0.81$). A separate analysis of the test information function reveals that measurement precision concentrates at moderate ability levels and drops sharply above $θ= 0$, where frontier models sit. These findings suggest that HLE's domain subscores do not warrant distinct capability interpretations and that the benchmark's ability to discriminate among the strongest models is limited.

</details>


### [144] [Leveraging Trajectory Graphs for Pre-Execution Error Diagnosis in Agentic LLM Systems](https://arxiv.org/abs/2607.27443)
*Xu Zheng,Zhuomin Chen,Chaohao Lin,Hua Wei,Haifeng Chen,Wei Cheng,Dongsheng Luo*

Main category: cs.AI

TL;DR: 提出Trajectory Graph Copilot框架，通过图神经网络建模历史轨迹来预判LLM智能体在长时程交互任务中的潜在动作错误，实现执行前诊断与自我纠正。

Research relevance: 弱相关（1-2分）。该论文聚焦于LLM-based agents在embodied AI等长时程交互任务中的错误诊断与自我纠正，属于泛机器学习/泛AI方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也不涉及object detection或模型压缩/加速方法。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM agent领域的泛机器学习方法，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体长时程交互任务中的动作错误预诊断与自我纠正。应用场景：embodied AI、复杂交互任务。传感器类型：不适用。目标类别：不适用。

Model architecture: 核心组件为Graph Debugger：将历史轨迹建模为概率图（probabilistic graph），使用Graph Neural Network (GNN)识别频繁导致失败的序列动作模式，作为LLM agent的"copilot"提供执行前预警。

Lightweight method: 摘要未说明。不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。该方法强调"无需昂贵微调"（without costly fine-tuning），但并非轻量化技术。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，与星上/边缘部署无关。

Datasets and metrics: 在四个benchmark上使用三个LLM agents进行实验，评价指标为pass ratio，平均提升14.69%。具体benchmark名称和agent类型摘要未说明。

Experiments: 在四个基准上对三个LLM智能体进行广泛实验，平均pass ratio提升14.69%。具体对比方法和消融实验细节摘要未说明。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关；2. 摘要未说明具体benchmark、agent类型及计算开销；3. 未涉及任何遥感数据、目标检测任务或部署约束。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是"执行前错误诊断"的思想——在遥感在轨目标检测中，可类比为在推理前对输入图像或模型输出进行预检，避免在资源受限的星上环境中浪费计算。但整体关联度很低，不建议作为主要参考。

Abstract: Large Language Model~(LLM)-based agents have demonstrated exceptional performance across a wide range of complex interactive tasks. However, they often struggle with long-horizon interactive tasks common in domains, such as embodied AI. The complexity and vast action spaces in these settings lead to compounding errors, where a single suboptimal action can derail an entire trajectory, causing the agent to exhaust its limited step budget on inefficient or unrecoverable paths. To overcome this without costly fine-tuning, we draw inspiration from software debugging, where execution logs are analyzed to preemptively catch errors. We propose \textit{Trajectory Graph Copilot}, a novel framework that acts as a ``copilot'' for LLM agents by diagnosing potential action errors before they are executed. At its core,\textit{Graph Debugger} models historical trajectories as a probabilistic graph and uses a Graph Neural Network to identify sequential action patterns that frequently lead to failure. Functioning as a proactive diagnostic sandbox, our method provides early warnings on potentially flawed actions, prompting the agent to self-correct. This pre-action error diagnosis prevents costly mistakes, significantly enhancing the agent's ability to complete long-horizon tasks successfully. The extensive experiments on four benchmarks with three LLM agents demonstrate a $14.69\%$ pass ratio improvement on average.

</details>


### [145] [VAmoS Bench: Voice Agent Simulation Bench](https://arxiv.org/abs/2607.27453)
*Joshua Meyer,Sahar Shayegan,Ritiz Tambi,Ali Khan,Sun Kim,Victor Shih,Mehdi Jamei,Andi Partovi*

Main category: cs.AI

TL;DR: 提出VAmoS Bench，一个端到端评估完整语音客服智能体系统在真实有状态客服任务中"containment"（自主解决率）能力的模拟基准平台。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦语音客服智能体（voice agent）的端到端评估基准，属于自然语言处理/对话系统/语音交互领域，不涉及遥感、卫星、Earth observation、目标检测或模型压缩部署。相关性评分：1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于语音客服智能体评估领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语音客服智能体（voice agent）端到端评估；应用场景：金融客服（信用卡支持），模拟电话呼叫；传感器类型：不适用（音频对话）；目标类别：不适用（评估对象为客服智能体行为）。

Model architecture: 摘要未说明具体模型架构。论文核心是评估平台（benchmark）而非模型，涉及级联（cascaded）、语音到语音（speech-to-speech）、混合（hybrid）三种语音智能体架构的评估框架设计。

Lightweight method: 不涉及。论文未讨论任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化方法。

Onboard deployability: 不适用。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时推理等部署相关信息。

Datasets and metrics: 数据集：VAmoS Bench，包含100个客服场景（scenarios），每个场景配有模拟呼叫者（simulated caller）、私有目标（private goal）和PostgreSQL后端；约三分之一场景施加对抗性压力（adversarial pressure）。评价指标：containment（自主解决率，即无需转接人工的呼叫占比）、二进制断言（binary assertions）评估，覆盖对话轨迹、工具调用、参数和返回行；同时提及word error rate、latency、naturalness、turn-taking等组件质量指标。

Experiments: 摘要未说明具体实验结果或对比方法。论文主要贡献是提出评估平台本身及其评估协议，支持演进式排行榜（evolving leaderboard）。

Limitations: 摘要未说明局限性。但可推断：1) 当前版本仅聚焦金融服务领域，泛化性有限；2) 未涉及任何视觉/遥感任务；3) 未讨论模型效率、推理速度或部署约束；4) 未提供与现有语音智能体基准的定量对比结果。

Ideas for my research: 借鉴意义非常有限。唯一可能间接相关的点是：该论文强调"端到端系统级评估"（end-to-end system evaluation）而非仅组件级指标，这一思路可类比到遥感在轨目标检测中——即不仅评估模型精度（如mAP），还应评估完整在轨推理链路（从传感器输入到决策输出）的系统级性能。此外，其"二进制断言+完整轨迹追踪"的评估设计思想，可启发在轨检测系统验证时对"声称检测到目标但实际未正确执行"这类问题的核查。但整体上对本课题借鉴价值很低。

Abstract: Production voice agents span cascaded, speech-to-speech, and hybrid architectures. Voice-agent benchmarks typically measure component quality and conversational properties such as word error rate, latency, naturalness, and turn-taking. Fewer measure whether the agent handled a phone call correctly on its own. Contact centers refer to this as ``containment'': the share of phone calls the automated system resolves without handing off to a human. On some phone calls the right outcome is refusal or a redirect. To address this gap, we introduce VAmoS Bench, the Voice Agent Simulation Bench. It measures complete voice-agent systems end to end in a stateful customer-support task. The agent is Riley, a credit-card support representative for a fictional bank who can freeze, cancel, replace, or activate a card. Each of 100 scenarios supplies a simulated caller with a private goal and a seeded PostgreSQL backend. The platform uses each scenario to populate and activate an isolated simulation in which the caller reaches Riley over audio; roughly one-third apply adversarial pressure. The agent can use five tools that execute real SQL against the backend. Each scenario also defines binary assertions. A grader evaluates them against the complete trace of what the caller and agent said and what the agent did, including tool invocations, arguments, and returned rows. This catches an agent that claims to have changed a card without updating the database, as well as one that makes the right database change while disclosing protected information. This first benchmark version focuses on financial services. Its evaluation protocol supports an evolving leaderboard: additional voice agents can be evaluated on the same version, while later versions can expand the tasks and scenarios.

</details>


### [146] [Skill Use or Skill Theater? Evaluating the Reasoning Backroom in Skill-Augmented Language Agents](https://arxiv.org/abs/2607.27484)
*Jinwei Hu,Yi Qi,Xinmiao Huang,Youcheng Sun,Yi Dong,Xiaowei Huang*

Main category: cs.AI

TL;DR: 提出BACKTRACE评估框架，通过干预式反事实对照揭示语言智能体在技能使用声明与实际因果影响之间存在系统性差距（"Reasoning Backroom"），证明仅凭可观测推理或自我归因无法判断技能是否真正改变了决策。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于语言智能体（language agents）的技能使用归因与因果影响评估，属于自然语言处理/人工智能可解释性与评估方法论范畴，不涉及遥感、卫星、目标检测或模型压缩加速等任何相关主题。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于语言智能体评估与AI可解释性领域。除非对"模型行为归因与干预式评估"方法论有跨领域兴趣，否则无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言智能体技能使用的影响评估与归因分析；应用场景：受控逻辑推理与竞赛数学问题求解；传感器类型：不适用（非视觉/遥感任务）；目标类别：不适用。

Model architecture: 摘要未说明具体模型架构细节。涉及多模型家族（diverse model families）、单智能体与多智能体（single-agent and multi-agent）设置，以及技能增强型语言智能体（skill-augmented agents）。核心是BACKTRACE评估框架，包含技能条件答案与匹配的无技能反事实对照（matched no-skill counterfactual），并对技能含义、措辞、身份、内容与分配进行干预。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 数据集：BACKROOMBench，一个经过验证的测试平台（verified testbed），涵盖受控逻辑（controlled logic）与竞赛数学（competition mathematics）两类任务，多种技能条件，单/多智能体设置。评价指标：因果依赖（causal reliance）、符号效用（signed utility）、技能使用声明稳定性（stated skill use stability）、归因准确性等，以及基于直接技能使用声明、文本提及、轨迹相似性和LLM评判的观测检测器性能。

Experiments: 主要发现：1) 跨模型与领域，声明的技能使用往往保持稳定，而因果依赖与符号效用变化，产生"静默采纳"（silent uptake）与"表演性使用"（performative use）；2) 行为效应更可靠地跟随程序性内容（procedural content）而非展示的技能身份；3) 陈述性归因强烈响应工件可用性（artifact availability）；4) 基于直接技能使用声明、文本提及、轨迹相似性和LLM评判的观测检测器无法识别哪些决策真正依赖技能；5) 多智能体系统中，技能影响在来源丢失后仍可经通信存活，而无技能团队仍会命名从未提供的技能与来源。

Limitations: 摘要未说明局限性。但可推断：该工作聚焦语言智能体评估，与遥感/视觉/嵌入式部署场景完全无关；其反事实干预方法在视觉目标检测模型上难以直接迁移；未涉及任何计算效率或部署约束。

Ideas for my research: 借鉴价值有限，但存在一个方法论层面的间接启发：该论文强调"仅凭表面信号（如声明、文本提及）无法判断模型是否真正依赖某组件"，这提示在轻量化研究中，评估剪枝/蒸馏后模型性能时，不能仅看指标数字，还应通过干预式实验（如移除某模块、扰动某层权重）验证模型是否真正保留了关键特征提取能力，而非依赖表面捷径。此外，其"反事实对照"思想可类比于轻量化消融实验设计——对比压缩前后模型在相同输入上的决策一致性。

Abstract: Reusable skills are becoming a standard interface for extending language agents with task procedures. Yet evaluators usually infer skill use from visible reasoning or the agent's own attribution. These signals show what the agent appears to use, not whether the skill changed its decision. We ask whether skill-augmented agents exhibit a \textbf{Reasoning Backroom}, a systematic gap between stated skill use and intervention-measured influence. We introduce BACKTRACE, an evaluation framework that pairs each skill-conditioned answer with a matched no-skill counterfactual, intervenes on skill meaning, wording, identity, content, and assignment, and elicits attribution only after the answer is committed. We instantiate the framework as BACKROOMBench, a verified testbed spanning controlled logic and competition mathematics, multiple skill conditions, single-agent and multi-agent settings, and diverse model families. Our evaluation reveals a pervasive provenance failure. Across models and domains, stated skill use often remains stable while causal reliance and signed utility vary, producing both silent uptake and performative use. Behavioral effects follow procedural content more reliably than displayed skill identity, whereas stated attributions respond strongly to artifact availability. Observational detectors based on direct skill-use claims, text mentions, trace similarity, and an LLM judge do not identify which decisions actually depend on the skill. In multi-agent systems, skill influence can survive communication even after its source is lost, while no-skill teams still name skills and sources that were never supplied. These findings establish the Reasoning Backroom as a general AI provenance problem whose audit requires intervention.

</details>


### [147] [INCLAIR: Inception-Based Longitudinal Clinical Anomaly Detection with Informed Reasoning](https://arxiv.org/abs/2607.27487)
*Maxx Richard Rahman,Wolfgang Maass*

Main category: cs.AI

TL;DR: 提出INCLAIR框架，用于纵向临床档案中的异常检测，通过多历史上下文评分、证据聚合和自然语言解释，在有限专家监督下实现临床可操作的异常检测。

Research relevance: 1分，基本无关。该论文聚焦于临床纵向档案（longitudinal clinical profiles）中的异常检测，属于医疗健康/时间序列分析领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感场景完全无关，也不涉及目标检测或模型轻量化。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于临床医疗时间序列异常检测领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：纵向临床档案异常检测（anomaly detection in longitudinal clinical profiles）；应用场景：临床医疗诊断辅助；传感器类型：不适用（临床数据）；目标类别：临床异常事件/异常观测。

Model architecture: INCLAIR框架：对每个观测基于多个历史上下文（historical contexts）进行评分，在档案级别（profile level）聚合证据，并生成基于证据的自然语言解释（grounded natural-language explanations）。核心数学结构为order-l U-statistic形式的完整均值子序列评分，包含方差分解和不完整子集近似。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络等模型压缩或加速方法。唯一与"轻量"沾边的是通过incomplete-subset approximation控制组合推断成本（combinatorial inference cost），但这是针对统计推断计算复杂度，而非模型部署轻量化。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。

Datasets and metrics: 三个临床数据集（three clinical datasets），具体名称摘要未说明；评价指标摘要未说明；另有一个纵向类固醇档案（longitudinal steroid profiles）案例研究，与领域专家评估及DNA分析进行对比验证。

Experiments: INCLAIR在三个临床数据集上一致优于state-of-the-art基线方法（具体基线名称摘要未说明）；通过纵向类固醇档案案例研究，将INCLAIR的预测和解释与领域专家评估（辅以DNA分析）进行对比，验证了临床实用性。

Limitations: 摘要未说明局限性。但可推断：该工作完全面向临床医疗场景，与遥感在轨目标检测及轻量化方向无任何交集；其U-statistic统计框架和top-k pooling聚合策略虽在方法论上有一定理论深度，但迁移到遥感目标检测场景的可行性未在摘要中体现。

Ideas for my research: 借鉴价值极低。唯一可能间接参考的点：(1) top-k pooling聚合策略在异常证据稀疏场景下的有效性，或可类比遥感小目标/稀疏目标检测中的特征聚合；(2) incomplete-subset approximation控制组合计算成本的思路，或可类比遥感大图推理时的计算开销控制。但这些都是非常间接的类比，且论文本身未涉及任何视觉/遥感/部署内容。

Abstract: Detecting anomalies in longitudinal clinical profiles is clinically important but difficult: abnormal evidence is often sparse, patient histories have unequal length, and expert explanations are costly. We propose INCLAIR, a framework that scores each observation against multiple historical contexts, aggregates evidence at the profile level, and generates grounded natural-language explanations under limited expert supervision. Under stated within-profile exchangeability assumptions, the complete mean subsequence score takes an order-$l$ U-statistic form, yielding a variance decomposition and an incomplete-subset approximation that controls combinatorial inference cost independently of profile length. The same analysis shows that mean aggregation attenuates localized anomalies by a factor set by the anomaly support and profile length, motivating validation-selected top-$k$ pooling. Across three clinical datasets, INCLAIR consistently outperforms state-of-the-art baselines. We further validate practical relevance through a case study on longitudinal steroid profiles, comparing INCLAIR's predictions and explanations against domain-expert assessments supported by DNA analysis. The results show that INCLAIR enables clinically actionable anomaly detection under limited expert supervision.

</details>


### [148] [MedLLM: An Open Medical Language Model at the Sub-Billion Scale](https://arxiv.org/abs/2607.27490)
*Maxx Richard Rahman,Asim Ahmed,Mihan Mohagheghzadeh,Wolfgang Maass*

Main category: cs.AI

TL;DR: 提出MedLLM，一个0.1B参数的开放医疗语言模型，通过三阶段训练管线（通用预训练、领域微调、偏好对齐微调）实现，并揭示亚十亿参数规模下医疗能力按任务类型非均匀退化的现象。

Research relevance: 弱相关（2分）。本文是医疗领域的大语言模型（medical LLM）研究，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关；也不涉及目标检测（object detection）、小目标检测、旋转框检测等遥感视觉任务。唯一的间接关联在于其探讨"小模型（sub-billion scale）在压缩后能力如何退化"这一主题，与轻量化（lightweight）思想有方法论层面的类比价值，但并非遥感场景，也不涉及剪枝/量化/蒸馏等具体压缩技术。

Reading priority: 1分。本文是医疗NLP领域的大语言模型研究，与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及目标检测、遥感影像或任何模型压缩技术。仅"小模型能力退化规律"这一结论有间接的方法论启发，但不足以支撑深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医疗大语言模型（medical language model）的预训练与微调。应用场景：医疗问答（medical QA），包括context-grounded QA和knowledge-recall QA。传感器类型：不适用（纯文本NLP任务）。目标类别：不适用。

Model architecture: MedLLM，0.1B参数（约1亿参数）的开放医疗语言模型。训练管线为三阶段：1）通用预训练（general pretraining），采用curriculum sequence-length scheduling（课程式序列长度调度）；2）领域微调（domain fine-tuning），基于MedFineWeb语料库（从通用网络数据中通过embedding相似度筛选与医疗QA数据相关的参考引导语料）；3）偏好对齐微调（preference-aligned fine-tuning），结合SFT（supervised fine-tuning）与DPO（direct preference optimization）。

Lightweight method: 本文并非传统意义上的模型压缩方法研究（无剪枝pruning、量化quantization、知识蒸馏knowledge distillation、NAS等），而是直接训练一个0.1B参数的小规模模型（sub-billion regime），属于"小模型设计"而非"大模型压缩"。其轻量化思路体现在：通过三阶段训练管线（预训练+领域微调+偏好对齐）在小参数规模下尽可能逼近7B模型性能，并系统刻画了小模型在不同任务类型上的能力边界。

Onboard deployability: 摘要未说明。未提供参数量之外的部署指标（FLOPs、延迟、显存、能耗、实时性等均未提及）。0.1B参数规模本身暗示了较低的存储和计算需求，但本文面向医疗问答场景，未涉及星上/边缘部署（onboard/edge deployment）。

Datasets and metrics: 数据集：MedFineWeb（本文发布的参考引导医疗语料库，从通用网络数据中按embedding相似度筛选）；MedQA（clinical-vignette）；MedMCQA。评价指标：以与7B模型的性能差距（pp，percentage points）衡量，具体包括context-grounded QA和knowledge-recall QA上的准确率对比。

Experiments: 主要结果：1）在context-grounded QA上，MedLLM（0.1B）比医学适配的7B模型低2.9pp，但超过instruction-tuned和通用7B基线；2）在knowledge-recall QA上，MedLLM在MedQA（clinical-vignette）上接近任务下限，但在MedMCQA上显著超过所有7B和sub-7B基线；3）揭示了"医疗能力在压缩下非均匀退化，按任务类型分裂"的现象——该现象在7B规模被掩盖，仅在容量稀缺时显现。摘要未说明与更多基线或消融实验的详细对比。

Limitations: 1）与遥感/卫星/目标检测完全无关，属于纯NLP医疗领域；2）未涉及任何模型压缩技术（剪枝、量化、蒸馏等），而是直接训练小模型；3）未提供部署相关指标（FLOPs、延迟、能耗等）；4）未说明模型在遥感或视觉任务上的泛化能力；5）"能力按任务类型非均匀退化"的结论是否适用于视觉目标检测任务尚待验证。

Ideas for my research: 尽管领域不同，本文有几个方法论层面的借鉴点：1）"小模型能力非均匀退化"的发现提示：在遥感在轨目标检测模型轻量化时，不同任务（如小目标检测 vs 大目标检测、舰船 vs 飞机检测）对模型容量压缩的敏感度可能不同，应针对性地评估轻量化对不同类别/尺度目标的性能影响，而非只看平均mAP；2）curriculum sequence-length scheduling（课程式训练调度）的思想可类比迁移到遥感检测模型的渐进式输入分辨率/感受野训练策略；3）参考引导语料筛选（embedding similarity筛选高质量数据）可类比为遥感领域的高价值样本选择/难例挖掘策略；4）SFT+DPO的偏好对齐思想可类比为检测模型的后处理/置信度校准优化。但整体借鉴价值有限，建议仅作方法论参考。

Abstract: Open medical language models have converged on a single scale: every widely used system runs at 7B parameters or more, leaving the sub-billion regime uncharacterized. We present MedLLM, an open 0.1B-parameter medical language model trained through a fully open three-phase pipeline: general pretraining with curriculum sequence-length scheduling, domain fine-tuning on MedFineWeb, a reference-guided medical corpus we release that is selected from general web data by embedding similarity to medical question-answering (QA) data, and preference-aligned fine-tuning combining SFT with direct preference optimization (DPO). Across medical benchmarks, MedLLM shows a pattern visible only at sub-billion scale: medical competence does not degrade uniformly under compression but splits by task type. On context-grounded QA it comes within $2.9$pp of a medically adapted 7B model and surpasses the instruction-tuned and general-purpose 7B baselines; on knowledge-recall QA it stays near the task floor on clinical-vignette MedQA yet significantly exceeds every 7B and sub-7B baseline on MedMCQA, indicating that where recall fails the constraint is model capacity rather than adaptation. This dissociation is masked at 7B, where both capabilities are present, and surfaces only when capacity is scarce.

</details>


### [149] [A dataset of rated conceptual arguments](https://arxiv.org/abs/2607.27499)
*Emery Cooper,Caspar Oesterheld,Linh Chi Nguyen,Alexander Kastner,Ethan Perez*

Main category: cs.AI

TL;DR: 本文提出一个关于"概念性问题"（无标准答案的哲学/伦理/决策类问题）的论证批判数据集，并评测大语言模型在辩论式推理上的能力。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型（LLM）在哲学、伦理、AI安全等"概念性问题"上的论证推理能力，不涉及任何遥感、卫星、目标检测或模型压缩内容。属于泛NLP/LLM推理研究，与本课题方向弱相关（仅在大模型推理层面有间接联系）。

Reading priority: 1分。论文主题为LLM概念性推理与论证评估，与遥感卫星在轨目标检测及轻量化完全无关，无阅读价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型的概念性推理与论证评估；应用场景：哲学、AI安全、决策理论、社会选择等；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构。仅提及"benchmark a range of models"（评测一系列模型），并提出了两种scoring functions用于评估论证质量。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量化网络设计。

Onboard deployability: 不涉及。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时推理信息，与星上/边缘部署无关。

Datasets and metrics: 数据集：951条论证性批判（argumentative critiques）对应442篇立场文本（position texts），涵盖AI安全、决策理论、伦理、政治等主题；由6位专家评分，共1,458条评分，维度包括centrality（中心性）、strength（强度）、correctness（正确性）、clarity（清晰度）。评价指标：摘要未明确列出具体指标，仅提及两种scoring functions。

Experiments: 摘要提及benchmark了多种模型，结果显示模型性能与通用能力排名（general capability rankings）一致。未给出具体对比结果或消融实验细节。

Limitations: 与本研究方向完全无关。摘要未提供任何与遥感、目标检测、模型压缩或部署相关的信息。此外，该研究本身也面临"概念性问题无ground truth"的评估挑战，摘要对此有所讨论但未给出最终解决方案。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接联系是：如果未来在轨目标检测中引入大语言模型进行场景理解或推理，本文关于"论证评估"的思路或可参考，但这与本课题的轻量化目标检测核心方向相距甚远。不建议从本文提取技术路线。

Abstract: Large language models have improved rapidly on tasks with verifiable answers, such as mathematics and programming. Much less is known about their ability to reason about what we call conceptual questions: questions for which no ground truth is realistically accessible and no widely accepted resolution methodology exists, but on which progress can still be made by debating arguments. Most philosophical questions are of this kind, as are central components of questions in AI safety, decision theory, and social choice. Our approach is based on the view that while bottom-line conclusions on such questions are hard to evaluate, individual contextualized arguments can be evaluated far more reliably. We therefore introduce a dataset of 951 argumentative critiques of 442 position texts, spanning topics from AI safety and decision theory to ethics and politics, with 1,458 ratings by six expert raters along dimensions including centrality, strength, correctness, and clarity. We propose two scoring functions and benchmark a range of models. Performance tracks general capability rankings.

</details>


### [150] [Automated Transcript Analysis for Detecting Flaws in Agentic Benchmarks](https://arxiv.org/abs/2607.27518)
*Jeff Mohl,Nelson Gardner-Challis,Magda Dubois,Harry Coppock,Benjamin Allan-Rahill,Kaelan Yim,Damian Sójka,James Mann,Justin Olive*

Main category: cs.AI

TL;DR: 本文提出用AI扫描器自动审计agentic benchmark（如SWE-Bench-Verified、Inspect Evals）的有效性问题，检测ground truth访问、工具故障、猜测漏洞和答案格式歧义四类缺陷，作为人工审计的规模化替代方案。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型/智能体（agentic）基准测试的有效性审计，属于AI评估方法论领域，不涉及遥感、卫星、Earth observation、目标检测或任何模型压缩/加速技术。相关性评分：1分。

Reading priority: 1分。论文主题为AI基准测试有效性审计，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：基准测试（benchmark）有效性审计；应用场景：AI智能体（agentic AI）能力评估；传感器类型：不适用；目标类别：不适用。摘要未说明具体应用场景与遥感相关。

Model architecture: 摘要未说明具体模型架构。论文提出的是"AI scanners"用于自动检测基准测试中的四类有效性问题（ground truth access, tool failure, guessing vulnerability, answer format ambiguity），但未给出scanner的具体网络结构或算法设计细节。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署相关技术。

Onboard deployability: 不适用。摘要未涉及参数量、FLOPs、延迟、显存、能耗、实时推理或任何部署相关信息。

Datasets and metrics: 数据集：SWE-Bench-Verified、Inspect Evals benchmarks（held-out test set）；评价指标：scanner检测结果与人工标注（human labels）的一致性/有效性检出率。摘要未说明具体数值指标。

Experiments: 在五个广泛使用的benchmark上识别出若干已验证的质量问题，部分问题难以通过随机人工检查发现。但并非所有问题都被检出，scanner性能在不同benchmark、标准和模型间差异显著。摘要未给出具体对比实验数据和消融实验。

Limitations: 1. 与遥感目标检测和轻量化方向完全无关，无法直接借鉴；2. scanner性能在不同benchmark、标准和模型间差异大，可靠性不稳定；3. 存在未检出的问题案例，检测覆盖率有限；4. 摘要未说明scanner的计算开销、可扩展性及实际部署成本。

Ideas for my research: 弱相关，但可抽象出两点方法论借鉴：1) 自动化审计/验证的思路可类比于在轨模型部署前的自动化质量校验流程——例如用自动化工具检测轻量化模型在边缘设备上的推理异常或精度退化；2) "ground truth access"检测思想可类比于在轨场景中验证模型是否依赖了不合理的先验信息或数据泄漏。但整体借鉴价值有限。

Abstract: Capabilities of frontier models are often assessed using agentic benchmarks. To trust these results, benchmarks must accurately measure what they claim to and be free from invalidating flaws. Previous manual audits of benchmarks such as SWE-Bench-Verified have uncovered several validity issues in transcripts. However, manual review is difficult to scale, and it is unclear whether automated methods can reliably surface flaws that compromise benchmark validity. In this paper, we developed AI scanners to detect four types of validity issues: ground truth access, tool failure, guessing vulnerability, and answer format ambiguity. We produced grading rubrics for each to instruct human labeling, and evaluated the scanners against human labels on a held-out test set of Inspect Evals benchmarks. Our scanners identified several verified quality issues in five widely used benchmarks, including cases unlikely to be caught by random manual inspection. Not all cases were identified, and scanner performance varied substantially across benchmarks, criteria and models. We highlight several open challenges to be addressed to improve scanners for stronger quality assurance claims, including broader standardization gaps in the evaluation field that degrade scanner performance. Together, these results serve as a proof of concept for using automated transcript analysis to audit benchmark quality more broadly.

</details>


### [151] [AI Literacy: An Exercise in Power-Knowledge](https://arxiv.org/abs/2607.27547)
*Brady D. Lund,Zoë Abbie Teel*

Main category: cs.AI

TL;DR: 本文从批判教育学与福柯权力-知识理论视角，重新概念化AI素养为一种批判性实践，强调培养个体的认知主体性而非技术消费者角色。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于AI素养教育、批判教育学与数字素养的跨学科理论探讨，不涉及任何遥感、卫星、目标检测或模型压缩技术内容。相关性评分：1分。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测模型轻量化"研究方向完全无关，属于AI素养教育与社会批判理论领域，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型为AI素养的理论重构与批判性教育框架构建，属于教育学/哲学/社会科学范畴。不涉及遥感、Earth observation、aerial image、SAR、高光谱或多光谱等传感器场景，也不涉及任何目标类别检测。

Model architecture: 摘要未说明。论文不涉及任何机器学习模型或网络结构设计，属于纯理论性论述。

Lightweight method: 摘要未说明。论文不涉及pruning、quantization、knowledge distillation、NAS等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上部署、边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、显存或能耗等任何部署相关信息。

Datasets and metrics: 摘要未说明。论文不涉及任何实验数据集或评价指标，属于理论性论文，无实验设置。

Experiments: 摘要未说明。论文无实验、对比方法或消融实验，核心内容为基于Foucault、Freire及数字素养理论的三部分AI素养框架（contextual use, critical interrogation, participatory governance）的哲学论证。

Limitations: 该论文与遥感目标检测及轻量化方向完全无关。其局限性在于：作为纯理论性论文，缺乏实证验证；且其讨论的AI素养框架面向通用AI使用场景，与工程化的遥感在轨智能系统没有任何技术交集。

Ideas for my research: 对本课题无可借鉴的技术想法。唯一可能的间接启发是：论文强调"critical interrogation"（批判性审视）的思维方式，可类比于在模型轻量化过程中对已有方法的批判性评估，但这属于方法论层面的泛化启发，不构成具体技术借鉴。

Abstract: As generative artificial intelligence becomes one of the most significant systems of knowledge production in our society today, questions relating to who can access and shape that production grow increasingly important in our discourse. This paper argues that the existing frameworks for AI literacy, which are dominated by technical competency and responsible-use principles, are insufficient because they enforce a "consumer" orientation toward AI rather than fostering genuine epistemic agency. Based upon Foucault's concept of power-knowledge, Freire's pedagogy of critical consciousness, and scholarship of digital literacy, this paper proposes a reconceptualization of AI literacy as a critical practice that equips individuals not just to use AI systems, but to critically evaluate them, resist their structuring assumptions, and participate in their governance. The paper further argues that unequal access to AI tools in society recapitulates longstanding epistemic injustices, and that a literacy framework oriented toward empowerment must account for these structural inequities. A three-part framework of AI literacy based on the notions of contextual use, critical interrogation, and participatory governance frames this literacy as a cultivation of epistemic "agents" rather than the training of competent consumers of AI-generated information.

</details>


### [152] [Using Large Language Models for Idea Generation in Innovation](https://arxiv.org/abs/2607.27553)
*Lennart Meincke,Karan Girotra,Gideon Nave,Christian Terwiesch,Karl T. Ulrich*

Main category: cs.AI

TL;DR: 本文研究大语言模型（LLM）在生成新产品创意方面的效果，对比GPT-4与大学生生成的产品创意质量，发现AI生成的创意在购买意愿上更优但新颖性和多样性较差。

Research relevance: 与本研究方向基本无关（1分）。论文主题是LLM用于新产品创意生成（product idea generation），属于自然语言处理与市场营销交叉领域，完全不涉及遥感、卫星、目标检测或模型轻量化。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向完全无关，属于产品设计与NLP交叉领域，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务：新产品创意生成（new product idea generation）；场景：面向大学生的低价产品设计；传感器类型：不适用；目标类别：不适用。

Model architecture: 使用GPT-4（OpenAI）大语言模型，采用zero-shot和few-shot prompting两种提示策略生成产品创意。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（pruning, quantization, knowledge distillation, NAS等均未提及）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：三组产品创意池（大学生设计课程生成的创意、GPT-4 zero-shot生成的创意、GPT-4 few-shot生成的创意）；评价指标：平均购买意愿概率（average purchase intent probability）、文本挖掘评估创意相似度（text mining for idea similarity）、人工评分评估新颖性（human raters for novelty）、Top 10%创意排名概率。

Experiments: 主要发现：AI生成的创意在平均购买意愿上优于人类创意，few-shot略优于zero-shot；但AI创意被认为新颖性较低且相似度更高（尤其few-shot），多样性不足；聚焦最佳创意时，AI创意进入Top 10%的概率是人类创意的7倍。

Limitations: 摘要未说明。但明显局限包括：仅针对大学生和50美元以下产品，泛化性有限；未考虑AI生成创意的实际可制造性、成本可行性等；未讨论LLM的推理成本与部署问题。

Ideas for my research: 与遥感在轨目标检测轻量化课题基本无可借鉴点。唯一可能的间接启发是：如果未来考虑用LLM辅助遥感领域的数据增强或标注生成，可以参考其prompting策略（zero-shot vs few-shot）对输出质量的影响，但这与本课题核心方向关联极弱。

Abstract: This research evaluates the efficacy of large language models (LLMs) in generating new product ideas. To do so, we compare three pools of ideas for new products targeted toward college students and priced at 50 dollars or less. The first pool of ideas was created by university students in a product design course before the availability of LLMs. The second and third pools of ideas were generated by GPT-4 from OpenAI using zero-shot and few-shot prompting, respectively. We evaluated idea quality using standard market research techniques to predict average purchase intent probability. We used text mining to assess idea similarity and human raters to evaluate idea novelty. We find that AI-generated ideas outperform human-generated ideas in terms of average purchase intent, with few-shot prompting yielding slightly higher intent than zero-shot prompting. However, AI-generated ideas are perceived as less novel and exhibit higher pairwise similarity, particularly with few-shot prompting, indicating a less diverse solution landscape. When focusing on the quality of the best ideas rather than the average ideas, we find that AI-generated ideas are seven times more likely to rank among the top 10 percent of ideas, demonstrating a significant advantage over human-generated ideas. We propose that this seven-to-one advantage is a conservative estimate because it does not account for the greater productivity of AI. Our findings suggest that despite some drawbacks, AI creativity presents a substantial benefit in generating high-quality ideas for new product development.

</details>


### [153] [Evaluating Agentic Bioinformatics through Function, Evidence, and Validation](https://arxiv.org/abs/2607.27556)
*Phuc Pham,Truong-Son Hy*

Main category: cs.AI

TL;DR: 提出 Function–Evidence–Validation (FEV) 框架，以可检查的工作流轨迹为分析单元，对109个生物信息学智能体系统和28个基准资源进行系统梳理，主张以工作流正确性而非最终答案正确性来评估智能体系统的科学可信度。

Research relevance: 弱相关（1分）。论文聚焦生物信息学（bioinformatics）领域的 large language model agents，涉及基因组学、单细胞、蛋白质科学、药物发现、计算病理学等，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。不涉及 object detection、remote sensing、轻量化（pruning/quantization/distillation/NAS）等任何本方向核心技术。仅有的潜在关联是"agentic workflow"概念可能对遥感在轨智能体系统设计有间接启发，但非常牵强。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于生物信息学智能体评估领域。除非对"智能体工作流可审计性评估"这一跨领域概念感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：生物信息学智能体（agentic bioinformatics）系统的评估与分类。应用场景：genomics、single-cell and spatial omics、protein science、drug discovery、computational pathology、general bioinformatics automation。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测场景）。

Model architecture: 摘要未说明具体模型架构。论文提出的是评估框架 FEV（Function–Evidence–Validation），将智能体系统的工作流轨迹（inspectable workflow trajectory）作为主要分析单元，而非关注具体网络结构。涉及109个 agentic or agent-adjacent systems 和28个 benchmark/evaluation resources，共128篇出版物。

Lightweight method: 不涉及。论文未讨论任何模型压缩或加速方法，如 pruning、quantization、knowledge distillation、NAS、轻量网络设计等均未提及。

Onboard deployability: 不适用。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息，摘要未说明。

Datasets and metrics: 数据集/资源：28个 benchmark or evaluation resources，覆盖 genomics、single-cell and spatial omics、protein science、drug discovery、computational pathology、general bioinformatics automation 等领域。评价指标：摘要未说明具体量化指标，但提出以 workflow correctness（工作流正确性）而非 final-answer correctness（最终答案正确性）作为评估核心，并强调 replayability、provenance、robust scientific assessment、external validation、prospective empirical testing 等维度。

Experiments: 摘要未说明具体实验设置。论文对109个智能体系统和28个基准资源进行了系统映射（mapping），跨领域发现 planning 和 tool-mediated execution 进展较快，而 replayability、provenance、robust scientific assessment、external validation、prospective empirical testing 进展较慢。未提供对比方法或消融实验。

Limitations: 1. 论文与遥感/卫星/目标检测/轻量化方向完全无关，属于生物信息学领域。2. 摘要未提供任何定量实验结果、模型性能数据或部署指标。3. 摘要未说明 FEV 框架的具体操作化方法、评分标准或验证方式。4. 摘要未说明该框架是否可迁移到其他领域（如遥感智能体）。

Ideas for my research: 1. 尽管领域不同，FEV 框架中"以可检查的工作流轨迹而非最终输出作为分析单元"的思想，可类比借鉴到遥感在轨目标检测智能体/自动化pipeline的评估中——即不仅评估最终检测精度，还应评估中间处理步骤（如预处理、候选框生成、后处理）的可追溯性和可审计性。2. "workflow correctness vs final-answer correctness"的区分，对遥感在轨模型的可解释性和可靠性评估有启发。3. 除此之外，论文对"遥感卫星在轨目标检测模型轻量化"课题的直接借鉴价值非常有限。

Abstract: Large language model agents increasingly plan, execute, and interpret biological analyses, yet fluent responses, successful tool calls, and benchmark performance alone do not establish scientific credibility. Existing reviews primarily organize biological agents by application, architecture, and agentic capability, but do not jointly operationalize the accountability of agent-generated workflows. We address this gap by treating the inspectable workflow trajectory, rather than architecture or final output alone, as the primary unit of analysis. We introduce the Function--Evidence--Validation (FEV) framework, which separates demonstrated workflow operations, traceable support for actions and claims, and use-case-specific validation. Using FEV, we map 109 agentic or agent-adjacent systems and 28 benchmark or evaluation resources, representing 128 unique publications across genomics, single-cell and spatial omics, protein science, drug discovery, computational pathology, and general bioinformatics automation. Across domains, planning and tool-mediated execution have advanced more rapidly than replayability, provenance, robust scientific assessment, external validation, and prospective empirical testing. We therefore argue that agentic bioinformatics should be assessed through workflow correctness rather than final-answer correctness alone. FEV provides a practical basis for comparing systems and designing transparent, auditable, and scientifically accountable bioinformatics workflows.

</details>


### [154] [DeepResearch Agent System](https://arxiv.org/abs/2607.27562)
*Yong Huang,Yulu Huang,for the team Collaboration*

Main category: cs.AI

TL;DR: 本文介绍了一个基于稀疏激活架构的大语言模型智能体系统（DeepResearch Agent System），用于深度信息检索、多步推理和自主研究任务，强调推理速度、长上下文处理和工具调用能力。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文主题是大语言模型（LLM）智能体系统，面向信息检索、多步推理和自主研究任务，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景，也不涉及目标检测任务，更不涉及剪枝、量化、知识蒸馏等模型压缩方法。虽然提到了稀疏激活架构（sparse activation）和推理加速（3.2x faster inference），但这属于LLM领域的MoE类稀疏激活机制，与遥感目标检测模型的轻量化部署没有直接关联。

Reading priority: 1分。论文主题为LLM智能体系统，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然稀疏激活和推理加速概念有一定通用性，但缺乏遥感场景、目标检测任务和部署优化的直接关联，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：深度信息检索、多步推理、自主研究（agent search / research tasks）。应用场景：学术研究、商业分析、研发支持、教育。传感器类型：不适用（非遥感场景）。目标类别：不适用（非目标检测任务）。

Model architecture: 基于稀疏激活架构（sparse activation architecture）的大语言模型，总参数量300亿（30B），每个token仅激活30亿（3B）参数。支持128K-token上下文窗口，采用层级注意力机制（hierarchical attention mechanisms）。包含双模式推理引擎：ReAct范式（基础多步问题求解）和IterResearch模式（高性能迭代研究，最多20步推理）。多工具协调模块整合了检索、计算、网页搜索和文件解析。训练采用基于GRPO算法的强化学习优化框架。

Lightweight method: 摘要中提到的"轻量化"相关信息主要是稀疏激活架构（sparse activation），即MoE（Mixture of Experts）类机制——总参数30B但每个token仅激活3B参数，相比同等规模的稠密模型（dense counterparts）推理速度快3.2倍。这属于LLM领域的稀疏激活/条件计算加速方法，并非遥感目标检测领域常用的剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS等模型压缩技术。摘要未说明任何面向边缘部署或星上部署（onboard deployment）的优化。

Onboard deployability: 摘要未说明星上或边缘部署相关信息。虽然提到了推理加速（3.2x faster inference），但这是针对LLM推理场景，且模型总参数量达30B，即使稀疏激活后每token仅激活3B参数，对于遥感卫星在轨部署（通常受限于FPGA/NPU算力、内存和功耗）而言仍然过于庞大。摘要未提供参数量（除总参数外）、FLOPs、延迟、显存、能耗等具体部署指标。

Datasets and metrics: 评测基准：Humanity's Last Exam（87.3%）、BrowserComp Chinese（85.3%）、WebWalkerQA（91.2%）。指标：推理速度提升3.2倍、长上下文准确率提升18.7%、召回率提升23.4%、多步推理准确率提升31.2%、工具调用准确率92.1%、训练稳定性提升35%、收敛速度提升42%、数据合成可用率92.5%。这些均为LLM/agent基准，与遥感目标检测常用的数据集（如DOTA、HRSC2016、NWPU VHR-10、SSDD、DIOR等）和指标（mAP、AP50、AP75、FPS等）无关。

Experiments: 摘要报告了在多个agent搜索基准上的SOTA性能，包括Humanity's Last Exam、BrowserComp Chinese、WebWalkerQA，以及消融性对比（vs. 稠密模型、vs. 标准长上下文方法、vs. 单次推理基线）。但未提供与遥感目标检测相关的任何实验对比。

Limitations: 1. 与遥感卫星在轨目标检测方向完全无关，属于LLM/agent系统研究。2. 摘要未说明模型在真实遥感数据上的表现。3. 稀疏激活架构虽然降低了单token推理成本，但整体模型规模（30B参数）和推理资源需求远超星上部署能力。4. 摘要未提供任何关于模型压缩（剪枝、量化、蒸馏）或边缘/星上部署的讨论。

Ideas for my research: 尽管论文主题与遥感目标检测无关，但仍有少量可借鉴的通用思路：1. 稀疏激活/条件计算（sparse activation / conditional computation）的思想可以类比到遥感目标检测模型中——例如在检测网络中对不同尺度的特征图或不同复杂度的输入图像采用动态路由/条件计算，仅激活必要的子网络，从而降低平均推理成本。2. 层级注意力机制（hierarchical attention）对长上下文的高效处理思路，可类比到高分辨率遥感大图的全局-局部特征建模。3. 强化学习优化框架（GRPO）的训练稳定性提升思路，可借鉴到检测模型轻量化后的微调训练中。但这些均为间接借鉴，整体相关性很弱。

Abstract: The DeepResearch Agent System is a large language model system engineered for deep information retrieval, multi-step reasoning, and autonomous research tasks. Built upon a sparse activation architecture with 30 billion total parameters of which only 3 billion are activated per token, the system achieves state-of-the-art performance on multiple agent search benchmarks while delivering 3.2 times faster inference compared to dense counterparts of equivalent scale. The system supports a 128K-token context window with hierarchical attention mechanisms that yield 18.7% accuracy and 23.4% recall improvements over standard long-context approaches. A dual-mode reasoning engine provides both a ReAct paradigm for basic multi-step problem solving and an IterResearch mode for high-performance iterative research with up to 20 reasoning steps, collectively delivering a 31.2% accuracy improvement over single-pass baselines. Multi-tool coordination integrates retrieval, computation, web search, and file parsing modules to achieve 92.1% tool-use accuracy. A reinforcement learning optimization framework based on the GRPO algorithm provides token-level policy gradients that improve training stability by 35% and accelerate convergence by 42%. An automated data synthesis pipeline with seed-based expansion achieves a 92.5% usability rate. Benchmark results include 87.3% on Humanity's Last Exam, 85.3% on BrowserComp Chinese, and 91.2% on WebWalkerQA. The system is fully open-sourced, including data synthesis, training, and inference code, and supports applications in academic research, business analysis, R&D support, and education.

</details>


### [155] [What makes prompts a graph: necessary and sufficient conditions for prompt graph engineering](https://arxiv.org/abs/2607.27578)
*Sandeco Macedo*

Main category: cs.AI

TL;DR: 本文提出"prompt graph engineering"（提示图工程）的操作性定义与共享词汇，将提示视为显式、可执行、可改进的图中的节点，并给出包含/排除测试以界定该概念边界。

Research relevance: 弱相关（2分）。该论文属于自然语言处理/大语言模型（LLM）领域的prompt engineering与agent编排研究，讨论的是LangGraph、DSPy、Prompt Flow等LLM工作流框架中的图结构概念。与遥感、卫星、Earth observation、目标检测、模型轻量化均无直接关联，属于泛机器学习/泛AI方法研究。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM prompt engineering领域的概念性研究，不包含任何可迁移的检测或轻量化技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：prompt graph engineering概念定义与词汇体系构建（概念性/综述性研究）。应用场景：LLM多智能体系统、prompt编排、agent工作流。传感器类型：不适用（非遥感）。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。论文讨论的是将prompt作为图节点、以图结构组织LLM调用链的工程范式，涉及LangGraph、DSPy、Prompt Flow、AutoGen、CrewAI、Claude Code subagents等框架，但未给出具体网络结构设计。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络等模型压缩或加速方法。其"优化"指的是对prompt图结构的自动优化，而非模型轻量化。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明。论文采用概念分析（conceptual analysis）与灰色文献（grey literature）方法，对六个真实系统（LangGraph、DSPy、Prompt Flow、AutoGen、CrewAI、Claude Code subagents）应用包含/排除测试，但未使用标准机器学习数据集或量化评价指标。

Experiments: 摘要未说明传统实验。论文的"验证"方式是将所提出的包含/排除测试应用于六个真实系统，结果显示该测试能一致地包含和排除相关系统，属于概念验证而非实验对比。

Limitations: 1. 与遥感卫星在轨目标检测及模型轻量化方向基本无关，缺乏可迁移的技术方法。2. 论文为概念性/定义性研究，不包含任何实验数据、性能指标或工程实现细节。3. 摘要未说明该定义对实际系统性能提升的量化证据。4. 属于LLM/agent领域，与视觉目标检测任务无交集。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是：该论文强调"将复杂流程显式建模为可执行、可优化的图结构"这一思想，在遥感在轨目标检测流水线（如检测-跟踪-压缩-传输的星上多模块协同）中，或许可以借鉴"显式结构+模块解耦+可执行语义"的思路来组织星上推理pipeline。但这是非常间接的类比，不构成实质性的技术借鉴。

Abstract: Prompts stopped being isolated strings some time ago. In real systems, one model call feeds another, retrieval interleaves with generation, routers branch, and aggregators merge parallel results. Practice converged on a single structure to hold this together: the graph. Frameworks such as LangGraph, DSPy, and Prompt Flow expose it openly, and research systems already optimize it automatically. The vocabulary, however, lags behind. Graph names, variously, a reasoning topology inside one sampling strategy, a multi-agent conversation, or an orchestration artifact, while prompt engineering still evokes writing one good string. What is missing is a reference definition treating prompts as nodes of an explicit, executable, improvable graph. We build that definition through conceptual analysis over sources with persistent identifiers, complemented by primary grey literature. We reconstruct the genealogy of the idea, from dataflow graphs and build systems, through prompt chaining and the thought topologies (chain, tree, graph), to graphs compiled and optimized as artifacts. We then propose a constitutive definition of prompt graph engineering, state its four conditions (explicit structure, separation between structure and prompt content, executable semantics, and the graph as a first-class engineering artifact), and operationalize them as an inclusion and exclusion test. We draw the boundary against six neighboring concepts and apply the test to six real systems (LangGraph, DSPy, Prompt Flow, AutoGen, CrewAI, and Claude Code subagents); it includes and excludes consistently. We close with a research agenda organized along four design tension axes. The contribution is an operational definition and a shared vocabulary for a practice that industry already exercises daily without naming precisely.

</details>


### [156] [World Action Planner: Generalizable Decision-Making with Action-Conditioned World Models](https://arxiv.org/abs/2607.27599)
*Xiangcheng Zhang,Yilun Du*

Main category: cs.AI

TL;DR: 提出World Action Planner，一种结合Vision-Language Models推理能力与多任务位姿图像条件世界模型的机器人规划系统，通过想象世界模型推演来迭代优化动作规划，提升跨场景零样本泛化能力。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦机器人动作规划（robot planning），涉及Vision-Language Models、world model、imitation learning等，既非遥感/卫星场景，也非目标检测任务，更不涉及任何模型轻量化或部署优化。属于泛机器人学习/泛机器学习方法，与本方向弱相关。

Reading priority: 1分。该论文聚焦机器人动作规划与VLM推理，与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及遥感场景、目标检测或模型压缩部署，仅可作为泛化推理范式的边缘参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人动作规划（robot planning）与零样本泛化；应用场景：机器人操作、组合任务、新布局场景；传感器类型：摘要未说明（推测为视觉传感器，但未明确）；目标类别：不涉及目标检测，面向机器人动作序列规划。

Model architecture: 系统由两部分组成：(1) Vision-Language Models (VLMs) 负责推理并生成初始动作规划；(2) 多任务位姿图像条件世界模型（multi-task pose-image conditioned world model）提供物理grounding，通过想象世界模型推演（imagined world model rollouts）进行优化与搜索，迭代精炼动作规划。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。且该工作面向机器人规划而非星上/边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面仅提及在组合任务（compositional tasks）、新布局（new layouts）和零样本泛化（zero-shot generalization）场景下优于state-of-the-art端到端策略模型（如VLAs和WAMs），但未给出具体量化指标。

Experiments: 摘要仅说明在组合任务、新布局和零样本泛化场景中显著优于VLAs（Vision-Language Action models）和WAMs（World Action Models）等端到端策略模型，未提供具体实验数据、对比表格或消融实验细节。

Limitations: 摘要未说明局限性。主要缺口包括：未提供具体数据集与指标、未讨论计算开销与推理效率、未涉及任何部署或轻量化考量、未说明VLM推理的实时性瓶颈。

Ideas for my research: 借鉴意义有限，但可提取两点思路：(1) "world model rollout + 迭代优化"的推理范式，在遥感目标检测中可类比为利用场景先验/上下文模型对检测结果进行迭代精化，提升小目标或遮挡目标的检测鲁棒性；(2) VLM推理与物理模型结合的多模态思路，可启发遥感领域将视觉-语言模型与领域知识结合用于场景理解，但需注意VLM在星上部署的算力约束，这反而凸显了轻量化VLM/检测模型的研究价值。

Abstract: Building generalizable agents for diverse applications remains a fundamental challenge. While imitation learning-based policies succeed in specific training environments, they often fail to generalize to novel scenes and tasks. In this work, we propose World Action Planner, a robot planning system that leverages the reasoning capabilities of Vision-Language Models (VLMs) and the physical grounding of a multi-task pose-image conditioned world model. Our system enables an agent to propose initial action plans and iteratively refine them via optimization and search, reasoning over imagined world model rollouts. We demonstrate that our approach achieves superior performance across compositional tasks, new layouts, and zero-shot generalization scenarios, significantly outperforming state-of-the-art end-to-end policy models such as VLAs and WAMs. Project website at worldactionplanner.github.io

</details>


### [157] [From Minds to Models: The Intersection of Psychology and LLM Behaviours](https://arxiv.org/abs/2607.27579)
*Oliver Guidetti,Reza Ryan*

Main category: cs.AI

TL;DR: 本文研究使用心理学方法（基于Implicit Association Test的prompt改编）检测ChatGPT在开放式文本中是否表现出跨种族的情感差异，发现证据较弱且依赖分析方法。

Research relevance: 与本研究方向基本无关（1分）。论文主题是LLM（ChatGPT）的隐性偏见检测，属于心理学与NLP交叉领域，完全不涉及遥感、卫星、Earth observation、目标检测或模型轻量化。既无遥感场景，也无目标检测任务，更无任何模型压缩/加速/部署相关内容。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM隐性偏见检测（Implicit Association Test的prompt改编）；应用场景：政府与医疗决策中的AI行为分析；传感器类型：不适用（文本数据）；目标类别：种族类别（8个种族类别+种族无关对照）。

Model architecture: 不涉及视觉模型架构。研究对象为GPT-3.5T、GPT-4、GPT-4T三个LLM，通过126个prompt（14个基础问题×8个种族类别+对照）进行开放式文本生成。

Lightweight method: 摘要未说明。本文不涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化方法。

Onboard deployability: 摘要未说明。本文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：自建prompt集（126个prompt，378个响应）；评价指标：情感得分（sentiment scores，基于分类标签与源得分的组合编码）、two-way ANOVA（F统计量、p值、partial-eta squared）、rank-transformed sensitivity analysis、Tukey校正。

Experiments: 主要结果：two-way ANOVA发现种族条件有小的主效应（F(8,351)=2.04, p=.042, partial-eta²=.044），但模型无主效应、无交互效应；秩变换敏感性分析中效应不保留（p=.145）；Tukey校正后无显著两两差异；仅未校正的European-Indigenous Australian比较显著（事后选择，仅作假设生成）。结论：情感差异证据弱且依赖分析方法。

Limitations: 1. 情感得分无法区分评价性偏见与prompt引发的历史内容效价；2. 效应在敏感性分析中不稳健；3. 显著结果多为事后选择，未校正；4. 与遥感/目标检测/轻量化方向完全无关，无可借鉴的技术内容。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题无可借鉴点。本文属于心理学/NLP交叉领域，不涉及任何视觉检测、遥感数据处理或模型压缩技术。唯一可能的间接启发是"敏感性分析/稳健性检验"的实验方法论（如rank-transformed sensitivity analysis验证结论稳健性），可类比用于轻量化方法在不同数据集/配置下的鲁棒性验证，但关联非常间接。

Abstract: Large language models (LLMs) are often compared with the human mind because their decision-making is complex, non-linear and difficult to interpret. Psychological methods developed to investigate unobservable mental processes may therefore help examine LLM behaviour, particularly in government and healthcare. Building on prompt-based adaptations of the Implicit Association Test, this study tested whether ChatGPT produced sentiment differences across racial conditions in open-ended text. Fourteen base questions were crossed with eight racial categories and a race-agnostic control, producing 126 prompts. Each was submitted once to GPT-3.5T, GPT-4 and GPT-4T, yielding 378 responses. Sentiment scores were derived from categorical labels and source scores: positive labels retained the source score, negative labels were assigned its negative, and neutral responses were coded zero. A two-way ANOVA found a small main effect of racial condition, F(8, 351) = 2.04, p = .042, partial-eta squared = .044, but no effect of model, F(2, 351) = 0.07, p = .933, and no interaction, F(16, 351) = 0.23, p = .999. However, the effect was not retained in a rank-transformed sensitivity analysis, F(8, 351) = 1.53, p = .145, and Tukey-corrected comparisons found no significant pairwise differences. An uncorrected European-Indigenous Australian comparison was significant, but was selected post hoc and is reported only as hypothesis-generating. Evidence for sentiment differences was therefore weak and analysis-dependent. Sentiment scoring also cannot distinguish evaluative bias from the valence of historical content elicited by a prompt. We outline design changes needed to address these limitations and argue for interdisciplinary development of behavioural measures of model bias.
  Keywords: Implicit Bias, Psychological Research Methods, Artificial Intelligence, ChatGPT, Large Language Models, Sentiment Analysis

</details>


### [158] [HALO: Heterogeneous Admission through Localized Obligations for Safe Agentic Execution](https://arxiv.org/abs/2607.27636)
*Taewoo Park,Kyeonghyun Yoo,Kiseok Kim,Seunghyun Yoo,Hwangnam Kim*

Main category: cs.AI

TL;DR: 提出HALO运行时协议，用于agentic AI系统异构响应的准入控制，在组件级保留仍受支持的组件并重新检查动作，避免整体拒绝导致的有用组件丢失。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于agentic AI系统的运行时准入协议（HALO），涉及异构响应处理、组件依赖管理和动作重检查，属于AI系统/代理系统领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于agentic AI系统运行时协议研究，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：agentic AI系统的运行时准入控制（admission control）协议设计。应用场景：PX4/Gazebo无人机仿真环境中的冷启动会话。传感器类型：不涉及。目标类别：不涉及。

Model architecture: HALO协议包含三个核心机制：(1) 保留声明前提仍受支持的组件（preserve supported components whose declared prerequisites remain supported）；(2) 在分发前重新检查每个具体动作（recheck each exact action before dispatch）；(3) 允许被阻塞的动作仅由新候选替换（blocked actions replaced only by fresh candidates）。属于运行时协议而非神经网络架构。

Lightweight method: 不涉及。论文未提出任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量网络设计方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。虽然涉及PX4/Gazebo仿真环境，但这是无人机控制场景，与星上部署无关。

Datasets and metrics: 评估方式包括：(1) 96个准入期望（admission expectations）全部匹配；(2) 20个协议测试全部通过；(3) 结构化响应回放中保留248/248个受支持组件（其中128/128不受无关变更影响），而整体响应策略仅保留0/248；(4) 10次PX4/Gazebo冷启动会话中，HALO阻塞了所有测试的过期路径（stale route），未观察到匹配的过期设定点（stale setpoint），并完成了所有新恢复（fresh recoveries）。

Experiments: 主要实验包括：准入期望匹配测试（96/96）、协议测试（20/20）、结构化响应回放对比实验（HALO保留248/248 vs 整体响应策略保留0/248）、PX4/Gazebo冷启动会话验证（阻塞所有过期路径并完成新恢复）。

Limitations: 摘要未说明。主要局限可能包括：仅针对agentic AI系统的运行时协议，与遥感目标检测和模型轻量化完全无关；实验场景局限于PX4/Gazebo无人机仿真，未涉及遥感数据或卫星部署环境。

Ideas for my research: 与本人课题基本无可借鉴点。唯一可能的间接启发是：HALO中"组件级保留而非整体拒绝"的思想，在模型压缩场景中可类比为"保留对最终检测精度贡献大的通道/层，而非整体剪枝"，但这属于非常宽泛的类比，实际借鉴价值有限。

Abstract: Recent agentic AI systems may return a heterogeneous response containing notices, requests, handoffs, and actions. Conditions can change before external use, so components from the same response need not remain supported together. Rejecting the whole response discards useful components, whereas checking components independently can leave a dependent without its prerequisite. We present Heterogeneous Admission with Localized Obligations (HALO), a runtime protocol that preserves supported components whose declared prerequisites also remain supported, rechecks each exact action before dispatch, and allows blocked actions to be replaced only by fresh candidates. HALO matched all 96 admission expectations and passed all 20 protocol tests. In structured-response replay, it retained 248/248 supported components, including 128/128 unaffected by unrelated changes, while a whole-response policy retained 0/248. Across ten cold-start PX4/Gazebo sessions, HALO blocked every tested stale route, observed no matching stale setpoint, and completed all fresh recoveries.

</details>


### [159] [Wiring diagram extraction and gluing: a case study in classifying figure skating jumps using 3D dataset](https://arxiv.org/abs/2607.27598)
*Jason Lo,Mohammadnima Jafari*

Main category: cs.AI

TL;DR: 提出一种"胶合接线图"理论，使Hasse聚类算法可以通过迭代方式应用，从而在聚类数量增长时避免组合爆炸的计算不可行问题，并在花样滑冰跳跃视频分类任务中验证了该理论。

Research relevance: 弱相关（1-2分）。该论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景；也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解；更不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理或边缘部署。其核心是Hasse聚类的算法理论改进，属于数据挖掘/序列模式提取的数学方法，应用场景是花样滑冰视频分类，与遥感卫星在轨目标检测及模型轻量化方向基本无关。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于序列数据聚类算法的数学理论工作，应用场景为花样滑冰视频分类，无任何可迁移的检测或轻量化方法，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：序列数据中的模式聚类（Hasse clustering）；应用场景：花样滑冰跳跃视频分类；传感器类型：视频（非遥感传感器）；目标类别：花样滑冰跳跃动作类别（摘要未具体说明类别数）。

Model architecture: 摘要未说明具体网络结构。核心是Hasse聚类算法及其"胶合接线图"（gluing wiring diagrams）理论，通过迭代应用Hasse聚类来替代单次应用，以降低组合复杂度。未涉及任何深度学习模型架构。

Lightweight method: 摘要未涉及任何模型轻量化方法。论文的"迭代化"处理是针对Hasse聚类算法的计算复杂度问题（组合爆炸），而非神经网络模型的压缩或加速。这与剪枝、量化、知识蒸馏、NAS等轻量化手段无关。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。其应用场景为视频分类，与在轨部署无关。

Datasets and metrics: 数据集：花样滑冰跳跃视频数据集（摘要未说明具体数据集名称、规模）；评价指标：摘要未说明（未提及准确率、召回率、F1等具体指标）。

Experiments: 摘要仅说明在花样滑冰跳跃视频分类场景中测试了"胶合接线图"理论，验证迭代应用Hasse聚类与单次应用结果一致。未给出具体实验结果数值、对比方法或消融实验。

Limitations: 摘要未说明局限性。但可推断：1）该理论仅针对Hasse聚类算法，适用范围限于序列模式提取，与深度学习目标检测模型无关；2）实验仅在视频分类单一场景验证，未在遥感或其他视觉任务上测试；3）未提供计算复杂度降低的定量分析。

Ideas for my research: 借鉴意义有限。唯一可能的思想启发是：将复杂问题分解为可迭代的子问题以降低计算复杂度——这一思路在理论上与模型轻量化中的"分解/分治"思想有相通之处，但论文本身并未给出可直接迁移到遥感目标检测模型轻量化的具体方法。不建议作为主要参考。

Abstract: Hasse clustering is an algorithm that extracts common patterns in sequential data and represents them in graphical forms. As the number of expected clusters grows, however, the algorithm can become infeasible to run due to combinatorial complexity. In this article, we describe a theory of gluing wiring diagrams, allowing iterative applications of Hasse clustering to achieve the same result as a single application. We test our theory in the context of classifying videos of figure skating jumps.

</details>


### [160] [CORE: In-Context Reconstruction for Unified Tabular Anomaly Detection](https://arxiv.org/abs/2607.27615)
*Yunfeng Zhao,Qingfeng Chen,Yue Tan,Shiyuan Li,Yili Wang,Yixin Liu,Shirui Pan*

Main category: cs.AI

TL;DR: 提出CORE方法，通过去相关特征对齐模块和上下文重建模块，实现跨数据集的统一表格异常检测（unified TAD），无需标注或合成异常样本。

Research relevance: 弱相关（1-2分）。论文聚焦于表格数据（tabular data）上的异常检测（anomaly detection），属于通用机器学习/数据挖掘领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或边缘部署方法。

Reading priority: 1分。论文聚焦表格异常检测，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。仅当对跨域特征对齐或异常检测范式感兴趣时，可作为方法学参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：表格异常检测（Tabular Anomaly Detection, TAD），统一TAD（unified TAD）。应用场景：通用表格数据中的异常样本识别，非遥感场景。传感器类型：不适用（非图像/遥感数据）。目标类别：异常样本 vs 正常样本，非遥感目标类别。

Model architecture: CORE包含两个核心模块：(1) 去相关特征对齐模块（decorrelated feature alignment module），将异构表格特征直接对齐到统一表示空间，保留语义信息；(2) 上下文重建模块（in-context reconstruction module），利用上下文正常样本重建每个样本，重建误差反映其偏离正常分布的程度。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息，也未涉及星上或边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及"arbitrary unseen datasets"（任意未见数据集），暗示跨数据集泛化评估，但未给出具体实验设置。

Experiments: 摘要未说明具体实验结果、对比方法或消融实验细节。

Limitations: 摘要未说明局限性。但可推断：(1) 该方法针对表格数据设计，特征对齐和上下文重建机制是否适用于高维图像/遥感特征尚不明确；(2) 未讨论计算开销和推理效率；(3) 未涉及遥感场景下的目标检测任务。

Ideas for my research: 尽管与遥感目标检测和轻量化方向弱相关，但有两个概念可借鉴：(1) "去相关特征对齐"（decorrelated feature alignment）思想，可用于遥感多传感器（光学/SAR/多光谱）特征融合时的域对齐；(2) "上下文重建"（in-context reconstruction）中利用正常样本分布衡量偏差的思路，可类比遥感场景中利用正常地物分布检测异常目标（如舰船、飞机）的异常检测范式。但这些仅为方法学层面的间接启发，不涉及轻量化。

Abstract: Tabular anomaly detection (TAD), which focuses on identifying abnormal samples that deviate from the majority in tabular data, has received growing attention. Recently, there has been an emerging trend towards unified TAD, which seeks to detect anomalies across different datasets using a single generalizable model. In unified TAD, aligning heterogeneous data remains challenging. While existing methods often rely on distance-based unified feature construction, they may obscure the semantics of the original features. Moreover, existing approaches typically formulate anomaly detection as a binary classification task, which may overlook diverse anomaly patterns from various datasets and be misled by unrepresentative synthetic anomalies. To address these challenges, we propose an in-COntext REconstruction approach for unified TAD (CORE for short). It introduces a decorrelated feature alignment module to directly align heterogeneous features into a unified representation space, which retains their semantic information. Meanwhile, CORE formulates unified TAD as an in-context reconstruction problem, eliminating the need for labeled or synthesized anomalies. Specifically, the in-context reconstruction module reconstructs each sample by leveraging contextual normal samples to capture dataset-specific distributions, such that reconstruction errors reflect its deviation from normality, facilitating unified TAD on arbitrary unseen datasets.

</details>


### [161] [Hidden APIs in Language Models: Discovering Reusable Causal Interfaces from Forked Futures](https://arxiv.org/abs/2607.27617)
*SiYuan Ma,Yiqin Luo,Zhangji,Canran Xiao,Albert Gao,Wei-Hsing Huang,Wei Wang,Qiwei Wu,Xinran Li,Jinfeng Wei,Qixin Zhang*

Main category: cs.AI

TL;DR: 本文研究大语言模型内部隐藏状态的可复用因果接口（Shared/Local/Mixture/Distributed interfaces），通过"forked futures"方法在无需人工标注潜在标签的情况下，用前序因果描述长度评估不同接口结构的可复用性，属于可解释性与机制可解释性研究。

Research relevance: 弱相关（1-2分）。论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能；也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解；更不涉及剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等轻量化或部署技术。该论文属于大语言模型（LLM）内部表征与因果可解释性研究，与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于LLM可解释性研究，不建议投入阅读时间。除非对"因果分析指导特征保留"这一间接思想有特殊兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型内部隐藏状态的因果可解释性分析（interpretability / mechanistic interpretability）。应用场景：通用NLP模型（Qwen2.5-1.5B、Llama-3-8B等）。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 研究对象为LLM内部隐藏状态（hidden states），提出四种接口结构：Shared、Local、Mixture、Distributed interfaces。方法核心是"forked futures"——在prefix state形成后采样未来操作，通过操作诱导的响应分布比较状态，从而得到经验因果商（empirical causal quotient），无需研究者指定潜在标签。评估框架采用prequential causal description length，并受future-signature fidelity和matched capacity约束。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（无剪枝、量化、蒸馏、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 评估模型：Qwen2.5-1.5B、Llama-3-8B，以及一个five-backbone sweep（5个骨干模型）。指标：held-out description length（nats）、mean future-signature distortion、Sharedness Gain、joint target-correctness/locality/copy-preservation/composite profile、API-aligned path mediation（0.749 vs 0.150）、blind four-class model-organism test（14/16架构恢复）。数据集具体名称摘要未说明。

Experiments: 主要实验：(1) 两个详细模型评估中Shared接口取得最低held-out description length，Qwen2.5-1.5B上增益0.216 nats，Llama-3-8B上增益0.294 nats，同时保持紧密聚类的mean future-signature distortion；(2) five-backbone sweep保持Sharedness Gain的正方向；(3) figure-aligned transplantation分析中Shared接口在joint target-correctness、locality、copy-preservation、composite profile上最强；(4) API-aligned paths介导0.749的目标效应，而matched null paths仅0.150；(5) blind four-class model-organism test中14/16架构被正确恢复，12个non-Shared organisms中观察到1个non-Shared到Shared的错误。

Limitations: 作者明确声明结论是条件性的，依赖于候选架构、干预方式和held-out futures的选择。此外：与遥感/卫星/目标检测/轻量化方向完全无关；不涉及任何部署指标；不涉及视觉或遥感数据；方法针对语言模型内部表征，难以直接迁移到遥感目标检测任务。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：论文提出的"因果接口/可复用表征"思想，在概念层面或许可以类比思考轻量化模型中"哪些中间特征对下游检测任务最关键、哪些可以裁剪"——即用因果分析指导剪枝或蒸馏中保留哪些特征通道。但这种类比非常间接，且论文方法针对语言模型，迁移到遥感CNN/Transformer检测器需要大量重新设计，实际可操作性低。

Abstract: Identical language-model answers can arise from hidden states that support different future computations, so current-answer probes do not establish a reusable internal interface. We introduce forked futures: future operations are sampled only after a prefix state has formed, and states are compared through the response distributions induced by those operations. This yields an empirical causal quotient over hidden states without requiring researcher-specified latent labels. Shared, Local, Mixture, and Distributed interfaces then compete under prequential causal description length subject to future-signature fidelity and matched capacity constraints. In the two detailed model evaluations, Shared has the lowest held-out description length, with gains of 0.216 nats on Qwen2.5-1.5B and 0.294 nats on Llama-3-8B, while maintaining tightly clustered mean future-signature distortion; a five-backbone sweep preserves the positive direction of Sharedness Gain. The figure-aligned transplantation analysis gives Shared the strongest joint target-correctness, locality, copy-preservation, and composite profile, and API-aligned paths mediate 0.749 of the target effect versus 0.150 for matched null paths. In the blind four-class model-organism test, 14/16 architectures are recovered, with one observed non-Shared to Shared error among 12 non-Shared organisms. These results support an economical reusable causal interface within the tested operation banks, while keeping the claim explicitly conditional on the candidate architectures, interventions, and held-out futures.

</details>


### [162] [SCOPE: Synthetic Conditional Objectives for Policy Evolution in Black-Box Combinatorial Optimization](https://arxiv.org/abs/2607.27630)
*Nguyen Viet Tuan Kiet,Nguyen Huu Duc,Le Cong Bang,Tran Cong Dao,Huynh Thi Thanh Binh*

Main category: cs.AI

TL;DR: 提出SCOPE框架，通过合成条件目标函数引导策略演化，用于黑盒组合优化问题，在有限评估预算下提升搜索性能。

Research relevance: 弱相关（2分）。论文属于泛机器学习/组合优化领域，与遥感、卫星、目标检测、轻量化均无直接关联。既未涉及遥感影像或地球观测数据，也未涉及任何目标检测任务或模型压缩/加速方法。仅从方法论层面看，其"在有限评估预算下优化未知目标函数"的思想与模型搜索/超参优化有一定抽象相似性，但缺乏与本研究方向的直接联系。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于通用组合优化方法研究，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：黑盒组合优化（Black-box Combinatorial Optimization）。应用场景：通用组合优化问题，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: SCOPE框架包含双层循环结构：外层循环自适应更新和选择合成目标函数（synthetic objectives），内层循环返回一组top-performing搜索策略（portfolio of policies）。核心思想是用条件于搜索历史的合成目标函数替代不可访问的真实目标函数，引导策略演化生成多样化候选解。

Lightweight method: 摘要未说明。论文不涉及模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计等任何轻量化方法。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息，也未涉及星上或边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集名称。仅在多个benchmark组合优化问题上进行了实验，评价指标为黑盒搜索性能（black-box search performance），在有限评估预算（limited evaluation budgets）下衡量。

Experiments: 摘要提到在多个benchmark问题上进行了广泛实验，结果表明SCOPE在有限评估预算下持续提升黑盒搜索性能，并能泛化到多种组合结构。但未给出具体对比方法、消融实验或量化结果。

Limitations: 摘要未说明。但可推断：1）该方法面向离散组合优化空间，与连续参数空间中的神经网络优化/压缩问题差异较大；2）未涉及任何视觉任务或深度学习模型；3）未提供计算开销、收敛速度等工程细节。

Ideas for my research: 借鉴意义有限，但存在抽象层面的启发：1）"在有限评估预算下优化未知目标函数"的思想可类比于在轨场景中计算资源受限、无法频繁评估模型性能的约束，SCOPE的"合成目标函数引导搜索"思路或可迁移到轻量化模型的结构搜索（NAS）中，用代理目标替代昂贵的真实评估；2）"portfolio of policies"降低单一代理偏好风险的思路，可类比于轻量化时同时保留多个候选压缩策略以降低过拟合风险。但整体而言，该论文与遥感在轨目标检测轻量化课题的直接可借鉴点很少。

Abstract: Black-box combinatorial optimization requires systematically identifying high-quality solutions under a limited evaluation budget, yet the unknown objective function provides little guidance for deciding where the search should explore next. We introduce SCOPE, a general framework for Synthetic Conditional Objectives for Policy Evolution in Black-Box Combinatorial Optimization. Rather than directly optimizing the inaccessible objective, SCOPE learns a set of synthetic objectives conditioned on the accumulated search history, where each objective is designed to expose a distinct and potentially useful preference over candidate solutions. These objectives are then used to evolve search policies that generate diverse candidates, whose true quality is subsequently assessed through black-box evaluations. The outer loop adaptively updates and selects synthetic objectives according to how effectively their induced policies discover promising regions. In contrast, the inner loop returns a portfolio of top-performing policies to reduce the risk of relying on a single surrogate preference. This formulation reframes objective design as a mechanism for guiding policy exploration, enabling the search process to exploit observed evidence while maintaining structured diversity across discrete solution spaces. Extensive experiments across multiple benchmark problems demonstrate that SCOPE consistently improves black-box search performance under limited evaluation budgets and generalizes well across diverse combinatorial structures.

</details>


### [163] [ReDiPPO: Reference-Guided Value Calibration and Discrepancy-Aware Token Reweighting for Mathematical Reasoning](https://arxiv.org/abs/2607.27631)
*Zhenrong Zhang,Fei Wu,Jun Du,Jianshu Zhang,Si Wei*

Main category: cs.AI

TL;DR: 提出ReDiPPO，一种用于大语言模型数学推理的参考引导与差异感知PPO强化学习框架，通过参考答案作为特权信号改进token级价值估计与优势加权。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型（LLM）的数学推理强化学习优化，属于自然语言处理/强化学习领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等任何遥感相关任务，也不涉及目标检测或模型轻量化/部署。

Reading priority: 1分。论文属于LLM强化学习领域，与遥感卫星在轨目标检测与模型轻量化方向基本无关，阅读优先级极低。仅当对"差异感知加权"这一通用思想在蒸馏/难例挖掘中的迁移感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型数学推理（mathematical reasoning）的强化学习策略优化。应用场景：LLM推理能力增强。传感器类型：不适用（非遥感）。目标类别：不适用。

Model architecture: ReDiPPO框架包含：1) 参考引导critic（reference-guided critic），利用参考答案作为训练时特权信号（privileged signals）提供更准确的价值估计；2) 标准critic（standard critic），与参考引导critic共同量化token级参考-标准差异（reference-standard discrepancy）；3) 差异用于重加权token级优势（advantage）进行PPO优化。整体基于Proximal Policy Optimization (PPO)框架。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法，也不涉及实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等部署相关内容。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标，且其研究对象为大语言模型，与星上/边缘部署场景无关。

Datasets and metrics: 在多个数学推理基准（mathematical reasoning benchmarks）上进行实验，具体数据集名称摘要未说明。评价指标为最终推理性能（final reasoning performance），对比基线包括PPO、DAPO、GSPO等策略优化方法。

Experiments: 实验表明ReDiPPO提升了价值估计精度（value-estimation accuracy），并在最终推理性能上一致优于PPO、DAPO、GSPO等强策略优化基线。具体数值结果摘要未说明。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向完全无关；2) 摘要未说明模型规模、计算开销、推理延迟等关键信息；3) 仅针对数学推理任务，领域泛化性未验证；4) 参考答案作为特权信号在推理阶段不可用，存在训练-推理不一致问题（摘要未讨论）。

Ideas for my research: 尽管论文与遥感目标检测轻量化无直接关系，但存在以下间接可借鉴思路：1) "参考引导+差异感知"的思想可类比于知识蒸馏中教师-学生差异的利用——在蒸馏时用教师输出作为"特权信号"来引导学生对困难样本/困难token进行加权学习，这与ReDiPPO的discrepancy-aware reweighting思路相通；2) 对困难样本（如遥感小目标、密集舰船场景）进行差异感知的损失加权，可借鉴其"差异作为困难度指示器"的思想，用于设计面向小目标检测的难例挖掘策略。但整体借鉴价值有限。

Abstract: Reinforcement learning has emerged as an effective paradigm for enhancing the mathematical reasoning capabilities of large language models. Among existing policy optimization methods, Proximal Policy Optimization (PPO) remains particularly appealing because its learned critic can, in principle, provide token-level credit assignment. However, in mathematical reasoning tasks characterized by long reasoning horizons and sparse outcome rewards, reliable token-level credit assignment remains challenging. The standard critic often fails to accurately evaluate intermediate reasoning states, resulting in noisy advantage estimates and suboptimal policy updates. In this paper, we propose ReDiPPO, a Reference-guided and Discrepancy-aware PPO framework for mathematical reasoning. ReDiPPO introduces a reference-guided critic that uses reference answers as training-time privileged signals to provide more accurate value estimation. Meanwhile, it retains a standard critic and quantifies the token-level reference-standard discrepancy between the standard value estimate and the reference-guided value estimate. This discrepancy serves as an indicator of difficult reasoning states and is used to reweight the corresponding token-level advantages during PPO optimization. Extensive experiments on diverse mathematical reasoning benchmarks demonstrate that ReDiPPO improves value-estimation accuracy and consistently outperforms strong policy optimization baselines, including PPO, DAPO, and GSPO, in final reasoning performance. Our code is available on https://github.com/cii030/ReDiPPO.

</details>


### [164] [HealthCAT: An Interpretable Encoder-only Transformer Framework for Health Indicator Prediction and Temporal Interpretation of Wearable Sensor Data](https://arxiv.org/abs/2607.27635)
*Xiaotong Yu,Joshua Y. Kim,HaeJin Lee,Kalina Yacef*

Main category: cs.AI

TL;DR: HealthCAT是一个基于Encoder-only Transformer与Attentive Class Activation Token的可解释框架，用于可穿戴传感器多变量时间序列数据的健康行为模式分析与预测。

Research relevance: 与本研究方向基本无关（1分）。该论文聚焦于可穿戴传感器（wearable sensors）的健康监测时间序列数据，属于健康信息学/生物医学信号处理领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及object detection、目标检测或模型轻量化/部署。唯一可能的间接联系是Transformer架构在时间序列建模上的通用性，但这与遥感在轨目标检测的关联非常微弱。

Reading priority: 1分。该论文属于健康信息学领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对Transformer可解释性机制（如AttentiveCAT）有跨领域兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：可穿戴传感器多变量时间序列的健康指标预测与可解释性分析。应用场景：健康监测、行为模式分析、干预设计。传感器类型：可穿戴传感器（wearable sensors），非遥感传感器。目标类别：健康行为模式（如时间相关的活动周期），非遥感目标。

Model architecture: Encoder-only Transformer架构，集成Attentive Class Activation Token（AttentiveCAT）模块，用于生成类别特定的时间步级解释（class-specific, time-step-level interpretations）。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（pruning, quantization, knowledge distillation, NAS等均未提及）。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该论文面向健康监测场景，与星上部署（onboard deployment）无关。

Datasets and metrics: 使用两个真实世界可穿戴传感器数据集，共306名参与者。评价指标：F1-score（最高提升17%）和accuracy（最高提升12%），显著性检验p<0.05。另进行了masking实验验证时间步的预测信息量。

Experiments: HealthCAT在两个可穿戴传感器数据集上优于深度学习baseline，F1-score最高提升17%，accuracy最高提升12%（p<0.05）。Masking实验表明其识别的时间步比随机选择具有显著更高的预测价值（p<0.05）。

Limitations: 摘要未说明具体局限性。但明显局限包括：1）仅针对健康监测时间序列，不涉及遥感图像/目标检测；2）未提供模型复杂度、推理效率等部署信息；3）未涉及轻量化技术；4）样本量较小（306人），泛化性待验证。

Ideas for my research: 尽管该论文与遥感在轨目标检测方向基本无关，但有两个概念层面的可借鉴点：1）Attentive Class Activation Token（AttentiveCAT）的可解释性思路——类似CAM（Class Activation Mapping）机制，可类比思考在遥感目标检测中如何利用注意力token生成类别相关的空间显著性图，辅助小目标定位；2）时间步级解释（time-step-level interpretation）的思想，在遥感时序数据（如多时相变化检测）中可能有类比价值。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值很低。

Abstract: Wearable sensors continuously capture fine-grained multivariate time-series data, providing opportunities to model behavioural patterns associated with health outcomes. However, existing deep learning methods prioritise predictive accuracy over interpretability, limiting their application in health research. In this study, we present HealthCAT, a flexible framework that integrates an Encoder-only Transformer with an Attentive Class Activation Token (AttentiveCAT) to generate class-specific, time-step-level interpretations. These interpretations can be mapped back onto behavioural cycles that are relevant to the domain (e.g., time-of-day), supporting individual-level analysis of wearable sensor data. We evaluated HealthCAT using two real-world wearable sensor datasets (306 participants in total). HealthCAT outperformed deep learning baselines by up to 17\% in F1-score and 12\% in accuracy on both datasets ($p<0.05$). In masking experiments, the time steps identified by HealthCAT carried significantly more predictive value than random selection across all masking conditions ($p<0.05$), indicating that the identified time steps are predictively informative. By coupling predictive performance with validated time-step-level interpretability, HealthCAT moves wearable sensor analysis beyond aggregated metrics towards temporal patterns that support health monitoring, behavioural pattern analysis, and intervention design in health research. The significance of this work is that it enables accurate prediction of health indicators from wearable sensor data while providing insights into when and how physical activity patterns occur, rather than relying solely on aggregated summary measures.

</details>


### [165] [Evaluating and Pricing Advertisements in AI-Generated Responses](https://arxiv.org/abs/2607.27686)
*John L. Turner-Smith,Zimeng Huang,Yuhan Fu,Yihang Zhang,Tonghan Wang*

Main category: cs.AI

TL;DR: 本文提出一种基于心理学agent模拟框架构建点击意图监督信号，并将其蒸馏为参数高效评估器，用于LLM驱动的广告生成响应中的点击意图预测与定价机制设计。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于LLM驱动的搜索引擎广告评估与定价机制，属于自然语言处理、广告推荐与机制设计领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感场景，也不涉及object detection、小目标检测、旋转框检测等目标检测任务，更不涉及pruning、quantization、knowledge distillation（此处distillation指知识蒸馏到评估器，但对象是广告意图评估而非视觉模型）、NAS、实时推理、边缘部署、星上部署等轻量化部署技术。

Reading priority: 1分。论文主题为LLM广告评估与定价机制，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM驱动广告响应中的点击意图预测与广告质量评估、定价机制设计。应用场景：搜索引擎广告系统。传感器类型：不适用（非遥感传感器）。目标类别：广告、点击意图、广告质量维度。与遥感目标检测完全无关。

Model architecture: 摘要未说明具体网络结构。核心组件包括：1) 心理学grounded的agent模拟仿真框架，用于构造点击意图的监督信号；2) 参数高效评估器（parameter-efficient evaluator），将agent模拟的监督信号蒸馏为可微的点击意图与广告质量估计；3) 基于评估器估计构建的定价层（pricing layer），推导truthful bidding最优的支付规则。

Lightweight method: 摘要提及"parameter-efficient evaluator"和"distil"（蒸馏），即通过知识蒸馏将agent模拟框架产生的监督信号压缩到参数高效评估器中。但这属于NLP/广告评估场景下的模型压缩，与遥感目标检测模型的轻量化（pruning、quantization、NAS、FPGA/NPU部署等）完全不同，不具备直接借鉴意义。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。其"parameter-efficient"仅指评估器参数量小，并非面向硬件部署的轻量化设计。

Datasets and metrics: 数据集：摘要未说明具体训练数据集名称；验证涉及103个虚构产品（fictional products）的泛化测试，以及5名标注者（five annotators）的人工偏好评估。评价指标：relevance sensitivity（相关敏感性，79% vs 60-67%）、graded content degradation跟踪、泛化错误率（103个虚构产品上无错误）、人工偏好一致率（86% pairwise judgements）。

Experiments: 主要实验：1) 评估器在relevance sensitivity上超越frontier zero-shot judges（79% vs 60-67%）；2) 能跟踪分级内容退化（graded content degradation）；3) 在103个虚构产品上无错误泛化；4) 与5名标注者的人工偏好86%一致，且一致性随评估器置信度上升；5) 在best-of-k分配上演示定价机制，并扩展到非单调分配。

Limitations: 摘要未说明局限性。但明显局限包括：1) 与遥感/目标检测/轻量化部署完全无关；2) 评估器依赖agent模拟框架生成的监督信号，其心理学grounding的有效性需进一步验证；3) 定价机制的理论推导与实际广告系统部署之间的差距未讨论；4) 未涉及任何视觉/遥感数据处理。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的可借鉴点非常有限。唯一可能的概念性启发：1) "distill into a parameter-efficient evaluator"的思路——即用大模型/复杂框架生成监督信号，再蒸馏到轻量模型，这与遥感检测中的knowledge distillation思想在方法论层面有共通之处，但本文蒸馏对象是广告意图评估而非视觉检测模型，无法直接迁移；2) "smooth, differentiable estimates"的平滑可微估计思想，在遥感检测的损失函数设计上或有概念参考。总体而言，本文对遥感在轨目标检测轻量化课题的借鉴价值极低。

Abstract: As search increasingly shifts toward LLM-driven answer engines, advertising is becoming embedded within the generated response itself and should therefore be evaluated for both user utility and commercial value. The key challenge is click-through intent: behavioural logs are unavailable, human annotation resists calibration, and frontier LLM judges conflate intent with linguistic fluency. These gaps compound, as principled pricing presupposes a continuous intent signal, while generating such a signal presupposes supervision that is currently unavailable. We construct the missing supervision through a psychologically grounded agent simulation framework, and distil it into a parameter-efficient evaluator that predicts click-through intent, together with the three companion dimensions of ad quality, as smooth, differentiable estimates. Validated through sign-certain behavioural perturbations, the evaluator surpasses frontier zero-shot judges on relevance sensitivity (79% versus 60-67%), tracks graded content degradation, generalises without error to 103 fictional products, and agrees with human preference in 86% of pairwise judgements across five annotators, with agreement rising in the evaluator's confidence. Upon its estimates we build the pricing layer directly, deriving the unique payment rule under which truthful bidding is optimal, demonstrating it on a best-of-k allocation, and extending the mechanism to non-monotone allocations. The same differentiable signal stands ready as a training objective for ad generation.

</details>


### [166] [Rehearse: Stepping Back from the Confidence Cliff in Self-Improving Autoresearch](https://arxiv.org/abs/2607.27687)
*Jiazhen Ji,Shouhong Ding*

Main category: cs.AI

TL;DR: 本文研究自动化机器学习研究（autoresearch）中，LLM代理在执行训练前对修改方案可行性的预判可靠性，提出"confidence cliff"现象及Rehearse轻量技能来提升后期选择性准确率。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于自动化机器学习研究（autoresearch）中LLM代理对代码修改方案的预执行判断可靠性，属于机器学习系统/自动化研究领域，不涉及遥感、卫星、Earth observation、目标检测或模型压缩/部署等任何相关主题。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于自动化机器学习研究（autoresearch）领域，不涉及遥感场景、目标检测或模型压缩/部署。除非对自动化搜索流程设计感兴趣，否则无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：自动化机器学习研究（autoresearch）中LLM代理的修改方案预判与选择。应用场景：AutoSOTA自动研究循环、nanochat、image classification、time-series forecasting。传感器类型：不适用。目标类别：不适用。

Model architecture: Rehearse技能：提出多个想法→执行前比较→运行最有前景的方案→用聚焦的过往尝试与结果记忆进行判断。核心是"focused outcome memory"机制，用于提升LLM judge在autoresearch循环后期的选择性准确率。

Lightweight method: 摘要中"lightweight skill"指Rehearse作为autoresearch循环中的轻量级技能（即轻量化的流程改进），并非模型压缩/剪枝/量化/蒸馏等传统意义上的模型轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、FLOPs等。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集/基准：AutoSOTA公开日志（Li et al., 2026; Tsinghua FIB Lab, 2026）、296个same-baseline修改对（来自39个paper-derived AutoSOTA任务）、366-pair完整基准、4000次预算训练运行（覆盖三个循环：nanochat、image classification、time-series forecasting）。评价指标：选择性准确率（selective accuracy，基于strict-consensus verdict）、帮助性修改比例（fraction of helpful modifications）。

Experiments: 主要结果：1) 帮助性修改比例从前两次迭代的70%降至第6+次迭代的43%；2) LLM judge在296对中达到79.5%准确率（strict consensus子集）；3) 完整366对基准上，选择性准确率从82.8%降至56.9%（confidence cliff现象）；4) Rehearse的focused outcome memory将后期选择性准确率提升至83.5%；5) 在4000次预算训练运行中，Rehearse在三个循环上均改善了endpoint指标。

Limitations: 摘要未说明具体局限性。但可推断：1) 与遥感/卫星/目标检测/模型轻量化完全无关；2) 实验任务（nanochat、image classification、time-series forecasting）均为通用机器学习任务，无任何遥感场景；3) 未涉及任何部署相关指标。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但"confidence cliff"和"focused outcome memory"的思想对自动化超参搜索/架构搜索流程设计有一定启发：在NAS或轻量化搜索过程中，前期判断可靠、后期判断衰减的现象值得注意，可考虑引入历史结果记忆来提升搜索后期决策质量。但整体借鉴价值有限。

Abstract: Autoresearch improves machine-learning code by proposing changes, running full training jobs, and keeping changes that improve the metric. The efficiency of this loop depends not only on generating ideas, but also on the agent's ability to decide, before spending a training run, whether a proposed modification is likely to work. We study how the reliability of this pre-execution judgment changes over the course of an autoresearch trajectory. In public AutoSOTA logs (Li et al., 2026; Tsinghua FIB Lab, 2026), the fraction of helpful modifications falls from 70% in the first two iterations to 43% by iteration 6+. On 296 same-baseline modification pairs from 39 paper-derived AutoSOTA tasks, each containing one modification that improved the metric and one that did not, with measured outcomes hidden, an LLM judge given candidate rationales but no prior-attempt history reaches 79.5% accuracy on the pairs where strict consensus returns a verdict. On the full 366-pair benchmark, however, this ability weakens substantially late in the loop. As successful changes accumulate, selective accuracy - accuracy conditioned on a strict-consensus verdict - falls from 82.8% to 56.9%, while the judge remains willing to decide. We call this operational pattern the confidence cliff. Rehearse implements the loop change as a lightweight skill for autoresearch loops: propose several ideas, compare them before execution, run the most promising, and judge with a focused memory of similar past attempts and outcomes. This focused outcome memory raises late selective accuracy to 83.5%. Across 4,000 budgeted training runs over three loops, Rehearse improves the endpoint under the same training-run budget on nanochat, image classification, and time-series forecasting.

</details>


### [167] [Guiding Large Language Models with Genetic Programming-Evolved Heuristic Knowledge for Dynamic Multi-Mode Project Scheduling](https://arxiv.org/abs/2607.27698)
*Yuan Tian,Yi Mei,Mengjie Zhang*

Main category: cs.AI

TL;DR: 本文研究动态多模式项目调度中，利用遗传规划（GP）超启发式规则提取的知识来引导大语言模型（LLM）在线决策，通过特征选择、特征提示、规则参考和规则遵循四种机制提升调度性能与决策稳定性。

Research relevance: 与本研究方向基本无关（1分）。论文主题是项目调度（project scheduling）与遗传规划（GP）超启发式、大语言模型（LLM）决策，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能、目标检测或模型轻量化中的任何一项。虽然文中提到"knowledge distillation"式的知识迁移思想（从GP规则向LLM迁移），但这是调度决策领域的知识注入，并非深度学习模型压缩/蒸馏，与遥感目标检测轻量化无直接关联。

Reading priority: 1分。论文主题为项目调度与LLM决策，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对"知识注入/规则引导"这一方法论思想感兴趣时，可作为泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：动态多模式项目调度（dynamic multi-mode project scheduling）中的在线决策。应用场景：项目管理/调度优化。传感器类型：不适用。目标类别：不适用（调度活动/任务）。

Model architecture: 核心框架为GP超启发式（genetic programming hyper-heuristics）与大语言模型（LLM）的混合决策系统。知识迁移方向为GP→LLM，通过四种机制注入知识：Feature Selection（特征选择）、Feature Hint（特征提示）、Rule Reference（规则参考）、Rule Follow（规则遵循）。

Lightweight method: 摘要未说明。本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。文中"knowledge transfer"是从GP规则向LLM的知识注入，属于决策知识引导，而非深度学习模型轻量化。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时推理等部署指标。文中提到"token consumption"（token消耗）作为LLM决策效率的评估指标，但这是LLM推理成本，与遥感在轨部署无关。

Datasets and metrics: 评估指标包括：调度性能（scheduling performance）、token消耗（token consumption）、决策稳定性（decision stability）、生成rationales中表达的特征关注（feature focus）。数据集和具体实验设置摘要未说明。

Experiments: 主要发现：GP引导总体上优于无引导的LLM，但知识表示方式影响显著。简化决策上下文或提供显式决策逻辑比突出重要特征更有效。Feature Selection在token效率上最优，Rule Follow在性能上更强但token成本更高。引导还改善了决策稳定性并改变了rationales中表达的特征。

Limitations: 摘要未说明具体局限性。从研究角度看：1）本文完全面向调度领域，与遥感目标检测无任何交集；2）LLM在线决策的token成本高、推理延迟大，与在轨部署的实时性、资源受限约束背道而驰；3）未涉及任何视觉/图像处理内容。

Ideas for my research: 尽管整体弱相关，但有一个方法论层面的可借鉴点：文中"从高质量规则中提取知识并注入决策模型"的思想，与知识蒸馏（knowledge distillation）中"教师模型知识迁移到学生模型"的理念有相似之处。在遥感在轨目标检测轻量化中，可借鉴这种"规则/知识显式注入"的思路——例如将领域先验（如舰船/飞机的几何先验、旋转框角度约束）以特征提示或规则约束的方式注入轻量检测网络，帮助小模型在低FLOPs下保持精度。但需注意，这仅是思想层面的类比，具体技术路线完全不同。

Abstract: In dynamic multi-mode project scheduling, activities have alternative execution modes and uncertain durations, while precedence relations and limited resources constrain their execution. Heuristic priority rules support fast online decisions, but their design requires substantial domain expertise. Genetic programming (GP) hyper-heuristics can automatically evolve such rules. Large language models (LLMs), meanwhile, provide a flexible interface for interpreting scheduling information and explaining decisions. However, zero-shot LLM decisions may lack domain knowledge, consume many tokens, and vary across repeated queries. GP-evolved rules therefore provide a potential source of scheduling knowledge for guiding LLM decisions. Unlike existing LLM--GP hybrids that use LLMs to support heuristic evolution, we transfer knowledge in the reverse direction, using knowledge extracted from high-quality GP rules to guide an online LLM decision maker. We extract knowledge from high-quality GP rules and inject it through Feature Selection, Feature Hint, Rule Reference, and Rule Follow. These mechanisms are evaluated in terms of scheduling performance, token consumption, decision stability, and the feature focus expressed in generated rationales. GP-derived guidance generally improves the unguided LLM, but its representation matters. Simplifying the decision context or supplying explicit decision logic is more effective than highlighting important features. Feature Selection offers the best token efficiency, whereas Rule Follow achieves strong performance at greater token cost. Guidance also improves decision stability and changes the features expressed in generated rationales.

</details>


### [168] [SpatialCLI: Learning to Reason With Spatial Tools, Then Without Them](https://arxiv.org/abs/2607.27703)
*Yang Zhou,Zixuan Huang,Sunzhu Li,Zhuo Yang,Chen Zhang,Shunian Chen,Caijun Yan,Jianyao Xu,Shunyu Liu,Weijie Fu,Peiliang Li,Xiaozhi Chen,Yuxiang Cai*

Main category: cs.AI

TL;DR: 提出 SpatialCLI 框架，通过三阶段（Call-Learn-Internalize）让视觉语言模型学会调用空间工具（如定位、分割、深度、姿态等专家视觉模型）并逐步内化这些感知能力，从而弥合通用 VLM 与专用视觉模型之间的能力鸿沟。

Research relevance: 弱相关（2分）。该论文属于泛视觉-语言模型（VLM）与具身智能（embodied agents）方向，核心是让 VLM 学会调用和内化空间感知工具，不涉及遥感/卫星场景，也不涉及目标检测的轻量化或星上部署。虽然涉及 localization、segmentation、depth、pose 等感知任务，但属于通用视觉领域，与遥感在轨目标检测及模型轻量化方向基本无关。

Reading priority: 1分。该论文属于 VLM 与具身智能方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，仅"Internalize"的知识内化思想在蒸馏策略上有间接参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视觉语言模型（VLM）的空间推理与工具调用（tool use）；应用场景：具身智能（embodied agents）；传感器类型：通用视觉输入（非遥感/卫星传感器）；目标类别：定位（localization）、分割（segmentation）、深度（depth）、姿态（pose）等组合感知任务。

Model architecture: 基于 Qwen3-VL-8B-Instruct 等 VLM 作为基础模型，通过三阶段框架 SpatialCLI 实现：(1) Call 阶段将专家视觉模型暴露为空间工具（spatial tools）以增强 VLM 感知；(2) Learn 阶段使用 Cold-Start SFT 和 agentic RL 改进工具使用；(3) Internalize 阶段将成功的工具使用轨迹语言化，使 VLM 内化专家感知能力。摘要未说明具体工具模型的架构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS 或轻量网络设计等模型压缩/加速方法。虽然"Internalize"阶段有类似知识迁移的思想，但并非传统意义上的模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标。基础模型为 Qwen3-VL-8B-Instruct（8B 参数规模），在星上/边缘部署场景下体量过大，不具备在轨部署可行性。

Datasets and metrics: 提出 SpatialCLI-Bench，包含 516 个样本的组合感知基准，覆盖 localization、segmentation、depth、pose 四类任务。在 MindCube 数据集上评估，指标为任务成功率（accuracy）。Qwen3-VL-8B-Instruct 从 29.3% 提升至 84.6%（带工具），超过 GPT-5.6 Sol（72.1%）；内化后无工具时保留 73.8% 性能。

Experiments: 在 MindCube 上验证 SpatialCLI 效果：Qwen3-VL-8B-Instruct 使用工具后从 29.3% 提升到 84.6%，超越 GPT-5.6 Sol with tools（72.1%）；经过 Internalize 阶段后，无工具情况下仍保持 73.8% 的性能，说明感知能力被部分内化。摘要未提供消融实验和更多对比细节。

Limitations: 1. 与遥感/卫星/在轨部署完全无关，属于具身智能与 VLM 方向；2. 基础模型为 8B 参数级 VLM，计算开销大，不具备边缘/星上部署条件；3. 摘要未说明工具调用的额外推理开销和延迟；4. 基准 SpatialCLI-Bench 为通用场景，非遥感数据；5. 未涉及任何轻量化技术。

Ideas for my research: 1. "Internalize"阶段的知识内化思想可类比知识蒸馏（knowledge distillation）——将大模型/专家模型的感知能力迁移到轻量模型，可借鉴其"先工具增强、后内化"的训练范式用于遥感小目标检测的蒸馏；2. 工具调用（tool use）思想可启发遥感在轨场景中"检测模型+后处理模块"的协同设计；3. 但整体而言，该论文对遥感在轨目标检测轻量化课题的直接借鉴价值有限，仅可作为知识迁移/蒸馏训练策略的间接参考。

Abstract: Vision-language models (VLMs) are increasingly used in embodied agents to interpret visual inputs, reason about spatial relationships, and make task-level decisions based on that reasoning. However, a fundamental capability mismatch remains: general VLMs can reason about the overall task but often miss the visual details that determine success, while specialist vision models can capture those details but cannot translate them into task-level decisions. In this work, we propose SpatialCLI, a framework that teaches VLMs to reason with spatial tools and progressively internalize the specialist perceptual capabilities they provide. SpatialCLI proceeds in three stages: (1) Call exposes specialist vision models as spatial tools to augment the VLM's perception; (2) Learn uses Cold-Start SFT and agentic RL to improve tool use; and (3) Internalize verbalizes successful tool-use trajectories to internalize specialist perceptual capabilities. We further introduce SpatialCLI-Bench, a 516-example benchmark for compositional perception across localization, segmentation, depth, and pose. On MindCube, SpatialCLI raises Qwen3-VL-8B-Instruct from 29.3% to 84.6% with tools, surpassing GPT-5.6 Sol with tools (72.1%), while retaining 73.8% without tools after internalization.

</details>


### [169] [Albilich: Steerable Proof-State Orchestration for LLM-Based Mathematical Research with CAS Integration](https://arxiv.org/abs/2607.27705)
*Ting Gong,Michael Ruofan Zeng,Yong Yang*

Main category: cs.AI

TL;DR: 提出Albilich，一个用于数学自动研究的开源agentic框架，结合长程推理、计算机代数系统(CAS)、文献检索和SQLite持久化上下文管理，在RealMath基准和Kourovka Notebook群论开放问题上取得突破。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于数学自动研究（autoresearch in mathematics），涉及大语言模型、计算机代数系统、群论问题求解，不涉及任何遥感、卫星、地球观测、目标检测或模型压缩内容。相关性评分为1分。

Reading priority: 1分。论文主题为数学自动研究，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：数学自动研究（mathematical autoresearch），包括定理证明、反例构造、开放问题求解。应用场景：AI辅助数学研究。传感器类型：不适用（非遥感传感器）。目标类别：数学问题（RealMath基准、Kourovka Notebook群论问题）。

Model architecture: Albilich是一个agentic harness框架，核心组件包括：长程推理（long-horizon reasoning）、计算机代数系统（CAS, computer algebra system）、文献检索（literature retrieval）、基于SQLite的持久化上下文管理（persistent SQLite-based context management），以及advisor agent（顾问智能体）用于指导证明路径合成。

Lightweight method: 摘要未说明轻量化方法。论文中提到的"32.0% token reduction"是指启用CAS后token消耗的减少，属于推理效率优化而非模型压缩/轻量化技术。不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或边缘部署。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存或能耗等部署相关信息。其"token reduction"指标属于LLM推理成本优化，与在轨部署无关。

Datasets and metrics: 数据集/基准：RealMath benchmark（Zhang et al. 2025）和Kourovka Notebook（Khukhro and Mazurov 2026）中的群论开放问题。评价指标：问题解决率（10/10 on RealMath with CAS，9/10 without CAS）、token reduction（32.0%）、verifier-rejection rate（验证器拒绝率）。

Experiments: 主要实验结果：1) RealMath上启用CAS解决10/10问题，无CAS解决9/10；2) Kourovka Notebook上构造了Problem 21.142的反例，并证明了Problem 20.2的加强版本；3) Problem 17.91上的消融实验显示启用CAS可减少32.0%的token消耗；4) Problem 21.142上的消融实验表明缺少advisor agent时验证器拒绝率更高且无法合成证明路径。

Limitations: 摘要未说明局限性。但可推断：1) 与遥感/目标检测/轻量化完全无关；2) 未讨论计算资源需求、推理延迟等部署指标；3) 数学自动研究场景与视觉感知任务差异极大，方法论难以直接迁移。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但存在极有限的间接借鉴点：1) "advisor agent"辅助决策的思想可类比于轻量化检测模型中的动态路由或条件计算（conditional computation），即根据输入复杂度动态决定是否启用高开销模块；2) CAS带来的token reduction表明"外部工具辅助降低模型推理开销"的思路，可类比于在轨场景中用传统图像处理算法辅助深度模型以减少计算量。但这些借鉴非常间接，整体参考价值有限。

Abstract: Large language models can contribute useful ideas to mathematical research, yet long-horizon proof attempts remain difficult to coordinate, evaluate, and reproduce. We present Albilich, an open-source agentic harness for autoresearch in mathematics that combines long-horizon reasoning, computer algebra systems (CAS), literature retrieval, and persistent SQLite-based context management.
  We evaluate Albilich on the RealMath benchmark (Zhang et al. 2025) and on open problems in group theory from the Kourovka Notebook (Khukhro and Mazurov 2026). It solved 10/10 problems on RealMath with CAS and 9/10 with no CAS. On the Kourovka problems, Albilich produced a counterexample to Problem 21.142 and a proof of a strengthening of Problem20.2. Anablation on Problem 17.91 demonstrates 32.0% token reduction when CAS is enabled. An ablation on Problem 21.142 demonstrates higher verifier-rejection rate and failure to synthesize proof routes in the absence of the advisor agent. These results support Albilich as a human-steerable, CAS-boosted environment for scalable AI-assisted mathematical research.

</details>


### [170] [MECA: A Mechanism-Centered Agent for Constructing Well-Specified and Valuable Mathematical Conjectures](https://arxiv.org/abs/2607.27709)
*Wentao Long,Yunfei Zhang,Chenyi Li,Zaiwen Wen*

Main category: cs.AI

TL;DR: 提出MECA（机制中心猜想智能体），一个多智能体框架，通过联合开发候选数学猜想及其支撑机制，将宽泛的研究方向转化为具体且有数学支撑的猜想。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于AI辅助数学发现（automated mathematical discovery），涉及conjecture generation、automated provers、mathematical mechanism等，完全不涉及遥感、卫星、目标检测或模型压缩。属于泛AI/数学推理方向，与本课题弱相关。

Reading priority: 1分。论文主题为AI辅助数学猜想发现，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及任何视觉、遥感或部署内容，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI辅助数学猜想自动构建（mathematical conjecture generation）。应用场景：数学发现与自动推理。不涉及任何传感器类型或目标类别。摘要未说明。

Model architecture: MECA为多智能体框架（multi-agent framework），包含Explorer agents（探索者智能体，提出机制、测试应用、修订猜想）和Critic agents（批评者智能体，评估数学有效性和研究价值）。通过agent间反馈迭代修改假设、范围和结论。摘要未说明底层模型架构（如LLM类型、参数规模等）。

Lightweight method: 不涉及任何模型压缩或加速方法。摘要未提及pruning、quantization、knowledge distillation、NAS、轻量网络等任何轻量化技术。

Onboard deployability: 不涉及星上/边缘部署。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 评估设置包括两个场景：(1) 从目标条件化但文章盲化的源材料重建预选目标论文结论，与generate-and-revise基线对比；(2) 从文献种子和现有开放问题构建100个半开放问题，通过automated provers进行独立的证明和反驳尝试。摘要未说明具体数据集名称和量化指标数值。

Experiments: 实验在两个互补设置下进行：一是与generate-and-revise基线对比重建目标论文结论；二是构建100个半开放问题并用automated provers评估。结果表明机制中心细化能产生规范且有研究价值的猜想，且对当前automated provers仍具挑战性。摘要未提供具体对比数值。

Limitations: 摘要未说明局限性。但可推断：该工作面向纯数学推理，与遥感视觉任务无直接关联；未涉及任何视觉模型、检测任务或部署约束；多智能体框架的计算开销和推理成本未提及。

Ideas for my research: 借鉴意义有限。可能的间接启发：(1) 多智能体协作与迭代反馈机制可类比于模型设计中"生成-评估-修订"的自动化搜索思路，或可用于NAS搜索策略设计；(2) 将复杂问题分解为"机制/中间步骤"的思想，可类比于将目标检测任务分解为子任务（如定位与分类解耦）进行轻量化设计。但整体上与本课题关联度很低。

Abstract: Automatically constructing well-specified and valuable mathematical conjectures remains a central challenge in AI-assisted mathematical discovery. Many existing open problems and conjectures are often too broad, underspecified, or difficult to connect to plausible proof or refutation strategies. We view a mathematical mechanism as a structure or reasoning principle that connects the assumptions of a candidate problem to its target conclusion, such as an inequality, invariant, decomposition, or reduction to an intermediate claim. We present MECA (MEchanism-centered Conjecture Agent), a multi-agent framework that constructs conjectures by jointly developing candidate statements and their supporting mechanisms. Explorer agents propose mechanisms, test how they apply, and revise the candidate conjecture accordingly, while critic agents assess their mathematical validity and research value. Their feedback guides changes to the assumptions, scope, and conclusion. Through this process, MECA transforms broad research directions into precise conjectures with substantive mathematical support while retaining a clearly identified unresolved core. We evaluate MECA in two complementary settings. First, we compare it with a generate-and-revise baseline on reconstructing preselected target-paper conclusions from target-conditioned but article-blind source materials. Second, we construct 100 semi-open problems from literature-derived seeds and existing open problems and evaluate them through independent proof and refutation attempts by automated provers. Our results indicate that mechanism-centered refinement produces well-specified and research-worthy conjectures that remain challenging for current automated provers.

</details>


### [171] [New Synchronous Computation Dynamics for Hopfield Networks](https://arxiv.org/abs/2607.27720)
*Francisco Requena-Domínguez,Rafaela Benítez-Rochel,Ezequiel López-Rubio*

Main category: cs.AI

TL;DR: 提出一种基于离散微分滤波器（DDF）的Hopfield网络同步动力学（SD-DDF），通过每步同时更新多个神经元并最大化能量下降，以缩短网络处理时间。

Research relevance: 弱相关（2分）。论文属于泛机器学习/神经网络动力学理论方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。Hopfield网络的同步动力学加速属于泛神经网络计算优化，对遥感在轨目标检测的轻量化没有直接借鉴意义。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于Hopfield网络动力学理论优化研究，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Hopfield网络动力学加速/同步更新策略。应用场景：泛神经网络计算优化，非遥感场景。传感器类型：摘要未说明。目标类别：摘要未说明。

Model architecture: 基于Hopfield网络，提出Discrete Differential Filter (DDF)工具，并构建Synchronous Dynamics based upon Discrete Differential Filter (SD-DDF)同步动力学。核心思想是每时间步同时更新一个或多个神经元，在保证收敛的前提下最大化能量下降。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等经典轻量化方法。本文的"加速"体现在通过同步更新策略减少Hopfield网络的处理时间步数，属于计算效率优化，但与深度学习模型压缩/加速方法不同。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及"减少处理时间"（reducing processing time）和"最短总处理时间"（shortest total processing time），但未给出具体数值或硬件部署信息。

Datasets and metrics: 摘要提及"四个计算实验"（four computational experiments）来评估处理时间的加速效果，但未说明具体数据集名称和评价指标。

Experiments: 摘要仅说明进行了四个计算实验以实证评估处理时间加速，并给出了理论证明（theoretical justification），但未提供具体实验结果数值、对比方法或消融实验细节。

Limitations: 摘要未说明：1) 与遥感/卫星/目标检测场景无关；2) 未涉及深度学习目标检测模型；3) 未提供部署相关指标；4) 未说明同步更新策略在大规模网络上的可扩展性；5) 未说明与现有加速方法的对比。

Ideas for my research: 借鉴意义有限。唯一可能的启发点是"同步更新+能量下降最大化"的优化思路，在理论上与模型压缩中的"重要性排序剪枝"有概念上的相似性（即优先处理对整体目标贡献最大的部分），但工程上难以直接迁移到遥感目标检测模型的轻量化。不建议作为主要参考。

Abstract: The dynamics of the original Hopfield network is asynchronous (sequential) (updates the state of only one neuron per time step). In this paper, we propose a new tool and a new dynamics to reduce the processing time by updating one or more neurons simultaneously per instant while ensuring process convergence and aiming for the maximum energy decrease at each step, thus guaranteeing the shortest total processing time. From the point of view of synchronous dynamics, calculating the next network state at which energy decreases the most from the current state while ensuring convergence is itself a combinatorial optimization problem. We develop and use a new tool to solve it. We call this new tool Discrete Differential Filter (DDF) and, based upon it, we develop a new synchronous dynamics which we call SD-DDF (Synchronous Dynamics based upon Discrete Differential Filter). In this paper, we review the original asynchronous dynamics for Hopfield networks and present a new tool and a new synchronous dynamics with its theoretical justification and four computational experiments to assess the speed up in processing time empirically.

</details>


### [172] [Baikal: Structured Search for Deep Research over Data Lakes](https://arxiv.org/abs/2607.27726)
*Dhruv Agarwal,Rishitha Guttapalle Mohan,Aarti Kumari,Ashi Sinha,Athulya Anil,Kavitha Srinivas,Horst Samulowitz,Andrew McCallum*

Main category: cs.AI

TL;DR: 提出Baikal框架，将数据湖上的深度研究建模为预算受限的搜索问题，通过语义区域聚类与自适应探索-利用平衡来提升LLM agent的报告质量。

Research relevance: 1分，基本无关。该论文研究的是LLM agent在数据湖（data lakes）上的深度研究/报告生成问题，涉及检索、聚类、探索-利用权衡等，与遥感、卫星、目标检测、模型轻量化均无任何直接或间接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署技术。

Reading priority: 1分。该论文研究LLM agent数据湖深度研究，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM agent深度研究（deep research）与报告生成；应用场景：数据湖（data lakes）上的异构表格与文本证据检索；传感器类型：不适用；目标类别：不适用（处理对象为表格和文本段落）。

Model architecture: Baikal框架：将异构证据聚类为语义区域（semantic regions），在区域内生成region-grounded子问题，使用finding quality作为奖励更新区域级价值估计，采用多种区域选择策略（random、LLM-guided、Bayesian ε-greedy、UCB）平衡探索与利用。底层依赖LLM agent（如DeepSearcher、OpenCode research agent作为baseline）。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（无剪枝、量化、蒸馏、NAS、轻量网络等）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，无参数量、FLOPs、延迟、显存、能耗或实时性相关指标。

Datasets and metrics: 数据集：HybridQA（10,993张表）和TAT-QA（2,757张表）数据湖，分别配227K Wikipedia段落和13K金融报告段落，共15个查询。评价指标：自建rubric，涵盖groundedness（接地性）、relevance（相关性）、diversity（多样性）、utility（实用性），使用GPT-5-mini打分。Baseline包括DeepSearcher和OpenCode research agent（含检索与聚类变体）。

Experiments: 在两个数据湖上，Baikal在多种区域选择策略下表现良好；最佳配置相比最强baseline在HybridQA上报告分数提升28%，在TAT-QA上提升36%。消融/分析表明增益来源于组织和探索语义证据区域，提升了groundedness和diversity，在相同子问题预算下产出更有用的发现。

Limitations: 与本研究方向完全无关，无遥感、目标检测或轻量化相关内容。摘要未说明计算开销、推理效率、模型规模等部署相关信息。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是：其"将大规模异构数据聚类为语义区域，再在区域内做预算受限的探索-利用平衡"的思想，在概念层面与"在轨遥感目标检测中如何在有限计算预算下高效搜索/处理多尺度目标"有一定抽象类比，但方法论上差异极大，不建议直接迁移。

Abstract: Deep research over data lakes requires an LLM agent to investigate evidence across thousands of heterogeneous tables and passages to synthesize a report. Existing methods perform iterative retrieval and generation, letting accumulated context determine what to investigate next, which can overexploit locally promising evidence and fail to cover distinct semantic regions under a fixed budget. To address this, we cast deep research over data lakes as a budgeted search problem and present Baikal - a framework that clusters heterogeneous evidence into semantic regions, then searches over them adaptively to balance exploration and exploitation. Within each selected region, Baikal generates and investigates region-grounded subquestions, using finding quality as rewards to update region-level value estimates and guide search under policies ranging from random and LLM-guided selection to Bayesian $ε$-greedy and UCB. We evaluate Baikal on 15 queries each over HybridQA and TAT-QA data lakes containing 10,993 and 2,757 tables, respectively, together with 227K Wikipedia passages and 13K financial report passages. We assess research quality with a new rubric covering groundedness, relevance, diversity, and utility, and use GPT-5-mini to score Baikal and strong baselines, including DeepSearcher and an OpenCode research agent with retrieval and clustering variants. Across both data lakes, Baikal performs strongly under several region-selection policies; its best configuration improves report scores over the strongest baselines by 28% on HybridQA and 36% on TAT-QA. Our analyses attribute these gains to organizing and exploring semantic evidence regions, which improves groundedness and diversity and yields more useful findings under the same subquestion budget. These results demonstrate the value of structured semantic exploration for systematic research and discovery over heterogeneous data lakes.

</details>


### [173] [VeriSkill: A Self-Evolution Framework for Program Verification Skills](https://arxiv.org/abs/2607.27733)
*Changguo Jia,Tianqi Zhao,Zhiyou Xiao,Weiming Zhang,Minghui Zhou*

Main category: cs.AI

TL;DR: 提出VeriSkill，一个面向程序验证任务的LLM智能体技能自进化框架，通过将验证失败归因于技能缺陷、提炼诊断签名并迭代优化候选技能来提升程序验证性能。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于程序验证（program verification）领域的LLM智能体技能自进化，属于形式化验证与AI智能体交叉方向，不涉及遥感、卫星、目标检测或模型轻量化中的任何一项。

Reading priority: 1分。论文主题为程序验证与LLM智能体，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：程序验证自动化（program verification）；应用场景：LLM智能体辅助软件验证；传感器类型：不适用；目标类别：不适用。

Model architecture: VeriSkill框架，包含技能归因（attribution）、诊断签名提炼（diagnostic signature distillation）、技能迭代优化（iterative refinement）等模块，基于LLM agent构建。

Lightweight method: 摘要未说明。论文中的"distilling"指从轨迹中提炼技能知识，属于LLM智能体技能蒸馏，并非模型压缩意义上的knowledge distillation（知识蒸馏）。不涉及剪枝、量化、NAS等轻量化方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息，也不涉及星上或边缘部署。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为程序验证性能（verification performance），在多个验证工具（verification tools）、智能体框架（agent frameworks）和LLM后端（LLM backends）上进行实验对比。

Experiments: 实验表明VeriSkill在多个验证工具、智能体框架和LLM后端上一致优于所有基线方法（consistently outperforms all baselines）。摘要未给出具体数值结果和消融实验细节。

Limitations: 摘要未说明。但可推断：该工作面向程序验证领域，与遥感目标检测和模型轻量化完全无关；其"distilling"概念与模型压缩中的知识蒸馏含义不同，不可直接迁移。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是：其"技能自进化"（skill self-evolution）思想——通过归因失败、提炼可复用经验、迭代优化——在方法论层面可类比于轻量化模型部署中的迭代调优流程（如剪枝后微调、量化感知训练中的反馈循环），但需要自行映射到遥感目标检测场景，直接可复现性低。

Abstract: Automating program verification with LLM agents requires generating specifications, annotations, auxiliary lemmas, and tool invocations, all of which depend on reusable skills. A natural remedy is skill self-evolution: distilling skills from trajectories and refining them through feedback. However, existing evolution methods struggle with program verification tasks because they cannot reliably identify skill-specific failures or extract actionable signals from opaque verifier feedback. In this paper, we propose VeriSkill, a self-evolution framework built for program verification. It attributes verification failures to skill deficiencies, distills diagnostic signatures into reusable lessons, and iteratively refines candidate skills, admitting only revisions that improve verification performance while preserving program semantics. Experiments show that VeriSkill consistently outperforms all baselines across multiple verification tools, agent frameworks, and LLM backends.

</details>


### [174] [SpecCal: Ambiguity-Aware Candidate Calibration for Infrared Spectrum-Based Molecular Structure Reconstruction](https://arxiv.org/abs/2607.27788)
*Yixuan Chen,Bo Liu,Yusen Tan,Guokun Yang,Wenjie Du,Jun Xia*

Main category: cs.AI

TL;DR: 提出SpecCal，一种无需训练的候选校准框架，用于从红外光谱推断分子结构，通过重排序和引入结构上合理的候选分子来改进IR-to-molecule预测。

Research relevance: 与本研究方向基本无关（1分）。论文研究的是化学信息学/计算化学领域的问题——从红外光谱（IR spectra）推断分子结构（molecular structure），属于分子重建/化学结构预测任务。完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感相关主题，也不涉及object detection、小目标检测、旋转框检测等目标检测任务，更不涉及pruning、quantization、knowledge distillation、NAS、轻量化、边缘部署等模型压缩与部署技术。弱相关原因：论文中的"candidate calibration"、"re-ranking"、"model-agnostic"等思想在方法论层面可能对通用机器学习有一定借鉴，但场景和任务与遥感在轨目标检测轻量化完全无关。

Reading priority: 1分。论文研究的是化学信息学中的分子结构推断问题，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然"training-free"后处理校准思想有一定方法论借鉴价值，但整体相关性极低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：IR-to-molecule prediction（从红外光谱预测分子结构），属于化学信息学/分子结构重建任务。应用场景：化学分析、分子结构鉴定。传感器类型：红外光谱仪（IR spectrometer），非遥感传感器。目标类别：分子结构（SMILES表示、scaffold骨架），非遥感目标。

Model architecture: SpecCal是一个training-free（无需训练）的candidate calibration框架，作用于已有base model的候选输出之上。核心机制包括：(1) 对当前候选分子进行re-ranking（重排序）；(2) 引入由spectral consistency（光谱一致性）引导的额外结构上合理的候选分子。框架是plug-and-play、model-agnostic（模型无关），无需参数更新即可集成到不同base model。

Lightweight method: 摘要未说明。论文不涉及pruning、quantization、knowledge distillation、NAS等模型压缩或加速方法。其"training-free"和"model-agnostic"特性属于推理阶段的后处理校准策略，而非模型轻量化手段。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。且该任务场景（分子结构推断）与星上/边缘部署无关。

Datasets and metrics: 摘要提到"multiple benchmarks"（多个基准数据集），但未具体说明数据集名称。评价指标为top-k reconstruction（top-k重建率），在SMILES层面和scaffold层面分别评估。具体数据集名称、规模、划分方式均未说明。

Experiments: 在多个基准上，SpecCal在不同base model上一致地提升了top-k reconstruction（SMILES和scaffold两个层面）。进一步分析表明，在光谱歧义（spectral ambiguity）下校准候选集是改进分子重建的实用途径。未提供具体数值结果、对比方法细节或消融实验细节。

Limitations: (1) 与遥感、卫星、目标检测、轻量化完全无关，无法直接借鉴到在轨遥感目标检测场景；(2) 摘要未提供具体实验数值、数据集名称、计算开销；(3) "training-free"的校准框架依赖已有base model的候选输出质量，若base model本身性能差则校准收益有限；(4) 光谱歧义下的候选校准思路仅适用于生成式候选集场景，与检测模型的轻量化部署问题不匹配。

Ideas for my research: 尽管论文场景与遥感在轨目标检测无关，但"training-free"、"model-agnostic"的推理阶段后处理校准思想有一定方法论启发：(1) 在遥感目标检测中，若已有检测模型输出候选框集合，可考虑类似的后处理重排序策略来提升检测精度，而不增加模型参数量或推理负担；(2) "spectral consistency"引导的候选补充思路，可类比为遥感检测中利用多光谱/时序一致性约束来补充或修正检测结果；(3) 但需注意，这些借鉴属于方法论层面的间接启发，论文本身不提供任何可直接复用的遥感检测或轻量化技术。

Abstract: Inferring molecular structures from infrared (IR) spectra is a fundamental yet challenging problem. A key difficulty is that an IR spectrum provides limited structural information: different molecules may share similar functional groups and local vibrational patterns, leading to highly similar spectral responses. Thus, even when an observed spectrum has a unique underlying structure, reconstructing it from the spectrum remains ambiguous. Existing IR-to-molecule models usually generate a ranked set of candidate molecules, but this set is largely determined by the model's learned generation preference and may not fully capture the structures that best satisfy the observed spectral constraints. To address this limitation, we propose SpecCal, a training-free candidate calibration framework for IR-to-molecule prediction. SpecCal operates on the candidate outputs of existing base models and improves the prediction set by re-ranking current candidates while introducing additional structurally plausible alternatives guided by spectral consistency. The framework is plug-and-play and model-agnostic, requiring no parameter updates for integration with diverse base models. Experiments on multiple benchmarks show that SpecCal consistently improves top-k reconstruction at both SMILES and scaffold levels across different base models. Further analyses demonstrate that calibrating candidate sets under spectral ambiguity provides a practical way to improve molecular reconstruction from IR spectra. The code is available at: https://anonymous.4open.science/r/SpecCal-B18A.

</details>


### [175] [Annotating Topical Legal Insights from Case Proceedings](https://arxiv.org/abs/2607.27792)
*Subinay Adhikary,Dwaipayan Roy,Debasis Ganguly,Shouvik Kumar Guha,Kripabandhu Ghosh*

Main category: cs.AI

TL;DR: 提出LeDA法律数据标注系统，用于从法律案件文书中提取概念/实体，构建文档的语义表示（bag of concepts），以支持先例检索、判决预测等下游任务。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦法律文本处理（legal NLP），涉及法律文档标注系统、概念抽取、语义表示，不涉及遥感、卫星、目标检测或任何模型压缩/轻量化技术。属于自然语言处理/法律信息学领域，与本课题无直接关联。

Reading priority: 1分。论文主题为法律文本标注系统，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无借鉴价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：法律文档概念/实体标注与语义表示构建。应用场景：法律案件文书处理（Indian Supreme Court case proceedings）。传感器类型：不适用（纯文本数据）。目标类别：法律概念/实体（legal concepts/entities），非视觉目标。

Model architecture: 摘要未说明具体模型架构。系统LeDA为基于web界面的数据标注工具，提供通用标注与裁决功能，支持动态创建新标签（dynamic tag creation），用于无预定义本体（ontology）的场景。

Lightweight method: 摘要未涉及任何轻量化方法（无pruning、quantization、knowledge distillation、NAS、轻量网络等）。论文核心是数据标注系统，而非模型压缩或加速。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：印度最高法院案件文书（Indian Supreme Court case proceedings），由3名评估者（assessors）进行标注。评价指标：摘要未说明具体量化指标。

Experiments: 摘要未说明具体实验对比或消融实验。仅描述了LeDA系统被3名评估者用于标注和裁决法律概念名称的使用过程。

Limitations: 摘要未说明局限性。整体上该工作与本课题方向完全无关，属于法律NLP领域，无法为遥感目标检测或轻量化提供直接借鉴。

Ideas for my research: 借鉴价值极低。唯一可能间接相关的点是"动态创建新标签"（dynamic tag creation）的思想——在遥感目标检测中，若遇到新类别目标（如新型舰船/飞机），可考虑动态扩展类别标签的机制，但这与本课题的轻量化核心目标关系甚微。不建议投入时间深入阅读。

Abstract: In this paper, we mainly concentrate on finding concepts or topics from the legal case proceedings, since adopting a structured representation for legal documents, as opposed to a mere bag-of-words flat text representation, can significantly enhance processing capabilities. To achieve this objective, we put forward a set of diverse concepts for legal case proceedings. With this motivation, we propose LeDA, a system for Legal Data Annotation. The system offers the generic functionality of annotating and adjudicating entities or concepts within documents via a web-based interface. A novel feature of our system is that it allows to dynamic create new tags for annotation, which is a particularly useful provision for situations where there exists no pre-defined ontology for the entities (concepts) that need to be annotated - these being rather discovered by annotators as they continue examining more documents. The system that we demonstrate is currently in use to annotate a set of concepts from legal documents to construct semantic representations of documents as bags of concepts that can then be used for several downstream tasks, such as prior case retrieval, judgment prediction, and so on. Along with the system features in general, we also describe how LeDA was used by 3 assessors to annotate and adjudicate legal concept names from Indian Supreme Court case proceedings.

</details>


### [176] [MemeBench: What LVLMs Miss When Interpreting Culture-Dependent Memes](https://arxiv.org/abs/2607.27798)
*Weihang Wang,Kainan Tu,Jielei Zhang,Run Yang,Boheng Sheng,Yuchen He,Yu Xie,Pengyu Chen,Peiyi Li,Huyang Sun,Longwen Gao,Zhouhui Lian*

Main category: cs.AI

TL;DR: 提出MemeBench诊断基准和KAR检索基线，用于评估和改善大型视觉语言模型（LVLMs）对网络迷因（meme）的文化知识理解能力。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于大型视觉语言模型对网络迷因（meme）的文化背景知识理解，属于多模态理解与视觉-语言推理领域，完全不涉及遥感、卫星、地球观测、目标检测或模型轻量化部署。虽然涉及LVLMs，但既非遥感场景，也非目标检测任务，更无任何轻量化/部署相关内容。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于多模态视觉-语言理解领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视觉-语言理解诊断与解释生成（meme interpretation）。应用场景：网络迷因理解，涉及动漫、漫画、游戏及在线亚文化。传感器类型：不适用（输入为普通网络图片）。目标类别：迷因中的视觉线索、文化实体、背景知识、推理机制。

Model architecture: 摘要未说明具体模型架构。涉及26个大型视觉语言模型（LVLMs）作为评估对象，并提出KAR（entity-guided retrieval baseline）检索基线，构建于CultureBase知识库之上。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：MemeBench，包含1,253个中英文迷因样本，带有人工撰写的参考答案和质量控制的VIKR标注。评价指标：VIKR Success（按Visual clues、Identity links、Knowledge units、Reasoning mechanisms四维度分解评估）、Visual-Knowledge gap（22.6%）。

Experiments: 在26个LVLMs上评估，发现所有模型对可见内容的覆盖优于对解释所需知识的覆盖，最强模型仍保留22.6%的Visual-Knowledge gap。引入KAR检索基线后，在四个受控模型上VIKR Success提升3.6-7.4%，相比通用检索修复更多答案且破坏更少；但两种检索条件均提升Identity和Knowledge的同时降低了Visual coverage。

Limitations: 与本研究方向完全无关。摘要未涉及遥感、目标检测、轻量化或部署的任何信息。此外，论文本身聚焦于迷因理解这一特定多模态任务，其基准和方法的可迁移性有限。

Ideas for my research: 尽管整体弱相关，但可借鉴的点有限：1) VIKR分解式评估思想（将复杂任务分解为多个可诊断的子维度）可类比用于遥感目标检测的误差分解分析（如定位误差vs分类误差vs小目标漏检率）；2) 诊断-改进闭环思路（先诊断gap再针对性修复）可迁移到轻量化模型精度损失的归因分析，例如通过逐层/逐模块诊断剪枝或量化后的精度下降来源。但这些仅为方法论层面的泛化借鉴，与遥感在轨检测无直接关系。

Abstract: Large vision-language models have improved at describing visual content, but accurate descriptions do not ensure interpretation when meaning depends on knowledge beyond the pixels. Memes expose this gap because they rely on cultural entities, background knowledge, and community conventions. Most meme benchmarks reduce interpretation to labels or holistic scores, obscuring where an explanation breaks down. We introduce MemeBench, a diagnostic benchmark of 1,253 Chinese and English memes with human-written references and quality-controlled VIKR annotations, centered on anime, comics, games, and adjacent online subcultures. Its VIKR schema decomposes explanations into Visual clues, Identity links, Knowledge units, and Reasoning mechanisms. Across 26 LVLMs, every model covers visible content more reliably than the knowledge needed to interpret it, and even the strongest retains a 22.6% Visual-Knowledge gap. To test whether this diagnosis can guide improvement, we introduce KAR, an entity-guided retrieval baseline built on CultureBase. Across four controlled models, KAR raises VIKR Success by 3.6-7.4% and, compared with generic retrieval, repairs more answers and breaks fewer. Yet both retrieval conditions improve Identity and Knowledge while reducing Visual coverage in every comparison. MemeBench reveals whether an interpretation succeeds, what is missing, and whether targeted evidence fills the diagnosed gap.

</details>


### [177] [STEREODISCO: Discovering Stereotypicality in LLMs](https://arxiv.org/abs/2607.27824)
*Farane Jalali Farahani,Corina Dima,Mojtaba Nayyeri,Raphael H. Heiberger,Steffen Staab*

Main category: cs.AI

TL;DR: 提出STEREODISCO框架，通过语义微分法在LLM内部激活空间中系统性地发现和验证刻板印象语义轴，并发现LLM编码的刻板印象内容与人类心理学文献存在分歧。

Research relevance: 1分，基本无关。该论文研究的是大语言模型（LLM）内部表征中的社会刻板印象（stereotypes），属于自然语言处理/社会计算领域，与遥感、卫星、目标检测、轻量化部署均无任何关联。

Reading priority: 1分。该论文与遥感卫星在轨目标检测与模型轻量化方向基本无关，属于LLM社会刻板印象研究，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务：在LLM内部激活空间中识别刻板印象语义轴（stereotypical semantic axes）。应用场景：社会心理学与NLP交叉研究。传感器类型：不适用。目标类别：社会群体（social groups）的刻板印象属性。

Model architecture: STEREODISCO框架：从WordNet反义词集构建约2000个候选语义轴，通过probing（探针）在LLM激活空间中恢复每个语义轴为几何轴，再通过概念投影的统计检验识别刻板印象轴。案例研究使用LLAMA-3-8B-INSTRUCT和MISTRAL-7B-INSTRUCT两个模型。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化方法。

Onboard deployability: 摘要未说明。论文完全不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：WordNet反义词集（构建候选语义轴）、LLAMA-3-8B-INSTRUCT和MISTRAL-7B-INSTRUCT的内部表征、人类标注者验证。评价指标：统计检验（statistical test over concept projections）、与人类评分的一致性比较。

Experiments: 主要发现：两个LLM在社会群体评分上彼此的一致性高于与人类的一致性，说明LLM编码的刻板印象内容与心理学文献记录存在分歧；发现了先前研究未涉及的刻板印象轴，包括humble vs. proud、narrow-minded vs. broad-minded、cowardly vs. brave，并得到人类标注者独立确认。

Limitations: 摘要未说明局限性。但明显与本课题方向完全无关，属于NLP/社会计算领域，不涉及任何遥感、目标检测或模型轻量化内容。

Ideas for my research: 无直接可借鉴点。该论文与遥感卫星在轨目标检测及轻量化方向完全无关。唯一可能的间接启发是"probing"（探针分析）思想——即通过线性探针在模型内部表征中恢复语义轴，这一方法论在深度学习可解释性研究中通用，理论上可迁移到分析轻量化目标检测模型内部特征表征的压缩损失，但需要大量改造且关联性很弱。

Abstract: LLMs encode, convey, and perpetuate stereotypes. Prior computational research focuses on a small set of semantic axes investigated in social psychology, and operates on word embeddings produced by language models, leaving open which other semantic axes carry stereotypical associations in LLMs and how LLMs internally represent such axes. We introduce STEREODISCO, a framework that adapts the semantic differential method (Osgood et al., 1957) to the systematic study of stereotypes in LLM internal representations. STEREODISCO constructs approx. 2,000 candidate semantic axes from WordNet antonym synsets, recovers each as a geometric axis in the LLM's activation space via probing, and identifies stereotypical axes via a statistical test over concept projections. As a case study, we apply STEREODISCO to social group stereotypes with LLAMA-3-8B-INSTRUCT and MISTRAL-7B-INSTRUCT. We find that the two LLMs agree with each other on social group ratings more than with humans, suggesting that LLM-encoded stereotype content diverges from that documented in social psychology. We also discover stereotypical axes not investigated in prior work -- including humble vs. proud, narrow-minded vs. broad-minded, and cowardly vs. brave, which human annotators independently confirm.

</details>


### [178] [Sign Language Question Answering: A New Task, Benchmark, and Baseline for Sign Language Understanding](https://arxiv.org/abs/2607.27826)
*Shiwei Gan,Lichen Wang,Xiao Liu,Yafeng Yin,Kuizhuang Liu,Sanglu Lu,Lei Xie*

Main category: cs.AI

TL;DR: 提出手语问答（SLQA）新任务，构建两个基于PHOENIX14T和CSL-Daily的SignQA基准数据集，并提出带Question-Conditioned Modulated Temporal Downsampling模块和域内知识迁移策略的基线模型。

Research relevance: 弱相关（1分）。该论文研究手语理解（Sign Language Understanding），属于视频理解与视觉-语言多模态方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关；不涉及目标检测、小目标检测、旋转框检测、舰船/飞机/车辆检测或遥感场景理解；也不涉及剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等轻量化或部署技术。仅有的微弱关联在于其使用了"knowledge transfer"（知识迁移）策略，与知识蒸馏（knowledge distillation）在概念上有一定相似性，但并非模型压缩意义上的蒸馏。

Reading priority: 1分。该论文研究手语问答任务，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：手语问答（Sign Language Question Answering, SLQA），属于视频理解与视觉-语言推理任务。应用场景：手语视频语义理解。传感器类型：普通RGB视频（手语视频）。目标类别：手语词汇（gloss）与口语语句，非遥感目标。

Model architecture: 基线模型包含两个关键模块：(1) Question-Conditioned Modulated Temporal Downsampling模块，用于问题条件化的时间特征建模与下采样；(2) in-domain knowledge transfer策略，从已有SLU任务（如连续手语识别、手语翻译）迁移知识。具体网络主干结构摘要未说明。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等模型压缩或加速方法。仅提到"in-domain knowledge transfer"（域内知识迁移），这是任务间知识迁移而非模型压缩意义上的知识蒸馏。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且该任务本身（手语视频问答）与星上/边缘部署场景无关。

Datasets and metrics: 数据集：基于PHOENIX14T和CSL-Daily两个手语数据集，通过模板自动生成问答对构建SignQA基准，覆盖五类问题：position reasoning（位置推理）、structural reasoning（结构推理）、visual search（视觉搜索）、gloss recognition（词汇识别）、translation understanding（翻译理解）。评价指标：摘要未明确说明具体指标，但提到与代表性vision-language模型对比，推测使用问答准确率类指标。

Experiments: 实验表明基线模型在所有问题类别上一致优于代表性vision-language模型（具体模型名称和数值摘要未说明）。数据集已在HuggingFace公开。

Limitations: 与本研究方向基本无关。主要局限：该工作面向手语视频问答，属于通用视频-语言理解范畴，与遥感目标检测、在轨部署、模型轻量化均无直接关联。摘要未提供模型复杂度、推理效率等对部署有价值的信息。

Ideas for my research: 借鉴价值有限，但可考虑以下几点：(1) "Question-Conditioned Modulated Temporal Downsampling"中"条件调制+时间下采样"的设计思路，在概念上可类比于遥感目标检测中基于注意力/条件调制来引导特征选择，但需大幅改造；(2) "in-domain knowledge transfer"（域内知识迁移）思想可类比于遥感领域从预训练检测模型向在轨轻量模型迁移知识，但该论文并非蒸馏方法；(3) 模板化自动生成标注数据的方法，可类比于遥感领域自动生成合成遥感图像标注，但应用场景差异大。总体而言，该论文对"在轨遥感目标检测模型轻量化"课题的借鉴点非常有限。

Abstract: Recent advances in sign language (SL) understanding (SLU) have led to remarkable progress in tasks such as continuous SL recognition and SL translation. However, these tasks are designed with predefined objectives, requiring models to learn a fixed mapping from sign videos to glosses or spoken-language sentences. As a result, they provide only a limited assessment of whether a model truly understands the semantic content of SL videos. To address this limitation, \textbf{we first propose a new task, Sign Language Question Answering (SLQA)}, which evaluates SL understanding by requiring models to answer arbitrary natural language questions about SL videos. Unlike previous SLU tasks, SLQA provides a more flexible and comprehensive evaluation framework that assesses multiple reasoning capabilities beyond recognition and translation. To facilitate this task, \textbf{we further construct two SignQA benchmarks} based on PHOENIX14T and CSL-Daily by automatically generating question-answer pairs from existing gloss and sentence annotations using carefully designed templates. The resulting datasets cover five complementary question categories, including position reasoning, structural reasoning, visual search, gloss recognition, and translation understanding. \textbf{Finally, we propose a simple yet effective baseline model} equipped with a Question-Conditioned Modulated Temporal Downsampling module and an in-domain knowledge transfer strategy, enabling effective knowledge transfer from existing SLU tasks while enhancing question-aware temporal feature modeling. Extensive experiments demonstrate that our baseline consistently outperforms representative vision-language models across all question categories, establishing a strong benchmark for future research on SLQA. Datasets are available at:{https://huggingface.co/datasets/hulala/SignQA-2026}.

</details>


### [179] [MemTxn: A Transaction Boundary for Source-Supported Updates and Complete-State Recovery in Agent Memory](https://arxiv.org/abs/2607.27834)
*Hanshuai Cui,Zhiqing Tang,Zhi Yao,Fanshuai Meng,Qianli Ma,Weijia Jia*

Main category: cs.AI

TL;DR: MemTxn 是一个面向大语言模型（LLM）智能体持久记忆系统的治理层，通过事务边界、来源验证、版本解析和快照日志来保证记忆更新的可靠性与故障恢复。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于大语言模型（LLM）智能体的持久记忆管理与事务性更新，属于自然语言处理与系统可靠性领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等任何遥感相关主题，也不涉及目标检测或模型轻量化。

Reading priority: 1 分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于 LLM 记忆系统可靠性研究，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM 智能体持久记忆的可靠更新与故障恢复（memory governance / transaction management）。应用场景：长期运行的 LLM agent 跨会话、跨任务复用信息。传感器类型：不适用。目标类别：不适用（涉及的是记忆条目/事实版本，而非视觉目标）。

Model architecture: MemTxn 作为 answer model 外部的治理层（governance layer），包含三个核心组件：Ordered PatchTest（验证写入是否被来源支持）、Temporal Resolver（事实冲突时选择可见版本）、durable snapshot journal（故障后恢复应用可见状态）。摘要未说明底层 answer model 的具体架构。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT 等模型压缩或加速方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。该工作面向 LLM 记忆系统可靠性，与星上/边缘部署无关。

Datasets and metrics: 数据集：LongMemEval-S、LoCoMo、MemoryAgentBench FactConsolidation。评价指标：item-disjoint audit 上的接受/拒绝率（60 个 supported originals 全部接受，179 个 hard negatives 全部拒绝）、多键故障下的状态恢复完整性、FactConsolidation 上的平均 F1。

Experiments: 主要结果：在 item-disjoint audit 上接受全部 60 个 supported originals 并拒绝全部 179 个 hard negatives；在 LongMemEval-S 和 LoCoMo 的持久多键故障下，无需知道实际物理写入集即可恢复完整声明的 active map；在 MemoryAgentBench FactConsolidation 上，跨 12 种 answer-model 配置取得最高平均 F1，在 5 个代表性设置中比 Dense 高出 17.06–24.07 分。

Limitations: 摘要未说明：底层 answer model 的规模与类型、MemTxn 引入的额外计算/存储开销、对超长会话或大规模记忆条目的可扩展性、在真实生产环境中的部署成本、以及该方法是否可迁移到视觉/遥感任务。此外，该工作与遥感目标检测和模型轻量化完全无关，缺乏任何可借鉴的视觉或部署信息。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的借鉴点极为有限。唯一可能的间接启发是：MemTxn 强调"事务边界"与"来源验证"的可靠性设计思想，若未来在轨智能系统需要管理多源遥感数据的增量更新或模型在线自适应（如持续学习中的记忆管理），其"版本解析 + 快照恢复"的思路或可类比用于管理在轨模型的增量更新与回滚。但这是非常间接的类比，不构成直接可复现的技术路线。

Abstract: Persistent memory lets long-running large language model agents reuse information across sessions and tasks. Yet errors in writable memory can persist and corrupt future behavior. Existing systems improve storage and retrieval, but they do not provide a transaction boundary for reliable updates and recovery. We therefore propose MemTxn, a governance layer outside the answer model. MemTxn verifies whether an update is supported by its source. It also selects the visible version when facts conflict and restores the application-visible state after a fault. The system uses Ordered PatchTest to validate writes, a Temporal Resolver to select versions, and a durable snapshot journal to recover state. On an item-disjoint audit, MemTxn accepts all 60 supported originals and rejects all 179 hard negatives. Under persistent multi-key faults on LongMemEval-S and LoCoMo states, it restores the complete declared active map without knowing the actual physical write set. On MemoryAgentBench FactConsolidation, MemTxn achieves the highest average F1 across all twelve answer-model configurations. It outperforms Dense by 17.06--24.07 points in five representative settings.

</details>


### [180] [Crossing the Margin Cliff: Toward Relearn-Robust LLM Unlearning via Margin Calibration](https://arxiv.org/abs/2607.27836)
*Xiangyu Yin,Jiaxu Liu,Zhen Chen,Chih-Hong Cheng*

Main category: cs.AI

TL;DR: 提出 Margin Calibration (MC) 方法，通过非饱和 margin hinge 和 KL probe 修复大语言模型 unlearning 在 relearn attack 下的脆弱性，并证明其优化几何上的 "margin cliff" 现象。

Research relevance: 弱相关（2分）。该论文聚焦大语言模型（LLM）的 unlearning 与 relearn attack 鲁棒性，属于自然语言处理/机器学习安全方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。仅"deployment variant"一词涉及部署，但并非面向遥感在轨场景。

Reading priority: 1分。该论文属于 LLM unlearning 安全方向，与遥感卫星在轨目标检测及轻量化课题基本无关。仅"损失饱和导致优化失效"的分析思路对知识蒸馏损失设计有极间接的启发，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务：大语言模型 unlearning（遗忘学习）与 relearn attack 防御。场景：NLP 模型安全与隐私。传感器类型：不适用（非遥感）。目标类别：不适用。

Model architecture: 基于 Llama-3、Llama-2-7B-hf、Phi-3.5 等大语言模型；方法为 plug-in 的 Margin Calibration (MC) 模块，包含非饱和 margin hinge 损失和基于 disjoint instruction corpus 的 KL probe。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS 等模型压缩或加速方法。虽然提到 "deployment variant"，但未涉及参数量、FLOPs、延迟等轻量化指标。

Onboard deployability: 摘要未说明。论文仅提及 "deployment variant"（部署变体），但未给出参数量、FLOPs、延迟、显存、能耗、实时性等任何星上/边缘部署相关信息。且部署对象是 LLM，与遥感在轨硬件（FPGA/NPU）无关。

Datasets and metrics: 数据集：TOFU（三个 Llama-3 尺寸、三个 forget tier）、MUSE-News（Llama-2-7B-hf）、Phi-3.5 panel。指标：ROUGE-L、membership AUC、per-token answer margin、relearn attack budget。

Experiments: 在 TOFU、MUSE-News、Phi-3.5 上验证 MC 方法：单一冻结配置赢得全部 14 个 head-to-head forget aggregates 和所有 relearn cells（panel-mean post-attack ROUGE-L 从 0.41 降至 0.18），并在 13/14 个单元降低 raw membership AUC。主要代价是 retain-side utility 下降。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于 LLM 安全方向；2. 主要代价是 retain-side utility 下降，即遗忘学习会损害模型保留知识的能力；3. 方法依赖 stated gradient-dominance condition，其适用边界需进一步验证；4. 摘要未说明计算开销、推理效率等部署指标。

Ideas for my research: 1. 论文中 "margin cliff" 的优化几何分析思路（损失饱和导致梯度消失）可类比迁移到轻量化目标检测中的知识蒸馏场景——当教师-学生 logits 差异过小时，蒸馏损失可能饱和，导致学生模型难以继续学习，可借鉴非饱和 margin hinge 思想设计更鲁棒的蒸馏损失；2. "plug-in polish" 的模块化设计思路可借鉴——在现有检测模型上以即插即用方式添加轻量校准模块，避免重新训练整个网络；3. 但整体而言，该论文与遥感在轨目标检测轻量化课题的直接借鉴价值有限。

Abstract: Large language model unlearning is consistently fragile under relearn attacks. On TOFU, fine-tuning on twenty forget examples substantially recovers held-out forget-set ROUGE for every method we evaluate, and we trace this fragility to optimization geometry. The per-token answer margin of fourteen post-hoc methods spanning gradient, preference, and distillation families converges into a narrow band above the retain reference in 41 of 42 method--size cells, a regularity we call the margin cliff. We prove that this cliff follows whenever the retain coupling holds the diagnostic log-odds of forget content above a floor, a condition that token-saturating losses induce at stationarity and that we verify directly on 34 of 42 cells. Margin Calibration (\textsc{MC}) is a plug-in polish adding a non-saturating margin hinge anchored at the reference's per-token margin plus a KL probe on a disjoint instruction corpus, restoring forget-side pressure where the native loss saturates. Under a stated gradient-dominance condition, whose on-trajectory gradient signature we measure by instrumenting the polish, its stationary set lies on the cliff-crossing side, yielding an attack-budget upper bound on the relearn margin lift. Across TOFU (three Llama-3 sizes, three forget tiers), MUSE-News on Llama-2-7B-hf, and a Phi-3.5 panel, a single frozen configuration wins all 14 head-to-head forget aggregates and all populated relearn cells (panel-mean post-attack ROUGE-L $0.41$ to $0.18$) and lowers raw membership AUC on 13/14, with reduced retain-side utility as the main cost. A deployment variant matches these gains without a retain-trained reference.

</details>


### [181] [Virtual Process Dossier: A Process-Aware Data Catalogue](https://arxiv.org/abs/2607.27840)
*Lukas Kubelka,Alexander Bott,Frank Döhner,Saksham Kiroriwal,Georg Zeeb,Julia Butte,Julius Pfrommer,Jürgen Beyerer,Tobias Käfer*

Main category: cs.AI

TL;DR: 提出Virtual Process Dossier (VPD)，一个基于知识图谱的数据目录系统，用于多阶段制造场景中捕获工作流溯源信息，以FAIR方式提供数据集并显式表达前瞻性和回顾性工作流溯源。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于制造业多阶段生产流程中的数据管理与工作流溯源（workflow provenance），属于知识图谱、数据目录和工业AI数据治理领域，不涉及遥感、卫星、目标检测或模型压缩/加速等任何相关主题。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于工业制造数据管理领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：数据目录与工作流溯源管理（data catalogue & workflow provenance）；应用场景：多阶段制造（multi-stage manufacturing）；传感器类型：不适用；目标类别：不适用。

Model architecture: VPD系统由三部分组成：(1) VPD本体（ontology）作为目录的语义核心；(2) VPD溯源框架，将本体实例化集成到生产环境中；(3) VPD用户界面，提供以人为中心的知识图谱交互。不涉及任何深度学习模型或神经网络架构。

Lightweight method: 不涉及。论文未提及任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计方法。

Onboard deployability: 不涉及。论文未提及星上部署、边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、显存或能耗等任何部署相关信息。

Datasets and metrics: 摘要未说明具体数据集和评价指标。论文关注的是制造流程中生成的数据集如何以FAIR方式被目录化和管理，而非模型训练/评测数据集。

Experiments: 摘要未说明具体实验结果、对比方法或消融实验。仅描述了VPD系统的三个贡献组件。

Limitations: 摘要未说明局限性。但明显与本课题方向无关：不涉及遥感图像、目标检测、模型轻量化或任何计算视觉任务。代码和本体已开源（GitHub），但面向的是制造数据管理而非遥感。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：VPD强调数据溯源（provenance）和FAIR数据管理，这提醒我们在遥感在轨目标检测研究中，也应重视训练数据集的版本管理、标注溯源和实验可复现性，但这对模型轻量化本身没有直接帮助。

Abstract: We propose the Virtual Process Dossier (VPD), a Knowledge Graph-based data catalogue that also captures workflow provenance. We developed VPD for multi-stage manufacturing use-cases where downstream AI-based optimization tasks require to distinct between datasets generated during individual workflow steps. VPD provides these datasets in a FAIR manner and makes both prospective and retrospective workflow provenance explicit. Our contributions are: (1) the VPD ontology that serves as the catalogue's semantic core; (2) the VPD provenance framework that integrates ontology instantiation into the production environment; and (3) the VPD user interface that provides human-centered interaction with the VPD Knowledge Graph. The ontology and code are available at https://github.com/kubeluk/VirtualProcessDossier .

</details>


### [182] [Simplifying Neural Networks During Training](https://arxiv.org/abs/2607.27854)
*Lorenzo Sciandra,Samuele Fonio,Roberto Esposito*

Main category: cs.AI

TL;DR: 提出一种基于 Neural Collapse (NC) 和 Tunnel Effect 的训练框架，通过 Inverse Fisher Criterion 监控表示动态，在训练过程中识别特征提取与分类的分割点，将尾部层替换为轻量分类头，实现训练阶段的网络简化与参数缩减。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/泛视觉方法，聚焦于 overparameterized 深度网络的训练动态与 Neural Collapse 理论，不涉及遥感、卫星、Earth observation 等场景，也不涉及目标检测任务。虽然其"训练中简化网络、减少参数"的思想与轻量化有间接关联，但并非面向遥感在轨目标检测的轻量化方法，也未涉及剪枝/量化/蒸馏/部署等具体压缩手段。

Reading priority: 2分。该论文与遥感在轨目标检测轻量化方向弱相关，主要价值在于 Neural Collapse 理论对"何时简化网络"的启发，但缺乏遥感场景、目标检测任务和部署指标，借鉴价值有限。可作为理论背景参考，不建议作为核心方法来源。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像分类 (image classification)；应用场景：通用视觉分类基准；传感器类型：不适用（非遥感）；目标类别：摘要未说明（通用分类数据集如 CIFAR 等，摘要未明确列出）。

Model architecture: 基于 MLP、VGG、ResNet 架构；核心思想是利用 Neural Collapse 和 Tunnel Effect 理论，通过 Inverse Fisher Criterion 监控 variability collapse 行为，识别特征提取与分类的 split point，将尾部层替换为轻量分类头 (lightweight classification head) 后继续训练简化模型。

Lightweight method: 属于"训练中动态简化"的轻量化思路，而非传统模型压缩方法。具体手段：在训练过程中根据 NC 动态识别可裁剪的尾部层，替换为轻量分类头。不涉及剪枝 (pruning)、量化 (quantization)、知识蒸馏 (knowledge distillation)、NAS 等经典压缩/加速手段。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。仅提及"substantial parameter reductions"，但未给出具体数值，也未涉及星上/边缘部署 (onboard/edge deployment)、FPGA/NPU/TensorRT 等硬件适配。

Datasets and metrics: 数据集：image-classification benchmarks（具体数据集名称摘要未说明）；评价指标：准确率 (accuracy) 与参数量缩减 (parameter reductions)；实验架构：MLP、VGG、ResNet。

Experiments: 在 MLP、VGG、ResNet 架构上的图像分类基准实验中，该方法在保持与完整模型相当准确率的同时实现了显著的参数缩减。摘要未给出具体对比数值、消融实验细节或与现有轻量化方法的对比。

Limitations: 1. 仅针对图像分类任务，未验证目标检测场景；2. 未涉及遥感/卫星数据；3. 未报告部署相关指标（FLOPs、延迟、能耗等）；4. 未与剪枝、量化、蒸馏等主流轻量化方法对比；5. 轻量化仅针对尾部分类层，对骨干网络 (backbone) 的特征提取层未做压缩，压缩空间有限；6. 摘要未说明该方法在复杂任务（如小目标检测、旋转框检测）上的适用性。

Ideas for my research: 1. Neural Collapse 理论可用于分析遥感目标检测模型在训练过程中的表示动态，帮助判断哪些层/阶段可以提前简化，为在轨模型的训练阶段轻量化提供理论依据；2. Inverse Fisher Criterion 作为 variability collapse 的稳定代理指标，可借鉴用于监控遥感检测模型的特征表示收敛状态，辅助确定剪枝或蒸馏的时机；3. "训练中动态简化 + 轻量分类头"的思路可迁移到遥感检测头 (detection head) 的轻量化设计，例如将复杂的检测头替换为更轻量的结构；4. 但需注意：该方法未覆盖 backbone 压缩，而遥感小目标检测的瓶颈往往在 backbone 特征提取层，需结合剪枝/蒸馏等方法补充。

Abstract: Understanding and exploiting the training dynamics of overparameterized deep neural networks remains a central challenge in modern machine learning. Recent evidence on Neural Collapse (NC) shows that class representations and classifiers exhibit highly structured geometry, while the Tunnel Effect suggests that only a subset of layers is essential for feature extraction. We combine these two perspectives and propose an NC-inspired training framework for simplifying deep networks during training. Our method monitors representation dynamics through the Inverse Fisher Criterion, a stable and efficient proxy for the variability collapse behavior, to identify both the split point between feature extraction and classification and the training stage at which simplification becomes viable. We then replace the trailing layers with a lightweight classification head and continue training the reduced model. Experiments on image-classification benchmarks across MLP, VGG, and ResNet architectures show that the proposed method achieves substantial parameter reductions while maintaining accuracy comparable to that of the full model. Code to reproduce the experiments can be found at: https://github.com/LorenzoSciandra/NNS.

</details>


### [183] [Back to All-Entity Ranking: Sampler-Dependent Evaluation in Continuous-Time Dynamic Graphs](https://arxiv.org/abs/2607.27861)
*Minwoo Yu,Young-guk Ha*

Main category: cs.AI

TL;DR: 本文研究连续时间动态图（CTDG）中"下一目的地预测"任务的负采样策略对模型评估的影响，指出非均匀负分布会改变贝叶斯最优排序，并推荐使用全实体排序（all-entity ranking）替代采样负样本评估。

Research relevance: 与本研究方向基本无关（1分）。论文主题是连续时间动态图（CTDG）中的下一目的地预测任务，属于图神经网络/时序图学习领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。更不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理或边缘部署。唯一可能间接相关的点是其讨论的"评估协议/基准设计"方法论，但这与遥感目标检测的部署优化没有直接关联。

Reading priority: 1分。本文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于动态图预测领域的评估方法论研究。除非对"评估协议设计"这一方法论层面有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：连续时间动态图（CTDG）中的下一目的地预测（next-destination prediction）。应用场景：动态社交网络/交互序列预测（LastFM、MOOC、Reddit、Wikipedia数据集）。传感器类型：不适用（非遥感传感器）。目标类别：不适用（预测的是图节点/目的地，而非遥感目标）。

Model architecture: 摘要未说明具体模型架构细节，仅提及使用了"six models"（六个模型）进行实验，以及一个基于pair-history membership的最小评分器（minimal scorer）。摘要未说明具体网络结构。

Lightweight method: 不涉及。摘要未提及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。本文不涉及星上或边缘部署。

Datasets and metrics: 数据集：LastFM、MOOC、Reddit、Wikipedia（均为动态图/时序交互数据集）。评价指标：Uniform-20（均匀采样20个负样本的评估指标）、全目录评估（full catalog / all-entity ranking）。实验设置：对重复正样本和新增正样本、已见负样本和未见负样本进行因子化评估（factorial evaluation）。

Experiments: 主要实验发现：(1) 非均匀负分布会改变贝叶斯最优排序；(2) 即使均匀采样的有限候选集也可能使模型排名不稳定；(3) 在四个数据集中的三个上，至少有一对模型在Uniform-20指标与全目录评估之间的相对顺序发生变化；(4) 同一模块的测量效果会随候选集大小和训练目标的变化而在幅度和方向上发生改变。结论：基于采样负样本的基准评估结论是有条件的，建议在具有可枚举固定目录的CTDG基准上使用all-entity ranking作为架构比较的主要证据。

Limitations: 摘要未说明局限性。但可指出：本文仅适用于"目的地可枚举的固定目录"场景，对于开放集或大规模不可枚举目录的场景，all-entity ranking可能不可行。此外，摘要未说明六个模型的具体名称、计算开销、以及该方法对训练过程（而非仅评估过程）的影响。

Ideas for my research: 尽管本文与遥感在轨目标检测轻量化方向基本无关，但其核心方法论思想——"评估协议的选择会显著影响模型排名和模块消融结论"——对遥感领域有间接借鉴意义：(1) 在遥感目标检测模型轻量化研究中，评估指标（如mAP@0.5 vs mAP@0.5:0.95、不同IoU阈值、不同尺度目标的分组评估）的选择可能影响轻量化方法（如剪枝、蒸馏）的优劣判断，应谨慎设计评估协议；(2) 类似地，在遥感小目标检测中，负样本采样策略（如难例挖掘）可能影响模型对比的公平性，值得注意；(3) 可借鉴其"全目录评估"思想，在遥感检测中采用全量测试集而非采样子集进行评估，以确保轻量化方法对比的可靠性。但这些仅为方法论层面的间接启发。

Abstract: Next-destination prediction in continuous-time dynamic graphs (CTDGs) commonly ranks an observed interaction against sampled negative destinations. The resulting score is conditional on both the negative distribution and the number of candidates chosen by the researcher. We show that a non-uniform negative distribution changes the Bayes-optimal ranking, while even a finite candidate set drawn uniformly can destabilize model rankings and measured module effects.
  Time-varying source-destination history membership and model operations that use this information directly transmit the sampler's influence to the evaluation score. We examine this mechanism using a factorial evaluation of repeated and new positives against seen and unseen negatives, a minimal scorer based solely on pair-history membership, and controlled representation interventions. Across six models on LastFM, MOOC, Reddit, and Wikipedia, at least one model pair changes relative order between the expected Uniform-20 metric and the full catalog on three of the four datasets. The measured effect of the same module also changes in magnitude and direction with the candidate-set size and training objective.
  These results establish that model-superiority and ablation conclusions from sampled-negative benchmarks are conditional on the stated candidate configuration. All-entity ranking evaluates every destination in a fixed catalog, eliminating negative-selection freedom and sampling variation while retaining the original CTDG scorer. We therefore recommend all-entity ranking as the primary evidence for architecture comparisons on CTDG benchmarks with an enumerable, fixed destination catalog.

</details>


### [184] [Orca: Neural Operators for Causal Reasoning in Continuous Time](https://arxiv.org/abs/2607.27867)
*Gerrit Großmann,David A. Selby,Sebastian J. Vollmer*

Main category: cs.AI

TL;DR: 提出Orca框架，将结构因果模型扩展到连续时间动态系统，用神经算子学习（neural operator learning）建模随时间演化的因果机制，支持干预和反事实推理。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于因果推理（causal reasoning）和神经算子学习（neural operator learning），属于机器学习/因果推断领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。论文主题为因果推理与神经算子学习，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：连续时间动态系统的因果推理与反事实分析。应用场景：患者健康、气候、经济等动态系统。传感器类型：不适用。目标类别：不适用。

Model architecture: Orca框架：将因果图中每个节点建模为时间的函数，每个机制（mechanism）是函数空间之间的学习映射（learned map between function spaces）。扩展了现有neural operator架构以表达因果机制，机制计算节点函数值时尊重时间方向（arrow of time），并将潜在外生噪声（latent exogenous noise）建模为可推断和复用的函数。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要仅提及在合成连续时间示例（synthetic continuous-time examples）上演示反事实推理。未说明具体数据集名称和评价指标。

Experiments: 摘要仅说明在合成连续时间示例上验证了反事实推理能力，未提供具体实验对比或消融结果。

Limitations: 摘要未说明局限性。但可推断：论文聚焦因果推理理论框架，未涉及视觉感知、目标检测或任何部署优化问题；且仅在合成数据上验证，缺乏真实场景实验。

Ideas for my research: 与"在轨遥感目标检测模型轻量化"课题的直接借鉴点有限。潜在间接启发：neural operator对函数空间的建模思想或许可用于遥感时序数据的因果建模，但与本课题核心（检测+轻量化）关联微弱。不建议作为主要参考。

Abstract: Structural causal models are the standard language for reasoning about interventions and counterfactuals, but they describe static variables, typically measured once, and usually forbid cyclic dependencies. Many systems we care about, such as patients, climates, and economies, instead evolve continuously in time, are observed at irregular time points, and contain feedback loops. We argue that neural operator learning provides a natural foundation for causal reasoning in this setting, and propose Orca, a framework in which each node of the causal graph is a function of time and each mechanism is a learned map between function spaces. We extend existing neural operator architectures to express causal mechanisms: a mechanism computes the function value of a node from its parent nodes by taking several parent functions as input, respects the arrow of time, and treats latent exogenous noise as a function that can be inferred and reused for counterfactuals. We formalize the model class and demonstrate counterfactual reasoning on synthetic continuous-time examples. Code is available at https://github.com/gerritgr/orca

</details>


### [185] [Search as Computation Allocation](https://arxiv.org/abs/2607.27871)
*Alexander Tuisov*

Main category: cs.AI

TL;DR: 本文形式化定义了"终端计算分配问题"（terminal computation-allocation problems），用Bellman方程刻画在固定预算、定价计算和精确认证下计算资源的最优分配，并建立了计算价值（VOC）与信息论（互信息、知识梯度）之间的理论联系，最终在特定误差模型下推导出加权A*搜索算法。

Research relevance: 与本研究方向弱相关（2分）。该论文是泛机器学习/决策理论层面的理论工作，讨论的是"计算资源如何在决策前分配"这一通用问题，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感场景，也不涉及object detection、小目标检测、旋转框检测等具体检测任务，更不涉及pruning、quantization、knowledge distillation、NAS等模型轻量化方法。其核心是计算分配与决策损失的理论框架，属于泛机器学习/泛决策理论方法。

Reading priority: 1分。该论文是决策理论/计算分配的理论工作，与遥感卫星在轨目标检测及模型轻量化方向基本无关。既无遥感场景、无目标检测任务、也无工程化轻量化方法，仅存在非常间接的理论思想借鉴价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：计算资源分配（computation allocation）的决策理论问题。应用场景：通用决策问题（bandit pulls、tree simulations、node expansions等），非遥感场景。传感器类型：摘要未说明。目标类别：摘要未说明。

Model architecture: 摘要未说明具体神经网络或模型结构。核心是Bellman方程刻画的最优计算分配框架，涉及value of computation (VOC)、mutual information、knowledge-gradient等理论量，并在特定误差模型下推导出weighted A*搜索算法（A*和greedy best-first search为其极限情形）。

Lightweight method: 不涉及传统意义上的模型轻量化方法（pruning、quantization、knowledge distillation、NAS等均未提及）。但该论文从"计算资源分配"角度提供了理论视角——即在固定计算预算下如何最优地分配计算以最小化终端决策损失，这一思想与轻量化中"在有限资源下最大化性能"的目标在哲学层面有相通之处，但并非工程化的轻量化方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关指标。该论文是纯理论工作，未讨论任何硬件平台（FPGA/NPU/TensorRT）或星上部署场景。

Datasets and metrics: 摘要未说明。未提及任何数据集（如DOTA、DIOR、NWPU VHR-10等）或评价指标（如mAP、FPS等）。

Experiments: 摘要未说明。未提及对比实验、消融实验或任何实证结果。论文以理论推导为主。

Limitations: 1. 纯理论工作，缺乏实验验证和实际应用场景；2. 不涉及遥感图像、目标检测或任何视觉任务；3. 未讨论计算分配理论如何映射到具体的深度模型压缩或加速工程实践；4. 摘要明确承认"不断言任何单一获取规则是普遍最优的"，说明理论框架的普适性有限；5. 与在轨部署的硬件约束（功耗、算力、存储）完全脱节。

Ideas for my research: 尽管与遥感在轨目标检测轻量化直接相关性弱，但存在以下可借鉴的理论视角：1) "固定计算预算下最优分配计算"的思想可类比于在轨场景中固定算力/功耗预算下如何分配检测网络各层或各阶段的计算资源；2) value of computation (VOC) 与互信息、knowledge-gradient的联系，可启发设计"计算价值感知"的轻量化策略——即优先压缩对最终检测损失贡献小的计算路径；3) 该框架对"计算-精度"权衡的理论刻画，可作为轻量化方法（如剪枝、量化）理论分析的工具。但这些均为间接借鉴，需要自行桥接至遥感目标检测的具体场景。

Abstract: Many algorithms spend an internal resource before returning a decision and are evaluated only by the quality of that terminal output. We formalize such procedures as terminal computation-allocation problems: costly computations produce observations, update beliefs about a latent environment, and matter only through terminal decision loss. Bellman equations characterize optimal allocation under fixed budgets, priced computation, and exact certification. We then relate value of computation (VOC) to information. Mutual information equals myopic VOC under log loss, whereas under simple regret VOC is a knowledge-gradient quantity; moreover, information gain can rank computations arbitrarily poorly, although it gives a one-sided upper bound on VOC. Bandit pulls, tree simulations, and node expansions illustrate the same model under different computation topologies. Finally, under an explicit frontier-resolution and heuristic-error model, maximizing approximate VOC recovers weighted A*, with A* and greedy best-first search as limiting cases. The theory identifies a shared decision problem without asserting that one acquisition rule is universally optimal.

</details>


### [186] [An Empirical Study of Coordination Mode as the First-Class Citizen in From-Scratch Multi-Agent Coding](https://arxiv.org/abs/2607.27877)
*Yanyu Ren,Yunfeng Bai,Xizheng Wang,Li Chen,Dan Li*

Main category: cs.AI

TL;DR: 提出MSEval基准，用于在真实全栈项目上评估多智能体编程协作的性能-速度-成本权衡，并配套LegoGent执行引擎和TAgent自动评分器。

Research relevance: 弱相关（1-2分）。该论文聚焦多智能体软件编程（multi-agent coding）基准测试，与遥感、卫星、目标检测、模型轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。仅从"多智能体协作拓扑影响性能-速度-成本权衡"这一方法论层面，可能对分布式在轨计算或多星协同推理有间接启发，但关联非常薄弱。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于多智能体软件工程领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多智能体软件编程（multi-agent coding）评估；应用场景：真实世界全栈软件开发；传感器类型：不适用（非遥感传感器）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构。涉及三个组件：MSEval（评估基准）、LegoGent（执行引擎，测试10种协作拓扑，通过周期性同步间隔协调，经原生CI/CD流水线部署）、TAgent（自动评分器，动态探测实现以联合衡量功能成功率、延迟和prefix-cached token成本）。

Lightweight method: 不涉及。论文未涉及剪枝、量化、知识蒸馏、NAS或轻量网络设计等模型压缩方法。仅提及"prefix-cached token cost"作为成本度量，属于推理成本评估而非模型轻量化。

Onboard deployability: 不适用。论文面向软件工程中的多智能体协作开发，与星上/边缘部署无关。摘要未说明参数量、FLOPs、显存、能耗等部署指标。

Datasets and metrics: 数据集：10个真实全栈项目，覆盖10个领域（authentic, full-stack projects across 10 domains）。评价指标：基于分层需求（hierarchical requirements）和确定性评分规则（deterministic rubrics）打分；TAgent联合测量功能成功率（functional success）、延迟（latency）和prefix-cached token成本。实验规模：100次运行（100 runs）。

Experiments: 主要发现：组织拓扑（organizational topology）对速度-成本-质量权衡的影响可与模型能力相媲美。相同任务和模型下，改变拓扑可使分数波动超过30分，墙钟时间翻倍。结构化流水线（structured pipelines）收敛最快且质量最高，而过重的管理监督（heavy managerial oversight）会降低性能。

Limitations: 摘要未说明局限性。但可推断：1）该基准面向软件工程而非遥感/视觉任务，与本研究方向无直接可迁移性；2）评估依赖真实项目，可能缺乏可复现的标准化遥感数据；3）未涉及任何视觉模型或遥感图像处理。

Ideas for my research: 尽管弱相关，仍有两点间接启发：1）"多智能体协作拓扑影响性能-成本权衡"的思想可类比到多星协同在轨推理或多节点分布式目标检测的调度策略设计——不同协作拓扑（如星间链路同步 vs 地面集中处理）可能显著影响检测精度与通信/计算成本权衡；2）"prefix-cached token cost"作为成本度量思路，可启发在轨推理中考虑缓存复用带来的能耗节省评估。但整体借鉴价值有限。

Abstract: Multi-agent vibe coding promises to accelerate software development, yet existing benchmarks rely on synthetic environments that ignore practical time and monetary costs, conflate reasoning with communication, and reward only superficial completion. We introduce multi-agent from-scratch evaluation benchmark, MSEval, evaluating multi-agent coding on real-world tasks. Grounded in 10 authentic, full-stack projects across 10 domains, MSEval scores performance using hierarchical requirements and deterministic rubrics.
  Its execution engine, LegoGent, tests 10 collaboration topologies where agents coordinate via periodic sync intervals and deploy through native CI/CD pipelines. Concurrently, the automated grader TAgent dynamically probes implementations to jointly measure functional success, latency, and prefix-cached token cost. Across 100 runs, MSEval reveals that organizational topology rivals model capability in shaping the speed--cost--quality trade-off. For identical tasks and models, varying the topology shifts scores by over 30 points and doubles wall-clock time. Structured pipelines converge fastest with the highest quality, whereas heavy managerial oversight degrades performance. Ultimately, MSEval establishes a rigorous, reproducible standard for measuring how multi-agent teams actually build software. The benchmark is released at https://github.com/robinren03/MSEval.

</details>


### [187] [Not All Tokens Deserve Equal Credit: Counterfactual Sensitivity Credit Reallocation for Long-CoT Reasoning](https://arxiv.org/abs/2607.27888)
*Qiangqiang He,Zhongheng Wu,ZiJian Wang*

Main category: cs.AI

TL;DR: 本文提出Counterfactual Sensitivity Credit Reallocation (CSCR)，一种改进GRPO的强化学习信用分配方法，用于提升大语言模型长链推理能力。

Research relevance: 1分，与本研究方向基本无关。论文聚焦于大语言模型（LLM）的强化学习与长链推理（long-CoT reasoning），完全不涉及遥感、卫星、目标检测或模型轻量化/部署。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或边缘部署技术。

Reading priority: 1分。论文主题为LLM强化学习与长链推理，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型强化学习中的token级信用分配优化；应用场景：数学推理benchmark上的长链推理（long-CoT reasoning）；传感器类型：不适用；目标类别：不适用。

Model architecture: 基于GRPO（Group Relative Policy Optimization）的强化学习框架，引入privileged self-teacher进行on-policy self-distillation (OPSD)，并在此基础上提出CSCR扩展模块，通过反事实敏感性分析对token级advantage进行重新归一化分配。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏（虽提及self-distillation但属于RL训练机制而非模型压缩）、NAS或任何轻量化/加速方法。

Onboard deployability: 摘要未说明。本文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：long-CoT数学推理benchmarks（具体名称摘要未说明）；评价指标：推理准确率（具体指标名称摘要未说明）；对比方法：GRPO baseline。

Experiments: CSCR在相同policy update次数下持续优于GRPO baseline；消融实验验证了三个诊断结论：privilege-induced directions不可靠、中等程度的downweighting最有效、过强调制会破坏优化稳定性。

Limitations: 摘要未说明具体局限性。但可推断：本文方法针对LLM数学推理场景设计，其token级信用分配机制与遥感目标检测的模型轻量化问题无直接关联；反事实敏感性分析的计算开销在遥感在轨场景下可能不可行。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：论文中"对token级贡献进行重新加权/信用再分配"的思想，在概念层面可类比于知识蒸馏或剪枝中对不同通道/层贡献度的差异化处理，但方法论上差异巨大，不建议直接迁移。

Abstract: Reinforcement learning with verifiable rewards (RLVR) is central to improving long-CoT reasoning in large language models. Critic-free methods such as GRPO convert response-level rewards into advantages and uniformly broadcast them across tokens, overlooking their unequal contributions to the final outcome. On-policy self-distillation (OPSD) instead provides dense distributional supervision by minimizing the forward KL divergence between an unprivileged policy and a privileged self-teacher, implicitly assuming that the resulting likelihood shifts encode reliable answer-aligned information. We test this premise by fixing each sampled trajectory and re-scoring it under two opposing outcome conditions, one asserting correctness and the other incorrectness. Most affected tokens shift in the same direction under both conditions, with few sign reversals and substantial overlap in the induced optimization signals. Large shifts also concentrate on highly substitutable surface-form tokens, whereas tokens carrying problem-specific reasoning content are less sensitive. These findings show that privileged shifts fail to provide reliable answer-aligned directions, while their magnitudes primarily reflect counterfactual sensitivity rather than token-level learning value. Based on these observations, we propose Counterfactual Sensitivity Credit Reallocation (CSCR), a simple extension of GRPO that reduces credit for highly sensitive tokens and renormalizes token-level advantages to preserve both the original credit budget and verifier-determined direction. On long-CoT mathematical reasoning benchmarks, CSCR consistently outperforms GRPO baseline with the same number of policy updates. Targeted ablations further corroborate our diagnosis: privilege-induced directions are unreliable, moderate downweighting is most effective, and stronger modulation destabilizes optimization.

</details>


### [188] [Dynamic Spectral Filtering for Temporal Graph Learning: Learning Evolving Propagation Operators](https://arxiv.org/abs/2607.27891)
*Yan Kong*

Main category: cs.AI

TL;DR: 提出动态谱滤波（DSF），将图传播机制本身建模为随时间演化的Chebyshev多项式滤波器，在时序链接预测任务上以极少的参数量和GPU显存取得与基线相当的性能。

Research relevance: 弱相关（2分）。论文属于图神经网络/时序图学习领域，面向的是时序链接预测（temporal link prediction），与遥感、卫星、目标检测、轻量化部署均无直接关系。虽然论文强调计算效率（参数量、GPU显存、训练时间），但其轻量化思路是针对图传播机制本身，而非CNN/Transformer类目标检测模型，且场景完全不是遥感。因此属于泛机器学习方法，弱相关。

Reading priority: 1分。论文面向时序图链接预测，与遥感卫星在轨目标检测及轻量化方向基本无关。虽然其参数高效和显存优化的思路有一定启发性，但方法针对图结构数据，难以直接迁移到遥感目标检测的CNN/Transformer架构中，阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务：时序链接预测（temporal link prediction）。应用场景：社交网络/知识图谱/交互网络（MOOC、Wikipedia、Reddit）。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: DSF（Dynamic Spectral Filtering）：将每个时间快照t的图传播表示为Chebyshev多项式滤波器，其系数为向量值且随时间变化。这些紧凑的多阶系数被显式视为循环时序状态（recurrent temporal states），由循环分支（recurrent branch）提出更新，并通过全局门控和阶数特定门控（multiplicative global and order-specific gates）调节更新幅度。时序状态与节点数量无关。

Lightweight method: 核心轻量化思想：将图传播机制本身参数化为一组紧凑的、与节点数无关的Chebyshev多项式系数（93K-133K参数），避免了对大规模节点状态或交互历史的显式编码。通过门控机制控制状态更新，显著降低参数量和显存占用。这属于"参数高效建模"思路，并非传统剪枝/量化/蒸馏/NAS等模型压缩方法。

Onboard deployability: 摘要未说明星上/边缘部署相关内容。但提供了计算效率数据：参数量93K-133K，峰值GPU显存68-182MB，每epoch训练时间1.6-2.1秒。相比DEFT基线，参数量减少8.3-8.6倍，GPU显存减少25-33倍，每epoch时间减少5-19倍。相比所有对比方法，GPU显存减少3.3-38.6倍。这些数据表明模型本身非常轻量，但未涉及推理延迟、FLOPs、能耗、FPGA/NPU/TensorRT等部署指标。

Datasets and metrics: 数据集：MOOC、Wikipedia、Reddit三个时序链接预测基准。评价指标：AP（Average Precision）。实验设置：时序链接预测标准协议。

Experiments: DSF在MOOC、Wikipedia、Reddit上AP分别达到0.7851、0.9088、0.9860。与DEFT基线对比：MOOC上更好，Reddit上差距在0.001 AP以内，Wikipedia上略低。同时参数量、显存、训练时间均大幅优于DEFT。相对所有对比方法，GPU显存节省3.3-38.6倍。

Limitations: 1. 任务场景为时序图链接预测，与遥感目标检测完全无关；2. 摘要未说明推理延迟、FLOPs、能耗等部署关键指标；3. 摘要未说明模型在目标检测任务上的适用性；4. 未涉及遥感图像、SAR、高光谱等传感器数据；5. 轻量化思路针对图传播机制，难以直接迁移到CNN/Transformer目标检测骨干网络。

Ideas for my research: 1. "将传播/变换机制本身参数化为紧凑可学习状态"的思路可借鉴——在遥感目标检测中，可考虑将特征金字塔或注意力机制中的某些固定操作参数化为紧凑的、与输入分辨率无关的可学习滤波器，降低计算开销；2. 门控机制（global and order-specific gates）控制状态更新的思想可用于轻量化检测头设计，实现自适应特征调节；3. 强调"计算效率作为一等公民"的设计理念值得借鉴，即在设计初期就将参数量和显存作为约束条件；4. 与节点数无关的紧凑状态表示思想，可类比为与图像分辨率无关的轻量特征表示，有助于在轨不同分辨率遥感图像上的统一推理。但整体借鉴价值有限，因为方法面向图结构数据而非图像。

Abstract: Temporal graph learning is commonly organized around the evolution of node states or the encoding of interaction histories. We study an underexplored, operator-centric question: should the graph propagation mechanism itself evolve over time? We introduce Dynamic Spectral Filtering (DSF), which represents propagation at snapshot t by a Chebyshev polynomial filter with vector-valued, time-dependent coefficients. DSF explicitly treats these compact multi-order coefficients as recurrent temporal states. A recurrent branch proposes updates, while multiplicative global and order-specific gates regulate their magnitude. The temporal state is independent of the number of nodes. On MOOC, Wikipedia, and Reddit temporal link-prediction benchmarks, converged DSF runs attain AP scores of 0.7851, 0.9088, and 0.9860, respectively, with 93K to 133K trainable parameters, 68 to 182 MB peak GPU memory, and 1.6 to 2.1 seconds of training per epoch. Against the closely related DEFT baseline, DSF is better on MOOC, within 0.001 AP on Reddit, and modestly lower on Wikipedia, while using 8.3 to 8.6 times fewer parameters, 25 to 33 times less GPU memory, and 5 to 19 times less time per epoch. Relative to all measured alternatives, it uses 3.3 to 38.6 times less GPU memory. These results support direct spectral-response evolution as a useful temporal inductive bias when computational efficiency is a first-class requirement.

</details>


### [189] [MMHBench: A Multi-Perspective Benchmark for Mental Health Understanding in Long-Form Videos](https://arxiv.org/abs/2607.27895)
*Jinpeng Hu,Erqiang Wang,Shan Wang,Zhuo Li,Peipei Song,Xun Yang,Meng Wang*

Main category: cs.AI

TL;DR: 提出MMHBench多模态基准，用于长视频心理健康理解评估，包含268个长视频和2184个问题，并引入多智能体问题生成框架（MAQG）来合成多视角问题，评估22个多模态大语言模型。

Research relevance: 弱相关（1-2分）。该论文聚焦于心理健康理解的多模态基准构建与多模态大语言模型评估，属于视频理解与心理状态推理领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于心理健康视频理解领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长视频心理健康理解（mental health understanding in long-form videos），包含第三人称评估（third-person assessment）和第一人称视角推理（first-person perspective-taking）两个子任务。应用场景：心理健康分析、视频多模态理解。传感器类型：视频（非遥感传感器）。目标类别：心理状态、可观察行为、人际情境（非遥感目标）。

Model architecture: 摘要未说明具体模型架构。评估对象为22个多模态大语言模型（MLLMs），包括开源和闭源模型，但未列出具体模型名称。提出Multi-Agent Question Generation (MAQG)框架，通过模拟多种社会角色合成问题，并经过多角色反馈和迭代优化。

Lightweight method: 摘要未涉及任何轻量化方法。不包含剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理、边缘部署、FPGA/NPU/TensorRT等任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。且该任务为长视频心理健康理解，与星上部署场景完全无关。

Datasets and metrics: 数据集：MMHBench，包含268个长视频和2,184个问题（605个第三人称评估问题 + 1,579个第一人称视角推理问题）。评价指标：摘要未说明具体指标名称。评估对象：22个多模态大语言模型（MLLMs）。

Experiments: 对22个代表性多模态大语言模型（涵盖开源和领先闭源模型）进行了广泛评估，结果表明长视频心理健康理解仍具高度挑战性。摘要未提供具体对比数值或消融实验细节。

Limitations: 摘要未说明局限性。但可推断：1）该基准聚焦心理健康领域，领域特定性强，难以迁移到遥感场景；2）评估对象为多模态大语言模型，计算开销巨大，与轻量化在轨部署需求背道而驰；3）未提供任何模型效率指标。

Ideas for my research: 借鉴意义有限，但可提取以下间接启发：1）MAQG多智能体问题生成框架的思路可类比用于遥感目标检测中合成多样化的训练样本或数据增强策略；2）多视角评估（第三人称/第一人称）的评测设计思路，可类比用于遥感检测模型在不同观测条件（不同分辨率、不同传感器、不同视角）下的鲁棒性评估；3）多角色反馈与迭代优化机制可类比用于遥感检测模型的迭代式训练策略。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值很低。

Abstract: Mental health understanding in long-form videos requires nuanced reasoning over observable behavior, interpersonal context, and latent psychological states. Existing benchmarks largely reduce this task to coarse-grained classification, providing limited insight into whether models truly understand psychological phenomena or rely on superficial correlations. To address this limitation, we introduce MMHBench, a comprehensive multimodal benchmark for multi-perspective mental health understanding, comprising 268 long-form videos and 2,184 carefully curated questions. MMHBench organizes the evaluation into two complementary settings: (1) third-person assessment, consisting of 605 questions that focus on the interpretation of observable behaviors and multimodal evidence, and (2) first-person perspective-taking, comprising 1,579 questions that require perspective-conditioned reasoning to identify the interpretation of the mental state supported by the available multimodal evidence. We propose a Multi-Agent Question Generation (MAQG) framework that simulates diverse social roles to synthesize questions from multiple perspectives. The generated questions are refined through multi-role feedback and iterative optimization, followed by expert-guided verification to ensure quality and validity. Extensive evaluation of 22 representative multimodal large language models (MLLMs), spanning both open-source and leading closed-source models, demonstrates that long-form video mental health understanding remains highly challenging.

</details>


### [190] [A Cross-Architecture Audit of Direction-Based Inference-Time Defences in Vision-Language Models](https://arxiv.org/abs/2607.27910)
*Xiangyu Yin,Tora Bodin,Rohan Menon,Chih-Hong Cheng*

Main category: cs.AI

TL;DR: 本文研究视觉语言模型（VLM）越狱攻击的推理时防御方法，比较五种基于残差流方向减除的防御候选在多个模型架构上的拒绝恢复与效用保持表现。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于视觉语言模型（VLM）的安全防御（jailbreak defence），属于多模态大模型安全领域，既不涉及遥感/卫星/地球观测场景，也不涉及目标检测任务，更不涉及任何模型压缩或轻量化部署方法。

Reading priority: 1分。论文主题为VLM安全防御，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：VLM越狱攻击的推理时防御（inference time defence against jailbreaks）。应用场景：多模态大模型安全。传感器类型：不适用（处理的是图像与文本输入）。目标类别：不适用。

Model architecture: 涉及四种架构家族的15个模型与层组合，包括LLaVA 1.5、Pixtral 12B、Qwen2.5 VL、Qwen2 VL 2B等VLM。防御方法基于在解码器层的残差流（residual stream）中减去一个校准方向（calibrated direction）。五种防御候选：mean image conditioning shift、CMRM style refusal direction、ShiftDC style attack specific residual、prompt instruction to ignore the image、random control。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法，也不涉及实时推理、边缘部署、星上部署等部署优化内容。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等任何部署相关指标，且其研究对象为大规模VLM，与星上/边缘部署场景无关。

Datasets and metrics: 摘要未明确说明具体数据集名称。评价指标包括：refusal recovery（拒绝恢复率）和utility preservation（效用保持），以及方向余弦对齐（cosine alignment）、sign test p值等。实验采用magnitude controlled protocol，为每个prompt匹配干预大小并配对随机控制向量。

Experiments: 在15个模型与层组合上比较五种防御候选。主要发现：没有单一候选在拒绝恢复和效用保持上同时占优；image conditioning shift在LLaVA 1.5和Pixtral 12B上领先，且是唯一在所有架构家族中效用损失保持在测量噪声底部的候选；prompt instruction在Qwen2.5 VL上领先；attack specific residual在Qwen2 VL 2B上领先。image conditioning方向在15个单元中的13个具有方向特异性，但强烈依赖架构且不可跨架构迁移。CMRM方向与image conditioning shift在所有15个单元中余弦对齐为正（均值0.35，范围0.17-0.65，为随机向量零值的15-25倍，sign test p值约3e-5）。

Limitations: 摘要未说明。但可推断：防御方向跨架构不可迁移，需针对每个语言解码器家族单独校准；论文未涉及遥感场景、目标检测任务或任何部署/轻量化内容。

Ideas for my research: 与本人课题（遥感卫星在轨目标检测模型轻量化）基本无可借鉴点。唯一可能的间接启发是：论文强调"方向/方法需针对不同架构家族单独校准"这一思路，可类比为轻量化方法（如剪枝、蒸馏）在不同检测骨干网络（如YOLO系列、Transformer检测器）上的迁移性也需要单独验证。但整体相关性极弱。

Abstract: Inference time defences against vision language model jailbreaks often subtract a calibrated direction from the residual stream at a chosen decoder layer. We compare five defence candidates across 15 model and layer cells from four architectural families under a magnitude controlled protocol that matches the intervention size for each prompt and pairs every direction with a random control of the same norm. The candidates are the mean image conditioning shift, a CMRM style refusal direction, a ShiftDC style attack specific residual, a prompt instruction to ignore the image, and a random control. No single candidate dominates on both refusal recovery and utility preservation. The image conditioning shift leads on LLaVA 1.5 and Pixtral 12B and is the only candidate whose utility loss remains at the measurement noise floor in every family. The prompt instruction leads on Qwen2.5 VL, while the attack specific residual leads on Qwen2 VL 2B. The image conditioning direction is direction specific in 13 of 15 cells, but strongly architecture specific and nontransferable across the only dimension compatible pair, LLaVA 1.5 13B and Pixtral 12B. We also connect text only and multimodal refusal geometry. The CMRM direction has positive cosine alignment with the image conditioning shift in all 15 cells, with mean 0.35, range 0.17 to 0.65, 15 to 25 times the random vector null, and a sign test p value of about 3e-5. These results show that the two recipes recover partially overlapping geometry and that direction based defences should be calibrated separately for each language decoder family.

</details>


### [191] [IFHierBench: Hierarchical Instruction Following for Large Language Models](https://arxiv.org/abs/2607.27912)
*Yuetian Mao,Chunyang Chen*

Main category: cs.AI

TL;DR: 提出IFHierBench，一个层级化指令遵循基准测试，包含600个提示词、四种约束树深度和35种不同约束，用于评估LLM在嵌套约束下的指令遵循能力。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于大语言模型（LLM）的指令遵循能力评估，属于自然语言处理/大模型领域，不涉及遥感、卫星、目标检测或模型轻量化部署。相关性评分：1分。

Reading priority: 1分。该论文属于大语言模型指令遵循评估领域，与遥感卫星在轨目标检测及模型轻量化研究方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：层级化指令遵循（hierarchical instruction-following）基准测试构建与评估。应用场景：大语言模型（LLM）在真实世界应用中的部署，特别是需要输出满足特定结构化约束的场景。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。评估对象为七个领先的专有和开源大语言模型（proprietary and open-weight models），但未列出具体模型名称或架构细节。

Lightweight method: 摘要未涉及任何模型压缩或加速方法，包括剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等均未提及。论文关注的是指令遵循能力评估，而非模型效率优化。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：IFHierBench，包含600个提示词（prompts），覆盖四种约束树深度（constraint-tree depths）和35种不同约束（constraints），每个提示词配有确定性检查器（deterministic checker）。评价指标：prompt-level accuracy（提示词级准确率）。

Experiments: 评估了七个领先的专有和开源大语言模型。主要发现：即使最强的模型在prompt-level准确率上也仅略超过50%，且准确率随约束深度增加而急剧下降。嵌套约束的可靠遵循仍是当前LLM的重大短板。

Limitations: 摘要未说明局限性。但可推断：1) 该基准仅覆盖35种约束，覆盖面有限；2) 确定性检查器可能无法完全覆盖真实世界约束的模糊性；3) 未涉及模型效率、部署成本等工程维度；4) 与遥感/视觉任务完全无关。

Ideas for my research: 该论文与"在轨遥感目标检测模型轻量化"课题基本无可借鉴点。唯一可能的间接启发是：如果未来在轨智能系统引入LLM作为任务规划或指令解析模块，层级化约束遵循能力可能对星上任务编排有参考价值，但这属于非常间接且遥远的关联。对于目标检测模型轻量化本身，该论文没有提供任何可复现的方法或思路。

Abstract: Instruction-following ability is critical for deploying large language models in real-world applications, where downstream components depend on the output satisfying specific constraints. Modern deployments increasingly handle the full task in a single LLM call, with one prompt specifying a layered output whose overall artifact, structural sections, and nested fields must each satisfy concrete constraints. Existing instruction-following benchmarks treat the constraint set as a flat list applied uniformly to the response, so they cannot scope a check to a particular section of the output. We introduce IFHierBench, a hierarchical instruction-following benchmark of 600 prompts stratified across four constraint-tree depths and 35 distinct constraints, each prompt paired with a deterministic checker that verifies satisfaction at every scope. Evaluating seven leading proprietary and open-weight models, we find that even the strongest model only marginally exceeds 50% prompt-level accuracy and that accuracy degrades sharply as constraint depth grows. Reliably following nested constraints remains a substantial gap for current LLMs, motivating future training methods that consider constraint adherence at finer granularity to achieve better instruction-following ability.

</details>


### [192] [One Anchor for All: Unified Multilingual and Multimodal Safety Alignment for LVLMs](https://arxiv.org/abs/2607.27917)
*Enyi Shi,Fei Shen,Chuancheng Shi,Linxia Zhu,Shuyi Miao,Jinhui Tang,Tat-Seng Chua*

Main category: cs.AI

TL;DR: 提出一种基于模态与语言共享安全神经元（MLS-Neurons）的跨维度安全对齐框架，通过仅更新约0.03%的参数实现多语言多模态大模型对复合恶意攻击的防御。

Research relevance: 1分，与本研究方向基本无关。该论文聚焦于大型视觉语言模型（LVLMs）的安全防御问题，涉及多语言指令与视觉信息结合的恶意攻击防护，属于AI安全与大模型对齐领域。既不涉及遥感/卫星场景，也不涉及目标检测任务，更不涉及模型轻量化或边缘/星上部署。虽然论文中提到了"仅更新约0.03%的参数"这一参数高效微调思想，但其目标是安全对齐而非计算资源受限场景下的模型压缩，与在轨遥感目标检测轻量化方向关联极弱。

Reading priority: 1分。该论文属于多模态大模型安全对齐方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不建议投入阅读时间。仅当对"神经元显著性分析驱动的参数高效更新"这一抽象方法论感兴趣时，可作为跨领域灵感参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多语言多模态大模型的安全对齐与恶意攻击防御。应用场景：通用LVLMs的安全部署，非遥感场景。传感器类型：不适用（涉及视觉与文本多模态输入）。目标类别：不适用（防御对象为有害/良性样本，非遥感目标）。

Model architecture: 提出MLS-Neurons框架：1) 通过对比有害与良性样本响应，量化神经元的功能显著性（激活强度+下游影响）；2) 在每种语言内对单模态安全神经元取交集，提取模态共享安全神经元（MS-Neurons）；3) 以英语为语义锚点，跨语言取交集得到模态与语言共享安全神经元（MLS-Neurons）；4) 仅更新这一最小共享神经元子集（约0.03%参数）完成安全对齐。

Lightweight method: 论文并非以轻量化为目标，但包含参数高效微调思想：仅更新约0.03%的共享安全神经元参数，将英语安全监督迁移到多语言多模态场景。这属于"稀疏参数更新"策略，与剪枝（pruning）有概念上的相似性（识别并只操作关键神经元子集），但目的不同——前者为安全对齐，后者为模型压缩加速。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、推理延迟、FLOPs、显存、能耗等部署指标。虽然"仅更新0.03%参数"暗示了训练阶段的计算效率，但未讨论推理阶段的模型大小或速度优化。

Datasets and metrics: 摘要未说明具体数据集名称。实验涉及"diverse multilingual and multimodal safety benchmarks"，评价指标为安全防御性能（相对SOTA方法的提升）以及通用能力保持（preserving general utility）。未给出具体benchmark名称和数值。

Experiments: 摘要仅说明"extensive experiments show that our method significantly outperforms state-of-the-art approaches across diverse multilingual and multimodal safety benchmarks while preserving general utility"。未给出具体对比方法、消融实验细节和量化结果。

Limitations: 1) 与遥感卫星在轨目标检测及轻量化方向完全无关；2) 摘要未提供具体数据集、指标数值和实验细节；3) 方法依赖"安全神经元"的可辨识性假设，其泛化性未充分论证；4) 仅更新0.03%参数的安全对齐是否在复杂真实攻击下足够鲁棒存疑；5) 未讨论推理阶段的计算开销。

Ideas for my research: 尽管整体弱相关，但有一个可借鉴的抽象思想：通过"神经元显著性分析"识别对特定任务最关键的最小参数子集，仅更新/保留这部分参数。这一思路在概念上可迁移到遥感目标检测模型的轻量化中——例如通过分析各通道/神经元对遥感小目标检测性能的贡献度，识别关键子网络进行结构化剪枝（structured pruning），在保持检测精度的同时大幅压缩模型。此外，"以某语义为锚点跨域迁移"的思想也可类比为"以通用检测知识为锚点，向遥感域迁移轻量模型"。但整体借鉴价值有限。

Abstract: As large vision-language models (LVLMs) are deployed globally, the combination of multilingual instructions and visual information makes malicious attacks more covert and sophisticated than ever before. However, existing methods isolate language and modality defenses, which, coupled with the scarcity of safety data and high fine-tuning costs, makes it difficult for models to defend against compound attacks. To address this severe challenge, we propose a neuron-level cross-dimensional safety alignment framework driven by modality- and language-shared safety neurons (MLS-Neurons). First, we identify monolingual and unimodal safety neurons by comparing responses to harmful and benign samples, quantifying functional saliency through activation strength and downstream impact. Then, by intersecting these unimodal neurons within each language, we extract modality-shared safety neurons (MS-Neurons) responsive to both visual and textual risks, bridging the safety representation gap between modalities. Furthermore, using English as a semantic anchor, we intersect MS-Neurons across languages to identify modality- and language-shared safety neurons (MLS-Neurons), serving as key defenses against compound attacks. Finally, we update only this minimal subset of shared neurons (~0.03% of parameters), transferring English-only safety supervision to multilingual and multimodal scenarios. Extensive experiments show that our method significantly outperforms state-of-the-art approaches across diverse multilingual and multimodal safety benchmarks while preserving general utility.

</details>


### [193] [Meta-Task: Turning Terminal Task Synthesis into a Terminal Task for Scalable Agent Training](https://arxiv.org/abs/2607.27929)
*Zhihong Pan,Jiyuan He,Kai Zhang,Yupeng Han,Ze Liu,Yuze Zhao,Yongcong Ye,Zhaohua Yang*

Main category: cs.AI

TL;DR: 提出 Meta-Task 框架，将终端任务合成本身建模为 Terminal-Bench 格式任务，让智能体在真实容器环境中迭代生成、执行和验证任务，以解决终端智能体训练数据稀缺问题。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于终端智能体（terminal agents）的训练数据合成，属于 LLM agent 与数据生成领域，不涉及遥感、卫星、目标检测或模型轻量化任何一方面。

Reading priority: 1 分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于 LLM 终端智能体数据合成领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：终端任务（terminal tasks）合成与数据生成；应用场景：终端智能体（terminal agents）训练；传感器类型：不适用；目标类别：不适用。

Model architecture: Meta-Task 框架：将任务合成建模为 Terminal-Bench 格式任务，智能体在真实容器环境中迭代生成、执行、验证任务；包含多阶段机制（multi-phase mechanism）动态设计任务规格，并引入可选外部材料支持增强多样性与真实性；使用 LLM-as-Judge 过滤保证训练数据质量。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS 或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：Terminal-Bench 2.0；评价指标：Avg Pass@1；实验设置：仅用 3,221 条 Meta-Task 合成轨迹微调 Qwen3-14B 和 Qwen3-32B，分别达到 22.5% 和 31.8% 的 Avg Pass@1。

Experiments: 在 Terminal-Bench 2.0 上，使用 3,221 条 Meta-Task 合成轨迹微调 Qwen3-14B 和 Qwen3-32B，分别取得 22.5% 和 31.8% 的 Avg Pass@1，以显著更少的训练数据超越同期方法。

Limitations: 摘要未说明局限性。但可推断：仅针对终端任务合成场景，与遥感/视觉/目标检测无关；依赖 LLM 作为生成与评判主体，计算开销可能较大；未涉及任何部署或轻量化考量。

Ideas for my research: 借鉴意义有限。唯一可能间接相关的思路是：其"在生成循环内部进行验证以保证数据质量"的思想，可类比到遥感目标检测训练数据的自动生成与校验（如合成遥感图像/标注的自动验证），但需自行迁移。此外，其"多阶段动态设计任务规格"的思路或可启发遥感场景下多样化训练样本的自动构造，但整体关联度很低。

Abstract: Training terminal agents at scale requires diverse, verifiable terminal tasks and high-quality interaction trajectories, yet acquiring such data remains a significant challenge. Existing synthesis methods face two key limitations: (1) weak reliability caused by the disconnect between task generation and real execution, and (2) limited diversity and scalability due to dependence on existing repositories. We propose Meta-Task, a framework that redefines terminal task synthesis as a Terminal-Bench-format task itself: an agent operates within a real container environment to iteratively generate, execute, and verify tasks, so that synthesized components are checked for internal consistency and executability within the generation loop itself. Building upon this, we decouple the target task requirements along multiple dimensions, introduce a multi-phase mechanism that dynamically designs novel task specifications before producing the actual tasks, and incorporate optional external material support to enhance diversity and realism. We additionally apply LLM-as-Judge filtering to ensure the quality of the final training data. Experiments on Terminal-Bench 2.0 show that fine-tuning on only 3,221 Meta-Task synthesized trajectories achieves 22.5% and 31.8% Avg Pass@1 for Qwen3-14B and Qwen3-32B respectively, outperforming concurrent approaches with significantly less training data.

</details>


### [194] [The Geometric Nature and a Free Proxy for Flow-Matching Uncertainty](https://arxiv.org/abs/2607.27933)
*Ziyang Rao,Yiren Zhao,Weiyu Guo,Ben Fei,Yandong Guo,Hui Xiong*

Main category: cs.AI

TL;DR: 提出一种基于 flow matching 速度场几何性质的零成本不确定性代理指标 accel，用于在线检测生成式动作模型（embodied model）的失败输出，无需额外训练或重采样。

Research relevance: 弱相关（2分）。该论文属于 embodied AI / 机器人控制领域的生成式动作模型（flow matching action head）不确定性估计，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关，也不涉及目标检测或模型轻量化。唯一可借鉴的是其"零额外计算开销的不确定性代理"思想，可能对在轨部署中模型置信度评估有间接启发，但整体与本研究方向关联度很低。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于 embodied AI 领域。虽然"零成本不确定性代理"思想有一定启发，但整体关联度极低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：embodied model 的 action head 不确定性估计与在线失败检测（online failure detection）。应用场景：实时控制/机器人部署（real-time control）。传感器类型：摘要未说明（非遥感传感器）。目标类别：动作块（action chunks），非视觉目标检测。

Model architecture: 基于 flow matching (FM) 生成式模型作为 action head。核心思想：在 velocity field 中给出 FM 不确定性的几何解释，认为不确定性表现为偏离理想 affine-isotropic contraction field 的偏差。提出 denoising acceleration (accel) 指标，通过单次前向传播（single forward pass）测量 denoising trajectory 的弯曲程度（bending）来代理不确定性。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS 等传统模型压缩方法。但 accel 指标本身具有"零成本"（cost-free）特性：无需额外模型评估（no additional model evaluations）、无需训练（no training）、无需重采样（no resampling），仅从单次前向传播中计算，计算开销极低，这本质上是一种推理阶段的轻量化不确定性估计方案。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等具体部署指标。但强调 accel 在 realistic deployment budget 下匹配或超越昂贵的 resampling- 和 training-based 基线，说明其计算开销远低于现有不确定性估计方法，适合实时/在线部署场景。不过这是针对机器人实时控制，而非星上部署。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及在 online failure detection 任务上测试，比较对象为 resampling-based 和 training-based 基线方法。具体实验设置、benchmark 均未在摘要中给出。

Experiments: 摘要未给出具体数值结果。仅定性说明 accel 能在 rollout 失败之前（well before termination）识别失败，且在不同设置下匹配或超越昂贵的基线方法。理论部分证明了 accel 是 FM 不确定性的忠实代理（faithful proxy）。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于 embodied AI 领域。2. 摘要未说明 accel 在视觉感知任务（如目标检测）上的适用性。3. 摘要未提供计算开销的量化数据（FLOPs、延迟等）。4. 摘要未说明该方法是否适用于离散动作或非生成式模型。5. 该方法针对 flow matching 生成式模型，对判别式检测模型（如 YOLO、DETR 系列）不直接适用。

Ideas for my research: 1. 借鉴"零额外计算开销的不确定性代理"思想：在星上部署的轻量检测模型中，可探索类似的不确定性估计方法，用于在轨场景下检测 OOD 输入（如云遮挡、传感器异常），避免额外训练或重采样带来的计算负担。2. 几何视角的启发：将模型输出的几何性质（如轨迹弯曲、特征分布偏移）作为置信度/不确定性信号，可能比 softmax 置信度更可靠。3. 在线失败检测思路可迁移到在轨目标检测的"检测失败预警"场景，帮助卫星自主判断何时需要重新采集或上传数据。4. 但需注意：该方法依赖 flow matching 生成式架构，迁移到判别式检测模型需要重新设计几何代理指标。

Abstract: Flow matching (FM) has become a popular action head paradigm for modern embodied models. However, as a conditional generative model, it does not explicitly expose its inherent uncertainty, producing faulty action chunks even when it misinterprets the scene or encounters out-of-distribution (OOD) inputs. Therefore, determining when an FM-generated action can be trusted is essential for safe deployment, yet existing uncertainty estimation methods on real-time control suffer from several issues: extra training budget, high computational overhead, and low generalization ability. In this work, we provide a geometric interpretation of FM uncertainty in the velocity field, showing that uncertainty manifests as deviation from an ideal affine-isotropic contraction field. Building on this observation, we introduce denoising acceleration ($\mathrm{accel}$), a highly-generalizable and cost-free uncertainty proxy that measures the bending of the denoising trajectory from a single forward pass, without additional model evaluations, training, or resampling. We theoretically and empirically demonstrate that $\mathrm{accel}$ is a faithful proxy for FM uncertainty and further test its utility in online failure detection. Results show that $\mathrm{accel}$ identifies failing rollouts well before termination, matching or even outperforming costly resampling- and training-based baselines across settings under realistic deployment budget. Code and demos available at: https://github.com/rrrrrrzy/fm-geometry.

</details>


### [195] [Shapes from Examples: Foundations of Shape Learning in Recursive SHACL](https://arxiv.org/abs/2607.27934)
*Bente Gortworst,Cem Okulmus,Magdalena Ortiz,Anni-Yasmin Turhan*

Main category: cs.AI

TL;DR: 本文研究SHACL数据图验证中的自动形状学习问题，聚焦于ELI描述逻辑核心片段的形状表达式拟合，给出了拟合存在性和最具体拟合计算的指数级上界及多项式特例。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于知识图谱/描述逻辑/图数据验证领域，不涉及遥感、卫星、目标检测或任何模型压缩/加速方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于知识图谱/描述逻辑理论领域，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：SHACL形状学习（shape learning）与图数据验证；应用场景：知识图谱（knowledge graph）应用；传感器类型：不适用；目标类别：不适用。

Model architecture: 摘要未说明具体模型结构。涉及SHACL形状表达式（shape expression C）、递归形状目录（recursive shape catalogue）、ELI描述逻辑核心片段等理论框架。

Lightweight method: 不涉及。论文讨论的是计算复杂度（拟合存在性、最具体拟合计算的时间上界），与模型剪枝、量化、知识蒸馏、NAS等轻量化方法无关。

Onboard deployability: 不适用。论文不涉及任何部署场景，未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。

Datasets and metrics: 摘要未说明具体数据集和评价指标。论文为理论性研究，关注拟合存在性（fitting existence）和最具体拟合计算（most specific fitting computation）的复杂度上界。

Experiments: 摘要未说明实验设置。论文主要贡献是理论结果：对well-founded、stable、supported三种语义，建立指数级时间上界，并在相关特例中获得多项式界。

Limitations: 摘要未说明局限性。作为纯理论论文，缺少实验验证和实际应用场景；且与遥感/目标检测/轻量化方向完全无关。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题无可借鉴点。论文属于知识图谱形式化验证领域，与遥感目标检测和模型压缩在方法论、应用场景和技术栈上均无交集。

Abstract: SHACL shapes enable data graph validation, making automatic shape learning essential for knowledge graph applications. We investigate the well-known fitting approach to this task: given sets P and N of positive and negative example nodes from an input graph, compute a shape expression C, possibly using shape names defined in a recursive shape catalogue, that validates at every node in P and none in N. We focus on the case where C is written in a core fragment of SHACL corresponding to the Description Logic ELI. For the catalogue, we consider the well-founded, stable, and supported semantics. We address fitting existence and most specific fitting computation, establish tight exponential-time upper bounds for both problems, and obtain polynomial bounds for relevant special cases.

</details>


### [196] [From Scoring to Acting: Outcome-Verified Comparative Self-Distillation for LLM Agents](https://arxiv.org/abs/2607.27937)
*Xu Xia,Jinghua Piao,Min Yang,Xiaochong Lan,Jiaju Chen,Yong Li*

Main category: cs.AI

TL;DR: 提出一种面向LLM智能体的结果验证对比自蒸馏方法（OVCSD），通过前缀树组织失败轨迹、自适应调用技能条件教师、仅保留结果验证成功的延续，并在首个状态对齐分歧处进行局部对比学习，在ALFWorld和WebShop上显著提升成功率。

Research relevance: 弱相关（2分）。该论文属于LLM agent能力内化的泛机器学习/强化学习方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可借鉴之处在于其"self-distillation"（自蒸馏）思想，与知识蒸馏（knowledge distillation）在概念上有一定交叉，但应用场景、任务类型、数据模态完全不同。

Reading priority: 1分。该论文聚焦LLM agent能力内化，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然涉及self-distillation概念，但应用场景、任务类型、数据模态均不匹配，且未提供任何部署相关指标。仅在知识蒸馏机制设计上可能有极有限的间接启发，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM agent技能内化（capability internalization），即让agent在推理时不依赖外部检索而保留有用技能。应用场景：ALFWorld（具身/文本交互环境）和WebShop（网页购物交互环境）。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。

Model architecture: 摘要未说明具体模型架构细节。涉及学生模型（student）和技能条件教师模型（skill-conditioned teacher），通过on-policy self-distillation机制进行训练。使用前缀树（prefix tree）组织失败rollout轨迹。

Lightweight method: 涉及self-distillation（自蒸馏）思想，属于知识蒸馏（knowledge distillation）范畴，但并非面向模型压缩或轻量化部署，而是面向LLM agent能力内化（减少推理时外部检索依赖）。不涉及剪枝（pruning）、量化（quantization）、NAS、实时推理或边缘部署。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗或星上/边缘部署相关信息。仅提到训练时增加不到3%的privileged interaction，但这属于训练开销而非推理部署开销。

Datasets and metrics: 数据集：ALFWorld和WebShop两个交互式LLM agent基准环境。评价指标：任务成功率（success rate），报告了相对最强baseline的绝对成功率提升（ALFWorld上最高29.7%，WebShop上最高5.4%）。实验覆盖三个模型规模（three model scales）。

Experiments: 在ALFWorld和WebShop上，跨三个模型规模，OVCSD一致优于skill-free RL和现有self-distillation baselines。ALFWorld上最高比最强baseline提升29.7个绝对成功率点，WebShop上提升5.4个绝对成功率点，训练时仅增加不到3%的privileged interaction。

Limitations: 摘要未说明局限性。但明显局限包括：1）仅验证于文本交互式agent环境（ALFWorld/WebShop），未涉及视觉感知或遥感场景；2）未报告模型压缩、推理速度、部署资源等轻量化指标；3）self-distillation的"教师-学生"框架与目标检测中的知识蒸馏在机制上差异较大，迁移到遥感目标检测需要大量适配。

Ideas for my research: 1）"outcome-verified teacher supervision"（结果验证的教师监督）思想可借鉴：在遥感目标检测的知识蒸馏中，可考虑仅用"结果验证有效"的教师预测（如高置信度、NMS后保留的检测框）作为蒸馏监督信号，避免教师错误预测干扰学生。2）"comparative learning over teacher-student trajectories"（教师-学生轨迹对比学习）可类比为：在检测蒸馏中对比教师与学生特征图的差异，在分歧处进行局部蒸馏，而非全图均匀蒸馏，可能更高效。3）"prefix tree组织失败样本"的思路可迁移到难例挖掘：将检测失败样本按场景/目标类型组织，针对性蒸馏。但整体而言，该论文与遥感在轨目标检测轻量化方向关联较弱，借鉴价值有限。

Abstract: Recent work on LLM agents is shifting from external capability elicitation to capability internalization, enabling agents to retain useful skills without retrieval at inference time. On-policy self-distillation (OPSD) offers a promising direction, but many existing methods typically supervise students by scoring actions along student-generated trajectories. Such supervision has two limitations: teacher preferences are not validated by environment outcomes, and action-level scores underuse information from student rollouts, teacher rollouts, and their behavioral relationship. We therefore advocate outcome-verified teacher supervision and comparative learning over teacher-student trajectories. Based on this view, we propose Outcome-Verified Comparative Self-Distillation (OVCSD). OVCSD organizes failed student rollouts into a prefix tree, adaptively invokes a skill-conditioned teacher from student-reached states, and retains only outcome-verified successful continuations. It then applies localized comparative learning at the first state-aligned divergence and distills the post-divergence teacher suffix to transfer completion behavior. Experiments on ALFWorld and WebShop across three model scales show that OVCSD consistently outperforms skill-free RL and existing self-distillation baselines, achieving up to 29.7 and 5.4 absolute success-rate gains over the strongest baselines on ALFWorld and WebShop, respectively, while adding less than 3% privileged interaction during training.

</details>


### [197] [MARS-RA: Rank Aggregation for Credit Assignment via Multimodal Comparisons in Embodied Multi-Agent Cooperation](https://arxiv.org/abs/2607.27967)
*Dawei Wang,Di Zhao,Xinyuan Liu,Marci Chi Ma,Xiaoyang Liu,Chengming Zhou,Gary Ushaw,Richard Davison*

Main category: cs.AI

TL;DR: 提出MARS-RA框架，将多智能体强化学习中的信用分配问题重构为基于大多模态模型生成贡献比较的排名聚合问题，用于潜在奖励塑形。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦多智能体强化学习（multi-agent reinforcement learning）中的信用分配（credit assignment）问题，属于embodied AI和合作式强化学习范畴，不涉及遥感、卫星、目标检测或模型压缩/轻量化任何一方面。

Reading priority: 1分。论文主题为多智能体强化学习的信用分配，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无阅读价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多智能体合作强化学习中的信用分配；应用场景：embodied AI环境；传感器类型：摘要未说明；目标类别：不适用（非检测任务）。

Model architecture: MARS-RA框架：将信用分配重构为排名聚合（rank aggregation）问题，利用大多模态模型（large multimodal models）生成基于贡献的智能体两两比较（pairwise comparisons），将比较结果转化为贡献分数用于基于势能的奖励塑形（potential-based reward shaping）。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集名称。实验在"challenging tasks of different types"上进行，评价指标为合作有效性（effective cooperation），具体指标未说明。

Experiments: 在多种不同类型的挑战性任务上进行了实验，结果表明MARS-RA能引导智能体实现有效合作。提供了框架收敛性和鲁棒性的理论证明，并以Shapley值作为可解释性参考。

Limitations: 摘要未说明局限性。此外，该论文与遥感目标检测和轻量化方向完全无关，缺乏任何可迁移的遥感场景信息、检测任务设定或部署约束考量。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：其"从绝对估计转向相对比较（relative vs absolute estimation）"的思想在鲁棒性设计上具有一定通用性，但与本课题（遥感在轨目标检测轻量化）几乎没有直接关联。不建议投入时间深入阅读。

Abstract: Credit assignment is a fundamental challenge in cooperative multi-agent reinforcement learning, particularly in embodied AI settings characterized by limited and delayed feedback as well as dynamically changing numbers of active agents. We propose MARS-RA, a framework that reformulates credit assignment as a rank aggregation problem using contribution-based pairwise comparisons among agents generated by large multimodal models. This shift from absolute to relative estimation ensures robustness against noise and dynamic agent participation, converting comparison results into contribution scores for potential-based reward shaping. We provide theoretical justification for the convergence and robustness of the proposed framework, and show that Shapley values can be used as an interpretive reference. Experimental results on challenging tasks of different types indicate that MARS-RA can guide agents toward effective cooperation.

</details>


### [198] [Share the Judge, Learn the Deferral: Where Specialization Helps LLM Evaluation](https://arxiv.org/abs/2607.27984)
*Weining Zhang*

Main category: cs.AI

TL;DR: 本文研究智能体评估系统（evaluator）的架构设计问题：领域专业化应内置于评估器权重中，还是内置于决定何时信任其判断的规则中，通过LoRA专家、级联路由等实验得出"共享判断学习、在审计发布边界放置领域适配"的设计规则。

Research relevance: 与本研究方向基本无关（1分）。论文研究的是LLM/智能体系统的评估器（evaluator）架构设计，涉及LoRA适配器、级联路由、RewardBench基准等，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。虽然涉及模型压缩相关概念（LoRA、参数效率、级联计算），但这是针对语言模型评估任务的参数效率研究，与遥感目标检测的轻量化（剪枝、量化、蒸馏、NAS、边缘部署）在任务、场景、方法上均无直接关联。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于LLM评估器架构研究。仅"级联路由降低平均计算量"和"共享-拆分"设计原则有极弱的概念借鉴意义，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：智能体系统评估器（evaluator）的架构设计，判断领域专业化应放在权重中还是信任规则中。应用场景：LLM评估、RewardBench基准测试。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 涉及LoRA（Low-Rank Adaptation）适配器、criterion-family LoRA judges（按标准族划分的LoRA评估器）、rank-64 adapter、共享预训练judge初始化的family adapters、learned correctness heads路由的0.6B-4B-8B级联（cascade）架构。核心是评估器权重与信任规则（deferral rule）的架构分工。

Lightweight method: 涉及LoRA（低秩适配）作为参数高效微调手段，以及通过级联路由（0.6B-4B-8B cascade）在保持精度的同时降低参数计算量（normalized parameter compute 0.415）。但这些是面向LLM评估任务的参数效率手段，并非面向遥感目标检测的剪枝、量化、知识蒸馏、NAS或边缘/星上部署方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、FPGA/NPU/TensorRT、延迟、显存、能耗或实时推理等部署相关信息。仅提及normalized parameter compute（0.415）作为计算效率指标，但这是针对LLM推理级联的抽象度量，与遥感在轨部署无关。

Datasets and metrics: 数据集：99,952个公开的rubric-conditioned示例；RewardBench 2基准。指标：locked-test accuracy、audited coverage（5%风险目标）、normalized parameter compute、one-sided 95% risk audit。这些均为LLM评估任务指标，与遥感目标检测的mAP、FPS、FLOPs、参数量等指标无关。

Experiments: 主要实验：1) 提供正确rubric比response-only控制提升2.11点，无关rubric损失2.66点；2) 将训练语料分为8个criterion-family LoRA judges损失10.05点，audited coverage从24.44%降至5.43%；3) 用rank-64 adapter匹配存储容量不重现该损失；4) 从共享trained judge初始化family adapters恢复至76.85%，比同学习率scratch训练高19.94点；5) RewardBench 2上learned correctness heads路由0.6B-4B-8B级联达89.40%准确率，优于8B单独的84.75%，参数计算量0.415。这些实验与遥感目标检测无关。

Limitations: 摘要未说明局限性。但就本研究方向而言，该论文完全不涉及遥感场景、目标检测任务或面向边缘/星上的模型压缩部署，其LoRA和级联路由方法虽属参数效率范畴，但面向的是LLM评估器而非视觉检测模型，方法迁移到遥感目标检测轻量化的可行性极低且摘要未提供任何相关证据。

Ideas for my research: 借鉴价值有限，但有两个概念层面可参考：1) "共享学习判断直到有足够数据再拆分"的设计原则，可类比思考遥感目标检测中"共享backbone特征提取、按场景/目标类别做轻量适配"的架构思路；2) 级联路由（cascade routing）思想——用小模型快速处理简单样本、大模型处理难样本，这一"early exit/级联推理"思路在遥感在轨检测中可借鉴为"简单场景快速推理、复杂场景深度推理"的自适应计算策略，以降低星上平均计算开销。但需注意这些仅是概念启发，论文本身不提供任何遥感或视觉检测的实证。

Abstract: Agentic systems have widened the gap between producing candidate outputs and reviewing them. This paper asks a practical architectural question: should domain specialization be built into an evaluator's weights, or into the rule that decides when its judgment can be trusted? We study 99,952 public, rubric-conditioned examples. Supplying the correct rubric improves locked-test accuracy by 2.11 points over a response-only control; replacing it with an unrelated rubric costs 2.66 points. Dividing the same training corpus among eight criterion-family LoRA judges, however, loses 10.05 points and cuts audited coverage at a 5% risk target from 24.44% to 5.43%. Matching the bank's stored capacity with one rank-64 adapter does not reproduce this loss. Nor is the result explained by learning rate or optimizer steps. Initializing the family adapters from a shared, trained judge recovers test accuracy to 76.85%, 19.94 points above scratch training at the same learning rate (95% interval 18.88-21.02). The result changes when specialization governs deferral rather than judgment. On RewardBench 2, learned correctness heads route examples through a 0.6B-4B-8B cascade without changing any reward score. Across 20 locked repartitions, the cascade attains 89.40% accuracy, compared with 84.75% for 8B alone, at 0.415 normalized parameter compute. Every run passes an exact one-sided 95% risk audit; margin-based rules remain near 84.8% accuracy while using at least 0.94 compute. These results suggest a qualified design rule: share the learning of judgment until there is enough data to justify a split, and place domain-specific adaptation in an audited release boundary.

</details>


### [199] [SKIMIX: Multi-Agent Harness-Time Scaling with Skill Mixture for Dynamic Harness Engineering](https://arxiv.org/abs/2607.27994)
*Jia Luo*

Main category: cs.AI

TL;DR: 提出SKIMIX多智能体框架，通过嵌入检索、子模路由和自适应技能演化实现技能库的协同组合，在数学推理任务上提升明显但在选择题上收益有限。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于AI agent的多智能体技能协作框架，属于大语言模型/智能体领域，不涉及遥感、卫星、目标检测或模型压缩/部署等任何相关主题。相关性评分：1分。

Reading priority: 1分。论文主题为AI agent技能协作，与遥感卫星在轨目标检测及轻量化方向完全无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多智能体协作推理（multi-agent collaboration）；应用场景：AI agent技能库管理与组合；传感器类型：不适用；目标类别：不适用。摘要未涉及任何遥感或视觉任务。

Model architecture: SKIMIX框架包含三个核心组件：embedding-based skill retrieval（基于嵌入的技能检索）、submodular anti-dilution routing（子模抗稀释路由）、adaptive skill evolution（自适应技能演化）。属于多智能体系统设计，非视觉模型架构。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 使用六个推理基准（six reasoning benchmarks），包括开放式数学推理（open-ended mathematical reasoning）和多选题（multiple-choice tasks）。具体数据集名称和评价指标摘要未说明。

Experiments: 主要发现：多智能体协作在开放式数学推理上显著提升，但在多选题任务上收益有限甚至为负；agent数量扩展呈非单调性；大部分提升出现在第一轮refinement中。

Limitations: 论文聚焦于agent技能协作，与遥感目标检测和模型轻量化完全无关。摘要未提供任何视觉任务、遥感数据或部署相关的信息。

Ideas for my research: 与本人课题（遥感卫星在轨目标检测模型轻量化）基本无可借鉴点。唯一可能的间接启发是"任务特性决定方法收益"这一结论——类似地，在遥感目标检测轻量化中，不同任务（如舰船检测 vs 飞机检测）对压缩策略的敏感度可能不同，但这一类比非常牵强，不建议深入参考。

Abstract: AI agents increasingly rely on large skill libraries, but selecting, combining, and maintaining skills remains difficult. We propose SKIMIX, a multi-agent framework in which agents with different skill portfolios collaborate through iterative refinement. SKIMIX combines embedding-based skill retrieval, submodular anti-dilution routing, and adaptive skill evolution. Across six reasoning benchmarks, multi-agent collaboration substantially improves open-ended mathematical reasoning but offers limited or negative gains on multiple-choice tasks. Agent-count scaling is non-monotonic, and most improvements arise during the first refinement round. These results show that task characteristics determine whether skill-level ensembles help and provide practical guidance for scalable agent design.

</details>


### [200] [MMLDSum-LLM: Multimodal Long-Document Summarization with Visual-Alignment and Keyword-Aware](https://arxiv.org/abs/2607.28006)
*Xianpeng Zhang,Jiahua Yang,Dongyu Chen,Lei zhang,Jian Ma,Xu guohuan,Haonan Lu,Tianhuang Su,Chuangchuang Wang,Kai Tang*

Main category: cs.AI

TL;DR: 提出多模态长文档摘要基准MMLDSum-Bench和两阶段训练框架MMLDSum-LLM，通过视觉对齐加权损失、关键词加权损失和GRPO多目标奖励来提升多模态长文档摘要的关键信息覆盖率和跨模态一致性。

Research relevance: 弱相关（2分）。该论文聚焦多模态长文档摘要（multimodal long-document summarization），属于自然语言处理与多模态理解领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关联。论文不涉及object detection、remote sensing、轻量化（pruning/quantization/distillation/NAS）或边缘/星上部署。唯一可能的间接借鉴点在于其跨模态对齐（inter-modal alignment）和注意力漂移（attention drift）问题处理思路，或可类比遥感多模态数据（如光学+SAR）的特征融合，但关联非常间接。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于多模态NLP摘要任务，不涉及遥感、目标检测或任何轻量化/部署技术。除非对多模态对齐或长程依赖建模有跨领域兴趣，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态长文档摘要（multimodal long-document summarization）。应用场景：专业文档知识摘要，非遥感场景。传感器/数据模态：文本+图像（visual-textual modality），非遥感传感器。目标类别：不涉及目标检测。

Model architecture: MMLDSum-LLM采用两阶段训练框架：第一阶段为supervised fine-tuning（SFT），结合visual-alignment weighted loss和keyword-aware weighted loss；第二阶段为GRPO（Group Relative Policy Optimization）强化学习，使用多目标奖励函数（keyword coverage、image-text alignment、ROUGE、length control）。底层为多模态大语言模型（multimodal LLM），具体骨干网络摘要未说明。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法，如pruning、quantization、knowledge distillation、NAS、轻量网络设计、实时推理或边缘部署。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其面向的是多模态LLM摘要任务，与星上/边缘部署场景无关。

Datasets and metrics: 数据集：MMLDSum-Bench（自建多模态长文档摘要基准），覆盖多个领域、多种上下文长度尺度和视觉-文本模态分布。评价指标：LLM-as-a-judge评分、atomic-claim precision/recall、image-text alignment（ITA）、ROUGE。对比对象：领先的闭源和开源多模态模型，在统一评估协议下比较。

Experiments: 在MMLDSum-Bench上进行了广泛实验，对比多个闭源和开源多模态模型，结果显示所提方法显著提升了关键信息覆盖率和跨模态一致性。具体数值结果摘要未说明。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于NLP多模态摘要任务。2. 摘要未说明模型规模、推理效率、部署可行性。3. 未涉及任何轻量化技术。4. 基准和方法的泛化性（是否适用于遥感多模态数据）未验证。

Ideas for my research: 尽管方向不同，仍有少量可借鉴思路：1. 其"attention drift in long-range dependency modeling"问题与遥感大图/长序列目标检测中的长程依赖建模有概念相似性，可参考其缓解注意力漂移的加权策略。2. 其"inter-modal alignment"思想可类比遥感多模态融合（如光学+SAR、多光谱+高光谱）中的跨模态对齐问题，但其具体方法（visual-alignment weighted loss）是否适用于遥感特征融合需进一步验证。3. GRPO多目标奖励的优化思路或可迁移到遥感检测模型的训练策略设计中。总体而言，借鉴价值有限。

Abstract: Multimodal long documents are core carriers of professional knowledge, where critical evidence is sparsely distributed across paragraphs and modalities. This easily causes key information omission and cross-modal hallucinations in summarization by multimodal LLMs. These issues stem from attention drift in long-range dependency modeling and gaps in inter-modal alignment. To address this, we introduce MMLDSum-Bench, a high-quality benchmark for multimodal long-document summarization, covering multiple domains, context-length scales, and visual-textual modality distributions. We further propose MMLDSum-LLM, a reproducible two-stage training framework that combines supervised fine-tuning with visual-alignment weighted loss and keyword-aware weighted loss, followed by GRPO with a multi-objective reward (keyword coverage, image-text alignment, ROUGE, and length control). Extensive experiments on MMLDSum-Bench, comparing against leading closed-source and open-source multimodal models under a unified evaluation protocol - including LLM-as-a-judge scoring, atomic-claim precision/recall, image-text alignment (ITA), and ROUGE - demonstrate that our approach significantly improves key-information coverage and cross-modal consistency.

</details>


### [201] [MUL-T: Decoding Spatial Cellular Architecture in Multiplexed Tissue Images](https://arxiv.org/abs/2607.28030)
*Farzaneh Seyedshahi,Kai Rakovic,Adalberto Claudio Quiros,John LeQuesne,Ke Yuan*

Main category: cs.AI

TL;DR: MUL-T 是一个轻量级 transformer 框架，将组织架构建模为离散细胞 token 上的掩码上下文预测任务，在多项临床下游任务上以远少于 foundation ViT 的参数和训练成本取得相当性能。

Research relevance: 弱相关（2分）。该论文属于生物医学影像/多重成像（multiplexed imaging）领域，处理的是组织切片中的细胞表型与空间上下文建模，与遥感、卫星、Earth observation、aerial image、SAR、高光谱等遥感场景完全无关。虽然论文标题和摘要中出现了"lightweight transformer"和"computationally efficient"等轻量化相关表述，但其轻量化动机和场景是医学病理分析，而非遥感在轨目标检测。其轻量化 transformer 设计思路（如掩码上下文预测、[CLS] embedding）对遥感轻量化模型设计有一定方法学借鉴意义，但整体与本研究方向弱相关。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，属于生物医学影像领域。虽然其轻量 transformer 设计和掩码自监督思路有一定方法学借鉴价值，但场景差异大，且摘要缺乏部署相关量化指标，对本课题的直接参考价值有限。建议仅作泛读，关注其轻量 transformer 设计思路即可。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：组织架构建模（tissue architecture modelling），具体为掩码上下文预测（masked contextual prediction）；下游任务包括肿瘤模式分类（tumour pattern classification）、患者分级（patient-level grading）、PD-L1 阳性预测、跨数据集治疗响应预测。应用场景：多重成像（multiplexed imaging）病理分析。传感器类型：非遥感传感器，为生物医学多重成像。目标类别：细胞表型（cellular phenotypes）、细胞 token。与遥感目标检测（舰船/飞机/车辆/小目标）完全无关。

Model architecture: MUL-T 是一个轻量级 transformer 框架。核心设计：将组织架构重构为离散细胞 token 上的掩码上下文预测任务；学习上下文化的 [CLS] embedding，无需任务特定监督（task-specific supervision）；通过掩码预测捕获高阶细胞交互（higher-order cellular interactions）。摘要未说明具体 transformer 层数、头数、embedding 维度等网络结构细节。

Lightweight method: 论文自称 lightweight transformer，轻量化的实现方式是通过"掩码上下文预测 + 无任务特定监督的 [CLS] embedding"来减少对大规模监督数据和复杂任务头（task-specific heads）的依赖，从而降低参数量和训练成本。摘要未说明是否使用了剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或 NAS 等经典模型压缩手段。其轻量化更多体现在"设计上更省"而非"压缩已有模型"。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等具体部署指标。仅提到"substantially fewer parameters and lower training cost"（参数显著更少、训练成本更低），但未给出量化数据。与星上部署（onboard deployment）、FPGA/NPU/TensorRT、实时推理（real-time inference）等边缘部署场景完全无关。

Datasets and metrics: 摘要未说明具体数据集名称。提及了多项临床下游任务：core-level 肿瘤模式分类、patient-level 分级、PD-L1 阳性预测、跨数据集治疗响应预测。评价指标摘要未说明。对比基线包括经典特征基方法（classical feature-based baselines）和 foundation ViT 模型。

Experiments: 主要实验结论：MUL-T 在多项临床下游任务上一致优于经典特征基基线（classical feature-based baselines），并以显著更少的参数和更低的训练成本达到与 foundation ViT 相当的性能。摘要未提供具体数值结果、消融实验细节或对比表格。

Limitations: 1. 与遥感/卫星/在轨目标检测完全无关，属于生物医学影像领域。2. 摘要未提供任何量化部署指标（参数量、FLOPs、延迟等）。3. 未说明轻量化的具体实现机制（是否用蒸馏/剪枝/量化）。4. 未提供数据集规模、评价指标和具体性能数值。5. 跨数据集泛化能力仅提及"treatment response prediction"一个场景，泛化性证据有限。

Ideas for my research: 1. 掩码上下文预测（masked contextual prediction）作为一种自监督预训练策略，可借鉴到遥感目标检测中，用于在无标注遥感影像上预训练轻量检测骨干网络，减少对大规模标注数据的依赖。2. 无任务特定监督的 [CLS] embedding 设计思路，可用于遥感场景理解中构建轻量级全局上下文编码器。3. "以设计换轻量"的思路——通过更高效的任务建模方式（而非单纯压缩已有模型）来降低参数量和训练成本，对遥感在轨轻量化模型设计有启发。4. 但需注意：该论文场景（细胞 token 序列建模）与遥感图像目标检测（空间网格特征 + 检测头）差异较大，直接迁移需谨慎。

Abstract: Understanding tissue organisation in multiplexed imaging requires modelling both cellular phenotypes and their spatial context. Existing approaches typically rely on handcrafted features, such as marker intensity statistics or cell-type proportions, which often fail to scale or generalise across cohorts with heterogeneous marker panels. We introduce MUL-T, a lightweight transformer framework that reframes tissue architecture as a masked contextual prediction task over discrete cell tokens. By learning contextualised [CLS] embeddings without task-specific supervision, the model captures higher-order cellular interactions while remaining computationally efficient. We evaluate MUL-T on several clinically relevant downstream tasks, including core-level tumour pattern classification, patient-level grading, PD-L1 positivity prediction, and cross-dataset treatment response prediction. Across tasks, MUL-T consistently outperforms classical feature-based baselines and achieves performance comparable to a foundation ViT model, despite substantially fewer parameters and lower training cost.

</details>


### [202] [DataClawEval: A Benchmark for Data Engineering Agents in Real Industrial Harness](https://arxiv.org/abs/2607.28033)
*Debin Meng,Jiaming Yang,Zefang Zong,Tengyue Xu,Haining Xie,Yang Li,Peng Chen*

Main category: cs.AI

TL;DR: 提出DataClawEval，首个面向真实数据工程场景的端到端自主智能体评测基准，覆盖5种执行引擎的100个任务，用确定性脚本评测16个前沿智能体，发现最强模型仅得74.9分且无单一模型全面占优。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦LLM智能体在数据工程（data engineering）场景的端到端任务评测，属于自然语言处理与数据管理交叉领域，不涉及遥感、卫星、目标检测或模型压缩/加速。相关性评分为1分。

Reading priority: 1分。论文主题为LLM智能体数据工程评测，与遥感卫星在轨目标检测及轻量化方向无直接关联，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM-based agent的端到端数据工程任务完成能力评测。应用场景：企业级数据工程（PySpark、MySQL、HiveSQL、PrestoSQL/Trino、FlinkSQL）。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。评测对象为16个前沿LLM-based agents，但未给出其具体网络结构或设计细节。

Lightweight method: 不涉及。论文未讨论任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计方法。

Onboard deployability: 不涉及星上/边缘部署。摘要未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：DataClawEval，包含100个端到端数据工程任务，覆盖PySpark、MySQL、HiveSQL、PrestoSQL/Trino、FlinkSQL五种执行引擎，基于企业级生产代码构建。评价指标：确定性规则脚本评分（deterministic, rule-based scripts），总分制（最强模型74.9分），而非LLM-as-a-judge非确定性评分。

Experiments: 评测了16个前沿LLM-based agents。主要发现：最强模型仅达到74.9分；没有单一模型在所有引擎上全面占优，每个模型在不同引擎上各有优势，呈现严格的领域专精（domain specialization）而非全能表现。摘要未说明具体对比方法名称和消融实验。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，无任何可迁移的技术方法。2. 摘要未说明评测任务的具体难度分布、各引擎任务数量分配。3. 未讨论评测基准本身的偏差、任务覆盖完整性等局限。4. 未涉及模型效率、推理速度等部署维度。

Ideas for my research: 尽管论文与本研究方向基本无关，但可借鉴其"确定性规则脚本评测"（deterministic rule-based evaluation）的思想——在遥感目标检测模型轻量化研究中，可考虑用确定性指标（如mAP、FLOPs、延迟的精确测量）而非主观评估来量化压缩前后性能损失，确保评测的可复现性。此外，"领域专精而非全能"的发现提示：在星上轻量化模型设计中，针对特定遥感场景（如舰船检测）做专用优化可能优于追求通用检测能力。

Abstract: Large language models (LLMs) and LLM-based agents are increasingly being deployed to automate complex workflows, promising to revolutionize data management and processing. However, existing benchmarks predominantly focus on simplified Text-to-SQL translation or data analysis, leaving the critical and complex domain of end-to-end data engineering largely unexplored. To bridge this gap, we introduce DataClawEval, the first comprehensive benchmark designed specifically to evaluate the end-to-end task completion capabilities of autonomous agents in real-world data engineering scenarios. Built upon production-grade code authored by professional enterprise data engineers, it comprises 100 rigorous, end-to-end tasks spanning five execution engines: PySpark, MySQL, HiveSQL, PrestoSQL/Trino, and FlinkSQL. Rather than non-deterministic LLM-as-a-judge scoring, each task is executed within a case-specific, isolated sandbox and graded by deterministic, rule-based scripts. Evaluating 16 frontier agents exposes critical limitations: The strongest model attains only 74.9 overall, and no single model dominates, as each excels on a different engine, revealing strict domain specialization rather than omnipotent proficiency. Thus, autonomous data engineering remains a formidable, unresolved challenge. We release our dataset, containerized environments, and deterministic evaluation scripts at https://github.com/Dicemy/DataClawEval/tree/master

</details>


### [203] [SKILL-KD: Contrastive Skill Distillation for LLM Agents](https://arxiv.org/abs/2607.28048)
*Qiming Shi,Yibo Dou,Jiawen Zhu,Yulong Tao,Linbo Jin,Zhaolu Kang,Yunfan Zhou,Di Weng*

Main category: cs.AI

TL;DR: 提出SKILL-KD，一种将技能作为显式蒸馏媒介的对比式技能蒸馏框架，用于提升弱学生LLM智能体的任务能力。

Research relevance: 1分，与本研究方向基本无关。论文聚焦于大语言模型（LLM）智能体的技能蒸馏（skill distillation），属于自然语言处理与智能体学习领域，完全不涉及遥感、卫星、目标检测或模型轻量化部署。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体的技能获取与蒸馏（skill distillation for LLM agents）。应用场景：通用智能体benchmark任务。传感器类型：不适用。目标类别：不适用。

Model architecture: SKILL-KD框架包含：对比式技能蒸馏（contrastive skill distillation）、技能补丁（skill patch）生成与迭代精炼、trace-linked edit histories、Drift-Aware Skill Consolidation（漂移感知技能整合）。核心是teacher-student蒸馏范式，但面向的是文本技能而非神经网络权重。

Lightweight method: 论文中的"distillation"是知识蒸馏概念在LLM智能体技能层面的延伸，但并非面向模型压缩/加速的轻量化方法。不涉及剪枝（pruning）、量化（quantization）、NAS、FLOPs优化等传统模型轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及任何星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性指标。

Datasets and metrics: 使用了五个agent benchmarks和两种student设置。具体数据集名称和评价指标摘要未说明。

Experiments: 在五个agent benchmarks和两种student设置下，SKILL-KD一致地提升了冻结学生智能体（frozen student agents）相对于固定模型适配基线（fixed-model adaptation baselines）的性能。

Limitations: 摘要未说明局限性。但明显与遥感/卫星/目标检测/轻量化方向无任何关联，属于纯NLP/智能体领域工作。

Ideas for my research: 借鉴意义非常有限。唯一可能的概念性启发是"teacher-student蒸馏"的思想——在遥感目标检测轻量化中，知识蒸馏（knowledge distillation）是常用手段，但本论文的蒸馏对象是文本技能而非视觉特征或模型权重，方法论上无法直接迁移。若强行寻找关联，其"对比式蒸馏"和"迭代精炼"的思想或许可启发蒸馏损失函数的设计，但关联度极低。

Abstract: Skill-based prompting has become a practical mechanism for improving large language model (LLM) agents, yet existing skill acquisition methods often treat skills as experience summaries, memory entries, or direct summaries of successful demonstrations. This creates a mismatch for weaker student agents: when a student fails because it lacks task knowledge or operational strategy, its failed trajectory may not contain enough evidence to infer the missing behavior, while the teacher trajectory may be too implicit to be internalized as reusable guidance. We propose SKILL-KD, a contrastive skill distillation framework that treats skills as an explicit distillation medium between agents of different capabilities. Given a student failure and the teacher trajectory on the same task, SKILL-KD distills their actionable discrepancy into a textual skill patch, evaluates the patch by re-running the student, and iteratively refines the patch when the student still fails. To prevent repeated local updates from causing skill drift, SKILL-KD further maintains trace-linked edit histories and performs Drift-Aware Skill Consolidation, deciding whether each patch should add a new rule, delete or modify an existing rule, or be skipped. Across five agent benchmarks and two student settings, SKILL-KD consistently improves frozen student agents over fixed-model adaptation baselines.

</details>


### [204] [IndustryForge-27B: A Domain-Enhanced Multimodal Foundation Model for Industrial CAD](https://arxiv.org/abs/2607.28050)
*Nianchen Deng,Jiaxin Ai,Tao Hu,Shu Zou,Yurui Dong,Siqi Li,Xinyu Cai,Xuemeng Yang,Licheng Wen,Hongbin Zhou,Hairong Zhang,Pinlong Cai,Botian Shi*

Main category: cs.AI

TL;DR: 构建基于Qwen3.5-VL-27B的工业CAD多模态基础模型IndustryForge-27B，通过整合六个工业CAD子语料库进行多任务SFT训练，在CAD领域基准上大幅提升并保持通用能力。

Research relevance: 与本课题（遥感卫星在轨目标检测与模型轻量化）基本无关。论文聚焦工业CAD设计与制造领域的多模态基础模型，涉及工程图纸理解、3D几何、参数化建模脚本和Windows COM API代码生成，属于工业设计自动化方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无关联。不涉及目标检测（object detection）、小目标检测、旋转框检测、舰船/飞机/车辆检测或遥感场景理解。也不涉及轻量化（pruning/quantization/distillation/NAS）、实时推理、边缘部署、星上部署等。相关性评分为1分。

Reading priority: 1分。论文聚焦工业CAD多模态大模型，与遥感卫星在轨目标检测及模型轻量化方向基本无关。不涉及遥感场景、目标检测或任何轻量化/部署技术，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：工业CAD多模态基础模型（multimodal foundation model），涵盖CAD视觉问答（CAD-VQA）、参数化建模代码生成（text2cadquery）、装配级CAD代码生成（text2cadquery-assembly）、以及Inventor/SolidWorks的Windows COM API代码生成（com_2d/com_3d/com_assembly）。应用场景为工业CAD设计与制造自动化，非遥感场景。传感器类型：工程图纸、3D几何截图（非遥感传感器）。目标类别：CAD零件与装配体（非遥感目标）。

Model architecture: 基于Qwen3.5-VL-27B构建，采用统一的多任务SFT（supervised fine-tuning）训练配方。整合六个工业CAD子语料库（约52k多模态样本）进行训练。模型规模为27B参数，属于大规模多模态基础模型架构。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等。相反，模型规模为27B参数，属于大模型范畴，与轻量化方向背道而驰。

Onboard deployability: 摘要未说明。27B参数规模的多模态模型在计算资源上远超星上/边缘部署能力，不具备在轨部署可行性。摘要未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。

Datasets and metrics: 数据集：六个工业CAD子语料库，总计约52k多模态样本，包括CAD-VQA、text2cadquery、text2cadquery-assembly、com_2d、com_3d、com_assembly。评测基准：四个CAD领域基准（具体名称摘要未说明）和十一个通用能力基准。指标：CAD领域平均提升+33.65个百分点，通用能力平均提升+1.56个百分点。

Experiments: 在四个CAD领域基准上，IndustryForge-27B相对基座模型平均提升+33.65个百分点，并在全部四个基准上超越闭源强模型GPT-5.4。在十一个通用能力基准上保持并略微提升基座模型性能（+1.56个百分点均值），未出现灾难性遗忘（catastrophic forgetting）。

Limitations: 1. 与遥感/卫星/在轨目标检测方向完全无关，无可借鉴的遥感技术内容。2. 27B参数规模的大模型方案与轻量化、边缘部署需求严重冲突。3. 摘要未说明模型推理效率、部署成本等关键工程指标。4. 摘要未说明CAD领域基准的具体名称和评测细节。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但仍有少量间接可借鉴点：1. 多任务统一SFT训练配方（multi-task SFT recipe）的思路可迁移到遥感多任务场景（如同时检测+分类+分割），但需注意遥感场景更强调轻量化和实时性，而非大模型。2. 论文强调"避免灾难性遗忘"的持续学习策略，对遥感模型在轨增量学习（如新类别目标出现时的在线更新）有一定方法论参考价值。3. 多子语料库整合与统一训练框架的工程组织方式，可类比遥感多传感器（光学/SAR/高光谱）数据的统一训练策略。总体而言，借鉴价值有限。

Abstract: Automating industrial CAD design and manufacturing places distinctive demands on multimodal foundation models: the model must see engineering drawings and 3D geometry screenshots, write correct parametric-modelling scripts and Windows COM API code, and cover the full range from single parts to assemblies. General-purpose multimodal models fall short on these tasks, while single-task fine-tuning is too narrow to support the diverse calls that upper-layer agents issue. We build IndustryForge-27B on top of Qwen3.5-VL-27B by curating and integrating six industrial-CAD sub-corpora totalling $\sim$52k multimodal samples---CAD Visual QA (CAD-VQA), parametric CAD code (text2cadquery), assembly-level CAD code (text2cadquery-assembly), and three COM sub-corpora for Inventor / SolidWorks (com_2d / com_3d / com_assembly)---and training with a unified multi-task SFT recipe. Across four CAD-domain benchmarks IndustryForge-27B lifts the base model by $+33.65$~pp on average and outperforms the strong closed-source model GPT-5.4 on all four; across eleven general-capability benchmarks it retains, and slightly improves upon, the base model ($+1.56$~pp mean, no catastrophic forgetting). IndustryForge-27B will serve as the common substrate for downstream industrial-agent projects, providing a unified starting point for a full-stack industrial agent that spans from CAD design to industrial-software operation, from parts to assemblies, and from single-shot generation to closed-loop self-improvement.

</details>


### [205] [SemPIC: Learning Semantic Position-Independent KV Caches](https://arxiv.org/abs/2607.28069)
*Hui Xie,Peng Xiao,Yutong Deng\textsuperscript,Shuoran Dou,Jian Yang,Jinyang Guo*

Main category: cs.AI

TL;DR: 提出SemPIC方法，通过行为蒸馏训练LoRA Writer为文档编译原生逐层KV缓存，解决长上下文检索与agentic工作负载中前缀缓存无法复用、位置无关缓存不可靠的问题，将micro-F1从0.53提升至0.60。

Research relevance: 弱相关（2分）。该论文属于大语言模型（LLM）长上下文检索与KV缓存优化方向，核心关注的是文档级KV Cache的编译与复用，属于泛机器学习/系统优化方法。虽然涉及"缓存"和"蒸馏"等概念，但完全不涉及遥感、卫星、地球观测、目标检测或边缘/星上部署等本研究方向的任何核心要素。

Reading priority: 1分。该论文聚焦LLM长上下文KV缓存优化，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然涉及蒸馏和LoRA等概念，但应用场景和优化目标完全不同，借鉴价值有限，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长上下文检索（long-context retrieval）与agentic工作负载中的KV缓存复用。应用场景：LLM推理加速。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: SemPIC架构包含两个核心组件：1) Writer——一个通过LoRA微调训练的模块，负责将文档编译为逐层（per-layer）原生KV状态；2) Reader——保持不变的预训练decoder，用于消费Writer编译的KV缓存。通过行为蒸馏（behavioral distillation）将Reader的表示能力迁移到Writer。此外引入KV Gradient Checkpointing技术，在不切断通过缓存KV的梯度的情况下降低峰值训练内存。

Lightweight method: 涉及知识蒸馏（behavioral distillation）和LoRA（低秩适配）两种轻量化相关技术，但它们的用途是训练Writer模块以编译KV缓存，而非用于模型压缩或推理加速部署。KV Gradient Checkpointing用于降低训练内存，而非推理时的显存优化。整体上轻量化方法的应用场景与遥感在轨部署无关。

Onboard deployability: 摘要未说明。论文未涉及参数量、FLOPs、延迟、能耗、实时性等部署指标，也未讨论边缘或星上部署可行性。其优化目标是KV缓存命中率与检索准确率（micro-F1），而非推理速度或资源占用。

Datasets and metrics: 实验覆盖3个模型和4个任务，评价指标为micro-F1。对比基线包括KV Packet（0.53）和Full Recompute（0.62），SemPIC达到0.60。摘要未说明具体数据集名称。

Experiments: 主要实验结果表明：1) 学习到的边界条件基线（boundary-conditioned baseline）能显著降低可复用块边界附近的注意力偏差，但内部和任务级残差仍然存在；2) SemPIC在三个模型和四个任务上将平均micro-F1从KV Packet的0.53提升至0.60，接近Full Recompute的0.62。摘要未提供消融实验细节。

Limitations: 1) 摘要未说明SemPIC在推理延迟、内存占用等实际部署指标上的表现；2) 未讨论Writer训练的计算开销和LoRA训练成本；3) 未涉及遥感、卫星或任何视觉/图像任务；4) 摘要未说明KV Gradient Checkpointing的具体内存节省比例；5) 未讨论该方法在资源受限边缘设备上的可行性。

Ideas for my research: 尽管与遥感在轨目标检测无直接关联，但以下思路可借鉴：1) "行为蒸馏"（behavioral distillation）思想——用未修改的预训练模型作为Teacher，训练轻量模块作为Student，这一范式可迁移到遥感目标检测模型的轻量化蒸馏中；2) LoRA低秩适配用于训练轻量辅助模块的思路，可类比于在检测模型上训练轻量head或适配器；3) "缓存复用"思想——在遥感在轨场景中，多帧连续观测图像存在大量冗余信息，可借鉴缓存复用思路设计帧间特征复用机制以降低重复计算。但这些均为间接借鉴，需要大幅改造。

Abstract: Long-context retrieval and agentic workloads repeatedly reuse the same documents under changing instructions, histories, and document orders. Prefix caching cannot exploit this reuse, while position-independent caching (PIC) remains unreliable because independently compiled KV states lack the future context in which they will be consumed. Our diagnostics show that a learned boundary-conditioned baseline sharply reduces attention deviation near reusable-block boundaries but leaves interior and task-level residuals, motivating adaptation of the document representation itself. We present \emph{SemPIC}, which trains a LoRA-enabled Writer to compile native per-layer document KVs through behavioral distillation while retaining the pretrained decoder as an unchanged Reader. Adaptation is confined to offline cache construction, preserving the standard KV interface and cache-hit decoding path. We further introduce KV Gradient Checkpointing, which reduces peak training memory without severing gradients through cached KVs. Across three models and four tasks, SemPIC raises mean micro-F1 over KV Packet from 0.53 to 0.60, approaching Full Recompute at 0.62.

</details>


### [206] [Echoverse: Deep, Evolving Environments for Training Computer-Use Agents at Scale](https://arxiv.org/abs/2607.28074)
*Yash Pandya,Sahil Gupta,Sarthak Harne,Archana Yadav,Kavyansh Chourasia,Hussein Mozannar,Vibhav Vineet,Sara Abdali,Corby Rosset,Yash Lara,Ahmed Awadallah,Ece Kamar,Akshay Nambi*

Main category: cs.AI

TL;DR: Echoverse 是一个用于训练 computer-use agents 的合成环境生成与协同进化框架，通过将规格编译为有状态应用并利用数据库进行任务评分，配合协同进化循环同时改进环境和模型。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于 computer-use agents（计算机操作智能体）的训练环境生成，属于 LLM agent / 人机交互 / 合成环境领域，不涉及 remote sensing、satellite、object detection、模型压缩或边缘部署等任何相关主题。

Reading priority: 1 分。论文主题为 computer-use agents 的合成环境生成，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：computer-use agents 的训练环境生成与协同进化。应用场景：登录受限、有状态的应用程序（如网页应用）的自动化操作。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非视觉目标检测）。

Model architecture: Echoverse 框架将规格（specifications）编译为有状态应用，任务通过应用自身的数据库进行评分。包含一个协同进化循环（co-evolution loop），每次读取 graded rollout 两次：一次用于修复环境、任务和验证器，一次作为模型的训练信号。训练使用 9B 规模的模型。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS 或轻量网络设计等模型压缩/加速方法。虽然提到"9B model"和"frontier model"，但这是模型规模对比而非轻量化研究。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：12 个合成环境（Echoverse 生成），并发布 4 个环境作为 benchmark（含应用、种子数据和 grounded graders）。评价指标：跨 14 个 evaluation splits 的准确率（36.5% → 67.1%），以及 live-site accuracy、held-out score 等。还涉及 reinforcement learning 场景下的 reward 设计。

Experiments: 主要结果：9B 模型在 14 个 evaluation splits 上从 36.5% 提升到 67.1%，与更大的 frontier model 差距在 14 个百分点以内。消融实验：浅层环境使 live-site accuracy 从 80.0 降至 75.0，深层环境提升至 85.0 和 65.0；针对单一界面控件的训练可迁移到 held-out widget families 和开放网络；修复单个环境使模型从 16.2% 提升到 38.5%。RL 场景下，结合 grounded verifier 与 dense per-step judge 的 reward 将 held-out score 从 58.8% 提升到 68.0%。

Limitations: 与本研究方向完全无关，因此局限性主要体现在：不涉及遥感图像、目标检测、模型压缩或部署；其合成环境生成与协同进化方法面向的是 LLM agent 的交互训练，与遥感在轨目标检测的视觉感知任务没有直接关联。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是"协同进化"（co-evolution）思想——即环境/数据与模型同步改进——可以类比到遥感目标检测中"难例挖掘与模型迭代训练"的闭环思路，但这是非常泛化的方法论迁移，与轻量化部署无直接关系。

Abstract: Computer-use agents learn from what their actions change, so training one needs applications it can act on, break and reset. The applications that matter most are login-gated and stateful, so synthetic environments stand in for them. Recent pipelines generate such environments in bulk, which moves the bottleneck from how many exist to what is inside each one. The returns, we find, come from three properties: how much behavioural depth an environment carries, whether it targets the interaction an agent actually fails, and whether it improves alongside the model. We present Echoverse, which compiles specifications into stateful applications whose tasks are graded against the application's own database, and a co-evolution loop that reads every graded rollout twice: as repairs to the environment, its tasks and its verifier, and as training signal for the model. Trained on twelve such environments, a 9B model improves from $36.5\%$ to $67.1\%$ across fourteen evaluation splits, within fourteen points of the much larger frontier model that taught it. We examine each property in turn. On the same domains, shallow environments push live-site accuracy below the base model ($80.0 \to 75.0$) while deep ones raise it ($80.0 \to 85.0$ and $48.0 \to 65.0$); drilling one interface control across many renderings transfers to held-out widget families and to the open web; and repairing a single environment lifts the model trained on it from $16.2\%$ to $38.5\%$. The same worlds serve as reinforcement-learning environments, where a reward combining the grounded verifier with a dense per-step judge raises held-out score from $58.8\%$ to $68.0\%$. We release four environments as a benchmark, with their applications, seed data and grounded graders. Code: https://aka.ms/echoverse

</details>


### [207] [Group-Reflective Self-Distillation for Agentic Reinforcement Learning](https://arxiv.org/abs/2607.28076)
*Binbin Zheng,Zijun Xie,Guanqun Zhao,Enlei Gong,Xing Ma,Xiaoliang Fu,Zeyu Chen*

Main category: cs.AI

TL;DR: 提出 Group-Reflective Self-Distillation (GRSD)，一种用于大语言模型智能体训练的强化学习与自蒸馏方法，通过策略自身验证轨迹的分组反思生成能力对齐的引导信号，改进回合级信用分配。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型（LLM）智能体的强化学习训练与自蒸馏，属于自然语言处理/智能体学习领域，不涉及遥感、卫星、地球观测、目标检测或模型压缩部署。相关性评分：1分。

Reading priority: 1分。论文主题为 LLM 智能体强化学习训练，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型智能体（LLM agent）的强化学习训练与自蒸馏。应用场景为通用智能体环境（agentic environments），非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 核心是 Group-Reflective Self-Distillation (GRSD) 框架：策略（policy）对每个 prompt 的 on-policy 验证轨迹进行反思，通过 stop-gradient snapshot 对比成功与失败轨迹的反思结果，构建 group-level privileged guidance；基于该 guidance，self-teacher 通过调制 outcome-based advantages 细化 turn-level credit assignment，同时保持 verifier 确定的学习方向。

Lightweight method: 论文涉及 self-distillation（自蒸馏）概念，但这里的蒸馏目的是增强 LLM 智能体的训练监督信号，而非模型压缩或推理加速。不涉及剪枝（pruning）、量化（quantization）、NAS、实时推理、边缘部署、FPGA/NPU/TensorRT 等轻量化手段。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要提及在多个 agentic environments 和不同模型规模（model scales）上进行实验，但未给出具体数据集名称、评价指标或实验设置细节。

Experiments: 实验表明 GRSD 在多个智能体环境和模型规模下 consistently outperforms competitive baselines，并在 unseen tasks 上具有更好的泛化能力。摘要未给出具体对比方法名称和量化结果。

Limitations: 摘要未说明局限性。此外，论文与遥感/目标检测/轻量化方向完全无关，无法提供该领域的直接借鉴。摘要也未给出具体数据集、指标、计算开销等关键信息。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但其中 self-distillation 的思想（利用策略自身输出构造引导信号）在概念层面可类比于知识蒸馏中"自蒸馏"（self-distillation）的思路——即用模型自身或浅层快照作为教师。若未来在遥感目标检测轻量化中探索自蒸馏式训练（例如用大模型自身蒸馏出轻量学生），可参考其"stop-gradient snapshot"和"group-level guidance"的设计思路。但整体借鉴价值有限。

Abstract: Reinforcement learning with verifiable rewards (RLVR) is effective for training large language model agents. However, terminal rewards provide only coarse trajectory-level supervision, leaving successful behaviors, recurring mistakes, and incidental choices entangled in the same outcome signal. Existing agentic self-distillation methods enrich sparse supervision with natural-language skills, but skills retrieved externally or extracted from a single trajectory by stronger models may mismatch current experience, exceed the policy's capability, or remain path-specific. We propose Group-Reflective Self-Distillation (GRSD), which derives capability-aligned and outcome-discriminative guidance from the policy's own verified rollouts. For each prompt, the policy reflects on each verified trajectory in an on-policy group, and a stop-gradient snapshot contrasts the resulting reflections from successful and failed rollouts to construct group-level privileged guidance. Conditioned on this guidance, a self-teacher refines turn-level credit assignment by modulating outcome-based advantages while preserving the verifier-determined learning direction. Experiments across multiple agentic environments and model scales demonstrate that GRSD consistently outperforms competitive baselines and generalizes more effectively to unseen tasks.

</details>


### [208] [Distilling Answer Set Programming Theories from Large Language Models](https://arxiv.org/abs/2607.28086)
*Nelson Higuera Ruiz,Markus Hofmarcher,Claudiu Leoveanu-Condrei*

Main category: cs.AI

TL;DR: 本文研究利用神经符号方法让大语言模型在固定agent框架下自动蒸馏出完整正确的Answer Set Programming (ASP) 理论，以VQA任务（CLEVR、GQA、CLEVRER）为应用域，对比了9种不同规模模型在1小时时限内从零编写ASP理论的能力。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于神经符号推理与ASP理论自动生成，属于知识表示与推理（KR&R）和LLM能力评估领域，不涉及遥感、卫星、目标检测或模型轻量化部署中的任何一项。虽然涉及"distill"（蒸馏）一词，但这里的蒸馏指的是从LLM中提取逻辑理论，而非深度学习中的knowledge distillation用于模型压缩。

Reading priority: 1分。论文主题（神经符号ASP理论蒸馏）与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何模型压缩/部署技术，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：ASP理论自动生成（neurosymbolic theory distillation）；应用场景：VQA（视觉问答）；传感器类型：不适用（非遥感传感器）；目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体模型架构细节。涉及9种LLM模型：4个前沿模型（Claude Sonnet 4.6, Claude Opus 4.7, GPT-5, DeepSeek V4 Pro）、2个中端模型（DeepSeek V4 Flash, gpt-oss-120b）、3个开源权重模型（qwen3.6-27b, gpt-oss-20b, qwen3.5-9b）。采用固定agent harness + solver-in-the-loop的神经符号框架。

Lightweight method: 不涉及传统模型压缩方法（pruning, quantization, NAS等）。论文中的"distill"指从LLM中蒸馏逻辑理论（ASP theories），而非知识蒸馏用于模型轻量化。摘要未说明任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。仅提及1小时时间限制用于理论推导，但这与推理延迟无关。

Datasets and metrics: 数据集：CLEVR、GQA、CLEVRER（均为VQA基准数据集，非遥感数据集）。评价指标：理论完整性（complete theory）和正确性（correct theory），以各数据集上的准确率百分比衡量（如CLEVR 100%、GQA 92.8%-98.8%、CLEVRER 92.7%-95.3%）。

Experiments: 主要实验发现：4个前沿模型中有3个在CLEVR上达到100%，在GQA上达到92.8%-98.8%；CLEVRER上Sonnet、Opus、DeepSeek V4 Pro得分92.7%-95.3%。GPT-5在CLEVR上达98.7%但在GQA上骤降至41.8%、CLEVRER上86.7%。加入其他数据集的手写参考理论后，3个前沿模型变化不超过±3.4个百分点，但GPT-5准确率下降3-19个百分点。论文还对比了模型规模（frontier vs mid-tier vs open-weights）对任务完成能力的影响。

Limitations: 与本研究方向完全无关，因此局限性主要体现在：1）不涉及任何遥感/卫星/目标检测内容；2）不涉及模型压缩、加速或边缘部署；3）"distillation"概念与深度学习中的知识蒸馏含义不同，容易造成术语混淆；4）实验仅在VQA合成/半合成数据集上验证，缺乏真实场景泛化性验证。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的借鉴点非常有限。唯一可能的间接启发是：论文展示了不同规模模型在复杂推理任务上的能力差异，这提示在轻量化遥感检测模型设计时需关注模型规模与任务复杂度之间的权衡（scaling law），但这一联系非常间接。不建议从本文提取具体方法用于遥感轻量化研究。

Abstract: Writing Answer Set Programming (ASP) theories from scratch is a difficult and time-consuming task. We take a neurosymbolic approach to study whether a model can distill complete and correct theories, given a fixed agent harness with the solver in the loop. The protocol is dataset-agnostic: with a single prompt and an empty file as the starting point the model is given a 1-hour time limit to derive a complete theory. We chose VQA as the application domain, three benchmarks (CLEVR, GQA, CLEVRER), as these are publicly available and non-trivial. In order to study the model scale required for solving this task we nine different models: four frontier (Claude Sonnet 4.6, Claude Opus 4.7, GPT-5, DeepSeek V4 Pro), two mid-tier (DeepSeek V4 Flash, gpt-oss-120b), and three open-weights (qwen3.6-27b, gpt-oss-20b, qwen3.5-9b). Three of four frontier models reach 100% on CLEVR and 92.8%-98.8% on GQA; on CLEVRER, Sonnet, Opus, DeepSeek V4 Pro score 92.7%-95.3%. GPT-5 reaches 98.7% on CLEVR but drops to 41.8% on GQA and to 86.7% on CLEVRER. Adding handwritten reference theories from other datasets moves the other three frontier models by at most +/-3.4 pp but reduces GPT-5's accuracy by 3-19 pp. We release the code, prompts, and theories distilled.

</details>


### [209] [Diversifying Personalized Research Ideation against AI-Induced Homogenization](https://arxiv.org/abs/2607.28087)
*Rui Xu,Yunke Wang,Linwei Tao,Wenjie Xuan,Yong Luo*

Main category: cs.AI

TL;DR: 提出DivAlign，一个用于AI辅助科研选题的四阶段流水线，通过细粒度研究者画像与对齐保持去同质化，降低社区级研究方向的冗余度。

Research relevance: 1分（基本无关）。该论文研究的是AI辅助科研选题（research ideation）问题，属于自然语言处理/推荐系统/科研方法学方向，与遥感、卫星、目标检测、轻量化部署均无任何关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或边缘部署技术。

Reading priority: 1分。与"遥感卫星在轨目标检测模型轻量化"研究方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI辅助科研选题生成与去同质化（research ideation de-homogenization）。应用场景：科研社区研究方向推荐。传感器类型：不适用。目标类别：不适用（处理对象是研究者画像与研究方向文本）。

Model architecture: DivAlign四阶段流水线：(1) 提取细粒度研究者画像（fine-grained researcher profiles）；(2) 生成画像条件化的候选研究方向（profile-conditioned candidate directions）；(3) 沿三个对齐维度（Executability, Comprehensibility, Growth Potential）对候选方向打分；(4) 在保留研究者局部方向的同时降低社区组合冗余。摘要未说明底层模型架构（如是否基于LLM）。

Lightweight method: 不涉及。论文不包含任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量网络设计方法。摘要中出现的"lightweight researcher contexts"指轻量的研究者上下文信息，与模型轻量化无关。

Onboard deployability: 不适用。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。

Datasets and metrics: 自建benchmark：来自5个子领域的95位AI研究者的数据。评价指标：平均成对相似度（average pairwise similarity）、最近邻相似度（nearest-neighbor similarity）、研究者-方向匹配度（researcher-direction fit score）。

Experiments: 与粗粒度单次选题（coarse single-shot ideation）相比，DivAlign将平均成对相似度从0.331降至0.294，最近邻相似度从0.704降至0.608；与独立top-choice变体相比，最近邻相似度从0.663降至0.608，同时保留了99.9%的研究者-方向匹配度。

Limitations: 摘要未说明：底层模型细节、计算开销、可扩展性、对非AI领域（如遥感）的泛化能力。此外，该工作聚焦科研选题推荐，与遥感在轨目标检测轻量化课题完全无关。

Ideas for my research: 借鉴意义极低。唯一可能的间接启发是"去同质化"思想——若在轻量化遥感检测模型设计中考虑避免社区内方法趋同（如过度依赖某类剪枝策略），可类比其多样性保留思路，但这是非常间接的类比，实际参考价值有限。

Abstract: AI-assisted research ideation has emerged as a promising paradigm for accelerating scientific discovery, with systems now capable of generating research directions conditioned on papers, topics, or lightweight researcher contexts. Yet current systems largely optimize individual suggestions in isolation. This leaves two blind spots. First, coarse researcher representations may elicit mainstream directions that appear broadly feasible, but lack sufficient researcher-specific grounding. Second, independent recommendations can concentrate a community's portfolio around recurring high-probability themes. To address these blind spots, we propose DivAlign, a four-stage pipeline for alignment-preserving de-homogenization. DivAlign extracts fine-grained researcher profiles, generates profile-conditioned candidate directions, scores them along three alignment dimensions (Executability, Comprehensibility, and Growth Potential), and surfaces researcher-local directions while reducing redundancy across the community portfolio. On a benchmark we construct from 95 AI researchers across five subfields, DivAlign reduces community-level redundancy while preserving researcher-direction fit. Compared with coarse single-shot ideation, it lowers average pairwise similarity from 0.331 to 0.294 and nearest-neighbor similarity from 0.704 to 0.608. Compared with the independent top-choice variant, DivAlign reduces nearest-neighbor similarity from 0.663 to 0.608 while retaining 99.9% of the researcher-direction fit score. Code and data are available at https://github.com/Ruixxxx/DivAlign.

</details>


### [210] [PerturbMap: Cross-Context Transfer of Single-Cell Perturbation Responses](https://arxiv.org/abs/2607.28090)
*Panpan Cui,Yiqi Liu,Wenhao Sun*

Main category: cs.AI

TL;DR: PerturbMap 是一种用于预测单细胞扰动图谱中缺失的受体上下文效应的机器学习方法，通过结合受体局部低秩基与源到受体的岭回归专家传输来预测缺失扰动效应。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文属于单细胞生物学/生物信息学领域，研究的是 Perturb-CITE-seq 单细胞扰动图谱中的缺失效应预测问题，不涉及遥感、卫星、地球观测、目标检测或任何模型压缩/加速技术。相关性评分为 1 分。

Reading priority: 1 分。该论文属于单细胞生物学/生物信息学领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：单细胞扰动效应预测（missing perturbation effect prediction）。应用场景：单细胞生物学、扰动图谱（perturbation atlas）。传感器类型：不适用（使用 Perturb-CITE-seq 单细胞测序数据）。目标类别：不适用（预测的是细胞扰动效应而非目标检测）。

Model architecture: PerturbMap 结合了：(1) 受体局部低秩基（recipient-local low-rank base）；(2) 源到受体的岭回归专家（source-to-recipient ridge experts）；(3) 基于验证锚点估计的路由可靠性来确定 proposal 权重。整体是一个跨上下文传输学习的预测框架。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS 或任何模型压缩/轻量化方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：Perturb-CITE-seq melanoma cohort（黑色素瘤单细胞扰动图谱数据集）。评价指标：full-effect MSE（均方误差）、condition-mean specificity diagnostic、top-10 counterpart retrieval by cosine。对比方法：FedAvg、zero-response、raw-copy、calibrated-copy、identity-shuffled affine controls，以及 centralized token-matched pooled reference。

Experiments: 在 Perturb-CITE-seq melanoma cohort 上，PerturbMap 相比受体局部低秩基在 full-effect MSE 上提升 4.1%，且 MSE 低于 FedAvg、zero-response、raw-copy、calibrated-copy 和 identity-shuffled affine 等对照方法。与 centralized token-matched pooled reference 相比，MSE 差距在 2.82×10⁻⁶ 以内。条件均值特异性诊断显示，同受体 top-10 余弦检索从低秩基的 74.5% 提升到 PerturbMap 的 80.5%。

Limitations: 摘要未说明局限性。但该论文与遥感目标检测方向完全无关，其方法（跨上下文扰动效应传输）在遥感场景中缺乏直接可迁移性。摘要也未提供模型复杂度、推理效率等部署相关信息。

Ideas for my research: 该论文与本人课题（遥感卫星在轨目标检测模型轻量化）基本无直接可借鉴点。唯一可能的间接启发是"跨域/跨上下文知识迁移"的思想——即源域（如自然图像）训练的目标检测模型向目标域（遥感图像）迁移时，如何避免错误信号传递，这与 domain adaptation 有一定概念上的相似性，但论文本身并未涉及视觉或遥感任务，借鉴价值有限。

Abstract: Single-cell perturbation atlases rarely measure every intervention in every cellular context: a query perturbation is often observed in one or more source contexts but missing in the recipient context where its effect is needed. Ignoring those measured responses discards query-specific experimental evidence, whereas copying or weakly calibrating them across contexts risks transferring the wrong signal. We propose PerturbMap, which predicts a missing recipient-context effect by combining a recipient-local low-rank base with accepted proposals that transport the same perturbation's measured source responses through source-to-recipient ridge experts fit on paired training perturbations, with proposal weights determined by route reliability estimated on validation anchors. On the Perturb-CITE-seq melanoma cohort, PerturbMap improves full-effect MSE by 4.1\% over a recipient-local low-rank base and achieves lower MSE than FedAvg, zero-response, raw-copy, calibrated-copy, and identity-shuffled affine controls. It remains within $2.82\times10^{-6}$ MSE of our centralized token-matched pooled reference, which uses a stronger training interface. A condition-mean specificity diagnostic shows the same direction: same-recipient top-10 counterpart retrieval by cosine increases from 74.5\% for the low-rank base to 80.5\% for PerturbMap.

</details>


### [211] [An Instrument to Evaluate Governance Proposals: AI Policy Analysis at Scale](https://arxiv.org/abs/2607.28094)
*Paulo Carvao,Claudio Mayrink Verdun,Isabel Adler,Jeffrey Zhou*

Main category: cs.AI

TL;DR: 提出一个用于AI治理政策分析的多维评估框架，结合专家反馈与计算文本分析构建政策属性评分标准，并对比商用LLM与领域训练模型的评估能力。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦AI治理政策分析框架，属于AI政策与治理领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等任何遥感相关主题，也不涉及object detection、轻量化、剪枝、量化、知识蒸馏等模型压缩或部署技术。相关性评分：1分。

Reading priority: 1分。论文主题为AI治理政策分析，与遥感卫星在轨目标检测及模型轻量化研究方向完全无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI治理政策分析（policy analysis），非目标检测任务。应用场景：政策制定、监管环境评估。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。提及使用商用LLM（commercial LLMs）和领域训练的rubric校准模型（domain-trained rubric-calibrated model）进行政策分析，但未给出网络结构细节。

Lightweight method: 不涉及。摘要未提及任何模型压缩、加速或轻量化方法（如pruning、quantization、knowledge distillation、NAS等）。

Onboard deployability: 不适用。摘要未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集。方法上采用mixed-methods（混合方法），整合subject matter experts的定性反馈与computational text analysis。评价指标方面，通过comparative visualizations支持跨政策比较，但未给出量化指标名称。

Experiments: 摘要提及将商用LLM的rubric-based policy analysis输出与domain-trained rubric-calibrated model进行benchmark对比，但未给出具体实验结果、对比方法细节或消融实验。

Limitations: 摘要明确说明该框架不评估政策有效性或可取性（effectiveness or desirability），仅关注相关性与对齐（relevance and alignment）。此外，摘要未说明框架在真实政策场景中的验证结果、计算成本、可扩展性等关键信息。

Ideas for my research: 该论文与"在轨遥感目标检测模型轻量化"课题基本无可借鉴点。唯一的间接启发可能在于其"rubric-calibrated model"作为基准来对比通用模型的思想——类似地，在轻量化研究中可以用一个高精度大模型作为teacher或baseline来对比压缩后的小模型，但这属于泛化的benchmark设计思路，与本文具体内容关联极弱。

Abstract: This paper introduces a policy analysis framework for systematic, transparent assessment of AI governance proposals in an evolving and contested regulatory landscape. AI policy debates often collapse into binary positions that obscure underlying tradeoffs and normative assumptions. The framework structures policy analysis around multiple policy attributes, allowing users to surface priorities and tensions without prescribing outcomes. We use a mixed-methods approach that integrates qualitative insights from subject matter experts with computational text analysis to inform the design of policy attribute rubrics. This quantifies the relative emphasis of different policy objectives and presents them through comparative visualizations that support interpretability and cross-policy comparison. The paper also examines the use of commercial LLMs for rubric-based policy analysis, benchmarking their outputs against a domain-trained rubric-calibrated model with explicitly defined analytical assumptions. Rather than assessing policy effectiveness or desirability, the framework focuses on relevance and alignment across attributes. By making analytical assumptions explicit, including attribute selection, rubric construction, and weighting schemes, the framework enables users to evaluate whether its embedded priorities align with the users' own normative commitments. The approach is jurisdiction-agnostic and intended to support policymakers, analysts, and researchers navigating complex AI governance environments.
  Contributions: (1) multidimensional policy assessment through empirically grounded rubrics that surface tradeoffs rather than resolving them; (2) a transparent hybrid methodology combining feedback from subject-matter experts with computational validation; and (3) use of domain-trained rubric-calibrated models as a benchmark for comparing different general-purpose large language models.

</details>


### [212] [SciDataSailor: Deep Scientific Data Exploring](https://arxiv.org/abs/2607.28098)
*Jiyong Rao,Yicheng Qiu,Chi Zhang,Chunfeng Song,Runkai Zhao*

Main category: cs.AI

TL;DR: 提出Deep Scientific Data Exploration智能体任务范式及SciDataSailor框架，用MCTS合成工具交互轨迹，用于科学数据集的探索、整合与分析。

Research relevance: 弱相关（2分）。论文涉及earth sciences数据集，但核心是LLM agent对科学数据仓库的探索与问答，并非遥感图像目标检测，也不涉及任何模型轻量化或星上部署。属于泛机器学习/LLM agent方法，与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅在地球科学数据层面有极弱的场景关联。

Reading priority: 1分。论文主题为LLM agent科学数据探索，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不推荐精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：科学数据探索（scientific data exploration）与问答（question answering）；应用场景：科学数据仓库的检查、整合与分析；传感器类型：不涉及遥感传感器；目标类别：不涉及目标检测。摘要未说明具体遥感图像或卫星数据的使用。

Model architecture: SciDataSailor框架，基于Monte Carlo Tree Search (MCTS)的轨迹合成，包含四个机制：difficulty-stratified exploration seeds、dual-feedback first-play urgency、hierarchical strategy-to-tool action generation、entropy-guided branching。底层依赖LLM agent的planning、reasoning和tool use能力。摘要未说明具体LLM模型架构。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或模型压缩方法。

Onboard deployability: 不涉及。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，也未提及星上/边缘部署。

Datasets and metrics: 构建了SciDataSailor-SFT-2K（用于监督微调）和SciDataSailor-Bench（用于评估），后者包含627个meta-information summarization任务和586个scientific question-answering任务，覆盖27个数据集，横跨life、earth和physical sciences。摘要未说明具体评价指标。

Experiments: 摘要仅介绍框架构建和benchmark设计，未给出具体实验结果、对比方法或消融实验细节。

Limitations: 与遥感目标检测和轻量化方向完全无关。摘要未涉及遥感图像处理、目标检测、模型压缩或部署。该工作聚焦于LLM agent对科学数据仓库的探索能力，属于数据科学/LLM agent领域。

Ideas for my research: 借鉴意义有限。可能的间接启发：(1) MCTS-based trajectory synthesis方法若用于遥感数据标注或数据增强策略搜索，或许有一定参考价值；(2) 其"hierarchical strategy-to-tool action generation"思想或可类比为遥感检测模型中的分层特征选择策略。但整体上与本课题（在轨遥感目标检测模型轻量化）关联微弱，不建议作为核心参考。

Abstract: Scientific datasets are commonly organized as hierarchical repositories containing heterogeneous and interdependent files, making their inspection, integration, and analysis labor-intensive and reliant on domain expertise. Although large language model (LLM) agents have advanced substantially in planning, reasoning, and tool use, existing research has largely overlooked their ability to interact with real scientific data assets through executable environments. We introduce Deep Scientific Data Exploration, an agentic task paradigm in which agents navigate repositories, interpret heterogeneous files and schemas, execute analyses, integrate cross-file evidence, and produce conclusions grounded in executed observations. To operationalize this paradigm, we present SciDataSailor, a framework for synthesizing tool-interactive trajectories by balancing broad exploration with targeted exploitation. SciDataSailor instantiates trajectory synthesis as Monte Carlo Tree Search (MCTS) with four task-specific mechanisms: difficulty-stratified exploration seeds, dual-feedback first-play urgency, hierarchical strategy-to-tool action generation, and entropy-guided branching. Using this framework, we construct SciDataSailor-SFT-2K for supervised fine-tuning and SciDataSailor-Bench for evaluation, with the latter comprising 627 meta-information summarization tasks and 586 scientific question-answering tasks across 27 datasets spanning the life, earth, and physical sciences.

</details>


### [213] [MIND: Lightweight and Effective Memory Injection Defense for LLM Agents via Intent-Aware Information Bottleneck](https://arxiv.org/abs/2607.28103)
*Dongyi Liu,Haixing He,Xiaobao Wu,Jia Li*

Main category: cs.AI

TL;DR: 提出MIND，一个面向LLM智能体记忆注入攻击的轻量级防御框架，利用意图感知信息瓶颈提取紧凑表示以识别恶意记忆。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/LLM安全领域，与遥感、卫星、Earth observation、目标检测均无直接关联。虽然使用了"lightweight"一词，但其轻量化指的是降低LLM审计的计算开销，而非面向边缘/星上部署的模型压缩。与"遥感卫星在轨目标检测模型轻量化"方向基本无关，仅在"轻量级防御框架"这一泛化概念上存在极弱的借鉴意义。

Reading priority: 1分。该论文属于LLM安全/对抗攻击防御领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。仅在Information Bottleneck特征压缩这一泛化概念上存在极弱的跨领域借鉴价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体（LLM-based agents）的记忆注入攻击防御（memory injection attack defense）。应用场景：多轮对话智能体安全。传感器类型：不适用（非遥感/视觉任务）。目标类别：恶意记忆/攻击轨迹识别。摘要未说明具体视觉或遥感目标。

Model architecture: MIND框架包含两个核心组件：(1) intent-aware Information Bottleneck (IB)，用于从初始用户意图和turn-level行为中提取紧凑的intent-behavior表示；(2) 轻量级检测器（lightweight detector），从IB提取的表示中识别恶意记忆。整体架构基于LLM agent框架（如ReAct），在记忆检索环节加入防御模块。

Lightweight method: 该论文的"lightweight"体现在：通过intent-aware Information Bottleneck压缩多轮上下文信息，避免对每一轮都进行重复的LLM审计（repeated LLM auditing），从而降低计算开销。这属于"信息压缩/降维"思路，而非传统意义上的模型剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS。摘要未说明参数量、FLOPs等具体轻量化指标。

Onboard deployability: 摘要未说明。论文面向LLM智能体安全场景，未涉及星上/边缘部署、FPGA/NPU/TensorRT、实时推理、能耗等部署相关信息。虽然声称"preserving inference efficiency"，但未给出具体延迟、显存、能耗等量化指标。

Datasets and metrics: 数据集：ReAct-StrategyQA（摘要中明确提及）。评价指标：ASR-r（attack success rate - 具体含义未完全说明，推测为攻击成功率相关指标）、ASR-a、average accuracy、latency。摘要未说明其他数据集或更详细的实验设置。

Experiments: 在ReAct-StrategyQA上，MIND将mean ASR-r和ASR-a分别降低55.4%和55.3%，同时在与未防御agent相当的平均准确率和延迟下运行。摘要未说明与其他防御方法的详细对比、消融实验或更多数据集上的结果。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于LLM安全领域；2. 摘要未提供参数量、FLOPs、推理延迟等具体轻量化量化指标；3. 仅在单一数据集（ReAct-StrategyQA）上报告结果，泛化性存疑；4. "lightweight"仅指避免重复LLM审计，并非面向硬件受限环境的模型压缩；5. 摘要未说明方法对多轮上下文长度、记忆库规模等的扩展性。

Ideas for my research: 尽管该论文与遥感在轨目标检测无直接关系，但以下概念可作跨领域借鉴：1. Information Bottleneck思想——在遥感目标检测中，可用IB思想压缩特征表示，过滤与检测任务无关的冗余信息，辅助轻量化特征提取；2. "意图感知"的注意力/特征筛选机制——类似地，在遥感检测中可设计"任务意图感知"的特征选择，只保留与目标类别相关的通道/空间信息，辅助剪枝或通道选择；3. 轻量级检测器嵌入主框架的思路——在遥感检测中可借鉴"轻量级辅助模块"设计，如轻量级ROI筛选或候选框过滤模块。整体而言，借鉴价值有限，不建议作为核心参考。

Abstract: Memory-augmented LLM-based agents are vulnerable to memory injection attacks: Agents may retrieve poisoned memory from attackers, which diverts their behavior from initial user intent and finally causes task failure. However, existing defense mechanisms either incur high computational cost or suffer from information redundancy in multi-turn contexts. To address these challenges, we propose Memory Intent-Aware Neural Denoising(MIND), a lightweight defense framework for memory injection attack. Our preliminary analysis reveals that benign and poisoned trajectories exhibit distinguishable relationships between the initial user intent and subsequent behavior. Building on this observation, MIND employs an intent-aware Information Bottleneck(IB) to extract compact intent--behavior representations from the initial intent and turn-level behavior. The IB preserves intent-relevant cross-turn attack signals while filtering task-irrelevant and repetitive information, and a lightweight detector identifies malicious memories from the resulting representations. As such, MIND mitigates information redundancy in multi-turn contexts while avoiding the overhead of repeated LLM auditing. Extensive experiments show that MIND reduces attack success rates while preserving task accuracy and inference efficiency. Notably, on ReAct-StrategyQA, MIND reduces mean ASR-r and ASR-a by 55.4% and 55.3%, respectively, while matching the undefended agent in average accuracy and latency.

</details>


### [214] [Beyond Rephrasing: Book-Level Organization Improves Synthetic Textbook Data for Mid-Training](https://arxiv.org/abs/2607.28109)
*Jiawen Tao,Miao Peng,Yaoming Li,Xiaokun Yuan,Mengzhou Wu,Wenhan Yu,Guoan Wang,Nuo Chen,Tong Yang,Maxm Pan*

Main category: cs.AI

TL;DR: 论文研究合成教科书数据对语言模型预训练的影响，核心发现是"书级文档组织"（book-level organization）比生成内容本身更能提升下游性能，并提出了一套可扩展的合成数据流水线。

Research relevance: 弱相关（1分）。该论文属于自然语言处理/大语言模型预训练数据合成方向，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。仅从"数据组织方式影响模型性能"这一方法论层面可能对数据增强策略有间接启发，但整体与本研究方向基本无关。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于NLP预训练数据合成领域。除非对"数据组织策略影响模型性能"这一方法论感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型预训练数据合成（synthetic pre-training data）。应用场景：NLP大模型预训练/中训练（mid-training）。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构细节，仅提及在Llama3-8B上进行了验证实验。核心研究对象是数据流水线而非模型结构，包括：检索源材料（retrieval）、主题聚类（clustering）、层次目录规划（hierarchical TOC planning）、源文段组装成书（book assembly）。

Lightweight method: 不涉及。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或任何模型压缩/加速方法。其关注点是数据层面的组织方式，而非模型层面的轻量化。

Onboard deployability: 不适用。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，摘要未说明。

Datasets and metrics: 数据集：合成教科书语料（686K textbooks, 32B tokens，覆盖15,000+学科），基于预训练语料检索构建。评价指标：下游任务平均性能提升（+1.09 average gain）。实验设置包括多种对照条件：Full（完整书级组织）、Split（内容相同但每节独立成文档）、RandomConcat（跨书随机拼接）、Rephrase（独立改写检索文档）。

Experiments: 主要实验结果：1) 用合成教科书替换自然书籍进行mid-training，下游性能平均提升+1.09；2) Full vs Split对照显示+1.02的增益来自文档打包（document packaging）；3) Full vs RandomConcat对照排除了文档长度单一因素；4) Full vs Rephrase对照显示+1.17增益证明了结构化合成（clustering + TOC + book assembly）的价值；5) 在Llama3-8B上Full同样优于RandomConcat和Natural Books。

Limitations: 摘要未说明局限性。但可推断：1) 该研究完全面向NLP预训练，与遥感视觉任务无直接迁移路径；2) 未涉及任何视觉/遥感数据；3) 未讨论计算开销或部署约束；4) 合成数据流水线的可扩展性在遥感领域是否适用未知。

Ideas for my research: 尽管弱相关，但"数据组织方式影响模型性能"这一核心洞察对遥感目标检测有一定间接启发：1) 在构建遥感目标检测训练数据时，可借鉴"结构化组织优于随机拼接"的思想——例如按场景类型（港口、机场、城区）组织训练样本，而非随机打乱，可能提升小目标检测性能；2) "层次化目录规划"思想可类比于遥感场景的层次化标注体系（scene-level → object-level）；3) 合成数据流水线（检索-聚类-组装）可迁移到遥感数据增强中，例如从已有遥感数据中检索相似场景并合成新的训练样本。但这些均为间接借鉴，需自行验证。

Abstract: Synthetic textbook data has improved language model pre-training, but prior work largely treats the benefit as a property of generated content or local rewriting style. We study a different factor: whether related content is organized into coherent book-level documents. We contribute both a scalable synthesis pipeline and controlled evidence that this organization matters. The pipeline retrieves source material from a pre-training corpus, clusters it into topical units, plans hierarchical tables of contents, and assembles source-grounded sections into complete books (our Full setting), yielding 686K textbooks (32B tokens) across 15,000+ disciplines. Replacing natural books in a mid-training mix with this corpus improves downstream performance by +1.09 on average. Controlled comparisons then disentangle the relevant design factors. A content-matched Split condition holds generated text and tokens fixed but treats each section as an independent document; Full's +1.02 mean gain isolates document packaging. A length-matched RandomConcat control that joins sections from different books remains below Full, ruling out document length alone. A retrieval-pool-matched Rephrase condition independently rewrites individual retrieved documents under the same audience-by-style scheme, without clustering, TOC planning, or book assembly; Full's +1.17 gain demonstrates the value of structured synthesis. On Llama3-8B, Full likewise outperforms both RandomConcat and Natural Books, supporting book-level organization as a useful axis for synthetic pre-training data design.

</details>


### [215] [BlueprintRepair: Typed Local Edits for Failed Lean Proof Blueprints](https://arxiv.org/abs/2607.28110)
*Ruslan Khrulev*

Main category: cs.AI

TL;DR: 提出 BlueprintRepair，一种基于蓝图依赖图的定理证明修复接口，通过十种 schema 检查的局部操作让模型修改证明蓝图，并构建 BlueprintTrace 基准（142 个受控失败案例）对比三种修复接口在多个 LLM 上的表现。

Research relevance: 1 分，与本研究方向基本无关。论文主题是 LLM 驱动的形式化定理证明（Lean proving systems）中的蓝图修复接口设计，属于形式化验证/自动推理领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无任何关联。

Reading priority: 1 分。论文属于形式化定理证明/LLM 推理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：形式化定理证明中的证明蓝图修复（blueprint repair）。应用场景：Lean 证明系统、交互式定理证明。传感器类型：无。目标类别：无。摘要未涉及任何遥感或视觉任务。

Model architecture: 核心是 BlueprintRepair 修复接口，将证明组织为蓝图（dependency graph of formal statements），通过十种 schema-checked 局部操作修改该图。操作需声明所编辑的节点，目标定理不可被改变，Lean 对每次应用变更进行校验。对比了三种接口：typed edits（类型化编辑）、exact source patches（精确源码补丁）、complete module rewrites（完整模块重写）。使用 DeepSeek-V4-Flash 和 Qwen3.6-Flash 两个 LLM 作为修复模型。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络等模型压缩或加速方法。唯一与"轻量"相关的表述是"typed repair is the cheapest per solved state"（类型化修复每个求解状态成本最低），但这是关于 token 预算/计算成本的比较，并非模型轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性指标。仅提到 token 预算（10,000 completion tokens per task）和相对成本倍数（patching 1.30x、rewriting 2.06x），这些是 LLM 推理成本而非部署指标。

Datasets and metrics: 数据集：BlueprintTrace，包含 142 个受控失败案例，带有完整的接受/拒绝修复轨迹（accepted and rejected repair trajectories）。评价指标：求解状态数（solved states）、每个求解状态的成本（cost per solved state）、覆盖率（coverage）。实验设置：在匹配的 source、feedback、model、budget 条件下对比三种接口，每个状态和接口一个 episode。

Experiments: 主要结果：1) 使用 DeepSeek-V4-Flash 时，三种接口解决的局部失败数量几乎相同；2) typed repair 每个求解状态成本最低（patching 为 1.30x，rewriting 为 2.06x）；3) typed repair 在 10,000 completion tokens 内达到几乎全部最终覆盖率，而两种 free-form 接口明显落后；4) Qwen3.6-Flash 求解状态更少，但 typed repair 仍最便宜，且在 proof-authoring 状态上领先，并重复了局部失败的模式。摘要未说明消融实验细节。

Limitations: 1) 与遥感/卫星/目标检测/轻量化完全无关，属于形式化验证领域；2) 摘要未说明模型参数量、推理延迟等部署指标；3) 摘要未说明 BlueprintRepair 的泛化能力、对更大规模证明的扩展性；4) 摘要未说明修复质量评估标准（除求解状态数外）；5) 仅对比两个 LLM，模型泛化性有限。

Ideas for my research: 虽然论文主题与遥感在轨目标检测轻量化无直接关系，但可借鉴的间接思路：1) "typed repair is cheapest per solved state" 体现了结构化/受约束的接口设计能降低推理成本，这类似于在轻量化检测模型中通过结构化剪枝或约束化设计减少计算开销的思路；2) 论文强调"operation names the node it edits, so the target theorem cannot be changed"，即通过约束保证正确性，可类比于在模型压缩中通过约束保证检测精度不退化；3) 构建受控失败基准（BlueprintTrace）并对比多种方案成本的做法，可迁移到轻量化检测模型的 benchmark 设计。总体借鉴价值有限。

Abstract: LLM-based Lean proving systems increasingly organize a proof as a blueprint: a dependency graph of formal statements. We introduce BlueprintRepair, a repair interface that lets a model change this graph through ten schema-checked local operations. An operation names the node it edits, so the target theorem cannot be changed. Lean checks every applied change, and an accepted repair must declare every blueprint lemma its proof uses. We also construct BlueprintTrace, a benchmark of 142 controlled failures with complete accepted and rejected repair trajectories. We compare typed edits, exact source patches, and complete module rewrites under matched source, feedback, model, and budget, one episode per state and interface. With DeepSeek-V4-Flash, the three interfaces solve almost the same number of the benchmark's localized failures. Typed repair is the cheapest per solved state (patching is 1.30x as expensive, rewriting 2.06x), and within 10,000 completion tokens per task it reaches almost all of its final coverage, while both free-form interfaces are well behind. A second model, Qwen3.6-Flash, solves fewer states but keeps typed repair cheapest, puts it ahead on the proof-authoring states, and repeats the localized pattern.

</details>


### [216] [ConMem: Contribution-Aware Memory for Long-Horizon Manufacturing Inspection Logs](https://arxiv.org/abs/2607.28126)
*Bingchen Liu,Yuanyuan Fang,Lei Liu,Guangyuan Dong,Xing Fu,Yuanyuan Gao,Shuyue Wei,Xin Li,Xiangtian Meng*

Main category: cs.AI

TL;DR: 提出ConMem，一种面向LLM辅助设备检修的贡献感知记忆框架，通过Shapley式贡献评估在有限记忆预算下保留高价值证据，实现早期风险筛查。

Research relevance: 弱相关（2分）。该论文面向钢铁设备检修场景，属于工业设备检测与LLM检索增强生成（RAG）方向，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然涉及"inspection"（检修/检测），但指的是工业设备检修日志的文本推理，而非遥感图像目标检测。轻量化方面仅涉及输入token压缩和响应时间优化，属于LLM推理效率优化，与模型剪枝/量化/蒸馏等传统轻量化手段不同。

Reading priority: 1分。该论文面向工业设备检修的LLM文本推理，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM辅助的钢铁设备检修早期风险筛查（equipment inspection early-risk screening）；应用场景：工业设备检修、人机协同（human-in-the-loop）；传感器类型：不涉及传感器，处理的是文本检修日志（inspection logs）；目标类别：设备密封磨损（seal-wear）等退化信号。

Model architecture: ConMem框架包含三个模块：(1) 将检修日志分割为功能证据单元（functional evidence units）；(2) 通过Shapley-style估计评估每个记忆单元对下游诊断的贡献；(3) 在受限记忆预算下保留高价值证据。底层依赖LLM（8K-context baseline）和RAG（retrieval-augmented generation）系统。

Lightweight method: 并非传统模型压缩方法。其"轻量化"体现在：通过贡献感知的记忆筛选，将输入token数量平均减少88.2%，响应时间降低86.6%。这属于LLM推理效率优化（prompt压缩/上下文精简），而非剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS等模型轻量化技术。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、显存、能耗、边缘部署或星上部署（onboard/edge deployment）相关信息。仅报告了响应时间降低86.6%，但未给出具体延迟数值或部署硬件平台。

Datasets and metrics: 数据集：真实世界检修日志数据集（real-world dataset）；评价指标：QA accuracy（76.0%）、平均输入token数（减少88.2%）、响应时间（降低86.6%）。未说明数据集规模、类别分布等细节。

Experiments: ConMem在真实数据集上达到76.0%的QA准确率，超过最强直接对比基线；相比naive 8K-context LLM基线，平均输入token减少88.2%，响应时间降低86.6%。消融实验表明功能角色感知分割和基于贡献的价值评估有助于优先识别弱退化信号。实际部署验证了ConMem能在三个检修周期内保留弱早期信号，提供早期密封磨损预警。

Limitations: 与遥感/卫星/目标检测/模型轻量化方向基本无关。局限性包括：仅针对文本检修日志，不涉及图像/遥感数据；Shapley-style贡献估计的计算开销未讨论；未报告模型参数量、FLOPs等部署指标；未涉及任何视觉目标检测任务。

Ideas for my research: 尽管方向不同，但有两个思路可借鉴：(1) "贡献感知的信息筛选"思想——在遥感在轨目标检测中，可借鉴类似思路对多帧/多时相遥感图像进行重要性评估，在有限星上存储/带宽约束下优先保留高价值图像帧或特征；(2) "受限预算下的信息保留"——在星上边缘部署中，如何在有限内存/算力预算下保留对检测最有贡献的特征通道或中间特征，与特征剪枝/通道选择有概念上的相通性。但整体借鉴价值有限。

Abstract: Long-horizon steel-equipment inspection requires reasoning over heterogeneous records accumulated across repeated inspection cycles. Existing retrieval-augmented generation systems treat historical logs as a static corpus and retain records without estimating their diagnostic value, failing to report early risk. To this end, we propose ConMem, a contribution-aware memory framework for LLM-assisted equipment inspection, supporting a human-in-the-loop early-risk screening system. Specifically, our ConMem first segments inspection logs into functional evidence units, then estimates each memory unit's contribution to downstream diagnosis through a Shapley-style estimation, and finally retains high-value evidence under a constrained memory budget. In experiments, we evaluate ConMem on real-world dataset and ConMem achieves 76.0% QA accuracy, exceeding the strongest directly comparable baseline. Relative to the naive 8K-context LLM baselines, it reduces the average number of input tokens by 88.2% and response time by 86.6%. Ablation studies also show that the functional-role-aware segmentation and contribution-based valuation are helping prioritize weak degradation signals for targeted field inspection. Practical deployments further confirm that ConMem retains the weak early signal across three inspection cycles, providing an early-stage seal-wear alert targeted for on-site inspectors.

</details>


### [217] [AgenticASR: Refining Speech Recognition in Real-World Scenarios via an Agentic Approach](https://arxiv.org/abs/2607.28175)
*Zixuan Jiang,Binghao Qiang,Jiaying Chi,Yanqiao Zhu,Kai Yu,Xie Chen*

Main category: cs.AI

TL;DR: 提出 Agentic Speech Recognition (AgenticSR)，一种面向语音转写文本的"音频到干净文本"任务，通过 ASR–Refiner 架构在流式语音中持续修订输出文本，去除口误、重复和自纠正，保留说话者最终意图。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦自动语音识别（ASR）和语音转写文本的规范化处理，属于自然语言处理/语音处理领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。轻量化方面仅提及"quality–latency trade-off"（质量-延迟权衡），但这是针对在线/离线推理的延迟讨论，并非面向边缘/星上部署的模型压缩或加速方法。

Reading priority: 1分。论文属于语音识别/自然语言处理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对"流式在线推理"或"质量-延迟权衡"的通用方法论感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语音识别后处理（spoken-to-written text normalization），即去除语音转写中的口误（disfluencies）、重复、错误起始和自纠正，并规范化书面形式。应用场景：流式语音转写（streaming speech transcription），任意时长的音频流。传感器类型：不适用（音频输入）。目标类别：不适用。

Model architecture: ASR–Refiner 架构：由一个 ASR 前端（front end）和一个 Refiner 模块组成。Refiner 反复变换一个有界的活动上下文（bounded active context），并在音频到达时替换对应的输出片段（output span），实现持续输出与修订（continual emission and revision）。摘要未说明 Refiner 的具体网络结构（如 Transformer、LSTM 等）。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS 或轻量网络设计。仅提及在线/离线推理的 quality–latency trade-off，属于推理延迟层面的讨论，而非模型压缩或边缘部署优化。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、显存、能耗或实时性指标。仅提到在线（online）与离线（offline）推理之间的质量-延迟权衡，但未给出具体延迟数值或部署平台（FPGA/NPU/TensorRT 等均未提及）。

Datasets and metrics: 引入 AASR-Bench，一个双语（bilingual）基准测试集，采用细粒度原子评分规则（fine-grained atomic rubrics）。评价指标为 AASR-Bench 分数。此外进行了 human–AI agreement 研究，验证基于评分规则的判断与独立专家评估的一致性。摘要未说明具体数据集规模、语言构成或基线系统细节。

Experiments: 在多个 ASR 前端（multiple ASR front ends）上评估，AgenticASR 在 AASR-Bench 上取得最高分数。消融实验（ablation）考察了 Refiner 容量（capacity）、上下文长度（context length）以及在线与离线推理之间的质量-延迟权衡。摘要未给出具体数值结果或对比方法列表。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于语音处理领域。2. 摘要未说明 Refiner 的具体网络结构、计算开销、模型规模。3. 未讨论在资源受限设备上的部署可行性。4. 未提供 AASR-Bench 的规模、语言构成等细节。5. 未说明 ASR 前端的具体选择及对结果的影响。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但存在两个可借鉴的抽象思路：1) "持续修订"（continual emission and revision）的在线推理范式——在遥感在轨场景中，卫星持续接收图像流，模型可借鉴这种"有界上下文 + 输出片段替换"的流式处理思想，用于视频流/多帧序列上的增量式目标检测，减少重复计算。2) "quality–latency trade-off"的在线/离线推理权衡分析框架——在星上部署中，可借鉴这种系统化的质量-延迟权衡评估方法，用于衡量轻量化模型在实时推理与精度之间的取舍。但整体借鉴价值有限。

Abstract: Automatic speech recognition (ASR) has achieved substantial gains in transcription accuracy, yet verbatim transcription does not necessarily produce readily usable text. It retains fillers, repetitions, false starts, and self-corrections that increase reading effort, obscure the speaker's final intent, and propagate unresolved or abandoned content to downstream tasks. Existing spoken-to-written methods process completed audio or transcripts but cannot revise emitted text when later speech changes how preceding content should be interpreted. We therefore formulate Agentic Speech Recognition (AgenticSR), an audio-to-clean-text task that removes disfluencies, resolves self-corrections, and normalizes written form while preserving the speaker's final intent. AgenticASR implements this task through an ASR--Refiner architecture that repeatedly transforms a bounded active context and replaces its corresponding output span as audio arrives. This enables continual emission and revision over streams of arbitrary duration. We also introduce AASR-Bench, a bilingual benchmark with fine-grained atomic rubrics. Across multiple ASR front ends, AgenticASR attains the highest AASR-Bench scores among evaluated systems. A human--AI agreement study shows that rubric-based judgments align with independent expert assessments. Ablations characterize Refiner capacity, context length, and the quality--latency trade-off between online and offline inference. Together, these results establish AgenticASR as a practical framework for intent-preserving clean transcription during ongoing speech. Code, AASR-Bench, and a demo will be released at https://github.com/AnXMuy/AgenticASR.

</details>


### [218] [Old Tricks, New Models: How Simple Image Transformations Break Modern AI-based Content Moderation](https://arxiv.org/abs/2607.28187)
*Marco Alecci,Francesco Marchiori,Iyiola Emmanuel Olatunji,Tegawendé F. Bissyandé,Jacques Klein*

Main category: cs.AI

TL;DR: 本文对三个商业多模态内容审核API进行黑盒鲁棒性评估，发现简单的图像变换即可绕过这些基于大模型的审核系统，指出其不能作为独立安全边界。

Research relevance: 与本研究方向基本无关（1分）。论文主题是"content moderation"（内容审核/安全过滤），属于网络安全与多模态大模型鲁棒性评估领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感场景，也不涉及object detection、pruning、quantization、knowledge distillation、NAS、轻量化或边缘部署等目标检测与模型压缩技术。

Reading priority: 1分。论文主题为内容审核API鲁棒性评估，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像内容审核（image moderation）鲁棒性评估；应用场景：社交媒体/在线平台的有害内容筛查；传感器类型：不适用（普通RGB图像）；目标类别：harm categories（有害内容类别，如multimodal content、self-harm等）。与遥感目标检测无关。

Model architecture: 摘要未说明具体模型架构。评估对象为三个商业多模态审核API（基于large foundation models），但未给出其内部网络结构。方法层面是黑盒评估，使用七种model-agnostic图像变换（如color inversion、grayscale conversion等）。

Lightweight method: 不涉及。本文不讨论任何模型压缩或加速方法（pruning、quantization、knowledge distillation、NAS、轻量网络等均未提及），也不涉及实时推理或边缘部署。

Onboard deployability: 不适用。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时性指标。本文讨论的是云端商业API的安全鲁棒性，与星上/边缘部署无关。

Datasets and metrics: 摘要提及在多个数据集、harm categories、perceptual-similarity constraints和transformation intensities下进行评估，但未给出具体数据集名称。评价指标涉及"unsafe-to-safe decision changes"（安全判定翻转率）和鲁棒性对比，但未给出具体数值。

Experiments: 对三个商业图像审核服务进行大规模黑盒评估，使用七种简单、model-agnostic图像变换。主要发现：(1) 所有三个服务均可被无需梯度/替代模型/目标系统知识的廉价图像变换绕过；(2) 固定变换（如color inversion、grayscale）即可引发unsafe-to-safe判定翻转，且内容对人类仍可识别；(3) 鲁棒性在不同数据集和harm categories间差异显著，multimodal content和self-harm类别漏洞突出。

Limitations: 与本研究方向完全无关，无法提供遥感目标检测或轻量化方面的任何信息。摘要未说明具体模型、数据集名称、量化指标等。此外，该研究仅针对商业API的黑盒评估，未涉及模型内部机制分析。

Ideas for my research: 借鉴价值极低。唯一可能间接相关的点是：该文强调"系统必须在真实变换下评估并作为分层pipeline的一部分部署"，这一思想在遥感在轨目标检测中可类比为——轻量化模型在轨部署前需在真实遥感图像退化/噪声条件下进行鲁棒性验证，而非仅在理想数据集上评估。但这一借鉴非常间接。

Abstract: While automated content-moderation systems have become essential for screening harmful content at scale, conventional task-specific classifiers often provide limited policy cov- erage and contextual understanding. Recently, commercial multimodal moderation APIs built on large foundation models have been introduced with the promise of providing broader and more capable safety filters. In this work, we analyze whether this shift also yields more robust image moderation. We conduct a large-scale black-box evaluation on three established commercial image-moderation services and compare their robustness. By evaluating seven simple, model-agnostic image transformations across multiple providers, datasets, harm categories, perceptual-similarity constraints, and transformation intensities, we find that: (1) all three commercial services can be bypassed using inexpensive image transformations that require no gradients, surrogate models, or knowledge of the target system; (2) even fixed transformations such as color inversion and grayscale conversion induce unsafe-to-safe decision changes while preserving content that remains recognizable to humans; (3) their robustness varies substantially across datasets and harm categories, with multimodal content and self-harm exhibiting pronounced vulnerabilities. This yields the conclusion that replacing conventional moderation classifiers with foundation-model-based APIs does not, by itself, provide a reliable security boundary. Such systems must be evaluated under realistic transformations and deployed as one component of a layered moderation pipeline rather than as standalone safety filters.

</details>


### [219] [Qwen-UI-Agent Technical Report: Toward Next-Generation Real-World Centric Foundation GUI Agents](https://arxiv.org/abs/2607.28227)
*Hanzhang Zhou,Panrong Tong,Xu Zhang,Quyu Kong,Chenglin Cai,Tianyu Xia,Gongjie Zhang,Jianan Zhang,Long Li,Long Chen,Lei Wang,Gaole Dai,Pengxiang Li,Liangyu Chen,Yue Wang,Steven Hoi*

Main category: cs.AI

TL;DR: Qwen-UI-Agent 是一个面向真实设备的通用 GUI 智能体基础模型，覆盖移动端、电脑端、网页和 DeepSearch 环境，通过统一动作空间、AutoResearch 数据飞轮和在线强化学习实现跨平台长程任务执行。

Research relevance: 弱相关（2分）。该论文属于泛 AI 智能体（GUI agent）方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关，也不涉及目标检测任务。虽然文中提到"lightweight harness layer"，但这里的"轻量"指的是智能体系统架构中的轻量级封装层，并非模型压缩/轻量化（pruning, quantization, knowledge distillation, NAS）意义上的轻量化，与在轨部署无关。

Reading priority: 1分。该论文属于 GUI 智能体领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测任务或模型压缩/加速技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：GUI agent（图形用户界面智能体），用于移动设备、电脑使用、网页浏览和 DeepSearch 环境的通用数字设备操作。应用场景为通用数字设备交互，非遥感场景。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: Qwen-UI-Agent 是一个基础 GUI agent 模型，覆盖 mobile、computer-use、web、DeepSearch 四种环境。其核心设计包括：统一动作空间（unified action space），将 GUI 操作与 CLI 执行交错结合，并在单次模型生成中输出批量动作（batched actions）；结合多样化的沙箱环境与大规模真实设备移动端运行时（real-device mobile runtime）；AutoResearch 风格的数据飞轮（data flywheel）用于自动构建任务和环境、诊断失败并规划后续迭代；在线强化学习（online RL）支持超过 100 轮轨迹训练，利用超过 10,000 个并发环境加速 rollout；轻量级封装层（lightweight harness layer）支持主动服务启动和跨移动端/电脑端的有状态工作流。

Lightweight method: 摘要中提到的"lightweight harness layer"是指智能体系统架构中的轻量级封装层，用于支持主动服务启动和有状态工作流，并非模型压缩/加速意义上的轻量化方法。摘要未说明任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS 或模型压缩技术。

Onboard deployability: 摘要未说明。论文未涉及星上部署、边缘部署、FPGA/NPU/TensorRT 等部署相关内容，也未报告参数量、FLOPs、延迟、显存、能耗等部署指标。虽然提到"lightweight harness layer"，但这是系统架构层面的轻量封装，与在轨/边缘部署无关。

Datasets and metrics: 评估基准：MobileWorld（82.1%）、MobileWorld-Real（92.2%）、AndroidDaily（97.5%）用于移动端使用；OSWorld-Verified（79.5%）和 OSWorld-v2（40.0% partial-progress）用于电脑端使用；WebArena（73.6%）用于浏览器使用；ScreenSpot-Pro（81.5%）用于 GUI grounding。对比模型包括 Opus 4.8、Gemini 3.1 Pro 和 GPT-5.6 Sol。这些均为 GUI agent 领域的基准，与遥感目标检测数据集（如 DOTA、HRSC2016、NWPU VHR-10 等）无关。

Experiments: 在移动端基准上取得 SOTA 性能（MobileWorld 82.1%、MobileWorld-Real 92.2%、AndroidDaily 97.5%），在电脑端和浏览器任务上与前沿模型（Opus 4.8、Gemini 3.1 Pro、GPT-5.6 Sol）相比具有竞争力。摘要未说明消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于通用 GUI 智能体领域。2. 摘要未说明模型参数量、计算开销、推理延迟等部署相关指标。3. 摘要未说明轻量化方法（剪枝、量化、蒸馏等）的具体技术细节。4. 摘要未说明在资源受限设备上的运行可行性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴的宏观思路：1. "AutoResearch 数据飞轮"中自动构建任务、诊断失败并迭代的思想，可类比用于遥感目标检测中的自动数据增强或难例挖掘策略。2. "在线强化学习 + 大规模并发环境"的训练范式，可启发在遥感检测模型训练中引入更高效的训练调度策略。3. "统一动作空间 + 批量动作生成"的架构设计思路，可类比为遥感检测中的多任务统一输出设计。但总体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: GUI agents have the potential to become a general purpose executor over existing digital devices. To advance them toward real-world use, we envision agents that operate reliably on real devices, execute workflows across platforms, combine GUI interaction with CLI execution, complete long-horizon tasks, proactively initiate useful services, and autonomously improve their capabilities with minimal human effort. Guided by this vision, we present Qwen-UI-Agent, a real-world centric foundation GUI agent spanning mobile, computer-use, web, and DeepSearch environments. Qwen-UI-Agent combines diverse sandbox environments with a large-scale real-device mobile runtime. Its unified action space interleaves GUI operations with CLI execution and generates batched actions in a single model turn. An AutoResearch-style data flywheel uses agents to construct tasks and environments, diagnose failures, and plan subsequent iterations. Online RL supports training on trajectories exceeding 100 turns, with over 10,000 concurrent environments accelerating rollout. A lightweight harness layer supports proactive service initiation and stateful workflows across mobile and computer.
  Across a broad suite of evaluations, Qwen-UI-Agent sets state-of-the-art performance on mobile-use benchmarks while delivering competitive performance on computer- and browser-use tasks against frontier models, including Opus 4.8, Gemini 3.1 Pro, and GPT-5.6 Sol. On mobile use, it achieves 82.1% on MobileWorld, 92.2% on MobileWorld-Real, and 97.5% on AndroidDaily. On computer use, it achieves 79.5% on OSWorld-Verified and a 40.0% partial-progress score on OSWorld-v2. On browser use and GUI grounding, it achieves 73.6% on WebArena and 81.5% on ScreenSpot-Pro, respectively.

</details>


### [220] [CDAE: Enhancing Perturbation Robustness in Pretrained Language Models with Contrastive Denoising](https://arxiv.org/abs/2607.28236)
*Sina Heydari,Amirreza Abbasi,Mohsen Hooshmand,Majid Ramezani*

Main category: cs.AI

TL;DR: 提出轻量级对比去噪自编码器（CDAE），通过联合优化对比学习和重构目标来精炼预训练BERT嵌入，使其对同义替换、掩码和词丢弃等语义保持型文本扰动具有不变性，提升句子嵌入的稳定性。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于自然语言处理（NLP）领域的句子嵌入表示学习，涉及预训练语言模型（BERT）和对比学习，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。虽然标题中出现"lightweight"一词，但这里的"轻量"指的是模型结构相对精简，并非面向边缘/星上部署的模型压缩与加速，与遥感目标检测轻量化毫无关联。

Reading priority: 1分。论文属于NLP领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对"扰动不变表示学习"这一通用思想感兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：句子嵌入表示学习（sentence embedding learning），具体为扰动不变表示学习（perturbation-invariant representation learning）。应用场景：自然语言处理，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 基于预训练BERT的对比去噪自编码器（Contrastive Denoising Autoencoder, CDAE），联合优化对比学习目标（contrastive objective）和重构目标（reconstruction objective），以学习对语义保持型文本扰动（同义替换、掩码、词丢弃）不变的嵌入表示。对比基线包括原始BERT嵌入和SimCSE。

Lightweight method: 摘要中"lightweight"仅指CDAE框架本身结构相对轻量（在BERT之上附加对比与重构头），并非传统意义上的模型压缩方法（如pruning、quantization、knowledge distillation、NAS等）。摘要未说明任何面向部署的轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且BERT本身为大规模Transformer模型，与在轨遥感目标检测的轻量化需求方向不符。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为嵌入相似度（embedding similarity），即在多种扰动策略和不同强度下，衡量CDAE生成的嵌入与原始嵌入之间的相似度保持能力。对比对象为原始BERT嵌入和SimCSE。

Experiments: 实验采用多种扰动策略（synonym substitution、masking、word dropout）及不同扰动强度进行验证。结果表明CDAE在扰动下能持续保持更高的嵌入相似度，且随着扰动增强，相对原始BERT和SimCSE的提升更加显著，说明其有效增强了表示稳定性同时保留了语义信息。

Limitations: 摘要未说明局限性。主要缺口包括：未报告具体数据集和下游任务（如STS基准）上的定量结果；未说明模型参数量和计算开销；未讨论对真实NLP下游任务的泛化能力；未涉及任何遥感或视觉场景。

Ideas for my research: 尽管论文与遥感目标检测无关，但"扰动不变表示学习"（perturbation-invariant learning）的思想在概念上可迁移：在遥感目标检测中，可借鉴对比学习+重构的联合优化思路来增强模型对光照变化、云遮挡、传感器噪声等遥感图像扰动的鲁棒性。此外，其"轻量"框架设计思路（在预训练模型上附加轻量头而非重新训练）对遥感检测模型的增量式轻量化改造有一定启发。但整体借鉴价值有限。

Abstract: Pre-trained language models have significantly improved sentence representation learning, yet their embedding remain sensitive to semantic preserving textual perturbations such as synonym substitution, masking and word dropout. This work proposes a lightweight Contrastive Denoising Autoencoder (CDAE) that refines pre-trained BERT embedding by jointly optimizing contrastive and reconstruction objective to learn perturbation-invariant representation. We evaluate the proposed framework using multiple perturbation strategies with varying strengths and compare it against the original BERT embeddings and SimCSE. Experimental results show that CDAE consistently preserves higher embedding similarity under perturbations, with the improvements becoming more pronounced as framework effectively enhances representation stability while preserving semantic information, highlighting perturbation-invariant learning as a promising direction for improving sentence embeddings. The source code is publicly available at: https://github.com/ComputationIASBS/CDAE

</details>


### [221] [AI and Authenticity in Islamic Research: A Critical Evaluation of Generative AI Reliability, Hallucination, and Source Fidelity in Quranic, Hadith, and Fiqh Knowledge](https://arxiv.org/abs/2607.28237)
*Muhammad Sajjad Akbar*

Main category: cs.AI

TL;DR: 评估六种主流生成式AI系统在伊斯兰宗教知识问答中的准确性、真实性、幻觉率与可靠性，属于AI可信度评估研究。

Research relevance: 1分，基本无关。论文研究的是生成式AI在伊斯兰宗教知识领域的可靠性评估，与遥感、卫星、Earth observation、目标检测、模型轻量化、在轨部署等方向完全无关。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。

Reading priority: 1分。论文主题为生成式AI在伊斯兰宗教知识领域的可靠性评估，与遥感卫星在轨目标检测及模型轻量化研究方向完全无关，无阅读价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：生成式AI系统在宗教知识问答中的可靠性评估（LLM evaluation）。应用场景：伊斯兰宗教教育、教法咨询、学术研究。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体模型架构。仅提及"six leading generative AI systems"（六种主流生成式AI系统），但未列出具体模型名称（如GPT、Claude、Gemini等），也未说明其网络结构。

Lightweight method: 不涉及。摘要中没有任何关于pruning、quantization、knowledge distillation、NAS、轻量网络、实时推理、边缘部署、FPGA/NPU/TensorRT等模型压缩或加速方法的描述。

Onboard deployability: 不适用。摘要未涉及星上部署、边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。

Datasets and metrics: 数据集：50个真实世界的开放式伊斯兰问题，覆盖Qur'anic interpretation、Hadith、Fiqh、ethics、pastoral advice、Madhhab-sensitive topics等六个领域。数据来源：澳大利亚和英国参与者。评价指标：domain accuracy、citation verification、hallucinations、jurisprudential consistency、uncertainty handling、source provenance、geographical variation，采用mixed-method框架分析。

Experiments: 对六种生成式AI系统进行50个问题的问答评估，回答四个研究问题：(1)各领域AI回答的准确性与真实性；(2)幻觉、不完整引用、不可验证宗教参考的程度；(3)模型对教法分歧、Madhhab多样性和不确定性的处理一致性；(4)AI系统是否足以用于宗教指导、伊斯兰教育和学术研究。结论：AI可作为入门学习的辅助工具，但不应作为宗教裁决或研究的权威来源。

Limitations: 摘要未说明具体局限性。但明显与本课题方向完全无关：不涉及遥感图像、目标检测、模型轻量化或任何部署优化。此外，摘要未披露具体模型名称、评估样本的统计显著性、以及不同模型间的量化对比结果。

Ideas for my research: 对本课题（遥感卫星在轨目标检测模型轻量化）基本无可借鉴点。唯一可能的间接启发是：该论文采用的"mixed-method评估框架"和"多维度可靠性评估"思路，在方法论层面可类比迁移到轻量化模型在轨部署前的多维度评估（如精度、速度、能耗、鲁棒性综合评估），但这一关联非常间接且牵强。

Abstract: Generative Artificial Intelligence (AI) is increasingly used by Muslims for religious guidance, Qur'anic interpretation, Hadith explanation, jurisprudential rulings, and Islamic education. Despite its growing adoption, there is limited empirical evidence on whether current AI systems provide authentic, verifiable, and trustworthy Islamic knowledge suitable for high-trust religious contexts. This study evaluates six leading generative AI systems using fifty realistic open-ended Islamic questions covering Qur'anic interpretation, Hadith, Fiqh, ethics, pastoral advice, and Madhhab-sensitive topics. Responses were collected under real-world conditions from participants in Australia and the United Kingdom and analysed using a mixed-method framework examining domain accuracy, citation verification, hallucinations, jurisprudential consistency, uncertainty handling, source provenance, and geographical variation.
  The study addresses four research questions: (1) How accurate and authentic are AI-generated responses across major Islamic knowledge domains? (2) To what extent do AI systems produce hallucinations, incomplete citations, or unverifiable religious references? (3) How consistently do models handle jurisprudential disagreement, Madhhab diversity, and uncertainty? (4) Are current AI systems sufficiently reliable for religious guidance, Islamic education, and scholarly research?
  Overall, current generative AI systems are valuable as assistive tools for introductory Islamic learning but should not be treated as authoritative sources for religious rulings or Islamic research without verification against authenticated primary sources and qualified scholarly expertise. This study provides one of the first comprehensive empirical evaluations of AI reliability within Islamic knowledge, offering practical guidance for researchers, educators, AI developers, and the wider Muslim community.

</details>


### [222] [Operationally Guided Placement-Aware Learning for Industrial Online 3D Bin Packing](https://arxiv.org/abs/2607.28257)
*Dheeraj Poolavaram,Aanchal Rajesh Chugh,Sebastian Dorn*

Main category: cs.AI

TL;DR: 提出OPAL框架，通过操作引导的空余空间候选生成器（OG-EMS）、xLSTM位置编码器和轻量级循环核心，解决工业在线三维装箱问题（3D-BPP）中的候选生成与表示优化。

Research relevance: 弱相关（2分）。该论文属于物流与工业码垛领域的在线三维装箱问题（3D-BPP），与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关。虽然论文中提到了"lightweight recurrent core"（轻量级循环核心），但这只是模型内部的一个轻量模块设计，并非面向边缘/星上部署的模型压缩或加速方法（如pruning、quantization、knowledge distillation、NAS等）。因此与"遥感卫星在轨目标检测与模型轻量化"方向仅存在非常间接的借鉴可能。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，属于物流三维装箱问题。虽然个别组件（如xLSTM编码器、操作引导候选生成）有间接借鉴可能，但整体参考价值很低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：在线三维装箱问题（online 3D-BPP），即给定物品序列，实时决策每个物品的放置位置。应用场景：物流与工业码垛（industrial palletizing）。传感器类型：不涉及遥感传感器。目标类别：三维装箱物品（非遥感目标）。

Model architecture: OPAL框架包含三个核心组件：1) Operationally Guided Empty-Maximal-Space generator (OG-EMS)——在自由空间区域内评估多个锚点，优先选择低、支撑良好、紧凑且空间多样化的候选放置；2) 基于xLSTM的Placement Encoder——建模几何与操作候选属性之间的依赖关系；3) 轻量级recurrent core——将嵌入与当前物品和托盘状态结合，对可行动作进行排序。训练采用masked ranking policy + proximal policy optimization (PPO)。

Lightweight method: 摘要中仅提到"lightweight recurrent core"（轻量级循环核心），但这属于模型内部模块的轻量设计，并非面向部署的模型压缩方法。摘要未说明是否采用剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等轻量化技术。也未涉及FPGA/NPU/TensorRT等边缘部署加速手段。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署相关信息。仅提到"maintaining robust inference-time performance"（保持稳健的推理性能），但未给出具体数值。该工作面向物流码垛场景，与星上部署（onboard deployment）无关。

Datasets and metrics: 数据集：BED-BPP benchmark。评价指标：平均空间利用率（mean space utilization），OPAL达到0.49。消融实验显示：操作引导的候选生成带来15.1%的提升，学习排序带来6.3%的提升。

Experiments: 在BED-BPP benchmark上，OPAL达到平均空间利用率0.49。通过消融实验验证了各组件贡献：OG-EMS操作引导候选生成贡献15.1%的提升，学习排序贡献6.3%的提升。摘要未说明与其他基线方法的详细对比结果。

Limitations: 1) 与遥感/卫星/在轨目标检测完全无关，属于物流装箱领域；2) 摘要未说明模型参数量、推理延迟、计算复杂度等部署关键指标；3) 未涉及任何模型压缩或加速技术；4) 空间利用率0.49在装箱问题中并不算高，可能仍有较大改进空间；5) 摘要未说明泛化能力、不同托盘尺寸/物品形状的适应性。

Ideas for my research: 尽管该论文与遥感在轨目标检测方向弱相关，但仍有少量可借鉴思路：1) "操作引导的候选生成"思想——在遥感目标检测中，可类比为基于先验几何/物理约束（如舰船长宽比、飞机朝向）引导anchor或proposal生成，减少无效候选；2) xLSTM作为序列编码器——可用于遥感视频流或时序遥感数据中的目标检测特征建模；3) masked ranking policy + PPO的训练范式——可借鉴到遥感检测中的决策优化或检测框排序；4) 轻量级recurrent core的设计思路——可作为轻量化检测模型中的时序模块参考。但这些借鉴均较为间接，整体参考价值有限。

Abstract: The online three-dimensional bin packing problem (3D-BPP) is a longstanding challenge in logistics and industrial palletizing. Recent learning-based methods use a learned policy to select among feasible candidate placements. Performance depends on the candidate generator and representation, especially in industrial settings where packings must be space-efficient, stable, compact, and balanced. However, prior work has mainly optimized the policy, while candidate generation and representation remain largely geometry-driven. We address this gap with OPAL, an operationally guided placement-aware learning framework for industrial online 3D-BPP which combines an Operationally Guided Empty-Maximal-Space generator (OG-EMS), an operational representation for each candidate placement, and a masked ranking policy trained with proximal policy optimization. OG-EMS evaluates multiple anchors within each free-space region and prioritizes low, well-supported, compact, and spatially diverse placements. An xLSTM-based Placement Encoder models dependencies among geometric and operational candidate attributes, while a lightweight recurrent core combines the resulting embeddings with the current item and pallet state to rank feasible actions. On the BED-BPP benchmark, OPAL achieves a mean space utilization of 0.49, with improvements of 15.1% from operationally guided candidate generation and 6.3% from learned ranking, while maintaining robust inference-time performance.

</details>


### [223] [LLM-Guided Evolutionary Search for Constraint Model Reformulation to Improve Solver Efficiency](https://arxiv.org/abs/2607.28268)
*Kostis Michailidis,Dimos Tsouros,Nguyen Dang,Tias Guns*

Main category: cs.AI

TL;DR: 本文研究利用大语言模型（LLM）通过进化式框架自动对约束满足问题（CSP）的声明式模型进行性能导向的重构优化，提出Profile-Diverse Retention（PDR）策略以保留行为多样化的历史尝试，在CSPLib基准上取得显著加速。

Research relevance: 弱相关（1-2分）。论文完全面向组合优化/约束满足问题（Combinatorial problems / Constraint Satisfaction Problems），与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。唯一可能的间接借鉴点是其利用LLM进行自动化模型重构优化的思路，与NAS（Neural Architecture Search）或自动化轻量化设计有方法论上的类比，但场景完全不同。

Reading priority: 1分。论文主题为组合优化问题的约束模型重构，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无直接可借鉴的技术方法或实验结论。仅作为方法论层面的泛读参考，不建议深入精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：组合优化问题的声明式约束模型自动重构（model reformulation）。应用场景：工业组合优化、约束求解（CSPLib基准）。传感器类型：不适用。目标类别：不适用。摘要未涉及任何遥感或视觉任务。

Model architecture: 核心框架为进化式搜索（evolutionary framework），其中LLM作为proposal生成器提出候选模型重构方案，候选方案通过后端solver验证和基准测试。提出Profile-Diverse Retention（PDR）策略，利用Maximal Marginal Relevance（MMR）对实例级运行时向量（instance-level runtime vectors）进行行为多样性保留。对比了多种AHD（Automatic Heuristic Design）适应的搜索策略，包括仅保留最近尝试、仅保留最快尝试等。

Lightweight method: 摘要未涉及传统模型压缩方法（pruning, quantization, knowledge distillation, NAS等）。本文的"轻量化"含义是求解性能优化（runtime speedup），即通过LLM自动重构约束模型以降低求解时间，而非神经网络模型的轻量化。与遥感在轨部署的轻量化目标（参数量、FLOPs、延迟、能耗）无直接关系。

Onboard deployability: 摘要未说明。本文不涉及任何星上/边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、显存、能耗等部署相关信息。其优化目标是约束求解器的求解速度（held-out speedup），与在轨推理部署完全无关。

Datasets and metrics: 数据集：CSPLib（约束满足问题标准库）中的8个问题。评价指标：held-out speedup（留出集加速比），采用validation-based final model selection（基于验证集的最终模型选择）。

Experiments: 主要实验结果：(i) 迭代式重构（iterative reformulation）可产生显著的held-out加速；(ii) 保留行为多样化上下文（diverse context）的策略优于仅保留最近或最快尝试的策略；(iii) 基于验证集的选择方法能提升所有策略的held-out加速比。摘要未提供具体数值。

Limitations: 与本研究方向基本无关。局限包括：仅针对CSPLib的8个问题，泛化性未知；依赖LLM作为重构生成器，计算开销和推理成本未讨论；摘要未说明重构后模型的可解释性、稳定性及对solver类型的依赖。对遥感目标检测轻量化课题无直接可借鉴的局限性分析。

Ideas for my research: 尽管场景完全不同，但本文的"LLM驱动的自动化搜索+多样性保留"方法论在概念上可类比于NAS或自动化轻量化设计：1) 可借鉴其"进化式搜索+验证反馈"的闭环思路，用于自动化搜索轻量化目标检测网络结构或剪枝策略；2) PDR的多样性保留思想可类比于NAS中防止搜索陷入局部最优的种群多样性维护策略；3) validation-based selection思想可类比于轻量化模型在验证集上做架构/超参选择。但整体借鉴价值有限，因为问题域、优化目标和评估方式差异巨大。

Abstract: Combinatorial problems appear in numerous industrial applications. A common approach is to formulate these problems as declarative constraint models that can subsequently be compiled to and solved by a range of back-end solvers. Recent work shows that Large Language Models (LLMs) can produce correct models from natural language, but even a correct model can be expensive to solve because performance remains sensitive to modelling choices. In this work, we investigate whether LLMs can automate performance-oriented model reformulation. Inspired by Automatic Heuristic Design (AHD), we use an evolutionary framework in which an LLM proposes candidate reformulations that are verified and benchmarked against the user-defined baseline model. We compare AHD-adapted search strategies that control which prior attempts, instructions, and measured feedback enter each prompt. Existing retention strategies prioritize recency or performance, but do not explicitly diversify the context. To cover this gap, we introduce Profile-Diverse Retention (PDR), which applies Maximal Marginal Relevance (MMR) to instance-level runtime vectors to retain behaviourally diverse attempts. We systematically evaluate the strategies on eight CSPLib problems using validation-based final model selection. The results show that: (i) iterative reformulation can produce substantial held-out speedups; (ii) strategies that keep the retained context diverse outperform those that retain only recent or the fastest attempts; and (iii) validation-based selection improves the held-out speedup of every strategy.

</details>


### [224] [MemHarness: Memory Is Reconstructed, Not Replayed](https://arxiv.org/abs/2607.28272)
*Rong Wu,Daocheng Fu,Licheng Wen,Xuemeng Yang,Shu Zou,Jianbiao Mei,Yuxin Wang,Hairong Zhang,Yu Yang,Tao Hu,Cong Zhang,Botian Shi,Pinlong Cai*

Main category: cs.AI

TL;DR: 提出MemHarness框架，让LLM智能体基于当前情境主动重构检索到的历史经验，而非机械回放，通过GRPO端到端训练提升决策性能。

Research relevance: 弱相关（1-2分）。该论文聚焦于大语言模型（LLM）智能体的记忆增强与经验重构机制，属于自然语言处理/强化学习领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。唯一的间接联系在于其"基于当前情境重构历史经验"的思想可能对遥感目标检测中的知识蒸馏或增量学习有一定类比启发，但整体与本研究方向基本无关。

Reading priority: 1分。该论文属于LLM agent记忆增强方向，与遥感卫星在轨目标检测及轻量化研究基本无关，不建议优先阅读。除非对LLM-based决策或增量学习中的经验重构机制有跨领域兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM agent决策任务（memory-augmented agent）；应用场景：ALFWorld和WebShop等文本交互环境；传感器类型：不适用（非遥感传感器）；目标类别：不适用（非视觉目标检测）。

Model architecture: MemHarness框架：包含一个统一的policy model，在每个决策步骤对检索到的历史经验进行critique和reconstruct，生成context-grounded guidance后再执行动作。训练采用GRPO（Group Relative Policy Optimization）进行端到端优化。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法，也不涉及轻量网络设计。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标，且其研究对象为LLM agent，与星上/边缘部署场景无关。

Datasets and metrics: 数据集：ALFWorld和WebShop（均为LLM agent基准环境，非遥感数据集）；评价指标：摘要未具体说明指标名称，仅提及相比pure RL和static memory-augmented baselines有显著提升，并在OOD场景下表现鲁棒。

Experiments: 在ALFWorld和WebShop上对比了pure RL和static memory-augmented baselines，MemHarness在标准场景和OOD场景下均表现更优。分析实验表明reconstruction objective不仅能防止negative transfer，还能在训练中作为latent guidance，提升agent的内在推理能力。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于NLP/RL领域；2. 摘要未说明模型规模、推理开销、部署可行性；3. 仅在文本交互环境验证，未涉及视觉或遥感数据；4. 摘要未说明在真实复杂场景下的泛化能力。

Ideas for my research: 尽管论文与遥感目标检测无直接关系，但其核心思想"基于当前情境重构历史经验而非机械回放"可类比迁移到遥感目标检测的增量学习或知识蒸馏场景：例如在星上模型持续学习时，可基于当前遥感图像分布动态重构/加权历史样本或教师模型输出，避免negative transfer。此外，"reconstruction作为latent guidance"的思想可启发设计更有效的蒸馏损失函数。但这些仅为间接类比，借鉴价值有限。

Abstract: Retrieving past experiences has become a common strategy to enhance large language model agents. However, most existing memory-augmented agents treat retrieved experiences as static records to be replayed verbatim, injecting them into the context regardless of whether they align with the agent's current situation. This ``replay'' paradigm ignores the gap between the abstract, general nature of stored experience and the concrete, ever-changing states encountered at decision time, frequently causing negative transfer. In contrast, humans rarely recall past experiences verbatim; instead, they reorganize and adapt retrieved memories to fit the present context. Inspired by this, we propose MemHarness, a framework that equips LLM agents to actively harness and reconstruct past experiences based on the present context. At each decision step, a unified policy model critiques and reconstructs the retrieved experience conditioned on the current state, producing context-grounded guidance before acting. This reconstructive ability emerges naturally through end-to-end training with GRPO. Experiments on ALFWorld and WebShop show that MemHarness substantially outperforms pure RL and static memory-augmented baselines, demonstrating strong robustness in out-of-distribution (OOD) scenarios. Furthermore, our analyses reveal that this reconstruction objective not only prevents negative transfer but also serves as latent guidance during training, fundamentally improving the agent's intrinsic reasoning capabilities.

</details>


### [225] [Tycho: Active Abstraction with Programmatic World Models for ARC-AGI-3](https://arxiv.org/abs/2607.28287)
*Jens Lehmann,Andrei Aioanei,Sahar Vahdati*

Main category: cs.AI

TL;DR: 本文提出Tycho编码智能体系统，将ARC-AGI-3抽象推理任务形式化为参数化渲染的确定性Moore机，通过主动抽象（active abstraction）策略在交互中构建、测试、修复或绕过游戏模型，以提升相对人类动作效率（RHAE）。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于通用抽象推理（ARC-AGI-3）和编码智能体（coding agent）的动作效率优化，属于通用人工智能/强化学习/程序合成领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。轻量化方面仅涉及"动作效率"（action efficiency）和推理预算（inference budgets），并非模型压缩或部署优化。相关性评分：1分。

Reading priority: 1分。论文属于通用AI/编码智能体领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。除非对"有限预算下的自适应推理决策"这一抽象概念感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：抽象推理与技能获取（abstraction and skill acquisition），具体为ARC-AGI-3基准测试中的游戏规则推断、隐藏状态推理与目标识别。应用场景：通用编码智能体（coding agent）在交互式环境中的决策。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: Tycho系统由编码智能体（coding agent）构成，核心设计包括：(1) 将环境形式化为参数化渲染的确定性Moore机（parameterized rendered deterministic Moore machines）；(2) 从结构化历史中分离可操作的观测（actionable observations）与中间动画、关卡完成、游戏结束帧；(3) 支持对自由形式可执行假设（free-form executable hypothesis）进行建模、测试、规划、修复或绕过。采用四种编排策略（orchestration policies）进行对比，其中actor-requested delegation策略表现最佳。

Lightweight method: 摘要未说明传统意义上的模型压缩方法（如pruning、quantization、knowledge distillation、NAS）。论文中的"轻量化"概念体现在动作效率（action efficiency）和推理预算（inference budgets）的约束下优化决策，即"every move counts"——在有限交互预算下最大化信息获取与任务完成效率。这与模型参数量、FLOPs、延迟等部署层面的轻量化无关。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。论文关注的是推理预算（matched inference budgets）下的动作效率（RHAE），而非边缘/星上部署可行性。不涉及FPGA/NPU/TensorRT等硬件加速或边缘部署方案。

Datasets and metrics: 数据集：ARC-AGI-3的25个公开游戏（public games），共183个关卡（levels）。评价指标：Relative Human Action Efficiency (RHAE)、game-balanced first-run human-replay midrank、scored actions数量。实验设置：每种策略在匹配推理预算下各运行一次（one matched public-set run per policy），使用Claude Opus 4.8进行策略对比，并额外测试GPT-5.6 Sol和Opus 5。

Experiments: 主要实验结果：(1) Actor-requested delegation策略获得最高平均RHAE为88.49；(2) 在该策略下，GPT-5.6 Sol和Opus 5均达到100.00 RHAE并完成全部183个关卡；(3) 两者的game-balanced first-run human-replay midrank分别为98.5和100.0；(4) Opus 5比官方人类基线少用61%的scored actions；(5) 自动修复（automatic repair）虽能更准确复现观测转移，但RHAE仅达83.07，说明"转移匹配"（transition match）不等于识别目标或改进下一步动作。摘要未说明消融实验细节。

Limitations: 摘要未说明的缺口包括：(1) 未涉及任何遥感/卫星/目标检测场景；(2) 未提供模型参数量、计算量、延迟等部署指标；(3) 未讨论模型压缩或加速方法；(4) 实验仅基于ARC-AGI-3的25个游戏，泛化性有限；(5) 依赖Claude Opus 4.8、GPT-5.6 Sol等大型闭源模型，实际推理成本与部署可行性未讨论；(6) "active abstraction"概念虽有趣，但与应用在遥感在轨场景的轻量化目标检测无直接关联。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向弱相关，但以下概念可能有间接借鉴价值：(1) "active abstraction"——在有限推理预算下决定何时构建、使用、修复或绕过模型，可类比于星上资源受限时"何时运行检测模型、何时跳过或降级"的自适应推理策略；(2) "action efficiency"思想可启发在轨场景中减少不必要的推理调用，降低能耗；(3) 将环境形式化为确定性Moore机的建模思路，可用于星上环境状态建模。但总体而言，该论文对"遥感在轨目标检测模型轻量化"课题的直接可借鉴点有限。

Abstract: ARC-AGI-3 turns abstraction into an interactive problem of skill acquisition. A player must infer an unfamiliar game's rules, hidden state, and goal while maintaining action efficiency because every move counts. We formalize these environments as parameterized rendered deterministic Moore machines and introduce Tycho, a coding-agent system that constructs and uses game-specific models during interaction. Tycho separates actionable observations from intermediate animation, level-completion, and game-over frames. From this structured history, an agent can model, test, plan with, repair, or bypass a free-form executable hypothesis.
  In one matched public-set run per policy, we compare four orchestration policies on all 25 public games using Claude Opus 4.8 under matched inference budgets. Actor-requested delegation to a model builder obtains the highest observed mean Relative Human Action Efficiency (RHAE), 88.49. With this selected policy, GPT-5.6 Sol and Opus 5 both reach 100.00 RHAE and complete all 183 levels. Their game-balanced first-run human-replay midranks are 98.5 and 100.0. Opus 5 uses 61% fewer scored actions than the aggregate official human baselines.
  Automatic repair after verification failures produces models that reproduce observed transitions much more accurately, yet reaches only 83.07 RHAE. Transition match indicates whether a simulator reproduces observed dynamics, not whether it has identified the objective or improves the next action. Strong play also requires deciding when to construct, repair, use, or bypass a model. We call this joint problem active abstraction: generating a testable model from costly interaction and deciding when acquiring or using it is worth its cost.

</details>


### [226] [One Human, $N$ Agents: Audit-Budget Allocation for LLM Agent Fleets under Miscalibrated, Correlated Confidence](https://arxiv.org/abs/2607.28317)
*Cesare Zavattari,Alessandro Tommasi,Giuseppe Prencipe*

Main category: cs.AI

TL;DR: 本文研究在有限审计预算下如何对多个LLM智能体进行置信度排序审计，提出基于两层高斯copula的有噪检查模型，并给出置信度校准失效阈值δ*的定量判据。

Research relevance: 与本研究方向基本无关（1分）。论文主题是LLM智能体审计与置信度校准，属于AI安全/机器学习理论范畴，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能，也不涉及目标检测或任何模型轻量化/部署技术。

Reading priority: 1分。论文主题为LLM智能体审计与置信度校准，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体审计（budgeted noisy inspection）；应用场景：AI安全与监督（oversight）；传感器类型：不适用；目标类别：不适用（对象是LLM agent及其置信度报告）。

Model architecture: 摘要未说明具体网络结构。核心建模工具为两层高斯copula（two-level Gaussian copula），用于刻画审计者与智能体之间的相关误差结构。

Lightweight method: 不涉及。摘要未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络或任何模型压缩/加速方法。

Onboard deployability: 不适用。摘要未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 实验对象为5个open-weight LLM和1个proprietary模型，评估其置信度报告的信息量；评价指标为置信度校准阈值δ*、审计策略排序（confidence-ranked vs random）。摘要未说明具体数据集名称。

Experiments: 主要发现：5个开源LLM的置信度近乎恒定（operationally useless），点估计位于或超过翻转阈值δ*但置信区间跨越该阈值；proprietary模型置信度具有信息量且位于阈值之下。通过回放策略于记录轨迹（replaying policies on recorded traces）验证了排序结论。

Limitations: 摘要未说明。但就本研究方向而言，该论文与遥感目标检测及轻量化完全无关，无任何可迁移的技术细节。其"置信度校准"与"审计预算分配"概念虽在广义上涉及资源受限下的决策，但与在轨计算资源约束的工程问题性质完全不同。

Ideas for my research: 借鉴意义极为有限。唯一可能的抽象启发是：在资源受限（如星上算力/功耗预算B）下如何分配有限计算资源进行目标检测的"审计/校验"，可类比为在轨检测中如何用有限推理预算优先处理高置信度/高风险区域——但这属于非常间接的类比，不构成实质性技术借鉴。

Abstract: A single human must audit $N$ LLM agents under a budget of $B \ll N$ audits per round, guided by self-reported confidence that may be adversarially miscalibrated and by correlated errors. We model this as budgeted noisy inspection over a two-level Gaussian copula and locate the miscalibration threshold $δ^*$ past which confidence-ranked auditing is \emph{worse} than random. Two a-priori expectations reverse: $δ^*$ \emph{rises} as the budget shrinks, and cross-family correlation is not low---shared difficulty dominates lineage. Five open-weight LLMs show operationally useless (near-constant) confidence, point estimates at or beyond the flip though CIs straddle it; a proprietary model is informative and lands below it. We give a quantitative criterion for \emph{vacuous} oversight, and replaying policies on recorded traces confirms the ordering.

</details>


### [227] [PathView-Bench: Can Multimodal Large Language Models Achieve Fine-grained Multiscale Understanding of Pathology Images?](https://arxiv.org/abs/2607.28318)
*Zongyi Chen,Yu Liang,Jie Lin,Liansheng Wang*

Main category: cs.AI

TL;DR: 提出PathVU，一个面向计算病理学多模态大语言模型（MLLM）的视觉锚定基准，用于评估其在多尺度病理图像上的细粒度视觉理解能力。

Research relevance: 弱相关（2分）。该论文属于计算病理学（computational pathology）领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景完全无关；不涉及目标检测、小目标检测、旋转框检测等遥感目标检测任务；也不涉及剪枝、量化、知识蒸馏、NAS等轻量化或部署方法。仅在多尺度视觉理解（multiscale visual understanding）和细粒度视觉任务评估这一方法论层面，对遥感多尺度目标检测的评估设计可能有间接借鉴意义。

Reading priority: 1分。该论文属于计算病理学领域，与遥感卫星在轨目标检测和模型轻量化方向基本无关，仅在多尺度视觉评估方法论上有极有限的间接参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态大语言模型（MLLM）的细粒度多尺度视觉理解评估（VQA-style tasks）。应用场景：计算病理学（computational pathology），包括Region FOV（高分辨率局部区域）和Slide FOV（宏观全切片视野）。传感器类型：病理切片图像（pathology images），非遥感传感器。目标类别：病理组织、细胞等，非遥感目标。

Model architecture: 摘要未说明具体模型架构。论文评估了18个代表性MLLM（包括通用型、医学领域、病理专用模型），但未给出这些模型的网络结构细节。核心贡献是基准数据集PathVU的构建与评估协议设计，而非提出新的模型架构。

Lightweight method: 不涉及。摘要中未提及任何模型压缩或加速方法，包括剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计、实时推理、边缘部署等均未涉及。

Onboard deployability: 不适用。该论文面向病理图像分析，与星上部署、边缘部署、FPGA/NPU/TensorRT等硬件加速无关。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时性指标。

Datasets and metrics: 数据集：PathVU，由23个公开病理影像数据集构建，含61,673张图像、308,070个样本、覆盖28个器官、7,253,526个标注。包含14个VQA-style任务。评价指标：程序化评分（programmatic scoring），覆盖区域定位（region localization）、视觉识别（visual recognition）、数量估计（quantity estimation）、空间推理（spatial reasoning）、上下文不足判断（insufficient-context judgment）。评估了18个代表性MLLM。

Experiments: 在18个代表性MLLM（通用型、医学领域、病理专用）上进行评估，发现即使是先进模型在多尺度病理图像的细粒度视觉任务上仍存在显著局限。摘要未给出具体数值对比结果。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关，属于病理影像领域；2. 摘要未说明评估模型的参数量、推理速度等部署指标；3. 摘要未说明PathVU基准在真实临床或部署场景中的验证情况；4. 未涉及任何模型压缩或加速技术。

Ideas for my research: 1. 多尺度视觉理解评估框架的设计思路可借鉴：PathVU将原始标注转换为确定性任务目标并实现程序化评分，类似地，遥感目标检测可构建多尺度（如不同GSD/分辨率）下的细粒度评估协议，用于检验模型在跨尺度上的检测鲁棒性；2. "上下文不足判断"（insufficient-context judgment）任务设计思路，可类比遥感中模型对遮挡、云雾、低分辨率下目标不可判场景的置信度校准评估；3. 基准构建方式（从多个公开数据集统一标注并程序化评分）对遥感多源数据集的统一评测基准设计有参考价值。但整体借鉴价值有限，因为不涉及检测模型本身和轻量化方法。

Abstract: Multimodal large language models (MLLMs) are increasingly used to analyze pathology images. However, dominant multimodal benchmarks in pathology mainly score final diagnostic answers, captions, or reports. These evaluations provide limited insight into whether a model understands the multiscale visual content needed for pathology reasoning and decision-making. We introduce PathVU, a vision-anchored benchmark for fine-grained and multiscale visual understanding in computational pathology. Built from 23 public pathology imaging datasets with human-supervised labels and spatial annotations, PathVU evaluates MLLM understanding in two fields of view: Region FOV for high-resolution local regions and Slide FOV for macro whole-slide views. By converting raw annotations into deterministic task targets, PathVU enables programmatic scoring of region localization, visual recognition, quantity estimation, spatial reasoning, and insufficient-context judgment. The benchmark contains 14 VQA-style tasks, 61,673 images, and 308,070 samples across 28 organs and 7,253,526 annotations. Evaluating 18 representative general-purpose, medical-domain, and pathology-oriented MLLMs, we observe substantial limitations even in advanced models on fine-grained visual tasks across multiscale pathology images. PathVU provides a reproducible basis for developing and evaluating pathology MLLMs with explicit multiscale visual understanding.

</details>


### [228] [Paying for Honesty Without Knowing the Truth: Reputation-Penalty Design for LLM Marketplace Agents](https://arxiv.org/abs/2607.28330)
*Mingdai Yang,Shicheng Fan,Kejing Yu,Duohao Wang,Li Sun,Hao Peng,Philip S. Yu,Zhiwei Liu*

Main category: cs.AI

TL;DR: 本文提出CARP声誉惩罚机制和SPARC代码门控反思机制，用于抑制LLM智能体作为自主商家时编造商品属性的行为，属于AI安全与机制设计领域。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文研究的是LLM智能体在电商场景中的虚假属性编造问题，属于AI安全、机制设计、博弈论范畴，不涉及遥感、卫星、目标检测或模型压缩部署。

Reading priority: 1分。论文主题为LLM智能体电商行为治理与机制设计，与遥感卫星在轨目标检测及模型轻量化方向完全无关，无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务：抑制LLM智能体在自主撰写商品列表时的属性编造行为。场景：电商/市场机制设计。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。涉及CARP（声誉惩罚机制，含deadband容错和state-dependent severity）和SPARC（byte-clean code-gated reflection机制）两个机制组件，作用于LLM merchant agents。

Lightweight method: 不涉及。摘要未提及任何剪枝、量化、知识蒸馏、NAS、轻量网络设计或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。

Datasets and metrics: 摘要未说明具体数据集。评价指标涉及consumer welfare（消费者福利）、sales volume（销量）、fabrication rate（编造率）等，均为机制设计/行为经济学指标，与检测精度、mAP、FPS等无关。

Experiments: 摘要提到跨多个模型观察到CARP+SPARC的binding效应，并给出置信区间支持，但未说明具体实验设置、对比基线细节和消融实验。

Limitations: 摘要未说明局限性。该研究聚焦LLM智能体行为治理，与遥感目标检测和模型轻量化无任何交集，其方法（声誉惩罚、代码门控反思）无法迁移到视觉检测任务。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题基本无可借鉴点。唯一可能的间接启发是：SPARC中"penalty-gated self-correction reasoning"（惩罚门控的自我修正推理）这一概念，在抽象层面类似于轻量化中通过损失函数惩罚项引导模型自我修正/压缩的思路，但关联非常牵强，不建议深入参考。

Abstract: LLM agents increasingly act as autonomous merchants that write their own product listings, and under competitive pressure, they fabricate attributes to win sales. Even under instructions to be honest, they fabricate attributes in a majority of listings across models. A platform's obvious remedy---verifying each claim against the truth---is unavailable, because it observes only a noisy, biased complaint signal, never the ground truth. We design CARP, a reputation-penalty mechanism with a deadband that forgives complaint noise and a state-dependent severity that counters reputation-driven detection erosion. CARP requires no product-level ground truth and is robust to strategic gaming. CARP protects consumers by suppressing the sales volume of low-rated liars while sparing honest sellers. Paired with SPARC, it closes most of the consumer-welfare gap relative to a perfect-information oracle, without ever accessing the truth. It also achieves the best welfare of the policies we compare. We further show that this felt penalty becomes behaviorally binding through SPARC, a byte-clean code-gated reflection mechanism: LLM merchants fabricate when lying is free but restrain themselves when fabrication costs them sales, a self-interested response rather than compliance. We trace this distinction to penalty-gated self-correction reasoning, and observe the binding across models, with supporting confidence intervals.

</details>


### [229] [Correcting What You Cannot See: Credit Assignment for Perception Distillation in Multimodal Reasoners](https://arxiv.org/abs/2607.28336)
*Feng Xiong,Leyan Xue,Hongyu Lin*

Main category: cs.AI

TL;DR: 提出 Perception-Correction Distillation (PCD)，一种无标签的多模态蒸馏方法，通过感知-推理分离的 rollout 和 teacher-student 分歧作为互补证据，识别可纠正的感知失败并强化蒸馏信号。

Research relevance: 弱相关（2分）。该论文属于多模态大模型（multimodal reasoner）的知识蒸馏方法研究，核心关注感知（perception）与推理（reasoning）的解耦与蒸馏信号优化。虽然涉及 knowledge distillation 这一轻量化技术，但场景是通用多模态推理（如视觉问答），并非遥感/卫星/在轨目标检测场景。蒸馏方法本身对轻量化有借鉴意义，但缺乏遥感目标检测和星上部署的任何信息。

Reading priority: 2分。该论文与遥感在轨目标检测轻量化方向弱相关，属于通用多模态蒸馏方法。虽然蒸馏信号设计的思路有一定启发，但场景差异大、模型规模不匹配、缺乏部署信息，优先级较低。可作为蒸馏方法论参考，但不值得优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态推理（multimodal reasoning）中的知识蒸馏。应用场景：通用视觉-语言推理（非遥感）。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（非遥感目标）。

Model architecture: 摘要未说明具体网络结构。涉及 teacher-student 蒸馏框架，学生模型规模为 2B 和 8B，教师模型规模为 8B 和 32B。采用分离的 perception-reasoning rollouts 设计，通过 PSR（Perception Success Rate）估计感知成功率，并用 teacher-student 分歧作为补充证据。

Lightweight method: 知识蒸馏（knowledge distillation），具体为 on-policy distillation 的改进。PCD 通过软 AND 门（soft AND gate）机制，仅在感知失败可纠正（即同时存在下游失败和 teacher-student 分歧）时强化蒸馏信号。使用 mean-preserving weights 保持推理目标不变。属于蒸馏信号设计的创新，而非剪枝/量化/NAS 等压缩手段。

Onboard deployability: 摘要未说明。论文未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。虽然蒸馏本身是模型压缩手段，但该论文关注的是蒸馏质量而非部署效率，且模型规模（2B-32B）远超星上可部署的规模。

Datasets and metrics: 使用 8 个 benchmark 进行评测。关键指标为 macro average：8B→2B 蒸馏从 OPD 的 44.50 提升到 PCD 的 47.28；32B→8B 从 56.94 提升到 61.22。消融实验中，移除 PCD 使 held-out average 下降 2.22 分，移除 separated rollout 下降 0.88 分。具体数据集名称摘要未说明。

Experiments: 在 8 个 benchmark 上对比 PCD 与 OPD（on-policy distillation）。主要结果：8B→2B 提升 2.78 分（44.50→47.28），32B→8B 提升 4.28 分（56.94→61.22）。2B 匹配消融实验验证了 PCD 和 separated rollout 各自的有效性。通过 Bayesian evidence combination 论证了乘法作为唯一归一化双线性门（normalized bilinear gate）的合理性。

Limitations: 1. 场景为通用多模态推理，与遥感/卫星目标检测无直接关联；2. 模型规模（2B-32B）远大于星上可部署的轻量模型；3. 摘要未说明计算开销、训练成本、推理延迟等部署相关信息；4. 未涉及遥感图像特性（如小目标、旋转框、SAR/高光谱等）；5. PSR 本身仍存在歧义性，论文通过补充证据缓解但未完全解决。

Ideas for my research: 1. 蒸馏信号设计思路可迁移：在遥感目标检测的 teacher-student 蒸馏中，可借鉴"区分可纠正错误与不可纠正错误"的思想——例如区分检测失败源于特征提取不足（感知）还是后处理/分类错误（推理），从而针对性强化蒸馏；2. soft AND gate 的 Bayesian 论证框架可用于设计遥感场景下多证据融合的蒸馏权重；3. separated rollout 思想可类比为遥感检测中分离 backbone 蒸馏与 head 蒸馏，分别优化感知与决策模块；4. 但需注意：遥感目标检测的蒸馏通常关注特征图对齐（如 feature-based distillation），与本文的 label-free 多模态蒸馏差异较大，借鉴时应聚焦方法论而非直接套用。

Abstract: On-policy distillation provides dense supervision for multimodal reasoners, but its trajectory-level reward cannot determine whether a failed answer arose from perception or subsequent reasoning. Perception Success Rate (PSR), estimated from multiple reasonings sharing one perception, remains ambiguous because low success conflates perceptual insufficiency with reasoning difficulty. We introduce \textbf{Perception-Correction Distillation (PCD)}, a label-free method that identifies correctable perception failures using downstream failure and teacher--student disagreement as complementary witnesses. Their product, , forms a soft AND gate that strengthens distillation only when both witnesses are present. We motivate this rule through Bayesian evidence combination and show that multiplication is the unique normalized bilinear gate that vanishes when either witness is absent. PCD uses separated perception--reasoning rollouts and mean-preserving weights, leaving the reasoning objective unchanged. Across eight benchmarks, PCD improves the 8B 2B macro average from 44.50 with OPD to 47.28 and the 32B 8B result from 56.94 to 61.22. In matched 2B ablations, removing PCD and separated rollout reduces held-out average by 2.22 and 0.88 points, respectively. Effective multimodal distillation therefore depends not only on what the teacher predicts, but also on identifying when perception is the appropriate target of correction.

</details>


### [230] [How Benchmarks Mis-Score Computer-Use Agents](https://arxiv.org/abs/2607.28367)
*Zihan Dong,Zhiyuan Ma,Zekun Wang,Yunqing Li,Zirou Liu,Ruixuan Deng,Qishi Zhan,Rui Qian*

Main category: cs.AI

TL;DR: 本文提出一个针对计算机使用智能体（CUA）基准测试的可靠性评估框架，审计了150个失败轨迹，发现15.3%的FAIL判定是错误的，并提出了三层次诊断分类法来区分失败原因。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于计算机使用智能体（Computer-use agents）的基准测试可靠性评估，属于人机交互、智能体评估和软件工程领域，不涉及遥感、卫星、目标检测或模型压缩/加速。相关性评分为1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于CUA智能体评估领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：CUA（Computer-use agents）基准测试可靠性审计与评估框架设计。应用场景：网页浏览、企业工作流、桌面软件控制。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体模型架构。论文关注的是评估CUA（Computer-use agents）的基准测试管线（pipeline），包括任务构建（task construction）、轨迹观测（trajectory observation）、评分（scoring）和报告（reporting）四个环节。

Lightweight method: 不涉及。论文未讨论任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计方法。

Onboard deployability: 不涉及。论文未讨论星上部署、边缘部署、参数量、FLOPs、延迟、显存、能耗或实时推理等部署相关信息。

Datasets and metrics: 审计了150个公开的失败评分轨迹，来自5个基准测试（web、enterprise-workflow、desktop-control三类）。评价指标包括FAIL判定错误率（15.3%总体错误，其中10.7%为评估器假阴性，4.7%为任务本身损坏），以及三层次诊断分类法。

Experiments: 主要实验发现：15.3%的FAIL判定是错误的（10.7%评估器假阴性 + 4.7%损坏任务）；在真实失败中，验证/反馈和规划错误占主导，而非执行/接地错误；单一标量成功率无法解释失败原因。论文还将其发现与更新的长时程CUA基准测试联系起来，提出了分阶段的设计规则。

Limitations: 摘要未说明局限性。但明显与遥感目标检测和模型轻量化方向完全无关，属于智能体评估领域的研究。其核心关注点是基准测试的可靠性，而非模型性能或部署效率。

Ideas for my research: 借鉴意义非常有限。唯一可能间接相关的点是：论文强调"评估指标可靠性"和"诊断失败原因"的思路，可以类比到遥感目标检测模型评估中——即单一mAP指标可能无法全面反映模型在轨部署时的真实性能，需要考虑多维度评估（如不同场景、不同目标尺度、不同光照条件下的分项诊断）。但这种借鉴非常间接。

Abstract: Computer-use agents (CUA) are being deployed to browse the web and operate desktop software, yet their benchmark scores are still commonly produced by brittle scripted oracles. A score is the output of a pipeline in which tasks can be stale, trajectories can omit decisive visual evidence, evaluators can reject valid alternatives, and aggregate reports can hide the cause of failure. We organize these problems into a reliability framework spanning task construction, trajectory observation, scoring, and reporting. We then audit 150 public failure-scored trajectories from five web, enterprise-workflow, and desktop-control benchmarks, find that 15.3\% of FAIL verdicts are wrong: 10.7\% are evaluator false negatives and 4.7\% are broken tasks. For genuine failures, a three-tier diagnostic taxonomy shows that verification/feedback and planning failures dominate execution/grounding errors, while a single scalar success rate can not explain. We connect these findings to newer long-horizon CUA benchmarks and derive stage-specific design rules for CUA evaluation.

</details>


### [231] [HyperClaim: Fine-Grained Cross-Modal Hypergraph Reasoning for Video Misinformation Detection](https://arxiv.org/abs/2607.28375)
*Xiangbo Wang,Jiasheng Zhang,Xingtong Yu,Luoqiang Lei,Delvin Ce Zhang*

Main category: cs.AI

TL;DR: 提出HyperClaim，一个判别式时序超图框架，用于视频虚假信息检测中的样本级真实性分类，通过稀疏异构超图建模查询词、证据文本与视频帧之间的高阶跨模态依赖。

Research relevance: 弱相关（2分）。该论文属于泛视觉/多模态机器学习方向，聚焦视频虚假信息检测（video misinformation detection），与遥感、卫星、Earth observation、SAR、高光谱、多光谱、空间在轨智能均无直接关联。虽然涉及视频帧（frames）的视觉处理，但目标不是遥感目标检测，也不涉及任何轻量化或边缘部署技术。其超图建模高阶跨模态依赖的方法论对多模态特征交互有一定借鉴意义，但整体与"遥感卫星在轨目标检测与模型轻量化"方向相关性很弱。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于视频多模态理解领域。仅当对高阶跨模态特征交互建模（超图）感兴趣时，可作为方法论参考，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视频虚假信息检测（video misinformation detection），样本级真实性分类（sample-level authenticity classification）。应用场景：社交媒体/新闻视频真实性判别。传感器类型：视频帧（frames）+ 文本（title/paired text）。目标类别：真实/虚假二分类。与遥感目标检测无关。

Model architecture: HyperClaim框架：1) 构建稀疏异构超图（sparse heterogeneous hypergraph），节点包括query tokens、evidence tokens和sampled frames；2) 置信度感知过滤（confidence-aware filtering）和源预算（source budgeting）形成紧凑的文本-帧和短时域证据单元；3) 自适应软关联推理（adaptive soft-incidence reasoning）配合残差文本-视频校准（residual text-video calibration）；4) 通过差异感知读出（discrepancy-aware readout）聚合文本、视觉和超边状态。不依赖生成式rationales或外部工具调用。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法，也未提及实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等部署相关技术。

Onboard deployability: 摘要未说明。未报告参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）等任何部署相关指标。由于该框架涉及超图构建与高阶推理，计算复杂度可能较高，但摘要未提供任何效率数据。

Datasets and metrics: 数据集：FakeSV、FakeTT、FakeVV三个视频虚假信息检测基准数据集。评价指标：准确率（accuracy）。实验协议：FactGuard temporal protocol。在FakeSV、FakeTT、FakeVV上分别达到83.7%、82.0%、87.3%的准确率。

Experiments: 在三个数据集上均优于强判别式基线（discriminative baselines）和推理中心基线（reasoning-centric baselines）。学习到的关联权重（incidence weights）和注意力权重揭示了token级和帧级结构。摘要未提供消融实验细节、对比方法的具体名称和数值。

Limitations: 1) 与遥感/卫星/在轨目标检测完全无关，属于视频虚假信息检测领域；2) 摘要未说明模型复杂度、推理速度、参数量等部署关键信息；3) 超图构建和自适应软关联推理的计算开销可能较大，不利于资源受限的星上部署；4) 摘要未说明泛化能力、跨域迁移能力；5) 未提供消融实验细节。

Ideas for my research: 1) 超图（hypergraph）建模高阶跨模态/跨目标交互的思想可借鉴：在遥感多光谱/高光谱目标检测中，不同波段特征与空间上下文之间可能存在高阶依赖，超图可作为一种替代pairwise attention的特征交互建模方式；2) 置信度感知过滤（confidence-aware filtering）和源预算（source budgeting）的思路可用于遥感小目标检测中的候选框/特征筛选，减少冗余计算；3) 差异感知读出（discrepancy-aware readout）可借鉴用于多尺度特征融合时的信息聚合策略；4) 但需注意：该框架本身计算开销较大，若借鉴到在轨场景需配合轻量化手段（如剪枝、蒸馏）进行压缩。

Abstract: Video misinformation detection is often approached through global multimodal fusion or free-form multimodal reasoning. Both paradigms can under-represent localized authenticity cues that arise from coupled interactions among query phrases, contextual text, and short temporal spans of frames. Because such interactions are inherently higher-order, pairwise graph formulations are insufficient to capture multi-way cross-modal dependencies, whereas hypergraphs offer a suitable representation for these relations. We propose HyperClaim, a discriminative temporal hypergraph framework for sample-level authenticity classification. Using the title or benchmark-provided paired text as a claim-like query, HyperClaim constructs a sparse heterogeneous hypergraph over query tokens, evidence tokens, and sampled frames; applies confidence-aware filtering and source budgeting to form compact text-frame and short-range temporal evidence units; performs adaptive soft-incidence reasoning with residual text-video calibration; and aggregates textual, visual, and hyperedge states through a discrepancy-aware readout. Without relying on generated rationales or external tool calls, HyperClaim preserves fine-grained cross-modal and temporal structure that global fusion tends to flatten. Under the FactGuard temporal protocol, it achieves 83.7%, 82.0%, and 87.3% accuracy on FakeSV, FakeTT, and FakeVV, respectively, outperforming strong discriminative and reasoning-centric baselines. Learned incidence and attention weights further reveal token- and frame-level structure.

</details>


### [232] [When Specifications Conflict: A Symmetry-Based Framework for Measuring LLM Preferences](https://arxiv.org/abs/2607.28384)
*Tairan Wang,Liang Zhou,Zikang Zhan,Pingchuan Yan*

Main category: cs.AI

TL;DR: 提出一个可控实验框架，用于研究大语言模型（LLM）在面对相互冲突的规格说明时的偏好选择模式，并通过数学基准测试和多种领域验证框架的通用性。

Research relevance: 1分。该论文研究的是大语言模型（LLM）在冲突规格说明下的偏好行为分析，属于自然语言处理与机器学习可解释性方向，与遥感、卫星、目标检测、轻量化部署均无任何关联。

Reading priority: 1分。该论文研究LLM冲突规格偏好分析，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM冲突规格偏好分析（conflict resolution preference analysis）。应用场景：数学基准测试、布尔代数、代码生成、临床领域。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。框架基于可控实验设计（controlled experimental framework），采用对称性设计（symmetry-based design）减少混淆因素，比较四种表示类型：纯自然语言（pure natural language）、形式语言（formal language）、自然化形式语言（naturalized formal language）、输入-输出示例（input-output examples）。

Lightweight method: 不涉及。该论文不包含任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计相关内容。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：可执行的数学基准测试，包含550个冲突实例，覆盖11个函数族（function families）；另扩展到布尔代数、代码生成和临床领域。评价指标：偏好排序（preference ordering），即 Formal ≈ Naturalized Formal > Pure Natural Language > Input-Output Examples。

Experiments: 主要实验发现：模型在冲突规格下表现出系统性偏好模式而非随机行为；偏好排序为 Formal ≈ Naturalized Formal > Pure Natural Language > Input-Output Examples；示例效应（example effects）依赖于模型能力和函数族；框架在布尔代数、代码生成和临床领域均验证了适用性。

Limitations: 摘要未说明局限性。但该论文与遥感在轨目标检测及轻量化方向完全无关，不涉及任何视觉任务、遥感数据或模型压缩技术。

Ideas for my research: 该论文对本课题基本无可借鉴之处。唯一可能的间接启发是：其"对称性设计减少混淆因素"的实验方法论思想，在消融实验设计上或有一定参考价值，但与本课题的遥感目标检测和轻量化方向关联极弱。

Abstract: Large language models (LLMs) are increasingly required to integrate multiple sources of information that may be inconsistent or conflicting. However, there is still a lack of controllable and attributable methods for analyzing how models resolve conflicts between competing specifications. We propose a controlled experimental framework for studying model preferences under conflicting specifications. By constructing specifications with explicit conflicts, the framework enables model choices between competing specifications to be directly observed and analyzed. A symmetry-based design further reduces confounding factors, allowing preferences across representation types to be compared systematically.
  We evaluate the framework on an executable mathematical benchmark with 550 conflict instances spanning 11 function families, comparing four representation types: pure natural language, formal language, naturalized formal language, and input--output examples. Results show systematic preference patterns rather than random behavior, with a consistent ordering: $ \text{Formal} \approx \text{Naturalized Formal} > \text{Pure Natural Language} > \text{Input--Output Examples} $. Example effects further depend on model capability and function family.
  We extend the framework to heterogeneous specification conflicts in Boolean algebra, code generation, and the clinical domain, demonstrating its applicability across diverse tasks and specification forms. The framework provides a unified approach for measuring how LLMs resolve conflicts between competing sources of information.

</details>


### [233] [GLM-RAG: Graph Language Models for Graph-Based Retrieval-Augmented Generation](https://arxiv.org/abs/2607.28397)
*Maya Arseven,Anette Frank,Beni Egressy,Johann Higl,Moritz Plenz*

Main category: cs.AI

TL;DR: 提出基于图语言模型（GLM）的检索器用于知识图谱上的RAG任务，对比GLM、GNN和向量检索三类检索器在单跳/多跳推理及跨域泛化上的表现。

Research relevance: 弱相关（2分）。论文属于自然语言处理/知识图谱检索增强生成方向，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。仅可作为泛机器学习方法参考，对在轨遥感目标检测轻量化课题借鉴价值有限。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于NLP/知识图谱检索领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：知识图谱上的Retrieval-augmented generation (RAG)检索器设计；应用场景：多跳问答（multi-hop QA）与跨域泛化；传感器类型：不适用；目标类别：不适用（处理对象为知识图谱节点与语义信息）。

Model architecture: 对比了三类检索器架构：(1) GLM-based retriever（图语言模型检索器，融合图推理与语言模型语义能力）；(2) GNN-based retriever（图神经网络检索器，建模图拓扑结构）；(3) 传统vector-search-based retriever（向量检索基线）。摘要未说明具体网络层数、参数规模等细节。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏、NAS等轻量化方法，也未讨论模型压缩或推理加速策略。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标，也未讨论边缘或星上部署可行性。仅提及"promising scaling as parameters and subgraph coverage increase"，说明模型性能随参数规模增长，但未给出具体部署约束分析。

Datasets and metrics: 使用两个multi-hop benchmark数据集（摘要未说明具体数据集名称），以及in-domain multi-hop QA数据集和single-hop数据集。评价指标为检索/问答准确率（具体指标名称摘要未说明），报告了SOTA对比结果。

Experiments: 主要发现：(1) 微调后的GLM检索器在跨域（out-of-domain）泛化上表现更好，在两个multi-hop benchmark上达到SOTA；(2) 在in-domain multi-hop QA上与前人工作相当，且随参数和子图覆盖增加有良好扩展性；(3) GNN检索器在训练效率上更优且图覆盖更高；(4) 向量检索基线在single-hop数据集上表现最佳。摘要未说明具体数值结果和消融实验细节。

Limitations: 与本研究方向（遥感卫星在轨目标检测与轻量化）基本无关。主要局限：(1) 领域完全不匹配——面向NLP/知识图谱而非遥感图像；(2) 无任何目标检测、遥感场景理解相关内容；(3) 无轻量化、部署、实时性讨论；(4) 摘要未提供具体数据集名称、指标数值、模型规模等可复现细节。

Ideas for my research: 借鉴价值有限，但可提取两点泛化思路：(1) GLM融合图结构与语义信息的思路，若类比到遥感领域，可考虑将遥感图像的空间拓扑关系（如目标间相对位置、场景上下文）与检测特征融合，提升小目标检测的上下文感知能力；(2) 跨域泛化（out-of-domain transferability）的评估视角，可借鉴到遥感目标检测中不同传感器/不同分辨率/不同地域数据间的泛化评估。但整体上对"在轨轻量化"这一核心课题的直接帮助很小。

Abstract: Retrieval-augmented generation (RAG) over knowledge graphs requires retrievers that can effectively capture both graph structure and semantic information. Recent approaches have explored graph neural network (GNN)-based retrievers to model graph topology in multi-hop reasoning tasks. In parallel, graph language models (GLMs) have emerged as a promising paradigm that integrates graph reasoning and the semantic capabilities of language models. In this work, we introduce a GLM-based retriever and investigate the comparative strengths of GLM-based, GNN-based, and traditional vector-search-based retrievers in single- and multi-hop RAG settings, and with a particular focus on transferability to unseen domains. Our findings suggest that finetuned GLM retrievers generalize better out of domain, achieving SOTA on two multi-hop benchmarks. On in-domain multi-hop QA datasets they remain comparable to prior work, with promising scaling as parameters and subgraph coverage increase. GNN-based retrievers achieve higher graph coverage with an efficient training setup, whereas the vector-search baseline excels at single-hop datasets.

</details>


### [234] [QuantWAMs: Calibrating at the Right Granularity for World Action Models](https://arxiv.org/abs/2607.28405)
*Jiacheng Zhou,Jinfan Lv,Ruixuan Li,Longtai Zhang,Yan Wang,Wenqiang Zhang,Lizhe Qi*

Main category: cs.AI

TL;DR: QuantWAMs 是一个面向世界动作模型（World Action Models, WAMs）的后训练量化（PTQ）框架，通过共享基异常值校准、联合训练目标显著性分析和固定干预 rollout 审计三种策略，在 W4A4 量化设置下将峰值权重-激活内存降至 FP16 的约 29%，并提供 1.4–1.6× 的块级加速。

Research relevance: 弱相关（2分）。论文涉及 post-training quantization (PTQ) 这一模型压缩/轻量化技术，且关注部署可行性（deployment feasibility），但研究对象是 World Action Models（世界动作模型，面向机器人操作/视频-动作联合预测），并非遥感/卫星场景，也不涉及 remote sensing、object detection、aerial image、SAR 等遥感目标检测内容。其量化方法论（如 outlier calibration、Fisher-based saliency、rollout auditing）对轻量化部署有方法论借鉴意义，但场景完全不相关。

Reading priority: 2分。论文的 PTQ 方法论（异常值校准、Fisher-based 精度分配、rollout 审计）对轻量化量化策略设计有一定借鉴意义，但研究对象是机器人世界模型而非遥感目标检测，场景差异大，且未提供遥感相关实验。可作为量化方法论的补充阅读，但优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：World Action Models (WAMs) 的量化压缩与高效部署，涉及未来观测与动作的联合预测（jointly predict future observations and actions）。应用场景：机器人操作（real-robot manipulation）、仿真环境（RoboTwin 2.0、LIBERO）。传感器类型：视频/视觉观测（video observations）与动作信号（actions）。目标类别：机器人操作任务，非遥感目标检测。摘要未说明遥感、卫星、aerial image、SAR、高光谱、多光谱等遥感相关内容。

Model architecture: 研究对象为 World Action Models (WAMs)，其核心特点是迭代去噪（iterative denoising）与闭环执行（closed-loop execution）。评估的模型包括 Fast-WAM 和 LingBot-VA。QuantWAMs 框架本身包含三个策略模块：(1) shared-basis outlier calibration（共享基异常值校准），仅在坐标兼容模块间池化激活证据；(2) co-training-objective saliency（联合训练目标显著性），从视频-动作联合梯度计算 empirical-Fisher scores，在校准稳定的层粒度分配权重精度；(3) fixed-intervention rollout auditing（固定干预 rollout 审计），在不改变精度预算的前提下修订去噪步保护调度。摘要未说明具体网络结构细节（如 backbone、transformer 层数等）。

Lightweight method: 核心方法是 post-training quantization (PTQ)，具体为 W4A4 主导的量化设置（权重4bit、激活4bit）。QuantWAMs 提出三种量化策略：shared-basis outlier calibration（共享基异常值校准）、co-training-objective saliency（基于 empirical-Fisher scores 的联合训练目标显著性分析）、fixed-intervention rollout auditing（固定干预 rollout 审计）。这些方法针对 WAMs 的迭代去噪和闭环执行特性设计，区别于传统依赖 open-loop objectives 和 homogeneous model assumptions 的 PTQ 方法。摘要未涉及 pruning、knowledge distillation、NAS 等其他轻量化手段。

Onboard deployability: 摘要提供了部分部署相关信息：在 W4A4 主导设置下，QuantWAMs 将目标视频和动作块的峰值权重-激活内存（peak weight-and-activation memory）降至 FP16 的约 29%，并提供 1.4–1.6× 的块级加速（block-level speedups）。在真实机器人（AgiBot G2）上完成了三个操作任务的部署可行性验证。但摘要未说明：总参数量、总 FLOPs、端到端延迟、显存占用、能耗、实时性指标、是否适配 FPGA/NPU/TensorRT 等边缘/星上部署硬件。由于场景是机器人操作而非星上部署，未涉及在轨推理相关约束。

Datasets and metrics: 评估数据集/环境：RoboTwin 2.0、LIBERO、真实机器人操作（AgiBot G2）。评估模型：Fast-WAM 和 LingBot-VA。评价指标：仿真环境下与 FP16 的均值差异（simulation means differ from FP16 by 0.2–0.7 percentage points）、峰值权重-激活内存、块级加速比。摘要未说明具体检测/任务精度指标（如 mAP、IoU 等），因为这不是目标检测任务。

Experiments: 在 Fast-WAM 和 LingBot-VA 两个模型上，于 RoboTwin 2.0、LIBERO 和真实机器人（AgiBot G2）三个场景进行验证。W4A4 主导设置下，仿真均值与 FP16 相差仅 0.2–0.7 个百分点，说明量化精度损失很小。真实机器人实验验证了三个操作任务的部署可行性。内存降至 FP16 的约 29%，块级加速 1.4–1.6×。摘要未说明与现有 PTQ 方法的详细对比表格、消融实验细节。

Limitations: 1. 场景为机器人操作（World Action Models），与遥感/卫星目标检测完全无关，无法直接迁移到遥感在轨检测任务。2. 摘要未说明总参数量、FLOPs、端到端延迟、能耗等关键部署指标。3. 加速比仅为块级（block-level），未给出端到端推理加速。4. 未涉及遥感图像特性（如小目标、旋转框、复杂背景、多光谱/SAR 数据分布）。5. 未说明量化对检测精度（如 mAP）的影响，因为任务不是目标检测。6. 未涉及剪枝、蒸馏、NAS 等其他轻量化方法的组合。

Ideas for my research: 1. shared-basis outlier calibration 思想可借鉴：在遥感目标检测模型中，不同层/不同特征金字塔层（FPN）的激活分布差异大，可借鉴"仅在坐标兼容模块间池化激活证据"的思路，设计针对遥感多尺度特征的异常值校准策略。2. co-training-objective saliency（基于 empirical-Fisher scores 的精度分配）可迁移到遥感检测模型：对检测损失（分类+回归）联合梯度计算 Fisher 信息，在层粒度分配量化精度，尤其适用于小目标检测中回归分支对精度更敏感的场景。3. fixed-intervention rollout auditing 思想可类比到遥感在轨场景：通过"固定干预"方式在闭环/在线推理中审计量化保护调度，可借鉴为在轨推理时的动态精度调整策略。4. W4A4 量化在保持精度（0.2–0.7pp 差异）的同时将内存降至 29%，说明激进量化在复杂生成式模型上可行，对遥感检测模型的 4-bit 量化有参考价值。5. 需注意：该方法的 rollout/闭环审计依赖环境交互，遥感在轨场景为开环推理，需改造为基于数据分布或任务目标的静态审计策略。

Abstract: World Action Models (WAMs) jointly predict future observations and actions, but their iterative denoising and closed-loop execution make efficient deployment costly. Existing post-training quantization (PTQ) methods are poorly suited to WAMs because they rely on open-loop objectives, homogeneous model assumptions, and calibration distributions that do not reflect deployment. We present QuantWAMs, a PTQ framework that aligns quantization decisions with the calibration context defined by model structure, rollout distribution, and task objective. QuantWAMs introduces three strategies: shared-basis outlier calibration, which pools activation evidence only across coordinate-compatible modules; co-training-objective saliency, which computes empirical-Fisher scores from the joint video--action gradient and assigns weight precision at a calibration-stable layer granularity; and fixed-intervention rollout auditing, which revises denoising-step protection schedules using reachable closed-loop states without changing the precision budget. We evaluate QuantWAMs on Fast-WAM and LingBot-VA across RoboTwin 2.0, LIBERO, and real-robot manipulation with an AgiBot G2. Under a W4A4-dominant setting, the reported simulation means differ from FP16 by 0.2--0.7 percentage points. Real-robot trials further establish deployment feasibility on three manipulation tasks. For the targeted video and action blocks, QuantWAMs reduces peak weight-and-activation memory to about 29\% of FP16 and provides 1.4--1.6$\times$ block-level speedups.

</details>


### [235] [WIDE: Boosting Adaptive LLM Inference via Token-level Dynamic Width Pruning](https://arxiv.org/abs/2607.28418)
*Haozhe Hu,Hao Wu,Peiran Yin,Chao Han,Yunpu Ma,Xiaoyu Shen*

Main category: cs.AI

TL;DR: 提出WIDE，首个端到端可微的token级动态宽度剪枝框架，用于LLM的prefill和decode场景，通过token动态选择attention-head组和FFN-channel组实现细粒度计算分配，并配合剪枝-内核协同设计实现实际加速。

Research relevance: 弱相关（2分）。论文聚焦于大语言模型（LLM）的动态宽度剪枝，属于泛机器学习/泛NLP方向的模型压缩方法。虽然剪枝（pruning）与轻量化方向相关，但完全不涉及遥感、卫星、目标检测等场景，属于泛视觉/泛机器学习方法，对遥感在轨目标检测的借鉴意义有限。

Reading priority: 2分。虽然剪枝方法本身有借鉴价值，但论文完全面向LLM推理场景，与遥感卫星在轨目标检测无直接关联。仅当需要了解动态剪枝与内核协同设计的最新进展时值得一读，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM推理加速（prefill和decode场景）；应用场景：大语言模型推理；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: WIDE框架：token级动态宽度剪枝，允许每个token动态选择attention-head组和FFN-channel组，将动态剪枝从layer级扩展到neuron-block级粒度。采用两阶段训练pipeline学习token-wise稀疏执行模式。剪枝-内核协同设计将动态稀疏加速分解为mask重排序、硬件无关的block级跳过、硬件相关的intra-block跳过三个层次。

Lightweight method: 动态结构化剪枝（dynamic structured pruning）：token级动态宽度剪枝，区别于静态结构化剪枝和粗粒度动态剪枝。核心创新在于：(1) 端到端可微的token级动态宽度选择；(2) 两阶段训练pipeline；(3) 剪枝-内核协同设计（pruning-kernel co-design）实现实际加速。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。仅报告了推理加速数据：50%稀疏度下相比SOTA动态深度剪枝提升55.1%性能；prefill场景kernel级加速最高1.98x，decode场景最高4.95x；端到端加速分别为1.68x和1.55x。这些加速数据针对LLM推理场景，与星上部署的硬件约束（FPGA/NPU）不直接相关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为质量保持（quality retention）、性能提升百分比、kernel级和端到端加速比（speedup）。实验设置包括calibration-only设置和prefill/decode推理负载。

Experiments: 在50%稀疏度下，WIDE相比SOTA动态深度剪枝在calibration-only设置下提供55.1%的性能提升；prefill场景kernel级加速最高1.98x，decode场景最高4.95x；端到端加速分别为1.68x（prefill）和1.55x（decode）。代码已开源。

Limitations: 摘要未说明：具体实验数据集、模型规模、与静态剪枝的详细对比、在真实硬件（如GPU型号）上的部署细节、能耗数据。此外，该方法针对LLM的token级动态剪枝，其token动态选择机制与遥感目标检测的静态图像推理场景差异较大，直接迁移性有限。

Ideas for my research: 尽管场景不同，但以下思路可借鉴：(1) 剪枝-内核协同设计（pruning-kernel co-design）思想——将稀疏加速分解为mask重排序、block级跳过、intra-block跳过，这种分层加速策略可迁移到遥感目标检测模型在FPGA/NPU上的部署；(2) 端到端可微的剪枝训练pipeline（两阶段训练）可用于遥感检测模型的轻量化训练；(3) 动态计算分配（input-adaptive computation）思想可启发遥感场景下的自适应推理——对不同复杂度的遥感图像动态分配计算资源；(4) 细粒度（neuron-block级）动态剪枝思路可借鉴到CNN检测骨干网络的通道级动态剪枝。

Abstract: Pruning is a promising approach for improving the efficiency of LLMs. Existing static structured pruning methods are hardware-friendly and can deliver practical throughput gains, but their input-agnostic computation allocation often causes substantial accuracy degradation under aggressive sparsity. Recent dynamic sparsity methods improve quality retention by adapting computation to individual inputs, yet they remain largely limited to coarse-grained structural decisions and their practical acceleration under real-world inference scenarios remains challenging. To address these challenges, we present WIDE, the first end-to-end differentiable token-level dynamic width pruning framework designed for both prefill and decode scenarios. WIDE enables fine-grained computation allocation by allowing each token to dynamically select attention-head groups and FFN-channel groups, extending dynamic pruning beyond layer-level decisions to neuron-block-level granularity. Through a two-stage training pipeline, WIDE learns effective token-wise sparse execution patterns and achieves substantially better quality retention than existing approaches. To make such fine-grained dynamic pruning practical, we further propose a pruning--kernel co-design framework that decomposes dynamic sparsity acceleration into mask reordering, hardware-agnostic block-level skipping, and hardware-dependent intra-block skipping, enabling efficient execution across different granularities. At 50% sparsity, WIDE provides 55.1% performance boost when compared to the state-of-the-art dynamic depth pruning under calibration-only settings. Under prefill and decoding inference workloads, WIDE achieves close-to-theoretical kernel-level speedups of up to 1.98x for prefill and 4.95x for decoding, as well as 1.68x and 1.55x end-to-end acceleration. Our code is available at https://github.com/EIT-NLP/LLM-Pruning/tree/main/WIDE.

</details>


### [236] [When Derived Measurements Mislead: Quantifying and Mitigating LLM Over-Trust with Privileged-Modality Reliability Evidence](https://arxiv.org/abs/2607.28421)
*Zongheng Guo,Tao Chen,Tianli Li,Mingzhe Cui,Yang Jiao,Lei Xie,Yi Pan,Xiao Hu,Manuela Ferrario*

Main category: cs.AI

TL;DR: 本文提出"派生特征过度信任"(DFOT)框架，研究LLM管道中派生测量值被当作直接事实使用的问题，以生理传感(PPG-ECG)为案例，用ECG-to-PPG特权蒸馏作为基线方法进行量化评估。

Research relevance: 1分，基本无关。该论文聚焦于大语言模型(LLM)管道中派生测量值的信任评估问题，以生理传感(PPG-ECG)为应用场景，与遥感、卫星、在轨目标检测、轻量化部署均无直接关联。虽然涉及"蒸馏"(distillation)概念，但这里的"privileged distillation"是生理信号领域的特权信息蒸馏，与模型压缩/知识蒸馏用于轻量化部署的目标完全不同。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM可信度评估与生理信号处理交叉领域。除非对"特权蒸馏"这一概念在遥感场景的迁移感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM管道中派生测量值的可信度评估与修复；应用场景：生理传感(PPG心率信号与ECG心电图对照)；传感器类型：PPG(光电容积脉搏波)、ECG(心电图)；目标类别：无目标检测任务，涉及的是生理信号分类/评估。

Model architecture: 摘要未说明具体模型结构。涉及LLM管道、ECG-to-PPG特权蒸馏(privileged distillation)作为基线、PPG-only推理。核心是DFOT评估框架而非特定网络架构。

Lightweight method: 摘要未说明传统意义上的模型压缩方法。涉及"ECG-to-PPG privileged distillation"，但这是利用ECG作为特权信息辅助训练PPG推理模型，属于知识蒸馏的一种形式，但目标不是模型轻量化，而是提升PPG-only推理的可靠性。

Onboard deployability: 摘要未说明。无参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。该工作面向LLM推理管道，与星上/边缘部署无关。

Datasets and metrics: 数据集：50,000对PPG-ECG记录，协议锁定的187患者测试集。评价指标：COTR(冲突过度信任率)、CIR(上下文诱导错误率)、CRR(正确修复率)、ESRM(证据特定修复边际)、UHR(效用伤害率)。

Experiments: 在187患者测试集上，ECG-to-PPG特权蒸馏基线在四个修复和特异性指标上提升1.82-6.69个百分点，所有配对置信区间排除零；UHR增加0.67个百分点(95% CI: -0.4到+1.7)。

Limitations: 1) 与遥感/卫星/目标检测/轻量化完全无关；2) 摘要未说明模型架构细节、计算开销、部署可行性；3) 仅以生理传感为案例，泛化性待验证；4) 摘要未说明DFOT框架在非生理场景的适用性。

Ideas for my research: 尽管整体弱相关，但有两个概念可抽象借鉴：1) "特权蒸馏"(privileged distillation)思想——在遥感目标检测中，可考虑利用高分辨率/多光谱/全色影像作为特权信息辅助训练轻量模型，推理时仅用低分辨率或单光谱输入，这与遥感场景中"训练用全谱、推理用轻量"的思路有类比价值；2) "派生特征过度信任"的评估框架思想——在轨遥感目标检测中，模型对某些派生特征(如融合特征、压缩特征)的过度信任可能导致误检，可借鉴其量化评估思路。但整体借鉴价值有限。

Abstract: Derived measurements increasingly enter large language model (LLM) pipelines as direct facts despite their instance-dependent validity. We define derived-feature over-trust (DFOT) as the failure in which a downstream LLM assigns such a measurement the epistemic status of a direct fact or uses it outside its valid scope. Using physiological sensing as a case study, D1 tests acceptance of a PPG-derived rhythm contradicted by offline ECG, whereas D2 tests rejection of an offline-confirmed reliable PPG rhythm under misleading severe history. ECG supplies training supervision and offline reference construction but is never shown to the LLM. Five estimands quantify this chain: conflict over-trust rate (COTR) and context-induced error rate (CIR) characterize D1/D2; correct repair rate (CRR) measures frozen-error repair; evidence-specific repair margin (ESRM) contrasts matched and patient-disjoint shuffled evidence; and utility harm rate (UHR) measures unnecessary verification among HIGH-reliability cases used without verification at baseline. The framework does not depend on a particular reliability generator. We demonstrate it on 50,000 paired PPG-ECG records using ECG-to-PPG privileged distillation as an illustrative baseline and PPG-only inference. On a protocol-locked 187-patient test, the baseline improves four repair and specificity endpoints by 1.82-6.69 percentage points, with all paired confidence intervals excluding zero; UHR increases by 0.67 percentage points (95% CI: -0.4 to +1.7). DFOT provides a common evaluation target for stronger mitigation methods. The code is available at https://github.com/Zongheng-Guo/When-Derived-Measurements-Mislead.

</details>


### [237] [A foundation model of numerical intelligence with cross-disciplinary generalization](https://arxiv.org/abs/2607.28432)
*Chenghan Wu,Zongmin Yu,Liu Yang*

Main category: cs.AI

TL;DR: 提出UNICON，一个基于图上下文推断数值关系的跨学科基础模型，实现无需重训练的数值智能（numerical intelligence）。

Research relevance: 1分，基本无关。该论文是通用数值智能/基础模型方向，聚焦于从数值上下文（graph-based examples）中推断预测关系，应用于科学和社会系统。完全不涉及遥感、卫星、地球观测、目标检测或模型轻量化，属于泛机器学习/基础模型研究，与"遥感卫星在轨目标检测与模型轻量化"方向无直接关联。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于数值智能/基础模型领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：数值智能（numerical intelligence）——从数值上下文推断预测关系并应用到新查询。应用场景：跨学科科学与社会系统（具体学科摘要未说明）。传感器类型：不适用。目标类别：不适用。

Model architecture: UNICON（UNified In-Context Operator Networks），一种基础模型（foundation model），使用graph-based examples作为上下文，推断系统内共享的预测关系并应用于同系统的查询。可与language-model agents结合以进一步提升性能。具体网络结构细节摘要未说明。

Lightweight method: 摘要未涉及任何轻量化方法（无pruning、quantization、knowledge distillation、NAS等）。模型压缩与加速信息完全缺失。

Onboard deployability: 摘要未说明。无参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。作为基础模型，通常参数量较大，不适合星上部署，但摘要未给出具体数据。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及跨学科科学与社会系统数据，且包含训练中未出现的学科（unseen disciplines），用于验证泛化能力。

Experiments: 主要实验结论：1) 在训练中未出现的学科上，同一模型无需重训练即可接近专家性能（specialist performance）；2) 与language-model agents结合后可超越训练中未见学科的最先进专家模型；3) 训练语料多样性（training-corpus diversity）可提升对未见学科的泛化能力。具体对比方法和消融实验细节摘要未说明。

Limitations: 摘要未说明局限性。但可推断：1) 该工作面向科学/社会系统数值预测，与视觉目标检测任务无交集；2) 未涉及任何遥感图像处理、目标检测或边缘/星上部署问题；3) 基础模型通常计算开销大，与轻量化在轨部署需求相悖。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发：UNICON的"in-context learning"思想——即利用上下文示例快速适应新任务而无需重训练——在遥感领域可类比为"少样本/零样本遥感目标检测"或"跨传感器（如光学→SAR）泛化"的思路。但其核心机制（graph-based numerical inference）与视觉目标检测差异巨大，且未涉及轻量化，对在轨部署课题的直接参考价值很低。

Abstract: Intelligence is commonly understood as the ability to acquire and apply knowledge, adapt to unfamiliar situations and solve new problems. Large language models exhibit this capacity by inferring task-relevant knowledge from textual context and applying it to new tasks. Yet intelligence need not be confined to language. For scientific and social systems, we need models that acquire and apply knowledge from numerical context-an ability we call numerical intelligence. Here we introduce UNified In-Context Operator Networks (UNICON), a foundation model that exhibits numerical intelligence across disciplines. Using graph-based examples from a system as context, UNICON infers the predictive relation shared across them and applies it to queries from the same system. Across scientific and social systems, including those from disciplines absent from training, the same model approaches specialist performance without retraining. Combining UNICON with language-model agents yields further gains, enabling it to surpass state-of-the-art specialists in a discipline unseen in training. We further show that training-corpus diversity improves generalization to unseen disciplines. Together, these results establish UNICON as a foundation model of numerical intelligence and position it as a building block for a broader ecosystem of artificial intelligence.

</details>


### [238] [Metaphor Tracer: A Theory-Informed Analysis of Hidden States](https://arxiv.org/abs/2607.28434)
*Marc Heimann,Roxana Assadi Moghaddam,Olga Brovkina,Mark Pettifor,Lutz Goetzmann*

Main category: cs.AI

TL;DR: 本文研究语言模型隐藏状态如何表征单个文本内部的组织结构，提出aggregator和differentiator两个指标来度量token位置在文本中的结构价值，属于计算语言学/认知科学范畴。

Research relevance: 1分，基本无关。论文研究的是语言模型（language model）隐藏状态对文本内部组织结构的表征，属于NLP/计算语言学领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无任何关联。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于语言模型可解释性/计算语言学领域，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：文本结构分析（text organization analysis）；应用场景：语言模型可解释性、心理语言学、临床文本分析；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构，仅提及"three unrelated models"（三个不相关的语言模型）以及一个matched base/instruct pair（匹配的基础/指令微调模型对）。核心是分析语言模型的hidden states（隐藏状态）。

Lightweight method: 不涉及。摘要未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 数据集：一个discovery text（发现文本，用于冻结常数）、临床转录文本（clinical transcripts）、以及一个engineered register（工程化语域）。评价指标：aggregator score、differentiator score、surprisal、attention，以及6/6 cells和34/36 cells的匹配准确率。

Experiments: 实验包括：1) 在三个不相关模型上验证aggregator在token重复时保持稳定而surprisal和attention下降；2) 在engineered register上6/6 cells匹配；3) 在心理分析师的临床转录标注上34/36 cells匹配；4) 迁移测试（transfer test）比较不同模型的单文本阅读能力。

Limitations: 摘要未说明局限性。但明显与遥感/目标检测/轻量化方向完全无关，属于NLP可解释性研究，无法为在轨遥感目标检测提供直接借鉴。

Ideas for my research: 几乎无可借鉴点。唯一可能的间接启发是：论文强调"relational rather than essentialist reading"（关系性而非本质性解读），这一思想在理论上与注意力机制中token间关系建模有相通之处，但与本课题（遥感在轨目标检测与轻量化）无直接关联，不建议投入时间深读。

Abstract: What do a language model's hidden states say about the organization of a single text? From one forward pass, without training, we score every token position on two properties. The *aggregator* measures whether the position consolidates the whole text into a stable configuration. The *differentiator*, whether other tokens are transiently carried into its subspace as the model reads: metaphor in its root sense, transport. Constants were frozen on one discovery text; every other is confirmatory.
  The aggregator is not, in the classic sense, an information measure, nor a measure of salience. Across three unrelated models, as a signifier repeats, its surprisal and its attention drain while its aggregator score holds: the channel marks a token's place in the text. That this tracks a reading rests on independent ground truth: an engineered register the aggregator follows across its boundaries (6/6 cells), and a psychoanalyst's marking of clinical transcripts, fixed before the instrument existed, in 34/36 cells, with a graded increment above lexical controls and dissociations no type-level measure reproduces. A transfer test gives the result its shape: the model whose token structure travels with lexical type reads the singular discourse worst, and in a matched base/instruct pair tuning raises fidelity without moving type-transfer. Structural value is a property of a token's place in *this* text, not of its vector alone: a relational rather than essentialist reading of hidden states, operationalizing theory that predated the instrument.

</details>


### [239] [SVR: Self-Verifying Refinement via Joint Verdict-Confidence Reinforcement Learning for Adaptive Test-Time Compute](https://arxiv.org/abs/2607.28457)
*Hongyu Chen,Liang Lin,Guangrun Wang*

Main category: cs.AI

TL;DR: 提出 Self-Verifying Refinement (SVR)，一种无外部反馈的多轮强化学习框架，让语言模型通过自验证信号作为计算控制策略，实现自适应测试时计算分配。

Research relevance: 弱相关（2分）。该论文属于大语言模型（LLM）推理与测试时计算分配方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可借鉴点是"自适应计算分配"（adaptive test-time compute allocation）的思想，与轻量化中"按输入难度动态调整计算量"的思路有一定类比价值，但并非遥感场景，也非目标检测或模型压缩方法。

Reading priority: 2分。与遥感在轨目标检测轻量化方向弱相关，核心价值仅在于"自适应测试时计算分配"这一概念性启发，可快速浏览了解动态计算调度思路，无需深入精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型数学推理（mathematical reasoning）与自适应测试时计算分配。应用场景：LLM推理，非遥感/卫星场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 基于 Qwen3.5-2B 语言模型，采用多轮强化学习框架 SVR。每轮模型同时输出：解决方案（solution）、离散正确性判定（discrete correctness verdict）和置信度分数（confidence score）。当判定为 Correct 且置信度超过阈值时保留当前答案，否则继续自我验证精炼。训练使用 GRPO（Group Relative Policy Optimization），在固定长度轨迹上训练，自适应停止仅在推理时激活。

Lightweight method: 摘要未说明传统模型压缩方法（剪枝/量化/蒸馏/NAS）。但提出了一种"自适应测试时计算分配"（adaptive test-time compute allocation）机制，通过自验证信号控制推理轮数，避免对简单输入浪费计算，本质上是一种推理阶段的动态计算调度策略，与轻量化中"按需计算"思想有概念上的类比。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提到模型为 Qwen3.5-2B（约20亿参数），平均推理轮数为2.99轮，相比固定10轮推理大幅减少计算量。但该模型规模远超星上可部署的轻量目标检测模型，且面向语言推理而非视觉目标检测，不具备直接星上部署可行性。

Datasets and metrics: 数据集：7个数学推理基准（seven mathematical reasoning benchmarks）。评价指标：macro-average accuracy（宏平均准确率），平均推理轮数（inference turns）。对比基线：标准 GRPO、强多轮基线、固定预算的 oracle-guided score-feedback 参考方法。实验结果：SVR 在 Qwen3.5-2B 上达到宏平均准确率 0.563，平均仅需 2.99 个推理轮。

Experiments: 在7个数学推理基准上，SVR 超过标准 GRPO、强多轮基线和固定预算的 oracle-guided score-feedback 参考方法，同时相比固定10轮推理大幅减少推理轮数。摘要未提供逐数据集详细结果、消融实验细节或计算开销对比。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于LLM推理方向；2. 摘要未说明模型推理的具体计算开销（FLOPs、延迟、能耗）；3. 未涉及视觉任务，无法直接迁移到遥感目标检测；4. 自验证机制依赖语言模型的文本输出，不适用于检测模型的边界框回归输出；5. 摘要未说明阈值选择策略、训练稳定性等实现细节。

Ideas for my research: 1. "自适应计算分配"思想可类比借鉴：在遥感在轨目标检测中，可设计"按输入难度动态调整推理深度"的机制——简单场景（如均匀背景）用浅层网络快速推理，复杂场景（如密集小目标）用深层网络精检，从而在保证精度的同时降低平均计算量；2. 自验证/置信度门控思想可迁移为检测模型的"置信度早停"策略，即当检测头输出高置信度时提前终止后续精炼模块；3. 但需注意：该方法是LLM推理框架，与CNN/Transformer检测器的轻量化（剪枝/量化/蒸馏）方法体系差异较大，借鉴价值主要体现在方法论层面的"动态计算调度"概念。

Abstract: Scaling test-time computation can improve language-model reasoning, but uniform budgets waste computation on easy inputs, while verifier-guided refinement relies on external feedback. We introduce Self-Verifying Refinement (SVR), an oracle-free multi-turn reinforcement learning framework that learns to use self-verification as a compute-control policy. At each turn, the model produces a solution together with a discrete correctness verdict and a confidence score; it retains the current answer only when the verdict is Correct and confidence exceeds a threshold, and otherwise continues refinement using its own self-verification. Ground-truth correctness is used only to construct training rewards and is never exposed to the policy through refinement prompts or required at inference. SVR is trained with GRPO on fixed-horizon trajectories using rewards that promote solution correctness, calibration-aware self-verification, and stop-ready correct states; adaptive stopping is activated only at inference. On seven mathematical reasoning benchmarks with Qwen3.5-2B, SVR achieves a macro-average accuracy of 0.563 with only 2.99 inference turns on average. In the evaluated complete-system comparison, it exceeds standard GRPO, strong multi-turn baselines, and a fixed-budget oracle-guided score-feedback reference while requiring substantially fewer turns than fixed ten-turn inference. These results demonstrate that learned self-verification can serve as an effective internal control signal for answer retention and adaptive test-time compute allocation.

</details>


### [240] [LeanCSP: A Framework for Certifying Constraint Reformulation and Solving in Lean](https://arxiv.org/abs/2607.28459)
*Pablo Manrique,Stefan Szeider*

Main category: cs.AI

TL;DR: 本文提出一个在Lean定理证明器中验证约束规划（Constraint Programming）求解正确性的框架，涵盖公式级验证和求解器证书检查两个层面，并通过验证的对称性破缺约束将求解搜索开销降低最多2×10^7倍。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文主题是约束规划（Constraint Programming）的形式化验证，属于定理证明、组合优化和软件验证领域，不涉及遥感、卫星、目标检测或模型轻量化中的任何要素。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于约束规划形式化验证领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：约束规划问题的形式化验证（formulation-level verification 和 solver certificate checking）。应用场景：调度（scheduling）、规划（planning）、配置（configuration）、验证（verification）等组合优化问题。不涉及遥感传感器、卫星平台或目标检测任务。

Model architecture: 框架基于Lean定理证明器，包含两个验证层面：(1) 公式级验证——证明等价性（equivalence）、可满足等价性（equisatisfiability）、对称性破缺约束（symmetry-breaking constraints）的正确性，可参数化地覆盖整个问题族；(2) 求解器证书检查——通过翻译后端（translation backends）将证书导出为MiniZinc、SMT-LIB、OPB等外部格式进行验证。不涉及神经网络或深度学习模型架构。

Lightweight method: 摘要未说明。本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。其"轻量化"概念完全不同——指的是通过参数化证明复用减少求解搜索开销，而非模型压缩。

Onboard deployability: 摘要未说明。本文不涉及星上部署、边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、显存或能耗等部署相关信息。唯一相关的性能指标是求解搜索开销降低最多2×10^7倍，以及in-Lean认证耗时最多几分钟，但这属于约束求解效率而非模型部署效率。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括：求解搜索开销降低倍数（最多2×10^7倍）、in-Lean认证耗时（最大实例最多几分钟）。不涉及遥感数据集（如DOTA、DIOR、NWPU VHR-10等）或目标检测指标（如mAP、FPS等）。

Experiments: 摘要仅提及实验结果表明验证的对称性破缺约束在实践中有效：每个问题族只需一次参数化证明，可跨所有实例规模复用，将求解搜索开销降低最多2×10^7倍；整个in-Lean认证在最大实例上最多耗时几分钟。未提供与目标检测或遥感相关的实验对比。

Limitations: 与本研究方向完全无关。主要局限包括：仅适用于约束规划/组合优化问题，不涉及视觉感知任务；验证框架针对的是求解器正确性而非模型推理效率；没有提供任何与遥感目标检测或模型轻量化相关的信息。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题几乎没有直接可借鉴点。唯一可能的间接启发是：其"参数化证明复用"思想（一次证明覆盖整个问题族）在方法论上类似于轻量化中的"一次训练/蒸馏，多尺度复用"思路，但关联非常牵强。不建议从本文提取技术路线。

Abstract: Constraint programming is a core technology for solving complex combinatorial problems in scheduling, planning, configuration, and verification. Trusting its results therefore demands guarantees at two levels: that reformulations applied beforehand are semantics-preserving, and that solvers produce correct answers. In this work, we introduce a framework that addresses both verification levels in the Lean theorem prover: it can be used to prove formulation-level properties, such as equivalence, equisatisfiability, and the correctness of symmetry-breaking constraints, parametrically for entire problem families; and to check solver-produced certificates for individual instances via translation backends to external formats such as MiniZinc, SMT-LIB, and OPB. Combining both levels yields an end-to-end workflow that establishes the satisfiability or unsatisfiability of a constraint problem without trusting the external solver. Experimental results show that our framework's verified symmetry breaking also pays off in practice: a single parametric proof per problem family, reused across all instance sizes, reduces solver search effort by a factor of up to 2x10^7, while the entire in-Lean certification stays affordable, taking at most a few minutes for our largest instances.

</details>


### [241] [A report-grounded vision-language foundation model for colonoscopy from 280000 routine reports](https://arxiv.org/abs/2607.28466)
*Jia Yu,Yan Zhu,Yili He,Zilong Wang,Xinyang Jiang,Peiyao Fu,Ruijie Yang,Tianyi Chen,Siyuan Li,Zhihua Wang,Fei Wu,Quanlin Li,Xian Yang,Pinghong Zhou,Shuo Wang*

Main category: cs.AI

TL;DR: 提出EndoCLIP，一个基于125,756个病灶级图像-文本对训练的结肠镜视觉-语言基础模型，通过从常规报告中恢复病灶与帧的对应关系，在图像-文本检索、结构化报告生成和六个多中心临床分类任务上超越通用和生物医学视觉-语言编码器。

Research relevance: 弱相关（1-2分）。该论文是医学影像（结肠镜）领域的视觉-语言基础模型研究，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关系。唯一可借鉴的是其"从大规模弱标注文本中恢复图像-文本对应关系"的数据挖掘思路，以及视觉-语言模型（VLM）的zero-shot与linear-probe评估范式，但这些属于泛视觉-语言方法层面的间接借鉴，并非遥感场景。

Reading priority: 1分。该论文为结肠镜医学影像视觉-语言模型研究，与遥感卫星在轨目标检测及轻量化方向基本无关。仅当对视觉-语言基础模型的数据挖掘或评估范式感兴趣时可作泛读参考，不建议作为核心文献精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：视觉-语言基础模型（vision-language foundation model），包括图像-文本检索（image-text retrieval）、结构化报告生成（structured report generation）、临床分类（clinical classification）。应用场景：结肠镜检查（colonoscopy）医学影像。传感器类型：内窥镜图像（非遥感传感器）。目标类别：病灶（lesion），包括良恶性分类（benign-versus-malignant）。

Model architecture: EndoCLIP，基于CLIP架构的视觉-语言编码器（vision-language encoder），包含图像编码器和文本编码器双塔结构。训练数据为125,756个病灶级图像-文本对，从280,476条常规结肠镜记录中逐步恢复（progressively recovered）。摘要未说明具体backbone网络结构（如ViT或ResNet）及文本编码器细节。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法。也未涉及实时推理、边缘部署、星上部署等部署优化内容。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。作为基础模型（foundation model），EndoCLIP通常参数量较大，但摘要未给出具体数值。

Datasets and metrics: 训练数据：125,756个病灶级图像-文本对，从280,476条常规结肠镜记录中恢复。评估任务：病灶级图像-文本检索、结构化报告生成、六个多中心临床分类任务。评估设置：zero-shot和linear-probe两种设置。对比对象：通用视觉-语言编码器（general-purpose）和生物医学视觉-语言编码器（biomedical vision-language encoders）。另含一项涉及12名内镜医师的盲法研究（blinded study）用于良恶性分类对比。摘要未说明具体指标名称（如Recall@K、ACC、AUC等）。

Experiments: 主要结果：在病灶级图像-文本检索、结构化报告生成和六个多中心临床分类任务上，EndoCLIP在zero-shot和linear-probe设置下均优于通用和生物医学视觉-语言编码器。在良恶性分类上，其linear probe在12名内镜医师参与的盲法研究中接近专家读者（expert readers）的表现。摘要未提供具体数值和消融实验细节。

Limitations: 1. 与遥感/卫星/在轨目标检测/轻量化方向基本无关，属于医学影像领域。2. 摘要未说明模型参数量、推理速度等部署指标。3. 摘要未提供具体分类准确率、检索指标数值。4. 未说明模型在资源受限设备上的可行性。5. 数据来自单一医学场景（结肠镜），泛化到遥感场景需要大量重新设计。

Ideas for my research: 1. 数据挖掘思路可借鉴：EndoCLIP从常规报告中"恢复病灶与帧的对应关系"（finding-to-frame correspondence），类似地，遥感领域可从已有的遥感图像标注文本/元数据中自动挖掘弱监督图像-文本对，用于预训练遥感视觉-语言模型。2. zero-shot与linear-probe评估范式可借鉴：在遥感目标检测中，可评估预训练模型在未见类别上的zero-shot检测能力。3. 视觉-语言基础模型在遥感中的应用趋势值得关注（如遥感CLIP类模型），但本论文本身不提供遥感相关方法。4. 若未来做遥感VLM轻量化，可参考其"用语言指定任务目标而非逐任务标注"的范式，但需自行设计轻量化方案。

Abstract: Vision-language models remain underused in colonoscopy despite the rich expert descriptions recorded in routine reports. These reports document lesion appearance, size and location but summarise entire procedures rather than caption individual frames, leaving clinical findings only weakly linked to the corresponding images. Here we develop EndoCLIP, a colonoscopy vision-language foundation model trained on 125,756 lesion-level image-text pairs progressively recovered from 280,476 routine colonoscopy records. Across lesion-level image-text retrieval, structured report generation and six multi-centre clinical classification tasks, EndoCLIP outperforms general-purpose and biomedical vision-language encoders in both zero-shot and linear-probe settings. On benign-versus-malignant classification, its linear probe approaches the performance of expert readers in a blinded study involving 12 endoscopists. These results suggest that recovering finding-to-frame correspondence can transform routine documentation into scalable supervision, enabling clinical targets to be specified in language rather than separately annotated for each task.

</details>


### [242] [Towards Autonomous Aircraft Surveillance from Nanosatellites through On-Board Inference and Generative Data Augmentation](https://arxiv.org/abs/2607.28470)
*Antonio Delgado-Rosa,David Muñoz-Valero,Enrique Adrian Villarrubia-Martin,Juan Moreno-Garcia*

Main category: cs.AI

TL;DR: 提出一种结合星上推理与生成式数据增强的工作流：在6U CubeSat上部署低功耗边缘张量加速器执行目标检测，同时用LoRA微调的扩散模型生成少数类合成图像并自动伪标注，解决下行带宽受限与飞机类别数据不平衡问题。

Research relevance: 5分。论文同时涉及遥感/卫星（低轨纳米卫星、6U CubeSat、星上推理）、目标检测（airborne surveillance、飞机检测、mAP/F1指标）以及轻量化/部署（低功耗边缘张量加速器、量化检测器、25-30 FPS在轨推理、片上内存适配），三者强相关，与"遥感卫星在轨目标检测模型轻量化"课题高度契合。

Reading priority: 5分。该论文与"遥感卫星在轨目标检测模型轻量化"方向高度相关，同时覆盖星上推理、量化部署、边缘加速器、数据增强解决类别不平衡等多个关键维度，且提供了具体的在轨FPS和mAP数据，具有很高的参考价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：低轨卫星对空目标检测（airborne surveillance from LEO），即星上飞机目标检测。应用场景：纳米卫星（nanosatellite）在轨实时自主监视，替代传统bent-pipe被动数据收集架构。传感器类型：摘要未明确说明（推测为光学/EO成像，但摘要未说明）。目标类别：飞机（aircraft），且存在严重类别不平衡（class-imbalanced），少数类飞机类别是重点。

Model architecture: 检测器：摘要未说明具体检测网络架构（如YOLO、Faster R-CNN等），仅提及"quantised detector"（量化检测器）和"intermediate detector"（中间检测器用于伪标注）。生成模型：diffusion model（扩散模型），通过low-rank adaptation（LoRA）进行微调，用于生成少数类合成图像。整体工作流包含：星上推理模块 + 生成式数据增强模块 + 自动伪标注模块。

Lightweight method: 主要轻量化手段：1) 模型量化（quantised detector），使检测器适配片上内存（on-chip memory）；2) 部署在低功耗边缘张量加速器（low-power edge tensor accelerator）上；3) 在6U CubeSat平台上实现25-30 FPS在轨推理。摘要未说明具体量化位宽（如INT8/INT4）、剪枝或知识蒸馏等细节。

Onboard deployability: 部署可行性较强：在6U CubeSat上搭载低功耗边缘张量加速器执行推理，量化后的检测器适配片上内存，在轨推理速度达到25-30 FPS。摘要未说明具体参数量、FLOPs、显存占用、功耗（瓦数）和延迟等量化指标。整体架构从bent-pipe被动传输改为星上自主推理，显著降低下行带宽需求。

Datasets and metrics: 数据集：开放卫星飞机数据集（open satellite datasets for aircraft），存在严重类别不平衡。评价指标：global mean average precision（全局mAP，从77.9%提升到82.2%）、少数类F1（从0.683提升到0.811）。摘要未说明具体数据集名称、类别数量、训练/测试划分等细节。

Experiments: 主要实验结论：1) 平衡数据集（合成少数类+经典增强）使全局mAP从77.9%提升至82.2%；2) 少数类F1从0.683提升至0.811；3) 量化检测器适配片上内存，在轨投影25-30 FPS。摘要未说明对比方法、消融实验细节、不同量化策略对比等。

Limitations: 摘要未说明：1) 具体检测网络架构与量化位宽；2) 参数量、FLOPs、功耗、延迟等部署量化指标；3) 合成图像质量评估与域差距（domain gap）问题；4) 扩散模型在星上是否运行（推测在地面生成，星上仅推理）；5) 数据集名称与规模；6) 与现有星上检测方法的定量对比。此外，扩散模型+LoRA的生成流程本身的计算成本未讨论。

Ideas for my research: 1) 星上推理+地面生成式增强的"端-云协同"范式值得借鉴：将重计算（扩散模型训练/生成）放在地面，星上仅部署轻量量化检测器，平衡了在轨算力限制与数据稀缺问题；2) 量化检测器适配片上内存并达到25-30 FPS，可作为在轨实时推理的参考基线；3) 类别不平衡问题在遥感目标检测中普遍存在，LoRA微调扩散模型生成少数类样本+自动伪标注的思路可迁移到舰船/车辆等稀有类别；4) 可进一步探索更激进的轻量化手段（剪枝、蒸馏、NAS）与量化结合，在保证mAP的同时提升FPS；5) 建议补充参数量、FLOPs、功耗等部署指标，与本文形成更完整的在轨部署评估体系。

Abstract: Airborne surveillance from low Earth orbit is hindered by two interconnected bottlenecks: nanosatellites have a limited downlink budget, yet the conventional approach still transmits terabytes of raw imagery to the ground for processing, and open satellite datasets for aircraft are scarce and severely class-imbalanced. These limitations either delay timely decision-making or prevent standard detectors from learning robust representations of rare aircraft classes. In this paper, a workflow that combines on-board inference with generative data augmentation is proposed to address both limitations jointly. Inference is executed on a 6U CubeSat equipped with a low-power edge tensor accelerator, while a diffusion model fine-tuned through low-rank adaptation generates synthetic minority-class imagery. This synthetic output is automatically annotated, pseudo-labelled, by an intermediate detector and merged with classically augmented samples. The results show that the balanced dataset increases global mean average precision from 77.9% to 82.2%, with the minority class rising from F1=0.683 to F1=0.811, and that the quantised detector fits the on-chip memory and projects 25-30 frames per second on orbit. This approach contrasts with the conventional bent-pipe architecture, in which the satellite acts as a passive data collector. Therefore, the computational tests support the proposed workflow as a decision-support tool for real-time, autonomous airborne surveillance from nanosatellites.

</details>


### [243] [A Fuzzy Rule-based Neuro-Symbolic Approach for Pipe Severity Prediction in Sewer Networks](https://arxiv.org/abs/2607.28481)
*Ngoc Thai Le,Thanh Ma,Umberto Straccia*

Main category: cs.AI

TL;DR: 提出一种模块化的模糊规则神经符号框架，将Swin Transformer视觉感知与决策树生成的IF-THEN规则推理解耦，用于下水道管道缺陷严重程度评估，提升可解释性并改善类别不平衡下的分类性能。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习方法，核心任务是下水道管道缺陷严重程度评估（sewer pipe severity assessment），与遥感、卫星、在轨部署、目标检测均无直接关联。虽然使用了Swin Transformer作为视觉骨干，且涉及可解释性推理，但场景、任务、传感器类型均与遥感目标检测无关，也未涉及任何轻量化或部署优化内容。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，场景（下水道检测）、任务（图像分类而非目标检测）、方法（可解释性推理而非模型压缩）均不匹配，且未提供任何部署相关指标。仅当对神经符号推理或可解释性方向有额外兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：下水道管道缺陷严重程度分级（sewer pipe severity assessment），属于图像分类任务而非目标检测。应用场景：城市基础设施检测（下水道管道）。传感器类型：普通管道内窥图像（CCTV图像）。目标类别：14种多标签CODE缺陷等级 + 5个严重程度类别（severity classes）。

Model architecture: 两阶段模块化框架：(1) 感知模块使用Swin Transformer直接从图像预测14个多标签CODE缺陷等级；(2) 推理模块使用决策树（Weka J48）将CODE与严重程度标签的关系转换为19条固定IF-THEN规则，再通过模糊逻辑（t-norm/s-norm算子对）进行可解释推理。评估了Product、Łukasiewicz、Hamacher三种模糊算子对。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等均未提及。Swin Transformer作为骨干网络本身计算量较大，且论文未讨论推理效率或部署优化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署相关指标。Swin Transformer作为视觉骨干在边缘/星上部署的可行性未被讨论。该任务场景（下水道检测）本身与在轨部署无关。

Datasets and metrics: 数据集：3,244张下水道管道图像，覆盖5个高度类别不平衡的严重程度类别。标签生成方式：由5个独立大语言模型（LLM）分析原始检查员笔记，通过共识机制生成ground-truth标签。评价指标：accuracy、balanced accuracy、Macro F1、MCC（Matthews Correlation Coefficient）。

Experiments: 相比纯图像分类基线，所提框架在accuracy、balanced accuracy、Macro F1和MCC上分别提升17.9%、12.2%、23.0%和17.3%。对比了三种模糊算子对（Product、Łukasiewicz、Hamacher）的性能。整体在类别不平衡条件下实现了有竞争力的类别均衡性能，同时提供了从预测CODE到规则支持再到严重程度证据的可追溯推理。

Limitations: 1. 任务场景与遥感/卫星目标检测完全无关；2. 未涉及任何轻量化或部署优化内容；3. 未报告模型复杂度、推理速度等工程指标；4. 数据集规模较小（3,244张）且为特定领域（下水道），泛化性存疑；5. 依赖LLM生成标签的可靠性需要进一步验证；6. 模糊规则推理的扩展性（当规则数量增大时）未讨论。

Ideas for my research: 尽管场景无关，但以下思路可借鉴：1. 神经-符号解耦框架（neural perception + symbolic reasoning）的思想可用于遥感目标检测的可解释性增强，例如将检测结果与领域规则（如舰船尺寸/形状约束）结合；2. 模糊逻辑推理处理类别不平衡的思路可迁移到遥感小目标检测的类别不平衡问题；3. 多标签CODE预测与规则推理结合的方式，可类比遥感多类别目标检测中的类别关系建模；4. 使用LLM辅助生成标注的思路可借鉴于遥感数据标注效率提升。但整体借鉴价值有限，因为论文未涉及轻量化核心问题。

Abstract: Standard automated sewer pipe severity assessment relies on direct image classification, creating a "black box" where the link between visual defects and final severity scores remains implicit. This study introduces a modular, fuzzy rule-based neuro-symbolic framework that bridges this gap by decoupling neural perception from symbolic reasoning. The perception module utilizes a Swin Transformer to predict 14 multilabel inspection CODE degrees directly from images. For reasoning, a DT, specifically Weka's J48, algorithm is trained on ground-truth CODEs and severity labels, and its paths are converted into 19 fixed IF--THEN rules. Inference operates via fuzzy logic: t-norm activations from CODE conditions are weighted by rule confidence and combined with corresponding s-norms to produce interpretable class evidence. We assessed Product, Łukasiewicz, and Hamacher operator pairs using a dataset of 3,244 images spanning five highly imbalanced severity classes. Ground-truth labels were robustly generated via consensus from five independent large language models analyzing original inspector notes. Our results show an improvement of accuracy, balanced accuracy, Macro F1 and MCC by 17.9%, 12.2%, 23.0%, and 17.3%, respectively, over image-only based classification.
  Overall, the framework combines competitive class-balanced performance with traceable reasoning from predicted CODE degrees to rule supports and severity evidence.

</details>


### [244] [SCOPE: Supply-Chain Operations through Coupled Policies for End-to-End Coordination](https://arxiv.org/abs/2607.28488)
*Yunhao Liang,Xianqi Cao,Pujun Zhang,Yuan Qu,Yongzhi Qi,Ningxuan Kang,Max Z. J. Shen*

Main category: cs.AI

TL;DR: 提出SCOPE复合策略模型，将供应链实体表示为token并通过共享操作表征进行端到端耦合决策优化，应用于城市生鲜零售补货场景。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦供应链运营规划（supply-chain operations），涉及补货计划、品类选择、配送路由等运筹优化问题，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能等任何相关主题，也不涉及目标检测或模型轻量化。

Reading priority: 1分。论文主题为供应链运营优化，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及任何相关技术、数据集或部署指标，无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：供应链端到端运营规划（replenishment planning, assortment selection, source assignment, delivery routing）。应用场景：城市生鲜零售补货（urban fresh-retail replenishment）。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用。

Model architecture: SCOPE（Supply-Chain Operations through Coupled Policies for End-to-End Coordination）复合策略模型：将供应链实体表示为token，通过共享操作表征（shared operational representation）进行上下文建模，并将每种token类型映射到对应决策接口。每个决策基于先前决策形成的部分计划，最终计划通过共享系统级效用（system-level utility）评估。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络等任何模型压缩或加速方法，也不涉及实时推理、边缘部署或星上部署。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其应用场景（供应链运营）与星上/边缘部署无关。

Datasets and metrics: 数据集：Dingdong（叮咚买菜）和JD.com（京东）两家大型供应链的真实运营数据（real operational data），分别处于不同补货层级（replenishment echelons）。评价指标：摘要未明确列出具体指标名称，仅说明SCOPE在两种设置下均优于逐阶段独立优化方法及实践导向基线。

Experiments: 在Dingdong和JD.com两个真实供应链数据集上验证，SCOPE在两种设置下均一致优于逐阶段独立优化的方法以及供应链运营中常用的实践导向基线（practice-oriented baselines）。摘要未提供具体数值结果或消融实验细节。

Limitations: 与本研究方向完全无关。摘要未涉及遥感、目标检测、轻量化或部署相关内容；也未提供模型规模、计算复杂度、推理效率等对轻量化研究有价值的信息。

Ideas for my research: 借鉴意义有限。唯一可能的概念性启发是"端到端耦合决策"思想——即多个决策模块共享一个统一表征并协同优化，而非各自独立优化。这一思想在遥感在轨检测中可类比为"检测-跟踪-压缩"或"感知-决策"联合优化，但论文本身不提供任何可直接复用的技术细节。

Abstract: Can supply-chain AI move beyond isolated decision modules toward unified operational planning? A complete replenishment plan specifies which products each location carries, which upstream facility supplies it, how often it is replenished, and how deliveries are routed. These decisions are operationally coupled: the selected assortment changes the demand and load passed to later stages; source assignment and replenishment frequency reshape the delivery requests; and route feasibility and cost, in turn, determine the system value of the earlier choices. Yet in modern supply chains, these decisions are often handled by separate departments and optimized through separate systems, which can lead to stockouts, inventory exposure, and avoidable transportation. We propose SCOPE: Supply-Chain Operations through Coupled Policies for End-to-End Coordination, a composite policy model that represents supply-chain entities as tokens, contextualizes them through a shared operational representation, and maps each token type to the corresponding decision interface. Each decision builds on the partial plan formed by earlier decisions while the completed plan is evaluated using a shared system-level utility. We instantiate this framework in urban fresh-retail replenishment, where service frequency, assortment, capacity pressure, and road-network routing interact strongly, and evaluate it on real operational data from Dingdong and JD.com, two large-scale supply chains operating at different replenishment echelons. Across both settings, SCOPE consistently outperforms methods that optimize each decision stage separately, as well as practice-oriented baselines commonly used in supply-chain operations. These results show that learning and coordinating cross-department operational couplings lead to more effective end-to-end supply-chain decisions.

</details>


### [245] [InfoOps Bench: A live information operations safety benchmark](https://arxiv.org/abs/2607.28503)
*Dorian Quelle,Lisa-Maria Neudert,Jonathan Bright,John Gallacher*

Main category: cs.AI

TL;DR: 本文提出了一个持续更新的AI基准测试（InfoOpsBench），用于评估前沿语言模型被国家支持的信息操作所利用的完整性，测试了17个模型在四种提示框架下的拒绝率与事实核查率。

Research relevance: 1分（基本无关）。该论文聚焦于大语言模型（LLM）的安全性与信息操作完整性评估，属于AI安全/社会计算领域，与遥感、卫星、目标检测、模型轻量化均无任何关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于大语言模型安全评估领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型完整性基准测试（integrity benchmark）；应用场景：检测语言模型是否被国家支持的信息操作（state-backed information operations）所利用；传感器类型：不适用（非遥感）；目标类别：不适用（非视觉目标）。

Model architecture: 摘要未说明具体模型架构。测试了来自8个提供商的17个模型，包括Z.ai的GLM 5.2等，但未给出模型结构细节。

Lightweight method: 不涉及。摘要中未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未涉及参数量、FLOPs、延迟、显存、能耗、实时推理或任何部署相关信息。

Datasets and metrics: 数据集：来自实时监控管道的2,100多个信息操作案例，追踪俄罗斯、中国和伊朗的国家支持信息资产；评价指标：完整性分数（integrity scores，定义为拒绝请求的百分比，范围8.8%-94.5%）、事实核查率（fact-checking rates，范围2.9%-72.9%）。

Experiments: 测试了17个模型（来自8个提供商）在四种提示框架下的表现。主要发现：大多数模型可被用于信息操作；完整性分数跨度达85.7个百分点且与模型规模无关；模型选择会改变操作特征（有的模型编造细节产生更有害输出，有的在合规时化解主张）；中国开发的模型在事实依据充分但批评中国的主张上合规率大幅下降48-70个百分点（GLM 5.2除外）。

Limitations: 该论文与遥感卫星在轨目标检测及模型轻量化方向完全无关。摘要未说明任何与视觉感知、目标检测、模型压缩或部署相关的内容。作为AI安全基准研究，其方法论（动态基准避免饱和）虽有一定通用借鉴意义，但对本课题无直接参考价值。

Ideas for my research: 对本课题（遥感卫星在轨目标检测模型轻量化）基本无可借鉴点。唯一可能间接相关的思路是"动态持续更新的基准测试"这一方法论——在遥感领域可类比为持续更新的遥感目标检测基准（如不断纳入新传感器、新场景、新目标类别的动态评测集），但这是评测层面的启发，与模型轻量化本身无关。

Abstract: In this paper we present an active, constantly updated AI benchmark which measures the integrity of frontier language models against being co-opted for state-backed information operations. We draw on over 2,100 information operations from a live monitoring pipeline which tracks Russian, Chinese and Iranian state-backed information assets. Alongside this paper, we release a companion website that tracks the most prominent claims spread by state-backed media outlets, updated weekly, available from: pattrn.ai/research/infoopsbench. The dynamic nature of the benchmark makes it resistant to saturation.
  In the benchmark, we test 17 models from 8 providers across four prompt framings. We find that most models can be co-opted for information operations. Integrity scores, defined as the percentage of refused requests, range from 8.8% to 94.5%, an 85.7-percentage-point spread not explained by model size. Model choice also changes the character of the resulting operation. Some models fabricate details and produce output more harmful than the source material, others defuse claims even while complying, and fact-checking rates vary from 2.9% to 72.9%.
  Integrity against information operations is at least partly related to refusal to produce content even for benign claims, illustrating the challenge of balancing model usability with safety. With one exception (Z.ai's GLM 5.2), the Chinese-developed models sharply cut compliance on factually grounded but China-critical claims, dropping 48-70 percentage points relative to matched benign claims.

</details>


### [246] [Selective Credibility-Limited Belief Update](https://arxiv.org/abs/2607.28523)
*Theofanis Aravanis,Costas D. Koutras*

Main category: cs.AI

TL;DR: 本文提出选择性可信度受限信念更新（selective credibility-limited belief update）框架，允许将复合认知输入按源世界转化为更弱的代理后再进行可信度受限转换，统一了Katsuno-Mendelzon更新与可信度受限更新。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于人工智能中的知识表示与推理（belief revision/update）领域，讨论的是逻辑语义与公理化刻画，不涉及任何遥感、卫星、目标检测或模型压缩内容。

Reading priority: 1分。论文主题为信念更新的逻辑语义与公理化，与遥感卫星在轨目标检测及模型轻量化方向完全无关，无阅读价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：信念更新（belief update）的形式化逻辑研究。应用场景：知识表示与推理、智能体信念管理。不涉及传感器、遥感影像或目标类别。

Model architecture: 摘要未说明。本文不涉及神经网络或机器学习模型架构，而是逻辑算子（update operators）的语义与公理化框架设计。

Lightweight method: 摘要未说明。不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗或实时推理等部署相关信息。

Datasets and metrics: 摘要未说明。本文为纯理论逻辑研究，无实验数据集和评价指标。

Experiments: 摘要未说明。无实验对比或消融实验，仅提供语义与公理化刻画及理论性质证明。

Limitations: 本文与遥感目标检测和轻量化方向完全无关，属于形式逻辑/知识表示领域。摘要未涉及任何工程应用、计算复杂度或可扩展性讨论，对计算机视觉或嵌入式部署无直接参考价值。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题无可借鉴点。该论文属于纯逻辑理论（belief update），与深度学习目标检测、遥感图像处理、模型压缩加速均无交集。不建议投入时间阅读。

Abstract: Belief update concerns changes in an agent's beliefs induced by changes in the underlying world. Standard Katsuno-Mendelzon update assumes that an epistemic input can be incorporated from every initially possible world, whereas credibility-limited belief update restricts, for each source world, the successor worlds regarded as credible or reachable. Nevertheless, existing credibility-limited approaches treat the epistemic input as an indivisible whole, and therefore cannot represent cases in which only part of a compound epistemic input can be realized. We introduce selective credibility-limited belief update, in which the epistemic input is transformed, relative to each source world, into a weaker proxy before the credibility-limited transition is performed. We provide semantic and axiomatic characterizations of the resulting class of update operators. We then identify two well-behaved sub-classes; namely, consistency-preserving update operators, which require every transformed epistemic input to be credible from its source world whenever the original epistemic input is consistent, and maximal consistency-preserving update operators, which additionally require the selected proxy to be maximally informative among the credible consequences of the original epistemic input. Finally, we establish the generality of the proposed framework by showing that credibility-limited belief update is recovered as a special case, while Katsuno--Mendelzon belief update emerges when credibility restrictions are removed and the transformation functions are taken to be identities. These results demonstrate that the framework provides a unified and strictly more expressive account of belief update, encompassing established approaches while supporting source-dependent selective acceptance.

</details>


### [247] [MANTA: Multi-Agent Network Topology Adaptation for Self-Evolving Multi-Agent Systems](https://arxiv.org/abs/2607.28527)
*Mao-xun Huang,Jerry Wang,Yi-Cheng Lai,Zhengxin Zhang,Claire Cardie,Hen-Hsen Huang*

Main category: cs.AI

TL;DR: 提出MANTA框架，让大语言模型多智能体系统的通信拓扑结构在推理时自适应演化，通过任务条件化初始化和有界结构更新提升多任务协作性能。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦大语言模型多智能体系统的通信拓扑自适应演化，属于自然语言处理/多智能体协作领域，完全不涉及遥感、卫星、目标检测或模型轻量化。既无遥感场景，也无目标检测任务，更无任何模型压缩或部署优化方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于大语言模型多智能体协作领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多智能体协作问题求解（信息检索、工具使用、规划、工作流执行、数学推理）。应用场景：大语言模型驱动的通用智能体系统，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: MANTA框架：基于大语言模型的多智能体系统，核心创新在于通信拓扑（agent roles、communication links、execution order、information visibility、validation pathways）在推理时自适应演化。包含任务条件化拓扑初始化和有界结构更新两个阶段。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法。虽然提到"bounded structural updates"和"preserving the task interface and agent budget"，但这是关于智能体数量预算约束，而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等任何部署相关指标。且大语言模型多智能体系统通常计算开销巨大，与星上/边缘部署的轻量化需求相悖。

Datasets and metrics: 五个基准测试：信息检索（information seeking）、工具使用（tool use）、规划（planning）、工作流执行（workflow execution）、数学推理（mathematical reasoning）。评价指标：平均得分（average score），MANTA达到74.0，超过最强baseline 5.8个百分点，并在PlanCraft上取得最佳结果。

Experiments: 与代表性单智能体和多智能体baseline对比，在五个基准上MANTA取得最高平均分74.0，比最强baseline高5.8个百分点，并在PlanCraft上获得最佳结果。摘要未提供消融实验细节。

Limitations: 摘要未说明。但可推断：1）大语言模型多智能体系统计算开销大，不适合资源受限的星上部署；2）仅评估了通用NLP任务，未涉及任何视觉/遥感任务；3）推理时拓扑演化的额外计算开销未报告。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是"任务条件化初始化 + 推理时有界更新"的自适应思想，可类比为遥感目标检测中根据输入场景动态调整网络结构或推理路径（类似dynamic routing / early exit），但这属于泛化思路，与轻量化本身无直接关联。不建议作为主要参考。

Abstract: Large language model-based multi-agent systems improve complex problem solving through task decomposition, agent specialization, information exchange, and intermediate validation. However, existing systems typically treat communication topology as a fixed design choice or an offline optimization target. We introduce MANTA, a framework for Multi-Agent Network Topology Adaptation that enables communication structures to self-evolve at inference time. Before execution, MANTA initializes a task-conditioned topology from prior structural experience. During deployment, it monitors collaboration traces and applies bounded structural updates when the current organization becomes insufficient. These updates can modify agent roles, communication links, execution order, information visibility, and validation pathways while preserving the task interface and agent budget. We evaluate MANTA against representative single-agent and multi-agent baselines on five benchmarks spanning information seeking, tool use, planning, workflow execution, and mathematical reasoning. MANTA achieves the highest average score of 74.0, outperforming the strongest baseline by 5.8 percentage points and obtaining the best result on PlanCraft. These results show that inference-time self-improvement can extend to the architecture of collaboration itself.

</details>


### [248] [Rethinking Inference-Time Scaling in Local Computer-Use Agents: Failure Modes and Compute Tradeoffs](https://arxiv.org/abs/2607.28573)
*Woongkyu Lee,Jungwook Choi*

Main category: cs.AI

TL;DR: 系统研究本地部署的computer-use agents（CUAs）在推理时扩展（inference-time scaling）的效果，发现额外计算常带来收益递减并改变失败模式，提出需要选择性计算分配和失败感知控制机制。

Research relevance: 弱相关（2分）。论文研究的是本地部署的computer-use agents（GUI操作智能体），属于泛AI/多模态大模型领域，与遥感、卫星、Earth observation、目标检测均无直接关联。虽然涉及"本地部署""硬件约束""资源受限"等与轻量化/边缘部署相关的概念，但研究对象是GUI操作智能体而非遥感目标检测模型，轻量化方法也非剪枝/量化/蒸馏等模型压缩技术，而是推理时计算分配策略。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于GUI智能体领域。仅"选择性计算分配"和"失败感知控制"两个概念对星上自适应推理设计有间接启发，但整体借鉴价值有限，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：computer-use agents（计算机操作智能体）的推理时扩展策略评估；应用场景：本地部署的GUI自动化操作；传感器类型：不适用（非遥感传感器）；目标类别：不适用（非目标检测任务）。

Model architecture: 评估的模型包括Qwen3-VL-8B/30B-A3B、UI-TARS-1.5-7B、OpenCUA-7B，均为多模态视觉-语言大模型（VLM），用于GUI操作理解与执行。摘要未说明具体网络结构细节。

Lightweight method: 不涉及传统模型压缩方法（剪枝、量化、蒸馏、NAS等）。论文关注的是"推理时扩展"（inference-time scaling），即在执行阶段通过额外计算提升性能，从contextual、temporal、structural、parallel四个维度进行系统研究。这属于计算资源分配策略而非模型轻量化技术。

Onboard deployability: 摘要涉及"本地部署""严格硬件约束""资源受限"等概念，但未给出参数量、FLOPs、延迟、显存、能耗等具体部署指标。模型规模从7B到30B不等，属于大模型范畴，与星上部署的轻量化目标检测模型差异较大。摘要未说明具体硬件平台和部署细节。

Datasets and metrics: 基准数据集：OSWorld（GUI操作智能体基准）。评价指标：任务成功率（task success）、轨迹稳定性（trajectory stability）、最大步数停滞（max-step stalls）、token成本等。摘要未说明具体数值结果。

Experiments: 在OSWorld上对Qwen3-VL-8B/30B-A3B、UI-TARS-1.5-7B、OpenCUA-7B进行系统实验。主要发现：(1) 额外计算常带来收益递减并改变失败模式；(2) contextual scaling提供历史grounding，改善轨迹稳定性和任务准确率，但收益随token成本增加而饱和；(3) temporal scaling减少max-step stalls但未显著提升任务成功率；(4) structural decomposition在本地两阶段agent中引入规划和格式化开销；(5) parallel scaling以大量计算成本部分缓解失败。

Limitations: 摘要未说明：具体实验数值、模型压缩/轻量化方法、与遥感或目标检测的任何关联。论文聚焦GUI操作智能体，与遥感在轨目标检测场景差异极大。此外，摘要未讨论模型本身的轻量化（如剪枝、量化），仅讨论推理时的计算分配策略。

Ideas for my research: 尽管论文与遥感目标检测无直接关联，但以下思路可借鉴：(1) "选择性计算分配"（selective compute allocation）思想——在星上资源受限场景中，可对困难样本/小目标分配更多推理计算，对简单样本快速通过，实现自适应计算；(2) "失败感知控制机制"（failure-aware control）——在轨检测中可设计基于置信度或不确定性的失败检测与回退机制；(3) "收益递减"的实证结论提醒我们，在轻量化设计中需权衡计算投入与性能增益，避免过度计算；(4) 多维度扩展（contextual/temporal/structural/parallel）的系统评估方法论可迁移到轻量化目标检测的消融研究中。

Abstract: Deploying autonomous computer-use agents (CUAs) locally is increasingly important for privacy, cost efficiency, and practical usability, yet improving their performance under strict hardware constraints remains challenging. While recent studies show that inference-time scaling can improve frontier computer-use agents through additional computation during execution, its effectiveness for resource-constrained local models remains poorly understood. We present a systematic empirical study of inference-time scaling in local CUAs across contextual, temporal, structural, and parallel dimensions. We evaluate Qwen3-VL-8B/30B-A3B, UI-TARS-1.5-7B, and OpenCUA-7B on the OSWorld benchmark. Our results show that additional computation often yields diminishing returns while changing failure modes. Contextual scaling provides historical grounding that improves trajectory stability and task accuracy, but its gains saturate as token cost increases and failures shift from repetitive or stalled trajectories toward premature false successes. Temporal scaling similarly reduces max-step stalls, yet does not substantially improve task success, indicating that longer horizons often extend erroneous trajectories rather than correct them. We further find that structural decomposition can introduce planning and formatting overhead in local two-stage agents, while parallel scaling partially mitigates these failures at a substantial computational cost. Overall, our findings suggest that efficient local CUAs require selective compute allocation, failure-aware control mechanisms, and agentic frameworks designed around the capabilities and limitations of local models.

</details>


### [249] [DualG-MRAG: Decoupling Macro-Reasoning and Micro-Matching for Multimodal Retrieval-Augmented Generation](https://arxiv.org/abs/2607.28580)
*Jiacheng Tao,Qingyun Sun,Haonan Yuan,Ziwei Zhang,Jianxin Li*

Main category: cs.AI

TL;DR: 提出DualG-MRAG，一种双层级解耦架构（Macro推理图+Micro匹配图）的多模态检索增强生成框架，通过GNN检索器和动态规划解码来提升复杂多跳推理能力。

Research relevance: 弱相关（1分）。该论文聚焦于多模态检索增强生成（Multimodal RAG）和复杂多跳推理任务，属于自然语言处理与多模态理解领域，与遥感、卫星、在轨目标检测、模型轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。

Reading priority: 1分。该论文属于多模态RAG/NLP领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。除非对图神经网络在结构化推理中的应用有跨领域兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态检索增强生成（Multimodal RAG）中的复杂多跳推理（multi-hop reasoning）。应用场景：通用多模态问答（complex QA）。传感器类型：不涉及遥感传感器，仅涉及通用多模态数据（文本+图像）。目标类别：不涉及目标检测类别。

Model architecture: DualG-MRAG采用双层级解耦架构：Macro Graph用于全局拓扑路由（global topological routing），Micro Graph用于细粒度局部验证（precise local verification）。核心组件包括：GNN Retriever（图神经网络检索器，将检索建模为query-driven message passing过程）、动态规划解码机制（dynamic programming decoding，从GNN前向传播中提取显式推理路径）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法，也未讨论实时推理、边缘部署或星上部署相关话题。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。该工作面向通用多模态RAG推理，与星上/边缘部署场景无关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括：evidence recall（证据召回率）和complex QA accuracy（复杂问答准确率）。

Experiments: 摘要仅说明DualG-MRAG在evidence recall和complex QA accuracy上优于baseline方法，但未给出具体数值、对比方法名称或消融实验细节。

Limitations: 摘要未说明局限性。但可推断：1）该工作面向通用多模态RAG，未涉及遥感图像或卫星数据；2）GNN检索器与动态规划解码可能带来额外计算开销，但摘要未讨论效率问题；3）未涉及任何部署或轻量化考量。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但其"解耦全局推理与细粒度匹配"的思想有一定方法论借鉴潜力：1）在遥感目标检测中，可将"全局场景理解（Macro）"与"局部小目标精细检测（Micro）"解耦，避免小目标检测中全局上下文与局部细节相互干扰；2）GNN message passing的检索思路可类比于遥感场景中目标间空间关系的结构化建模。但整体而言，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: While Multimodal Retrieval-Augmented Generation (MM-RAG) has shown promising results, it still struggles with complex multi-hop reasoning tasks. Existing methods primarily focus on independent instance-level matching, which often fails to capture explicit relationships across modalities and documents. Although Graph-enhanced methods introduce structural modeling, they face a fundamental challenge in multimodal scenarios: incorporating fine-grained visual features leads to rapid graph expansion and retrieval noise, whereas coarse-grained representations cause the discarding of critical local evidence. To address this dilemma, we propose DualG-MRAG, a Dual-tier framework that introduces a decoupled architecture comprising Macro-reasoning and Micro-matching Graphs for Multimodal RAG. Specifically, to suppress retrieval noise by isolating global structural reasoning from fine-grained evidence matching, we construct a Macro Graph for global topological routing and a Micro Graph for precise local verification. Subsequently, to enable dynamic relevance propagation across heterogeneous evidence sources, we formulate retrieval as a query-driven message passing process via a GNN Retriever. Furthermore, to provide the generative model with coherent structural guidance, we introduce a dynamic programming decoding mechanism that extracts explicit reasoning paths directly from the GNN's forward pass, replacing the standard input of isolated document chunks. Extensive experiments demonstrate that DualG-MRAG outperforms baselines in both evidence recall and complex QA accuracy.

</details>


### [250] [OSReward: Instituting Standardized Evaluation for Cross-Platform Computer-Use Reward Models](https://arxiv.org/abs/2607.28609)
*Qiushi Sun,Kanzhi Cheng,Yian Wang,Bowen Yang,Hang Yan,Liheng Chen,Fangzhi Xu,Zichen Ding,Nuo Chen,Jialin Cao,Xingdong Gong,Zehao Li,Kaiming Jin,Xinfeng Yuan,Zhoumianze Liu,Jingyang Gong,Zhangyue Yin,Jiahui Gao,Zhiyong Wu,Tianbao Xie,Jianbing Zhang,Ben Kao,Lingpeng Kong*

Main category: cs.AI

TL;DR: 提出OSReward基准，系统评估视觉语言模型（VLM）作为计算机使用智能体（CUA）轨迹裁判的可靠性，并训练开源奖励模型OS-Shepherd以低成本提供稳定可靠的奖励信号。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于计算机使用智能体（CUA）的轨迹评估与奖励模型，属于通用人工智能/多模态大模型评估领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。虽然涉及VLM和模型效率（低成本、低开销），但目标场景是数字世界的agent评估，而非遥感图像理解或目标检测。

Reading priority: 1分。论文主题为CUA轨迹评估与VLM奖励模型，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：CUA（Computer-using agents）轨迹的自动验证与奖励建模；应用场景：数字世界中的智能体评估、数据整理、强化学习；传感器类型：不适用（处理的是agent轨迹文本/视觉记录）；目标类别：不适用（评估对象是agent是否完成任务指令）。

Model architecture: 摘要未说明具体网络结构细节。涉及两类模型：作为裁判的VLM（vision-language models）judges，以及训练的开源奖励模型OS-Shepherd（9B和35B参数规模）。

Lightweight method: 摘要未说明传统轻量化方法（剪枝、量化、蒸馏、NAS等）。但涉及"低成本"（low-cost）和"效率"（efficiency）考量：OS-Shepherd以比前沿商业模型低30-60%的成本提供奖励信号，属于模型效率/成本优化范畴，但并非面向边缘或星上部署的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。OS-Shepherd为9B和35B参数的开源模型，规模较大，不适用于星上或边缘部署场景。摘要仅提及推理成本比前沿模型低30-60%，但未给出具体部署指标。

Datasets and metrics: 数据集：OSReward基准（含OSReward-Hard挑战集和OSReward-Multi细粒度评分集）、OS-Shepherd-100K开放语料库；评价指标：VLM裁判的可靠性（与ground-truth verdicts的一致性）、效率（efficiency）和对齐（alignment）评分；实验设置：多阶段人工标注ground-truth，覆盖多种agent backbone和平台。

Experiments: 主要发现：最先进的VLM裁判也达不到理想裁判水平，存在系统性"宽松偏差"（leniency bias），将失败运行误判为成功；少数可靠的裁判成本过高，无法大规模运行；开源模型性能落后。OS-Shepherd（9B和35B）作为开源奖励模型，以比前沿模型低30-60%的成本匹配商业裁判性能。

Limitations: 与本研究方向完全无关。摘要未涉及遥感图像、目标检测、小目标、旋转框、轻量化网络设计、星上部署等任何相关要素。该工作属于LLM/VLM评估与奖励建模领域，对遥感在轨目标检测轻量化课题无可借鉴的直接技术点。

Ideas for my research: 弱相关借鉴点有限，但可考虑：1) 其"低成本模型匹配高成本模型"的思路与轻量化目标一致，可类比为用轻量模型逼近大模型性能（类似知识蒸馏思想）；2) 其系统化构建benchmark评估模型可靠性的方法论，可借鉴用于构建遥感轻量化检测模型的评估基准；3) 其"宽松偏差"分析提醒在评估轻量化模型时需注意系统性偏差。但整体上对遥感在轨目标检测轻量化课题的直接帮助很小。

Abstract: Computer-using agents (CUAs) are advancing rapidly across the digital world. A CUA trajectory records the agent's actions, states, and reasoning. Verifying whether it fulfilled the task instruction is central to CUA evaluation, data curation, and reinforcement learning. Neither human-written verifiers nor human annotators can provide such verification at scale, so the field increasingly turns to vision-language models (VLMs) as judges of CUA trajectories. But a fundamental question has long gone unexamined: are these VLM judges reliable enough? To study it systematically, we introduce OSReward, a realistic, high-quality benchmark that evaluates VLM judges on CUA trajectories. The trajectories come from diverse agent backbones executing human-verified instructions across platforms, then rigorously labeled with ground-truth verdicts through multi-stage human annotation. Building on it, we derive OSReward-Hard, a challenge set concentrating genuinely hard cases, and OSReward-Multi for fine-grained efficiency and alignment scoring. The most comprehensive evaluation of VLM judges to date finds even state-of-the-art models fall short of an ideal judge, sharing a systematic leniency bias that mislabels failed runs as successes. The few reliable enough to trust are too expensive to run at scale, while affordable open models trail far behind. To close this gap, we construct and release OS-Shepherd-100K, an open corpus of reasoning-annotated trajectory judgments for the CUA community. On it, we train OS-Shepherd (9B and 35B), open reward models that supply low-cost, stable, and reliable reward signals, matching commercial judges at 30-60% lower cost than the frontier. Extensive analyses further inform the design of reliable CUA reward at scale. Our code, benchmark, dataset, and model checkpoints are available at https://os-copilot.github.io/OSReward-Home/.

</details>


### [251] [AISPA: User-Centric System Prompt Auditing for Large Language Model Applications](https://arxiv.org/abs/2607.28617)
*Xiangning Lin,Shenzhe Zhu,Shu Yang,Zhenyu Zhang,Haoqian Zhang,Yipeng Zhao,Chengxuan Qian,Tianwei Wang,Ziheng Zhang,Zhenlong Yuan,Dingcheng Wang,Juncheng Wu,Yuan Si,Jiaxin Liu,Baolong Bi,Robert Mahari,Tobin South,Dazza Greenwood,Zexue He,Rishi Bommasani,Sophia Kazinnik,Andreas Haupt,Samuele Marro,Erik Brynjolfsson,Alex Pentland,Jiaxin Pei*

Main category: cs.AI

TL;DR: 提出AISPA框架，系统审计88个商业AI产品中3,249条system prompt指令，按八个维度评估其对用户的保护性或问题性。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于AI系统system prompt的审计与透明度问题，属于AI治理/安全领域，不涉及遥感、卫星、目标检测或模型压缩。

Reading priority: 1分。论文主题为AI系统prompt审计与治理，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：AI系统prompt审计与评估；应用场景：商业AI产品的系统提示词治理；传感器类型：不适用；目标类别：不适用。

Model architecture: 摘要未说明具体模型结构。核心贡献是AISPA审计框架，包含八个评估维度，用于分类system prompt指令为protective（保护用户）或problematic（损害用户）。

Lightweight method: 不涉及。摘要未提及任何剪枝、量化、知识蒸馏、NAS、轻量网络或模型压缩方法。

Onboard deployability: 不涉及。摘要未提及星上部署、边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：88个商业AI产品的3,249条system prompt指令。评价指标：八个审计维度、protective/problematic指令分类比例、产品覆盖率（98.9%含至少一条protective指令，24%覆盖全部八维度，约40%产品含至少一条problematic指令）。

Experiments: 对3,249条指令进行审计，得出四项核心发现：(1)不同产品/开发者的prompt设计差异大；(2)protective指令广泛但浅层；(3)prompt随时间变长且更保护用户；(4)problematic指令仍普遍存在，且protective与problematic常共存于同一prompt。

Limitations: 摘要未说明局限性。但该研究与遥感目标检测/轻量化完全无关，属于AI安全与治理领域，对在轨检测课题无直接参考价值。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题无可借鉴点。该论文属于AI系统治理/审计方向，与遥感、目标检测、模型压缩均无交集。若强行寻找关联，其"系统化审计框架"的思维方法（多维度评估、大规模样本审查）可类比于对轻量化模型进行多维度评估，但这是非常间接且牵强的联系。

Abstract: System prompts are instructions configured by developers to govern the behaviors of foundation models in AI applications. They are used throughout commercial AI products, but are rarely disclosed to the public or regulators, creating a serious trust and accountability gap in the wide deployment of AI systems. In this paper, we introduce Artificial Intelligence System Prompt Assurance (AISPA), a user-centric framework for systematically auditing system prompts in AI systems. AISPA examines specific parts of a system prompt and evaluates them along eight dimensions that matter to users. We then use this framework to review 3,249 instructions from system prompts in 88 commercial AI products, classifying each instruction as either protective (of users) or problematic. Our audit surfaces four core findings. First, system prompt design varies substantially across products and developers, with some organizations averaging over 60 protective instructions per product while others average fewer than 5. Second, protective instructions are widely adopted but shallow in scope: 98.9% of products contain at least one, yet only 24% cover all eight dimensions of the AISPA taxonomy. Third, system prompts have grown steadily longer and more protective of users, suggesting that user protection is becoming a more visible concern in commercial prompt design. Fourth, despite this progress, problematic instructions remain pervasive: roughly 40% of products contain at least one instruction that works against user interests, and protective and problematic instructions frequently coexist within the same prompt. Our findings highlight the need for greater transparency, standardization, and independent oversight for system prompts in commercial AI products.

</details>


<div id='cs.RO'></div>

# cs.RO [[Back]](#toc)

### [252] [It's Not Just More Demos: Counterfactual Action Sensitivity Coverage for Data-Efficient Robust Robot Imitation](https://arxiv.org/abs/2607.27261)
*Giovanni D'urso,Kaushik Roy,Nicholas Lawrance,Brendan Tidd*

Main category: cs.RO

TL;DR: 提出Counterfactual Nuisance Behaviour Cloning (CFNBC)，一种离线数据选择框架，通过测量策略在"干扰"下的action drift来挑选少量高价值演示数据，实现针对性的鲁棒性修复。

Research relevance: 弱相关（2分）。该论文属于机器人操作领域的visuomotor imitation learning，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。它关注的是策略鲁棒性修复的数据选择问题，属于泛机器学习/机器人学习范畴，对遥感目标检测轻量化课题的借鉴价值有限。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向基本无关，但"action drift"数据选择思想对遥感模型鲁棒性修复有一定间接启发。若非时间充裕，可不优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：visuomotor imitation learning（视觉运动模仿学习）中的鲁棒性修复；应用场景：机器人操作（bimanual cube transfer, cube stacking）；传感器类型：视觉观测（RGB）；目标类别：机器人操作对象（立方体）。与遥感场景无关。

Model architecture: 摘要未说明具体网络结构。核心是CFNBC框架，包含：1) 基于nominal policy生成paired clean/nuisance观测；2) 计算action drift作为策略敏感性信号；3) 基于该信号从候选池中选择紧凑且响应多样的修复集。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。本文的"轻量"体现在数据层面——选择少量（20-30个）高价值样本进行鲁棒性修复，而非模型层面的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署信息。本文不涉及星上或边缘部署。

Datasets and metrics: 实验环境：MuJoCo bimanual cube transfer和SimplerEnv cube stacking。评价指标：action drift与nuisance-induced failure的相关性、修复后策略性能（与matched-budget random selection对比）。

Experiments: 主要结果：1) action drift与nuisance-induced failure相关；2) 仅用20-30个selected candidates进行response-guided repair，显著优于同预算随机选择，接近更大随机预算的性能。

Limitations: 1) 仅验证于机器人操作仿真环境，未在真实场景或遥感数据上验证；2) 摘要未说明对复杂视觉干扰（如光照、遮挡）的泛化边界；3) 未涉及模型压缩或推理加速；4) 数据选择依赖paired clean/nuisance观测的生成，在遥感场景中难以直接迁移。

Ideas for my research: 1) "action drift"思想可类比迁移：在遥感目标检测中，可度量模型在输入扰动（如光照变化、传感器噪声、云遮挡）下的预测漂移，用于识别模型的脆弱响应模式；2) 数据选择策略可借鉴：在遥感数据增强或微调时，优先选择能覆盖模型脆弱模式的样本，而非盲目增加数据量；3) 该框架的"数据中心视角"（最有用的数据不是最多、最多样或最难的，而是覆盖当前策略脆弱响应模式的）对遥感小样本场景下的数据筛选有启发。

Abstract: Visuomotor imitation learning has demonstrated success for manipulation tasks. However, the trained policies remain brittle to visual `nuisances', with even minor task-preserving variations such as lighting, distractions or changes in colour result in heavy degradation of the trained policy's performance. While increasing data diversity can improve robustness, it is unclear which additional demonstrations are informative for a particular trained policy. We propose Counterfactual Nuisance Behaviour Cloning (CFNBC), an offline data-selection framework for targeted robustness repair. Starting from a nominal policy trained on `clean' demonstrations, CFNBC generates paired clean and nuisance observations that preserve the expert action, then measures \emph{action drift}: the change in the policy's predicted action under a nuisance that should not alter the desired behaviour. This provides a policy-specific sensitivity signal for selecting a compact, response-diverse repair set from a larger candidate pool, without requiring rollout success labels or online policy execution. We show in MuJoCo bimanual cube transfer and SimplerEnv cube stacking that action drift correlates with nuisance-induced failure, and that response-guided repair with only $20$--$30$ selected candidates substantially outperforms matched-budget random selection while approaching the performance of much larger random repair budgets. These results support a data-centric view of robustness repair: the most useful data are not necessarily the most numerous, visually diverse, or obviously difficult, but the examples that cover fragile response modes of the current policy.

</details>


### [253] [Simulation of Surgical Suturing Using Position-Based Dynamics and the Material Point Method for Robot Reinforcement Learning](https://arxiv.org/abs/2607.27494)
*Tleukhan Mussin,Yafei Ou,Mahdi Tavakoli*

Main category: cs.RO

TL;DR: 提出一个基于PBD缝合线和MPM软组织的机器人手术缝合仿真环境，支持缝合线与软组织双向接触耦合，并配套GPU并行加速和强化学习训练环境。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于手术机器人缝合仿真，涉及物理仿真（PBD/MPM）、GPU并行计算和强化学习，既非遥感/卫星场景，也非目标检测任务，更不涉及模型轻量化或边缘部署。属于机器人仿真与物理建模领域。

Reading priority: 1分。论文主题为手术机器人缝合仿真，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务：手术缝合仿真环境构建与自主缝合强化学习训练。场景：机器人手术仿真（surgical robotics simulation）。传感器类型：不适用（非遥感传感器）。目标类别：缝合线（suture）、软组织（soft tissue）、针（needle）。

Model architecture: 采用position-based dynamics (PBD)建模缝合线，material point method (MPM)建模软组织，提出PBD缝合线与MPM软组织之间的双向接触耦合方法（含摩擦力和拖曳力）。仿真环境针对GPU执行优化，使用多个CUDA streams并行处理场景。强化学习环境基于ML-Agents构建。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。仅提到GPU并行优化（multiple CUDA streams）和并行场景处理，属于仿真计算加速而非深度学习模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。该工作面向GPU仿真环境，与星上/边缘部署无关。强化学习智能体训练于仿真环境，但未讨论模型推理时的资源开销或实时部署约束。

Datasets and metrics: 数据集：未使用遥感或视觉数据集，使用自建仿真环境。评价指标：针插入（needle insertion）和针提取（needle extraction）的成功率（success rate），在最严格距离阈值下分别达到80%和68%。

Experiments: 使用ML-Agents训练强化学习智能体，验证了在仿真环境中自主完成缝合子任务（针插入、驱动、提取）的可行性。在严格距离阈值下，针插入成功率为80%，针提取成功率为68%。摘要未提供与其他方法的对比实验或消融实验细节。

Limitations: 摘要未说明：模型参数量、推理速度、部署资源需求；未提供与已有缝合仿真方法的定量对比；未讨论仿真到真实（sim-to-real）迁移问题；未涉及任何遥感或目标检测相关内容。该工作与本研究方向相关性极低。

Ideas for my research: 借鉴意义有限。可能的间接启发包括：(1) GPU并行优化（CUDA streams）思路可用于遥感模型推理加速的工程实现；(2) 强化学习在仿真环境中训练的策略可类比于在轨模型的自适应学习场景，但关联较弱；(3) 物理仿真中的接触耦合建模方法与遥感目标检测无直接关联。总体而言，该论文对本课题的可借鉴点很少。

Abstract: Recent advances in robotics research have created a strong demand for high-performance simulators. Surgical robotics simulation faces unique challenges due to the need to model diverse objects, such as rigid instruments, soft tissue, and fluids. While many studies simulate sutures or soft tissue independently, only a few have considered the complete soft-tissue suturing scenario, including the contact between sutures and deformable tissue during suture insertion. Building on previous work, this paper presents a novel suturing simulation environment using sutures modelled by position-based dynamics (PBD) and soft bodies modelled by the material point method (MPM) while considering two-way contact with frictional and drag forces. We introduce a contact coupling method between the PBD suture and the MPM soft tissue, enabling visually plausible suture-tissue interactions. The simulator is optimized for GPU execution with parallel scenes using multiple CUDA streams, and we present a Reinforcement Learning (RL) environment for autonomous suturing sub-tasks, including needle insertion, driving, and extraction. Using ML-Agents, RL agents trained in the simulator show stable learning and achieve 80% and 68% success rates in needle insertion and extraction, respectively, under the strictest distance threshold.

</details>


### [254] [Corrigible Assistance in One Round: Pragmatic-Pedagogic Best Response](https://arxiv.org/abs/2607.27508)
*Elle Lazarski,Jaime Fernández Fisac*

Main category: cs.RO

TL;DR: 本文研究人机协作中的"assistance games"（辅助博弈），提出在特定类别下通过pragmatic-pedagogic推理在单时间步内消解目标不确定性，使全时域博弈可通过可处理的best-response过程精确求解，并在积木搭建协作任务上验证。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于人机交互（Human-Robot Collaboration）、博弈论（game theory）、POMDP规划与inverse optimal control领域，不涉及遥感、卫星、Earth observation、目标检测或模型压缩/轻量化。相关性评分为1分。

Reading priority: 1分。论文属于人机交互与博弈论领域，与遥感卫星在轨目标检测及模型轻量化研究方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：人机协作辅助博弈（assistance games）中的目标推断与策略求解；应用场景：协作式积木搭建（collaborative block-building）；传感器类型：不适用（非视觉感知任务）；目标类别：不适用。

Model architecture: 摘要未说明具体模型结构。核心方法基于pragmatic-pedagogic reasoning（语用-教学推理）与best-response过程，用于在单时间步内消解目标不确定性，替代传统POMDP全时域规划。

Lightweight method: 不涉及。论文未讨论任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计。其"tractable"（可处理）性体现在博弈求解的计算复杂度降低（将POMDP求解简化为best-response），而非模型轻量化。

Onboard deployability: 不适用。摘要未说明任何星上/边缘部署相关信息，未提及参数量、FLOPs、延迟、显存、能耗或实时推理指标。

Datasets and metrics: 摘要未说明具体数据集名称。实验仅在"simple collaborative block-building example"（简单协作积木搭建示例）上进行验证，未提及标准benchmark数据集或量化评价指标。

Experiments: 摘要仅提及在简单积木搭建协作示例上验证了理论结果和所提方法，并与mainstream inverse optimal control（主流逆最优控制）进行了对比，指出后者存在inference ceiling（推断天花板）问题。未提供具体数值实验结果。

Limitations: 摘要未说明局限性。但可推断：方法仅适用于特定类别的assistance games（需满足pragmatic-pedagogic推理可在单步内消解目标不确定性的条件），泛化性有限；实验仅基于简单积木示例，缺乏复杂场景验证。

Ideas for my research: 与本人课题（遥感卫星在轨目标检测模型轻量化）基本无可借鉴点。唯一可能的间接启发是：论文中"将复杂POMDP求解简化为可处理的best-response过程"的思想，在方法论层面类似于"将复杂检测模型通过结构简化降低计算复杂度"的思路，但两者领域差异过大，借鉴价值极低。

Abstract: Assistance games formalize human-robot collaboration under asymmetric information: the human knows the goal, while the robot must infer it from observation and interaction in order to assist effectively. In general, computing optimal assistance game strategies online is intractable, since exact solutions require planning in a POMDP. We identify a class of assistance games in which pragmatic-pedagogic reasoning resolves goal uncertainty in a single time step, rendering the full-horizon game exactly solvable by a tractable best-response procedure. Within this class, we show that mainstream inverse optimal control exhibits an inference ceiling that hinders alignment, while pragmatic-pedagogic reasoning overcomes this barrier by immediately disambiguating goals through actions that look equivalent under task execution alone. Finally, we validate our theoretical results and proposed method on a simple collaborative block-building example.

</details>


### [255] [Failure Detection for Surgical Robot Imitation Policies via Flow-Matching World Modeling](https://arxiv.org/abs/2607.27511)
*Zhefeng Huang,Yilin Cai,Ankit Patel,Mohammad Hajiha,Brendan Browne,Yue Chen*

Main category: cs.RO

TL;DR: 提出FoMo-FD，一种基于flow-matching世界模型的机器人手术失败检测方法，通过逆传输非一致性评分实现无需失败样本的窗口级检测。

Research relevance: 弱相关（1-2分）。论文聚焦于自主机器人手术（autonomous robotic surgery）中的失败检测（failure detection），属于机器人操作与安全监控领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。既非遥感/卫星场景，也不涉及目标检测或模型压缩/轻量化方法。唯一可能的间接关联是flow-matching world model作为一种生成式时序建模方法，或可迁移到遥感视频时序建模，但关联非常间接。

Reading priority: 1分。论文聚焦机器人手术失败检测，与遥感卫星在轨目标检测和模型轻量化方向基本无关，不涉及遥感场景、目标检测或模型压缩方法，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人手术失败检测（failure detection）；应用场景：自主机器人手术（autonomous robotic surgery），使用da Vinci Research Kit (dVRK)；传感器类型：腕部相机（wrist-camera）等视觉观测；目标类别：手术操作中的失败模式（20种failure modes），非遥感目标。

Model architecture: FoMo-FD采用action-conditioned flow-matching world model（动作条件流匹配世界模型），学习名义上的短时程视觉动力学（nominal short-horizon visual dynamics）。通过逆传输非一致性评分（inverse-transport nonconformity scoring）对观测到的端点潜变量（endpoint latents）进行评分，实现窗口级（window-level）视觉-动作不一致性检测。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法，也未讨论实时推理、边缘部署或星上部署等轻量化相关话题。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。虽然失败检测需要实时性，但摘要未给出任何计算开销或部署可行性数据。

Datasets and metrics: 数据集：四个手术相关操作任务（surgically relevant manipulation tasks），包含20种失败模式，在仿真和真实世界实验中评估，使用da Vinci Research Kit (dVRK)。评价指标：失败检测率（Failure Detection Rate, FDR）和误报率（False Alarm Rate, FAR）。最佳结果：腕部相机视角达到96.6% FDR在1.3% FAR下。

Experiments: 与observation-level anomaly baselines以及同一世界模型的prediction-error变体进行对比。FoMo-FD在四个手术任务上优于这些基线，腕部相机视角表现最强（96.6% FDR @ 1.3% FAR）。采用conformal calibration在成功执行样本上校准检测阈值，无需假设未来失败类型。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于机器人手术安全领域；2. 摘要未说明模型计算复杂度、推理速度、参数量等部署指标；3. 仅在手术场景验证，未涉及遥感图像或视频数据；4. 依赖action-conditioned世界模型，需要动作标签，在遥感场景中难以直接迁移。

Ideas for my research: 1. flow-matching world model作为一种生成式时序建模方法，理论上可借鉴用于遥感视频时序异常检测或目标运动建模，但迁移成本高；2. conformal calibration（保形校准）用于无监督阈值设定的思想，可借鉴到遥感目标检测中无标注场景下的置信度校准；3. 逆传输非一致性评分（inverse-transport nonconformity）作为一种无需失败样本的异常检测思路，或可用于遥感图像中的异常目标检测。总体而言借鉴价值有限。

Abstract: Imitation learning has shown increasing promise for autonomous robotic surgery, yet safe deployment remains challenging due to the safety-critical nature of surgical tasks and the complexity and variability of surgical environments. Failure detection is therefore an essential safeguard, but its development remains difficult due to the challenges of scarce failure data, highly variable manipulation dynamics, and the need to balance missed detections against disruptive false alarms. To address these challenges, we introduce FoMo-FD (Flow-Matching World Model for Failure Detection), a failure detection method that learns nominal short-horizon visual dynamics with an action-conditioned flow-matching world model. FoMo-FD scores the inverse-transport nonconformity of observed endpoint latents, enabling window-level detection of visual-action inconsistencies without requiring failure demonstrations. Detection thresholds are obtained by conformal calibration on successful executions, yielding task-specific alarms without assuming future failure types. We evaluate FoMo-FD on four surgically relevant manipulation tasks with twenty failure modes across simulation and real-world experiments using the da Vinci Research Kit (dVRK). Results show that FoMo-FD outperforms observation-level anomaly baselines and a prediction-error variant of the same world model, with the wrist-camera view achieving the strongest performance, including a 96.6% failure detection rate (FDR) at a 1.3% false alarm rate (FAR).

</details>


### [256] [Cross-Embodiment Transfer via Behavior-Aligned Representations](https://arxiv.org/abs/2607.27549)
*Ajay Sridhar,Jensen Gao,Jonathan Yang,Jean Mercat,Suneel Belkhale,Dorsa Sadigh*

Main category: cs.RO

TL;DR: 研究大规模模仿学习中行为对齐表示（如物体边界框、语言动作、末端执行器轨迹）在视觉-语言-动作（VLA）模型中的作用，以促进跨机器人本体迁移。

Research relevance: 与本研究方向弱相关（2分）。论文属于机器人操作领域的跨本体迁移研究，不涉及遥感、卫星、地球观测或航空图像，也不涉及目标检测模型轻量化或星上部署。虽然文中提到object bounding boxes，但这是作为机器人行为对齐表示的一部分，并非遥感目标检测任务。

Reading priority: 1分。论文主题为机器人操作跨本体迁移，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人操作（robot manipulation）中的跨本体迁移（cross-embodiment transfer）；应用场景：仿真到真实（sim-to-real）机器人策略学习；传感器类型：机器人视觉（RGB）；目标类别：机器人操作对象（非遥感目标）。

Model architecture: 视觉-语言-动作（VLA）模型，融合行为对齐表示（behavior-aligned representations），包括object bounding boxes、language motions、end-effector traces等。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署信息。

Datasets and metrics: 使用仿真基准（simulation-based benchmark）评估跨本体迁移；真实机器人策略在仿真数据预训练后，任务完成进度提升28%。具体数据集名称和评价指标细节摘要未说明。

Experiments: 对比不同表示类型及其融合方式；发现end-effector traces对迁移最有利；表示在更大先验数据集下更有用；可利用无动作数据（action-free data）；仿真到真实跨本体迁移使真实机器人任务完成进度提升28%。

Limitations: 论文聚焦机器人操作领域，与遥感卫星目标检测及轻量化方向基本无关。摘要未提供模型复杂度、推理效率等与部署相关的信息。

Ideas for my research: 借鉴意义有限。可参考的点：其一，"行为对齐表示"的思想——在遥感目标检测中，可考虑引入与任务对齐的中间表示（如旋转框、语义掩码）来提升模型泛化能力；其二，"利用无标注/无动作数据"的思路与遥感领域利用无标签遥感影像进行自监督预训练有相似之处。但整体上该论文与在轨目标检测轻量化课题关联度很低。

Abstract: Recent progress in large-scale imitation learning for robot manipulation has been driven by leveraging datasets across a wide range of robot embodiments. However, achieving significant cross-embodiment transfer is often still challenging. In this work, we study the role of using behavior-aligned representations (e.g., object bounding boxes, language motions, end-effector traces of robot motion) in vision-language-action (VLA) models to promote cross-embodiment transfer. We hypothesize that by possessing invariances across embodiments while being predictive of robot actions, these representations can help unify large-scale cross-embodiment data to enhance transfer. To assess our hypothesis, we develop a simulation-based benchmark designed to assess transfer with diverse cross-embodiment data to new embodiments. Using this benchmark, we compare different representations and ways of incorporating them. We identify that end-effector traces can be particularly beneficial for transfer, representations are generally more useful with larger prior datasets, and can be used to benefit from action-free data. We also demonstrate that they can enhance sim-to-real cross-embodiment transfer, improving task completion progress of real robot policies pre-trained on simulation data by 28%. We provide videos of our evaluations at our website: https://ajaysridhar.com/barx/.

</details>


### [257] [A Systems Engineering Framework for Vision-Language-Enabled UAV Triage and Disaster Response](https://arxiv.org/abs/2607.27597)
*Swapnil Saha,Bhuvan Rajanasiriyur Jagadeesha,Karishma Patnaik,Neelakshi Majumdar*

Main category: cs.RO

TL;DR: 本文探讨将视觉语言模型（VLM）作为人类-UAV协同中的协调智能体嵌入灾害响应系统，通过MBSE方法设计架构并集成软件在环测试，初步人因评估显示可降低操作员感知工作负荷并提升AI信任度。

Research relevance: 弱相关（2分）。论文涉及UAV（无人机）和灾害响应场景，但核心是Vision Language Model（VLM）作为人机协同协调智能体，属于human-autonomy teaming和决策支持方向，不涉及遥感图像目标检测，也不涉及模型轻量化或星上部署。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅UAV和灾害响应场景有间接关联。

Reading priority: 1分。论文聚焦VLM驱动的灾害响应人机协同系统，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测算法、遥感图像处理或模型压缩技术，仅作为跨领域系统架构参考价值有限。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：灾害响应中的人类-UAV协同任务协调（mission-level task coordination）；应用场景：灾害响应、应急管理、civil safety；传感器类型：摘要未明确说明（涉及UAV，但未指明具体传感器如光学/SAR/高光谱）；目标类别：摘要未说明（不涉及具体目标检测类别）。

Model architecture: 核心架构为VLM Coordinator Agent（视觉语言模型协调智能体），配合UAV Mission Control和Task Allocator三个关键组件，集成于仿真与控制环境中。采用Model-Based Systems Engineering（MBSE）方法，使用use case和block definition diagrams描述系统角色、内部结构和组件交互。摘要未说明具体VLM模型名称或网络结构。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（无pruning、quantization、knowledge distillation、NAS等）。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗等部署指标。虽然涉及UAV在环系统，但讨论的是任务协调层面的软件架构，而非机载/星上推理的硬件部署可行性。

Datasets and metrics: 数据集：摘要未说明（不涉及遥感数据集）。评价指标：采用人因评估（human-factors evaluation），包括NASA-TLX风格的主观工作负荷（mental demand、effort、frustration）、AI信任度评分和沟通清晰度评分。实验设置：7名参与者进行初步人因评估。

Experiments: 主要实验为初步人因评估（preliminary human-factors evaluation），7名参与者参与。结果显示：感知工作负荷在mental demand、effort、frustration三个维度上降低，AI信任度和沟通清晰度评分较高。摘要未说明与基线方法的定量对比或消融实验。

Limitations: 1. 样本量极小（仅7人），人因评估的统计效力有限；2. 摘要未说明VLM的具体选型、推理成本或实时性；3. 未涉及任何遥感图像处理或目标检测任务；4. 未涉及模型轻量化或边缘/星上部署；5. 属于系统架构与交互设计层面研究，与视觉感知算法本身关系不大。

Ideas for my research: 1. 借鉴其MBSE（Model-Based Systems Engineering）系统建模思路，可用于在轨遥感目标检测系统的整体架构设计，明确星上推理、任务调度与地面协同的组件关系；2. 其human-autonomy teaming和任务协调思想可启发"星上检测结果如何与地面任务系统协同"的接口设计；3. 但总体而言，本文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限，不建议作为核心参考。

Abstract: Recent advances in Vision Language Models (VLMs) have created new opportunities for disaster response, where responders must interpret large volumes of sensor data under time pressure. Current VLM applications include social media monitoring for situational awareness, generation of draft action plans, and translation of technical alerts into public-facing messages. While these efforts can accelerate information flow, they remain largely limited to decision-support roles. Such approaches can increase operator burden because humans must still translate outputs into coordinated actions across teams and robotic assets. This study explores the viability of embedding VLMs as coordination agents within the human-UAV loop. The proposed architecture integrates natural language interaction, mission-level task coordination, software-in-the-loop implementation, and communication aligned with the Incident Command System (ICS). Rather than functioning solely as advisory tools, VLMs facilitate communication between human operators, mission control logic, and UAV task execution. The framework was developed using a Model-Based Systems Engineering (MBSE) approach, with use case and block definition diagrams representing system roles, internal structure, and component interactions. Three key elements, the VLM Coordinator Agent, UAV Mission Control, and Task Allocator, were implemented within an integrated simulation and control environment. A preliminary human-factors evaluation with seven participants showed reduced perceived workload across mental demand, effort, and frustration, along with high ratings for AI trust and communication clarity. By integrating MBSE, software-in-the-loop testing, and human-factors evaluation, this work advances scalable human-autonomy teaming for high-stakes disaster response, with broader implications for aerospace autonomy and civil safety.

</details>


### [258] [Arm2Air: Cross-Embodiment Skeleton Transfer for 3D Relay Formation](https://arxiv.org/abs/2607.27627)
*Dohun Lee,Kyeonghyun Yoo,Seokmin Kim,Byongho Lee,Seungjoo Oh,Hwangnam Kim*

Main category: cs.RO

TL;DR: Arm2Air通过跨具身迁移（cross-embodiment transfer）将机械臂避障骨架迁移到无人机中继部署任务，利用Low-Rank Adaptation（LoRA）实现数据与计算高效的UAV relay placement规划。

Research relevance: 弱相关（2分）。论文涉及UAV（无人机）中继网络部署，属于机器人/通信规划领域，与遥感卫星在轨目标检测及轻量化方向基本无关。虽然涉及LoRA参数高效微调（0.134M参数 vs 1.383M），在"参数高效"层面有一定方法学借鉴意义，但任务场景（UAV relay placement）、传感器类型（无遥感影像）、目标（无目标检测）均与本研究方向不匹配。

Reading priority: 2分（低优先级）。论文与遥感卫星在轨目标检测及轻量化方向弱相关，核心价值仅在LoRA参数高效微调的方法学层面有一定借鉴意义，但任务场景、数据模态、部署目标均不匹配。若时间紧张可跳过，仅需了解LoRA在跨域迁移中的应用即可。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：UAV relay placement（无人机中继部署/路径规划），属于机器人运动规划与通信网络优化交叉任务。应用场景：灾后通信基础设施损坏后的应急通信恢复。传感器类型：摘要未说明（不涉及遥感影像）。目标类别：无目标检测，目标是规划中继节点位置。

Model architecture: 采用transformer-based transfer platform作为迁移平台，源域使用pretrained Neural MP model（神经运动规划模型）生成机械臂运动轨迹，转换为有序骨架（ordered skeletons）用于预训练；目标域通过Low-Rank Adaptation（LoRA）适配到UAV域。整体为"源域预训练 + 目标域LoRA微调"的跨具身迁移框架。

Lightweight method: 核心轻量化手段是Low-Rank Adaptation（LoRA）参数高效微调：仅更新0.134M参数（对比Scratch和Full Fine-tuning的1.383M参数），参数更新量减少约90.3%。这属于参数高效微调（PEFT）范畴，而非传统模型压缩（剪枝/量化/蒸馏/NAS）。摘要未涉及剪枝、量化、知识蒸馏或NAS。

Onboard deployability: 摘要未说明星上/边缘部署相关信息。未报告FLOPs、延迟（推理延迟）、显存、能耗等指标。仅报告了规划运行时间（end-to-end planning runtime）减少64.9%，以及参数更新量0.134M，但这是训练阶段参数效率而非推理部署效率。与在轨部署（FPGA/NPU/TensorRT）无直接关联。

Datasets and metrics: 数据集：9张高遮挡3D城市地图（held-out）+ 30张密集城市地图（high-obstruction组）。评价指标：end-to-end planning runtime（端到端规划运行时间）、bottleneck capacity（瓶颈容量）、capacity variance、maximum hop distance、hop-distance variance、relay displacement、relay-position RMSE（均方根误差）。无遥感数据集（如DOTA、NWPU VHR-10、SAR等）。

Experiments: 主要结果：1) 在9张高遮挡3D城市地图上，相对最快传统规划器，中位端到端规划运行时间降低64.9%；2) 在30张密集城市地图的高遮挡组中，相对IMPC-MD基线，bottleneck capacity提升32.6%，capacity variance降低74.7%，最大跳距降低13.2%，跳距方差降低75.2%，relay displacement降低16.9%；3) 仅用3张目标域训练地图，relay-position RMSE相对from-scratch训练降低53.6%，仅更新0.134M参数（对比1.383M）。

Limitations: 1) 任务场景与遥感/卫星目标检测完全无关，属于机器人运动规划+通信网络优化；2) 摘要未说明推理阶段的计算开销、模型大小、部署硬件；3) 未涉及任何遥感影像数据（光学/SAR/高光谱/多光谱）；4) 未涉及目标检测任务；5) 跨具身迁移的泛化性仅在城市地图场景验证，未说明对遥感场景的适用性。

Ideas for my research: 1) LoRA参数高效微调思路可借鉴：在遥感目标检测模型轻量化中，若需将预训练大模型适配到特定遥感场景（如舰船/飞机检测），可用LoRA仅微调少量参数，降低训练成本；2) "有序结构先验跨任务迁移"的思想可类比：将通用检测模型的结构先验迁移到遥感小目标检测任务；3) 参数更新量对比（0.134M vs 1.383M）的量化报告方式值得借鉴，可作为轻量化论文中训练效率的指标；4) 但需注意，本文的轻量化是训练阶段参数效率，而非推理阶段模型压缩，与在轨部署（推理延迟/FLOPs/能耗）需求不同。

Abstract: Unmanned aerial vehicle (UAV) relay networks can restore connectivity after communication infrastructure is damaged. Urban relay placement is difficult because line-of-sight blockage, communication range, altitude, and three-dimensional obstacles must be considered jointly. Arm2Air transfers obstacle-avoidance skeletons from robot arms to UAV relay placement through cross-embodiment transfer. Source-domain robot-arm motions from a pretrained Neural MP model are converted into ordered skeletons that pretrain a transformer-based transfer platform, which is then adapted to the UAV domain using limited target data and Low-Rank Adaptation. The transferred skeleton initializes a relay chain that is refined for connectivity, bottleneck capacity, delay, and movement cost. On nine held-out high-clutter 3D urban maps, Arm2Air reduced median end-to-end planning runtime by 64.9 percent relative to the fastest conventional planner. On the high-obstruction group of a separate 30-map dense urban holdout, it increased bottleneck capacity by 32.6 percent, reduced capacity variance by 74.7 percent, reduced maximum hop distance by 13.2 percent, reduced hop-distance variance by 75.2 percent, and reduced relay displacement by 16.9 percent relative to IMPC-MD. With only three target-domain training maps, Arm2Air reduced relay-position root mean square error by 53.6 percent relative to training from scratch while updating 0.134 million parameters, compared with 1.383 million for Scratch and Full Fine-tuning. These results demonstrate computationally and data-efficient UAV relay placement and suggest a broader principle for transferring ordered structural priors across heterogeneous embodied tasks.

</details>


### [259] [LabEvolver: Training-Free Experience Evolution for Safe and Grounded Wet-Lab Agents](https://arxiv.org/abs/2607.27690)
*Jingya Wang,Yuyang Gao,Liuzhenghao Lv,Yonghong Tian,Yuyang Liu*

Main category: cs.RO

TL;DR: 提出LabEvolver，一个无需训练的框架，为湿实验室机器人智能体配备基于执行经验的记忆系统，通过内层试错循环和外层经验演化循环提升任务完成效率与安全性。

Research relevance: 弱相关（1分）。论文聚焦于wet-lab机器人智能体（实验室自动化）和ALFWorld具身智能任务，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及object detection、小目标检测、旋转框检测等遥感目标检测任务，更不涉及pruning、quantization、knowledge distillation、NAS等轻量化方法。与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。论文主题为wet-lab机器人智能体与具身智能，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：wet-lab机器人任务（溶液配制、pH调节）及ALFWorld具身智能任务。应用场景：实验室自动化、闭环科学发现。传感器类型：摘要未说明（推测为机器人视觉/传感器，但未明确）。目标类别：非遥感目标，涉及实验室操作对象。

Model architecture: LabEvolver包含两个核心循环：(1) 内层试错循环（inner trial loop），用于自适应感知、在线规划和安全验证；(2) 外层演化循环（outer evolution loop），将完成的轨迹蒸馏为可复用的技能、策略和安全经验。整体采用episodic memory（情景记忆）机制，属于training-free框架。

Lightweight method: 摘要未说明。论文未涉及剪枝、量化、知识蒸馏（此处"distill"指轨迹蒸馏为经验，非模型压缩中的knowledge distillation）、NAS、轻量网络等模型压缩或加速方法。虽然框架是training-free，但这是指无需训练智能体策略，而非模型轻量化。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。该工作面向实验室机器人而非星上/边缘部署场景。

Datasets and metrics: 数据集/环境：ALFWorld（具身智能基准环境）以及真实wet-lab机器人溶液配制任务。评价指标：pH调节完成时间（降低48.2%）、安全门拦截次数（降低60.0%）、20步内累计成功率（从ReAct的76.2%提升至91.4%，在500个连续任务上）。

Experiments: 主要实验：(1) 真实wet-lab溶液配制任务，pH调节完成时间降低48.2%，安全门拦截降低60.0%；(2) ALFWorld上500个连续任务，20步内累计成功率从ReAct的76.2%提升到91.4%。对比基线为ReAct。摘要未说明消融实验细节。

Limitations: 与本研究方向基本无关。摘要未说明模型复杂度、推理效率、部署约束等关键信息。该工作面向具身智能/实验室自动化，其"经验演化"思想虽有一定通用性，但与遥感目标检测和模型轻量化无直接关联。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是"经验演化/记忆复用"的思想——在在轨目标检测中，可类比为利用历史轨道观测数据积累的检测经验来提升新场景的检测性能（如增量学习、持续学习），但这与本论文的机器人经验记忆机制关联较弱。对于轻量化本身（pruning/quantization/distillation/NAS）无直接可借鉴内容。

Abstract: We introduce LabEvolver, a training-free framework that equips safe and grounded wet-lab agents with episodic memory from execution experience. LabEvolver couples a state-grounded inner trial loop for adaptive perception, online planning, and safety validation with an outer evolution loop that distills completed trajectories into reusable skill, strategy, and safety experience. On robotic solution-preparation tasks, LabEvolver demonstrates real-world feasibility, reducing pH-regulation completion time and safety-gate intercepts by 48.2% and 60.0%, respectively. On ALFWorld, it further improves cumulative success rate within 20 steps from 76.2% with ReAct to 91.4% over 500 continual tasks, showing generality beyond wet-lab settings. These results support learn-by-doing experience evolution as a feasible path toward closed-loop automated scientific discovery. The project page is available at https://github.com/AndyGao6186/LabEvolver.

</details>


### [260] [Write-Safe Flow Field Mapping under Ambiguous Onboard Sensing and Localization Drift](https://arxiv.org/abs/2607.27713)
*Linhao Jin,Qimin Feng,Peter Gunnarson,Qiang Zhong*

Main category: cs.RO

TL;DR: 提出一种地图参考感知的保守融合框架，通过学习写入安全分数来抑制移动机器人在流场建图中因定位漂移和观测歧义导致的鬼影结构污染。

Research relevance: 弱相关（2分）。该论文属于移动机器人流场建图（flow mapping）领域，涉及机载感知、地图融合、定位漂移处理，与遥感卫星在轨目标检测及模型轻量化方向基本无关。既非遥感/卫星场景，也非目标检测任务，更不涉及模型压缩或部署优化。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于移动机器人流场建图领域，仅在"安全融合/置信度门控"概念上有极间接的借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：移动机器人流场建图（flow mapping）与地图融合；应用场景：合成射流（synthetic jet）与横流（crossflow）环境、推进器尾流（thruster wake）实测；传感器类型：压力传感器与光流（optical-flow）测量；目标类别：不涉及目标检测，处理对象为局部速度场（velocity patch）与全局地图。

Model architecture: 摘要未说明具体网络结构。模型预测局部速度patch和一个可学习的写入安全分数（write-safety score），该分数持续衰减不确定的地图更新，同时在无可靠地图参考时允许初始化。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法，也未讨论实时推理、边缘部署或星上部署。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。虽然提及"机载感知"（onboard sensing）和"零样本硬件回放"（zero-shot hardware replay），但未涉及模型轻量化或资源受限部署优化。

Datasets and metrics: 数据集：合成射流与横流仿真环境、推进器尾流实测数据（压力与光流测量）。评价指标：鬼影污染（ghost contamination）降低比例、地图覆盖率（map coverage）。实验设置：对比无门控融合（ungated fusion）基线，采用零样本硬件回放验证。

Experiments: 主要结果：在合成射流与横流环境中，相比无门控融合，平均鬼影污染降低42%；在推进器尾流实测数据的零样本硬件回放中，鬼影污染进一步降低39%，同时保留81%地图覆盖率。

Limitations: 摘要未说明模型复杂度、推理速度等部署指标；未涉及遥感图像或卫星平台；未讨论目标检测任务；未涉及任何模型压缩技术。该工作聚焦于流场建图的安全融合策略，与遥感在轨目标检测轻量化方向关联度很低。

Ideas for my research: 借鉴意义有限，但有一个概念可迁移：其"写入安全分数"（write-safety score）思想类似于在轨目标检测中应对定位/配准误差的置信度门控策略——即在遥感目标检测中，可考虑对预测框的置信度结合地理配准可靠性进行联合衰减，避免因卫星姿态/轨道误差导致的误检累积。此外，其"无可靠参考时允许初始化"的思路可类比于在轨模型冷启动时的自适应阈值策略。但这些仅是概念层面的间接启发，不涉及轻量化核心技术。

Abstract: Mobile robots can infer local flow structure from onboard sensing, but a locally plausible estimate is not always safe to write into a global map. Similar flow structures may produce ambiguous observations, while localization drift causes predicted patches to be written at incorrect locations. Repeated misregistered updates then accumulate into persistent ghost structures. We address this failure mode with a map-reference-aware conservative fusion framework. The model predicts a local velocity patch and a learned write-safety score that continuously attenuates uncertain map updates while permitting initialization when no reliable map reference is available. Across synthetic jet and crossflow environments, the proposed method reduces average ghost contamination by 42% relative to ungated fusion. A zero-shot hardware replay using real pressure and optical-flow measurements from a thruster wake further reduces ghost contamination by 39% while retaining 81% map coverage. These results show that safe map writing is critical for flow mapping under ambiguous sensing and localization drift.

</details>


### [261] [RedFlow: Redirect Failure into Action-Level Corrections for Flow-matching VLA Policy](https://arxiv.org/abs/2607.27782)
*Zhengyang Yan,Junhao Li,Fangqi Zhu,Zijun Wang,Quanxin Shou,Yikun Miao,Xiaoyi Pang,Zicong Hong,Song Guo*

Main category: cs.RO

TL;DR: 提出RedFlow，一个面向flow-matching VLA策略的细粒度离线强化学习框架，通过上下文感知的纠正匹配机制将失败经验转化为动作级别的纠正监督，提升机器人操作策略的鲁棒性。

Research relevance: 弱相关（2分）。该论文属于机器人操作（robotic manipulation）领域的视觉-语言-动作（VLA）策略与离线强化学习研究，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。虽然涉及视觉（vision）和动作（action）建模，但场景是机器人操作而非遥感影像理解，且不涉及任何模型压缩或部署优化技术。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于机器人操作与离线强化学习领域。除非对"利用失败样本改进检测模型"的训练策略感兴趣，否则不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人操作策略学习（robotic manipulation policy learning），具体为flow-matching VLA策略的离线强化学习改进。应用场景：LIBERO基准测试和三个真实世界机器人操作任务。传感器类型：视觉（相机）+ 语言指令 + 机器人动作。目标类别：机器人操作动作序列，非遥感目标检测。

Model architecture: 基于flow-matching的Vision-Language-Action (VLA)策略模型。核心组件包括：(1) Context-Aware Corrective Matching机制，用于识别导致失败的动作并从相似上下文中检索成功替代动作作为纠正目标；(2) Adaptive Redirection Objective，联合强化成功动作、抑制不良动作、并将可恢复失败重定向到纠正目标。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。该论文关注的是策略学习质量而非部署效率。

Datasets and metrics: 数据集：LIBERO基准测试（机器人操作标准benchmark）以及三个真实世界机器人操作任务。评价指标：真实世界任务成功率（success rate），从56.7%提升至74.7%；同时与PPO、GRPO、DDPO等on-policy方法对比训练样本效率。

Experiments: 在LIBERO基准和三个真实世界操作任务上，RedFlow一致优于SOTA离线RL基线，将真实世界成功率从56.7%提升到74.7%。在性能上匹配强on-policy方法（PPO、GRPO、DDPO），但所需训练样本约少一个数量级。

Limitations: 摘要未说明具体局限性。但可推断：(1) 仅针对机器人操作场景验证，未涉及遥感或视觉检测任务；(2) 未提供模型规模、推理效率等部署指标；(3) 未讨论在计算资源受限的边缘/星上环境中的可行性；(4) 依赖flow-matching VLA框架，迁移到遥感目标检测模型需要大量适配。

Ideas for my research: 尽管场景不同，但RedFlow的"将失败样本转化为细粒度纠正监督"的思想对遥感目标检测有一定启发：(1) 在遥感小目标检测中，可将漏检/误检样本视为"失败经验"，通过上下文感知的纠正匹配机制为检测器提供更密集的监督信号；(2) 离线RL中"从混合质量数据中学习"的思路可用于利用大量未标注或弱标注的遥感影像数据；(3) 但需注意，该方法的计算开销和训练复杂度较高，与轻量化目标可能冲突，借鉴时应聚焦于训练策略而非模型结构。

Abstract: Flow-matching Vision-Language-Action (VLA) policies have shown strong potential for robotic manipulation but often suffer from compounding errors caused by distribution shifts during deployment. While offline reinforcement learning (RL) provides a practical way to improve deployed policies using rollout data, existing methods either ignore failure data or exploit it only at the trajectory level, resulting in low learning efficiency and persistent errors. We propose **RedFlow**, a fine-grained offline RL framework that redirects failure experiences into action-level corrective supervision for flow-matching VLA policies. RedFlow consists of two key components: (1) a **Context-Aware Corrective Matching** mechanism that identifies failure-inducing actions and retrieves successful alternatives from similar contexts as corrective targets, and (2) an **Adaptive Redirection Objective** that jointly reinforces successful actions, suppresses undesirable ones, and redirects recoverable failures toward corrective targets. By converting both successful and failed experiences into dense supervision, RedFlow enables robust recovery learning from mixed-quality data. Experiments on the LIBERO benchmark and three real-world manipulation tasks show that RedFlow consistently outperforms state-of-the-art offline RL baselines, improving the real-world success rate from 56.7% to 74.7%. It also matches strong on-policy methods (PPO, GRPO, and DDPO) while requiring roughly an order of magnitude fewer training samples.

</details>


### [262] [DexDirect: Direct Kinesthetic Arm Guidance for Efficient Dexterous Demonstration Collection](https://arxiv.org/abs/2607.27784)
*Beom Jun Kim,Shiu-Jen Wang,Jonathan Liu,Alvin Zhu,Quanyou Wang,Hanzhang Fang,Feng Xu,Mingzhang Zhu,Yuchen Cui,Dennis W. Hong*

Main category: cs.RO

TL;DR: DexDirect 提出一种直接动觉式机械臂引导与视觉手部重定向相结合的灵巧操作演示采集接口，用于高效收集机器人操作演示数据。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于机器人学习中的灵巧操作演示采集（dexterous manipulation demonstration collection），属于机器人操作/人机交互领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能，也不涉及目标检测或模型轻量化。相关性评分：1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于机器人操作/人机交互领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：灵巧操作演示采集（dexterous demonstration collection）与机器人学习（robot learning）。应用场景：机器人操作、人机交互、遥操作。传感器类型：6-DoF 重力补偿机械臂、单目网络摄像头（webcam）。目标类别：不涉及目标检测，涉及灵巧手（16 joints, 13-DoF dexterous robot hand）的操作重定向。

Model architecture: 摘要未说明具体模型架构。涉及 diffusion policy 作为下游策略模型，以及视觉手部重定向（vision-based hand retargeting）模块，但未给出网络结构细节。

Lightweight method: 不涉及。论文未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT 等任何模型压缩或加速方法。

Onboard deployability: 不适用。论文未涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗等部署相关信息。摘要仅提及"low-setup"和"low cost"的采集接口设计理念，但这是针对演示采集硬件而非模型部署。

Datasets and metrics: 数据集：摘要未说明具体公开数据集，采用用户研究（user studies）方式采集演示数据。评价指标：成功演示数量（successful demonstrations）、NASA-TLX 任务负荷评估（mental demand, effort, frustration, physical demand）、下游任务成功率（90% success rate on cube pick-and-place task）。对比基线：AnyTeleop（纯视觉）、TeleDex（姿态追踪）。

Experiments: 主要实验结果：DexDirect 相比 AnyTeleop 和 TeleDex 分别收集到 17.2x 和 3.2x 更多的成功演示；NASA-TLX 显示 mental demand、effort、frustration 显著降低，但 physical demand 上升；基于 DexDirect 演示训练的 diffusion policy 在 cube pick-and-place 任务上达到 90% 成功率。

Limitations: 与本研究方向完全无关，无法提供遥感目标检测或轻量化方面的局限性分析。摘要本身也未说明该方法的局限性，如物理疲劳、可扩展性、泛化能力等。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题无可借鉴点。该论文属于机器人操作演示采集领域，与遥感卫星在轨目标检测、模型压缩、边缘部署等研究方向无直接关联。唯一的间接启发可能是其"low-setup, low cost"的设计哲学，但这一理念在遥感轻量化领域已有更直接的相关文献支撑。

Abstract: Scalable collection of dexterous manipulation demonstrations remains a major bottleneck for robot learning. High-fidelity interfaces often require costly hardware and extensive setup, while low-setup, low cost alternatives tend to provide less precise control and impose greater cognitive workload on operators. We present DexDirect, a direct kinesthetic arm guidance for efficient dexterous demonstration collection. The operator drags a 6-DoF gravity-compensated robot arm directly by a handle, while a single webcam retargets operator's other hand onto a 16 joints 13-DoF dexterous robot hand. User studies suggest DexDirect collects 17.2x and 3.2x more successful demonstrations compared to purely vision (AnyTeleop) and pose-tracking (TeleDex) baselines. An adapted NASA-TLX shows DexDirect greatly reduces mental demand, effort, and frustration, despite raising physical demand. A diffusion policy trained on DexDirect demonstrations reaches a 90% success rate on a cube pick-and-place task. These results suggest that direct kinesthetic arm guidance combined with vision-based hand retargeting provides an efficient low-setup and scalable interface for collecting dexterous manipulation demonstrations

</details>


### [263] [RoboBRIDGE: A Modular Framework for Bridging Policies to Robust Real-World Robotic Agents](https://arxiv.org/abs/2607.27881)
*Sihyung Yoon,Minjong Yoo,Sanghyun Ahn,Seojeong Choi,Honguk Woo*

Main category: cs.RO

TL;DR: 提出RoboBRIDGE模块化框架，通过Monitor、Perceptor、Planner、Controller、Robot Interface五个协调模块将预训练VLA模型转化为鲁棒的机器人智能体，解决失败恢复、长时程执行不一致和域偏移鲁棒性问题。

Research relevance: 弱相关（2分）。该论文属于机器人操作（robotic manipulation）领域的Vision-Language-Action (VLA)模型部署框架，与遥感、卫星、Earth observation、aerial image、SAR、高光谱等遥感场景完全无关，也不涉及目标检测任务。虽然涉及模型部署和LoRA适配器（轻量化微调手段），但应用场景是机器人控制而非遥感在轨目标检测，属于泛机器人/泛机器学习方法，仅LoRA微调思想可能有一定借鉴意义。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于机器人操作领域。虽然LoRA微调和模块化编排思想有一定借鉴价值，但整体相关性极低，不建议优先精读。若对LoRA在遥感检测中的参数高效微调感兴趣，可仅阅读Controller模块相关部分。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人操作（robotic manipulation）中的动作预测与智能体部署。应用场景：LIBERO、RoboCasa仿真环境及真实机器人平台。传感器类型：视觉观测（机器人相机）。目标类别：机器人操作任务中的物体操作，非遥感目标。摘要未说明遥感相关传感器或目标类别。

Model architecture: RoboBRIDGE模块化框架，包含五个协调模块：Monitor（失败检测与分层恢复）、Perceptor（异步场景理解更新）、Planner（重规划触发）、Controller（原始技能微调，将操作分解为域不变primitive并配备专用LoRA适配器）、Robot Interface（机器人接口）。底层使用预训练VLA模型作为动作预测器。

Lightweight method: 采用LoRA（Low-Rank Adaptation）适配器对Controller中的primitive skill进行参数高效微调，将操作分解为域不变primitive并配备专用LoRA适配器，以降低VLA对域偏移的敏感性。这属于参数高效微调（PEFT）范畴，而非传统意义上的剪枝、量化、知识蒸馏或NAS等模型压缩方法。摘要未说明参数量、FLOPs、延迟等轻量化指标。

Onboard deployability: 摘要未说明。论文聚焦于机器人智能体的鲁棒性编排（orchestration），而非计算资源受限的边缘/星上部署。未提及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。LoRA适配器的使用暗示了参数高效微调以降低存储/计算开销，但未给出具体量化数据。

Datasets and metrics: 数据集/基准：LIBERO、RoboCasa仿真基准，以及真实机器人平台上的case studies。涉及多个机器人平台和VLA backbone。评价指标：摘要未明确列出具体指标名称，仅提及"consistently outperforms standalone policies and prior augmented VLA deployments"。

Experiments: 在LIBERO、RoboCasa和真实世界case studies上，跨多个机器人平台和VLA backbone进行实验，RoboBRIDGE一致优于standalone policies和prior augmented VLA deployments。摘要未给出具体数值结果、消融实验细节或对比表格。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于机器人操作领域。2. 摘要未提供任何量化性能数据（成功率、任务完成率等具体数值）。3. 未涉及模型压缩、推理加速、边缘部署等轻量化核心问题。4. 未说明LoRA适配器带来的实际参数/计算节省量。5. 框架复杂度较高（五个模块），在资源受限的星上环境中部署可行性存疑。

Ideas for my research: 1. LoRA参数高效微调思想可借鉴：在遥感目标检测模型轻量化中，可考虑用LoRA对预训练检测器进行域自适应微调（如从自然图像迁移到遥感场景），减少微调参数量。2. Monitor-失败检测与恢复机制：在轨目标检测中可借鉴"检测质量监控+分层恢复"思想，例如对检测置信度进行在线监控，低置信度时触发重推理或切换轻量/重量模型。3. Perceptor异步更新思想：在星上推理中可借鉴异步场景理解机制，避免推理阻塞，提高流水线效率。4. 模块化编排思想：将检测、跟踪、场景理解等任务模块化编排，便于在资源受限平台上灵活调度。

Abstract: Vision-Language-Action (VLA) models have attracted growing interest as a scalable approach to robotic manipulation. While these models are effective action predictors, deploying them as robotic agents exposes critical gaps: no mechanism for failure recovery, inconsistent execution over long horizons, and limited robustness to shifts in observations, tasks, or embodiments. Existing solutions address these limitations individually through model retraining or environment-specific modules, yet what is needed is a general framework that systematically transforms a pretrained VLA into a robotic agent. We present RoboBRIDGE, a modular framework that provides an orchestration layer over five coordinated modules, namely Monitor, Perceptor, Planner, Controller, and Robot Interface, to compose robust robotic agents from off-the-shelf components, including pretrained VLAs. The Monitor pairs rapid failure detection with hierarchical recovery to correct errors before they cascade. When the environment diverges from the current plan, the Planner triggers replanning while the Perceptor updates scene understanding asynchronously, avoiding execution stalls. Within the Controller, primitive skill fine-tuning factors manipulation into domain-invariant primitives with dedicated LoRA adapters, reducing sensitivity to domain shifts when a VLA is used. Across LIBERO, RoboCasa, and real-world case studies spanning multiple robot platforms and VLA backbones, RoboBRIDGE consistently outperforms both standalone policies and prior augmented VLA deployments. These results suggest that reliable robotic agency does not arise from scaling action predictors alone, but from structured orchestration around them.

</details>


### [264] [Static In, Dynamic Out: Counterfactual Action Augmentation for Moving Object Manipulation](https://arxiv.org/abs/2607.27890)
*Woo Chul Shin,Zhenyang Chen,Alfred Cueva,Nadun Ranawaka Arachchige,Yingyan Celine Lin,Benjamin Joffe,Shreyas Kousik,Danfei Xu*

Main category: cs.RO

TL;DR: 提出SIDO（Static In, Dynamic Out）反事实动作增强方法，使仅在静态物体演示上训练的visuomotor policy能够适应测试时物体的未知运动，通过将移动物体操作分解为"预测物体未来位置"和"到达该预测位姿"两个子问题来实现。

Research relevance: 弱相关（1-2分）。该论文属于机器人操作（robot manipulation）和visuomotor policy领域，涉及移动物体的抓取与操作，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。也不涉及目标检测、轻量化、剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署等任何与本研究方向相关的技术。唯一可能的间接关联是"预测物体未来位置"这一思想在概念上类似于遥感视频中的运动目标预测，但方法论和场景差异极大。

Reading priority: 1分。该论文属于机器人操作领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感图像处理或任何模型压缩/加速技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人操作（robot manipulation）中的移动物体抓取/操作。应用场景：传送带上的零件、风中摇摆的水果等动态物体操作。传感器类型：摘要未说明（推测为相机视觉输入）。目标类别：Mug（杯子）、Square（方块）、Stack（堆叠）、Gantry（龙门架）、Peachtree（桃树）等操作任务中的物体。

Model architecture: 摘要未详细说明网络结构。核心是SIDO反事实动作增强模块：将物体位移到反事实的未来位置，并变形演示的动作块（action chunk）以保持手-物相对位姿，从而得到goal-conditioned policy。部署时由一个物体位姿预测器（object pose predictor）提供未来位置。

Lightweight method: 不涉及。摘要未提及任何模型压缩、加速或轻量化方法（pruning, quantization, knowledge distillation, NAS等均未涉及）。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。该工作面向机器人实时操作场景，但未讨论计算资源约束。

Datasets and metrics: 三个仿真任务（Mug, Square, Stack）在五种物体运动模式（five object motion patterns）下评估；两个真实世界任务（Gantry, Peachtree）。评价指标：moving object success（移动物体操作成功率），同时保持static object performance（静态物体性能）。

Experiments: 在三个仿真任务和两个真实世界任务上，SIDO相比baseline方法在移动物体操作成功率上有所提升，同时保持静态物体性能不下降。摘要未给出具体数值和对比方法名称。

Limitations: 1. 与遥感卫星在轨目标检测方向基本无关，方法论不可直接迁移。2. 摘要未说明模型规模、计算开销、推理速度等关键部署指标。3. 仅针对机器人操作场景，未涉及遥感图像/视频中的目标检测。4. 物体位姿预测器的精度和鲁棒性对整体性能的影响未在摘要中讨论。

Ideas for my research: 1. "预测物体未来位置 + 到达该位置"的分解思想，在概念上可类比于遥感视频中"运动目标轨迹预测 + 检测"的两阶段思路，但需要重新设计以适应遥感场景。2. 反事实数据增强（counterfactual augmentation）的思路可用于遥感目标检测的数据增强，例如对目标进行几何变换、运动模拟等，但需结合遥感图像特性。3. 整体借鉴价值有限，建议仅作为方法论层面的灵感参考，不投入过多阅读时间。

Abstract: Visuomotor policies have advanced on manipulation tasks where the target object stays static during execution, but real deployments break this assumption: parts drift on conveyors and fruits sway in the wind. We introduce Static In, Dynamic Out (SIDO), a counterfactual action augmentation that enables a policy trained only on static object demonstrations to adapt to unseen object motion at test time. Our key idea is to factorize moving object manipulation into two sub-problems: predicting where the object will be, and reaching that predicted pose. SIDO displaces the object to a counterfactual future position and morphs the demonstrated action chunk to preserve the hand-object relative pose, yielding a goal-conditioned policy. At deployment an object pose predictor supplies the future position. Across three simulated tasks (Mug, Square, Stack) under five object motion patterns and two real-world tasks (Gantry, Peachtree), SIDO improves moving object success over the baselines while preserving static object performance. Project website: https://sido-staticindynamicout.github.io/.

</details>


### [265] [Learning Social Robot Navigation By Sensing Human Legs](https://arxiv.org/abs/2607.27922)
*Alberto Vaglio,Andrea Garulli,Antonio Giannitrapani,Renato Quartullo,Tommaso Van Der Meer*

Main category: cs.RO

TL;DR: 提出CALF（Convolutional Attention for Leg Features），一种结合卷积、注意力与MLP的端到端神经网络架构，直接从2D LiDAR扫描中解读行人腿部运动并生成安全导航指令，配套轻量级仿真器LegNav进行深度强化学习训练，并在TurtleBot 4上实现零样本部署。

Research relevance: 弱相关（2分）。该论文属于机器人导航与行人感知领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景；也不涉及object detection中的遥感目标检测（舰船/飞机/车辆等）。虽然涉及"lightweight simulator"和"deployment-ready"等轻量化/部署概念，但这是指仿真器轻量化和机器人端部署，与星上部署、模型压缩（pruning/quantization/distillation）无关。属于泛机器人/泛机器学习方法，对遥感在轨目标检测的借鉴价值有限。

Reading priority: 1分。该论文聚焦地面机器人行人导航，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然涉及端到端网络设计和仿真到真实部署的迁移，但缺乏遥感场景、目标检测任务和模型压缩方法等核心要素，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：行人感知与安全导航（pedestrian-aware navigation）；应用场景：地面移动机器人在行人中导航；传感器类型：2D LiDAR（近地面安装）；目标类别：行人腿部（legs），而非完整人体。不涉及遥感传感器或遥感目标类别。

Model architecture: CALF架构：端到端神经网络，由卷积层（convolutional layers）、注意力机制（attention）和MLP组成，直接从LiDAR扫描中提取腿部运动特征并输出导航指令。训练框架为深度强化学习（deep reinforcement learning），配套LegNav仿真器（基于JAX编写），结合2D LiDAR ray tracing与行人步态模型（pedestrian gait model）。

Lightweight method: 摘要中提到的"lightweight"主要指LegNav仿真器本身轻量化（可在单张消费级GPU上1小时内完成训练），而非模型压缩方法。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等模型轻量化技术。CALF模型本身是否轻量（参数量、FLOPs）摘要未说明。

Onboard deployability: 摘要提到CALF策略通过zero-shot部署在TurtleBot 4上完成真实世界验证，生成平滑且符合社交规范的轨迹。但未给出参数量、FLOPs、延迟、显存、能耗等具体部署指标。训练在单张消费级GPU上1小时内完成（得益于JAX编写的LegNav仿真器），但这是训练效率而非推理部署效率。与星上部署（FPGA/NPU/TensorRT）无关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标涉及导航性能（navigation performance）和社交合规性（social compliance），与经典方法和基于学习的方法进行对比。真实世界验证通过TurtleBot 4平台进行zero-shot部署。具体指标数值摘要未说明。

Experiments: 实验包括：1）在LegNav仿真器中训练CALF策略；2）与经典方法（classical baselines）和基于学习的方法（learning-based baselines）对比导航性能与社交合规性；3）在TurtleBot 4上进行真实世界zero-shot部署验证，获得平滑且社交合规的轨迹。消融实验和具体对比数值摘要未说明。

Limitations: 1）与遥感/卫星在轨目标检测完全无关，属于地面机器人导航领域；2）轻量化仅体现在仿真器训练效率上，未涉及模型压缩或推理加速；3）未提供模型参数量、FLOPs、推理延迟等部署关键指标；4）传感器为2D LiDAR而非遥感成像传感器；5）目标为行人腿部而非遥感目标（舰船/飞机/车辆等）。

Ideas for my research: 尽管整体弱相关，仍有少量可借鉴思路：1）"端到端架构结合卷积+注意力+MLP"的设计模式可迁移到轻量遥感检测网络的特征提取模块设计；2）"轻量仿真器加速训练"的思路可类比为在轨检测模型训练时构建高效的遥感数据仿真/增强管线；3）"zero-shot从仿真到真实部署"的迁移思路对遥感模型从地面数据到在轨部署的域适应有参考价值；4）JAX框架的高效训练实现可作为轻量化研究中的工程参考。但整体而言，该论文对遥感在轨目标检测轻量化的直接借鉴价值有限。

Abstract: Robots navigating among pedestrians typically sense their surroundings with a 2D LiDAR mounted close to the ground. At that height, the sensor mostly sees moving legs rather than whole people, yet most learning-based navigation methods still treat pedestrians as simple shapes like circles. This paper addresses that gap with CALF (Convolutional Attention for Leg Features), an end-to-end neural architecture that combines convolutional layers, attention, and MLP to interpret leg motion directly from LiDAR scans and produce safe navigation commands. The CALF policy is trained using deep reinforcement learning algorithms within LegNav, a custom lightweight 2D simulator that combines 2D LiDAR ray tracing with a novel pedestrian gait model. The resulting policy is compared against classical and learning-based baselines in terms of navigation performance and social compliance. The approach is validated through real-world experiments via zero-shot deployment on a TurtleBot 4, yielding smooth and socially compliant trajectories. Written in JAX, the LegNav simulator enables the training of a deployment-ready CALF policy in under an hour on a single consumer GPU.

</details>


### [266] [RaDiVe: Robust 4D Radar Odometry with Distance-Bounded NDT and Velocity-Discrepancy Point Uncertainty](https://arxiv.org/abs/2607.28045)
*Sangwoo Jung,Dongjae Lee,Chiyun Noh,Ayoung Kim*

Main category: cs.RO

TL;DR: 提出RaDiVe，一个基于4D雷达的里程计框架，通过距离受限NDT、速度差异点不确定性模型和SDF表面点提取，提升雷达点云配准的精度与鲁棒性，在多个公开数据集上平均降低44.4%平移ATE和21.3%旋转ATE，同时保持实时性能。

Research relevance: 弱相关（2分）。论文属于机器人/自动驾驶领域的4D radar odometry（雷达里程计），涉及点云配准与实时性能，但既不是遥感/卫星场景，也不涉及目标检测，更不涉及模型轻量化。仅"实时性能"和"计算效率"与轻量化方向有间接借鉴意义，但整体与遥感卫星在轨目标检测及轻量化方向基本无关。

Reading priority: 1分。论文属于4D雷达里程计领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或模型压缩技术，仅"实时性能"和"计算效率优化"有极弱的间接借鉴意义，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：4D radar odometry（4D雷达里程计/点云配准）。应用场景：机器人/自动驾驶在恶劣天气下的感知与定位。传感器类型：4D radar（含Doppler径向速度信息的雷达点云）。目标类别：不涉及目标检测，无目标类别。

Model architecture: RaDiVe框架包含三个核心模块：(1) distance-bounded NDT（距离受限Normal Distributions Transform），通过限制对应搜索到近距离体素对来提升优化稳定性和计算效率；(2) velocity-discrepancy point uncertainty model（速度差异点不确定性模型），根据测量Doppler径向速度与估计自车速度预测径向速度之间的差异对每个点加权；(3) SDF-based surface point extraction via implicit neural mapping（基于隐式神经映射的SDF表面点提取），构建几何一致且去噪的局部子地图。

Lightweight method: 摘要未明确涉及模型轻量化方法（如pruning、quantization、knowledge distillation、NAS等）。仅提到distance-bounded NDT通过限制搜索范围提升了"computational efficiency"（计算效率），以及整体框架保持"real-time performance"（实时性能），但这些属于算法层面的效率优化，而非模型压缩或加速技术。

Onboard deployability: 摘要提到"maintaining real-time performance"（保持实时性能），但未给出具体参数量、FLOPs、延迟、显存、能耗等部署指标。未涉及星上/边缘部署（FPGA/NPU/TensorRT）相关内容。摘要未说明具体硬件平台和实时性量化指标。

Datasets and metrics: 使用多个公开4D雷达数据集（摘要未列出具体数据集名称）。评价指标：translational Absolute Trajectory Error (ATE，平移绝对轨迹误差) 和 rotational ATE（旋转绝对轨迹误差）。对比对象：现有4D radar odometry基线方法。

Experiments: 在多个公开数据集上，RaDiVe相比现有4D radar odometry基线，平均降低44.4%的平移ATE和21.3%的旋转ATE，同时保持实时性能。摘要未提供消融实验细节和具体数值表格。

Limitations: 摘要未说明：(1) 具体数据集名称和规模；(2) 实时性能的量化指标（帧率、延迟）；(3) 计算资源需求（GPU型号、参数量、FLOPs）；(4) 对恶劣天气（雨雪雾）的定量鲁棒性分析；(5) 与遥感/卫星场景完全无关，无法直接迁移。此外，implicit neural mapping（隐式神经映射）通常计算开销较大，其实际部署可行性存疑。

Ideas for my research: 尽管论文与遥感卫星在轨目标检测无直接关系，但有以下可借鉴点：(1) distance-bounded NDT中"限制搜索范围以提升计算效率"的思想，可类比到遥感目标检测中限制候选区域搜索或特征匹配范围以降低计算量；(2) velocity-discrepancy uncertainty model中对每个点根据置信度加权，可借鉴到遥感检测中对不同质量特征/样本进行不确定性加权；(3) 实时性能与精度权衡的评估思路（同时报告精度和实时性）值得在轻量化检测模型评估中借鉴。但整体借鉴价值有限。

Abstract: Recent advances in 4D radar enable robust perception in adverse weather; however, the inherent sparsity, noise, and limited positional precision of radar point clouds pose significant challenges for registration-based odometry. In this letter, we propose RaDiVe, a 4D radar odometry framework designed to improve the accuracy and robustness of radar point-cloud registration. We introduce a distance-bounded Normal Distributions Transform (NDT), which improves optimization stability and computational efficiency by restricting the correspondence search to near-distance voxel pairs. To mitigate measurement ambiguity, we propose a velocity-discrepancy point uncertainty model that weights each input 4D radar point according to the discrepancy between its measured Doppler radial velocity and the radial velocity predicted from the estimated ego-velocity. Furthermore, we incorporate Signed Distance Function (SDF)-based surface point extraction via implicit neural mapping to construct a geometrically consistent and noise-filtered local submap. Evaluations across multiple public datasets demonstrate that RaDiVe outperforms existing 4D radar odometry baselines by 44.4% in translational Absolute Trajectory Error (ATE) and 21.3% in rotational ATE on average, while maintaining real-time performance. The source code will be made publicly available to the robotics community: https://github.com/to-be-open-sourced.

</details>


### [267] [UniCross: Unified Cross-Skill Dexterous Manipulation Synthesis](https://arxiv.org/abs/2607.28198)
*Hui Zhang,Julian Ferchow,Jie Song,Mirko Meboldt*

Main category: cs.RO

TL;DR: 提出一个统一的灵巧手操作框架，将抓取、重定位、手内旋转和手内平移四种技能建模为共享状态/动作空间和共同目标结构的单一公式，并通过知识蒸馏得到跨技能策略。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于机器人灵巧手操作（dexterous manipulation），属于机器人学与强化学习领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感相关场景，也不涉及目标检测任务，更不涉及模型轻量化或边缘/星上部署。唯一相关的技术点是知识蒸馏（knowledge distillation），但蒸馏在这里是用于跨技能策略的整合，而非模型压缩。

Reading priority: 1分。论文属于机器人灵巧操作领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：灵巧手操作（dexterous manipulation），包括grasping（抓取）、relocation（重定位）、in-hand rotation（手内旋转）、in-hand translation（手内平移）四种技能。应用场景：机器人操作与手-物交互（hand-object interaction）。传感器类型：摘要未说明。目标类别：操作对象（物体），非遥感目标。

Model architecture: 摘要未说明具体网络结构。提出一个统一框架（unified framework），将四种操作技能建模为单一公式，共享相同的state space和action space，以及共同的目标结构（common objective structure）。通过该统一公式蒸馏（distillation）出单个跨技能策略（cross-skill policy）。框架支持跨不同手部形态（hand morphologies）迁移。

Lightweight method: 摘要中提及知识蒸馏（knowledge distillation），但其用途是将多种技能整合为单一跨技能策略，而非用于模型压缩或加速。不涉及剪枝（pruning）、量化（quantization）、NAS、轻量网络设计、实时推理或边缘/星上部署等轻量化方法。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作面向机器人操作，与星上/边缘部署无关。

Datasets and metrics: 摘要未说明具体数据集和评价指标。仅提及实验验证了策略在每种技能上的表现、对未见物体的泛化能力（generalizes to unseen objects）、对扰动的鲁棒性（robust to disturbances）以及长时程技能链式组合（chaining skills into long-horizon manipulation）的能力。

Experiments: 摘要未说明具体实验设置和对比方法。主要结论包括：统一公式可蒸馏出单一跨技能策略，在每种技能上表现强，能泛化到未见物体，对扰动鲁棒，可无缝链式组合为长时程操作，且能跨不同手部形态迁移。

Limitations: 摘要未说明局限性。从研究角度看，该工作完全面向机器人灵巧操作，与遥感目标检测和轻量化无直接关联。摘要未提供任何关于模型规模、计算开销、推理效率的信息，也未涉及视觉感知或目标检测模块。

Ideas for my research: 借鉴意义有限。唯一可能相关的点是"统一公式+知识蒸馏"的思想——即用统一的任务表述整合多个子任务，再通过蒸馏得到单一紧凑策略。这一思路在概念上可类比于将遥感多类目标检测（如舰船、飞机、车辆）统一建模后蒸馏为轻量模型，但论文本身并未涉及视觉检测或模型压缩，迁移价值较低。

Abstract: Many dexterous manipulation tasks require the object to remain securely held throughout the interaction. From the perspective of hand-object relational motion, such manipulation comprises four canonical skills: grasping, relocation, in-hand rotation, and in-hand translation. Human hands flexibly compose these skills to accomplish complex tasks. Existing approaches, however, model these skills separately with skill-specific action constraints, objectives, or even dedicated hand morphologies, which breaks the compatibility and continuity required for long-horizon composition. In this work, we present a unified framework that models all four skills in a single formulation that shares the same state and action spaces and a common objective structure. This formulation enables straightforward distillation of a single cross-skill policy that performs strongly on every skill, generalizes to unseen objects, stays robust to disturbances, and chains skills seamlessly into long-horizon manipulation. The framework also transfers effectively across different hand morphologies. Overall, our results suggest that different dexterous manipulation skills can be viewed as instantiations of a shared task formulation, revealing the intrinsic consistency across different behaviors.

</details>


### [268] [When Robots Exchange Meaning: A Demo of Goal-Oriented Semantic Communications for Collaborative Robotics](https://arxiv.org/abs/2607.28256)
*Peizheng Li,Xinyi Lin,Sajida Gufran,Adnan Aijaz*

Main category: cs.RO

TL;DR: 本文介绍了一个机器人-边缘语义通信（SemCom）测试平台，将机器人端视觉压缩（VQ-VAE + ONNX Runtime）与边缘端语义建图（RTAB-Map）结合，用于6G任务导向通信研究。

Research relevance: 弱相关（2分）。论文涉及视觉压缩（VQ-VAE tokenization）和边缘端推理（Jetson Orin），与"轻量化/边缘部署"有一定交集，但场景是移动机器人协同（collaborative robotics）和6G语义通信，并非遥感/卫星场景，也不涉及目标检测任务。核心关注点是语义通信传输效率而非检测模型轻量化。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，主要价值在于VQ-VAE压缩和ONNX Runtime边缘部署的工程参考，但场景、任务和核心方法均不匹配，可作为边缘部署工程实践的补充阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人-边缘语义通信（SemCom）测试平台；应用场景：6G任务导向通信、移动机器人协同、embodied AI；传感器：RGB-D相机、LiDAR；目标类别：语义对象（semantic object handling），非特定遥感目标。不涉及遥感、卫星、aerial image、SAR、高光谱等。

Model architecture: 系统架构：机器人端（ROS 2）搭载RGB-D + LiDAR，使用ONNX Runtime编码器将RGB帧编码为VQ-VAE tokens；边缘端（Jetson Orin）使用PyTorch解码器重建图像，并执行RTAB-Map建图、语义对象处理和浏览器可视化。320×240图像表示为80×60 token grid，payload 5400 bytes。

Lightweight method: 采用VQ-VAE（Vector Quantized Variational Autoencoder）进行视觉压缩，将RGB图像编码为离散token，实现42.67×的数据缩减（相对于模型输入RGB字节）。使用ONNX Runtime在机器人端进行高效推理。这属于"端侧视觉压缩"而非传统模型轻量化（剪枝/量化/蒸馏/NAS），但ONNX Runtime部署和token化压缩对边缘部署有参考价值。

Onboard deployability: 边缘节点为Jetson Orin（NVIDIA嵌入式AI平台），具备边缘推理能力。摘要给出了压缩率（42.67× reduction）和payload大小（5400 bytes），但未说明模型参数量、FLOPs、延迟、显存占用、能耗等具体部署指标。摘要未说明实时性数据。

Datasets and metrics: 摘要未说明具体数据集和评价指标。这是一个demo论文，主要展示系统原型和端到端流程，未提供定量评估指标（如PSNR、SSIM、任务完成率等）。

Experiments: 摘要未说明对比实验或消融实验。仅作为proof of concept演示了从语义视觉传输到对象级地图交互的完整路径，未与其他压缩方法或通信方案进行定量对比。

Limitations: 1) 场景为移动机器人而非遥感卫星，传感器和任务目标差异大；2) 不涉及目标检测任务；3) 未提供模型轻量化（剪枝/量化/蒸馏）相关方法；4) 缺乏定量评估指标和对比实验；5) 压缩率仅针对RGB输入字节，未考虑整体系统开销；6) 摘要未说明VQ-VAE编码器的训练方式、模型大小和推理延迟。

Ideas for my research: 1) VQ-VAE token化压缩思路可借鉴：在遥感卫星上，可将高分辨率遥感图像编码为离散token后再传输或处理，降低星上到地面的通信带宽需求；2) ONNX Runtime作为跨平台推理引擎，可用于星上NPU/FPGA部署的模型转换与优化；3) "端侧压缩+边缘重建"的架构思想可迁移到"星上压缩+地面重建"的遥感场景，但需考虑遥感图像分辨率更高、目标更小、语义更复杂的特点；4) 语义通信（SemCom）理念可启发"任务感知的星上传输"，即只传输对目标检测有用的语义信息而非完整图像。

Abstract: Collaborative robotics is a representative task-oriented 6G use-case, where communication quality should be reflected in mission execution, environment understanding, and closed-loop operation rather than packet delivery alone. This demo paper presents a robot-edge semantic communication (SemCom) testbed integrating robot-side visual compression, edge-side semantic mapping, and dashboard-based mission interaction. A mobile robot equipped with RGB-D sensing and LiDAR runs ROS 2, while a Jetson Orin edge node performs reconstruction, RTAB-Map mapping, semantic object handling, and browserbased visualization. As an initial proof of concept, RGB frames are encoded on the robot into VQ-VAE tokens using an ONNX Runtime encoder and reconstructed on the edge using a PyTorch decoder. A 320 X 240 image is represented by an 80 X 60 token grid with a packed payload of 5400 bytes, corresponding to a 42.67X reduction relative to model-input RGB bytes. The reconstructed visual stream is further associated with depth, pose, and 3D mapping information to generate a semantic map for downstream robotic applications. The demo exposes the full path from semantic visual transport to object-level map interaction, and provides a practical platform for future task-aware 6G networking studies at the intersection of SemCom, embodied AI, and physical AI-enabled robotics. A video of the demo is available at https://tinyurl.com/Tos09

</details>


### [269] [SemAnCorr: Semantic Anchored Correspondence for Zero-Shot Manipulation Skill Transfer](https://arxiv.org/abs/2607.28382)
*Xiaoxiang Dong,William Baron,Hongyi Chen,Uksang Yoo,Jeffrey Ichnowski,Weiming Zhi*

Main category: cs.RO

TL;DR: 提出SemAnCorr，一个无需训练的密集对应关系建立框架，通过语义一致锚点区域选择与functional maps传播约束，实现跨几何差异但功能相似物体的操作技能迁移。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文属于机器人学习（robot learning）领域，聚焦于操作技能迁移（manipulation skill transfer）和物体间密集对应关系（dense correspondence）建立，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能等任何遥感相关场景，也不涉及目标检测或模型轻量化。相关性评分：1分。

Reading priority: 1分。该论文属于机器人操作技能迁移领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：机器人操作技能迁移（manipulation skill transfer）与密集对应关系建立（dense correspondence）。应用场景：机器人操作（robot manipulation），非遥感场景。传感器类型：视觉描述符（dense visual descriptors）与3D特征场（3D feature fields），非遥感传感器。目标类别：功能相似但几何不同的物体实例（object instances sharing functionality but differing in geometry），非遥感目标。

Model architecture: SemAnCorr框架包含：1) 联合位姿-对应优化（joint pose-correspondence optimization）选择语义一致锚点区域（semantically consistent anchor regions）；2) 使用functional maps在物体表面传播约束；3) 训练-free（training-free）设计，无需额外训练。整体为基于对应关系匹配的几何-语义联合优化框架。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络等模型压缩或加速方法。其"training-free"特性仅指无需训练即可建立对应关系，并非模型轻量化手段。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其应用场景为机器人操作而非星上/边缘部署。

Datasets and metrics: 数据集：PartNet-Mobility上的密集对应基准（dense correspondence benchmark）。评价指标：语义准确率（semantic accuracy），达到90.8%；几何一致性（geometric coherence）优于近期SOTA基线。此外在真实世界（real-world）机器人操作实验中，使用单次演示（single demonstration）评估零样本操作技能迁移（zero-shot manipulation skill transfer）的可靠性。

Experiments: 主要实验：1) 在PartNet-Mobility密集对应基准上达到90.8%语义准确率，几何一致性优于近期SOTA基线；2) 真实世界机器人实验中，使用单次演示即可实现比现有对应方法更可靠的零样本操作技能迁移到未见物体。摘要未提供消融实验细节。

Limitations: 1) 与遥感卫星在轨目标检测及轻量化方向完全无关，属于机器人学习领域；2) 摘要未说明方法在复杂场景、遮挡、光照变化等条件下的鲁棒性；3) 摘要未说明计算开销与运行效率；4) 摘要未说明方法对大规模物体类别或动态场景的扩展性。

Ideas for my research: 虽然该论文与遥感在轨目标检测轻量化方向基本无关，但可借鉴的间接思路有限：1) "training-free"设计理念——在遥感在轨场景中，星上资源受限，若目标检测模型能通过无需训练或轻量微调的方式适应新任务，可减少在轨更新成本；2) 语义一致性约束（semantic consistency）思想——在遥感目标检测中，可通过语义一致性约束提升小目标或旋转目标的检测稳定性。但整体借鉴价值很低。

Abstract: Transferring manipulation skills across object instances that share functionality but differ in geometry remains a fundamental challenge in robot learning. While recent correspondence methods leverage dense visual descriptors and 3D feature fields, nearest-neighbor feature matching often produces spatially incoherent correspondences that fail to recover the local geometric frames required for reliable skill transfer. We introduce SemAnCorr, a training-free framework that establishes dense correspondence by selecting semantically consistent anchor regions through joint pose-correspondence optimization and propagating these constraints over the object surface using functional maps. The resulting correspondences preserve both semantic consistency and geometric coherence, enabling object-centric manipulation skills to transfer across geometrically diverse instances. We evaluate SemAnCorr on a dense correspondence benchmark built on PartNet-Mobility, achieving 90.8% semantic accuracy in our benchmark evaluation while improving geometric coherence over recent state-of-the-art baselines. Finally, we show that these improvements translate directly into real-world manipulation performance: using a single demonstration, SemAnCorr enables substantially more reliable zero-shot manipulation skill transfer to previously unseen objects than existing correspondence methods. Videos and additional visualizations are available at [https://semancorr.github.io](https://semancorr.github.io) .

</details>


### [270] [TacWAM: Anchor-Guided World Action Model with Mechanics-Aware Tactile Prediction](https://arxiv.org/abs/2607.28391)
*Lei Jin,Yiding Ma,Xin Zhang,Chen Gao,Wei Wu,Yong Li*

Main category: cs.RO

TL;DR: TacWAM 提出一种力学感知的触觉世界动作模型，通过空间对齐融合触觉编码器、触觉历史编码器和锚点引导三模态注意力，利用未来触觉状态监督机器人接触丰富的操作任务学习。

Research relevance: 弱相关（2分）。该论文属于机器人操作（robot manipulation）与触觉感知（tactile sensing）领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。论文不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。虽然涉及"deployment-consistent information constraints"这一部署相关概念，但指的是机器人部署而非星上/边缘部署，且不涉及任何模型轻量化方法（pruning, quantization, knowledge distillation, NAS, FLOPs等均未提及）。

Reading priority: 1分。该论文属于机器人触觉感知与操作学习领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测或任何模型压缩/加速技术。除非对多模态融合或训练-部署信息一致性约束的通用方法论感兴趣，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：接触丰富的机器人操作（contact-rich manipulation），包括脆弱物体抓取（fragile grasping）、持续表面接触（sustained surface contact）、动态手内操作（dynamic in-hand manipulation）。传感器类型：触觉传感器（tactile sensor）+ 视觉传感器（visual）。目标类别：非遥感目标，为机器人操作中的物体。应用场景：机器人操作与物理交互，非遥感/卫星场景。

Model architecture: TacWAM 包含三个核心模块：(1) Spatially Aligned Fusion (SAF) Tactile Encoder——将触觉外观、密集力场和形变流映射到共享潜在预测空间，并通过双侧力与力矩重建保留全局接触信息；(2) Tactile History Encoder——提供时间上下文，使未来触觉预测反映力和形变随时间的变化；(3) Anchor-Guided Tri-Modal (AGT) Attention——分离当前视觉和触觉锚点、未来预测token和动作token，使未来触觉状态监督训练但不被动作分支直接读取。整体属于World Action Model (WAM) 框架。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（如剪枝、量化、知识蒸馏、NAS、轻量网络设计等）。"deployment-consistent information constraints"仅指训练与部署时信息一致性约束，非模型轻量化技术。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署指标。其"deployment-consistent"概念指的是确保未来触觉预测token在训练时用于监督但部署时不作为动作分支输入，属于信息流约束而非计算资源优化。

Datasets and metrics: 数据集：四个真实世界接触丰富操作任务（fragile grasping, sustained surface contact, dynamic in-hand manipulation），无公开标准数据集名称。评价指标：平均成功率（average success rate），TacWAM达到75.0%，超过最强基线37.5个百分点。消融实验：移除触觉历史编码器、放宽未来预测目标访问权限时性能一致下降。

Experiments: 在四个真实世界接触丰富操作任务上评估，TacWAM平均成功率75.0%，比最强基线高37.5个百分点。分阶段消融实验显示：移除触觉历史（tactile history）和放宽未来预测目标访问（access to future prediction targets）均导致性能一致下降，验证了未来触觉监督结合信息丰富的触觉表征和部署一致性信息约束的有效性。

Limitations: (1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于机器人操作与触觉感知领域；(2) 摘要未报告模型规模、计算复杂度、推理速度等部署指标；(3) 仅在机器人操作任务上验证，未涉及任何遥感场景；(4) 未讨论模型压缩或边缘部署可行性；(5) 摘要未说明基线方法的具体名称和对比细节。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴的通用思想：(1) "deployment-consistent information constraints"——训练时使用未来/额外信息监督、部署时不依赖该信息的思路，可类比于知识蒸馏中teacher-student的信息传递设计，即在轨场景中可用高精度大模型（地面）蒸馏到星上轻量模型；(2) 多模态信息融合（视觉+触觉）的Spatially Aligned Fusion思想，可类比于遥感中多光谱/高光谱与SAR多模态融合检测；(3) 锚点引导注意力机制分离不同信息流的思路，可用于遥感检测中分离背景与目标特征。但这些借鉴点较为间接，整体参考价值有限。

Abstract: World Action Models (WAMs) combine future-state prediction with robot action generation, but existing approaches largely rely on visual futures. Visual prediction captures scene structure and object motion, yet provides limited supervision for force, deformation, shear, and slip during contact-rich manipulation. This creates two design requirements: tactile futures should carry meaningful physical information, and they should not become privileged cues for action generation. We present TacWAM, a mechanics-aware tactile WAM that addresses this challenge in three steps. First, a Spatially Aligned Fusion (SAF) Tactile Encoder maps tactile appearance, dense force fields, and deformation flow into a shared latent prediction space, with bilateral force and torque reconstruction preserving global contact information. Second, a tactile history encoder provides temporal context so future tactile prediction reflects how force and deformation change beyond the current tactile observation. Third, Anchor-Guided Tri-Modal (AGT) Attention separates current visual and tactile anchors, future prediction tokens, and action tokens, allowing future tactile states to supervise training without being directly read by the action branch. We evaluate TacWAM on four real-world contact-rich manipulation tasks covering fragile grasping, sustained surface contact, and dynamic in-hand manipulation. TacWAM achieves an average success rate of 75.0%, exceeding the strongest evaluated baseline by 37.5 percentage points. Staged ablations show consistent degradation when tactile history is removed and access to future prediction targets is relaxed. These results indicate that future tactile supervision can improve contact-aware action learning when combined with informative tactile representations and deployment-consistent information constraints.

</details>


### [271] [FasTac: A Curved Multispectral Vision-Based Tactile Sensor for High-Speed High-Precision 3D Shape and Force Perception](https://arxiv.org/abs/2607.28416)
*Xiaofan Lu,Kaiji Huang,Jiahui Chen,Yuankai Lin,Hua Yang,Zhouping Yin*

Main category: cs.RO

TL;DR: 提出FasTac，一种基于FPGA硬件加速的曲面视觉触觉传感器，结合多光谱光度立体、动态卷积力估计和FPGA部署，实现精细几何重建、三轴力估计与高速处理。

Research relevance: 弱相关（2分）。该论文属于机器人触觉感知领域，与遥感卫星、Earth observation、aerial image、SAR、高光谱/多光谱遥感目标检测均无直接关系。虽然涉及多光谱成像、FPGA硬件加速、动态卷积等与轻量化/边缘部署相关的技术点，但应用场景是机器人灵巧操作的触觉传感器，而非遥感在轨目标检测。其FPGA部署和硬件加速思路对星上部署有一定方法学借鉴意义，但整体与本研究方向弱相关。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，主要价值在于FPGA硬件加速和动态卷积的方法学借鉴，但整体场景差异大，不涉及遥感目标检测任务。可作为FPGA部署和动态卷积设计的补充参考，不建议作为核心阅读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：触觉感知（tactile sensing），包括3D表面重建、三轴力估计、振动测量。应用场景：机器人灵巧操作（dexterous manipulation）、反馈抓取（feedback grasping）。传感器类型：曲面视觉触觉传感器（curved vision-based tactile sensor），采用多光谱光度立体（multispectral photometric stereo）。目标类别：非遥感目标，为接触几何与力学信号。

Model architecture: FasTac传感器系统包含三个核心模块：(1) 单图像传感器同时多光谱成像（single-image-sensor simultaneous multispectral imaging），提供空间对齐观测用于表面法向估计；(2) 边界先验快速泊松深度重建（boundary-prior fast Poisson depth reconstruction）；(3) HyperForce模块，使用位置感知动态卷积（position-aware dynamic convolution）建模曲面弹性体空间非均匀力学响应并估计三轴力。完整图像到法向-力流水线部署在FPGA上。

Lightweight method: 主要轻量化/加速手段为FPGA硬件加速（hardware acceleration on FPGA），将完整的image-to-normal-force处理流水线部署到FPGA上，将处理延迟从GPU上的3.26 ms缩短至1.09 ms。此外，动态卷积（dynamic convolution）用于高效建模空间非均匀响应。摘要未说明剪枝、量化、知识蒸馏或NAS等模型压缩方法。

Onboard deployability: FPGA部署将处理延迟从GPU的3.26 ms降至1.09 ms，展示了硬件加速在边缘/嵌入式场景的可行性。但摘要未说明参数量、FLOPs、显存、能耗等具体部署指标。该工作面向机器人触觉而非星上遥感，但其FPGA加速思路对星上在轨推理的硬件部署具有方法学参考价值。

Datasets and metrics: 数据集：摘要未说明具体公开数据集名称，实验基于自建触觉传感器采集数据。评价指标：深度平均绝对误差（depth MAE）、归一化平均绝对误差（NMAE，分别用于法向力和剪切力）、处理延迟（processing latency）。关键结果：NIR照明与边界先验将depth MAE从0.2730 mm降至0.0415 mm；HyperForce在法向力和剪切力上分别达到2.74%和2.39%的NMAE；FPGA部署将延迟从3.26 ms（GPU）降至1.09 ms。

Experiments: 实验验证了：(1) NIR照明与边界先验对深度重建精度的提升（MAE从0.2730 mm降至0.0415 mm）；(2) HyperForce在三轴力估计上的精度（法向NMAE 2.74%，剪切NMAE 2.39%）；(3) FPGA部署相对GPU的延迟缩短（3.26 ms→1.09 ms）；(4) 多目标重建、反馈抓取和振动测量验证了精细几何感知、稳定力反馈和动态接触感知能力。

Limitations: 与本研究方向相关性弱：该论文面向机器人触觉感知而非遥感目标检测，不涉及遥感图像、目标检测任务、遥感数据集或遥感场景理解。摘要未说明模型参数量、FLOPs、能耗等轻量化关键指标，也未涉及剪枝/量化/蒸馏等模型压缩方法。FPGA加速虽具参考价值，但触觉传感器与遥感在轨检测在数据模态、任务目标和部署约束上差异显著。

Ideas for my research: 尽管场景不同，以下技术点可借鉴：(1) FPGA硬件加速思路——将完整推理流水线部署到FPGA以降低延迟，对星上在轨目标检测的硬件加速方案设计有参考价值；(2) 动态卷积（dynamic convolution）——用于建模空间非均匀响应，可类比遥感图像中不同区域/不同目标尺度的非均匀特征，动态卷积或可提升检测器对多尺度目标的适应能力；(3) 多光谱成像与多光谱信息融合——虽然此处用于光度立体，但多光谱/高光谱遥感目标检测可借鉴其多通道信息利用策略；(4) 边界先验（boundary prior）加速重建——类似地，在遥感小目标检测中引入目标边界先验可能提升定位精度。

Abstract: Curved tactile fingertips for dexterous manipulation must resolve fine contact geometry, distinguish normal and tangential loads, and capture transient signals. Existing curved vision-based tactile sensors struggle to combine accurate 3D reconstruction, three-axis force estimation, and high-speed processing in a compact form. This article presents FasTac, a curved vision-based tactile sensor integrating multispectral photometric stereo, dynamic-convolution force estimation, and hardware acceleration on a field-programmable gate array (FPGA). Single-image-sensor simultaneous multispectral imaging provides spatially aligned observations for robust surface normal estimation, followed by boundary-prior fast Poisson depth reconstruction. HyperForce uses position-aware dynamic convolution to model the spatially nonuniform mechanical response of curved elastomers and estimate three-axis forces. The complete image-to-normal-force pipeline is deployed on an FPGA. Experiments show that near-infrared (NIR) illumination and the boundary prior decrease depth mean absolute error (MAE) from 0.2730 mm to 0.0415 mm; HyperForce achieves normalized mean absolute error (NMAE) values of 2.74% and 2.39% for normal and shear forces, respectively; and FPGA deployment shortens processing latency from 3.26 ms on the GPU to 1.09 ms. Multi-object reconstruction, feedback grasping, and vibration measurement validate fine geometric perception, stable force feedback, and dynamic contact sensing.

</details>


### [272] [One Future, Every Robot: Label-Efficient Collective-State Prediction with Decentralized JEPA](https://arxiv.org/abs/2607.28443)
*Alan-Barsag Gazzaev,Alexey Garvilov,Sergey Muravyov*

Main category: cs.RO

TL;DR: 提出Collective-State JEPA (CS-JEPA)，一种用于多机器人群体分布式共享状态预测的循环联合嵌入预测架构，通过局部观测和带宽受限消息预测群体未来集体状态。

Research relevance: 弱相关（2分）。该论文属于多机器人群体/分布式系统的预测方法研究，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关联。不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。虽然涉及"轻量"的带宽受限消息传递和部署时低参数量，但这是群体通信约束而非模型压缩/轻量化，与遥感在轨目标检测轻量化方向基本无关。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于多机器人群体预测领域，不涉及遥感场景、目标检测或经典模型压缩方法，仅存在极弱的架构设计思想借鉴价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多机器人群体分布式共享状态预测（decentralized shared-state prediction）。应用场景：机器人群体（swarm）协同预测，非遥感场景。传感器类型：摘要未说明（仅提及局部观测local observations）。目标类别：不涉及目标检测，预测对象是群体未来集体状态（future collective state）。

Model architecture: CS-JEPA是一种循环联合嵌入预测架构（recurrent joint-embedding predictive architecture），输出为每个机器人共享的未来token字段。部署时每个机器人使用16帧局部历史（16-frame local history）和每条有向边一个64浮点数的循环消息（64-float recurrent message）。无全局池化（global pooling）、无目标编码器（target encoder）、无episode时钟、无记录的未来动作。

Lightweight method: 摘要未明确涉及剪枝、量化、知识蒸馏、NAS等经典模型压缩方法。但体现了轻量化设计思想：部署时仅使用16帧局部历史和每条边64浮点数的循环消息，无全局池化和目标编码器，通信带宽受限。对比方法额外使用9,607个仅训练期参数（training-only parameters），说明CS-JEPA在部署容量上更紧凑。但这属于通信/部署约束下的架构设计，而非面向边缘/星上推理的模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等具体部署指标。仅提及部署时每个机器人使用16帧局部历史和每条有向边64浮点数的循环消息，以及对比方法多出9,607个仅训练期参数。未涉及FPGA/NPU/TensorRT等硬件加速或星上部署相关内容。

Datasets and metrics: 数据集：摘要未说明具体数据集名称，但涉及in-distribution、ring、mutual-kNN、unseen-size等拓扑/规模变体，机器人数量最多108个。评价指标：prediction-error、inter-robot-agreement label-budget AUC、branch-value MSE（降低45.5%）、within-context candidate-score Pearson correlation（提升0.1291）。实验设置：ridge probes在6、12、24个全局标注episode上评估冻结表征；五种子前瞻性注册实验和八种子密封实验。

Experiments: 主要结果：1) 五种子前瞻性注册实验（prospectively registered five-seed follow-up）中，CS-JEPA在in-distribution、ring、mutual-kNN、unseen-size家族上均优于raw-future reconstruction，5/5外种子全部有利；2) 八种子密封实验（sealed eight-seed follow-up）中，CS-JEPA将branch-value MSE降低45.5%，within-context candidate-score Pearson correlation提升0.1291，8/8种子均有利，包括unseen N=32。

Limitations: 1) 与遥感/卫星/在轨目标检测完全无关，属于多机器人群体预测领域；2) 摘要未说明具体数据集、传感器类型、模型参数量、FLOPs、延迟等关键部署指标；3) 未涉及目标检测任务；4) 轻量化仅体现在通信带宽约束和部署容量上，非面向边缘/星上推理的模型压缩；5) 摘要未说明方法在真实硬件上的部署验证。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但仍有少量可借鉴点：1) JEPA（联合嵌入预测）架构的"无目标编码器"设计思想，可启发在轨模型训练时减少对标注/目标编码的依赖，降低训练成本；2) 带宽受限下的紧凑消息传递设计（64-float recurrent message），对星地链路带宽受限场景下的模型间通信/协同推理有参考价值；3) 冻结表征+ridge probe的评估范式，可用于在轨模型轻量化后的快速评估。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Can every robot in a swarm predict the same future collective state from only local observations and bandwidth-limited messages? We formulate this as decentralized shared-state prediction and introduce Collective-State JEPA (CS-JEPA), a recurrent joint-embedding predictive architecture whose output at every robot represents one common future token field. At deployment, each robot uses a 16-frame local history and one 64-float recurrent message per directed edge; there is no global pooling, target encoder, episode clock, or recorded future action. After pretraining without downstream collective labels, frozen representations are evaluated with ridge probes fitted on 6, 12, or 24 globally labeled episodes. Against raw-future reconstruction with the same receiver anchor and deployment capacity but 9,607 additional training-only parameters, a prospectively registered five-seed follow-up improves prediction-error and inter-robot-agreement label-budget AUC on in-distribution, ring, mutual-kNN, and unseen-size families up to 108 robots. Every effect favors CS-JEPA in 5/5 outer seeds. In a separate sealed eight-seed follow-up, matched action-conditioned predictors receive each candidate four-step plan before producing receiver-local predictive representations. CS-JEPA reduces branch-value MSE by 45.5% and improves within-context candidate-score Pearson correlation by 0.1291, with both effects favorable in 8/8 seeds, including at unseen N=32. These results support common-future JEPA targets as a label-efficient primitive for decentralized swarm prediction under topology and size shift, with additional evidence of planning-relevant value estimation.

</details>


### [273] [Machines that know they are aging: a framework for hardware-aware autonomous intelligence](https://arxiv.org/abs/2607.28451)
*Cheng Siong Chin,Jianhua Zhang,Mohan Venkateshkumar*

Main category: cs.RO

TL;DR: 提出 Aging-Aware Autonomous Intelligence (AAAI) 框架，将硬件健康状态（电池退化、传感器漂移、处理器时序误差、内存可靠性下降）直接整合进自主系统的推理、规划与任务执行中，通过硬件自感知、自适应推理和生存中心智能实现硬件老化感知的自主决策。

Research relevance: 弱相关（2分）。论文讨论的是自主系统硬件老化感知与自适应推理的通用框架，并非遥感/卫星目标检测场景，也未涉及模型轻量化方法。虽然提及 space missions 作为应用场景之一，但核心关注点是硬件健康监测与寿命管理（prognostics, lifecycle management），而非遥感目标检测或模型压缩。与"遥感卫星在轨目标检测模型轻量化"方向仅存在间接关联：卫星在轨运行同样面临硬件老化问题，且自适应推理（adjusts inference complexity）与在轨计算资源动态管理有概念上的相似性，但论文未涉及任何遥感数据、目标检测任务或轻量化技术。

Reading priority: 2分。该论文与"遥感卫星在轨目标检测模型轻量化"方向弱相关，属于概念性框架论文，无遥感数据、无目标检测任务、无轻量化技术细节。仅"硬件老化感知+自适应推理复杂度"这一概念对在轨部署场景有间接启发价值，但缺乏可复现的技术贡献，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：自主系统硬件老化感知与自适应决策框架（非目标检测任务）。应用场景：空间任务、海洋机器人、植入式医疗设备等不可达或安全关键环境。传感器类型：摘要未说明（提及 sensing subsystem 但未具体化）。目标类别：不适用（非检测任务）。

Model architecture: AAAI 框架由三大支柱构成：(1) hardware self-awareness——利用 physics-of-failure 模型持续估计电源、感知、内存和计算子系统的健康状态；(2) self-adaptive reasoning——根据剩余硬件能力调整推理复杂度（inference complexity）、规划时域（planning horizon）和任务优先级；(3) survival-centric intelligence——通过性能优化、资源节约和优雅降级（graceful degradation）在任务目标间分配剩余运行寿命。整体为闭环认知架构（closed-loop cognitive architecture），统一了 prognostics、生命周期管理和硬件感知计算。

Lightweight method: 摘要未说明具体的模型轻量化方法（无 pruning、quantization、knowledge distillation、NAS 等）。但 self-adaptive reasoning 支柱提到"adjusts inference complexity"，即根据硬件健康状态动态调整推理复杂度，这与在轨场景下根据计算资源动态降级模型复杂度有概念上的相似性，但论文未给出具体实现方法。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等具体部署指标。论文强调"Rather than introducing new hardware"，即不引入新硬件，而是通过软件层面的认知架构整合硬件健康感知。提及适用于"inaccessible or safety-critical environments, including space missions"，说明面向星上/嵌入式部署场景，但未给出任何量化部署数据。

Datasets and metrics: 摘要未说明任何数据集、评价指标或实验设置。论文属于概念性/框架性工作，未提及具体实验验证。

Experiments: 摘要未说明实验结果、对比方法或消融实验。该论文目前停留在框架提出与论证层面（"We argue that such integration is essential..."），缺乏实证支撑。

Limitations: (1) 纯概念性框架，无实验验证和量化结果；(2) 未涉及遥感目标检测任务，也未讨论任何检测模型；(3) 未涉及模型轻量化技术（pruning/quantization/distillation 等）；(4) 未讨论硬件老化感知与推理复杂度调整的具体实现机制；(5) 未说明如何将硬件健康状态映射为具体的推理复杂度调整策略；(6) 未讨论在轨卫星计算资源受限下的具体部署约束。

Ideas for my research: (1) 概念借鉴：卫星在轨运行中硬件会老化（如电池容量衰减、存储单元可靠性下降、处理器时序漂移），这为"在轨目标检测模型轻量化"提供了新的动机维度——不仅是为了降低资源占用，更是为了在硬件退化后仍能维持可用的检测性能；(2) 自适应推理复杂度（adaptive inference complexity）的思想可迁移到在轨检测场景：根据卫星剩余计算能力动态调整检测模型的深度/分辨率/帧率，实现 graceful degradation；(3) 硬件健康状态可作为动态模型切换（如大模型→轻量模型）的触发信号，这与在轨模型动态部署策略有结合潜力；(4) 但需注意，该论文未提供任何可复现的技术细节，仅可作为概念启发，不宜作为技术方法直接借鉴。

Abstract: Autonomous systems inevitably age, yet their artificial intelligence typically assumes hardware remains in its original condition. Batteries degrade, sensors drift, processors accumulate timing errors, and memory reliability declines, creating a growing mismatch between assumed and actual capability. This can lead to agnostic collapse, where mission failure arises from accumulated hardware degradation rather than a single component fault. We propose Aging-Aware Autonomous Intelligence (AAAI), a framework that integrates hardware health directly into reasoning, planning, and mission execution. AAAI is built on three pillars: hardware self-awareness, which continuously estimates the health of power, sensing, memory, and computation subsystems using physics-of-failure models; self-adaptive reasoning, which adjusts inference complexity, planning horizon, and task priorities according to remaining hardware capability; and survival-centric intelligence, which allocates remaining operational life across mission objectives through performance optimization, resource conservation, and graceful degradation. Rather than introducing new hardware, AAAI unifies prognostics, lifecycle management, and hardware-aware computing into a closed-loop cognitive architecture. We argue that such integration is essential for autonomous systems operating in inaccessible or safety-critical environments, including space missions, marine robotics, and implantable medical devices. By enabling machines to recognize and respond to their own aging, AAAI improves resilience, extends operational lifetime, and supports safer, more graceful mission completion.

</details>


### [274] [TEA-AgriVLN: Traversability Estimation Alarm for Agricultural Vision-and-Language Navigation](https://arxiv.org/abs/2607.28474)
*Xiaobei Zhao,Xingqi Lyu,Xin Chen,Xiang Li*

Main category: cs.RO

TL;DR: 提出TEA模块，通过估计相机图像的可穿越性来辅助农业场景下的视觉-语言导航（VLN-CE）决策，集成到AgriVLN骨干中形成TEA-AgriVLN方法，在A2A基准上取得SOTA性能。

Research relevance: 弱相关（2分）。论文属于视觉-语言导航（VLN-CE）领域，面向农业场景的机器人导航，与遥感卫星、在轨目标检测、模型轻量化均无直接关联。虽然涉及"traversability estimation"（可穿越性估计）和场景理解，但既不是遥感/卫星数据，也不是目标检测任务，更不涉及任何轻量化或部署优化。属于泛机器人视觉导航方法，仅在地面场景理解层面有微弱借鉴意义。

Reading priority: 1分。论文属于农业场景视觉-语言导航（VLN-CE），与遥感卫星在轨目标检测及模型轻量化方向基本无关。既非遥感/卫星场景，也非目标检测任务，更不涉及任何轻量化或部署优化技术。仅"可穿越性估计+告警重思考"的决策机制在方法论层面有极微弱的迁移可能，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务：Vision-and-Language Navigation (VLN-CE)，即视觉-语言连续环境导航，预测低层动作序列以引导机器人从起点导航到目标位置。场景：农业场景（agricultural scenes），由A2A基准定义，从室内场景扩展而来。传感器：相机图像（camera image）。目标类别：地面可穿越性（traversability）分类，涉及不同地面类别（如未成熟玉米地）和场景类别。

Model architecture: TEA模块（Traversability Estimation and Alarm）：估计相机图像的可穿越性，当预测动作与可穿越性图不一致时向决策器发出告警以触发重新思考。集成到AgriVLN骨干网络中，构成TEA-AgriVLN方法。摘要未说明具体网络结构细节（如backbone类型、特征提取器、动作预测头等）。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计。也未提及实时推理、边缘部署、星上部署（onboard deployment）、FPGA/NPU/TensorRT等部署相关内容。

Onboard deployability: 摘要未说明。未提供参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）等任何部署相关指标。虽然VLN-CE本身面向机器人实时导航，但论文未讨论计算效率或资源约束。

Datasets and metrics: 数据集：A2A benchmark（农业VLN-CE基准）。评价指标：Success Rate (SR)（成功率，从0.47提升到0.54）、Navigation Error (NE)（导航误差，从2.91m降低到2.70m）。此外进行了消融实验（ablation studies）和案例研究（case study），讨论TEA在不同地面类别和场景类别上的有效性。

Experiments: 在A2A基准上，TEA-AgriVLN相比AgriVLN基线将SR从0.47提升到0.54（+0.07），NE从2.91m降低到2.70m（-0.21m），达到农业VLN-CE领域的SOTA。进行了消融实验和案例研究，分析TEA在不同地面类别和场景类别上的效果与局限。摘要未说明与其他SOTA方法的详细对比。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关，属于地面机器人导航领域。2. 摘要未说明TEA模块的计算开销、推理速度等效率指标。3. 摘要未说明模型参数量、FLOPs等轻量化相关信息。4. 农业场景的可穿越性估计依赖相机图像，与遥感卫星的俯视成像（top-down view）差异显著。5. 摘要未讨论泛化性、鲁棒性等细节。

Ideas for my research: 1. "可穿越性估计+告警重思考"（TEA）的决策机制思想可借鉴：在遥感目标检测中，当模型预测结果与场景先验/上下文不一致时，可设计类似的"告警-重思考"机制来提升检测可靠性。2. 农业场景中"模糊类别"（如未成熟玉米地对不同agent可穿越性不同）的思想，可类比遥感中"模糊目标"（如云雾遮挡、低分辨率下的小目标）的检测不确定性处理。3. 场景理解与决策的耦合方式（traversability map与动作决策对齐）可启发遥感中"检测结果与地理上下文对齐"的思路。4. 但整体借鉴价值有限，因为不涉及遥感数据、目标检测任务或轻量化技术。

Abstract: Vision-and-Language Navigation in Continuous Environments (VLN-CE) requires an agent to follow a natural language instruction, predicting a sequence of low-level actions to navigate a robot from a starting point to a target location. The A2A benchmark and the AgriVLN method pioneeringly extended VLN-CE from indoor scenes to agricultural scenes, while we observed a challenging distinction: In indoor scenes, whether a zone is traversable tends to be clear to classify, such as wood floors are traversable but concrete walls are not. In agricultural scenes, however, this issue tends to be ambiguous, such as an unripe cornfield might be traversable for a robotic dog but might be non-traversable for a human. To address this issue, we propose the TEA module, which estimates the traversability of the camera image, then alarm the decision-maker for rethinking when the predicted action does not align with the traversability map. We integrate it into the AgriVLN backbone to build our TEA-AgriVLN method. When evaluated on A2A, it improves Success Rate (SR) from 0.47 to 0.54 and Navigation Error (NE) from 2.91 m to 2.70 m, showing the state-of-the-art performance in the agricultural VLN-CE domain. We further implement the ablation studies and the case study, discussing the effectiveness and limitations of TEA on different ground categories and scene classes. Code: https://github.com/AlexTraveling/TEA-AgriVLN.

</details>


### [275] [X-NavDP: Generalizing Navigation Diffusion Policy to Novel Behavior and Embodiments with Group Q-score Reweighted Matching](https://arxiv.org/abs/2607.28560)
*Tianyu Yang,Yiming Zeng,Wenzhe Cai,Yuqiang Yang,Jiaqi Peng,Hui Cheng,Jiangmiao Pang,Tai Wang*

Main category: cs.RO

TL;DR: 提出GQRM框架，通过自引导探索和组Q-score重加权匹配，对预训练的导航扩散策略进行数据高效的RL后训练，提升跨本体视觉导航性能。

Research relevance: 弱相关（2分）。该论文属于机器人视觉导航与扩散策略强化学习领域，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。虽然涉及"onboard local observations"（机载局部观测）和"cross-embodiment"（跨本体），但场景是地面/空中机器人导航，而非遥感卫星在轨目标检测。其RL后训练和扩散策略方法对泛机器学习有一定借鉴意义，但与本研究方向的遥感目标检测和模型轻量化基本无关。

Reading priority: 1分。该论文聚焦机器人导航扩散策略的RL后训练，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：跨本体视觉导航（cross-embodiment visual navigation）；应用场景：机器人导航，包括死胡同逃逸、长障碍绕行等挑战性场景；传感器类型：视觉（onboard local observations，机载局部观测）；目标类别：导航路径/行为决策，非目标检测任务。

Model architecture: 基于预训练的导航扩散策略（navigation diffusion policy），通过RL后训练微调得到X-NavDP策略。框架包含两个核心设计：(i) 自引导探索策略（self-bootstrapped exploration）结合行为扰动（behavior perturbation）以保持预训练策略先验；(ii) 组Q-score归一化机制（group Q-score normalization），在每个状态上计算逐轨迹价值用于高效重加权分数匹配（reweighted score matching）。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。其核心是RL后训练的数据效率优化，而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署指标。虽然涉及"onboard local observations"（机载局部观测），但未讨论边缘部署或实时推理约束，也未涉及FPGA/NPU/TensorRT等硬件加速。

Datasets and metrics: 在仿真环境中进行分布式在线RL训练，跨异构本体（heterogeneous embodiments）。评价指标为导航成功率（success rate）：仿真环境从61.20%提升到84.28%；真实世界困难场景从10%提升到65%。具体数据集名称摘要未说明。

Experiments: 主要实验：在仿真和真实世界两种环境下验证X-NavDP策略。仿真中成功率从61.20%提升至84.28%；真实世界困难场景成功率从10%提升至65%。对比对象为之前的RL for diffusion方法（仅带来边际改进）。代码和模型已公开。

Limitations: 1. 与遥感卫星在轨目标检测方向完全无关，无法直接借鉴其检测或轻量化方法。2. 摘要未说明模型规模、推理速度、计算资源需求等部署相关信息。3. 未涉及目标检测任务，仅涉及导航策略。4. 未讨论模型压缩或加速技术。

Ideas for my research: 1. 虽然场景不同，但其"self-bootstrapped exploration + behavior perturbation"的数据高效训练思想，可类比借鉴到遥感小样本目标检测的难例挖掘或数据增强策略。2. "group Q-score normalization"的逐轨迹价值归一化思想，在遥感检测的样本重加权（如困难样本加权）中有一定参考价值。3. 其"cross-embodiment"（跨本体泛化）思想可类比为遥感检测中跨传感器/跨分辨率泛化问题。但整体借鉴价值有限。

Abstract: Pretraining navigation diffusion policies rely on large-scale expert demonstrations. These data are typically generated by a fully-informed oracle planner suited to a single nominal robot. This limits the policy's generalization to diverse embodiments and challenging scenarios (e.g., escaping dead ends or detouring long obstacles) that demand diverse local reactive behaviors with only onboard local observations. Post-training the policy with reinforcement learning (RL) offers a principled remedy. However, previous RL for diffusion approaches lead to only marginal improvements. This is because the intractable likelihood of diffusion policies renders policy gradients unstable in addition to inefficient policy exploration. To address these challenges, we propose a data-efficient diffusion RL post-training framework - GQRM (Group Q-score Reweighted Matching). Our framework introduces two complementary designs: (i) a self-bootstrapped exploration strategy with behavior perturbation that preserves the pretrained policy prior, and (ii) a group Q-score normalization mechanism that computes per-trajectory values on each state for efficient reweighted score matching. By conducting distributed online RL training across heterogeneous embodiments, the resulting fine-tuned policy, X-NavDP, achieves state-of-the-art cross-embodiment visual navigation performance, improving the overall success rate from 61.20% to 84.28% in simulation and 10% to 65% in real-world hard cases. The code and model are publicly available at https://yty-sky.github.io/x-navdp-project-page.

</details>


### [276] [FA-RDP: A Frequency-Adaptive Reactive Diffusion Policy for Contact-Rich Manipulation](https://arxiv.org/abs/2607.28596)
*Lifeng Zhuo,Wendi Chen,Han Xue,Shirun Tang,Jun Lv,Cewu Lu,Chuan Wen*

Main category: cs.RO

TL;DR: 提出FA-RDP，一种频率自适应的反应式扩散策略，通过多频视觉-力Transformer和流形一致性蒸馏，在接触丰富操作任务中兼顾接触前的动作多模态保持与接触后的快速力反馈响应。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于机器人接触丰富操作（contact-rich manipulation）中的策略学习，属于机器人学习/扩散策略领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。虽然论文中使用了"distillation"（蒸馏）一词，但其"Manifold Consistency Distillation"是用于扩散策略的动作流形重参数化，并非模型压缩中的knowledge distillation，与轻量化目标检测无关。相关性评分：1分。

Reading priority: 1分。论文属于机器人扩散策略学习领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。仅当对"频率自适应推理"这一概念性思想感兴趣时，可略读以获取灵感。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：接触丰富操作（contact-rich manipulation）中的机器人策略学习。应用场景：机器人操作（如抓取、装配等接触任务）。传感器类型：视觉（visual）+ 力觉（force feedback）。目标类别：不涉及目标检测，目标是机器人动作策略。摘要未说明具体机器人平台。

Model architecture: FA-RDP（Frequency-Adaptive Reactive Diffusion Policy）：一个共享的多频视觉-力Transformer（shared multi-frequency visual-force Transformer），在低频和高频两个频率上预测动作块（action chunks）。包含一个学习的多模态指示器（learned multimodality indicator），用于动态选择采样策略：接触前采用多步低频采样，动作歧义降低后切换为单步高频采样。MCD（Manifold Consistency Distillation）将扩散网络重参数化以在机器人动作流形上预测动作，同时保留DDPM-based残差监督。

Lightweight method: 论文中的"distillation"（MCD, Manifold Consistency Distillation）并非模型压缩意义上的知识蒸馏，而是扩散策略网络的重参数化方法，用于将动作预测约束在机器人动作流形上。论文不涉及剪枝（pruning）、量化（quantization）、NAS、轻量网络设计、实时推理优化、边缘/星上部署等轻量化技术。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。论文讨论的"reactivity"（反应性）和"high-frequency sampling"（高频采样）是机器人控制层面的实时性需求，而非边缘/星上部署的推理加速。与FPGA/NPU/TensorRT等硬件部署无关。

Datasets and metrics: 在三个接触丰富操作任务（three contact-rich manipulation tasks）上进行实验。评价指标为成功率（success rate）以及接触前轨迹模式多样性（diverse pre-contact trajectory modes）。摘要未说明具体数据集名称和实验环境细节。

Experiments: FA-RDP在三个接触丰富操作任务上取得了最高成功率（highest success rate），同时保持了接触前多样的轨迹模式。摘要未提供与基线方法的详细对比数据、消融实验细节或定量结果。

Limitations: 与本研究方向（遥感卫星在轨目标检测与轻量化）完全无关。摘要未说明模型规模、推理速度、部署硬件等关键信息。此外，摘要未说明MCD对训练稳定性和收敛速度的影响，也未提供多频采样的计算开销对比。

Ideas for my research: 虽然整体方向无关，但有两个概念层面的借鉴点：(1) "frequency-adaptive"（频率自适应）思想——在遥感在轨检测中，可类比考虑根据场景复杂度或目标密度动态调整推理频率/帧率，在简单场景降低推理频率以节省星上算力与能耗；(2) "multimodality indicator"（多模态指示器）——可类比设计一个轻量的场景复杂度判别器，动态决定是否启用高精度重检测或轻量快速检测分支，实现精度与速度的自适应权衡。但需注意这些仅是概念层面的类比，论文本身不提供任何遥感或轻量化部署的技术方案。

Abstract: In contact-rich manipulation, action multimodality and reactivity dominate different stages of a single episode. Before contact, multiple trajectories might be equally valid, making it important to preserve diverse action modes. After contact, geometric constraints and force limits narrow the solution space, while successful execution demands rapid responses to force feedback. However, standard diffusion policies use a fixed inference frequency and sampling steps throughout the episode, forcing a fundamental compromise: low-frequency, multi-step sampling better preserves pre-contact multimodality but responds slowly to force feedback, whereas high-frequency sampling improves reactivity but tends to collapse distinct pre-contact modes. To resolve this tradeoff, we present FA-RDP, a frequency-adaptive reactive diffusion policy. A shared multi-frequency visual-force Transformer predicts action chunks at both low and high frequencies, while a learned multimodality indicator dynamically selects multi-step low-frequency sampling before contact and one-step high-frequency sampling as action ambiguity decreases. We further introduce Manifold Consistency Distillation (MCD), which reparameterizes the diffusion network to predict actions on the robot action manifold while retaining DDPM-based residual supervision. Experiments on three contact-rich manipulation tasks show that FA-RDP achieves the highest success rate while preserving diverse pre-contact trajectory modes. Code and videos are available at https://fa-rdp.github.io.

</details>


### [277] [PAC-MAN: Perception-Aware CBF-RL for Whole-Body Safety in Humanoid Dodgeball](https://arxiv.org/abs/2607.28623)
*Lizhi Yang,Junheng Li,Aaron D. Ames*

Main category: cs.RO

TL;DR: 提出PAC-MAN，一种感知感知的CBF-RL框架，将控制屏障安全与部署现实的机载感知耦合，用于全身人形机器人躲避球任务，并在Unitree G1上零样本部署。

Research relevance: 弱相关（2分）。该论文属于机器人控制与强化学习领域，涉及机载感知（onboard sensing）、轻量级策略部署（lightweight policy）和实时推理，但场景是人形机器人躲避球（humanoid dodgeball），与遥感、卫星、Earth observation、aerial image、SAR、高光谱等完全无关。虽然涉及"onboard"和"lightweight"概念，但属于机器人机载而非星上部署，且任务不是目标检测而是运动控制/躲避策略。

Reading priority: 1分。该论文属于人形机器人运动控制与强化学习领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然涉及机载感知和轻量策略部署的概念，但任务类型、传感器模态、应用场景均不匹配，仅"训练-部署分离"和"感知鲁棒性"思想有极有限的间接借鉴价值。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：人形机器人躲避球（whole-body humanoid dodgeball）运动控制与躲避策略；应用场景：机器人运动控制、强化学习部署；传感器类型：头戴式相机（head-mounted camera）的segmentation-masked depth；目标类别：球（ball）。与遥感目标检测无关。

Model architecture: PAC-MAN框架：CBF（Control Barrier Function）与RL（Reinforcement Learning）耦合。策略输入为分割掩码后的深度图像（segmentation-masked depth），训练时CBF引导提供每个身体链路的clearance信息，并引入对抗性运动先验（adversarial motion prior）正则化躲避反射。包含Joint-CBF和Link-CBF两种变体。

Lightweight method: 摘要提及"lightweight Link-CBF policy"，但未说明具体的轻量化手段（如剪枝、量化、蒸馏、NAS等）。轻量化的体现主要在于策略仅依赖固定机载相机（fixed onboard camera）的感知输入，而非特权状态（privileged state），属于感知层面的轻量化/简化，而非模型压缩技术。具体参数量、FLOPs、延迟等均未说明。

Onboard deployability: 摘要说明在Unitree G1人形机器人上零样本（zero-shot）部署，成功躲避95%的投掷。涉及机载相机感知、实时推理，但未给出延迟、能耗、显存、参数量、FLOPs等具体部署指标。属于机器人机载部署，非星上部署。

Datasets and metrics: 使用受控的any-link contact benchmark，包含两种模式：单次投掷（single throws）和部署循环（deployment loop，机器人走回站点并在投掷间恢复）。评价指标为躲避成功率（95% on real world throws）以及与特权状态oracle（privileged state oracle）的差距（within a few points）。摘要未说明具体数据集名称。

Experiments: 在受控基准上评估：固定机载相机单独足以完成躲避。发现可用屏障结构依赖感知可观测性：Joint-CBF在准确球状态时表现最佳，在固定相机观测下作为训练引导时性能下降，在球跟踪云台（ball-tracking gimbal）或特权运行时滤波器下恢复。最终部署Link-CBF策略在真实世界Unitree G1上零样本运行，容忍不完美感知，95%投掷成功。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于机器人控制领域；2. 摘要未说明模型参数量、FLOPs、延迟、能耗等部署指标；3. 未涉及任何目标检测任务或遥感图像处理；4. 轻量化仅体现在感知输入简化，未涉及模型压缩技术；5. 未说明具体网络架构和训练细节。

Ideas for my research: 1. "感知可观测性影响控制/策略性能"的思想可借鉴：在星上部署时，传感器观测质量（如分辨率、噪声）对检测模型性能的影响值得系统研究；2. "训练时使用特权信息引导、部署时仅依赖机载感知"的teacher-student式训练范式，与知识蒸馏（knowledge distillation）思想相通，可借鉴到遥感目标检测模型的训练-部署分离策略；3. "容忍不完美感知"的鲁棒性设计思路，对星上资源受限、图像质量下降场景下的检测模型鲁棒性有参考价值；4. 零样本部署（zero-shot deployment）的思想可借鉴到模型从训练环境到星上环境的迁移。

Abstract: We present PAC-MAN, a perception-aware CBF-RL framework that couples control-barrier safety with deployment-realistic onboard sensing for whole-body humanoid dodgeball. The deployed policy sees the ball only as segmentation-masked depth from a head-mounted camera, while training-time CBF guidance represents clearance to every body link, and an adversarial motion prior regularizes the resulting evasive reflexes. We evaluate on a controlled any-link contact benchmark with seeded throws in two regimes: single throws and a deployment loop in which the robot walks back to its station and recovers between throws. On this benchmark, the policy comes within a few points of a privileged state oracle: a fixed onboard camera alone is adequate for evasion. We find that usable barrier structure depends on perceptual observability: Joint-CBF gives the best performance with accurate ball states, degrades under fixed-camera observations when used only as training guidance, and recovers with a ball-tracking gimbal or privileged runtime filter. We therefore deploy a lightweight Link-CBF policy zero-shot on the Unitree G1 in the real world, where it tolerates imperfect perception, succeeds on 95% of throws, and uses semantic segmentation to dodge different balls.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [278] [Recursive transformers for semiconductor thermo-mechanical reliability](https://arxiv.org/abs/2607.27251)
*Kart-leong Lim*

Main category: cs.LG

TL;DR: 提出一种深度递归Transformer架构，通过权重共享减少可学习参数，用于工程设计中替代昂贵仿真的代理模型（surrogate model），在低维小数据集上实现参数效率与计算开销的平衡。

Research relevance: 弱相关（2分）。论文属于泛机器学习/Transformer架构优化方法，核心是递归权重共享Transformer用于工程代理建模（热机械可靠性分析、PDE数值求解），与遥感、卫星、目标检测、在轨部署均无直接关联。但其"减少可学习参数、增加计算复用"的递归权重共享思想，以及硬件感知的FLOPs/参数量评估框架，对轻量化模型设计有一定方法论借鉴意义。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向弱相关，核心贡献在工程代理建模领域。但其递归权重共享的参数压缩思路和硬件感知评估框架有一定方法论参考价值，可作为轻量化方法综述中的补充阅读，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：工程代理建模（surrogate modeling），替代有限元分析（FEA）和PDE数值求解。应用场景：先进半导体封装的热机械可靠性分析、Laplace PDE电容场迭代求解。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。

Model architecture: 三种递归Transformer范式对比：a) Tiny Recursive Model；b) 提出的Depth Recursive Transformer；c) 简单递归Transformer。核心思想是递归权重共享（recursive weight-sharing），即在不同深度层间共享参数，以"额外计算"换取"更少可学习参数"，避免小数据集上的过拟合。

Lightweight method: 递归权重共享（recursive weight-sharing）作为参数压缩手段：通过在不同网络深度复用同一组权重，显著降低可学习参数量，同时保持足够的计算深度。这是一种结构层面的轻量化策略，与剪枝、量化、蒸馏等经典压缩方法不同，属于"参数共享型"轻量化设计。

Onboard deployability: 摘要未说明具体部署平台。但论文强调"hardware-aware evaluation"和"resource-constrained scenarios"，比较了参数量与FLOPs，说明关注资源受限场景下的部署可行性。未提及延迟、显存、能耗、FPGA/NPU/TensorRT等具体部署指标。

Datasets and metrics: 两个低维工程预测任务：1) 先进半导体封装热机械可靠性分析（热循环下应力与翘曲，基于FEA数据）；2) Laplace PDE电容场迭代数值求解。评价指标：Recall、Mean Reciprocal Rank (MRR)、参数量、FLOPs。摘要未说明具体数据集规模。

Experiments: 系统对比三种递归Transformer范式在预测性能（Recall、MRR）、参数量、计算复杂度（FLOPs）上的表现，提供资源受限场景下选择递归Transformer架构的设计指南。摘要未给出具体数值结果和消融实验细节。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于工程仿真代理建模领域；2) 摘要未说明模型在目标检测任务上的适用性；3) 未提供具体参数量、FLOPs数值和速度对比；4) 递归权重共享在深层网络中可能面临梯度传播和表达力受限的问题，摘要未讨论；5) 仅验证了低维小数据场景，未验证高维遥感图像数据。

Ideas for my research: 1) 递归权重共享思想可借鉴到遥感目标检测骨干网络的轻量化设计中——在检测网络深层复用浅层权重，减少参数量，适合星上存储受限场景；2) "以计算换参数"的设计哲学值得参考：星上部署往往同时受限于存储和算力，但若存储瓶颈更突出，递归共享可作为剪枝/量化的补充手段；3) hardware-aware评估框架（同时报告参数量、FLOPs、精度）可作为本人论文实验设计的参考模板；4) 可尝试将递归权重共享与知识蒸馏结合，用大模型蒸馏指导递归共享小模型的训练，缓解共享权重带来的表达力下降。

Abstract: Transformer-based surrogate models are increasingly used to replace expensive first-principles simulation in engineering design. But conventional transformer architectures are often over parameterized for the small, low-dimensional datasets typical of engineering design spaces, where large simulation data is expensive to generate. Under these conditions, excess parameter capacity leads to overfitting rather than improved accuracy, while also incurring unnecessary memory and compute overhead. This motivates a shift towards architectures that focus on additional compute rather than additional learnable parameters. This paper presents a hardware-aware evaluation of three recursive transformer paradigms for surrogate thermo-mechanical analysis of advanced packages: a)Tiny Recursive Model, b) our proposed Depth Recursive transformer, c) and a simple recursive transformer. We systematically compare their predictive performance (Recall, Mean Reciprocal Rank), parameter count, computational complexity (FLOPs), providing practical design guidelines for selecting recursive transformer architectures under resource-constrained scenarios. We validate this principle on two low-dimensional engineering prediction tasks: 1) thermo-mechanical reliability analysis of advanced semiconductor packages, where stress and warpage from thermal cycling must be evaluated repeatedly across a design-of-experiments sweep under costly finite element analysis (FEA). 2) Laplace PDE iterative numerical solver for capacitance field. Overall, recursive weight-sharing transformers provide an effective and generalizable trade-off between prediction accuracy, parameter efficiency, and computational cost for small data engineering surrogate modeling.

</details>


### [279] [Regularizing modality contribution drift in multimodal continual learning](https://arxiv.org/abs/2607.27260)
*Zhen Zhang,Jielei Chu,Bin Liu,Tianrui Li*

Main category: cs.LG

TL;DR: 提出CMCDR方法，通过模态贡献漂移正则化来缓解多模态持续学习中的灾难性遗忘问题。

Research relevance: 弱相关（2分）。论文属于多模态持续学习（Multimodal Continual Learning）领域，与遥感/卫星/在轨部署/目标检测/轻量化均无直接关联。它不涉及遥感图像、卫星数据、目标检测任务，也不涉及任何模型压缩或加速方法。仅作为泛机器学习方法，对持续学习场景下的多模态知识保持有参考价值，但与本研究方向的直接相关性很低。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于泛多模态持续学习领域。仅在持续学习知识保持和模态贡献分析方面有间接参考价值，不建议作为核心阅读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态持续学习（MMCL），包括多模态类增量学习（multimodal class-incremental learning）和持续视觉问答（continual visual question answering）。应用场景为通用多模态学习，非遥感场景。传感器类型：摘要未说明（推测为通用视觉+文本模态）。目标类别：摘要未说明。

Model architecture: 摘要未详细说明具体网络架构。核心是提出Modality Contribution Drift (MCD) score来量化模态贡献漂移，并基于此设计CMCDR正则化方法。CMCDR包含两个版本：replay-based版本（使用存储的旧样本作为诊断探针，比较当前模型与冻结旧模型的模态贡献分布）和replay-free版本（使用当前任务样本作为探针，蒸馏冻结模型的旧任务贡献响应）。

Lightweight method: 不涉及轻量化方法。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation，虽然replay-free版本使用了蒸馏思想但目的是知识保持而非模型压缩）、NAS、实时推理或边缘部署等。摘要未说明任何模型压缩或加速手段。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作聚焦于持续学习的算法层面，不涉及在轨或边缘部署可行性。

Datasets and metrics: 摘要未具体说明数据集名称。实验在multimodal class-incremental learning和continual visual question answering两个设置上验证，但未列出具体数据集（如COCO、VQA等）和评价指标（如平均准确率、遗忘率等）。

Experiments: 摘要提到通过理论分析和实证分析解释了现有MMCL方法为何无法可靠缓解模态贡献漂移，并在多模态类增量学习和持续VQA上验证了CMCDR的通用性和有效性。但未给出具体数值结果、对比方法列表或消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关；2. 摘要未提供具体数据集、指标和数值结果；3. 未讨论计算开销，CMCDR需要额外的模态子集干预（modality-subset interventions）和冻结模型推理，可能引入额外计算成本；4. 未说明该方法在单模态遥感场景下的适用性。

Ideas for my research: 尽管与遥感目标检测轻量化方向弱相关，但有以下可借鉴点：1. 持续学习（continual learning）思想可迁移到遥感在轨场景——卫星在轨运行中会持续接收新类别/新场景数据，模型需要在不遗忘旧知识的情况下增量学习，CMCDR的模态贡献正则化思路可启发遥感多模态（光学+SAR+高光谱）在轨增量学习中的模态平衡设计；2. replay-free的蒸馏式知识保持策略与知识蒸馏（knowledge distillation）在轻量化中的teacher-student范式有相通之处，可借鉴其"冻结旧模型作为teacher"的思路用于模型压缩后的知识保持；3. MCD score的量化思路可用于评估轻量化模型在压缩前后多模态贡献结构的变化，作为压缩质量评估的新视角。

Abstract: Multimodal continual learning (MMCL) aims to learn emerging knowledge from multimodal data while preserving knowledge. To mitigate forgetting, current MMCL methods usually focus on cross-modal representation alignment or semantic similarity, but they overlook whether the relative contributions of individual modalities and their interactions remain stable across incremental tasks. We term this decision-level shift Modality Contribution Drift (MCD) and quantify it with the MCD score, which combines contribution-strength and relative-reliance changes under controlled interventions on modality subsets. Theoretical and empirical analyses further explain why current MMCL methods cannot reliably mitigate this drift. To this end, we propose Continual Modality Contribution Drift Regularization (CMCDR), which preserves the modality contribution structure of previously learned tasks. Since MMCL settings differ in whether old exemplars are available, CMCDR includes both replay-based and replay-free versions. The replay-based version uses modality-subset interventions as diagnostic probes on stored old samples, compares their contribution profiles between the current model and a frozen previous model, and constrains changes in old-sample modality-specific and interaction contributions. The replay-free version uses current-task samples as probes and distills the frozen model's old-task contribution responses, thereby regularizing the observed contribution profile without exemplars. Experiments on multimodal class-incremental learning and continual visual question answering validate the generality and effectiveness of CMCDR.

</details>


### [280] [DoTime: A Synthetic Benchmark Generator for Interventional and Counterfactual Time Series](https://arxiv.org/abs/2607.27263)
*Dennis Thumm,Billy Tim Anthony,Ying Chen*

Main category: cs.LG

TL;DR: DoTime 是一个用于生成多变量时序结构因果模型（TSCM）及其干预/反事实数据的开源生成器，附带四个冻结评估套件，并验证了干预训练相比观测训练在方向准确率上的优势。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于因果推断（causal inference）的时序数据生成与评估基准，属于统计机器学习/因果推断领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等任何遥感相关任务，也不涉及 object detection、小目标检测、旋转框检测等目标检测任务，更不涉及 pruning、quantization、knowledge distillation、NAS、实时推理、边缘部署等轻量化/部署方法。

Reading priority: 1 分。论文属于因果推断/时序数据生成领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多变量时序结构因果模型（TSCM）的数据生成与因果推断基准构建。应用场景：healthcare、policy evaluation、climate science 等因果推断场景。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 核心是 TSCM（temporal structural causal models）生成器，支持 continuous-time intervention windows、counterfactual sampling modes、regime-switching SCMs、非平稳动力学（switching SCM parameters）、确定性 ramp 和 sinusoidal 干预剖面。同时包含一个 causal foundation model 参考实现（prior-fitted network, PFN）。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（无剪枝、量化、蒸馏、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。

Datasets and metrics: 数据集：DoTime 生成器产出的四个冻结评估套件，包含 100,000 条轨迹的训练规模快照和八个具名识别结构（identification structures），每个结构都有精确 ground truth，包括配对的干预轨迹和共享噪声的反事实。评价指标：direction-accuracy（方向准确率），对比干预训练与观测训练模型。实验设置：每个 arm 三个训练种子，在 held-out episodes 上进行结构匹配评估。

Experiments: 主要实验验证了一个可证伪声明：interventional training 相比同容量的 observational model 在方向准确率上具有可测量的优势。在每种结构、轨迹长度和种子下，interventional prior-fitted network（PFN）的 gap 均为正。

Limitations: 摘要未说明局限性。但明显与遥感目标检测和轻量化方向无关；此外，摘要未报告生成器的计算开销、可扩展性上限、以及干预训练优势在真实数据上的泛化验证（仅在合成数据上验证）。

Ideas for my research: 借鉴意义非常有限。唯一可能间接相关的点是：论文提出的"干预训练相比观测训练带来方向准确率优势"这一方法论思想，若未来在遥感目标检测中引入因果/干预式数据增强（如对遥感场景中的目标-背景关系进行干预采样），可能有助于提升模型对分布偏移的鲁棒性。但这属于非常间接的迁移，且与本课题的轻量化主线无关。

Abstract: Most benchmarks for causal inference over time series are observational, small, or domain-specific, leaving interventional and counterfactual estimation under-served exactly where it matters most, such as in healthcare, policy evaluation, and climate science. We introduce \textbf{DoTime}, an open, scalable, and theoretically grounded generator of multivariate temporal structural causal models (TSCMs) with interventions, released as the \code{dotime} PyPI package together with four frozen evaluation suites. Beyond existing work, it adds capabilities absent from prior generators: continuous-time intervention \emph{windows}, counterfactual sampling modes with a positivity guard, regime-switching SCMs as a strict generalization of interrupted time series, non-stationary dynamics by construction with switching SCM parameters, and deterministic ramp and sinusoidal intervention profiles that place trends and structural breaks \emph{inside} the evaluation window. Moreover, it demonstrates the suitability of the generator as a prior for a causal foundation model reference implementation. The released suites span a training-scale snapshot of $100{,}000$ trajectories and eight named identification structures, each with exact ground truth: paired interventional trajectories from the same SCM throughout, and shared-noise counterfactuals in the continuous-time suite. We ship reference baseline implementations with an evaluation harness, and pose a falsifiable claim: interventional training buys a measurable direction-accuracy advantage over an observational model of identical capacity. It is tested across three training seeds per arm. Under structure-matched evaluation on held-out episodes, the interventional prior-fitted network's (PFN) gap is positive in every structure, trajectory length, and seed tested.

</details>


### [281] [PlatformBid: An Auto-Bidding Benchmark from a Unified Advertising Platform's Perspective](https://arxiv.org/abs/2607.27265)
*Shengtian Yang,Yewen Li,Peng Jiang,Zhiyi Lyu,Bo An,Peng Jiang,Qingpeng Cai,Lei Feng*

Main category: cs.LG

TL;DR: 提出PlatformBid——首个从统一广告平台视角构建的实时竞价(auto-bidding)基准，并基于flow-matching提出BidFlow方法，在快手线上实验取得+0.68%目标成本改进。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦计算广告领域的实时竞价(auto-bidding)问题，涉及SSP/DSP/Ad Exchange、广告转化率优化、平台收益最大化等，属于计算广告与强化学习/生成式建模范畴，与遥感、卫星、Earth observation、目标检测、模型轻量化均无关联。

Reading priority: 1分。论文主题为计算广告实时竞价，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：实时竞价(auto-bidding)策略优化；应用场景：计算广告平台（社交媒体、电商）；传感器类型：不适用；目标类别：广告竞价/转化优化，非视觉目标。

Model architecture: 摘要未说明具体网络结构细节。提出BidFlow方法，基于flow-matching的生成式策略表示，用于处理动态竞争环境下的竞价决策。此外评估了经典控制方法、基于强化学习(RL-based)的方法和生成式(generative)方法。

Lightweight method: 摘要未涉及任何模型压缩或加速方法。未提及pruning、quantization、knowledge distillation、NAS、轻量网络、边缘部署等轻量化技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署信息。仅在快手(Kuaishou)平台进行了线上实验验证，属于广告平台部署而非星上/边缘部署。

Datasets and metrics: 数据集：PlatformBid基准（自建，覆盖三种竞争场景：同质竞争、异质竞争、促销竞争）；评价指标：目标成本(target cost)，线上实验显示+0.68%改进；实验平台：快手(Kuaishou)线上环境。

Experiments: 在PlatformBid基准上系统评估了多种现有auto-bidding方法（经典控制方法、RL-based方法、生成式方法），并对比了提出的BidFlow方法。线上实验在快手平台验证了offline-online一致性，BidFlow在目标成本上取得+0.68%改进。

Limitations: 与本研究方向完全无关。摘要未提供任何遥感、目标检测、轻量化相关信息。作为计算广告领域工作，其benchmark设计思路和flow-matching方法对遥感目标检测轻量化课题无可借鉴之处。

Ideas for my research: 基本无可借鉴点。唯一可能的间接启发是：其"从统一平台视角构建benchmark"的思路，类比到遥感领域可理解为"从星上部署视角构建统一的轻量化目标检测评估框架"，但这属于非常间接的方法论类比，实际参考价值有限。

Abstract: Real-time bidding is central to computational advertising, comprising three elements: Supply Side Platform (SSP) selling ad impressions, Demand Side Platform (DSP) bidding for advertisers, and Ad Exchange conducting auctions between them. Traditional auto-bidding algorithms focus solely on the DSP side, maximizing advertiser conversions by adjusting bids against competitors. However, current big ad platforms, such as social media and e-commerce companies, now integrate SSP, DSP, and Ad Exchange functions internally. From such ad platforms' perspective, the goal of the auto-bidding algorithms is not only to maximize the advertisers' conversions, but also the total revenue of the platform. Given the lack of platform-centric evaluation frameworks and the pressing need to advance auto-bidding research, we propose PlatformBid - the first comprehensive benchmark designed from a unified ad platform's perspective. To accurately reflect the real-world auto-bidding scenarios, we define three representative settings: (1) homogeneous competition with identical algorithms across advertisers, (2) heterogeneous competition with diverse algorithmic strategies, and (3) promotional competition where some advertisers surge budgets for boosting sales during promotional events like Black Friday. We systematically evaluate a broad spectrum of existing auto-bidding methods across these settings, encompassing classical control methods, RL-based methods, and recent generative methods. Besides these methods, we further propose a novel auto-bidding method based on flow-matching, termed BidFlow, which leverages the flow-matching method's expressive policy representation to effectively handle dynamic competitive environments. Online experiments on Kuaishou further show a +0.68\% improvement in target cost, providing deployment evidence for the offline-online consistency of PlatformBid.

</details>


### [282] [Beyond KV Reconstruction: Functional Reconstruction for MLA Draft Models in Speculative Decoding](https://arxiv.org/abs/2607.27269)
*Weiye Shi,Fanxu Meng,Muhan Zhang*

Main category: cs.LG

TL;DR: 提出一种针对大语言模型（LLM）的MHA/GQA到MLA注意力机制转换后的功能重建方法（Functional Reconstruction），通过优化转换后的MLA模块以复现原始注意力的输出，从而提升投机解码（speculative decoding）的草稿token接受率。

Research relevance: 弱相关（2分）。该论文聚焦于大语言模型（LLM）的注意力机制转换与投机解码加速，属于泛机器学习/泛AI推理优化方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然涉及模型推理效率优化，但研究对象是LLM的KV cache与注意力机制，而非遥感目标检测模型。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，仅"functional reconstruction"思想在方法学层面有一定借鉴价值，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM长上下文推理加速（long-context inference acceleration）；应用场景：大语言模型推理服务；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 涉及MHA（Multi-Head Attention）、GQA（Grouped-Query Attention）、MLA（Multi-head Latent Attention）三种注意力机制；核心方法是对MHA/GQA转换到MLA后的注意力模块进行功能重建（functional reconstruction），通过优化转换后的MLA模块使其在calibration hidden states上复现原始MHA/GQA经output projection后的响应。

Lightweight method: 涉及模型压缩/加速相关技术：MLA通过紧凑的latent states替代不断增长的KV cache以减少解码内存流量（cache efficiency）；投机解码（speculative decoding）作为互补加速手段。但未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等传统轻量化方法。

Onboard deployability: 摘要未说明。论文面向LLM推理服务（HF和vLLM后端），未涉及星上/边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、显存、能耗等具体部署指标。

Datasets and metrics: 评估配置：192种model-converter-backend-method-task组合，涵盖4对Llama/Qwen draft-target模型对、TransMLA和MHA2MLA两种转换方法、HF和vLLM两个推理后端、4个200-prompt任务。评价指标：草稿token接受率（draft-token acceptance），报告容差为0.5个百分点。

Experiments: 在192种配置中，Functional Reconstruction在64个匹配任务单元中有37个显著提升接受率，26个基本不变，1个显著下降。代码与评估工件开源在GitHub。

Limitations: 1. 研究对象为LLM注意力机制，与遥感目标检测完全无关；2. 摘要未说明方法在遥感/视觉任务上的可迁移性；3. 未报告计算开销、参数量、FLOPs等部署关键指标；4. 仅针对LLM推理场景，未涉及边缘/星上部署约束。

Ideas for my research: 1. 方法学上的借鉴：其"functional reconstruction"思想——即转换/压缩后通过calibration数据重建原模型功能——可类比迁移到遥感目标检测模型的轻量化场景，例如在知识蒸馏或模型压缩后，用遥感数据对压缩模型进行功能对齐微调，以恢复检测精度。2. 其"converter-agnostic post-conversion procedure"思路提示：轻量化转换（如量化、剪枝）后应有一个不依赖原始转换器的后处理校准步骤。3. 投机解码中"draft-target agreement"的度量思想，可类比为轻量化模型与原始模型在遥感目标上的输出一致性评估。

Abstract: Multi-head latent attention (MLA) is increasingly important for long-context LLM inference because compact latent states replace the growing key-value (KV) cache and reduce decoding memory traffic. Yet most capable open checkpoints use multi-head or grouped-query attention (MHA/GQA), so conversion is needed to obtain MLA's cache efficiency without retraining from scratch. Speculative decoding offers complementary acceleration, but its speedup depends on agreement between draft proposals and target verification. We find that direct MHA/GQA-to-MLA conversion can sharply reduce this agreement: low-rank factorization and RoPE handling introduce attention-function errors that may be tolerable for standalone generation but substantially lower draft-token acceptance. We therefore formulate MLA draft construction as functional reconstruction rather than cache compression. Our end-to-end (E2E) method optimizes each converted MLA attention module to reproduce the post-output-projection response of its original MHA/GQA counterpart on calibration hidden states. This converter-agnostic post-conversion procedure preserves the converted cache and inference graph and requires neither verifier logits nor verifier supervision. We evaluate 192 model-converter-backend-method-task configurations spanning four Llama/Qwen draft-target pairs, TransMLA and MHA2MLA, HF and vLLM, and four 200-prompt tasks. With a 0.5-percentage-point reporting tolerance, Functional Reconstruction materially improves acceptance in 37 of 64 matched task cells, leaves 26 practically unchanged, and materially decreases one. Code and evaluation artifacts are available at https://github.com/swyhahaha/FunctionalMLA.

</details>


### [283] [RLPF: Reinforcement Learning from Performance Feedback for Code Generation](https://arxiv.org/abs/2607.27271)
*Huihao Jing,Haozhe Cui,Wenbin Hu,Shaojin Chen,Haochen Shi,Changxuan Fan,Yuxuan Liu,Hanyu Yang,Sirui Zhang,Ziyi Chen,Haoran Li,Yangqiu Song*

Main category: cs.LG

TL;DR: 提出RLPF（reinforcement learning from performance feedback），一种基于执行反馈的分阶段奖励机制，用于训练代码智能体生成更快且正确的程序，将PerfCodeBench上正确可运行率从11.1%提升到54.6%，相对效率从8.1%提升到38.6%。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦于代码生成智能体（code agents）的训练方法，通过执行反馈优化程序运行效率，属于代码生成/程序合成领域，不涉及遥感、卫星、目标检测或模型轻量化中的任何一项。相关性评分为1分。

Reading priority: 1分。该论文属于代码生成/程序合成领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。除非对"以效率为优化目标"的强化学习奖励设计有跨领域兴趣，否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：代码生成智能体训练（code agent training），通过强化学习优化程序运行效率。应用场景：系统代码（systems code）生成与优化。不涉及遥感传感器、卫星平台或目标检测任务。

Model architecture: 基于Qwen3-32B大语言模型进行微调（fine-tuning），采用RLPF强化学习框架。摘要未说明具体的网络结构细节或代码生成模型架构。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩或轻量化方法。论文关注的是"程序运行效率"（program runtime efficiency）的优化，而非神经网络模型的轻量化。这里的"效率"指生成代码的执行速度，与模型部署的FLOPs/参数量/延迟优化属于不同概念。

Onboard deployability: 摘要未说明。论文不涉及星上部署、边缘部署、FPGA/NPU/TensorRT等部署相关内容。训练模型为Qwen3-32B，属于大规模语言模型，与在轨遥感目标检测的轻量化部署需求不匹配。

Datasets and metrics: 数据集：PerfCodeBench（主评测）、EffiBench-X（迁移评测）。评价指标：correct-and-runnable solutions（正确可运行率）、relative efficiency（相对效率）。摘要未说明具体数据规模、任务数量或评测细节。

Experiments: 主要实验：在PerfCodeBench上，RLPF将正确可运行率从11.1%提升到54.6%，相对效率从8.1%提升到38.6%。训练后的模型与更强的开源系统竞争。优化行为可迁移到EffiBench-X。消融/对比实验：模型生成参考（model-generated references）提供有用但较弱的监督；完整复合奖励（composite reward）比仅正确性（correctness-only）或仅运行时间（runtime-only）基线更可靠。

Limitations: 摘要未说明。但可推断：1）该工作聚焦代码生成领域，与遥感目标检测无直接关联；2）Qwen3-32B规模较大，训练成本高；3）运行时间作为奖励信号的脆弱性（fragile reward）问题在跨任务泛化上仍有挑战；4）迁移到EffiBench-X仅"适度"（modestly），泛化能力有限。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但可借鉴的间接思路包括：1）"分阶段奖励"（staged reward）思想——在模型压缩/蒸馏中，可设计分阶段优化目标（如先保证精度不降，再优化FLOPs/延迟），避免多目标联合优化的不稳定；2）"相对改进"（relative improvement）的评估方式——在轻量化模型评估中，可参考用相对基线（如相对原始模型的精度保持率和加速比）而非绝对指标来度量压缩效果；3）运行效率作为优化目标而非仅评测指标的理念，可类比为在轨部署中将延迟/能耗作为训练目标的一部分。但这些均为间接启发，不构成直接可复现的方法。

Abstract: Code models are increasingly trained with execution feedback, but most training signals still stop at correctness. This leaves an important gap for systems code: two programs can pass the same tests while differing greatly in runtime. We study how to train code agents to prefer faster correct implementations, rather than treating efficiency only as an evaluation metric. The key difficulty is that runtime is a fragile reward. It is meaningful only after a program is correct, varies across tasks, and gives little guidance when most sampled programs fail to compile or run. We propose \textbf{RLPF}, reinforcement learning from performance feedback, which turns execution outcomes into a staged reward. Failed programs are ordered by execution progress, while correct programs are ranked by their relative improvement from the baseline toward the expert reference. This gives useful feedback before correctness and performance-sensitive feedback after correctness. Fine-tuning Qwen3-32B with RLPF on PerfCodeBench raises correct-and-runnable solutions from $11.1\%$ to $54.6\%$ and improves relative efficiency from $8.1\%$ to $38.6\%$. The trained model becomes competitive with stronger open-weight systems, and its optimization behavior transfers modestly to EffiBench-X. Additional studies show that model-generated references provide useful but weaker supervision, and that the full composite reward is more reliable than correctness-only or runtime-only baselines. These results suggest that code agents can be trained not only to pass tests, but also to optimize the programs they write.

</details>


### [284] [SDO: Structure-Aware Data Organization for Efficient LLM Post-Training](https://arxiv.org/abs/2607.27273)
*Jinliang Gao,Ning Yang,Hai Wang,Baili Xiao,Pin Lyu*

Main category: cs.LG

TL;DR: 提出SDO（Structure-Aware Data Organization），一种面向大语言模型后训练（SFT/DPO/GRPO）的基于表征空间结构的数据组织框架，通过KNN邻域遍历构建局部一致的小批次，并利用曝光均衡调度减少过曝光样本的采样概率，从而加速收敛并提升各题型准确率的均衡性。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/大语言模型训练优化方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可借鉴的点在于其"数据组织/曝光均衡"思想可能对遥感目标检测训练数据采样策略有间接启发，但整体与本研究方向基本无关。

Reading priority: 1分。该论文聚焦LLM后训练数据组织，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅数据采样均衡思想有极有限的间接启发，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型后训练（post-training）数据组织优化，涵盖SFT、DPO、GRPO。应用场景：LLM训练效率提升。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: SDO框架包含两个核心模块：(1) locality-aware batching——通过KNN邻域遍历在冻结的外部embedding上构建语义连贯的mini-batch；(2) exposure-balanced scheduling——记录每个样本的参与次数，降低过曝光样本的采样概率以保持长期覆盖。整体为plug-and-play框架，逐epoch运行在冻结embedding上，无需模型warm-up训练开销。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。论文的"效率改进"聚焦于训练数据组织层面（减少冗余更新、加速收敛），而非模型推理侧的轻量化。不涉及FLOPs、参数量、延迟等部署指标。

Onboard deployability: 摘要未说明。论文完全不涉及星上/边缘部署、推理延迟、能耗、实时性等部署相关信息，与在轨遥感目标检测的部署需求无直接关联。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标涉及收敛速度（convergence acceleration）、梯度一致性（coherent gradients）、各题型准确率均衡性（balanced accuracy across question types）。实验覆盖SFT、DPO、GRPO三种训练范式。

Experiments: 摘要报告SDO在SFT、DPO、GRPO上均加速收敛，最大增益出现在训练早期到中期阶段，产生更一致的梯度，并在不永久排除训练样本的前提下实现各题型准确率更均衡。但未给出具体数值对比、基线方法细节或消融实验。

Limitations: 摘要未说明局限性。主要缺口包括：未提供具体加速倍数、未说明KNN邻域遍历的计算开销、未讨论embedding冻结假设在动态表征场景下的适用性、未涉及遥感或视觉任务验证。此外，该方法依赖外部embedding，其质量对效果的影响未讨论。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化无直接关系，但其"曝光均衡调度"思想可迁移到遥感目标检测训练中：(1) 遥感小目标检测常面临类别不平衡和难易样本不均衡问题，可借鉴曝光均衡机制动态调整难样本/易样本的采样概率，避免模型对易样本过拟合而忽略难小目标；(2) locality-aware batching的KNN邻域思想可用于遥感图像中相似场景（如不同时相、不同传感器的同区域影像）的批次组织，提升训练梯度一致性；(3) 该框架"plug-and-play、无需warm-up"的设计理念对遥感检测模型的训练效率优化有参考价值。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Post-training of large language models is expensive, and existing efficiency improvements mainly focus on selecting informative samples or designing training schedules. However, data organization itself is usually treated as a static preprocessing step: embedding-based grouping methods construct fixed partitions before training and cannot adapt to the evolving sample exposure during optimization. As a result, all samples receive similar exposure despite their different optimization needs, leading to redundant updates for some samples while leaving others under-optimized. To address this problem, we propose SDO (Structure-Aware Data Organization), a plug-and-play data organization framework with an exposure-driven feedback mechanism that organizes mini-batch composition and sample exposure according to representation-space structure. SDO operates epoch by epoch on frozen external embeddings, avoiding model warm-up training overhead: within each epoch, locality-aware batching forms coherent mini-batches via KNN neighborhood traversal; across epochs, exposure-balanced scheduling records per-sample participation and reduces the sampling probability of over-exposed samples to preserve long-term coverage. Across SFT, DPO, and GRPO, SDO accelerates convergence, with the largest gains observed in the early-to-mid phase, producing more coherent gradients and more balanced accuracy across question types without permanently excluding training samples.

</details>


### [285] [Rethinking EEG-Based Disease Diagnosis: Decoupling Instance Representation Learning from Subject-Level Supervision](https://arxiv.org/abs/2607.27274)
*Zhiyuan Ma,Zeyuan Li,Zhiyi Lu,Jiacheng Hao,Youlang Du,Zhen Jiang,Xinche Zhang,Yuhao Sun,Sen Song*

Main category: cs.LG

TL;DR: 提出BridgeMIL，一种两阶段多实例学习框架，用于脑电（EEG）疾病诊断，通过解耦实例表示学习与受试者级监督来避免继承标签问题。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于脑电（EEG）信号疾病诊断，属于生物医学信号处理与多实例学习（MIL）领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。轻量化方面仅涉及表示学习中的正则化与特征保留，不涉及剪枝、量化、知识蒸馏、NAS或边缘部署。相关性评分：1分。

Reading priority: 1分。论文主题为EEG疾病诊断与多实例学习，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在两阶段训练和正则化表示学习的方法论层面有极有限的间接启发，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：脑电（EEG）疾病诊断（subject-level classification）。应用场景：生物医学信号分析，非遥感场景。传感器类型：EEG（脑电图）。目标类别：疾病诊断类别（具体疾病类型摘要未说明）。

Model architecture: BridgeMIL为两阶段框架：Stage 1通过对齐时间邻近窗口和独立采样的受试者内子袋（within-subject sub-bags）来预训练编码器（encoder），并使用方差和协方差正则化防止表示坍缩并减少冗余（无需负样本对）；Stage 2将编码器迁移到基于注意力机制的多实例学习（attention-based MIL）聚合器，仅对受试者预测施加监督，并通过特征保留（feature retention）限制表示漂移。实验覆盖五个代表性骨干网络（backbones）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计或模型压缩。其"轻量化"相关概念仅体现在表示学习的正则化策略（variance/covariance regularization）和特征保留机制，属于训练策略而非模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时推理性能，也未涉及星上/边缘部署可行性。

Datasets and metrics: 使用三个EEG疾病数据集和五个代表性骨干网络，共15个dataset-backbone组合。评价指标为mean accuracy（平均准确率）。BridgeMIL在14/15个设置中取得最高平均准确率，总体平均准确率76.57%，比最强基线高4.28个百分点。

Experiments: 主要实验：在三个EEG数据集×五个骨干网络上对比BridgeMIL与基线方法，BridgeMIL在14/15设置中取得最高准确率。进一步分析包括：1) 继承标签可靠性在不同实例间存在显著差异；2) 性能对受试者稀缺性（subject scarcity）比对实例稀缺性（instance scarcity）更敏感；3) 表示空间更具结构化，受试者级聚类更清晰，诊断类别分离度更好。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向完全无关；2) 摘要未说明模型复杂度、推理速度等部署指标；3) 摘要未说明具体EEG疾病类型和数据集名称；4) 未讨论模型在资源受限设备上的可行性；5) 未涉及实时推理或边缘部署场景。

Ideas for my research: 尽管论文与遥感在轨目标检测无直接关系，但其两阶段解耦训练思想（decouple representation learning from task supervision）和"无负样本对的正则化表示学习"（variance/covariance regularization without negative pairs）在方法论层面有一定借鉴潜力：1) 在遥感小样本场景下，可借鉴其"先无监督预训练编码器、再迁移到任务头"的两阶段范式，缓解标注稀缺问题；2) 其"特征保留（feature retention）限制表示漂移"的思想可用于知识蒸馏或模型微调时防止灾难性遗忘；3) 其"对稀缺性敏感度分析"的实验设计思路可迁移到遥感小目标/少样本检测的消融分析中。但整体借鉴价值有限，属于间接方法论启发。

Abstract: EEG-based disease diagnosis requires one prediction per subject, yet common pipelines segment recordings into short instances, inherit the subject label for every instance, and train instance-level classifiers. This assumes that all instances provide equally reliable diagnostic evidence. Multiple instance learning (MIL) avoids inherited labels by treating each subject as a bag. However, EEG datasets contain far fewer subjects than instances, which can limit the quality of the representations learned by end-to-end MIL. We propose BridgeMIL, a two-stage framework that decouples instance representation learning from subject-level supervision. Stage 1 pretrains the encoder without inherited instance labels by aligning temporally nearby windows and independently sampled within-subject sub-bags. Variance and covariance regularization prevent collapse and reduce redundancy without negative pairs. Stage 2 transfers the encoder to an attention-based MIL aggregator, applies supervision only to subject predictions, and limits representation drift through feature retention. Across three EEG disease datasets and five representative backbones, BridgeMIL attains the highest mean accuracy in 14 of 15 dataset-backbone settings and an overall mean accuracy of 76.57%, 4.28 percentage points higher than the strongest baseline. Further analyses reveal substantial variation in inherited-label reliability across instances, greater performance sensitivity to subject scarcity than to instance scarcity, and a more structured representation space with distinct subject-wise clusters and improved separation between diagnostic classes. Together, these findings underscore the importance of aligning supervision with the subject-level prediction objective while learning from abundant EEG instances without assigning disease labels to individual instances.

</details>


### [286] [Flat Score, Amplified Failures: How the Error Budget Masks Damage in Quantized LLM Agents](https://arxiv.org/abs/2607.27275)
*Jiwon Jang,Kisu Yang,Heuiseok Lim,Hyunwoo Park*

Main category: cs.LG

TL;DR: 本文研究4-bit权重量化对多轮工具调用智能体（tool-calling agents）的影响，发现量化在标准指标上看似无损，但实际上会放大模型已有的失败（如工具名幻觉），只是被基准的误差预算掩盖。

Research relevance: 弱相关（2分）。本文属于泛机器学习/大语言模型（LLM）领域的后训练量化（post-training quantization）研究，与遥感、卫星、目标检测均无直接关系。唯一可借鉴的是其关于量化对模型行为影响的深入分析方法论，以及"标准指标掩盖真实损伤"的洞察，这对遥感模型轻量化评估有方法论启示，但场景完全不同。

Reading priority: 2分。与遥感卫星在轨目标检测轻量化方向弱相关，但其量化评估方法论（标准指标掩盖损伤、细粒度诊断）对轻量化评估设计有间接借鉴价值，可作为方法论参考而非核心文献。

<details>
  <summary>Details</summary>

Task and scene: 任务：多轮工具调用智能体（multi-turn tool-calling agents）的量化鲁棒性评估。场景：电信（telecom）和零售（retail）两个领域的工具调用。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 摘要涉及两个开源模型家族（open-weight model families），包含dense和MoE（Mixture-of-Experts）两种变体。具体模型名称摘要未说明。

Lightweight method: 后训练量化（Post-training Quantization, PTQ），将权重量化到16-bit、8-bit和4-bit三种精度。核心关注4-bit权重量化是否真正无损。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅涉及权重位宽（16/8/4-bit）这一量化维度，未涉及推理速度或资源占用数据。

Datasets and metrics: 数据集：τ²-bench（多轮工具调用基准），覆盖两个领域（电信、零售），8个cell，每个456个episode。指标：标准任务奖励（task reward）、每通道错误率（per-channel error rate）、缩小误差预算下的成功率、等价性检验（equivalence testing）、秩相关（rank correlation）。

Experiments: 主要发现：(1) 在标准指标上，4-bit量化看似无损，无cell的分数变化通过多重比较校正，最大过程损伤的cell中变化被等价性检验限制在±7.5分内；(2) 量化将模型在满精度下已有的失败（电信中的工具名幻觉、零售实体错误）放大最多2.5倍（每任务+17.6分），但几乎不产生新失败；(3) 失败集在各精度下相同（秩相关≥0.94，仅0.18%新事件）；(4) 分数持平是因为基准的10个错误预算吸收了额外失败，将预算缩至2个错误后重新暴露17分的差距；(5) 针对性的错误修复提示（error-repair prompt）能精确移除量化损伤。

Limitations: 摘要未说明：具体模型架构细节、参数量、推理速度、部署资源消耗。研究仅针对LLM工具调用场景，不涉及遥感图像、目标检测或任何视觉任务。量化仅覆盖权重（weights），未涉及激活量化（activation quantization）。

Ideas for my research: (1) 方法论借鉴：在遥感目标检测模型轻量化评估中，不能只看mAP等标准指标，应像本文一样引入"每通道错误率"和"缩小误差预算"等诊断手段，检测量化/剪枝是否放大了模型已有的失败模式（如小目标漏检、旋转框角度误差）；(2) 本文揭示"标准指标掩盖真实损伤"的洞察可迁移到遥感场景——例如量化后mAP看似不变，但可能放大了特定类别（如舰船、飞机）或特定尺度（小目标）的失败，需要细粒度评估；(3) 可借鉴"失败集一致性"分析（rank correlation）来验证轻量化前后模型失败模式是否一致；(4) 误差修复提示（error-repair prompt）的思路可类比为轻量化后的针对性补偿模块或微调策略。

Abstract: Post-training quantization to 4-bit weights is widely reported to be nearly lossless. We test this claim for multi-turn, tool-calling agents, where it now matters most. On $τ^2$-bench, across two open-weight model families in dense and MoE variants and two domains (eight cells, 456 episodes each, at 16-, 8-, and 4-bit weights), quantization indeed looks free on the standard metric. No cell shows a score change that survives multiple-comparison correction, and in the cell that carries the largest process damage, equivalence testing bounds the change within $\pm$7.5 points. The process tells a different story. Quantization amplifies the failure the model already exhibits at full precision (tool-name hallucination in telecom, with the same directional trend in retail entity errors) by up to 2.5$\times$ in volume (+17.6 points per task), while creating essentially no new failures. The failure set is the same at every precision (rank correlation $\geq$ 0.94, 0.18% novel events). The score stays flat because the benchmark's ten-error budget absorbs the extra failures. Shrinking the budget to two errors re-exposes a score gap of 17 points, and it does so only in the one cell where quantization added error volume, exactly as the masking account predicts. A targeted error-repair prompt, run for five telecom models at every precision, removes the damage exactly and only where it lives. Both diagnostics, the per-channel error rate and success under a shrinking budget, come from logs benchmarks already collect; we suggest reporting them alongside task reward.

</details>


### [287] [The Kinetics of Training: A Driven-Nucleation Rate Law for Emergence, Plasticity Loss, and Circuit Control in Language Models](https://arxiv.org/abs/2607.27281)
*Lei Dong*

Main category: cs.LG

TL;DR: 本文研究语言模型（LM）中"能力涌现"的机制，提出能力形成受"无部分信用联合对齐"（no-partial-credit joint alignment）这一速率限制步骤控制，并通过电路消融、前向预测、反向延迟等实验证明该机制，属于可解释性/机制性机器学习研究。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文研究对象是语言模型（language model）中的能力涌现机制，涉及transformer电路、注意力头消融、SGD噪声等，属于可解释性机器学习/机制性研究，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等任何遥感场景，也不涉及object detection、目标检测、轻量化、剪枝、量化、知识蒸馏、NAS、边缘/星上部署等任何相关技术。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于语言模型可解释性/机制性研究。除非对"能力涌现"的动力学机制有跨领域兴趣，否则不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型能力涌现机制研究（mechanistic interpretability / capability formation）。应用场景：大语言模型训练动力学分析。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 研究对象为transformer架构语言模型，具体包括Pythia系列（7种能力、3种规模，至1.4B参数）以及一个受控的gated-attention模型。涉及电路（circuit）、注意力头（attention head）、query-key切片与value切片等组件分析。

Lightweight method: 摘要未说明。本文不涉及任何模型压缩或加速方法（无剪枝、量化、蒸馏、NAS、轻量网络设计等）。文中提到的"re-initializing query-key slices"是用于恢复可学习性的机制性干预手段，而非轻量化方法。

Onboard deployability: 摘要未说明。本文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。研究对象为1.4B规模的语言模型，与在轨遥感目标检测的嵌入式/星载部署场景无直接关联。

Datasets and metrics: 数据集：Pythia模型（7种能力、3种规模）及受控gated-attention模型。评价指标：能力保留比例（capability retention，消融后中位数17%）、预测误差（5% median error）、成功率（10/10、0/12、6/6、0/6等）、p值（p=2e-10）、等待时间比（1.19-1.37）。这些均为机制性/统计性指标，非目标检测指标（无mAP、IoU、FPS等）。

Experiments: 主要实验包括：1）五部分电路缺三与三部分电路缺三的等待时间对比（1.19-1.37），证明等待时间取决于缺失部件数而非电路规模；2）Pythia上消融一个部件后能力保留中位数17%（32/32判别单元），而随机非部件头保留100%；3）前向预测：能力在浓度阈值以上点火（10/10），以下不点火（0/12），且可从前驱预测到达时间（5%中位误差）；4）反向延迟：延迟学习被扣留能力随等待增长，超过临界步后永不点火，但validation loss平滑下降（标准监控失效）；5）重初始化query-key切片恢复可学习性（6/6），value切片无效（0/6）；6）受控gated-attention模型证明机制；7）SGD噪声未通过fluctuation-dissipation检验，安装后按计划退火、熔化、固定电路。

Limitations: 摘要未说明明确局限性，但可推断：1）研究对象限于transformer语言模型（至1.4B），未涉及视觉/遥感模型；2）机制验证主要在语言模型电路上，泛化到其他架构（如CNN、检测头）未说明；3）与遥感目标检测、轻量化部署完全无关，无任何可迁移的工程方法；4）"能力涌现"概念与遥感检测模型训练中的收敛/性能提升可能有隐喻层面的类比，但缺乏直接技术关联。

Ideas for my research: 尽管本文与遥感在轨目标检测轻量化方向基本无关，但存在一些隐喻层面的启发：1）"能力形成受速率限制步骤控制"的思想，可类比思考轻量化检测模型在训练中性能"涌现"的临界条件（如数据量、训练步数、蒸馏温度等达到阈值才出现能力），对设计知识蒸馏或训练策略有启发；2）"标准监控（validation loss）对能力形成盲视"的发现，提示在遥感检测模型训练中仅看loss可能不足以判断小目标检测能力是否真正形成，需关注任务级指标；3）"重初始化部分切片恢复可学习性"的干预思想，可类比在蒸馏/微调中针对特定层进行重置以恢复小目标检测能力。但这些均为间接、隐喻层面的借鉴，无直接技术方法可复现。

Abstract: A capability appears in a language model when the last parts of its circuit align in one stochastic attempt, and getting all but one right is worth nothing. We show this no-partial-credit joint alignment is the rate-limiting step of capability formation. Two fingerprints: in a shortcut-free apparatus a five-part circuit missing three waits as long as a three-part circuit missing three (1.19-1.37), so the wait counts missing parts, not size; and on Pythia across seven capabilities and three scales, ablating one part leaves a median 17% of the capability in 32 of 32 discriminating cells, where partial credit predicts 50-83% (p = 2e-10), while a random non-part head leaves 100%.
  One rare event whose barrier grows with missing parts yields a rate equation -- sites x attempts x drive x exp(-beta*K), minus destruction -- read three ways, each preregistered with frozen constants. Forward: a capability flat at baseline ignites at a step of our choosing once the mix passes a concentration floor (10/10 above, 0/12 below), and while still flat its arrival is datable from its precursor to 5% median error on six held-out models. Backward: the delay to learn a withheld capability grows with waiting until, past a critical step, it never ignites -- yet validation loss falls smoothly throughout, so standard monitors are blind to it. We locate the damage (heads commit to the base data) and isolate the cure: re-initializing only the query-key slices restores learnability (6/6) while the value slices do nothing (0/6). We prove the mechanism in a controlled gated-attention model: occupation forces a deadline whose consequences need no mixing assumption. Completed: SGD's noise fails the fluctuation-dissipation test, so we install one and anneal, melt and pin circuits on schedule. Scope: conjunction circuits in transformers to 1.4B.

</details>


### [288] [Benchmarking the Residual: What Long-Horizon Evaluations Add Beyond Matched Short-Task Performance](https://arxiv.org/abs/2607.27283)
*Chao Peng,Zhiheng Lyu,Peijie Dong,Hande Dong,Qiang Lin*

Main category: cs.LG

TL;DR: 这是一篇关于长时程（long-horizon）智能体任务失败原因分析的立场论文（position paper），提出用"horizon residual"指标来区分任务失败是由普通错误累积还是由轨迹诱导退化（trajectory-induced degradation）导致。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文讨论的是通用AI智能体（agent）在长时程任务中的失败归因问题，属于泛机器学习/泛AI领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于通用AI agent评测方法论论文，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长时程智能体（long-horizon agent）任务失败归因分析；应用场景：通用AI agent部署与评测；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型结构。论文提出的是评测方法论框架（horizon residual），而非具体网络架构。

Lightweight method: 摘要未说明。不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集。提出新指标：horizon residual（实际完整任务成功率与基于短阶段基线预测的对数比值），用于量化长时程任务失败中轨迹诱导退化的贡献。

Experiments: 摘要未说明具体实验结果。作为position paper，主要提出评测框架和论证逻辑，未给出实验数据。

Limitations: 摘要未说明局限性。但作为立场论文，其提出的horizon residual框架本身需要大量实验验证；且该框架针对的是通用agent任务，与遥感目标检测场景无直接关联。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：论文强调"长任务失败不等于单阶段错误累积"的归因思想，类比到在轨遥感目标检测中，可思考"长时序遥感视频流中检测性能退化"是否源于单帧检测误差累积还是环境/上下文变化，但这属于非常间接的方法论类比，不涉及具体技术路线。

Abstract: Long-horizon benchmarks often show that agents fail more as tasks become longer. This observation is useful for deployment, but it does not by itself explain why failure occurs. More stages create more opportunities for ordinary errors to compound; longer tasks may also contain harder individual decisions or become harder as conversation history, tool outputs, and environment changes accumulate. We use trajectory-induced degradation to mean this last possibility: earlier execution makes later work harder. When the harmful accumulation is specifically the text visible to the model, it is often called context rot. In this position paper, we argue that to claim a "long-horizon failure", benchmarks must compare actual full-task success against a baseline prediction built from short, individual stages. We call the log-ratio between this prediction and actual success the horizon residual. The comparison must use the same agent configuration and specify in advance how stages, checkpoints, information, and budgets will be chosen. The residual shows that the full rollout differs from the chosen baseline; targeted experiments are still needed to explain why.

</details>


### [289] [TIER-MoE: Trust-Informed Expert Routing via Conditional Modality Risk for Multimodal Fusion in Biomedical Classification](https://arxiv.org/abs/2607.27289)
*Yu Chang,Anzhe Cheng,Chenwei Wu,Zhuoran Wang,Jiahao Chen,Tamoghna Chattopadhyay,Sophia I. Thomopoulos,Paul M. Thompson,Liyue Shen,Paul Bogdan*

Main category: cs.LG

TL;DR: 提出TIER-MoE，一种基于风险引导的子空间混合专家模型，通过样本特定的模态可靠性估计（从out-of-fold预测中学习）实现稀疏模态-专家路由，用于多模态融合中的不可靠/冗余模态识别，在四个生物医学多模态数据集上取得SOTA性能。

Research relevance: 与本研究方向基本无关（1分）。该论文聚焦多模态融合中的模态可靠性建模与混合专家路由，应用场景为生物医学（阿尔茨海默病、皮肤病变、视网膜分类），完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测任务，更不涉及任何轻量化/部署方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于生物医学多模态融合领域。除非对MoE稀疏路由或模态可靠性建模有特殊兴趣，否则不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态分类（multimodal classification）；应用场景：生物医学诊断（Alzheimer's disease status、skin-lesion malignancy、retinal classification）；传感器类型：生物医学影像/临床数据（摘要未说明具体模态类型）；目标类别：疾病状态分类，非目标检测。

Model architecture: TIER-MoE：risk-guided subspace mixture-of-experts模型。核心组件包括：(1) 样本特定模态可靠性估计——将模态可靠性定义为该模态单模态预测器预期产生的prediction loss，该risk从out-of-fold predictions学习；(2) 专家特定子空间兼容性（expert-specific subspace compatibility）用于稀疏模态-专家路由；(3) 始终激活的共享路径（always-active shared path）以保留多模态互补性。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署等任何模型压缩或加速方法。虽然MoE结构本身具有稀疏激活特性，但论文未从轻量化/推理效率角度进行讨论。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。MoE的稀疏路由机制理论上可降低推理计算量，但论文未从部署角度评估。

Datasets and metrics: 数据集：四个公开多模态生物医学数据集，涵盖Alzheimer's disease status、skin-lesion malignancy、retinal classification；评价指标：Macro-F1、Brier score（概率校准指标）；另有外部队列的zero-shot泛化评估。

Experiments: 与state-of-the-art多模态融合方法对比，在预测性能和概率校准（probability calibration）上均取得一致性提升，Macro-F1和Brier score均有改进，并在外部队列上展现强zero-shot泛化能力。摘要未提供具体数值和消融实验细节。

Limitations: (1) 应用领域为生物医学，与遥感/卫星场景完全无关；(2) 未涉及目标检测任务；(3) 未涉及任何模型轻量化或部署优化；(4) 摘要未说明模型复杂度、推理效率；(5) 模态可靠性估计依赖out-of-fold predictions，训练流程可能较复杂，在资源受限的星上场景中适用性存疑。

Ideas for my research: 尽管整体弱相关，但以下思路可迁移借鉴：(1) 模态可靠性/不确定性估计思想——在遥感多模态融合（如光学+SAR、多光谱+高光谱）中，可借鉴"risk-guided routing"思路，为不同模态分配动态权重，识别受云遮挡、噪声污染的遥感影像模态；(2) 稀疏专家路由（sparse MoE）思想——在遥感目标检测模型中引入条件计算（conditional computation），仅对困难样本激活更多专家，对简单样本走轻量路径，实现推理时的动态计算量控制，这与在轨轻量化目标一致；(3) 共享路径+专家路径的架构设计——可类比遥感检测中的"通用特征提取+任务/场景专用分支"设计。但这些均为间接借鉴，需自行改造适配遥感检测任务。

Abstract: The promise of multimodal fusion lies in combining complementary sources of evidence, yet more evidence does not always yield a better prediction. Recent multimodal models have advanced fusion through richer cross-modal interaction and sample-adaptive fusion. However, the influence assigned to a modality during fusion does not reveal whether that source is unreliable, redundant, or poorly matched to a specialized expert. To address this limitation, we introduce TIER-MoE, a risk-guided subspace mixture-of-experts model that defines sample-specific modality reliability as the prediction loss its unimodal predictor is expected to incur. This risk is learned from out-of-fold predictions generated by models that were not trained on the corresponding sample. TIER-MoE combines the estimated risk with expert-specific subspace compatibility for sparse modality-expert routing, while an always-active shared path preserves multimodal complementarity. We evaluate TIER-MoE on four public multimodal biomedical datasets spanning Alzheimer's disease status, skin-lesion malignancy, and retinal classification. Results demonstrate its superiority over state-of-the-art methods in predictive performance and probability calibration, with consistent improvements in Macro-F1 and Brier score and strong zero-shot generalization to an external cohort.

</details>


### [290] [THGFM: Dual-Branch Temporal Heterogeneous Graph Fusion Model](https://arxiv.org/abs/2607.27303)
*Yixin Peng,Diego Collarana,Er Jin,Stefan Decker*

Main category: cs.LG

TL;DR: 提出THGFM，一个面向大规模时序异构图的双路径图融合模型，通过共享空间时序注意力与关系类型分区时序注意力的耦合，以及旋转时序注意力机制，在学术图基准上取得性能提升。

Research relevance: 弱相关（2分）。该论文是泛机器学习/图神经网络方向的工作，聚焦于temporal heterogeneous graphs（时序异构图）上的表示学习，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。虽然其"parameter-efficient cross-type transfer"和"dual-path architecture"在方法论层面有一定借鉴意义，但既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于图神经网络/时序图表示学习领域，不涉及遥感场景、目标检测任务或模型压缩部署。仅当对多模态特征融合或时序注意力机制感兴趣时，可作为方法论参考，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：时序异构图上的节点/图级表示学习与预测；应用场景：学术图（academic graphs）基准，包括OAG-CS、PF-L2、PF-L1、OGBN-MAG、HTAG-ArXiv、HTAG-DBLP；传感器类型：不适用（非遥感）；目标类别：学术论文、作者、领域等图节点类型。

Model architecture: THGFM采用dual-path（双路径）架构：(1) Shared-Space Temporal Attention分支，用于parameter-efficient cross-type transfer；(2) Relational Type-Partitioned Temporal Attention分支，用于relation-aware specialization。两条路径通过Dual-Path Relational-Shared Fusion整合，具体实例化为Type-Conditioned Non-Competitive Gated Sum Fusion（类型条件非竞争门控求和融合），为共享和专用分支分配独立的类型条件特征级门控。此外引入Rotary Temporal Attention，在匹配前将query和key按相对时间半相位旋转，将相对时间直接编码进attention score。

Lightweight method: 论文并非以轻量化为核心目标，但包含"parameter-efficient cross-type transfer"（参数高效的跨类型迁移）思想，通过共享空间注意力分支减少跨类型参数冗余。摘要未说明剪枝、量化、知识蒸馏、NAS等经典模型压缩方法。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作面向web-scale图数据，其计算开销和部署可行性在摘要中完全未讨论。

Datasets and metrics: 数据集：OAG-CS、PF-L2、PF-L1、OGBN-MAG、HTAG-ArXiv、HTAG-DBLP等学术图基准；评价指标：六任务平均增益（six-task mean gain）+3.25%，各数据集峰值相对增益分别为OAG-CS PV +12.37%、PF-L2 +4.87%、PF-L1 +1.18%、OGBN-MAG +4.24%、HTAG-ArXiv +3.73%、HTAG-DBLP +4.61%。具体指标类型（如Accuracy、F1、MRR等）摘要未说明。

Experiments: THGFM在学术图基准上一致优于baseline graph transformer模型，六任务平均增益+3.25%，其中OAG-CS PV上取得最大相对增益+12.37%。摘要未提供消融实验、参数量对比或推理速度对比等详细信息。

Limitations: 局限性：(1) 与遥感/卫星/目标检测完全无关，属于图神经网络领域；(2) 摘要未说明模型参数量、计算复杂度、推理延迟等部署关键指标；(3) 未讨论模型在资源受限设备上的可行性；(4) 实验仅覆盖学术图场景，泛化性到其他领域（如遥感）未验证；(5) 摘要未说明具体评价指标类型和baseline方法细节。

Ideas for my research: 尽管与遥感目标检测无直接关联，但以下方法论思想可借鉴：(1) Type-Conditioned Non-Competitive Gated Sum Fusion的非竞争门控融合思想，可用于遥感多传感器（光学/SAR/高光谱）特征融合，避免多模态特征间的零和竞争；(2) Rotary Temporal Attention将时间直接编码进attention score的思路，可用于遥感时序影像（如多时相变化检测）中的时间建模；(3) dual-path架构中"共享+专用"分支的设计哲学，与轻量化中"共享backbone+任务专用头"的思路相通，可在遥感多任务检测中减少参数冗余；(4) parameter-efficient cross-type transfer思想可类比到遥感多类别目标检测中的跨类别特征共享。

Abstract: Temporal heterogeneous graphs offer a natural abstraction for dynamic relational systems in which diverse node and relation types co-exist and evolve over time. Learning on such graphs requires jointly modeling cross-type structural heterogeneity and the temporal dynamics of interactions, yet existing methods still struggle to reconcile parameter-efficient cross-type transfer with relation-aware specialization, and typically inject time only as additive features outside the attention kernel. We propose \textbf{THGFM}, a web-scale temporal heterogeneous graph fusion model that addresses both limitations within a unified dual-path architecture. THGFM couples a \textit{Shared-Space Temporal Attention} branch for parameter-efficient cross-type transfer with a \textit{Relational Type-Partitioned Temporal Attention} branch for relation-aware specialization, and integrates them through \textit{Dual-Path Relational--Shared Fusion}, instantiated with \textit{Type-Conditioned Non-Competitive Gated Sum Fusion}: a adaptive mechanism that assigns independent, type-conditioned feature-wise gates to the shared and specialized branches, allowing both to be amplified or suppressed without zero-sum competition. To directly incorporate relative time into the attention score, THGFM further introduces \textit{Rotary Temporal Attention}, which rotates queries and keys by half-phases of relative time before matching. THGFM consistently outperforms baseline graph transformer models on academic graphs benchmarks, delivering a $+3.25\%$ six-task mean gain, with peak relative gains of $+12.37\%$ on OAG-CS PV, $+4.87\%$ on PF-$L_2$, and $+1.18\%$ on PF-$L_1$, and $+4.24\%$, $+3.73\%$, and $+4.61\%$ on OGBN-MAG, HTAG-ArXiv, and HTAG-DBLP, respectively.

</details>


### [291] [EvoCause: LLM-Guided Evolution of Causal Graphs for Root Cause Analysis](https://arxiv.org/abs/2607.27290)
*Lei Zan,Keli Zhang,Shifeng Xie,Jiale Zheng,Zehao Xiao,Zhiwei Dong,Ke Zhang,Ruichu Cai,Malik Tiomoko,Lujia Pan*

Main category: cs.LG

TL;DR: EvoCause 利用大语言模型（LLM）对电信告警因果图进行语义化编辑优化，结合专家标注闭环提升根因分析（RCA）性能，并发布了专家标注的电信告警基准数据集 TeleRCA。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦电信/云/微服务系统的告警级联根因分析（Root Cause Analysis, RCA），属于网络运维与因果推断领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。虽然使用了 LLM 进行图编辑优化，但这是因果图结构学习问题，与模型轻量化（pruning/quantization/distillation/NAS）无关，也不涉及边缘/星上部署。

Reading priority: 1 分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于电信告警根因分析领域。虽然"LLM 辅助 + 推理期轻量"的范式在方法论上有微弱启发，但缺乏遥感场景、目标检测任务和轻量化部署信息，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：电信/云/微服务系统的告警级联根因分析（Root Cause Analysis, RCA），即从观测日志中识别引发告警级联的源头告警。应用场景：生产电信网络运维。传感器类型：不适用（非遥感传感器）。目标类别：告警类型（194 种 alarm types），非视觉目标。

Model architecture: 方法核心：EvoCause 框架。它基于因果图学习（如 PC causal discovery algorithm）初始化因果图，然后利用大语言模型（LLM）提出语义上合理的图编辑（graph edits），同时用确定性代码验证节点身份（node identities）和无环性（acyclicity），并在标注对齐集上保留最优图。测试阶段仅用精炼后的因果图做透明预测，无需调用 LLM。整体属于"因果图结构学习 + LLM 辅助编辑"的混合架构，非神经网络视觉模型。

Lightweight method: 不涉及模型轻量化。摘要未提及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计、实时推理或边缘/星上部署。唯一的"轻"相关点是测试阶段无需 LLM 调用（仅用精炼图做透明预测），但这属于推理时避免大模型开销，并非传统意义上的模型压缩/加速方法。

Onboard deployability: 不涉及星上/边缘部署。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性指标。虽然测试阶段避免了 LLM 调用（降低了推理开销），但该设计面向的是电信运维场景，与在轨遥感目标检测的部署约束（如 FPGA/NPU/TensorRT）无关。

Datasets and metrics: 数据集：1) 合成数据（synthetic data），用于与 PC 因果发现算法基线对比；2) TeleRCA——来自生产电信网络的专家标注基准，包含 485,681 条告警事件、194 种告警类型、5,621 个资源。评价指标：Node F1、Case EM（Case Exact Match）、Graph F1、nSHD（normalized Structural Hamming Distance）。

Experiments: 主要结果：1) 在合成数据上，EvoCause（以 PC 算法初始化）相比未精炼的 PC 基线，Node F1、Case EM、Graph F1 分别提升 11.59、9.40、4.59 个百分点，nSHD 降低 0.2379。2) 在 TeleRCA 上，将人类可读的告警标题替换为匿名标识符后，Node F1 和 Case EM 分别下降 6.12 和 8.04 个百分点，说明告警名称的语义信息对图精炼有贡献。摘要未说明消融实验细节、计算开销对比或部署相关实验。

Limitations: 1) 与遥感/卫星/目标检测/轻量化完全无关，属于网络运维根因分析领域。2) 依赖 LLM 进行图编辑，虽然测试阶段无需 LLM，但训练/精炼阶段的计算成本未说明。3) 依赖专家标注（expert labels），标注成本与可扩展性未讨论。4) 方法针对告警因果图，无法直接迁移到视觉目标检测任务。5) 摘要未说明在更大规模或动态变化网络上的泛化能力。

Ideas for my research: 尽管领域不同，仍有少量方法论层面的可借鉴点：1) "训练/精炼阶段用大模型辅助、推理阶段用轻量模型"的范式，与 knowledge distillation 中"教师模型离线指导、学生模型在线推理"的思路有相似之处，可类比思考在轨检测中如何用大模型离线蒸馏出轻量学生模型。2) 利用语义信息（如告警名称）提升性能的实验设计（匿名化消融）提示：在遥感任务中可做类似的"语义标签 vs 匿名标签"消融来验证语义先验的作用。3) 确定性校验（acyclicity 验证）与 LLM 生成结合的思路，可类比为在模型设计中用规则约束保证输出合法性。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Modern telecommunication, cloud, and microservice systems emit correlated alarm cascades when components fail. Root cause analysis (RCA) aims to identify the small set of alarms that initiate each cascade. A common approach learns a causal graph from observational logs and predicts all zero-in-degree alarms in each incident-induced subgraph. However, the learned graph remains fixed and cannot benefit from expert diagnoses of historical incidents. We close this loop with EvoCause. Expert labels constrain which alarms should be source nodes but do not specify the edge edits needed to satisfy those constraints. EvoCause uses a large language model (LLM) to propose semantically plausible graph edits, while deterministic code validates node identities and acyclicity and retains the best graph on a labeled alignment set. At test time, the refined graph alone produces transparent predictions without an LLM call. We also release TeleRCA, an expert-annotated benchmark from a production telecommunication network containing $485{,}681$ alarm events spanning $194$ alarm types over $5{,}621$ resources. On synthetic data, EvoCause initialized with the PC causal discovery algorithm outperforms the unrefined PC baseline, raising Node F1, Case EM, and Graph F1 by $11.59$, $9.40$, and $4.59$ percentage points, respectively, while reducing nSHD by $0.2379$. On TeleRCA, replacing human-readable alarm titles with anonymous identifiers lowers Node F1 and Case EM by $6.12$ and $8.04$ percentage points, respectively, indicating that alarm-name information contributes to graph refinement.

</details>


### [292] [Strategies for Milestone-driven Start-ups in Multi-activity Settings](https://arxiv.org/abs/2607.27563)
*Zhengli Wang*

Main category: cs.LG

TL;DR: 本文研究创业公司在里程碑导向情境下的最优策略，利用连续时间随机控制模型求解多控制变量的最优策略结构，提出"有效前沿曲线"来排序不同活动的风险性和成本效益。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于创业管理与随机控制/运筹学领域，不涉及任何遥感、卫星、目标检测或模型压缩相关内容。相关性评分为1分。

Reading priority: 1分。论文主题为创业公司的随机控制策略，与遥感卫星在轨目标检测及模型轻量化研究方向完全无关，无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：创业公司里程碑导向下的最优策略选择（随机控制/最优停止问题）。应用场景：创业决策管理。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明（不涉及任何神经网络或机器学习模型架构）。论文采用连续时间随机控制模型（diffusion process），涉及多控制变量（≥3）的最优策略求解。

Lightweight method: 不涉及。论文未提及任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络等模型压缩或加速方法。

Onboard deployability: 不适用。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 摘要未说明。论文为理论建模研究，未提及任何实验数据集或评价指标。

Experiments: 摘要未说明。论文主要贡献为理论推导，给出了最优策略的显式结构刻画，未提及实验对比或消融实验。

Limitations: 论文与遥感目标检测和轻量化方向完全无关，属于创业管理与随机控制理论领域。摘要中未涉及任何与计算机视觉、深度学习或遥感相关的技术内容。

Ideas for my research: 无直接可借鉴点。该论文属于运筹学/创业管理领域，与"在轨遥感目标检测模型轻量化"课题在方法论、技术路线和应用场景上均无交集。不建议从中提取任何技术思路。

Abstract: New venture start-ups need to ``survive'' through multiple stages of reaching milestone targets. We investigate the strategies for start-ups in a milestone-oriented setting. We examine a model of an entrepreneurial start-up firm, where its state is captured by a diffusion process. The entrepreneur can choose between multiple activities (or controls), which incur different cost and determine the drift and the variance of the process. Depending on whether the process reaches a fixed upper boundary or a lower one, the start-up firm succeeds or fails. Continuous-time stochastic models with multiple ($\ge 3$) controls are typically very challenging to deal with. In this work, we are able to completely solve for the optimal policy and provide an explicit characterization of its structure. In particular, the optimal policy only uses controls from a set characterized by a so-called efficient frontier curve that orders the controls by two intuitive measures: riskiness (drift-to-volatility ratio) and cost-effectiveness (drift-to-cost ratio). A unique feature of our model is that depending on the model parameters, the efficient frontier curves can be of different types, resulting in qualitatively different structures of the optimal policy. As far as we know, this is the first study that analyzes a stochastic control model which admits efficient frontier curves of different types. Our work provides start-up firms with intuitive measures to evaluate their activities and offers valuable insights on how the optimal strategies in a milestone-oriented setting change qualitatively contingent upon the specific scenario. We believe the results provide a foundational block in the study of entrepreneurial decision-making.

</details>


### [293] [Encryption-Compatible Clustered Federated Learning via Distributed Expectation-Maximization over Metadata](https://arxiv.org/abs/2607.28338)
*Michael Ben Ali,Imen Megdiche,André Péninou,Olivier Teste*

Main category: cs.LG

TL;DR: 提出FLAMECHE，将基于元数据的聚类联邦学习(CFL)重构为分布式期望最大化(EM)过程，仅使用加法操作进行服务器更新，从而兼容安全联邦学习加密方案，在隐私保护、通信成本和计算效率三者间取得更好平衡。

Research relevance: 弱相关（2分）。论文属于联邦学习(Federated Learning)领域，聚焦数据异构性下的客户端聚类问题，与遥感、卫星、目标检测、模型轻量化均无直接关联。虽然联邦学习在遥感多源数据协同训练和星上分布式学习场景中可能有潜在应用，但本文未涉及任何遥感场景、目标检测任务或模型压缩/加速方法。

Reading priority: 2分。与遥感卫星在轨目标检测及模型轻量化方向弱相关。仅在"多星分布式协同训练"这一间接场景下可能有参考价值，但本文不涉及目标检测、遥感数据或模型压缩，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：聚类联邦学习(Clustered Federated Learning, CFL)中的客户端分组与模型训练。应用场景：通用联邦学习环境，非遥感场景。传感器类型：摘要未说明。目标类别：不涉及目标检测，无具体目标类别。

Model architecture: FLAMECHE将元数据(metadata)驱动的CFL聚类问题形式化为分布式期望最大化(EM)过程。服务器端更新被限制为加法操作(additive operations)，以兼容安全联邦学习(secure FL)加密方案。具体网络结构细节摘要未说明。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等传统模型轻量化方法。本文的"效率"维度主要指通信效率和计算效率（通过元数据聚类降低通信/计算开销），而非模型本身的压缩或加速。与星上部署、FPGA/NPU/TensorRT等边缘推理优化无关。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。本文讨论的是联邦学习训练阶段的通信与计算开销，而非推理阶段的模型部署效率，与星上/边缘部署可行性无直接关联。

Datasets and metrics: 在多个数据集(multiple datasets)上进行了多种异构场景(heterogeneous scenarios)下的实验。具体数据集名称、评价指标、实验设置细节摘要未说明。

Experiments: 实验结果表明FLAMECHE提升了客户端模型的训练效果(effectiveness of client models)，并实现了加密兼容的元数据聚类，在CFL三难困境(CFL trilemma)中获得了更好的定位。具体对比方法、消融实验、量化结果摘要未说明。

Limitations: 1. 摘要未说明具体数据集、模型架构和量化性能指标；2. 未讨论在遥感/卫星场景下的适用性；3. 未涉及目标检测任务；4. 未涉及模型轻量化/压缩方法；5. 加密兼容性带来的额外计算开销未量化说明。

Ideas for my research: 1. 联邦学习框架可借鉴到多星协同在轨训练场景——多颗遥感卫星作为客户端，在数据不出星的前提下协同训练目标检测模型，FLAMECHE的加密兼容聚类思想可用于处理不同卫星观测区域的数据分布差异；2. 元数据(metadata)低维表示的思想可类比于遥感场景中不同卫星/传感器数据分布特征的轻量表征；3. 但本文未涉及目标检测模型本身，需自行将CFL框架与检测模型训练结合。整体借鉴价值有限。

Abstract: Clustered Federated Learning (CFL) addresses data heterogeneity in federated settings by grouping clients with similar data distributions to enable effective training. Existing methods face a trade-off between privacy preservation, communication cost, and computational efficiency. We formalize this as the CFL trilemma, according to which improving two of these dimensions comes at the expense of the third. A prominent paradigm relies on metadata (i.e., low-dimensional representations of client datasets shared with the server) to enable communication- and computation-efficient clustering. However, such approaches are not compatible with standard FL privacy-preserving mechanisms. To address this limitation, we propose FLAMECHE, which reformulates metadata-based CFL as a distributed Expectation-Maximization (EM) procedure, restricting server updates to additive operations while preserving efficiency. This design enables compatibility with practical secure FL schemes. We conducted extensive experiments on multiple datasets under various heterogeneous scenarios. Results show that FLAMECHE improves the effectiveness of client models. It enables encryption-compatible metadata-based clustering, enhancing its positioning within the CFL trilemma.

</details>


### [294] [Position, Not Provenance: Separating Reasoning Mediation from Sycophancy in Medical Vision-Language Models](https://arxiv.org/abs/2607.27304)
*Supratik Bhowal,Subhrajyoti Basu,Aritra Gir Mahanta,Anik Pal Chowdhury*

Main category: cs.LG

TL;DR: 提出CoT-Mediate行为学框架，通过扰动医学视觉语言模型（LLaVA-Med、MedGemma）自身生成的CoT推理中的临床属性，测量预测是否跟随被编辑的推理，以评估推理对预测的因果影响。

Research relevance: 1分。该论文研究医学视觉语言模型（medical VLM）的chain-of-thought推理因果性，属于医学影像+自然语言处理交叉方向，与遥感、卫星、在轨目标检测、轻量化部署完全无关。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或加速方法。

Reading priority: 1分。论文主题为医学VLM推理因果性分析，与遥感卫星在轨目标检测及轻量化方向基本无关，无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学视觉问答（medical VQA）中的推理因果性分析；应用场景：临床问答、医学影像理解；传感器类型：医学影像（VQA-RAD数据集，非遥感传感器）；目标类别：临床属性（如laterality左右侧、视觉证据等），非遥感目标。

Model architecture: 基于LLaVA-Med和MedGemma两个医学视觉语言模型，采用行为学实验框架（behavioral framework），包含dual-arm protocol（re-prompted evidence vs. prefix-forced continuation）和provenance-controlled intervention。摘要未说明具体网络结构细节。

Lightweight method: 不涉及。摘要未提及任何剪枝、量化、知识蒸馏、NAS、轻量网络设计或边缘/星上部署相关方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署信息，且该研究本身与在轨/边缘部署无关。

Datasets and metrics: 数据集：VQA-RAD（医学视觉问答数据集），各1,000个样本用于LLaVA-Med和MedGemma评估。评价指标：mediation faithfulness（推理中介忠实度），通过prefix-forced continuation与re-prompting两种机制对比测量。

Experiments: 主要发现：1) prefix-forced continuation比re-prompting产生更高的mediation faithfulness；2) provenance分析揭示不同模型具有不同的deference行为；3) 移除视觉证据会增加对注入推理的依赖；4) laterality（左右侧）是最不被忠实跟踪的临床属性；5) 上下文位置而非声明的来源是决定医学VLM是否使用生成推理的主要因素。

Limitations: 与本研究方向完全无关。局限在于：仅针对医学VLM的推理因果性，不涉及遥感目标检测或模型轻量化；摘要未说明模型规模、计算开销、推理速度等对部署有意义的信息。

Ideas for my research: 借鉴意义极低。唯一可能的间接启发是：该论文关于"推理注入机制影响模型行为"的方法论（如prefix-forced continuation vs. re-prompting）在方法论设计层面有一定参考价值，但与本课题（遥感在轨目标检测轻量化）几乎无交集。不建议投入时间深入阅读。

Abstract: Medical vision-language models (VLMs) generate chain-of-thought (CoT) reasoning before answering clinical questions, but whether this reasoning causally influences predictions remains unclear. We present CoT-Mediate, a behavioral framework that perturbs a single clinically meaningful attribute within a model's own generated reasoning and measures whether the resulting prediction follows the edited reasoning. Our framework combines a dual-arm protocol comparing re-prompted evidence with prefix-forced continuation, together with a provenance-controlled intervention that varies only the attributed source of identical reasoning to disentangle reasoning mediation from sycophancy. We evaluate LLaVA-Med and MedGemma on 1,000 VQA-RAD samples each. Prefix-forced continuation consistently yields higher mediation faithfulness than re-prompting, while the provenance analysis reveals distinct model-specific deference behaviors. Across both models, removing visual evidence increases reliance on injected reasoning, whereas laterality is the least faithfully tracked clinical attribute. These results show that the mechanism used to inject reasoning substantially affects measured faithfulness and that contextual position, rather than stated provenance, is the primary determinant of whether medical VLMs use their generated reasoning.

</details>


### [295] [On-Policy and Off-Policy Learning for Large Action Spaces](https://arxiv.org/abs/2607.28408)
*Imad Aouali*

Main category: cs.LG

TL;DR: 本文研究交互式系统中的策略学习，聚焦contextual bandits框架下的on-policy和off-policy学习，提出结构化贝叶斯方法（meTS、dTS）和结构化直接方法（sDM）等，解决大动作空间中的探索、数据覆盖、重要性加权方差等问题。

Research relevance: 弱相关（2分）。论文属于强化学习/在线决策（contextual bandits）领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可能的间接联系是off-policy learning中的知识蒸馏或模型压缩场景可能借鉴其重要性加权和悲观估计思想，但摘要中未涉及任何遥感或检测内容。

Reading priority: 1分。论文属于contextual bandits/强化学习理论方向，与遥感卫星在轨目标检测及轻量化课题基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：contextual bandits下的策略学习（on-policy与off-policy）；应用场景：交互式决策系统，非遥感场景；传感器类型：不适用；目标类别：不适用。

Model architecture: 提出三类方法：1) meTS（mixed-effect Thompson sampling扩展）；2) dTS（基于diffusion-inspired先验的Thompson sampling变体）；3) sDM（基于隐变量的结构化直接方法）。核心思想是通过结构化建模在动作之间共享信息。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。off-policy learning中的重要性采样加权和悲观正则化在方法论层面与蒸馏中的logit加权有概念相似性，但并非轻量化方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其研究对象是决策策略而非视觉模型，不适用于星上部署场景。

Datasets and metrics: 摘要未说明具体数据集和评价指标。理论层面关注regret guarantees（遗憾界），off-policy场景关注estimation error与optimization error的权衡。

Experiments: 摘要未说明具体实验设置、对比方法和消融实验。仅从方法论层面声称：meTS和dTS提供依赖有效动作数的regret界；sDM证明大动作空间中optimization error可主导estimation error；提出的concave policy-weighted log-likelihood目标可高效优化；悲观方法可控制正则化重要性采样估计器的bias-variance权衡。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于强化学习理论方向；2) 摘要未提供任何实验验证细节；3) 大动作空间假设与遥感目标检测的视觉特征空间差异巨大，方法论迁移困难。

Ideas for my research: 1) off-policy learning中的重要性加权和悲观估计思想，可类比迁移到知识蒸馏中teacher-student logit分布对齐时的样本加权策略；2) 结构化贝叶斯方法中"在动作间共享信息"的思想，可类比到轻量化检测模型中跨尺度特征共享或通道剪枝时的相关性建模；3) 但整体而言，本文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: This thesis studies policy learning in interactive systems where an agent observes a context, selects an action from a very large set, and receives partial feedback. The main framework is contextual bandits, with two paradigms: on-policy learning, where the agent interacts sequentially with the environment and minimizes regret, and off-policy learning, where it learns from logged data collected by a logging policy. In large action spaces, both settings face major challenges: inefficient exploration, sparse data coverage, high-variance importance weights, extrapolation bias, and difficult optimization landscapes. The first part develops structured Bayesian methods for on-policy learning. We introduce meTS, a mixed-effect extension of Thompson sampling, and dTS, which leverages diffusion-inspired priors to model dependencies between actions. These methods share information across actions and yield regret guarantees depending on an effective number of actions. The second part addresses off-policy learning. We propose sDM, a structured direct method based on latent variables, show that optimization error can dominate estimation error in large action spaces, and introduce concave, efficiently optimizable policy-weighted log-likelihood objectives. Finally, we develop differentiable pessimistic methods based on exponential smoothing and PAC-Bayesian bounds to control the bias-variance trade-off of regularized importance-sampling estimators.

</details>


### [296] [ZUNA1.1: A more flexible EEG foundation model for Denoising and Super-resolution](https://arxiv.org/abs/2607.27308)
*Christopher Warner,Jonas Mago,JR Huml,Beren Millidge*

Main category: cs.LG

TL;DR: ZUNA1.1是一个380M参数的diffusion autoencoder，用于灵活的EEG（脑电）信号重建，支持变长序列、任意通道数和任意头皮位置，性能优于标准EEG去噪重建方法。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于EEG（脑电）信号重建，属于生物医学信号处理领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无关联；不涉及object detection、remote sensing、轻量化（pruning/quantization/knowledge distillation/NAS）等任何相关技术。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：EEG信号重建与去噪；应用场景：脑电信号处理（生物医学）；传感器类型：EEG电极（非遥感传感器）；目标类别：不适用（非目标检测任务）。

Model architecture: ZUNA1.1为380M参数的diffusion autoencoder，支持变长序列（最长30s）、任意数量EEG通道、任意头皮位置，可重建通道内任意时间间隔或整条通道。摘要未说明具体网络结构细节（如backbone、注意力机制、扩散模型的具体设计）。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（无pruning、quantization、knowledge distillation、NAS、轻量网络设计等）。

Onboard deployability: 摘要未说明。未提及参数量部署考量（尽管模型为380M参数，但未讨论FLOPs、延迟、显存、能耗、实时推理或边缘/星上部署可行性）。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及与ZUNA1模型对比，以及与spherical spline interpolation（MNE包中广泛部署的标准EEG去噪重建方法）对比。

Experiments: 实验表明ZUNA1.1在重建性能上至少与ZUNA1持平，但灵活性和可处理任务范围更广；显著优于spherical spline interpolation等标准EEG去噪重建方法。摘要未说明消融实验、对比方法细节及定量指标数值。

Limitations: 摘要未说明局限性。但可指出：该工作完全面向EEG生物信号，与遥感在轨目标检测无任何交集；380M参数规模对在轨部署而言过大，且未讨论任何压缩或加速策略。

Ideas for my research: 借鉴价值极低。唯一可能的间接启发是：diffusion autoencoder的"灵活重建任意长度/任意通道/任意位置"思想，在遥感数据（如多光谱/高光谱波段缺失重建、SAR图像修复）中或有类比价值，但与本课题"在轨目标检测模型轻量化"的核心方向几乎无直接关联。

Abstract: We introduce ZUNA1.1, a 380M-parameter diffusion autoencoder for flexible EEG signal reconstruction. ZUNA1.1 is capable of reconstructing variable length sequences of up to 30s, with an arbitrary number of EEG channels at arbitrary scalp locations, and can reconstruct arbitrary temporal intervals within channels in addition to reconstructing entire channels. We demonstrate that ZUNA1.1 performs at least on par with our earlier ZUNA1 model, while being far more flexible and capable of handling a wide range of reconstruction tasks. ZUNA1.1 continues to substantially outperform standard EEG denoising and reconstruction methods such as spherical spline interpolation, which is ubiquitously deployed in the MNE package. The ZUNA1.1 model is released open source under the permissive Apache 2.0 license.

</details>


### [297] [Modeling Decisions in Blockchain Analytics: A Leakage-Aware Evaluation of Tree-Based vs. Sequential Models](https://arxiv.org/abs/2607.27350)
*Michał Bartnicki,Jarosław A. Chudziak*

Main category: cs.LG

TL;DR: 提出一种基于泄漏感知的以太坊Sybil机器人检测框架，使用Transaction Grammar表示钱包行为，并证明在消除标签泄漏后，树模型XGBoost在性能和低延迟部署上优于Transformer/BiLSTM序列模型。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文研究的是区块链/以太坊上的Sybil机器人检测，属于链上行为分析领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感相关任务，也不涉及目标检测。虽然论文讨论了模型轻量化/低延迟部署（XGBoost vs Transformer的延迟和能耗对比），但这是通用机器学习部署问题，与遥感在轨场景无直接关联。

Reading priority: 1分。论文主题为区块链Sybil检测，与遥感卫星在轨目标检测及轻量化方向基本无关。仅"泄漏感知评估"和"轻量模型vs重型模型公平对比"两个方法论思想有微弱借鉴价值，不值得优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：以太坊链上Sybil机器人检测（actor classification）。应用场景：区块链安全、空投奖励防作弊、治理投票防操纵。传感器类型：不适用（数据为区块链交易序列）。目标类别：有机用户（organic users）、Sybil机器人、MEV机器人。

Model architecture: 对比了Transformer和BiLSTM序列模型，以及XGBoost和SVM树/表格基线模型。核心表示方法为Transaction Grammar，建模钱包行为的节奏（rhythm）、EVM执行结构和意图（intent）。另提出Blind-Spot协议用于消除高信号智能合约带来的标签泄漏捷径。

Lightweight method: 论文本身不提出剪枝、量化、蒸馏或NAS等模型压缩方法。其轻量化相关贡献在于：证明在泄漏感知评估下，轻量的树模型XGBoost在性能上优于重型的Transformer序列模型，同时提供更低的延迟和估计能耗。这属于"选择轻量模型架构而非压缩重型模型"的部署策略，而非传统模型压缩技术。

Onboard deployability: 摘要提及XGBoost相比Transformer提供"lower latency and estimated energy use"，但未给出具体延迟数值、参数量、FLOPs、显存或能耗数据。部署场景为区块链实时监控（real-time monitoring），而非星上/边缘部署。具体部署指标摘要未说明。

Datasets and metrics: 数据集：以太坊链上钱包行为数据（具体数据集名称摘要未说明）。评价指标：摘要未明确列出具体指标名称（如准确率、F1等），仅提及"actor classification"任务和性能对比。实验设置：在泄漏感知评估协议下对比Transformer、BiLSTM、XGBoost、SVM四种模型。

Experiments: 主要结果：在泄漏感知评估下，XGBoost优于Transformer序列模型，同时延迟和能耗更低。论文还探讨了三个研究问题：有机用户/Sybil/MEV机器人在交易历史结构复杂度上的差异；序列模型在减少泄漏后是否仍优于树模型；交易顺序vs交易时序哪个是更强的行为信号。具体数值结果摘要未说明。

Limitations: 与本研究方向相关性极弱。局限性包括：1）区块链行为数据与遥感图像数据在模态、特征空间、任务目标上完全不同；2）论文的"轻量化"仅指选择轻量树模型，未涉及剪枝/量化/蒸馏等可迁移的模型压缩技术；3）无任何遥感、卫星、目标检测相关内容；4）部署场景（链上实时监控）与星上部署约束（功耗、辐射、算力）完全不同。

Ideas for my research: 尽管整体弱相关，仍有少量可借鉴的通用方法论：1）"标签泄漏感知评估"（leakage-aware evaluation）思想——在遥感目标检测中，训练/测试数据划分不当（如同一场景不同时相图像混入）可能导致性能虚高，值得在实验设计中警惕；2）"轻量模型在公平评估下可能优于重型模型"的结论——提示在遥感轻量化研究中，不应默认深度模型一定优于传统/轻量方法，应做公平对比；3）Transformer vs 轻量模型的延迟/能耗对比框架，可作为遥感在轨部署评估的参考范式。但这些借鉴点均属于方法论层面的通用启发，非直接可复现的技术。

Abstract: Sybil bots are Ethereum actors that imitate legitimate users to extract airdrop rewards or influence governance. Recent Sybil detection methods increasingly use deep learning and treat blockchain activity as a quasi-linguistic sequence. However, complex sequence models are computationally expensive for real-time monitoring, and their reported performance may be inflated by label leakage from high-signal smart contracts. We ask whether and how organic users, Sybil bots, and MEV bots differ in the structural complexity of their transaction histories; whether sequential models outperform tree-based tabular models once leakage is reduced; whether transaction order or timing provides the stronger behavioral signal; and whether the resulting models are practical for low-latency deployment. Our approach to leakage-aware Sybil bot detection consists of a Blind-Spot protocol and a Transaction Grammar representation of wallet behavior. The former eliminates shortcuts associated with high-signal contracts, whereas the latter models wallets using rhythm, EVM execution structure, and intent. We evaluate this approach on Ethereum actor classification by comparing Transformer and BiLSTM sequence models against XGBoost and SVM baselines. We contribute a framework for leakage-aware Ethereum actor classification and a Transaction Grammar representation of wallet behavior. Our results demonstrate that, under leakage-aware evaluation, XGBoost outperforms Transformer-based sequence models while providing lower latency and estimated energy use.

</details>


### [298] [Compression-Based Behavioral Similarity for Open-World Sybil Discovery on Ethereum](https://arxiv.org/abs/2607.27370)
*Michał Bartnicki,Jarosław A. Chudziak*

Main category: cs.LG

TL;DR: 提出一种基于压缩相似度（Gzip-NCD）和交易语法符号化的免训练区块链Sybil攻击者检测框架，用于开放世界区块链审计。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦区块链Sybil攻击者检测，属于区块链安全/图机器学习领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。虽然使用了"compression-based"（压缩）和"training-free"（免训练）概念，但这里的"压缩"指Gzip数据压缩用于相似度度量（NCD），并非模型压缩（pruning/quantization/distillation），与轻量化部署无关。相关性评分：1分。

Reading priority: 1分。论文主题为区块链Sybil检测，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。仅当对"training-free"检测范式或对抗鲁棒性评估框架有跨领域兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：区块链Sybil攻击者检测（Sybil actor detection）。应用场景：区块链空投（airdrop）防护、治理投票安全、开放世界区块链审计。传感器类型：不适用（数据来源为EVM交易轨迹/traces）。目标类别：Sybil bots、organic users、arbitrage bot wallets。

Model architecture: 方法框架包含：1) 从EVM traces中合成符号化Transaction Grammar，分别捕获交易节奏（transaction rhythm）、执行结构（execution structure）和功能意图（functional intent）；2) Blind-Spot Protocol用于过滤high-signal contracts；3) 基于Gzip的NCD（Normalized Compression Distance）构建行为图（behavioral graph）用于Sybil发现。核心NCD原语无需监督训练。

Lightweight method: 摘要未说明传统意义上的模型轻量化方法（如pruning、quantization、knowledge distillation、NAS）。文中"compression-based"指使用Gzip压缩距离（NCD）作为相似度度量，属于数据压缩而非模型压缩。方法本身是training-free的，无需训练模型，因此天然避免了模型存储和推理开销，但这与遥感模型轻量化部署是不同层面的概念。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。方法定位为"training-free local discovery primitive"，强调无需监督训练即可扩展可疑种子钱包，但未给出任何星上/边缘部署可行性分析。与在轨部署无关。

Datasets and metrics: 摘要未明确说明具体数据集名称。验证方式包括：与监督式机器学习baseline对比、时间划分（temporal split）测试、合成伪装压力测试（synthetic camouflage stress tests）。评价指标摘要未明确列出。

Experiments: 实验验证包括：1) 与监督式机器学习baseline对比；2) temporal split验证时间漂移鲁棒性；3) synthetic camouflage stress tests验证对抗扰动鲁棒性。摘要未给出具体数值结果。

Limitations: 1) 与遥感/卫星/目标检测/轻量化方向完全无关，属于区块链安全领域；2) 方法定位为"training-free local discovery primitive"而非正式的open-set recognition系统，适用范围有限；3) 摘要未说明计算复杂度、可扩展性、大规模图构建开销；4) 未提供定量实验结果和对比数据。

Ideas for my research: 尽管领域不同，仍有少量可借鉴的抽象思路：1) "training-free"思想——在遥感在轨场景中，若能用免训练的相似度/距离度量（如NCD类方法）辅助检测，可避免星上模型训练开销；2) 对抗鲁棒性测试框架（camouflage stress tests）的思路可迁移到遥感目标检测的对抗样本鲁棒性评估；3) 时间漂移（temporal drift）鲁棒性验证对遥感模型在不同季节/光照/传感器条件下的泛化评估有参考价值。但这些均为间接借鉴，直接可复现性低。

Abstract: Sybil attackers are Blockchain actors that adopt the characteristics of regular users to exploit airdrops or influence governance. Current methods of Sybil actor detection include constructing graphs, which requires token transfers between examined wallets. Machine learning algorithms have been employed as well, but they treat the task as a closed-set classification problem, making them vulnerable to frequent changes in attack strategies or evasion tactics. We address the following questions: can compression-based similarity differentiate Sybil bots, organic users, and arbitrage bot wallets without direct financial links? What is the effect of high-signal contracts on the discovery of Sybils, and how robust are behavioral graphs under temporal drift and adversarial perturbations? Our approach synthesizes a symbolic Transaction Grammar from EVM (Ethereum Virtual Machine) traces, capturing separately transaction rhythm, execution structure, and functional intent. The high-signal contracts are filtered with our own protocol, called the Blind-Spot Protocol. Gzip-based NCD is used to construct a behavioral graph for Sybil discovery. We validate this framework against supervised machine learning baselines, a temporal split, and synthetic camouflage stress tests. Ultimately, we contribute a leakage-aware behavioral framework for Sybil candidate discovery. Its core NCD primitive requires no supervised training and can expand suspicious seed wallets without explicit funding links. We position the method as a training-free local discovery primitive for open-world blockchain audits, rather than as a formal open-set recognition system.

</details>


### [299] [Explorative Modeling: Unlocking a Third Pretraining Axis and End-to-End Generation](https://arxiv.org/abs/2607.27372)
*Alexi Gladstone,Heng Ji,Yilun Du*

Main category: cs.LG

TL;DR: 提出Explorative Modeling (XM)新范式，通过分解训练循环而非生成过程，实现生成模型的端到端训练，在图像、视频和语言等连续与离散域上提升性能与效率。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦于生成式建模（generative modeling）的端到端训练范式，属于生成模型/深度学习基础研究，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能等任何遥感相关场景，也不涉及目标检测任务，更不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或边缘部署方法。

Reading priority: 1分。论文属于生成模型基础研究，与遥感卫星在轨目标检测及轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：生成式建模（generative modeling），包括图像生成、视频生成、语言生成以及控制任务（control tasks）。应用场景为通用深度学习/生成模型领域，非遥感场景。传感器类型：摘要未说明（不适用）。目标类别：摘要未说明（不适用）。

Model architecture: 提出Explorative Model (XM)范式：不分解生成过程（factoring the generation procedure），而是分解训练循环（factoring the training loop），探索K个候选的模型生成与数据之间的匹配，并在最优匹配上训练，使预测commit到模式（modes）而非模糊化。该方法作为现有生成模型的第三个预训练轴（beyond parameters and data），可扩展探索（exploration）规模。

Lightweight method: 不涉及传统模型压缩方法（剪枝、量化、蒸馏、NAS等）。但论文报告了效率提升指标：FLOP效率提升4.1x，样本效率提升6.2x，参数效率提升47%，以及在控制任务上以16-256x更少的推理步数匹配diffusion。这些效率指标属于训练/推理效率优化范畴，但并非面向边缘/星上部署的轻量化方法。

Onboard deployability: 摘要未说明星上/边缘部署相关信息。虽然报告了FLOP效率、参数效率等指标，但论文面向的是生成模型训练范式，不涉及目标检测推理的实时性、延迟、显存、能耗等部署指标，也未提及FPGA/NPU/TensorRT等硬件加速方案。

Datasets and metrics: 数据集：ImageNet（用于图像生成，达到1.43 FID）；还涉及视频和语言领域（具体数据集摘要未说明）。评价指标：FID（Fréchet Inception Distance）、FLOP效率、样本效率、参数效率、推理步数（inference steps）。控制任务上与diffusion对比。

Experiments: 主要实验结果：1) 探索规模（exploration）作为新的预训练轴，在连续和离散域（图像、视频、语言）上单调提升性能；2) 探索带来的增益随规模增大而增加——数据规模扩大时从7%提升到36%，模型规模扩大时从13%提升到23%；3) 在3x计算量下效率增益翻倍以上；4) FLOP效率提升4.1x，样本效率提升6.2x，参数效率提升47%；5) 在ImageNet上无guidance达到1.43 FID，接近state-of-the-art；6) 在控制任务上以16-256x更少的推理步数匹配diffusion。

Limitations: 摘要未说明局限性。但可指出：论文完全面向生成模型，与遥感目标检测任务无直接关联；其效率提升针对的是生成模型的训练/推理，而非检测模型的压缩与部署；未涉及任何遥感场景验证。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但可借鉴的点有限：1) "探索训练循环而非分解生成过程"的思想在方法论层面有一定启发性，但难以直接迁移到目标检测模型压缩；2) 其报告FLOP效率、参数效率等指标的方式可作为论文写作中效率评估的参考框架；3) 若未来在轨目标检测涉及生成式数据增强或域自适应，该范式可能间接相关，但当前阶段借鉴价值很低。

Abstract: The deep learning revolution, kicked off by AlexNet, taught us that end-to-end training beats decomposing a problem into hand-designed stages. Generative modeling, however, has remained the exception-despite generative models being remarkably capable, they are still not trained end-to-end. This is because, at its core, generative modeling is about handling distributions with many modes, and existing scalable approaches handle this the same way, by factoring the generation procedure, which prevents end-to-end generation. In this work, we introduce Explorative Modeling, a new paradigm that instead factors the training loop, exploring K candidate matches between model generations and data, and training on the best, so predictions commit to modes rather than blurring them. We find Explorative Models (XMs) useful in two settings. First, increasing exploration adds a third pretraining axis beyond parameters and data for existing generative models-where scaling exploration monotonically improves performance across both continuous and discrete domains (images, video, and language). Notably, gains from exploration increase with scale, climbing from 7% to 36% as data scales and from 13% to 23% as models grow, with efficiency gains more than doubling at 3x the compute. Concretely, exploration improves FLOP efficiency by 4.1x, sample efficiency by 6.2x, parameter efficiency by 47%, lifts the strongest of image-generation recipes to a near-state-of-the-art 1.43 FID on ImageNet without guidance, enables scaling how end-to-end existing models are, and unlocks scaling generalization. Second, XMs enable end-to-end reconstructive generative modeling, matching diffusion on control tasks with 16-256x fewer inference steps. Together, these results establish XMs as both a new pretraining axis for existing generative models and a standalone end-to-end generative modeling paradigm.

</details>


### [300] [The Convergence Behavior of Adam under Heavy-Tailed Noise](https://arxiv.org/abs/2607.27383)
*Yijiang Pang*

Main category: cs.LG

TL;DR: 本文为Adam优化器在重尾随机噪声下的收敛性建立了首个理论保证，分析了其在有界p阶中心矩条件下的迭代复杂度与次优性。

Research relevance: 弱相关（1-2分）。论文是纯优化理论工作，讨论Adam优化器在重尾噪声下的收敛性分析，与遥感、卫星、目标检测、轻量化均无直接关系。唯一可能的间接联系是：Adam是深度学习训练中常用的优化器，其收敛性理论可能对遥感目标检测模型的训练稳定性有潜在参考价值，但论文本身不涉及任何遥感场景、检测任务或模型压缩/加速方法。

Reading priority: 1分。论文为纯优化理论工作，与遥感卫星在轨目标检测及模型轻量化方向基本无关，除非课题涉及优化器收敛性理论分析，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：非凸随机优化理论分析。应用场景：通用深度学习优化，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 不涉及具体网络结构。论文聚焦于Adam优化器（vector-form Adam）的理论分析，基于online-to-nonconvex conversion框架，推广到重尾鞅差噪声场景。

Lightweight method: 不涉及。论文不讨论剪枝、量化、知识蒸馏、NAS或任何模型压缩/加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明。论文为纯理论分析，不涉及具体数据集、评价指标或实验设置。

Experiments: 摘要未说明。论文主要贡献为理论收敛性证明，未提及实验对比或消融实验。

Limitations: 论文本身指出Adam在重尾噪声下存在次优迭代复杂度和p依赖的收敛性，即使在有界方差（p=2）情况下次优性仍然存在；仅当已知域半径并用于控制online-learner输出时，收敛率才达到最优复杂度。此外，作为纯理论工作，缺乏实际实验验证。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的直接借鉴意义有限。潜在间接参考：1) 遥感在轨训练/微调场景中，梯度噪声可能呈现重尾分布（如SAR图像噪声、极端光照条件），理解Adam在此类噪声下的收敛行为有助于选择更稳健的优化器；2) 若未来涉及在轨增量学习或联邦学习场景，优化器收敛性理论可作为训练稳定性设计的理论依据。但整体而言，该论文与本课题方向关联度很低。

Abstract: We establish the first convergence guarantees for the plain vector-form \emph{Adam} optimizer under heavy-tailed stochastic noise. While several Adam variants are known to achieve optimal iteration complexity in bounded-variance nonconvex optimization, little is understood about their behavior when stochastic gradients admit only a bounded $p$-th central moment for some $p \in (1,2]$, a setting increasingly observed in modern deep learning. To address this gap, we generalize the recent online-to-nonconvex conversion framework to accommodate heavy-tailed martingale-difference noise. Building on this generalized framework, we develop a discounted regret analysis for Adam, without restrictive parameter coupling. Our results show that Adam converges to $(ρ,ε)$-stationary points under heavy-tailed noise. However, it exhibits a suboptimal iteration complexity and $p$-dependent convergence, a suboptimality that persists even in the bounded-variance case ($p=2$). When the domain radius is known and used to control the online-learner output, a standard setup in related literature, the convergence rate improves to match the optimal complexity. These findings provide new theoretical insight into the robustness and limitations of Adam in heavy-tailed regimes.

</details>


### [301] [FunL2O: LLM-Guided Feature Function Design for Learning to Optimize](https://arxiv.org/abs/2607.27389)
*Bingheng Li,Junyang Cai,Yupeng Zhang,Bistra Dilkina,Jayant Kalagnanam,Dzung T. Phan*

Main category: cs.LG

TL;DR: 提出FunL2O框架，利用LLM驱动的程序进化来自动化设计Learning-to-optimize (L2O)方法中的特征函数，替代手工特征设计，在连续和离散优化任务上均优于手工特征表示。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/优化领域，聚焦于Learning-to-optimize (L2O)中的特征自动设计，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然LLM驱动的自动化设计思想在方法论层面有一定借鉴潜力，但论文本身不涉及任何遥感场景、目标检测任务或模型压缩/加速技术。

Reading priority: 1分。论文与遥感卫星在轨目标检测与轻量化方向基本无关，属于优化与LLM交叉领域。除非对LLM驱动的自动化设计方法论有特别兴趣，否则不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Learning-to-optimize (L2O)中的特征函数自动设计；应用场景：线性规划（linear programming）、二次规划（quadratic programming）、混合整数优化（mixed-integer optimization）；不涉及遥感传感器、卫星平台或目标类别。

Model architecture: 核心框架为FunL2O，采用FunSearch风格的循环：LLM（大语言模型）提出可执行的特征函数，固定评估流程重新训练原始L2O模型并测量下游优化性能。涉及GNN-guided backdoor branching和Predict-and-Search等L2O方法。摘要未说明具体网络结构细节。

Lightweight method: 摘要未涉及任何模型压缩或加速方法（如pruning、quantization、knowledge distillation、NAS等）。论文关注的是特征表示设计的自动化，而非模型轻量化。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 评估任务包括：线性规划与二次规划上的solution prediction和warm-starting任务，以及混合整数优化上的GNN-guided backdoor branching和Predict-and-Search任务。使用4种不同的LLM进行实验。摘要未说明具体数据集名称和量化指标。

Experiments: 在连续优化（线性/二次规划）和离散优化（混合整数优化）任务上，使用4种LLM进行特征进化实验，进化得到的特征一致优于手工设计的特征表示。摘要未提供具体数值对比结果。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关；2. 摘要未说明特征进化过程的计算开销和收敛效率；3. 未讨论LLM驱动的特征进化在资源受限环境下的可行性；4. 未涉及任何视觉/图像任务，仅覆盖数学优化问题。

Ideas for my research: 尽管论文与遥感目标检测轻量化无直接关系，但LLM-driven程序进化（FunSearch风格）的自动化设计思想可借鉴到以下方向：1) 在轻量化网络设计中，可尝试用LLM自动搜索或进化轻量模块/剪枝策略，替代手工设计；2) 在知识蒸馏中，可考虑用LLM自动设计蒸馏特征映射函数；3) 在遥感目标检测的旋转框回归或小目标检测中，可借鉴自动化特征设计的思路来优化检测头或损失函数设计。但这些仅为方法论层面的间接启发，需自行验证在视觉任务上的可行性。

Abstract: Learning-to-optimize (L2O) methods accelerate repeated optimization by training models to predict solutions, warm starts, branching decisions, or other forms of solver guidance. A critical yet largely overlooked component of these pipelines is the feature function that maps problem instances to inputs for machine learning models. Existing L2O methods typically rely on hand-crafted features, making representation design manual and largely fixed across domains. We introduce FunL2O, the first unified framework for automating feature design through LLM-driven program evolution for L2O. In a FunSearch-style loop, an LLM proposes executable feature functions, while a fixed evaluation process retrains the original L2O model and measures downstream optimization performance. We evaluate FunL2O on linear and quadratic programming tasks involving solution prediction and warm-starting, as well as on mixed-integer optimization tasks using GNN-guided backdoor branching and Predict-and-Search. Across continuous and discrete optimization tasks and four LLMs, the evolved features consistently outperform hand-crafted representations. These results establish LLM-driven feature evolution as a general and effective approach to automating representation design in L2O.

</details>


### [302] [ECG-InterpBench: Benchmarking the Interpretability of ECG Foundation Models with Matched-Scale Sparse Autoencoders](https://arxiv.org/abs/2607.27404)
*Yixuan Duan,Wei Qiu*

Main category: cs.LG

TL;DR: 提出ECG-InterpBench基准，用稀疏自编码器系统评估心电信号基础模型内部表征的可解释性，构建450单元可解释性图谱。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦心电信号（ECG）基础模型的可解释性评估，属于生物医学信号处理领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感相关场景，也不涉及目标检测、小目标检测或旋转框检测，更不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理或边缘/星上部署。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于ECG基础模型可解释性评估领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：ECG基础模型表征可解释性评估基准构建。应用场景：心电信号分析、临床可解释性评估。传感器类型：心电信号（非遥感传感器）。目标类别：49个临床ECG测量指标（非目标检测类别）。

Model architecture: 评估对象为六个冻结的ECG foundation models；评估工具为sparse autoencoders（稀疏自编码器），作为标准化测量仪器。覆盖五种标准化encoder深度、三种匹配的字典宽度、三个随机种子，构成450-cell可解释性图谱。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS或轻量网络设计等模型压缩/加速方法。唯一相关的技术是sparse autoencoders，但其用途是表征可解释性测量工具，而非模型轻量化手段。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息，也未涉及星上或边缘部署场景。

Datasets and metrics: 数据集：MIMIC-IV-ECG（用于匹配复现验证）。评价指标：稀疏重建保真度（sparse reconstruction fidelity）、单特征可访问性与49个临床ECG测量的覆盖率（single-feature accessibility and coverage）、跨种子特征可复现性（cross-seed feature reproducibility）、患者采样不确定性、深度与种子依赖变化、对稀疏参数化的敏感性。

Experiments: 评估六个冻结ECG基础模型，跨五种encoder深度、三种字典宽度、三个随机种子，构建75个精确匹配的六模型对比块（共450单元）。在MIMIC-IV-ECG上进行匹配复现，发现重建保真度与临床可访问性识别出不同的领先模型。附带可执行评估代码、标准化清单、单元级指标和可复现性审计。

Limitations: 摘要未说明局限性。但就本研究方向而言，该论文与遥感目标检测和轻量化完全无关，属于生物医学信号处理领域，无可借鉴的遥感或部署相关内容。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题基本无可借鉴点。唯一可能间接相关的思路是：论文采用"标准化测量仪器+容量匹配"来公平对比不同模型的方法论，这种"受控对比"思想在遥感轻量化模型对比实验中（如公平比较不同剪枝/蒸馏方法）有一定方法论参考价值，但属于泛化方法论层面的间接启发，而非直接技术借鉴。

Abstract: Existing benchmarks for electrocardiogram foundation models primarily evaluate downstream predictive performance, providing limited insight into whether their internal representations can be faithfully decomposed, clinically interpreted, or reproduced across independent analyses. We introduce ECG-InterpBench, a benchmark designed to systematically evaluate the interpretability of ECG foundation-model representations. ECG-InterpBench uses sparse autoencoders as standardized measurement instruments and matches their capacity across models to enable controlled comparisons. We evaluate six frozen ECG foundation models across five standardized encoder depths, five matched dictionary widths, and three random seeds, producing a 450-cell interpretability atlas comprising 75 exactly matched six-model comparison blocks. The benchmark evaluates complementary dimensions of representation interpretability, including sparse reconstruction fidelity, single-feature accessibility and coverage of 49 clinically meaningful ECG measurements, and cross-seed feature reproducibility. The evaluation further quantifies patient-sampling uncertainty, depth- and seed-dependent variation, and sensitivity to the sparsity parameterization. The benchmark reveals that ECG foundation models exhibit distinct interpretability profiles. A matched replication on MIMIC-IV-ECG confirms that reconstruction fidelity and clinical accessibility identify different leading models. The benchmark is accompanied by executable evaluation code, standardized manifests, cell-level metrics, and reproducibility audits. ECG-InterpBench complements performance-centered ECG benchmarks by providing a capacity-controlled and reproducible framework for comparing ECG foundation models across distinct dimensions of representation interpretability.

</details>


### [303] [Context-Informed Ship Trajectory Prediction via Conditional Attention](https://arxiv.org/abs/2607.27418)
*Yuan Guan,Chandler Squires,Timothy Hu,Pradeep Ravikumar*

Main category: cs.LG

TL;DR: 提出Conditional Informer，一种将船舶轨迹预测建模为条件生成任务的编码器-解码器架构，通过Conditional Attention机制显式编码"天气调制但不生成船舶动态"的物理先验，并引入Modality Masking训练策略防止传感器回退时的灾难性退化。

Research relevance: 弱相关（2分）。论文涉及船舶轨迹预测（ship trajectory prediction），使用AIS和ERA5数据，属于海事/海洋场景而非遥感卫星场景。虽然船舶检测与遥感目标检测有一定关联，但本文核心是轨迹预测（trajectory prediction）而非目标检测（object detection），且完全不涉及遥感图像、卫星在轨智能或模型轻量化。属于泛机器学习/时序预测方法，对遥感目标检测轻量化课题借鉴意义有限。

Reading priority: 2分。论文与遥感卫星目标检测及轻量化方向弱相关，核心是海事轨迹预测而非遥感图像目标检测，且不涉及任何模型压缩/加速方法。仅Conditional Attention和Modality Masking两个设计思路对多模态遥感检测的鲁棒性训练有一定启发，但整体优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：长期船舶轨迹预测（long-term ship trajectory prediction）；应用场景：海事安全（maritime safety）与自主导航（autonomous navigation）；传感器/数据：AIS（船舶自动识别系统）轨迹数据 + ERA5（气象再分析）环境数据；目标类别：船舶运动状态（非图像目标检测）。

Model architecture: Conditional Informer：基于Transformer的编码器-解码器架构。核心模块为Conditional Attention机制，通过cross-attention让船舶状态显式查询环境上下文（天气等），编码"天气调制但不生成船舶动态"的物理方向性依赖。另引入Modality Masking训练策略，模拟传感器回退场景，防止模型在部分模态缺失时发生灾难性退化（catastrophic degradation）和捷径学习（shortcut learning）。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。

Onboard deployability: 摘要未说明。未报告参数量（parameters）、FLOPs、延迟（latency）、显存（memory）、能耗（energy consumption）或实时推理（real-time inference）等部署相关信息。Transformer架构通常计算开销较大，但本文未讨论边缘/星上部署可行性。

Datasets and metrics: 数据集：AIS（船舶轨迹数据）和ERA5（气象环境数据）。评价指标：预测精度（prediction accuracy），具体指标名称摘要未说明。实验对比了kinematic基线、concatenation-based基线以及无约束模型（unconstrained models）。

Experiments: 主要结果：在上下文（环境数据）可用时，相比kinematic和concatenation-based基线，预测精度提升15.4%；Modality Masking策略相比无约束模型将传感器回退误差（fallback error）降低近一个数量级（nearly an order of magnitude）。

Limitations: 1. 与遥感卫星目标检测及轻量化方向基本无关，属于海事时序预测领域；2. 摘要未说明模型参数量、计算复杂度、推理速度等部署指标；3. 未涉及图像/遥感影像数据，不涉及目标检测任务；4. 未讨论任何模型压缩或加速方法；5. 实验规模、消融细节、泛化性分析摘要未说明。

Ideas for my research: 1. Conditional Attention的思想可借鉴：在遥感目标检测中，如果融合气象/环境上下文（如云层遮挡、光照条件）辅助检测，可借鉴"状态显式查询上下文"的cross-attention设计，编码环境调制目标可见性的物理先验；2. Modality Masking训练策略对在轨场景有启发：星上传感器可能因故障或资源受限而回退到低分辨率/单波段模式，类似的多模态掩码训练可增强模型在传感器退化时的鲁棒性；3. 但整体而言，本文方法迁移到遥感目标检测需要较大改造，且未涉及轻量化，借鉴价值有限。

Abstract: Long-term ship trajectory prediction is a fundamental capability for maritime safety and autonomous navigation. While recent Transformer-based architectures have improved forecasting horizons, they predominantly rely on historical kinematic states, treating vessel motion as an isolated system. In reality, maritime navigation is profoundly modulated by extrinsic factors like weather and constrained by static vessel characteristics. Existing multimodal approaches fundamentally model the joint distribution over states and contexts, treating environmental variables as peer features rather than encoding the directional physical dependence of vessel dynamics on environmental conditions.
  In this work, we propose the Conditional Informer, a novel encoder-decoder architecture that formulates trajectory prediction as a conditional generation task. We employ a dedicated Conditional Attention mechanism where the vessel state explicitly queries environmental contexts through cross-attention, encoding the physical prior that weather modulates - but is not generated by - vessel dynamics. Furthermore, to address the intermittency of real-world data, we introduce a Modality Masking training strategy to prevent catastrophic degradation during sensor fallback. Extensive experiments on AIS and ERA5 data demonstrate that our approach outperforms kinematic and concatenation-based baselines by 15.4% in prediction accuracy when context is available. Crucially, Modality Masking prevents shortcut learning, reducing fallback error by nearly an order of magnitude compared to unconstrained models.

</details>


### [304] [Good Rankers, Bad Objectives: Bilinear Contrastive Critics under Expressive Policy Search](https://arxiv.org/abs/2607.27422)
*Ayushman Singh,Siddharth Aphale*

Main category: cs.LG

TL;DR: 本文研究对比学习critic（contrastive critic）在动作选择（best-of-K selection, planning, critic-guided generation）中作为价值目标时的安全性问题，指出无界双线性分数和余弦约束都无法避免选择离支持集（off-support）动作，并对比了Bellman训练的TD-Q在价值排序上的优势。

Research relevance: 1分（基本无关）。论文主题是强化学习中的对比学习critic价值校准问题，涉及navigation/manipulation任务（PointMaze, AntMaze, HumanoidMaze），与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署方法。

Reading priority: 1分。论文主题为强化学习对比critic的价值校准，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：强化学习中的动作选择与价值估计（best-of-K selection, planning, critic-guided generation）；应用场景：navigation和manipulation任务（PointMaze, AntMaze, HumanoidMaze, OGBench navigation tasks）；传感器类型：不适用（非视觉感知任务）；目标类别：不适用。

Model architecture: 对比学习critic（contrastive critic）模型，采用bilinear score结构，对比了cosine bounding、hybrid critic以及Bellman训练的TD-Q。摘要未说明具体网络架构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或轻量化方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：OGBench navigation tasks（四个任务）、PointMaze、AntMaze、HumanoidMaze；评价指标：对比分数校准性（calibration）、top score decile排序、regret、单步选择成本（single-step selection costs）、null检验功效（powered nulls）。

Experiments: 主要实验发现：1) 无界bilinear分数中embedding norm漂移导致off-support值膨胀，cosine bounding无法消除该失败；2) cosine和hybrid critic在多数池中仍选择off-support动作且regret相当；3) 对比分数在top score decile中弱校准或倒置，无法按价值排序固定查询动作；4) Bellman训练的TD-Q在参数匹配的函数类控制中成功排序；5) 模拟器rollout显示PointMaze和exact-Q* toy上存在单步选择成本，而AntMaze和HumanoidMaze上控制器可自校正。

Limitations: 论文聚焦于强化学习critic的价值校准问题，与遥感目标检测和轻量化方向完全无关。摘要未说明任何与遥感图像、目标检测、模型压缩或边缘部署相关的信息。作为泛机器学习方法，其关于对比学习分数校准性的理论分析可能对理解对比学习在检测任务中的表征质量有间接参考价值，但关联性很弱。

Ideas for my research: 尽管与遥感目标检测轻量化方向弱相关，但可借鉴的点有限：1) 论文揭示对比学习分数存在norm drift导致的假阳性问题，这提示在遥感目标检测中使用对比学习损失（如SimCLR、MoCo等自监督预训练）时，需注意embedding norm对检测置信度的影响；2) 关于训练目标与推理时归一化之间不一致的分析思路，可类比到检测模型训练与推理部署（如量化后）之间的分布漂移问题。但总体借鉴价值很低。

Abstract: Good action rankings do not make a contrastive critic safe to maximize. These critics increasingly act as value-like objectives for best-of-$K$ selection, planning, and critic-guided generation. Unbounded bilinear scores can let large embedding norms inflate off-support values, but cosine bounding does not remove the failure. A controlled support decomposition attributes most raw bilinear regret to norm drift. Cosine and hybrid critics nevertheless select off-support actions from most pools and incur comparable regret. Contrastive scores are weakly calibrated or inverted in the top score decile across four OGBench navigation tasks, and they fail to order fixed-query actions by value. Bellman-trained TD-Q succeeds, including in a parameter-matched function-class control. Realized costs depend on the task: simulator rollouts reveal single-step selection costs on PointMaze and the exact-$Q^*$ toy but well-powered nulls on AntMaze and HumanoidMaze, where the controller can self-correct. A training/readout decomposition traces the lost ordering to the cosine training objective; raw-trained embeddings retain weak ordering after inference-time normalization. Candidate maximization can therefore exploit false positives caused by norm drift, score saturation, or in-support misranking. Contrastive critics remain useful compatibility rankers on navigation and manipulation tasks, but action selection requires a value-calibrated scalar.

</details>


### [305] [SE(3)-MeanFlow: Few-Step Protein Backbone Generation on Lie Groups](https://arxiv.org/abs/2607.27431)
*Yikun Bai,Binghang Lu,Yikai Liu,Elaheh Akbari,Soheil Kolouri,Linxuan Wang,Ping He,Shuchan Wang,Ruqi Zhang,Guang Lin*

Main category: cs.LG

TL;DR: 提出SE(3)-MeanFlow，一种在蛋白质骨架生成的SE(3)李群几何上扩展MeanFlow的少步生成框架，通过闭式平均速度恒等式实现无模拟训练，并引入alpha-Flow目标与rectification后训练，在少步采样下超越需要更多采样步的flow-matching基线。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文聚焦蛋白质骨架的生成式建模（protein backbone generation），属于计算生物学/结构生物学领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感相关场景，也不涉及目标检测任务，更不涉及剪枝、量化、知识蒸馏、NAS等模型轻量化方法。唯一的间接关联是"少步采样/减少网络评估次数"这一推理加速思想，但其应用场景与目标检测完全不同。

Reading priority: 1分。论文属于蛋白质生成建模领域，与遥感卫星在轨目标检测和模型轻量化方向基本无关，不建议投入阅读时间。仅当对"少步生成/推理加速"的通用方法论感兴趣时，可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：蛋白质骨架的de novo生成建模（generative modeling of protein backbones）。应用场景：蛋白质设计与结构预测，属于计算生物学。传感器类型：不适用（非遥感传感器）。目标类别：蛋白质骨架（protein backbone frames），非遥感目标。

Model architecture: SE(3)-MeanFlow：将MeanFlow从欧氏空间扩展到蛋白质frame的SE(3)李群几何。核心设计包括：(1) 在so(3)李代数和R^3中推导旋转和平移的闭式平均速度恒等式，实现simulation-free训练目标；(2) SE(3) alpha-Flow目标，去除旋转分支的Jacobian-vector product，作为warm-up阶段；(3) 训练切换到small-t stabilized MeanFlow loss用于预训练和rectification-based post-training。

Lightweight method: 摘要未明确涉及传统模型压缩方法（剪枝/量化/蒸馏/NAS）。但其核心贡献是"few-step generative framework"，即通过减少ODE数值积分所需的网络评估次数（从数百次降到少步）来加速推理，这与轻量化/推理加速的目标一致，但方法层面属于生成模型的采样步数压缩，而非网络结构压缩或部署优化。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明。论文关注的是蛋白质生成的高通量设计（high-throughput design campaigns）中的推理瓶颈，即ODE积分需要数百次网络评估和Lie group指数映射。虽然"few-step"减少了推理开销，但未提供参数量、FLOPs、显存、延迟、能耗等任何星上/边缘部署相关数据，且应用场景与在轨遥感目标检测无关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面，摘要提到与flow-matching基线对比采样质量（quality）和多样性（diversity），并比较不同采样步数（sampling steps）下的表现，但未给出具体指标名称（如RMSD、TM-score等）。

Experiments: 主要实验结论：在蛋白质骨架生成中，SE(3)-MeanFlow在匹配或超越使用数倍采样步数的flow-matching基线；在少步（few-step）场景下优势更明显，rectification使其在每一个匹配的采样预算下都领先，但以一定的多样性损失为代价。摘要未给出具体数值结果和消融实验细节。

Limitations: (1) 与遥感/卫星/目标检测/轻量化部署完全无关，属于蛋白质结构生成领域；(2) 摘要未提供参数量、FLOPs、推理延迟等部署指标；(3) 以多样性损失为代价换取少步采样优势；(4) 方法针对SE(3)李群上的蛋白质frame几何，难以直接迁移到遥感目标检测任务；(5) 摘要未说明实际硬件部署或实时性验证。

Ideas for my research: 尽管场景完全不同，但有两个思想可借鉴：(1) "few-step推理加速"理念——通过减少网络前向评估次数来降低推理开销，这与在轨目标检测中追求低延迟、低能耗的目标一致，可类比思考如何减少检测模型的前向推理次数或采用early-exit策略；(2) "rectification-based post-training"——通过后训练优化来提升少步/低计算预算下的性能，可类比为在轻量化模型（剪枝/量化后）上做蒸馏或微调以恢复精度。但需注意，这些只是方法论层面的类比，论文本身对遥感在轨目标检测课题的直接借鉴价值很低。

Abstract: Generative modeling of protein backbones promises the de novo design of proteins with prescribed structural and functional properties. Existing diffusion and flow-matching models produce high-quality backbones on SE(3)^N, but inference requires numerically integrating an ODE over hundreds of network evaluations, each involving a Lie group exponential map - a bottleneck for high-throughput design campaigns. We introduce SE(3)-MeanFlow, a few-step generative framework that extends MeanFlow from Euclidean space to the Lie group geometry of protein frames. Working natively in the Lie algebra so(3) and in R^3, we derive closed-form average-velocity identities for rotations and translations, giving simulation-free training targets. We further introduce an SE(3) alpha-Flow objective that removes the Jacobian-vector product from the rotation branch and serves as a warm-up stage, after which training switches to a small-t stabilized MeanFlow loss that is used for the remainder of pretraining and for rectification-based post-training. In protein backbone generation, SE(3)-MeanFlow matches or exceeds flow-matching baselines that use several times more sampling steps, and its advantage widens in the few-step regime, where rectification lets it lead at every matched budget - at a modest cost in diversity.

</details>


### [306] [Neural Network-Assisted CLEAN for Channel Modeling in Low-SNR Regimes](https://arxiv.org/abs/2607.27450)
*Chaofan Deng,Linyu Sun,Jaeho Lee,Arijit Raychowdhury*

Main category: cs.LG

TL;DR: 提出NN-CLEAN混合框架，将多头残差网络嵌入CLEAN迭代提取循环，用快速并行前向传播替代穷举网格搜索，在低信噪比下实现高精度多径参数估计并大幅降低计算复杂度。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦无线通信MIMO系统的多径信道参数估计（channel estimation），属于无线通信信号处理领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关联。虽然涉及深度学习加速和计算复杂度降低，但并非面向遥感目标检测场景，也不涉及轻量化模型压缩方法（剪枝/量化/蒸馏/NAS等）。

Reading priority: 1分。论文属于无线通信信道估计领域，与遥感卫星在轨目标检测及轻量化方向基本无关，仅"混合物理+学习"和"并行化加速"的宏观思路有微弱借鉴价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多径信道参数估计（multipath parameter estimation）；应用场景：现代无线通信系统、MIMO系统；传感器类型：无线通信接收信号；目标类别：多径信道参数（非视觉目标）。

Model architecture: NN-CLEAN混合框架：将多头残差网络（multi-head residual network）嵌入传统CLEAN迭代提取循环中，用神经网络前向传播替代穷举网格搜索，残差减法仍由精确数学模型完成。

Lightweight method: 摘要未明确涉及传统轻量化方法（剪枝/量化/蒸馏/NAS）。但通过"用神经网络前向传播替代穷举网格搜索"实现了计算复杂度的显著降低，并展现出随batch size增大近乎平坦的运行时和内存扩展（near-flat scaling），具备高效并行化特性，可视为一种算法层面的计算加速策略。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等具体部署指标。仅提及"massive reduction in computational complexity"以及"near-flat scaling in execution runtime and memory consumption as batch sizes increase"，表明其并行化效率高，适合实时处理，但未给出量化数据。

Datasets and metrics: 采用Monte Carlo仿真（Monte Carlo simulations）进行实验验证；评价指标为估计精度（estimation accuracy），在5 dB SNR下超过96%；对比方法包括Grid-Search CLEAN (GS-CLEAN)、子空间方法（subspace methods）和独立单次神经网络（standalone one-shot neural networks）。

Experiments: Monte Carlo仿真表明：NN-CLEAN在5 dB SNR下估计精度超过96%，匹配传统GS-CLEAN基线；计算复杂度大幅降低；显著优于子空间方法和独立单次神经网络；随batch size增大，执行运行时和内存消耗呈近平坦扩展。

Limitations: 摘要未说明：模型参数量、FLOPs、具体推理延迟、部署平台（FPGA/NPU等）；未讨论在遥感/视觉任务上的泛化能力；未涉及多径密度变化和off-grid参数下的详细鲁棒性分析；未给出与传统轻量化方法（剪枝/量化/蒸馏）的结合方案。

Ideas for my research: 尽管场景不同，但"将神经网络嵌入传统迭代算法以替代穷举搜索"的混合思路（hybrid physics-informed + learning）对遥感在轨目标检测有一定借鉴意义：1）可借鉴"物理先验+深度学习"的混合范式，例如将传统CFAR或模板匹配与轻量检测网络结合，减少计算开销；2）"near-flat scaling"的并行化设计思路可启发在轨多路并行推理的架构设计；3）用网络前向传播替代高复杂度搜索的思想，可类比于在遥感大图中用轻量网络替代滑窗穷举搜索来加速小目标检测。

Abstract: Accurate multipath parameter estimation is critical for modern wireless communication systems, particularly in challenging low-SNR environments. Traditional Maximum Likelihood Estimation algorithms, such as CLEAN, provide high-resolution parameter extraction but suffer from prohibitive computational complexity due to exhaustive grid search. Conversely, purely data-driven deep learning approaches lack physical grounding and struggle to generalize across variable multipath densities and off-grid parameters. To address these limitations, this paper proposes Neural Network-Assisted CLEAN (NN-CLEAN), a hybrid framework that embeds a multi-head residual network directly into the iterative CLEAN extraction loop. By replacing the exhaustive grid search with rapid, parallelizable forward passes while delegating residual subtraction to exact mathematical models, NN-CLEAN isolates physical multipath parameters without accumulating non- physical errors. Extensive Monte Carlo simulations demonstrate that NN-CLEAN achieves estimation accuracy exceeding 96% at 5 dB SNR, matching the traditional Grid-Search CLEAN (GS- CLEAN) baseline, while providing a massive reduction in computational complexity and substantially outperforming subspace methods and standalone one-shot neural networks. Crucially, NN-CLEAN exhibits a near-flat scaling in execution runtime and memory consumption as batch sizes increase. This highly efficient parallelization establishes NN-CLEAN as a robust, real- time solution for channel estimation in MIMO systems.

</details>


### [307] [ODEWorld: A Continuous Predictive Architecture via Physical-Time Flow](https://arxiv.org/abs/2607.27924)
*Dongxiu Liu,Haoyi Niu,Peng Cheng,Yuan Gao,Xirui Kang,Sangli Teng,Koushil Sreenath,Xianyuan Zhan*

Main category: cs.LG

TL;DR: 提出PT-Flow和ODEWorld，一种基于连续时间潜空间ODE的world model，用于视频生成和机器人控制，解决离散时间预测的低效与表示坍缩问题。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/世界模型（world model）领域，聚焦连续时间动力学建模、视频生成与机器人控制，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然其"连续时间ODE建模"和"潜空间压缩"思想在方法论层面可能对时序遥感数据建模有潜在启发，但论文本身不涉及遥感场景、目标检测任务或任何模型压缩/加速技术。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于world model/视频生成/机器人控制领域。除非课题涉及遥感时序数据的连续时间建模，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：连续时间world model / 视频生成 / 机器人控制（policy learning）。应用场景为物理世界动力学建模，非遥感场景。传感器类型：摘要未说明（推测为机器人视觉/视频数据）。目标类别：不涉及目标检测，无具体目标类别。

Model architecture: 核心架构为ODEWorld，一种continuous-time latent world model。关键组件包括：(1) PT-Flow（Physical-Time Flow），学习在物理时间中运行的连续潜速度场；(2) 用ODE（ordinary differential equation）参数化序列数据的底层动力学，嵌入结构化表示空间；(3) 通过ODE solver在压缩潜空间中进行时间积分实现未来预测；(4) 提取time-variant特征并在动力学表示空间和潜速度场上施加ODE约束，以解决latent world model中的representation collapse问题。

Lightweight method: 摘要未明确涉及传统轻量化方法（pruning, quantization, knowledge distillation, NAS等）。但"在压缩潜空间（compressed latent space）中进行时间积分"这一设计本身具有计算效率优化的意图，属于模型层面的效率设计而非显式压缩技术。摘要未说明参数量、FLOPs等部署指标。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。ODE solver的迭代求解在计算上可能带来额外开销，且该工作面向视频生成与机器人控制，未考虑星上/边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集名称。实验涵盖video generation和robotic control两类任务，评价指标摘要未明确列出（推测为视频生成质量指标如FVD/PSNR及机器人控制成功率等，但摘要未说明）。

Experiments: 摘要仅概述实验结论：ODEWorld在video generation和robotic control上均表现优异，成功调和了planning-conducive dynamics abstraction与visual realism。具体对比方法、消融实验细节摘要未说明。

Limitations: (1) 与遥感/卫星/目标检测/轻量化完全无关，属于泛机器学习方向；(2) 摘要未提供任何部署相关指标（参数量、FLOPs、延迟等）；(3) ODE solver的数值积分可能带来推理延迟，实时性存疑；(4) 未讨论在遥感时序数据（如多光谱/高光谱时间序列）上的适用性；(5) 摘要未说明模型规模与训练成本。

Ideas for my research: 尽管与遥感目标检测轻量化无直接关系，但以下思想可能有间接借鉴价值：(1) 连续时间ODE建模思想可启发遥感时序数据（如多时相变化检测、视频卫星目标跟踪）的建模方式，将离散帧预测转化为连续时间积分；(2) "压缩潜空间中进行预测"的设计思路与轻量化中"在低维特征空间做推理以减少计算量"的理念相通，可借鉴到遥感目标检测backbone的轻量化设计中；(3) 解决representation collapse的ODE约束方法，对长时间序列遥感特征提取的稳定性有参考意义。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接可借鉴点有限。

Abstract: In the physical world we inhabit, space and time are fundamentally continuous. However, existing machine learning paradigms for world modeling are largely confined to discrete-time prediction, thereby exhibiting significant inefficiency in capturing the dynamics of physical world. We introduce Physical-Time Flow (\textbf{PT-Flow}), a novel approach that learns a continuous latent velocity field operating in physical time. Crucially, the underlying dynamics of sequential data are parameterized by an ordinary differential equation (ODE) embedded in a well-structured representation space. Under this paradigm, the prediction of future can be recast as temporal integration via an ODE solver in the compressed latent space. Building upon PT-Flow, we construct \textbf{ODEWorld}, a continuous-time latent world model that is both efficient and versatile. By extracting time-variant features and enforcing ODE properties on both the dynamical representation space and the latent velocity field, ODEWorld effectively addresses the long-standing representation collapse issue in latent world model literature. This also enables high-quality image reconstruction even after long-horizon prediction. Moreover, its continuous nature allows for arbitrary temporal resolution and even backward prediction, which is impossible for most discrete-time models. Lastly, ODEWorld can provide rich planning-oriented information to facilitate downstream policy learning. Comprehensive experiments demonstrate that ODEWorld successfully reconciles planning-conducive dynamics abstraction with visual realism, excelling in both video generation and robotic control. \href{https://dstate.github.io/odeworld_website/}{Project Website}.

</details>


### [308] [FADEx: Feature Attribution and Distortion-based Explanation of Dimensionality Reduction](https://arxiv.org/abs/2607.27463)
*Lucas Greff Meneses,Evandro S. Ortigossa,Claudio Silva,Luis Gustavo Nonato*

Main category: cs.LG

TL;DR: 提出FADEx，一种基于一阶泰勒展开和奇异值分解的局部逐实例特征归因方法，用于解释非线性降维（DR）方法的结果，通过加权最小二乘计算局部线性模型，实现对DR方法无关的解释和失真分析。

Research relevance: 弱相关（2分）。论文聚焦于高维数据降维（Dimensionality Reduction）的可解释性，属于泛机器学习/数据可视化领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然降维技术可能间接用于遥感特征分析，但论文本身不涉及遥感场景、目标检测或模型压缩。

Reading priority: 1分。论文主题为降维可解释性，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅存在非常间接的方法论借鉴可能，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：降维方法（DR）的可解释性分析，具体为局部逐实例特征归因（local per-instance feature attribution）。应用场景：高维数据探索、机器学习潜在空间分析、复杂黑盒模型解释。传感器类型：不适用（非遥感）。目标类别：不适用。

Model architecture: FADEx方法核心组件：1）一阶泰勒展开（first-order Taylor expansion）进行局部线性近似；2）奇异值分解（Singular Value Decomposition, SVD）；3）加权最小二乘（weighted least squares）计算局部线性模型；4）无需out-of-sample数据映射，对DR方法无关（agnostic）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。其核心贡献是解释性方法而非轻量化方法。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及通过定性（qualitative）和定量（quantitative）评估、与现有方法对比以及案例研究（case studies）来验证FADEx的有效性和通用性。

Experiments: 摘要未说明具体实验细节。提及与现有DR解释方法进行对比，结果显示FADEx在多个方面优于现有方法，提供稳健可靠的解释。但未给出具体对比方法名称和量化结果。

Limitations: 1）论文与遥感、卫星、目标检测、模型轻量化完全无关，属于数据可视化/可解释机器学习方向；2）摘要未说明计算复杂度、运行效率等关键性能指标；3）未说明在真实遥感数据上的适用性；4）局部线性近似的精度在高维非线性流形上可能受限，摘要未讨论该局限。

Ideas for my research: 尽管论文与遥感目标检测轻量化无直接关系，但存在间接借鉴点：1）FADEx的局部线性近似思想可用于解释轻量化模型（如剪枝/量化后）在遥感目标检测中的特征空间变化，帮助理解压缩对特征表征的影响；2）SVD和加权最小二乘可用于分析轻量化前后模型潜在空间的失真程度，辅助评估压缩质量；3）DR解释方法可用于可视化遥感高光谱/多光谱数据的降维结果，辅助理解特征选择对检测性能的影响。但整体借鉴价值有限。

Abstract: Dimensionality Reduction (DR) is a fundamental tool for high-dimensional data exploration, reducing the complexity of latent spaces of machine learning models, and assisting in the explanation of complex opaque models. However, non-linear DR techniques often function as opaque transformations themselves, making it challenging to understand how individual features influence instance positioning in the reduced space. This lack of transparency complicates the analysis and interpretation of structural patterns, hindering the ability to reason about the organization of high-dimensional data based on the projected layout. In order to address this challenge, dimensionality reduction explanation methods have shown promise in improving the understanding of the observed groups and cluster structures. Unfortunately, existing DR explanation approaches tend to suffer from limitations such as multiple attributions per feature and restricted applicability to specific dimensionality reduction methods, which hinder their use. In this work, we propose FADEx, a novel local per-instance feature attribution method that leverages local linear approximation via first-order Taylor expansion and Singular Value Decomposition to provide explanations. FADEx computes the local linear models via weighted least squares, eliminating the need for out-of-sample data mapping, making it agnostic to the DR method, while simultaneously providing local feature attributions and distortion analysis. Through qualitative and quantitative evaluations, comparisons with existing methods, and case studies, we demonstrate FADEx's effectiveness and versatility in providing explanations and analytical resources for analyzing the behavior of DR methods. The results indicate FADEx yields robust and reliable explanations, outperforming existing approaches in several aspects.

</details>


### [309] [Schreier-Coset Graph Rewiring](https://arxiv.org/abs/2607.27479)
*Aryan Mishra,Randy Martinez,Lizhen Lin*

Main category: cs.LG

TL;DR: 提出一种基于群论的图重连方法 Schreier-Coset Graph Rewiring (SCGR)，通过特殊线性群构造 Schreier-Coset 图来缓解 GNN 中的 over-squashing 问题，降低有效电阻并提升长程信息传播能力。

Research relevance: 1 分。该论文是图神经网络（GNN）领域的图拓扑重连方法研究，与遥感、卫星、目标检测、轻量化部署均无直接关联。它既不是遥感场景，也不涉及目标检测，更不涉及模型压缩或加速。属于泛机器学习/图学习方向的弱相关论文。

Reading priority: 1 分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于纯图学习理论方法研究，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图神经网络中的长程信息传播优化（over-squashing 缓解）。应用场景：通用图学习任务，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 提出 SCGR 方法，利用特殊线性群（special linear group）构造 Schreier-Coset 图，将其与输入图进行增强（augment），形成低有效电阻的旁路（bypass）用于长程通信。方法具有理论保证，能提供谱间隙（spectral gap）和有界有效电阻（bounded effective resistance）。

Lightweight method: 摘要未说明。该论文不涉及剪枝、量化、知识蒸馏、NAS 等模型压缩或加速方法。虽然图重连可能影响计算复杂度，但论文并未从轻量化角度讨论。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该方法面向通用图学习，与星上/边缘部署场景无关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括有效电阻（effective resistance）降低幅度（5-40%）和任务准确率（competitive accuracy）。

Experiments: 实验表明 SCGR 在多种学习任务上将有效电阻降低 5-40%，有效缓解连接瓶颈（connectivity bottlenecks），同时保持有竞争力的准确率。摘要未说明具体对比方法和消融实验细节。

Limitations: 摘要未说明。但可推断：1) 该方法仅针对图结构数据，适用范围有限；2) 群论构造的复杂度可能较高，实际工程落地成本未知；3) 未讨论计算开销和部署可行性；4) 与遥感目标检测场景完全脱节。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：如果未来在遥感场景中采用图结构建模（如场景图、目标关系图），可考虑用类似图重连方法改善长程依赖传播。但该论文与在轨遥感目标检测轻量化课题的核心方向（检测模型压缩、实时推理、星上部署）基本无关，不建议作为主要参考。

Abstract: The information flow in the graph neural networks (GNNs) is fundamentally constrained by over-squashing, where structural bottlenecks impede long range information propagation. Graph-rewiring methods, which modify graph topology, have been extensively used to alleviate this. However, existing approaches often introduce prohibitive structural and computational bottlenecks, fail to preserve the critical properties of original graphs, and increase the edge counts massively. We introduce a novel method Schreier-Coset Graph Rewiring , a group-theoretic rewiring method that augments the input graph with a Schreier-Coset graph derived from a special linear group. Our method provides theoretical guarantees, a graph that exhibits spectral gap and a bounded effective resistance, creating a low-resistance bypass for long-range communication. Empirical evaluations demonstrate that SCGR reduces effective resistance by 5-40% across various learning tasks, effectively mitigating connectivity bottlenecks while maintaining competitive accuracy.

</details>


### [310] [QQWorld: Quantile-Quantile Matching for World Model Regularization](https://arxiv.org/abs/2607.28415)
*Zhoushun Yu,Xiaoyu Hu,Xiangyu Xu*

Main category: cs.LG

TL;DR: 提出QQWorld，用分位数-分位数匹配目标替代Epps-Pulley目标，改进潜空间世界模型（LeWorldModel）的潜变量高斯对齐与尾部控制，提升规划成功率。

Research relevance: 弱相关（2分）。论文属于强化学习/世界模型（world model）领域的潜空间表征学习方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。唯一可能的间接借鉴点是其"潜空间分布正则化"思想，但与本课题方向关联很弱。

Reading priority: 1分。论文属于强化学习/世界模型方向，与遥感卫星在轨目标检测及轻量化研究基本无关，无直接可借鉴的检测方法或压缩技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：潜空间世界模型（latent world model）的规划（planning）与表征学习；应用场景：四个控制环境（control environments），属于强化学习benchmark；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 基于LeWorldModel（LeWM）架构，核心改进是用quantile-quantile（QQ）匹配目标替代Epps-Pulley（EP）目标来正则化潜变量分布；进一步提出cross-batch QQ，利用前一批次的detached样本扩大有效排序池（ranking pool）。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法，也未讨论推理速度、参数量或部署相关优化。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。且该工作面向强化学习控制环境，与星上/边缘部署场景无关。

Datasets and metrics: 数据集：四个控制环境（four control environments），具体名称摘要未说明；评价指标：平均规划成功率（average planning success rate）、高斯对齐程度（Gaussian alignment）、潜变量尾部厚度（latent tail thickness）。

Experiments: 在四个控制环境中，QQWorld相比LeWM有效提升了平均规划成功率，同时获得更好的高斯对齐和更薄的潜变量尾部。摘要未提供具体数值结果、对比方法细节或消融实验。

Limitations: 摘要未说明。但可推断：1）仅验证于控制环境，未涉及视觉/遥感数据；2）cross-batch QQ的bias-variance trade-off虽被刻画，但实际影响未量化；3）未讨论计算开销增加（QQ匹配涉及排序操作）对实时性的影响。

Ideas for my research: 借鉴价值有限。可能的间接启发：1）若在遥感目标检测中引入潜空间分布正则化（如让特征分布更接近高斯），可能有助于提升小目标特征的判别性，但需谨慎评估其计算开销；2）cross-batch利用历史样本的思想，可类比于检测训练中的memory bank或特征复用策略，但与本课题轻量化主线关联较弱。总体而言，不建议作为核心参考。

Abstract: Latent world models enable efficient planning by predicting future states in a compact representation space, but their performance depends critically on the quality of the learned latent distribution. LeWorldModel (LeWM) regularizes its latents toward an isotropic Gaussian using the Epps-Pulley (EP) objective. We show that the corrective gradients of EP rapidly vanish for isolated tail samples, leaving heavy-tailed deviations insufficiently controlled. To address this limitation, we propose QQWorld, which replaces EP with a quantile-quantile matching objective that directly aligns projected latent samples with rank-matched Gaussian quantiles, thereby maintaining effective corrective gradients in the tails. We further develop cross-batch QQ, which enlarges the effective ranking pool using detached samples from previous batches, and characterize its bias-variance trade-off. Across four control environments, QQWorld effectively improves the average planning success rate of LeWM, while consistently yielding better Gaussian alignment and thinner latent tails.

</details>


### [311] [Latent States in Neural Networks: Recovering the Temporal Structure of Drifting Data from Model Weights](https://arxiv.org/abs/2607.27482)
*Kevin Guan*

Main category: cs.LG

TL;DR: 本文研究如何从按时间顺序训练的模型权重轨迹中，通过隐马尔可夫模型（HMM）恢复数据流中的离散状态（regimes），并验证这些状态对跨时间窗口迁移泛化的价值。

Research relevance: 与本研究方向基本无关（1分）。论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能；也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解；更不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理或边缘部署。该论文属于时序数据流中的概念漂移（concept drift）与模型权重分析领域，应用场景为多模态虚假信息检测（Fakeddit）和情感分析（Yelp），与遥感卫星在轨目标检测及模型轻量化方向无直接关联。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于时序数据流概念漂移与模型权重分析领域。除非对"在轨模型权重漂移监测"这一边缘交叉方向感兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：时序数据流中的状态恢复（regime recovery）与跨窗口迁移泛化分析。应用场景：多模态虚假信息检测（multimodal misinformation detection，Fakeddit数据集）和情感分析（sentiment analysis，Yelp数据集）。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体分类器架构。方法核心是在连续时间窗口上训练分类器，然后对对齐后的模型权重轨迹拟合一个隐马尔可夫模型（HMM），以恢复潜在状态（latent states）来划分时间线为连贯的阶段（phases）。

Lightweight method: 不涉及。论文未提及任何模型压缩或加速方法，如剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：Fakeddit（多模态虚假信息检测）和 Yelp（情感分析）。评价指标：摘要未明确说明具体指标名称，但涉及跨窗口迁移泛化性能（within-state transfer advantage）、与时间邻近性对照（control for temporal proximity）、与等大小连续状态划分的对比、置换检验（permutation null）等分析手段。

Experiments: 主要实验发现：(1) 在两个数据集上，分类器对与其训练窗口共享HMM状态的窗口数据泛化更好，优于跨状态边界的窗口；(2) 这种within-state迁移优势在控制时间邻近性后仍然存在，且略优于朴素等大小连续状态划分；(3) 状态仅从模型权重估计，但与数据类别分布的变化相关性更强，而非与用于估计的权重空间几何相关；(4) 在残差化类别发散和滞后后，within-state优势在两个任务上均超过置换零假设；(5) 所有效应在两个任务上均复现，但在Yelp上减弱，因其标签分布更时间稳定。

Limitations: 摘要未说明具体局限性。但从内容可推断：(1) 论文聚焦于概念漂移下的状态恢复与迁移泛化，与遥感目标检测和模型轻量化完全无关；(2) 未涉及任何部署相关指标；(3) 方法依赖按时间顺序训练的模型权重轨迹，在遥感在轨场景中缺乏直接映射；(4) 摘要未说明分类器架构、训练细节和计算开销。

Ideas for my research: 尽管论文与遥感在轨目标检测及轻量化方向基本无关，但存在一个间接的、可借鉴的思考角度：在轨遥感目标检测模型可能面临数据分布随时间漂移（如季节变化、光照变化、传感器退化导致的数据分布偏移）的问题。本文提出的"从模型权重轨迹中恢复数据状态"的思想，理论上可用于在轨场景中监测模型权重漂移、判断数据分布是否发生状态切换，从而触发模型更新或重校准。但这属于边缘性的方法论借鉴，与核心的轻量化目标检测研究关联较弱。

Abstract: A temporally drifting data stream may pass through discrete regimes rather than changing continuously. We ask whether such regimes are recoverable from the weights of models trained on the stream, using a hidden Markov model (HMM) fit to the chronologically ordered trajectory of those weights. We study this question in two domains known to drift over time: multimodal misinformation detection, using the Fakeddit dataset; and sentiment analysis, using the Yelp dataset. We train classifiers on consecutive temporal windows and fit an HMM to the trajectory of their aligned weights, recovering latent states that partition each timeline into coherent phases. On both datasets, classifiers generalize better to data from windows sharing the state of their training window than to windows across state boundaries. This within-state transfer advantage survives a control for temporal proximity and modestly exceeds the advantage recovered by a naive partition into contiguous states of equal size. Although the states are estimated solely from model weights, they correlate more strongly with shifts in the data's class distribution than with the weight-space geometry used to estimate them. After class divergence and lag are residualized out, the within-state advantage exceeds its permutation null on both tasks, indicating that the states recover structure relevant to transfer beyond the data distribution. Every effect replicates on both tasks but is attenuated on Yelp, whose label distribution is more temporally stable.

</details>


### [312] [A Lightweight Foundation Model for Collider Physics with Multi-Domain Adaptation](https://arxiv.org/abs/2607.27501)
*Liangyu Wu,Qibin Liu,Alexander Yue,Julia Gonski*

Main category: cs.LG

TL;DR: 提出一个轻量级基础模型NEXUS，利用对撞机物理数据的无监督预训练，迁移到引力波、洪水预测、神经活动等其他科学领域的下游任务，并强调相比transformer的计算简单性和低功耗推理优势。

Research relevance: 弱相关（2分）。该论文属于泛科学机器学习/基础模型方法，不涉及遥感、卫星、地球观测、SAR、高光谱或多光谱等任何遥感场景，也不涉及目标检测任务。虽然论文强调"lightweight"和"power-efficient inference / edge applications"，但其轻量化讨论是针对科学实验数据的基础模型，与遥感在轨目标检测的轻量化场景完全不同。唯一可借鉴的是其"轻量基础模型+预训练迁移"的思路，但整体与本研究方向关联度很低。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，不涉及遥感场景、目标检测任务或经典模型压缩技术。仅"轻量基础模型+预训练迁移"的宏观思路有一定参考价值，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：基础模型（foundation model）预训练与下游任务迁移（kinematic regression, event classification）。应用场景：对撞机物理（Large Hadron Collider）、引力波、洪水预测、神经活动等科学领域。传感器类型：不涉及遥感传感器，使用charged particle track features。目标类别：不涉及目标检测，无遥感目标类别。

Model architecture: 全连接自编码器（fully connected autoencoder），约300万参数。采用无监督预训练方式，在大规模对撞机碰撞数据集上学习特征表示，下游任务基于预训练权重进行微调。论文对比了与transformer架构在同等规模下的计算复杂度。

Lightweight method: 论文的"轻量化"主要体现在模型架构选择上——使用全连接自编码器而非transformer，从而在同等规模下降低计算复杂度，实现power-efficient inference。摘要未说明具体使用剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）或NAS等模型压缩技术。

Onboard deployability: 摘要提到模型约300万参数，并强调相比transformer的计算简单性，可支持power-efficient inference和real-time/edge applications。但摘要未说明具体FLOPs、延迟、显存、能耗等量化指标，也未涉及星上部署（onboard deployment）或FPGA/NPU/TensorRT等硬件加速方案。

Datasets and metrics: 预训练数据集：Large Hadron Collider的大规模碰撞数据集（charged particle track features）。下游任务数据集：对撞机分析（kinematic regression, event classification）、引力波、洪水预测、神经活动数据。评价指标：摘要未明确说明具体指标，仅提到"improved accuracy with small labeled datasets"。

Experiments: 主要实验包括：1) 预训练模型在小标注数据集上的下游任务精度优于从零训练的等价架构；2) 通过latent space interpretation验证预训练收益；3) 将预训练迁移到引力波、洪水预测、神经活动等其他领域；4) 对比transformer在同等规模下的计算复杂度。

Limitations: 摘要未说明：1) 具体FLOPs、延迟、能耗等部署量化指标；2) 模型在遥感或目标检测任务上的表现；3) 与剪枝/量化/蒸馏等经典轻量化技术的结合；4) 全连接自编码器在图像/高维空间数据上的扩展性（遥感图像通常为高分辨率多通道数据，全连接架构可能难以直接适用）。

Ideas for my research: 1) "轻量基础模型+预训练迁移"的思路可借鉴——在遥感目标检测中，可考虑用轻量自编码器在无标注遥感数据上预训练，再迁移到小样本目标检测任务；2) 全连接自编码器在低维特征上的高效性提示：遥感在轨场景中可先对高维遥感图像做特征压缩，再用轻量网络处理；3) 论文强调"small labeled datasets"下的预训练收益，对遥感在轨场景中标注数据稀缺的问题有参考价值；4) 但整体借鉴价值有限，因为论文不涉及遥感图像的空间结构、目标检测任务和经典轻量化技术。

Abstract: We present a lightweight approach to foundation modeling (\textbf{NEXUS}) that leverages pre-trained learning from collider physics data towards out-of-domain tasks in other scientific datasets, using a fully connected autoencoder model with approximately 3 million parameters. The model pre-trains with no supervision over a large-scale collision dataset from the Large Hadron Collider modeled by charged particle track features. Downstream tasks for collider analyses, such as kinematic regression and event classification, are developed on pre-trained model weights and achieve improved accuracy with only small labeled datasets when compared to equivalent architectures trained from scratch. The benefits of pre-training are additionally investigated through latent space interpretation and application to other domains, including gravitational waves, flood forecasting, and neural activity. Furthermore, the relative computational simplicity of NEXUS is demonstrated compared to transformer approaches at comparable scale, opening the door to power-efficient inference and real-time or edge applications of foundation models in scientific experiments.

</details>


### [313] [Sparsity Induced Identifiability in Matrix Tri-Factorisation](https://arxiv.org/abs/2607.27507)
*Tingting Mu*

Main category: cs.LG

TL;DR: 本文首次建立了实数矩阵三因子分解（matrix tri-factorisation）中稀疏性诱导可辨识性的严格理论保证，通过新颖的分解策略将原问题转化为两个耦合的辅助分解问题，并推导了恢复保证、结构一致性等理论结果。

Research relevance: 弱相关（2分）。本文属于泛机器学习/数学理论方向，研究矩阵三因子分解的稀疏性理论，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然矩阵分解在数据压缩和降维方面有潜在应用，但论文本身不涉及遥感场景、目标检测任务或任何模型压缩/加速方法。

Reading priority: 1分。该论文是纯数学理论方向，与遥感卫星在轨目标检测及模型轻量化基本无关，仅在矩阵分解作为数据压缩工具的间接层面有微弱关联，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：矩阵三因子分解（matrix tri-factorisation）的理论分析；应用场景：数据压缩、去噪、结构发现、可解释表示学习、降维等通用机器学习任务；传感器类型：不适用；目标类别：不适用。

Model architecture: 摘要未说明具体网络结构。核心方法是矩阵三因子分解（matrix tri-factorisation），通过一种新颖的分解策略将原问题转化为两个耦合的辅助分解问题（two coupled auxiliary factorisation problems），以保留恢复原始因子矩阵所需的结构信息。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。论文中的"稀疏性约束"（sparsity constraints）是用于提升矩阵分解的可解释性和恢复性能，而非用于神经网络轻量化。矩阵分解本身可作为数据压缩工具，但本文未讨论其在模型压缩或边缘部署中的应用。

Onboard deployability: 摘要未说明。论文未讨论参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息，也未涉及星上或边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集。实验采用Monte Carlo仿真实验（Comprehensive Monte Carlo experiments）验证理论结果，评价指标包括恢复条件（recovery conditions）、收敛行为（convergence behaviour）、谱逼近误差（spectral approximation error）、高概率界（high-probability bounds）和结构保持（structure preservation）。

Experiments: 摘要仅提及通过Monte Carlo实验验证理论，结果表明理论结果与经验观测高度一致（close agreement between the theoretical results and empirical observations）。未提供具体对比方法或消融实验细节。

Limitations: 摘要未说明局限性。但可指出：1）论文为纯理论分析，缺乏实际应用场景验证；2）未讨论计算复杂度在实际大规模数据上的表现；3）与遥感目标检测、模型轻量化等应用方向完全脱节；4）稀疏性理论仅针对矩阵分解，未延伸到深度神经网络压缩。

Ideas for my research: 借鉴意义有限。可能的间接启发：1）矩阵分解（尤其是三因子分解）可作为低秩近似工具，用于遥感高光谱/多光谱数据的降维或特征压缩，从而间接降低后续检测模型输入维度；2）稀疏性约束在矩阵分解中提升可解释性和恢复性能的思路，可类比迁移到轻量化网络中的结构化稀疏正则化设计；3）理论分析框架（恢复保证、误差界）的推导思路，对研究剪枝后模型性能保证的理论分析有一定参考价值。但整体而言，该论文与本课题的直接关联较弱。

Abstract: Matrix factorisation is a fundamental tool for exploiting low-dimensional structure in high-dimensional data, with applications such as data compression, denoising, structure discovery, interpretable representation learning, and dimensionality reduction. Compared to conventional two-factor models, matrix tri-factorisation provides greater modelling flexibility, while sparsity constraints often improve both interpretability and recovery performance. Although the role of sparsity has been extensively studied for two-factor matrix factorisation, rigorous theoretical guarantees for general real-valued matrix tri-factorisation remain largely unexplored. To address this gap, we establish, to the best of our knowledge, the first rigorous theoretical study for sparsity-induced identifiability in general real-valued matrix tri-factorisation. Our analysis is enabled by a novel decomposition strategy that transforms the original problem into two coupled auxiliary factorisation problems, while preserving the structural information necessary to the recovery of the original factor matrices from the observations. Building upon this decomposition, we derive recovery guarantees and structural consistency results that characterise how coefficient sparsity influences the sufficient recovery conditions, convergence behaviour, spectral approximation error, high-probability bounds, and structure preservation. Comprehensive Monte Carlo experiments validate the proposed theory and demonstrate close agreement between the theoretical results and empirical observations.

</details>


### [314] [Expanding Data-Agnostic Pivotal Instances Selection Models with Proximity Trees and Ensemble Learning](https://arxiv.org/abs/2607.27522)
*Alessio Cascione,Mattia Setzu,Cristiano Landi,Paolo Maria Mancarella,Riccardo Guidotti*

Main category: cs.LG

TL;DR: 提出一种基于pivot选择的层次化可解释预测模型，通过选择代表性样本构建类似决策树的解释性模型，适用于表格、文本、图像和时间序列等多种数据类型。

Research relevance: 与本研究方向弱相关（2分）。该论文是泛机器学习的可解释性方法研究，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务，更不涉及轻量化、剪枝、量化、知识蒸馏、NAS或边缘/星上部署。仅有的弱关联在于其"数据模态无关"特性理论上可应用于图像数据，且"最小化pivot数量"的思路与模型压缩有一定概念上的相似性，但本质上是可解释性研究而非模型轻量化研究。

Reading priority: 1分。该论文属于泛机器学习的可解释性研究，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测任务或任何部署相关的轻量化技术，阅读价值很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：可解释的预测建模（interpretable predictive model）与pivot选择（pivot selection）。应用场景：通用机器学习决策任务，涵盖商业和社会挑战。传感器类型：不适用（数据模态无关，涵盖tabular data、text、images、time series）。目标类别：不适用，非目标检测任务。

Model architecture: 提出层次化的、可解释的pivot选择模型（hierarchical, interpretable-by-design pivot selection model），灵感来自decision trees。核心机制是基于pivot与输入实例之间的相似度（similarity）进行分层选择。扩展包括：pivot pairs（借鉴proximity trees和oblique trees）以及ensembles。利用pre-trained networks进行数据变换，实现数据模态无关。

Lightweight method: 摘要未说明传统意义上的模型压缩方法（如pruning、quantization、knowledge distillation、NAS）。唯一与"轻量"相关的点是"maintaining a minimal number of pivots"，即通过最小化pivot数量来保持模型简洁，但这属于可解释性/模型简洁性范畴，而非面向部署的轻量化技术。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关指标。且该方法依赖pre-trained networks进行数据变换，若用于遥感在轨场景，其计算开销和存储需求可能较大，但摘要未给出具体数据。

Datasets and metrics: 数据集：涵盖tabular data、text、images、time series等多种模态的多个数据集（具体名称摘要未说明）。评价指标：摘要未明确列出具体指标，仅提及与alternative instance selection strategies及state-of-the-art interpretable models进行对比，强调competitive results和minimal number of pivots。

Experiments: 实验在多种数据类型（表格、文本、图像、时间序列）上进行，结果表明该方法优于其他实例选择策略（alternative instance selection strategies），并与最先进的可解释模型（state-of-the-art interpretable models）取得竞争性结果，同时保持最少的pivot数量。具体对比方法和消融实验细节摘要未说明。

Limitations: 1. 与遥感、卫星、目标检测完全无关，属于通用可解释机器学习方法。2. 摘要未说明计算复杂度、推理效率等部署指标。3. 依赖pre-trained networks进行数据变换，可能引入额外计算开销。4. 未涉及任何遥感图像特有的挑战（如小目标、旋转目标、复杂背景、多尺度等）。5. 未讨论在资源受限边缘设备上的可行性。

Ideas for my research: 1. "最小化pivot数量"的思想可类比于训练样本选择或原型学习（prototype learning），在遥感目标检测中可用于减少标注样本依赖，但与本课题的模型轻量化方向关联有限。2. 层次化pivot选择的结构设计思路，理论上可借鉴到轻量化检测器的特征选择或注意力机制设计中，但需要大量改造。3. 总体而言，该论文对本课题的直接借鉴价值较低，不建议深入研读。

Abstract: As decision-making processes grow more complex, machine learning tools have become essential for tackling business and societal challenges. However, many existing methods rely on decision-making procedures that are difficult to interpret. Since humans naturally make decisions by comparing new cases with a few representative examples, we aim to design an approach that selects such pivots to construct an interpretable predictive model. Inspired by decision trees, we propose a hierarchical, interpretable-by-design pivot selection model based on the similarity between pivots and input instances. Our method functions both as a pivot selection technique and a standalone predictive model. Extending beyond single pivots, we incorporate pairs of pivots that are used by proximity and oblique trees, as well as ensembles, which enhance the versatility and effectiveness of our proposal. Additionally, our approach is data modality-agnostic, leveraging pre-trained networks for data transformation. Experiments across diverse datasets, including tabular data, text, images, and time series, demonstrate the effectiveness of our approach, outperforming alternative instance selection strategies and achieving competitive results against state-of-the-art interpretable models while maintaining a minimal number of pivots.

</details>


### [315] [Latent-Kernel Discrete Flow Maps for Few-Step Generation](https://arxiv.org/abs/2607.27529)
*Mansoor Ahmed,Yue-Tsz Fan,Hemanth Venkateswara,Murray Patterson*

Main category: cs.LG

TL;DR: 提出 Latent-Kernel Discrete Flow Maps (LKF)，一种从零训练的离散流映射核，通过共享潜变量将 M 个因子化分量绑定，使单步生成即可在相关位置（如主谓一致）上放置质量，以与因子化模型相同的采样时间复杂度实现少步文本生成，并在 LM1B 和 WikiText-103 上显著提升生成困惑度。

Research relevance: 弱相关（2 分）。该论文是面向自然语言文本生成的离散扩散/流匹配方法，属于泛机器学习/生成模型方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。其"共享潜变量绑定多个因子化分量以在少步内表达位置间相关性"的思想在方法论层面可能对少步采样/加速生成有借鉴意义，但并非遥感场景，也不涉及目标检测或模型压缩。

Reading priority: 1 分。该论文是纯 NLP 离散扩散/流匹配生成模型工作，与遥感卫星在轨目标检测及轻量化方向基本无关。仅在"共享潜变量实现少步生成"这一方法论层面有间接启发，但缺乏遥感、目标检测、部署效率等关键信息，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：无条件文本生成（unconditional text generation）。应用场景：自然语言处理中的离散扩散/流匹配生成。传感器类型：不适用（非遥感）。目标类别：文本 token 序列，非遥感目标。摘要未说明任何遥感、卫星、目标检测相关内容。

Model architecture: LKF (Latent-Kernel Discrete Flow Maps)：一种离散流映射核，由 M 个因子化分量（factorized components）组成的混合模型，通过单个共享潜变量（shared latent）绑定。给定潜变量后每个分量计算代价低，混合在潜变量上以闭式求和（closed form）完成（小 M 时）。每个序列只采样一个潜变量并在整个去噪轨迹中复用，因此单步采样时间复杂度与因子化模型相同。论文还证明 Masked Diffusion Language Model (MDLM) 是 M=1 时 LKF 的特例。

Lightweight method: 摘要未说明剪枝、量化、知识蒸馏、NAS 等传统模型压缩方法。论文的"轻量化"体现在采样/推理加速层面：通过共享潜变量使单步即可表达位置间相关性，避免多步修复不匹配，从而减少函数评估次数（few-step generation），采样时间复杂度与因子化模型相同。但这是生成模型的少步采样加速，并非面向遥感目标检测的模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。论文仅提及采样时间复杂度与因子化模型相同（每序列一个潜变量复用），但未给出具体硬件部署数据。与星上/边缘部署无关。

Datasets and metrics: 数据集：One-Billion-Word (LM1B) 和 WikiText-103 两个文本生成基准。评价指标：生成困惑度（generative perplexity）和多样性（diversity）。实验设置：无条件文本生成，对比似然基线（likelihood baselines）以及蒸馏/矫正的少步采样器（distilled and rectified few-step samplers）。

Experiments: 主要结果：LKF 模型学习到强异构分量（heterogeneous components），相比似然基线将生成困惑度提升 2.1x 到 3.3x，且不损失多样性。增益随 M 增大而增大，在 M=8 时超越蒸馏和矫正的少步采样器。摘要未说明与更多基线（如其他扩散模型）的详细对比、消融实验细节或计算开销对比。

Limitations: 1. 与遥感、卫星、目标检测完全无关，属于纯 NLP 生成模型工作。2. 摘要未说明模型参数量、训练/推理计算开销、FLOPs 等关键效率指标。3. 仅在文本生成（LM1B、WikiText-103）上验证，未涉及图像/遥感数据。4. 闭式求和仅在 M 较小时可行，M 增大时计算复杂度可能上升，摘要未讨论扩展性。5. 未讨论在目标检测或视觉任务上的适用性。

Ideas for my research: 1. 方法论借鉴：LKF 用"共享潜变量绑定多个因子化分量"来在少步内表达跨位置相关性，这一思想可类比到遥感小目标检测中——例如在旋转框检测中，框的多个参数（中心、宽高、角度）之间存在强耦合，可考虑用共享潜变量/隐式约束来联合建模，减少逐参数独立预测带来的误差。2. 少步采样加速思路：若未来在轨场景涉及生成式检测或扩散式检测头，LKF 的少步生成思想（每序列一个潜变量复用）可用于降低扩散检测模型的推理步数，从而减少星上计算延迟。3. 但整体而言，该论文与遥感目标检测轻量化课题的直接可借鉴点有限，更多是方法论层面的启发。

Abstract: Discrete diffusion and flow-matching models denoise a sequence over many steps, but to keep each step cheap, they factorize the transition across positions and decide every token independently. This makes few-step generation challenging for text when the target couples two positions, such as a subject and a verb that must agree. An independent update commits to them separately, and many function evaluations are spent repairing the mismatch. Existing few-step methods buy back the lost correlation by distilling or rectifying a slow teacher, and so inherit the teacher's quality ceiling. We ask instead whether a model can express correlated steps natively, and answer with Latent-Kernel Discrete Flow Maps (LKF), a from-scratch flow-map kernel that is a mixture of M factorized components tied by a single shared latent. Conditioned on the latent, each component is cheap, and the mixture is summed over the latent in closed form for small M. We show that a single step places mass on correlated completions with the same sampling time complexity as a factorized model, since one latent is drawn per sequence and reused across the entire denoising trajectory. We also show that the Masked Diffusion Language Model (MDLM) is a special case of our LKF model at M=1. The experiments for unconditional text generation on the One-Billion-Word (LM1B) and WikiText-103 benchmarks show that our LKF model learns strongly heterogeneous components and improves generative perplexity by 2.1x to 3.3x over the likelihood baselines without losing diversity. The gain grows with M, and at M=8, it surpasses distilled and rectified few-step samplers. The source code is available at: https://github.com/mansoor181/lkf.git

</details>


### [316] [When Does Explicit View Routing Work? A Controlled Study of Multi-View Graph-Text Alignment](https://arxiv.org/abs/2607.27530)
*Xiao Yue,Guangzhi Qu*

Main category: cs.LG

TL;DR: 本文研究图-文本检索（graph-text retrieval）中的语义路由问题，通过受控实验验证多视图图注意力模型（MV-GTA）的检索是否真正依赖内容语义，而非仅依赖架构通道化。

Research relevance: 1分，基本无关。该论文研究的是图-文本检索（graph-text retrieval）与多视图语义路由问题，涉及分子性质预测（BBBP、BACE数据集）和RDKit描述符，属于图神经网络与自然语言处理交叉领域。完全不涉及遥感、卫星、地球观测、目标检测或模型轻量化/部署。

Reading priority: 1分。该论文研究图-文本检索与分子性质预测，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图-文本检索（graph-text retrieval）与语义路由（semantic routing）分析；应用场景：分子性质预测（molecular property prediction）；传感器类型：不适用；目标类别：分子图（molecular graphs）与文本描述（text descriptions）。

Model architecture: 基于MV-GTA（Multi-View Graph-Text Attention）的受控变体，包含：隔离的文本编码器（isolated text encoders）、视图特定的图头（view-specific graph heads）、多视图图注意力机制。通过外部标签或RDKit描述符提供相关性信号。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（剪枝、量化、蒸馏、NAS等均未提及）。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。

Datasets and metrics: 数据集：BBBP和BACE（分子性质预测基准数据集）；评价指标：nDCG（normalized Discounted Cumulative Gain），包括label nDCG、property nDCG、topology nDCG、unseen-template nDCG；实验设置：三种子（three-seed）匹配对比、受控文本段（deterministic, verifiable text segments）、错乱训练（deranged training）对照。

Experiments: 主要结果：正确路由（correct routing）相比错乱训练（deranged training）在label和property nDCG上提升0.305至0.685；期望图头（expected graph head）超过最佳错误头（best wrong head）0.303至0.453；联合模型在topology/label/property nDCG上获得0.720/1.000/0.877，三个单独训练的Single专家获得0.633/0.976/0.859；属性释义增强（property paraphrase augmentation）在unseen-template nDCG上提升0.140和0.147。但一致性和硬模板扩展在某些设置下降低了规范检索性能。

Limitations: 作者明确承认：证据仅限于显式、外部grounded的标签和属性路由，未建立自由形式路由（free-form routing）、一致的三视图特化（consistent three-view specialization）、与专家的统计等价性（statistical equivalence to specialists）或优越的下游预测能力。拓扑（topology）视图在两个数据集上未表现出一致的特化。

Ideas for my research: 该论文与遥感在轨目标检测轻量化方向基本无直接借鉴价值。唯一可能间接相关的概念是"多视图特化与语义路由"的思想——在遥感多传感器（光学/SAR/高光谱）融合检测中，不同模态头是否真正按语义内容分工，而非仅架构通道化，这一验证思路（受控实验+错乱对照）在方法论上或有启发，但整体关联度极低。

Abstract: Graph-text retrieval typically maps a graph and its description to a single embedding, even when a query concerns only one semantic aspect, such as a class label or molecular property. Multiple heads can separate these aspects, but a change in the query head may alter retrieval even when the wrong text is sent to that head. Such behavior demonstrates architectural channelization, not necessarily semantic routing. We examine the conditions under which this distinction can be resolved. Our controlled version of MV-GTA uses deterministic, verifiable text segments; isolated text encoders; view-specific graph heads; and relevance derived from external labels or RDKit descriptors. Correct routing and per-sample derangements form a causal test of whether retrieval depends on content. On BBBP and BACE, correct routing improves label and property nDCG by 0.305 to 0.685 over deranged training. The expected graph head exceeds the best wrong head by 0.303 to 0.453. Topology does not specialize consistently across the two datasets. In a matched three-seed comparison, one joint model obtains mean topology, label, and property nDCG of 0.720/1.000/0.877; three separately trained Single specialists obtain 0.633/0.976/0.859. Property paraphrase augmentation also improves unseen-template nDCG by 0.140 and 0.147 over a matched-exposure canonical control. Consistency and hard-template extensions, however, reduce canonical retrieval in some settings. The evidence is therefore limited to explicit, externally grounded label and property routing and observed multi-interface consolidation. It does not establish free-form routing, consistent three-view specialization, statistical equivalence to specialists, or superior downstream prediction.

</details>


### [317] [Subtract or Replay? Exact Deletion from Language-Model Memory](https://arxiv.org/abs/2607.27539)
*Vishwajith Ramesh*

Main category: cs.LG

TL;DR: 本文研究预训练语言模型持久记忆中的精确删除（exact deletion）问题，探讨不同记忆表示方式（可寻址记忆 vs 共享循环状态）对删除操作的影响，并提出代数递减与回放重建等删除方法。

Research relevance: 1分。该论文完全聚焦于大语言模型（LLM）的持久记忆删除与遗忘机制，属于自然语言处理/机器学习记忆编辑方向，与遥感、卫星、Earth observation、目标检测、轻量化部署均无任何关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于LLM记忆删除/机器遗忘领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言模型记忆删除（machine unlearning / exact deletion）。应用场景：LLM持久记忆编辑、隐私删除（如临床记录删除）。传感器类型：不适用。目标类别：不适用（处理对象为文本token与记忆记录）。

Model architecture: 涉及Gemma 3（1B/4B/12B，将全局注意力层替换为support-vector memory）、Kimi Linear hybrid（48B，含additive writes、diagonal decay、delta rule等记忆写入机制）。核心概念包括addressable memory（可寻址记忆）、shared recurrent state（共享循环状态）、low-rank recovery、checkpointed rewind-and-replay。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。虽然提到low-rank recovery（低秩恢复），但这是用于记忆恢复而非模型轻量化。与轻量化部署无关。

Onboard deployability: 摘要未说明。未提供参数量（除模型规模1B/4B/12B/48B外）、FLOPs、延迟、显存、能耗、实时性等部署信息。且该研究面向LLM记忆删除，与星上/边缘部署无关。

Datasets and metrics: 使用pretrained models（Gemma 3、Kimi Linear）进行实验。评价指标包括：median KL divergence（KL散度，如5.4×10⁻¹⁵）、perplexity（困惑度，+2.0%/+11.2%/+44.3%）、attacks（elicitation、relearning、sampling、LiRA attacks）。上下文长度达18,842 tokens（临床记录删除场景）。

Experiments: 主要实验：1) 在Gemma 3上，将全局注意力层替换为support-vector memory，对比decrement与retained-key refit在31个support-token删除下的输出一致性（median KL 5.4×10⁻¹⁵）；2) 在4B和12B规模下验证certificate ordering，utility cost升至11.2%和44.3%；3) 在48B Kimi Linear hybrid上分析不同写入规则（additive/diagonal decay/delta rule）对删除的影响；4) 通过checkpointed rewind-and-replay在18,842 token上下文中精确删除临床记录。

Limitations: 1) 与遥感、目标检测、轻量化完全无关，属于LLM记忆编辑领域；2) 摘要未说明方法在视觉/遥感数据上的适用性；3) 大规模模型（12B/48B）下utility cost显著上升（44.3%），实用性受限；4) 摘要未说明计算开销、训练成本等工程细节。

Ideas for my research: 借鉴意义有限。唯一可能间接相关的点是：文中"low-rank recovery"和"support-vector memory"涉及低秩分解思想，这与模型压缩中的低秩近似（low-rank approximation）有一定概念交集，但本文并未将其用于模型轻量化。若未来考虑将低秩分解用于遥感检测骨干网络的压缩，可参考其低秩恢复的技术细节，但整体借鉴价值很低。

Abstract: Exact deletion from persistent language-model memory depends on how that memory represents a record. Addressable influence can be removed by algebraic decrement; influence transformed by later writes inside shared recurrent state requires rebuilding from before the write. We test this distinction in two pretrained models against explicit record-omitted references. First, we replace Gemma 3's global-attention layers with support-vector memory. After low-rank recovery at 1B, decrement and retained-key refit agree at the next-token output to median KL $5.4\times10^{-15}$ over 31 support-token deletions, with $+2.0\%$ perplexity relative to a matched fine-tune. A masked-refit proxy is indistinguishable from the never-ingested floor under elicitation, relearning, sampling, and LiRA attacks. At 4B and 12B, certificate ordering persists but utility cost rises to $11.2\%$ and $44.3\%$. Second, in a 48B Kimi Linear hybrid, additive writes admit a fixed decrement and diagonal decay a corrected one, whereas the delta rule makes $12$--$49\%$ of a record's contribution suffix-dependent. Checkpointed rewind-and-replay deletes real clinical records at contexts up to 18,842 tokens, matching never-ingested logits and all recurrent states bit for bit within a deterministic MLX implementation; replaying a correction provides exact amendment. Exact deletion is therefore a property of memory representation: subtract addressable records and replay entangled writes.

</details>


### [318] [Memory Efficient Tabular Foundation Models](https://arxiv.org/abs/2607.27546)
*Shuting Luo,Monika Mikhail Kanaan,Cameron Gordon,Anna Leontjeva,Simon Lucey*

Main category: cs.LG

TL;DR: 本文研究Tabular Foundation Models（如TabPFN）的部署内存需求，通过模型压缩方法实现最高7.6倍的内存缩减，部署需求降低近87%，同时保持相近性能。

Research relevance: 弱相关（2分）。论文聚焦于Tabular Foundation Models（表格基础模型）的部署内存优化，属于泛机器学习/模型压缩方向，与遥感、卫星、目标检测均无直接关联。虽然涉及model compression和deployment，但应用场景是表格机器学习任务（in-context tabular machine learning），而非遥感图像或目标检测。其模型压缩思路对轻量化研究有一定方法论借鉴意义，但场景完全不同。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，仅模型压缩方法论有间接借鉴意义，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：表格机器学习（tabular machine learning）任务的模型部署优化。应用场景：Tabular Foundation Models（如TabPFN）的实际部署。传感器类型：不适用（非遥感/图像数据）。目标类别：不适用。摘要未说明具体表格数据集或任务类别。

Model architecture: 研究对象为Tabular Foundation Models，以TabPFN为代表。TabPFN是基于in-context learning的表格基础模型，通过预训练在表格数据上实现少样本/上下文学习。摘要未说明具体网络结构细节。

Lightweight method: 采用model compression（模型压缩）方法，具体技术细节摘要未说明（可能涉及剪枝、量化等，但摘要未明确）。实现了最高7.6倍的内存缩减（memory reduction），部署需求降低近87%。

Onboard deployability: 摘要聚焦于内存（memory）和部署需求（deployment requirements）的优化，实现了7.6倍内存缩减和87%部署需求降低。但摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等具体部署指标，也未涉及星上/边缘部署场景。

Datasets and metrics: 摘要未说明具体数据集。评价指标涉及内存缩减倍数（memory reduction，最高7.6倍）、部署需求降低比例（约87%）、以及模型性能保持程度（similar levels of performance）。具体性能指标（如准确率等）摘要未说明。

Experiments: 摘要仅提及通过实验demonstrate了模型压缩方法可实现内存缩减和性能保持，但未给出具体实验设置、对比方法、消融实验等细节。

Limitations: 摘要信息量有限：1）未说明具体采用的压缩技术（剪枝/量化/蒸馏等）；2）未说明压缩后模型的具体性能指标和精度损失；3）未涉及遥感或图像任务，无法直接迁移；4）未说明压缩方法是否适用于CNN/Transformer等视觉模型；5）Tabular模型与视觉检测模型架构差异大，压缩策略可能不通用。

Ideas for my research: 1）虽然场景不同，但其"压缩后保持相近性能"的评估框架值得借鉴——在遥感目标检测轻量化中，应同时报告压缩率和精度保持率；2）TabPFN这类Foundation Model的部署优化思路，可类比思考遥感大模型（如SAM、DINOv2等）在星上部署时的压缩策略；3）其"内存缩减7.6倍、部署需求降低87%"的量化评估方式，提示在轨部署研究中应明确报告内存占用和部署资源节省的具体倍数。

Abstract: Tabular Foundation Models, such as TabPFN, have received a large amount of recent attention due to their performance on in-context tabular machine learning tasks, which often exceeds classical baselines. However, practical deployment considerations of these models has received less attention. In this paper we investigate the memory requirements for these models. We demonstrate that employing model compression approaches can enable memory reductions of up to 7.6 with similar levels of performance, reducing deployment requirements by nearly 87%. Our work provides insight to practitioners seeking efficient deployment of these models in practical settings.

</details>


### [319] [A Montage-Agnostic Encoder for Calibration-Light Cross-User Gesture Recognition from Surface Electromyography](https://arxiv.org/abs/2607.27565)
*Jethro Odeyemi,W. J. Zhang*

Main category: cs.LG

TL;DR: 提出一种与电极布局无关（montage-agnostic）的共享权重编码器，用于肌电假肢的模式识别控制，通过物理坐标而非索引定位电极，实现跨用户、跨通道数的泛化，并在多个数据库上超越传统Hudgins和LDA基线。

Research relevance: 弱相关（2分）。论文属于生物医学信号处理与肌电假肢控制领域（myoelectric prosthesis, pattern-recognition control），与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关。虽然涉及"encoder"、"shared weights"、"泛化"等概念，但既非遥感场景，也非目标检测任务，更不涉及任何轻量化/部署方法。其核心是肌电信号（EMG）的模式识别分类，属于泛机器学习/信号处理方法，对遥感在轨目标检测课题借鉴价值有限。

Reading priority: 1分。论文属于肌电假肢控制领域，与遥感卫星在轨目标检测及轻量化方向基本无关。虽然"共享权重+坐标定位"和"跨域泛化评估"有抽象层面的类比价值，但数据模态、任务类型、部署场景均不匹配，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：肌电假肢模式识别控制（myoelectric prosthesis pattern-recognition control），即手势/意图分类。应用场景：生物医学假肢控制，非遥感场景。传感器类型：肌电电极（electrode），非遥感传感器。目标类别：多种手势意图（intended gestures），非遥感目标。摘要未说明具体手势类别数量。

Model architecture: 提出montage-agnostic encoder：每个电极用共享权重（shared weights）读取，并通过物理坐标（physical coordinate）而非索引（index）定位电极，使单一架构可接受任意通道数而无需montage-specific参数。编码器包含三个关键组件，每个组件单独贡献了3-shot macro-F1的一半以上（消融实验）。摘要未说明编码器的具体网络结构（如层数、卷积/Transformer类型）。

Lightweight method: 摘要未明确涉及剪枝、量化、知识蒸馏、NAS等经典轻量化方法。但"共享权重（shared weights）"和"单一架构适配任意通道数"在参数共享与架构泛化层面有一定轻量化思想，即通过权重共享避免为不同电极布局训练独立模型，减少参数冗余。然而这并非面向推理加速或边缘部署的轻量化手段。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。虽然假肢控制本身有实时性需求，但摘要未给出任何计算开销或推理速度数据。与星上部署（FPGA/NPU/TensorRT）完全无关。

Datasets and metrics: 数据集：DB1、DB2、DB5三个肌电数据库（具体名称摘要未说明）。评价指标：macro-F1（宏平均F1）。实验设置：跨用户训练（trained across users），held-out subject评估；3-shot小样本设置；消融实验；subject-count sweep（训练人数从9到39的受控扫描）；与per-user Hudgins和linear-discriminant classifier（LDA）基线对比。结果：DB1上超过基线0.234 macro-F1，DB2上超过0.108，DB5上低于基线。

Experiments: 主要实验结论：(1) 跨用户训练下，在DB1和DB2上超过per-user Hudgins和LDA基线，在DB5上低于基线；(2) 消融实验显示编码器三个关键组件各自单独贡献3-shot macro-F1的一半以上；(3) 训练人数从9到39的扫描显示性能边际接近平坦，训练池仅作为稳定性下限；(4) 跨三个数据库比较方向由per-user基线强度决定，而基线强度由信号保真度（signal fidelity）决定；(5) 与LDA基线比较取决于训练模型预算和基线质量；(6) 自监督预训练在监督模型充分训练后无额外收益。

Limitations: 摘要未说明：模型具体结构、计算开销、部署可行性、DB5上性能低于基线的具体原因分析、信号保真度如何量化。核心局限在于：该工作面向肌电信号分类，与遥感目标检测任务在数据模态、任务类型、部署约束上均无直接关联，其"跨用户泛化"问题与遥感场景的"跨场景/跨传感器泛化"虽有类比空间，但方法本身难以直接迁移。

Ideas for my research: 尽管论文与遥感在轨目标检测弱相关，但仍有少量可借鉴的抽象思想：(1) "montage-agnostic"的共享权重+物理坐标定位思想，可类比到遥感中"传感器/分辨率无关"的检测头设计——用共享权重处理不同分辨率/不同传感器的输入，用物理坐标（如经纬度/像素坐标）而非通道索引来定位特征，可能有助于跨传感器泛化；(2) 跨用户（跨域）泛化的消融与训练池规模扫描方法论，可借鉴到遥感跨场景/跨传感器目标检测的泛化评估设计中；(3) 自监督预训练在监督充分后无收益的结论，对遥感轻量化模型预训练策略有一定警示意义。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Pattern-recognition control promises a myoelectric prosthesis that responds to many intended gestures rather than one or two, but the promise has stayed in the laboratory. A recogniser trained on one person rarely transfers to the next, and useful performance usually demands a fresh round of labelled calibration from the end user. A montage-agnostic encoder is introduced that reads each electrode with shared weights and locates it by its physical coordinate rather than its index, so one architecture ingests any channel count without montage-specific parameters. Trained across users, it exceeds a per-user Hudgins and linear-discriminant classifier by 0.234 macro-F1 on DB1 for every held-out subject and by 0.108 on DB2, and falls below it on the ten-subject DB5. Each of the encoder's three key components individually accounts for more than half of its 3-shot macro F1 in an otherwise budget-matched ablation study. A controlled subject-count sweep shows the margin is close to flat from nine training subjects to thirty-nine, so the training pool binds only as a stability floor below which cross-user training fails to converge; what tracks the direction of the comparison across the three databases is instead the strength of the per-user baseline, which signal fidelity sets. Comparing against an LDA baseline depends on budget spent training models and on how good that baseline is, and self-supervised pretraining had no benefits once a supervised model was adequately trained.

</details>


### [320] [Recognition and Label-Free Adaptation Across Recording Sessions in Surface-EMG Gesture Decoding](https://arxiv.org/abs/2607.27568)
*Jethro Odeyemi,W. J. Zhang*

Main category: cs.LG

TL;DR: 提出一种与电极布局无关（montage-agnostic）的编码器，用于肌电信号（myoelectric）跨用户、跨会话的迁移，通过特征统计对齐实现免标签的测试时自适应，在NinaPro DB6数据集上优于逐用户LDA基线。

Research relevance: 弱相关（2分）。该论文属于生物医学信号处理/肌电控制（myoelectric control）领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关；也不涉及object detection、remote sensing、pruning、quantization、knowledge distillation、FLOPs等本方向核心概念。唯一可能借鉴的是其"跨会话/跨域迁移"与"测试时自适应（test-time adaptation）"思想，这与遥感模型在不同场景/不同卫星载荷间的泛化问题有间接类比意义，但整体属于泛机器学习/信号处理方法，与本方向弱相关。

Reading priority: 1分。该论文属于肌电信号处理领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及object detection、remote sensing、模型压缩或部署。仅"测试时自适应"思想有极间接的类比价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：肌电模式识别（myoelectric pattern recognition）中的跨用户、跨会话手势分类。应用场景：假肢控制（prosthetic control）。传感器类型：表面肌电（sEMG）电极阵列。目标类别：手势/动作类别（NinaPro DB6，10名健全受试者）。与遥感目标检测无关。

Model architecture: 摘要未说明具体网络结构细节，仅描述为"montage-agnostic encoder"（与电极布局无关的编码器），用于提取跨用户、跨会话可迁移的特征表示。对比基线为per-user LDA分类流水线。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等模型压缩/轻量化方法。论文的核心技术是"测试时自适应（test-time adaptation）"中的feature-statistic alignment（特征统计对齐），即通过调整编码器特征统计量来适配新会话数据，属于域适应/迁移学习范畴，而非模型轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。该工作面向假肢控制场景，未涉及星上/边缘部署。虽然"免标签测试时自适应"在计算开销上可能较轻（仅需估计特征统计量），但摘要未给出任何量化数据。

Datasets and metrics: 数据集：NinaPro DB6（10名健全受试者，intact subjects）。评价指标：macro-F1（跨会话保留编码器0.688 vs 逐用户LDA流水线0.540）；per-window metric用于与已发表基线对比。实验设置：在一个记录会话上训练，在另一个不同会话上直接应用（不做任何调整），并测试了5种免标签测试时自适应方法。

Experiments: 主要结果：1) 跨会话直接迁移时，编码器macro-F1为0.688，优于逐用户LDA流水线的0.540；2) 在per-window指标上高于两个已发表的source-only基线；3) 5种免标签测试时自适应中，仅feature-statistic alignment对所有受试者均有提升；4) batch-normalisation re-estimation（域适应文献中的标准方法）在该架构上完全失效；5) 特征统计对齐恢复的效果约等于一次带标签的校准重复。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于生物医学信号处理领域；2) 摘要未说明编码器网络结构、参数量、计算开销；3) 仅在NinaPro DB6单一数据集上验证，泛化性存疑；4) 未涉及任何模型压缩或轻量化技术；5) 未讨论实时推理延迟等部署指标；6) "feature-statistic alignment"的具体实现方式（如何估计统计量、对齐到哪一层）摘要未说明。

Ideas for my research: 尽管该论文与遥感在轨目标检测无直接关系，但以下思想可间接借鉴：1) "免标签测试时自适应（label-free test-time adaptation）"思想——遥感在轨场景下，卫星过境后传感器/光照/大气条件变化导致数据分布漂移，无法获取新标注，可借鉴feature-statistic alignment思路对轻量化检测模型做在轨自适应；2) "跨会话/跨域迁移"与遥感模型在不同场景、不同卫星载荷间的泛化问题有类比性；3) 论文指出batch-normalisation re-estimation会崩溃，提示在轻量化模型部署时需谨慎选择自适应策略。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Recognition accuracy obtained during a recording session does not persist when a user puts on the electrodes again after the electrodes had previously been removed. The electrodes may have moved slightly, the skin may be drier or wetter, or the elbow may be positioned differently; these factors all contribute to day-to-day variability and therefore represent a major obstacle to implementing successful pattern-recognition based myoelectric control systems in daily practice. However, simply recalibrating a user's hand for 20 min at every doff/don event is a clearly unrealistic expectation. A montage-agnostic encoder built for cross-user, cross-montage transfer is trained here using data collected during a particular recording session, and then applied to data collected later in a different recording session without adjusting anything, on the ten intact subjects of NinaPro DB6. The performance of this approach is compared to that of a per-user LDA classification pipeline, and to that of two published approaches that only rely on source data collected from the same recording session. Carried unchanged across recording sessions, the encoder retains 0.688 macro-F1 against 0.540 for the per-user pipeline, and, on the per-window metric the published baselines use, sits above both published source-only results, a band of two points that locates the encoder rather than ranking it. Of five label-free test-time adaptations, only feature-statistic alignment improves every subject; batch-normalisation re-estimation, a standard method in the domain-adaptation literature, collapses this architecture entirely. Aligning the encoder's feature statistics to the new session recovers about what a single labelled calibration repetition would.

</details>


### [321] [Policy Gradient Steering: Interventions from Behavioral Objectives](https://arxiv.org/abs/2607.27574)
*Yoann Poupart,Aurélie Beynier,Nicolas Maudet*

Main category: cs.LG

TL;DR: 提出 Policy Gradient Steering (PGS)，将大语言模型中的 activation steering 形式化为强化学习问题，通过累积临时行为目标的梯度构建可移除的任务向量，用于在推理时动态改变模型行为。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于大语言模型（LLM）的 activation steering 方法，属于强化学习和决策制定领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。也不涉及目标检测、小目标检测、旋转框检测或遥感场景理解。虽然标题中使用了 "lightweight" 一词，但这里的 lightweight 指的是 activation steering 作为推理时动态改变模型行为的轻量级替代方案，与模型压缩/轻量化（pruning, quantization, knowledge distillation, NAS）完全无关。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于 LLM activation steering / 强化学习领域。除非对"任务向量"或"模型编辑"这一跨领域概念有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：activation steering（激活引导），即通过构造 task vector 在推理时动态改变模型行为。应用场景：两路线网格世界（two-route gridworld）、国际象棋谜题（chess puzzles）、竞技足球（competitive football）。传感器类型：不适用。目标类别：不适用。

Model architecture: 提出 Policy Gradient Steering (PGS) 方法，将 steering 形式化为强化学习问题。PGS 通过在一小组 rollouts 或 demonstrations 上累积临时行为目标的梯度，构造一个可移除的 task vector。该方法不涉及具体的神经网络架构设计，而是关注如何在已有策略模型上叠加行为向量。

Lightweight method: 摘要中的 "lightweight" 指的是 activation steering 作为推理时动态改变模型行为的轻量级替代方案（无需重新训练模型），而非传统意义上的模型压缩或加速方法。不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT 等轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及星上或边缘部署，不报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。虽然 activation steering 本身是一种推理时的轻量级干预手段，但论文未从部署角度评估其计算开销。

Datasets and metrics: 实验环境包括：两路线网格世界（two-route gridworld）、国际象棋谜题（chess puzzles）、竞技足球（competitive football）。评价指标摘要未明确说明，但涉及 steering 的校准性（calibration）、可逆性（reversibility）、组合性（composability）以及跨对手迁移（transfer across opponents）等评估维度。

Experiments: 主要实验包括：1) 在两路线网格世界中验证 PGS 的校准性和可逆性；2) 在国际象棋谜题中独立拟合 PGS 向量并单独及组合评估，发现兼容的战术目标可以累积性地构建；3) 在竞技足球中验证 PGS 可以改变特定团队行为，且效果可跨对手迁移。论文还指出现有 steering 方法在简单的两路线网格世界中即无法有效引导策略。

Limitations: 摘要未说明局限性。但可指出：论文完全面向 LLM/强化学习决策领域，与遥感目标检测和模型轻量化无任何交集；未涉及任何视觉目标检测任务；未提供任何部署相关指标；其 "lightweight" 概念与本研究方向的模型压缩/加速含义完全不同。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接启发是：activation steering 通过构造可移除的 task vector 在推理时动态调整模型行为，这一思路在概念上类似于"任务向量"或"模型编辑"技术，未来若在遥感目标检测中探索多任务动态切换（如在不同传感器/不同目标类别间切换检测头行为），或许可以参考这种"不重新训练、仅叠加向量"的思路。但这一关联非常间接，且需要大量额外工作才能迁移到 CNN/Transformer 检测器上。

Abstract: Activation steering has emerged in large language models as a lightweight alternative for dynamically changing a model's behavior at inference time. However, we show that existing steering methods fail to steer even a simple policy in a two-route gridworld environment. To address this limitation, we propose Policy Gradient Steering (PGS), which formulates steering as a reinforcement learning problem. PGS accumulates gradients of a temporary behavioral objective over a small set of rollouts or demonstrations to construct a removable task vector. We first demonstrate the calibration and reversibility of PGS in a two-route gridworld environment. Using chess puzzles, we then evaluate independently fitted PGS vectors both in isolation and in combination, finding that compatible tactical objectives accumulate constructively. Finally, in competitive football, we show that PGS can alter specific team behaviors and that its effects transfer across opponents. Together, these results show that policy gradients provide a natural interface for constructing temporary and composable behavioral adaptations across diverse decision-making domains.

</details>


### [322] [MUGEN: A Unified Framework for Efficient Motion Understanding and Generation](https://arxiv.org/abs/2607.27581)
*Zhankai Ye,Yukai Jin,Bingyang Wei,Bofan Li,Yusen Wu,Fangyi Li,Shangqian Gao,Xin Liu*

Main category: cs.LG

TL;DR: 提出MUGEN，一个统一运动-语言框架，通过单次采样的自适应长度自编码器将任意长度运动压缩为少量连续潜变量，避免codebook量化损失和长解码链，同时支持text-to-motion生成和motion understanding理解。

Research relevance: 弱相关（2分）。论文属于泛机器学习/多模态（运动-语言）方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关；不涉及目标检测、小目标检测、旋转框检测等遥感任务；也不涉及剪枝、量化、知识蒸馏、NAS、边缘部署等轻量化方法。唯一可借鉴的是其"单次采样/单次解码"的推理效率设计思想，以及用连续潜变量替代离散codebook以兼顾质量与效率的思路，但这属于泛视觉生成/理解领域的方法论，与遥感在轨目标检测轻量化仅存在间接的方法学借鉴意义。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于人体运动-语言多模态生成与理解领域。虽然其推理效率设计（单次采样、连续潜变量替代codebook）有一定方法学启发，但对本课题的直接参考价值有限，建议仅作泛读了解，不必精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：统一运动-语言框架，涵盖text-to-motion生成（motion generation）和motion understanding（运动理解/检索）。应用场景：physical AI、人体运动理解与生成，非遥感场景。传感器类型：不适用（处理的是人体运动序列数据）。目标类别：人体运动（human motion），非遥感目标。

Model architecture: MUGEN框架包含：1) 单个自适应长度自编码器（adaptive-length autoencoder），将任意长度运动压缩为少量连续潜变量（continuous latent slots），作为系统唯一的运动表示；2) 语言模型（language model）负责生成这些潜变量（text-to-motion）并读取它们用于运动理解（motion understanding）；3) Depth-routed hidden states机制，让每个潜变量slot从所需的transformer深度读取信息；4) 校准头（calibrated head）预测整个潜变量集合上的联合分布，使单次采样（single draw）即可携带文本条件化的跨slot变化。

Lightweight method: 摘要未明确使用传统轻量化方法（剪枝/量化/蒸馏/NAS）。但其核心设计思想与轻量化高度相关：1) 不使用离散codebook（避免量化带来的质量损失）；2) 单次采样（one draw）替代多步解码，解码成本仅为K个语言模型步+一次解码器前向；3) 用连续潜变量替代堆叠残差codebook，避免表示膨胀；4) 避免masked decoding、长自回归展开、数十到数百步的denoising链。这些属于"推理效率优化"层面的设计，而非模型压缩层面的轻量化。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅从推理成本角度描述：解码成本为K个语言模型步、一次采样、一次解码器前向，相比堆叠残差codebook、masked decoding、长自回归、数十至数百步denoising链等方法显著降低推理开销。但未给出具体数值，且该框架面向运动-语言任务，不涉及星上/边缘部署场景。

Datasets and metrics: 数据集：HumanML3D、SnapMoGen。评价指标：FID（生成质量）、retrieval precision（检索精度）、CIDEr、BLEU@4（文本-运动对齐/生成指标）、retrieval and alignment metrics（SnapMoGen上的检索与对齐指标）。对比对象：language-model baselines、discrete-token state-of-the-art方法。

Experiments: 主要结果：1) 在HumanML3D上，MUGEN以K个语言模型步+单次采样+一次解码器前向的成本，在FID上领先language-model基线；2) 在标准评估器下，检索精度超过真实运动参考（real-motion reference）；3) 取得最佳CIDEr和BLEU@4分数；4) 在SnapMoGen上，在所有检索和对齐指标上超越discrete-token SOTA。摘要未说明消融实验细节。

Limitations: 1) 与遥感/卫星/在轨目标检测完全无关，属于人体运动-语言多模态领域；2) 摘要未说明参数量、FLOPs、延迟等部署关键指标；3) 摘要未说明模型在真实硬件（如FPGA/NPU）上的部署验证；4) 连续潜变量方案在遥感目标检测场景下的适用性未知；5) 摘要未讨论该方法在图像/遥感数据上的泛化能力。

Ideas for my research: 尽管该论文与遥感在轨目标检测无直接关系，但其设计思想有间接借鉴价值：1) "单次采样/单次解码"的推理效率理念可迁移到遥感目标检测的轻量化设计中——例如在检测头设计上追求单次前向完成预测，避免多阶段/多尺度级联带来的推理开销；2) 用连续潜变量替代离散量化表示的思想，可类比于遥感检测中避免对特征做离散化/量化而损失信息，或用于设计更紧凑的中间表示；3) Depth-routed hidden states（按需读取不同深度特征）的思想，可借鉴到遥感小目标检测中——不同尺度目标可能需要不同深度的特征，可设计按需路由的轻量特征融合模块；4) 其"一个表示同时服务生成与理解"的统一设计，可类比于遥感中"一个轻量骨干同时服务检测与分割"的多任务共享设计，减少星上重复计算。但需注意，这些仅是方法学层面的间接借鉴，不能直接套用。

Abstract: Grounding human motion in language, and language in motion, is a central step toward physical AI systems that can understand, generate, and communicate human behavior. Unified motion--language systems first coupled the two directions through a shared discrete motion codebook, but quantization limits generation quality. The strongest generators buy quality back at growing cost: stacked residual codebooks enlarge the representation; masked decoding stages, long autoregressive rollouts, and denoising chains of tens to hundreds of steps stretch inference; even the continuous-latent designs among them reach their latent only through an iterative diffusion head; and none of this decoding machinery serves understanding. We therefore propose MUGEN, a unified motion--language framework that pays neither cost: no codebook, one draw. A single adaptive-length autoencoder compresses any-length motion into a few continuous latent slots, the system's only motion representation: the language model generates them for text-to-motion and reads them back for motion understanding. Depth-routed hidden states let each slot read from the transformer depth it needs, and a calibrated head predicts a joint distribution over the full latent set, so a single draw carries the text-conditional, cross-slot variation a description permits. At a decoding cost of K language-model steps, one draw, and one decoder pass, MUGEN leads language-model baselines on FID on HumanML3D while raising retrieval precision above the real-motion reference under the standard evaluator, achieves the best CIDEr and BLEU@4 scores, and surpasses the discrete-token state of the art on every retrieval and alignment metric on SnapMoGen.

</details>


### [323] [Prox: Training-Free FFN Activation Sparsity via Approximate Intermediate-Channel Salience in LLMs](https://arxiv.org/abs/2607.27591)
*Jinyi Liu,Wei Chen,Pengyu Chen,Xinyi Yuan,Minghe Bai,Guoquan Wu,Jun Wei*

Main category: cs.LG

TL;DR: 提出Prox，一种针对LLM中SwiGLU FFN的两阶段免训练激活稀疏化框架，利用中间状态的幅值排序而非精确值来构造通道掩码，实现高稀疏度下的高效推理加速。

Research relevance: 弱相关（2分）。论文聚焦于大型语言模型（LLM）推理中的FFN激活稀疏化，属于泛机器学习/泛视觉方法，与遥感、卫星、在轨部署、目标检测均无直接关联。但其稀疏化+量化+加速的推理优化思路，对遥感目标检测模型的轻量化与边缘部署有一定方法学借鉴意义。

Reading priority: 2分。论文方法学上有一定借鉴意义（稀疏化+量化组合、两阶段掩码构造），但场景为LLM推理，与遥感卫星在轨目标检测及轻量化方向关联较弱，仅作方法参考，非核心必读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM推理加速（activation sparsification）。应用场景：大语言模型推理，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 针对SwiGLU FFN结构设计。核心洞察：稀疏执行只需中间状态诱导的通道掩码，该掩码可由条目幅值排序而非精确值构造。Stage 1利用输入稀疏性和量化代理权重构造共享掩码；Stage 2精确计算被选中的通道，实现三个投影的稀疏执行。

Lightweight method: 激活稀疏化（activation sparsification）+ 量化（quantization）兼容 + 稀疏注意力（sparse attention）兼容。属于免训练（training-free）推理加速方法，非传统剪枝/蒸馏/NAS。核心创新是两阶段框架Prox：先用量化代理权重低成本构造通道掩码，再精确计算选中通道。

Onboard deployability: 摘要未说明具体参数量、FLOPs、显存、能耗等部署指标。仅报告了端到端解码加速比：在70% FFN稀疏度下最高达1.99×。该加速针对LLM解码场景，与星上遥感目标检测的部署约束（功耗、内存带宽、实时性）无直接对应关系。

Datasets and metrics: 在10个LLM、6个模型家族上评估。评价指标：不同稀疏度下的模型质量（quality）以及端到端解码加速比（end-to-end decoding speedup）。摘要未说明具体数据集名称。

Experiments: Prox在所有稀疏度水平上优于免训练基线（training-free baselines），在70% FFN稀疏度下实现最高1.99×端到端解码加速，并与量化和稀疏注意力兼容。摘要未提供与具体基线方法的详细对比表及消融实验细节。

Limitations: 1. 面向LLM FFN结构（SwiGLU），与遥感目标检测的CNN/Transformer检测头结构差异大，直接迁移受限。2. 摘要未说明在遥感图像、目标检测任务上的验证。3. 未报告参数量、FLOPs、能耗等部署关键指标。4. 免训练稀疏化在高稀疏度下仍存在质量退化风险，摘要未给出具体质量损失数值。

Ideas for my research: 1. "用幅值排序而非精确值构造掩码"的思想可迁移到遥感检测模型的通道剪枝或激活稀疏化中，降低掩码计算开销。2. 两阶段"先粗筛后精算"的框架设计思路，可用于遥感目标检测骨干网络（如轻量CNN或Transformer）的推理加速。3. 稀疏化与量化、稀疏注意力兼容的组合优化策略，对星上NPU/FPGA部署的带宽受限场景有参考价值。4. 可借鉴其"训练-free"思路，探索遥感检测模型无需重训练的推理期压缩方案。

Abstract: Feed-forward networks (FFNs) dominate memory traffic and computation in large language model (LLM) inference, making them a primary target for activation sparsification. However, existing training-free methods suffer substantial model-quality degradation at high sparsity due to limitations in their channel-selection strategies. We observe that the SwiGLU intermediate state provides a highly effective channel-selection signal, but obtaining it requires costly dense computation. To address this, we present \emph{Prox}, a two-stage training-free framework for sparse SwiGLU FFNs. Prox hinges on the key insight: sparse execution requires only the channel mask induced by the intermediate state, which can be constructed from the magnitude ranking of its entries rather than their exact values. Specifically, Stage 1 uses input sparsity and quantized proxy weights to construct a shared mask; Stage 2 computes the selected channels exactly, enabling sparse execution of all three projections. Across ten LLMs from six model families, Prox outperforms training-free baselines at all sparsity levels, achieves up to a $1.99\times$ end-to-end decoding speedup at 70\% FFN sparsity, and is compatible with quantization and sparse attention.

</details>


### [324] [Compliance2LoRA: On-Demand Safety Alignment on Arbitrary Policy Subsets via Hypernetwork-Generated LoRA Adapters](https://arxiv.org/abs/2607.27594)
*Pankayaraj Pathmanathan,Furong Huang*

Main category: cs.LG

TL;DR: 提出一种基于自适应超网络（hypernetwork）的统一框架，通过LoRA适配器生成器实现大型推理模型（LRM）的多策略合规对齐，避免为每个策略子集单独训练模型带来的组合开销。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于大型推理模型（LRM）的安全策略对齐与个性化合规，属于大语言模型/推理模型的后训练对齐领域，完全不涉及遥感、卫星、Earth observation、目标检测或模型轻量化部署。虽然涉及LoRA（一种参数高效微调方法），但其目的不是模型压缩或推理加速，而是策略适配，与遥感在轨目标检测轻量化无直接关联。

Reading priority: 1分。论文主题为大型推理模型的安全策略对齐，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大型推理模型（LRM）的多策略安全合规对齐（multi-policy compliance alignment）。应用场景：下游用户的个性化安全策略适配。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 提出一个统一的自适应超网络（adaptive hypernetwork）框架，其中安全策略作为可定制输入，馈入一个LoRA adapter生成器，该生成器学习为下游LRM产生符合特定策略子集的LoRA权重。将生成的LoRA权重添加到LRM后，模型即可生成符合指定策略子集的响应。

Lightweight method: 涉及LoRA（Low-Rank Adaptation）作为参数高效适配手段，但LoRA在此处用于策略适配而非模型压缩或推理加速。摘要未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或边缘/星上部署等轻量化方法。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要提到在不同规模的推理模型和不同评估数据集上验证了效果，但未给出具体数据集名称和评价指标。

Experiments: 摘要仅说明该方法在不同规模的推理模型和不同评估数据集上不牺牲任务性能，未给出具体实验对比结果、消融实验或基线方法细节。

Limitations: 摘要未说明局限性。但从内容看，该工作聚焦于大模型安全对齐，与遥感目标检测和轻量化部署完全无关；即使LoRA思想可迁移，其应用场景和优化目标也与在轨遥感目标检测差异巨大。

Ideas for my research: 借鉴价值有限。唯一可能的间接启发是：超网络（hypernetwork）动态生成LoRA权重的思想，在理论上可类比为"根据输入条件动态生成轻量化适配参数"，但与本课题（遥感在轨目标检测模型轻量化）的剪枝、量化、蒸馏、NAS等核心方向无直接关联。不建议作为主要参考。

Abstract: Post-training alignment in large reasoning models (LRMs) has significantly improved their adaptability to diverse safety compliance settings. However, as LRMs personalization for downstream users takes center stage, the demand for varying levels of policy compliance grows as different user-specific LRMs must adhere to distinct subsets of safety policies. Training a separate LRM for each policy subset introduces severe combinatorial overhead. While in context learning methods overcome this combinatorial overhead, they introduce additional computational challenges associated with long context generation. To address this challenge, we propose \ours, a unified adaptive hypernetwork-based framework for multi-policy compliance. In our framework, safety policies serve as customizable inputs to a LoRA adapter generator, which learns to produce policy compliant LoRA weights for downstream LRM. When added to the LRM these weights enable the generation of responses compliant with the specified policy subsets. In this work, we demonstrate that training such a hypernetwork enables on-demand policy adjustments on a single LRM without sacrificing task performance across reasoning models of different sized and different evaluation datasets. This highlights the effectiveness and practicality of adaptive hypernetwork based alignment in LRMs.

</details>


### [325] [Back from the Future: Key-Value Cache Management by Counter-Causal Surprise](https://arxiv.org/abs/2607.27600)
*Stephen Gould,Anton van den Hengel*

Main category: cs.LG

TL;DR: 提出一种基于反因果注意力掩码的KV缓存驱逐策略，通过预测冗余性来剪除LLM推理中的KV缓存条目，降低显存占用并加速推理。

Research relevance: 弱相关（2分）。该论文属于大语言模型（LLM）推理优化方向，聚焦KV cache管理，与遥感、卫星、目标检测均无直接关系。虽然涉及"pruning"和"inference speedup"等与轻量化相关的概念，但对象是LLM的KV缓存而非遥感目标检测模型，属于泛机器学习/泛视觉方法，对遥感在轨目标检测轻量化的借鉴价值有限。

Reading priority: 1分。该论文聚焦LLM KV cache管理，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在"冗余信息移除"和"推理加速"的抽象思路上有极有限的参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM推理阶段的KV cache管理（压缩与驱逐）。应用场景：大语言模型及多模态变体的输出生成阶段。传感器类型：不适用。目标类别：不适用。摘要未说明具体应用场景。

Model architecture: 核心思想：基于"可从近期token良好预测的过去token是冗余的"这一洞察，对KV cache中的条目进行驱逐。方法包括：(1) 反因果注意力掩码（counter-causal attention mask），使每个位置仅关注其未来上下文，用于评估token冗余度；(2) 快速单层近似（fast single-layer approximation），将反因果传递限制在最后一层transformer，以降低刷新开销。无需额外训练，直接复用已存储的KV表示。

Lightweight method: 涉及KV cache压缩与驱逐（eviction）策略，属于推理阶段的内存优化/加速手段，而非传统意义上的模型剪枝、量化或知识蒸馏。其"轻量化"体现在减少KV cache显存占用并加速推理，但对象是LLM推理而非遥感目标检测模型。

Onboard deployability: 摘要未说明。论文面向GPU内存受限的LLM推理场景，未涉及星上/边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、能耗等部署指标。仅提及"faster inference speed"作为驱逐的副产品，但未给出具体数值。

Datasets and metrics: 在多种开源LLM和benchmark数据集上评估，与state-of-the-art方法对比，显示竞争性或更优性能。摘要未列出具体数据集名称和评价指标。

Experiments: 在多种开源LLM和benchmark数据集上验证了所提驱逐策略的有效性，与现有SOTA方法相比取得竞争性或更优的性能。快速单层近似在边际精度损失下实现了显著的刷新周期加速。摘要未给出具体实验数值和对比表格。

Limitations: (1) 与遥感、卫星、目标检测完全无关，属于LLM推理优化领域；(2) 摘要未说明具体数据集、指标、加速比、显存节省量等量化结果；(3) 驱逐策略可能带来生成质量的潜在损失，摘要未讨论；(4) 单层近似方案的精度-速度权衡细节未展开。

Ideas for my research: (1) "冗余信息可被移除"的思想可类比到遥感目标检测中：对于连续遥感视频帧或时序SAR图像，可考虑利用时序冗余性减少重复计算；(2) 反因果/未来上下文预测冗余度的思路，可迁移到遥感视频目标检测的帧间特征复用策略设计中；(3) 其"无需额外训练"的即插即用设计理念值得借鉴，轻量化方法应尽量降低对重新训练的需求；(4) 但整体而言，该论文与遥感在轨目标检测轻量化方向关联较弱，借鉴价值有限。

Abstract: Key-value (KV) cache management through compression and eviction strategies has emerged as an important research direction in recent years. Computational demands of large language models (LLMs) and their multi-modal variants during output generation can be partially alleviated by caching previous key and value calculations needed by subsequent scaled dot-product attention operations. However, this leads to another problem: the size of the resulting KV cache grows linearly with context length and quickly consumes all available GPU memory when either the prompt or the generated output are long. KV cache management periodically prunes entries from the cache thereby reducing its memory footprint while attempting to retain sufficient information for accurate generation. A by-product is faster inference speed. We propose a simple yet effective KV eviction scheme motivated by the insight that past tokens which can be well-predicted from more recent tokens are redundant and their associated keys and values can be removed from the cache. To score entries for eviction we run the model on the tokens in their original order, reusing the key and value representations already stored in the KV cache, and applying a counter-causal attention mask so that each position attends only to its future context. This is in-distribution, tied directly to the actual cache contents, and requires no additional training. To further reduce cost, we additionally propose a fast single-layer approximation that restricts the counter-causal pass to the last transformer layer, achieving a significant speedup per refresh cycle at marginal accuracy cost. We evaluate our strategy on various open-source LLMs and benchmark datasets showing competitive or improved performance over other state-of-the-art methods. Reference code is available at https://github.com/metacognitionai/counter_causal.

</details>


### [326] [Kalman Meets Curriculum: Efficient Dynamic Prompt Selection for Adaptive RL Finetuning](https://arxiv.org/abs/2607.27610)
*Haodong Zhu,Yangyang Ren,Yanjing Li,Sheng Xu,Haiguang Liu,Linlin Yang,Baochang Zhang*

Main category: cs.LG

TL;DR: 提出一种基于卡尔曼滤波引导的提示词选择方法（KGPS），将大语言模型强化学习微调中的提示词难度选择建模为动态状态估计问题，以自适应策略漂移并减少rollout开销。

Research relevance: 弱相关（2分）。该论文聚焦于大语言模型（LLM）强化学习微调中的提示词选择问题，属于自然语言处理与强化学习领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可能的间接联系是RL微调方法本身可作为通用技术迁移，但论文未涉及任何遥感或目标检测场景。

Reading priority: 1分。论文主题为LLM强化学习提示词选择，与遥感卫星在轨目标检测及轻量化方向基本无关，仅在训练策略设计上有极间接的参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型强化学习微调中的在线提示词选择（online prompt selection）。应用场景：数学推理、规划、几何推理等LLM推理任务。传感器类型：不适用（非遥感）。目标类别：不适用。

Model architecture: KGPS方法基于线性高斯状态空间模型（linear-Gaussian state-space model）对每个提示词的潜在成功率进行建模，使用Kalman filter维护提示词难度的校准高斯后验分布。过程噪声与策略更新幅度耦合，使策略变化较大时不确定性增加。整体框架是提示词选择算法而非神经网络架构。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。其"效率"体现在减少RL训练所需的rollout数量（如DeepSeek-R1-Distill-7B上比DS少83%的rollouts），属于训练效率优化而非推理部署轻量化。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗等部署相关信息。其优化目标是RL训练阶段的rollout效率，而非推理阶段的部署效率。

Datasets and metrics: 数据集：数学推理、规划、几何推理基准（具体名称摘要未说明），以及多个RL算法。评价指标：最终准确率（final accuracy）和rollout效率（rollout efficiency）。在DeepSeek-R1-Distill-7B上，KGPS在六个数学推理基准上平均提升0.12分，同时减少83%的rollouts。

Experiments: 在数学、规划、几何推理基准上，结合多个RL算法进行实验，与强基线（包括evaluation-based和prediction-based的在线提示词选择方法）对比，KGPS在最终准确率和rollout效率上均取得最优（state-of-the-art）。具体对比方法名称摘要未说明。

Limitations: 1. 论文完全面向LLM推理任务，与遥感目标检测场景无直接关联。2. 摘要未说明方法在视觉/遥感数据上的适用性。3. 摘要未说明计算开销、部署资源需求。4. 未涉及任何目标检测模型或遥感图像处理。5. 摘要未说明在非LLM场景（如CNN检测器）上的泛化能力。

Ideas for my research: 尽管论文与遥感目标检测无直接关联，但以下思路可间接借鉴：1. 将"动态难度选择"思想迁移到遥感目标检测的训练数据采样/课程学习（curriculum learning）中，例如在训练轻量化检测器时动态调整难样本采样策略，以适应模型能力变化。2. Kalman filter对非平稳动态的建模思想可用于遥感在轨场景中模型自适应更新（如星上模型随数据分布漂移的在线微调）。3. 训练效率优化（减少rollout/迭代次数）的思路可类比为减少星上训练或微调的计算开销。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Reinforcement learning (RL) finetuning significantly enhances the reasoning capabilities of large language models (LLMs), yet its effectiveness critically depends on selecting prompts of appropriate difficulty for the current policy. This is challenging because prompt difficulty evolves throughout training. Existing online methods therefore face a trade-off: evaluation-based approaches are accurate but expensive, while prediction-based approaches are efficient but typically assume stationary difficulty, making them ill-suited to RL's non-stationary training dynamics. To address these issues, we propose a Kalman-Guided Prompt Selection method (KGPS), which reformulates prompt selection as a dynamic state estimation problem rather than static difficulty prediction. KGPS models each prompt's latent success rate in logit space using a linear-Gaussian state-space model, with process noise coupled to the magnitude of policy updates so that uncertainty increases when the policy changes more substantially. A Kalman filter then maintains a calibrated Gaussian posterior over prompt difficulty, and prompts are selected by maximizing a posterior-expected training utility that favors intermediate-difficulty prompts while naturally revisiting uncertain ones. The resulting procedure is adaptive to policy drift and requires no additional rollouts beyond standard policy training. Extensive experiments across mathematics, planning, and geometry reasoning benchmarks, as well as multiple RL algorithms, show that KGPS consistently improves both final accuracy and rollout efficiency over strong baselines, establishing state-of-the-art performance among online prompt selection methods. For example, on DeepSeek-R1-Distill-7B, KGPS uses 83% fewer rollouts than DS while even improving the average performance by 0.12 point across six math reasoning benchmarks.

</details>


### [327] [Real-Time Hard Peak Age-of-Information Safety with No-Regret Learning](https://arxiv.org/abs/2607.27626)
*Wentao Zhang,Wentao Mo*

Main category: cs.LG

TL;DR: 提出OCO-PAoI-Hard方法，将安全关键IoT系统中峰值信息年龄（peak AoI）的硬性每时隙截止期约束转化为仿射半空间约束下的时变约束在线凸优化问题，通过proposal-shield-update循环实现零违规调度。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/在线优化方法，聚焦于IoT安全关键系统中的Age of Information（AoI）调度问题，与遥感、卫星、目标检测、轻量化均无直接关联。虽然在线凸优化（OCO）和约束优化思想在理论上可能对星上资源调度有间接借鉴，但论文本身不涉及任何遥感场景、目标检测任务或模型压缩/加速方法。

Reading priority: 1分。该论文聚焦IoT AoI调度与在线凸优化理论，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感图像、模型压缩或星上部署等核心要素。除非对在线优化理论本身有专门兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：安全关键IoT系统中的峰值信息年龄（peak AoI, PAoI）硬截止期调度。应用场景：工业闭环控制、V2X协同、远程遥操作。传感器类型：IoT传感器网络。目标类别：不涉及目标检测，目标是调度资源分配向量以满足AoI截止期。

Model architecture: 摘要未说明具体神经网络结构。核心方法框架为OCO-PAoI-Hard：将分数峰值AoI截止期精确转化为资源分配向量上的仿射半空间约束，构建多面体安全集上的时变约束在线凸优化。采用严格因果的proposal-shield-update循环，每时隙执行一次欧几里得投影保证可行性，梯度步保持no-regret性质，经典虚拟队列简化为后验证书。

Lightweight method: 摘要未涉及模型轻量化方法。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计。其核心贡献是算法层面的在线优化调度方法，而非模型压缩或加速。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存或能耗等部署指标。每时隙仅需一次欧几里得投影，计算开销理论上较低，但论文未给出具体硬件部署评估。与星上部署（onboard deployment）、FPGA/NPU/TensorRT等均无关联。

Datasets and metrics: 实验采用四传感器对抗性流体模型陷阱信道（four-sensor adversarial fluid-model trap channel），共10个随机种子。评价指标：建模状态截止期违规次数（zero modeled-state deadline violations）、时隙错失率（baselines miss 1.65%-64.0% of slots）、归一化regret（empirical normalized regret低于理论包络线，跨越T的两个数量级）。

Experiments: 在四传感器对抗性流体模型陷阱信道上，OCO-PAoI-Hard在全部10个种子下实现零建模状态截止期违规；四个代表性baseline错失1.65%至64.0%的时隙；经验归一化regret在T的两个数量级范围内保持在理论包络线之下。理论方面建立了闭式静态和动态regret界、匹配的Omega(sqrt(T))极小极大下界、针对执行噪声的margin-safe变体以及截止期诱导的竞争比。

Limitations: 1. 与遥感卫星在轨目标检测及轻量化方向基本无关，缺乏遥感场景、目标检测任务和模型压缩方法。2. 摘要未说明方法在真实硬件或实际IoT系统上的部署验证。3. 包级安全（packet-level safety）需要更强的服务假设，摘要未展开。4. 实验仅在合成对抗性流体模型信道上进行，缺乏真实场景验证。5. 未涉及计算资源受限下的模型推理优化。

Ideas for my research: 1. 在线凸优化（OCO）框架中"将硬约束转化为仿射半空间约束+每时隙单次投影"的思想，可类比借鉴到星上计算资源受限时的动态任务调度或推理资源分配问题。2. "proposal-shield-update"的可行性保障循环思路，可用于星上目标检测模型在动态资源约束下的自适应推理策略设计。3. 但整体而言，该论文与遥感在轨目标检测轻量化课题的直接可借鉴点有限，仅作为在线优化理论背景参考。

Abstract: Safety-critical IoT systems such as industrial closed-loop control, V2X coordination, and remote teleoperation require every sensor's peak Age of Information (peak AoI, also abbreviated PAoI) to stay below a hard per-slot deadline, not merely an average bound. Existing approaches meet this requirement only under restrictive assumptions: stochastic channels for Whittle-index AoI, simulator rollouts for deep reinforcement learning, or sublinear cumulative violation for long-term constrained online convex optimization. Under adversarial coefficients, OCO-PAoI-Hard guarantees zero per-slot violation of the modeled AoI state under one-step viability and O(sqrt(T)) regret against any static safe comparator; packet-level safety requires stronger service assumptions. Our key observation is that the fractional peak-AoI deadline collapses exactly to an affine half-space constraint on the resource-allocation vector, turning hard real-time scheduling into time-varying constrained online convex optimization over a polyhedral safe set. A strictly causal proposal-shield-update loop enforces feasibility through one Euclidean projection per slot, the gradient step preserves no-regret behavior, and the classical virtual queue is reduced to an a-posteriori certificate. We establish closed-form static and dynamic regret bounds, a matching Omega(sqrt(T)) minimax lower bound, a margin-safe variant against execution noise, and a deadline-induced competitive ratio. On a four-sensor adversarial fluid-model trap channel, OCO-PAoI-Hard attains zero modeled-state deadline violations across all ten seeds, while four representative baselines miss between 1.65 percent and 64.0 percent of slots, and the empirical normalized regret stays below the theoretical envelope across two orders of magnitude in T.

</details>


### [328] [First-order Constrained Trilevel Optimization Over Distributed Networks for Robust Coreset Selection](https://arxiv.org/abs/2607.27632)
*Yang Jiao,Kaixuan Jiao,Kai Yang,Nadjib Aitsaadi,Ilhem Fajjari,Renwei,Li*

Main category: cs.LG

TL;DR: 提出F²CTO，一个用于分布式鲁棒coreset selection的三层优化框架，通过层次化复合值函数重构和分布式交替投影梯度算法求解，并证明其非渐近收敛率O(ε^{-3/2})。

Research relevance: 弱相关（2分）。该论文属于泛机器学习的分布式优化与数据选择（coreset selection）方向，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务，更不涉及轻量化/剪枝/量化/蒸馏/NAS等模型压缩方法。其核心是分布式鲁棒优化与三层优化问题求解，与"遥感卫星在轨目标检测与模型轻量化"方向基本无关，仅在"分布式训练/边缘网络"这一泛化概念上存在极弱的间接关联。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于分布式优化与数据选择的理论研究。虽然coreset selection和分布式优化思想在概念上可间接迁移，但缺乏遥感场景、目标检测任务和模型轻量化方法的直接关联，阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：分布式鲁棒coreset selection（数据子集选择）与分布式三层优化问题求解。应用场景：IoT边缘网络中的分布式学习、continual learning（持续学习）。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测目标类别。摘要未说明具体应用领域。

Model architecture: 提出F²CTO（Federated First-order Constrained Trilevel Optimization）框架，核心组件包括：(1) 层次化复合值函数重构（hierarchical composite value-function reformulation）；(2) 分布式交替投影梯度算法（distributed alternating projected gradient algorithm）。该框架将分布式鲁棒coreset selection建模为带层级约束的三层优化问题（trilevel optimization problem with level-wise constraints）。摘要未说明具体神经网络架构。

Lightweight method: 不涉及传统意义上的模型轻量化方法（如pruning、quantization、knowledge distillation、NAS）。该论文的"轻量"体现在数据层面——通过coreset selection选择代表性数据子集以减少训练数据量，从而降低计算开销和存储瓶颈，属于数据压缩/数据选择而非模型压缩。摘要未说明任何模型压缩或加速技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。论文关注的是分布式训练框架的收敛性和效率，而非推理阶段的边缘/星上部署。虽然提及"distributed edge networks"和"real-world deployments"，但未给出任何具体的部署硬件（FPGA/NPU/TensorRT）或推理性能数据。

Datasets and metrics: 摘要未说明具体数据集名称。仅在实验部分提到"reliable continual learning"（可靠的持续学习）场景下进行了实证评估。评价指标方面，摘要未明确列出具体指标，仅提及验证了方法的"effectiveness and efficiency"。理论方面给出了非渐近收敛率O(ε^{-3/2})用于找到ε-驻点。

Experiments: 摘要仅提及"Extensive empirical evaluations on reliable continual learning demonstrate the effectiveness and efficiency"，未给出具体实验数据、对比方法或消融实验细节。理论贡献方面证明了F²CTO在带层级约束的三层优化问题上达到O(ε^{-3/2})的非渐近收敛率。摘要未说明具体实验结果数值。

Limitations: (1) 与遥感/卫星/目标检测/模型轻量化方向完全无关，属于分布式优化与数据选择的理论方法研究；(2) 摘要未提供任何实验数据集、基线对比、消融实验或部署性能数据；(3) 三层优化问题的求解复杂度在实际大规模遥感场景中的可扩展性未知；(4) 未讨论与目标检测模型（如YOLO、Faster R-CNN等）的结合方式；(5) 收敛性分析仅针对优化算法本身，未涉及模型精度与效率的权衡。

Ideas for my research: 尽管该论文与本研究方向弱相关，但存在以下可借鉴的间接思路：(1) coreset selection思想可迁移到遥感在轨场景——在星上算力受限时，可通过数据子集选择减少需要处理/传输的遥感图像数据量，降低存储和带宽压力；(2) 分布式优化框架（federated learning思想）可类比于"多星协同"或"星地协同"场景，多颗卫星在轨分布式训练目标检测模型时，可借鉴其层级约束优化框架；(3) 鲁棒优化思想对遥感场景中的噪声、云遮挡、光照变化等干扰具有潜在价值。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限，不建议作为核心参考。

Abstract: With the rapid advancement of the Internet of Things (IoT), massive amounts of data are generated across distributed edge networks. Training models on full data incurs significant computational overhead and storage bottlenecks, rendering coreset selection a critical paradigm. Furthermore, given the privacy-sensitive nature of local data and the escalating demand for model robustness in real-world deployments, developing an effective distributed optimization framework for robust coreset selection is vital, yet remains largely unexplored. To this end, this work first characterizes the hierarchical dependencies among coreset selection, robust optimization, and distributed learning, and formulates the distributed robust coreset selection as a trilevel optimization problem with level-wise constraints. Furthermore, to effectively solve the trilevel problem in a distributed manner, the \underline{F}ederated \underline{F}irst-order \underline{C}onstrained \underline{T}rilevel \underline{O}ptimization (F$^2$CTO) is proposed, which synergistically integrates a hierarchical composite value-function reformulation and a distributed alternating projected gradient algorithm. To the best of our knowledge, F$^2$CTO is the first method developed for distributed robust coreset selection, as well as the first distributed optimization approach for trilevel optimization problems with level-wise constraints. Additionally, we prove that the proposed method achieves a non-asymptotic convergence rate of $\mathcal{O}(ε^{-3/2})$ for finding an $ε$-stationary point. Extensive empirical evaluations on reliable continual learning demonstrate the effectiveness and efficiency of the proposed F$^2$CTO.

</details>


### [329] [Certifying when decision-time information justifies adaptive experimentation](https://arxiv.org/abs/2607.27651)
*Jia Bi,Samuel Pinilla,Chenyang Zhu*

Main category: cs.LG

TL;DR: 提出 Opportunity-aware Policy Authorization for Laboratories (OPAL) 框架，用于在自适应实验开始前决定是否应启用自适应策略，通过预承诺契约控制目标风险和正执行价值。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于自适应实验室实验设计中的策略授权（policy authorization）问题，属于统计决策/自适应实验设计领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。论文属于自适应实验设计/统计决策领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅存在非常间接的概念类比价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：自适应实验中的策略授权决策（是否启用自适应）。应用场景：实验室实验设计（Cell Painting 化合物筛选、药物基因组学、有限活动研究）。传感器类型：不适用。目标类别：化合物/药物筛选中的阳性机会识别，非视觉目标。

Model architecture: OPAL 框架包含：预承诺契约（precommitted contract）、冻结门控（frozen gate）机制、目标校准恢复（target-calibrated recovery）、不可行性边界（impossibility boundary）理论分析。摘要未说明具体网络结构。

Lightweight method: 不涉及。论文未讨论剪枝、量化、知识蒸馏、NAS 或任何模型压缩/加速方法。摘要未说明轻量化相关内容。

Onboard deployability: 不涉及。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 数据集：11,265 化合物的 Cell Painting 分区（unseen partition）、药物基因组学（pharmacogenomic）数据集、有限活动研究（finite-campaign）数据集。指标：正执行价值（positive executed value）、假激活上界（false-activation upper bound，5.18%）、激活率（activation rate，595/11265 化合物被选中）、捕获阳性机会数（384 positive opportunities）。

Experiments: 在 unseen 的 11,265 化合物 Cell Painting 分区上，冻结门控选中 595 个化合物，捕获 384 个阳性机会，在最不利完成条件下实现严格正执行价值；假激活上界 5.18% 低于 7.5% 的限制。在六种方法中，仅 OPAL 同时实现了非零激活与风险控制。药物基因组学和有限活动研究用于区分策略错位（policy misalignment）与非可认证性（non-certifiability）。

Limitations: 摘要未说明计算开销、可扩展性、对大规模高维数据的适用性；未讨论与视觉/遥感任务的关联；未提供与传统目标检测或模型压缩方法的对比。该框架的适用场景（自适应实验）与遥感在轨检测场景差异极大。

Ideas for my research: 尽管整体弱相关，但"在启用自适应前先做授权/门控决策"的思想有一定启发：在遥感在轨目标检测中，可类比思考"是否值得在轨启用重推理/自适应模型"的决策层设计——例如通过轻量预筛门控决定是否调用更重的检测分支，从而节省星上算力与能耗。但这种借鉴属于概念层面的类比，不涉及具体技术方法。

Abstract: Adaptive laboratories choose measurements during experiments, yet most methods begin after adaptation is permitted. We introduce Opportunity-aware Policy Authorization for Laboratories (\OPAL{}), a framework that decides whether adaptation should be enabled at all. \OPAL{} uses a precommitted contract to require non-trivial adaptation, controlled target risk and positive executed value after cost. We establish an impossibility boundary: source outcomes and unlabelled target covariates cannot uniformly support non-trivial authorization under unrestricted conditional outcome shift, and derive a target-calibrated recovery. Applied to an unseen 11,265-compound Cell Painting partition, the frozen gate selected 595 compounds, captured 384 positive opportunities and achieved strictly positive executed value under least-favourable completion; its 5.18\% false-activation upper bound remained below a 7.5\% limit. Among six methods, only \OPAL{} combined non-zero activation with this risk control. Locked pharmacogenomic and finite-campaign studies distinguish policy misalignment from non-certifiability, establishing authorization as a distinct layer for safe adaptive science.

</details>


### [330] [Evaluation Protocols and Cross-Subject Generalization in EEG Emotion Recognition](https://arxiv.org/abs/2607.27655)
*Hanting Suo,Yuwen Li*

Main category: cs.LG

TL;DR: 本文研究EEG情绪识别中评估流程（目标量、开发流程、报告规则）对报告准确率的影响，以DGCNN在SEED和SEED-IV数据集上的实验为例，揭示subject-dependent、subject-disjoint和cross-session三种评估协议回答的是不同问题。

Research relevance: 1分，与本研究方向基本无关。论文主题是脑电（EEG）情绪识别，属于生物信号处理和情感计算领域，完全不涉及遥感、卫星、地球观测、目标检测或模型轻量化。唯一可能相关的点是文中提到"dynamical graph convolutional neural network (DGCNN)"这一图卷积网络架构，以及评估协议对模型性能报告的影响这一方法论思考，但这些与遥感在轨目标检测及其轻量化没有直接关联。

Reading priority: 1分。论文主题为EEG情绪识别，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及任何遥感、目标检测或模型压缩技术。除非对"评估协议设计"这一方法论层面感兴趣，否则无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：EEG情绪识别（emotion recognition）；应用场景：脑机接口/情感计算；传感器类型：EEG脑电信号；目标类别：情绪类别（SEED和SEED-IV数据集）。与遥感场景无关。

Model architecture: 使用存档的dynamical graph convolutional neural network (DGCNN)路径作为案例模型。摘要未说明具体网络结构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文不涉及星上部署、边缘部署、FPGA/NPU/TensorRT、参数量、FLOPs、延迟、显存或能耗等部署相关信息。

Datasets and metrics: 数据集：SEED和SEED-IV（EEG情绪识别基准数据集）。评价指标：window accuracy、trial accuracy、subject-level bias-corrected and accelerated (BCa)置信区间。评估协议：protocol-matched subject-dependent check、five-fold subject-disjoint evaluation、cross-session evaluation。

Experiments: 主要实验发现：(1) protocol-matched subject-dependent检查中SEED结果与公开参考值相差1.47个百分点，SEED-IV相差3.40个百分点；(2) 基于重复测试集评估的checkpoint选择将平均window accuracy从epoch 80的0.7855提升到0.8892；(3) 五折subject-disjoint评估中validation-selected checkpoints在SEED上达到0.9990、SEED-IV上达到0.9920的训练参与者trial accuracy；(4) 完全held-out参与者在SEED上准确率为0.5348，SEED-IV为0.3954；(5) train-to-held-out-subject差距与简单优化欠拟合不一致，但无法隔离subject identity与implementation、preprocessing、representation、distributional因素。

Limitations: 论文本身与遥感在轨目标检测及轻量化方向无关。从方法论角度看，其局限在于：train-to-held-out-subject差距无法隔离subject identity与实现、预处理、表征、分布因素；tail-risk ensemble未在独立最终评估中建立正向增益；SEED-IV的protocol-matched兼容性检查未解决。这些局限均针对EEG情绪识别领域，对遥感目标检测无直接参考价值。

Ideas for my research: 尽管论文主题无关，但有一个方法论层面的间接借鉴点：论文强调"评估流程（目标量、开发流程、报告规则）会影响报告的性能"，并区分subject-dependent、subject-disjoint和cross-session三种评估协议回答不同问题。类比到遥感在轨目标检测，可以借鉴的是：在报告轻量化模型性能时，应明确区分"训练集分布内评估"与"跨场景/跨传感器/跨时相评估"，避免仅用测试集反复调参导致过拟合评估（类似论文中checkpoint selection inflate准确率的问题）。此外，论文指出"基于重复测试集评估的checkpoint选择会显著提升报告准确率"，这提醒我们在遥感模型轻量化实验中应使用严格的validation/test分离，防止评估泄漏。但这些仅是方法论层面的泛化借鉴，与遥感目标检测和轻量化技术本身无直接关联。

Abstract: Reported accuracy in electroencephalography (EEG) emotion recognition depends on the complete evaluation procedure, not only the classifier. We separate the target quantity, development procedure, and reporting rule, then use one archived dynamical graph convolutional neural network (DGCNN) pathway on SEED and SEED-IV as an illustrative case. In a protocol-matched subject-dependent check, the SEED result was within 1.47 percentage points of the public reference value; the 3.40-point SEED-IV difference remained unresolved. Across 30 matched SEED subject-session trajectories, checkpoint selection based on repeated test-set evaluation increased mean window accuracy from 0.7855 at epoch 80 to 0.8892. Under five-fold subject-disjoint evaluation, validation-selected checkpoints achieved training-participant trial accuracies of 0.9990 on SEED and 0.9920 on SEED-IV. Accuracy for entirely held-out participants was 0.5348 (95% conditional subject-level bias-corrected and accelerated [BCa] interval [0.4667, 0.5985]) on SEED. The SEED-IV estimate was 0.3954 ([0.3343, 0.4648]) and is reported only as secondary sensitivity evidence because its protocol-matched compatibility check remained unresolved. The observed train-to-held-out-subject gaps are inconsistent with simple optimization underfitting, but they do not isolate subject identity from implementation, preprocessing, representation, or distributional factors. Supporting analyses further showed that participant rankings depended on representation and time scale, while a development-selected tail-risk ensemble did not establish a positive gain in a separate final evaluation. Subject-dependent, subject-disjoint, and cross-session results should therefore be reported as answers to different questions.

</details>


### [331] [Looped Transformers with Source-Centered State Evolution](https://arxiv.org/abs/2607.27656)
*Bum Jun Kim,Kohei Hayashi,Shunsuke Kamiya,Masanori Koyama,Yusuke Iwasawa,Yutaka Matsuo*

Main category: cs.LG

TL;DR: 提出Source-Centered State Evolution (SCSE)，通过零偏差掩码保证循环Transformer中锚点一步不动点，从而在固定参数下提升循环深度训练与推理质量。

Research relevance: 弱相关（2分）。该论文是泛自然语言处理/序列建模方向的循环Transformer架构改进，完全不涉及遥感、卫星、地球观测、目标检测或任何视觉任务，也不涉及模型压缩/轻量化/部署。仅"固定参数下增加有效深度"这一思路与轻量化中"参数效率"概念有间接关联，但方法本身面向语言建模，与遥感在轨目标检测无直接关系。

Reading priority: 2分。该论文与遥感在轨目标检测及轻量化方向基本无关，属于NLP循环架构研究。仅"参数共享增加有效深度"这一概念对轻量骨干网络设计有间接启发，但需要大量迁移工作，优先级低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：语言建模（language modeling）与文本补全（LAMBADA completion）。应用场景：自然语言处理，非遥感/卫星场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 基于Looped Transformer（循环Transformer），即复用同一Transformer block进行循环深度（recurrent depth）推理。提出SCSE模块，包含learned anchor（学习锚点）、initial deviation（初始偏差）、zero-deviation mask（零偏差掩码），使锚点成为一步固定点（one-step fixed point），同时允许非零偏差驱动循环计算。

Lightweight method: 摘要未明确涉及传统轻量化方法（pruning/quantization/distillation/NAS）。但其核心思想"在固定参数数量下通过循环复用同一block增加有效深度"本质上是一种参数效率/计算效率策略，与轻量化中"参数共享"思路有概念上的相似性，但论文未讨论部署、推理速度、FLOPs或硬件适配。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。且方法面向NLP任务，未讨论任何边缘/星上部署场景。

Datasets and metrics: 数据集：WikiText-2、WikiText-103、direct web-corpus pretraining、held-out web-text transfer、LAMBADA completion。评价指标：摘要未明确列出具体指标名称（如perplexity等），仅提及"controlled recurrent quality frontier"。

Experiments: 在多个语言建模数据集上验证SCSE优于基线；消融实验表明learned anchor和anchor-coordinate deviation recurrence是主要增益来源；通过trained-model case study验证anchor-response诊断与观测到的循环运动一致。

Limitations: 1. 完全面向NLP/语言建模，未涉及任何视觉或遥感任务；2. 未讨论计算开销、推理延迟或部署可行性；3. 未提供与传统轻量化方法（剪枝/量化/蒸馏）的对比；4. 循环深度在推理时的实际加速/减速效果未说明；5. 未讨论在目标检测等视觉任务上的泛化能力。

Ideas for my research: 1. "固定参数下通过循环复用增加有效深度"的思想可借鉴到轻量目标检测骨干网络设计中——用参数共享的循环模块替代深层堆叠，在保持参数量不变的情况下提升感受野或特征表达能力；2. "锚点一步不动点"的数学设计思路可用于设计稳定的循环特征提取模块，避免循环深度增加时的梯度消失/特征漂移问题；3. 若将循环Transformer用于遥感小目标检测的上下文建模，可参考其"输入条件化与参考保持"的平衡策略。但整体借鉴价值有限，需自行迁移到视觉域。

Abstract: Looped Transformers create a useful train- and test-time compute axis by reusing the same Transformer block over recurrent depth, increasing effective depth at a fixed parameter count. However, that shared block must then govern an entire trajectory of varying hidden states over trained and extrapolated depths. Furthermore, in additive-injection looped Transformers, an input-conditioned signal is reintroduced at every recurrent step, so applying the shared transition at an input-conditioned reference can still move the hidden state. In this paper, we propose Source-Centered State Evolution (SCSE), which is designed to reconcile input conditioning with reference-preserving shared recurrence. Specifically, SCSE retains input dependence through its learned anchor and initial deviation, allows nonzero deviations to drive recurrent computation while mapping zero deviation to zero, and guarantees exact anchor invariance through its zero-deviation mask. The designated anchor is thereby a one-step fixed point by construction. The zero-deviation forcing bias is the next deviation produced from the anchor itself and vanishes in SCSE, while nonzero deviations remain active and support state-dependent recurrent computation. Our theory shows that the zero-deviation forcing bias is a design degree of freedom whose task effect can be harmful, neutral, or beneficial; SCSE resolves this choice in favor of exact anchor invariance by setting the bias to zero. Across WikiText-2, WikiText-103, direct web-corpus pretraining, held-out web-text transfer, and LAMBADA completion, SCSE improves the controlled recurrent quality frontier. Ablation studies identify the learned anchor and the anchor-coordinate deviation recurrence as the primary contributors to the gain, and a trained-model case study grounds the anchor-response diagnostic in observed recurrent motion.

</details>


### [332] [Understanding Submodular Information Measure Based Objectives for Representation Learning: A Variance and Separation Perspective](https://arxiv.org/abs/2607.27660)
*Rishabh Iyer,Truong Pham,Anay Majee*

Main category: cs.LG

TL;DR: 本文提出一个统一的理论框架，将Submodular Information Measures (SIMs)与表示学习和统计模式识别中的经典概念联系起来，从几何和统计角度解释不同子模信息度量（如Total Information和Mutual Information）在对比学习中的行为特性。

Research relevance: 1分——与本研究方向基本无关。论文聚焦于子模信息度量（Submodular Information Measures）的理论分析，属于表示学习和多模态学习的理论性研究，完全不涉及遥感、卫星、目标检测或模型轻量化。虽然对比学习/表示学习在遥感目标检测中有潜在应用，但本文是纯理论工作，没有遥感场景、没有检测任务、没有轻量化方法。

Reading priority: 1分——与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。本文是表示学习理论论文，不涉及遥感、目标检测或轻量化部署，除非对对比学习目标函数的理论性质有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：表示学习（representation learning）与多模态学习的理论分析。应用场景：非遥感场景，属于通用机器学习理论。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。本文基于SCORE框架（majee2024score），该框架将SIMs作为监督对比学习的目标函数。论文从理论层面分析Graph Cut、LogDet、Facility Location三种子模函数在Total Information (TI)和Mutual Information (MI)两种目标下的几何与统计特性。

Lightweight method: 不涉及。本文没有任何模型压缩、剪枝、量化、知识蒸馏、NAS或轻量化网络设计相关内容。

Onboard deployability: 不适用。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时性信息，且论文本身不涉及部署问题。

Datasets and metrics: 使用受控合成实验（controlled synthetic experiments），独立变化方差（variance）、协方差（covariance）、类别不平衡（class imbalance）、类别分离度（class separation）和多模态重叠（multimodal overlap）来验证理论。摘要未说明具体数据集名称和评价指标。

Experiments: 通过合成实验验证理论推导，实验结果表明经验行为与所提出的理论高度吻合。具体对比方法和消融实验细节摘要未说明。

Limitations: 1. 纯理论工作，缺乏真实数据集上的实证验证；2. 仅使用合成数据，未在真实图像/遥感数据上验证；3. 摘要未说明理论结论如何转化为实际训练策略的指导；4. 与遥感、目标检测、轻量化部署完全脱节。

Ideas for my research: 借鉴意义非常有限。唯一可能的间接联系是：如果未来在遥感目标检测中使用对比学习/自监督预训练，本文对SIMs目标函数的理论理解（如Facility Location TI对稀有和易混淆类别的强调）可能对遥感小目标或稀有目标类别的表示学习有启发。但这属于非常间接的关联，且需要大量额外工作才能转化到遥感场景。

Abstract: Submodular Information Measures (SIMs) have recently emerged as a powerful framework for representation learning and multimodal learning. In particular, the SCORE framework~\cite{majee2024score} demonstrated that SIMs can serve as effective objectives for supervised contrastive learning. Despite their empirical success, however, the geometric and statistical properties induced by different submodular information measures remain poorly understood.
  In this work, we develop a unified theoretical framework connecting SIMs to classical concepts in representation learning and statistical pattern recognition. We show that Total Information (TI) objectives characterize intra-class structure: Graph Cut TI recovers within-class variance, LogDet TI recovers generalized variance and covariance volume, and Facility Location TI induces imbalance-aware separation that emphasizes rare and confusable classes. We further show that Mutual Information (MI) objectives capture complementary notions of inter-class structure: Graph Cut MI is closely related to centroid separation and Fisher-style discrimination, LogDet MI captures covariance-aware separation through Mahalanobis distance, and Facility Location MI measures nearest-mode representational overlap.
  We validate these theoretical characterizations using controlled synthetic experiments that independently vary variance, covariance, class imbalance, class separation, and multimodal overlap. Across all settings, the empirical behavior closely matches the proposed theory. Our results provide the first unified geometric and statistical understanding of submodular information measures and offer principled guidance for selecting and designing SIM-based objectives for representation learning.

</details>


### [333] [FedOGL: Combating Catastrophic Forgetting in Federated Open-World Multimodal Graph Learning](https://arxiv.org/abs/2607.27665)
*Zekai Chen,Haodong Lu,Shihao Li,Weiwei Ji,Xunkai Li,Xun Wu,Yinlin Zhu,Rong-Hua Li*

Main category: cs.LG

TL;DR: 提出FedOGL，一个面向联邦多模态图学习的语义-结构记忆保持框架，通过客户端重放与任务起始蒸馏、全局结构基投影保护图传播记忆，以及服务端紧凑类别原型共享，缓解联邦多模态图学习中的灾难性遗忘问题。

Research relevance: 弱相关（2分）。论文属于联邦图学习（Federated Graph Learning）与增量学习（class-incremental learning）领域，核心问题是多模态图数据上的灾难性遗忘。虽然涉及多模态数据、增量学习、知识蒸馏等概念，但完全不涉及遥感、卫星、目标检测或模型轻量化/部署。属于泛机器学习方法，与"遥感卫星在轨目标检测与模型轻量化"方向基本无关。

Reading priority: 1分。论文聚焦联邦图学习的灾难性遗忘问题，与遥感卫星在轨目标检测及模型轻量化方向基本无关。仅当课题涉及在轨持续学习/增量学习或星地协同联邦学习时，可作低优先级参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：联邦多模态图上的增量学习（class-incremental learning）与开放集识别（open-set recognition）。应用场景：去中心化图数据协作训练，客户端需学习新类别、保留历史类别并拒绝已知类别空间之外的样本。传感器类型：摘要未说明（涉及多模态图数据，但未指明具体模态）。目标类别：摘要未说明。

Model architecture: FedOGL框架包含客户端和服务端两部分。客户端：通过replay（重放）和task-start distillation（任务起始蒸馏）保留历史决策行为；通过将图传播记忆投影到全局共享结构基（globally shared structure basis）上来保护图传播记忆。服务端：维护并传递紧凑类别原型（compact category prototypes）以促进跨客户端知识共享，同时不暴露原始图数据。

Lightweight method: 摘要未明确涉及模型轻量化方法。虽然使用了知识蒸馏（distillation）和紧凑类别原型（compact category prototypes）等概念，但这些主要用于缓解灾难性遗忘和减少通信开销，而非针对模型压缩、剪枝、量化或推理加速。未提及pruning、quantization、NAS、FLOPs、延迟等轻量化/部署相关技术。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗、实时推理或边缘/星上部署相关信息。联邦学习框架本身与分布式训练相关，但与本课题关注的星上推理部署无关。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标方面，主要报告了灾难性遗忘导致的性能退化降低幅度（相比最优基线降低42.67%），以及下游任务性能保持或提升。未提及mAP、F1、IoU等目标检测常用指标。

Experiments: 摘要仅报告了与最优基线相比，FedOGL将灾难性遗忘导致的性能退化降低了42.67%，同时保持或提升了下游任务性能。未提供具体数据集、对比方法列表或消融实验细节。

Limitations: 1. 摘要未说明具体数据集、模态类型和实验规模；2. 未讨论模型计算开销和通信成本；3. 未涉及任何遥感场景或目标检测任务；4. 未提供轻量化/部署相关指标；5. 联邦图学习设定与在轨遥感目标检测的应用场景差异较大。

Ideas for my research: 尽管整体相关性弱，但以下概念可能对课题有间接借鉴：1. task-start distillation（任务起始蒸馏）思想可类比于在轨模型持续学习/增量更新场景中，新任务到来时通过蒸馏保留旧知识，避免在轨模型因数据分布漂移而遗忘已有检测能力；2. 紧凑类别原型（compact category prototypes）传递思想可用于星上-地面协同场景中，通过传递轻量原型而非原始数据来减少通信开销；3. 开放集识别（拒绝已知类别空间外样本）思想对遥感目标检测中未知目标/新类别处理有参考价值。但这些均为间接借鉴，论文本身不涉及遥感或轻量化。

Abstract: Federated graph learning enables collaborative training over decentralized graph data without sharing raw graph information. As such risks evolve, clients must learn emerging classes from private multimodal graph streams, retain historical categories, and reject samples outside the known class space. In this setting, clients must learn emerging classes from private multimodal graph streams while preserving historical categories and rejecting samples outside the current known class space. The core challenge is catastrophic forgetting, which in federated multimodal graphs is not merely a classifier-level failure: old knowledge can be erased through modality-semantic overwriting, topology-induced structural erosion, and federated memory fragmentation. To address this challenge, we propose \textbf{FedOGL}, a semantic-structural memory preservation framework. On the client side, FedOGL preserves historical decision behavior through replay and task-start distillation, while protecting graph-propagation memory via projection onto a globally shared structure basis. On the server side, FedOGL maintains and transfers compact category prototypes to facilitate cross-client knowledge sharing without exposing raw graph data. Extensive experiments demonstrate that, compared with the best-performing baselines, FedOGL reduces performance degradation caused by catastrophic forgetting by \textbf{42.67\%}, while maintaining or improving performance on downstream tasks.

</details>


### [334] [Tight Sample Complexity for Low-Rank Adaptation: Matching Bounds and Rank Selection](https://arxiv.org/abs/2607.27680)
*Arunan J*

Main category: cs.LG

TL;DR: 本文从统计学习理论角度分析LoRA（Low-Rank Adaptation）微调大模型的泛化性质，给出了rank-r LoRA经验风险最小化的上界与匹配的minimax下界，并据此提出rank选择二分法，指出过参数化惩罚源于未正则化的ERM而非LoRA类本身。

Research relevance: 弱相关（2分）。本文是泛机器学习/统计学习理论方向的工作，聚焦大语言模型微调中的LoRA机制，与遥感、卫星、在轨目标检测、轻量化部署均无直接关联。唯一可能的间接联系是LoRA作为一种参数高效微调（parameter-efficient fine-tuning）方法，可被视为一种"轻量化"思想（减少可训练参数量），但其研究对象是LLM微调而非遥感目标检测模型，且不涉及剪枝/量化/蒸馏/推理加速等部署手段。

Reading priority: 2分（低优先级）。本文是统计学习理论论文，与遥感卫星在轨目标检测及轻量化部署方向弱相关。仅当课题涉及LoRA式低秩微调或低秩分解轻量化方法时，其rank选择理论才有间接参考价值；否则可跳过。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：统计学习理论分析（LoRA微调的泛化界与rank选择）。应用场景：大语言模型（LLM）参数高效微调。传感器类型：不适用（非遥感）。目标类别：不适用。实验任务为文本分类（SST-2、MRPC），模型为DistilBERT和RoBERTa。

Model architecture: 研究对象为LoRA（Low-Rank Adaptation）微调机制，即对预训练大模型的权重更新施加低秩约束（rank-r子空间）。理论分析针对两类估计器：(1) 约束经验风险最小化器（constrained ERM）；(2) 核范数截断型自适应估计器（nuclear-norm-then-truncate）。

Lightweight method: LoRA本身可视为一种参数高效微调/轻量化思想（仅训练低秩增量矩阵，减少可训练参数量），但本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或推理加速等传统模型压缩手段，也不涉及边缘/星上部署。本文的"轻量化"仅体现在减少微调参数量这一层面，且是理论分析而非工程实现。

Onboard deployability: 摘要未说明。本文不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标，也不涉及星上/边缘部署可行性。其研究对象是LLM微调的理论性质，与在轨推理部署无关。

Datasets and metrics: 数据集：合成trace-regression基准；真实数据为SST-2和MRPC文本分类任务，模型为DistilBERT和RoBERTa。评价指标：excess risk（超额风险）、validation loss（验证损失）、paired permutation检验（p=0.016）。

Experiments: 理论结果：(1) 当目标适配秩≤r时，rank-r LoRA的ERM超额风险上界为O~(rd/n)；(2) 通过Fano型packing证明匹配的minimax下界Omega(rd/n)；(3) 得到rank选择二分法：对约束ERM，最优rank等于内在秩r*，过rank严格有害；对核范数截断型自适应估计器，过rank无害且速率饱和于Theta~(r*d/n)。实验验证：三个(model, task)配置均呈现预测的U形验证损失曲线，其中两个在大rank处出现统计显著的损失膨胀（paired permutation p=0.016）。

Limitations: 摘要未说明。但可推断：理论限于well-specified locally quadratic regime，对非二次损失、非凸优化、真实深度网络训练动态的适用性有限；实验仅覆盖文本分类任务，未涉及视觉/遥感场景；未提供任何部署相关指标。

Ideas for my research: 尽管本文与遥感在轨目标检测无直接关系，但其理论框架对"轻量化"课题有间接启发：(1) LoRA的低秩微调思想可迁移到遥感目标检测骨干网络的参数高效微调（如对预训练检测器仅微调低秩增量），减少星上模型更新成本；(2) rank选择二分法提示：在模型压缩/低秩近似中，rank并非越大越好，存在内在秩r*，这对低秩分解类轻量化方法（如低秩卷积分解）的秩选择有理论指导意义；(3) 过参数化惩罚源于未正则化ERM的结论，提示在轻量化模型训练中应重视正则化策略。但需注意，这些只是方法论层面的间接借鉴，本文不提供任何遥感或部署相关的实证。

Abstract: Low-Rank Adaptation (LoRA) has become the standard mechanism for fine-tuning large pretrained models, yet its statistical properties remain only partially understood. Existing generalization results provide upper bounds of the form O~(sqrt(rd/n)) or O~(rd/n), but a matching lower bound is missing, and the question of how to choose the LoRA rank r has no formal answer. Both gaps are closed here. A local Rademacher argument establishes an upper bound of O~(rd/n) on the excess risk of the empirical risk minimizer over rank-r LoRA, whenever the target adaptation has rank at most r. A matching minimax lower bound of Omega(rd/n) is then proved via a Fano-type packing of the rank-r subspace of R^{d x d}; the bound applies to any estimator whose output lies in the rank-r LoRA class. Combining the two yields a rank-selection dichotomy. For the constrained empirical risk minimizer, the optimal rank equals the intrinsic rank r*, and over-ranking strictly hurts. For adaptive estimators of the nuclear-norm-then-truncate type, over-ranking is harmless and the rate saturates at Theta~(r* d / n) regardless of r. Taken together, the three results characterize the statistical complexity of LoRA fine-tuning within the well-specified locally quadratic regime, and identify the empirically observed over-parameterization penalty as a property of unregularized empirical risk minimization rather than of the LoRA class itself. Predictions of the theory are verified on a synthetic trace-regression benchmark and on real LoRA fine-tuning across three (model, task) configurations covering DistilBERT and RoBERTa on SST-2 and MRPC. All configurations exhibit the predicted U-shape in validation loss, with two showing statistically significant loss inflation at large ranks (paired permutation p = 0.016).

</details>


### [335] [Event-Structured Physics-Informed Neural Networks for Differentiable Critical Clearing Boundaries](https://arxiv.org/abs/2607.27681)
*Baoli Hao,Chenxi Hu,Ming Zhong,Ren Wang*

Main category: cs.LG

TL;DR: 提出一种事件结构化物理信息神经网络（ES-PINN），用于电力系统暂态稳定评估和临界切除时间（CCT）估计，通过精确事件链式约束和可微稳定裕度实现高效边界提取与直接CCT预测。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于电力系统暂态稳定评估领域，涉及物理信息神经网络（PINN）和电力系统仿真，既非遥感/卫星场景，也非目标检测任务，更不涉及模型轻量化或边缘部署。唯一可能的弱关联是"distilled readout"（蒸馏读出）和"computational efficiency"（计算效率）提及，但并非面向部署的模型压缩方法。

Reading priority: 1分。论文属于电力系统暂态稳定评估领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在方法论层面（物理信息约束、蒸馏读出）有极弱的间接借鉴意义，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务：电力系统暂态稳定评估（transient-stability assessment）与临界切除时间（CCT）估计。应用场景：电力系统故障清除动力学仿真。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: ES-PINN（event-structured physics-informed neural network）：一种物理信息神经网络，其表示与pre-fault、fault-on、post-clearing三个阶段摆动动力学对齐，并在事件接口处强制精确状态链式约束（exact state chaining）。包含一个平滑轨迹诱导的稳定裕度（smooth trajectory-induced stability margin）用于定义CCT边界的可微近似，以及一个蒸馏读出（distilled readout）用于直接CCT预测。

Lightweight method: 摘要提及"distilled readout"（蒸馏读出）用于直接CCT预测，以及"computational efficiency"（计算效率）和"runtime analyses"（运行时分析），但未说明具体模型压缩方法（如剪枝、量化、NAS等）。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要未说明。虽然提及"computational efficiency"和"runtime analyses"，但未给出具体延迟、能耗、参数量或FLOPs数据，且该模型面向电力系统仿真而非星上部署场景。

Datasets and metrics: 数据集：IEEE 9-bus、14-bus、30-bus电力系统标准测试系统。评价指标：held-out trajectory accuracy（留出轨迹精度）、stability-boundary accuracy（稳定边界精度），以及full-network DAE validation、multi-fault experiments和runtime analyses。摘要未给出具体数值。

Experiments: 在IEEE 9、14、30节点系统上与matched neural-surrogate baselines对比，ES-PINN在留出轨迹和稳定边界精度上一致提升，覆盖机械和电气故障场景及多种清除配置。额外进行了全网络DAE验证、多故障实验和运行时分析。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于电力系统领域。2. 摘要未说明模型规模、推理速度、部署硬件等关键信息。3. 仅在IEEE小规模测试系统上验证，未涉及大规模实际电网。4. 物理信息神经网络（PINN）的训练收敛性和泛化性在复杂场景下可能受限，摘要未讨论。

Ideas for my research: 尽管领域不同，但以下方法论思想可借鉴：1. "event-structured"思想——将任务按阶段（pre-fault/fault-on/post-clearing）结构化建模，类比遥感目标检测中可考虑将不同成像条件（光照、云遮挡、传感器模式）作为事件阶段进行结构化建模。2. "distilled readout"（蒸馏读出）——通过蒸馏将复杂物理约束网络压缩为直接预测头，这一思想与知识蒸馏（knowledge distillation）在轻量化目标检测中的应用有相通之处。3. 物理信息约束（physics-informed）——在遥感目标检测中可考虑引入成像物理模型（如大气散射、传感器PSF）作为先验约束。4. 可微边界/裕度定义——类比检测中的可微IoU或可微NMS设计。

Abstract: Transient-stability assessment determines whether a power system can recover after a disturbance and is therefore essential to preventing generator trips and cascading outages. A key metric is the critical clearing time (CCT), which specifies the maximum time available to clear a fault before synchronism is lost. Reliable CCT estimation is challenging because complicated fault-clearing dynamics require repeated simulations over many fault severities and clearing times. We propose an event-structured physics-informed neural network (ES-PINN) that aligns its representation with the pre-fault, fault-on, and post-clearing swing dynamics and enforces exact state chaining across event interfaces. A smooth trajectory-induced stability margin defines a differentiable approximation of the CCT boundary, enabling accurate boundary extraction, local sensitivity analysis, and optional direct CCT prediction through a distilled readout. We further prove a local residual-to-trajectory-to-CCT error estimate, in which exact event chaining eliminates separate state-interface defect terms. Experiments on IEEE 9-, 14-, and 30-bus systems show that ES-PINN consistently improves held-out trajectory and stability-boundary accuracy over matched neural-surrogate baselines across mechanical and electrical contingencies with multiple clearing configurations. Additional full-network DAE validation, multi-fault experiments, and runtime analyses further demonstrate the effectiveness and computational efficiency of the proposed framework.

</details>


### [336] [NMINE: Normalized Mutual Information Neural Estimation](https://arxiv.org/abs/2607.27710)
*Petra Eerikinharju,Marko Tuononen,Ville Hautamäki*

Main category: cs.LG

TL;DR: 提出一种基于神经网络的归一化互信息估计器，结合MINE互信息估计与MI-NEE边际熵估计，用于连续多维变量的归一化依赖度量。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/信息论方法研究，聚焦于归一化互信息（normalized mutual information）的神经估计器设计，与遥感、卫星、目标检测、轻量化部署均无直接关联。它既不是遥感场景，也不涉及目标检测或模型压缩/加速方法，仅作为泛机器学习方法对研究方向有极弱的间接借鉴意义。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于纯信息论/统计估计方法研究，仅在极间接层面（互信息作为依赖度量工具）可能对剪枝或蒸馏有微弱参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：连续多维随机变量之间的归一化互信息估计（normalized mutual information estimation）。应用场景：分子动力学、可解释机器学习等（摘要提及），与遥感/卫星场景无关。传感器类型：不适用。目标类别：不适用。

Model architecture: 采用MINE（Mutual Information Neural Estimation）框架估计互信息（基于Donsker–Varadhan表示），并结合MI-NEE启发的神经边际熵估计器（通过学习每个边际分布与均匀参考分布之间的divergence来恢复熵）。整体为全神经网络（fully neural）估计器。

Lightweight method: 摘要未说明。论文不涉及任何模型压缩或加速方法（pruning、quantization、knowledge distillation、NAS等均未提及）。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该工作面向统计估计任务，与星上/边缘部署场景无关。

Datasets and metrics: 实验数据：1至8维的高斯数据（Gaussian data）。评价指标：估计精度（accuracy），与KSG-based归一化互信息基线进行对比。未使用任何遥感数据集或目标检测基准。

Experiments: 在1-8维高斯数据上，所提出的神经估计器相比KSG-based归一化互信息基线在精度上有所提升。结果表明神经估计是连续多维归一化依赖度量的有前景方向。无消融实验、无遥感相关实验。

Limitations: 摘要未说明。但可推断：仅在高斯合成数据上验证，缺乏真实高维复杂数据（如图像、遥感数据）的评估；未讨论估计器的计算开销、训练稳定性、维度扩展性等关键问题；与遥感目标检测及轻量化部署完全无关。

Ideas for my research: 借鉴意义非常有限。可能的间接启发：互信息（mutual information）可作为特征选择或通道重要性度量，用于轻量化中的剪枝（pruning）或特征蒸馏（feature distillation）——例如用归一化互信息衡量特征图与标签之间的依赖强度，从而指导通道剪枝或知识蒸馏中的特征对齐。但该论文本身并未提供此类应用，仅提供了一种更稳定的神经互信息估计思路，可作为剪枝/蒸馏中依赖度量工具的潜在参考。

Abstract: Mutual information is a general measure of statistical dependence that captures both linear and nonlinear relationships between random variables. For continuous and multidimensional variables For continuous multidimensional variables, mutual information must be estimated from samples. Because mutual information is unbounded, its values are not directly comparable across datasets, dimensions, or applications. Normalized mutual information addresses this limitation by converting mutual information into a normalized dependency score. Recent work has demonstrated the practical value of normalized mutual information in applications such as molecular dynamics {arXiv:2405.04980} and interpretable machine learning {arXiv:2409.16768}, but existing estimators remain sensitive to dimensionality and numerical stability {arXiv:2410.07642}.
  In this paper, we propose a fully neural normalized mutual information estimator for continuous variables. The proposed approach combines a MINE-based neural mutual information estimator {arXiv:1801.04062} with MI-NEE-inspired neural marginal entropy estimators {arXiv:1905.12957}. Mutual information is estimated using the Donsker--Varadhan representation, while marginal entropies are estimated by learning the divergence between each marginal distribution and a uniform reference distribution, from which entropy is recovered. The resulting estimator provides a neural alternative to k-nearest-neighbor-based normalized mutual information estimation {arXiv:2405.04980}.
  Experiments on Gaussian data from one to eight dimensions show that the proposed estimator improves accuracy over a KSG-based normalized mutual information baseline. These results indicate that neural estimation is a promising direction for normalized dependency measurement in continuous multidimensional settings.

</details>


### [337] [VESTIGE: A Knowledge-Guided Masking Strategy for Corruption-Aware Fine-Tuning of Genomic Transformers, Validated on Ancient DNA Reconstruction](https://arxiv.org/abs/2607.27712)
*Angshuman Chakravertty,Rahul Maheshwari*

Main category: cs.LG

TL;DR: 提出VESTIGE，一种无参数、即插即用的MLM数据整理器（collator）替代方案，通过将掩码分布与逐位置经验损伤分布对齐，提升古代DNA序列重建性能。

Research relevance: 弱相关（2分）。论文属于生物信息学/基因组学领域，聚焦ancient DNA (aDNA) 序列重建与masked-language-model (MLM) 微调策略改进。虽然其核心思想——"将掩码/退化分布与经验测量的逐位置损伤分布对齐"——在方法论上对遥感图像退化建模有一定类比借鉴意义，但论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等任何遥感场景，也不涉及目标检测或模型轻量化/部署。属于泛机器学习方法在生物序列上的应用，与本研究方向弱相关。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于生物信息学/基因组学领域。虽然其"位置感知退化建模"的思想有一定方法论类比价值，但缺乏遥感场景、目标检测任务和轻量化/部署内容的直接关联，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：ancient DNA (aDNA) 序列重建（sequence reconstruction）与degraded sequence输入下的智能系统训练。应用场景：古基因组学、生物信息学。传感器类型：不适用（非遥感传感器）。目标类别：DNA序列（mammoth CDS corpus，两个标本、七个基因）。

Model architecture: 基于DNABERT-2（预训练语言模型）进行微调；引入VESTIGE作为标准MLM collator的无参数替代。VESTIGE将掩码分布与mapDamage2经验测量的逐位置C-to-T / G-to-A脱氨损伤梯度对齐，重缩放使平均C/G掩码率等于15%（与标准MLM一致），从而隔离空间重分布作为唯一变量。另使用ESMFold进行蛋白质结构重建、1D CNN作为生物安全分类器。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络等任何模型压缩或加速方法。VESTIGE本身是"parameter-free"（无参数）的，但这指的是不引入额外可学习参数，而非模型轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。DNABERT-2和ESMFold均为大规模预训练模型，与在轨轻量化部署场景无直接关联。

Datasets and metrics: 数据集：mammoth CDS corpus（两个标本、七个基因），626对配对窗口，六种terminal-zone宽度。评价指标：验证集cross-entropy（3.274 vs 3.757，降低13%）、Delta（+4.18到+10.35个百分点，p<10^-8）、ESMFold重建的TM-score（>0.95）、1D CNN分类器AUC（0.935）及窗口通过率（98.2%）。

Experiments: 在六种terminal-zone宽度和626对配对窗口上，VESTIGE在所有宽度下均优于标准MLM（Delta = +4.18到+10.35 pp，p<10^-8）；验证集cross-entropy降低13%（3.274 vs 3.757）；在损伤放大10-30倍于真实PMD速率的情况下，六个重建（三个基因）的TM-score均>0.95；1D CNN生物安全分类器AUC=0.935，清除98.2%的重建窗口，剩余1.76%归因于参考基因组特征而非重建伪影。模型、数据、种子、超参数在两组DNABERT-2运行中保持固定。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于生物信息学领域；2. 摘要未说明模型规模、计算开销、推理延迟等部署指标；3. 仅验证了DNA序列场景，虽声称domain-agnostic（可推广到FFPE、bisulfite、metagenomic、nanopore等），但未在遥感/图像领域验证；4. 摘要未说明方法在非序列数据（如图像）上的适用性论证。

Ideas for my research: 尽管场景不同，VESTIGE的核心方法论思想可类比迁移到遥感在轨目标检测的退化建模中：1. "将掩码/增强分布与经验测量的逐位置退化分布对齐"——可类比为遥感图像中传感器退化（如大气散射、云遮挡、辐射噪声）具有空间位置依赖性的场景，设计位置感知的数据增强策略；2. "parameter-free、drop-in replacement"的设计哲学——即插即用、不引入额外参数，与在轨轻量化约束契合；3. "隔离单一变量（空间重分布）进行消融"的实验设计思路，可用于验证遥感数据增强/退化建模方法的有效性归因；4. 对"degraded or noisy sequence inputs"的鲁棒训练思想，可类比遥感图像在轨获取时的噪声/退化鲁棒目标检测。但整体借鉴价值有限，属于间接方法论启发。

Abstract: Standard masked-language-model fine-tuning applies a uniform masking probability across every token position, assuming reconstruction difficulty is position-agnostic. When the degradation process is characterised and concentrated at predictable positions, this assumption fails: at peak damage sites the model can underperform a frequency-matched random predictor. We introduce VESTIGE, a parameter-free, drop-in replacement for the standard MLM collator that aligns the masking distribution with an empirically measured per-position corruption profile. We apply it to ancient DNA (aDNA) reconstruction, where cytosine deamination produces a position-dependent C-to-T / G-to-A gradient quantified per-position by mapDamage2. Rescaling so the mean C/G masking rate equals 15% - identical to standard MLM - isolates spatial redistribution as the sole variable, with model, data, seed, and hyperparameters held fixed across both DNABERT-2 runs on a mammoth CDS corpus (two specimens, seven genes). Across six terminal-zone widths and 626 paired windows, VESTIGE leads standard MLM at every width (Delta = +4.18 to +10.35 pp, all p < 10^-8), cuts validation cross-entropy by 13% (3.274 vs. 3.757), and yields ESMFold reconstructions with TM-score > 0.95 across all six reconstructions (three genes) even under damage amplified 10-30x beyond authentic PMD rates. A 1D CNN biosecurity classifier returns AUC = 0.935 and clears 98.2% of reconstructed windows, the 1.76% remainder attributable to reference-genome features, not reconstruction artefacts. The principle is domain-agnostic: any measurable position- or context-specific corruption profile - FFPE, bisulfite, metagenomic, or nanopore - substitutes directly for the PMD array, making VESTIGE a knowledge-guided training routine for intelligent systems operating on degraded or noisy sequence inputs.

</details>


### [338] [Towards joint scaling laws with optimal batch size schedules](https://arxiv.org/abs/2607.27731)
*Jiaxiang Li,Zhiqi Bu,Shiyun Xu*

Main category: cs.LG

TL;DR: 本文从凸优化视角研究深度学习训练动力学，推导出学习率与batch size的联合损失表征，提出动态batch size调度策略，在LLM训练中优于静态batch size基线。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型（LLM）训练中的batch size调度策略，属于深度学习训练动力学/优化理论范畴，不涉及遥感、卫星、目标检测或模型压缩/轻量化任何一方面。相关性评分为1分。

Reading priority: 1分。与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM训练优化理论范畴，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：深度学习训练动力学分析与batch size调度优化。应用场景：大语言模型（large language model）训练。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构，仅提及"适用于通用优化器和模型架构"（general optimizers and model architectures），并针对大语言模型训练场景进行验证。

Lightweight method: 不涉及轻量化方法。本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计。其核心贡献是训练策略层面的batch size动态调度，而非模型压缩或推理加速。

Onboard deployability: 摘要未说明。论文不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。其关注点是训练阶段（training）而非推理阶段（inference）的效率。

Datasets and metrics: 摘要未说明具体数据集和评价指标。仅提及在"大语言模型训练"中验证动态batch size调度优于静态基线，但未给出具体数据集名称、loss指标数值或对比实验细节。

Experiments: 摘要仅说明所提出的联合scaling laws（joint scaling laws）"持续优于静态batch size基线"（consistently outperform static batch size baselines），但未提供具体实验数据、对比方法列表或消融实验细节。

Limitations: 摘要未说明局限性。但可推断：1）该研究聚焦于训练阶段而非推理阶段，与部署/轻量化无直接关联；2）验证场景为LLM训练，未涉及遥感图像、目标检测等视觉任务；3）动态batch size调度在分布式训练中的工程实现复杂度未讨论。

Ideas for my research: 尽管与本研究方向弱相关，但存在间接借鉴点：1）若在轨遥感目标检测模型需要在地面进行大规模预训练，动态batch size调度策略可能有助于加速训练收敛、节省训练资源，从而间接支持轻量化模型的快速迭代；2）学习率与batch size的联合调度思想可迁移到知识蒸馏（knowledge distillation）训练中，优化教师-学生模型的蒸馏训练效率。但整体借鉴价值有限。

Abstract: Modern deep learning typically keeps the batch size static throughout training, thus overlooking the joint effect of learning rate and batch size on the training dynamics. In this paper, we study the deep learning dynamics through the lens of convex optimization and derive a joint characterization of loss in terms of both schedules, applicable to general optimizers and model architectures. This characterization yields a closed-form optimal batch size schedule for any prescribed learning rate schedule, and further leads to joint scaling laws that consistently outperform static batch size baselines, highlighting the significance of dynamic batch size schedule in large language model training.

</details>


### [339] [Improving the Robustness/Accuracy Tradeoff Against Adversarial Attacks Using Information Bottleneck Distillation Through Dual Teachers](https://arxiv.org/abs/2607.27737)
*Vincent Ryusuke Takahashi,Yoshinari Takeishi,Jun'ichi Takeuchi,Kave Salamatian*

Main category: cs.LG

TL;DR: 提出一种双教师知识蒸馏框架，在IBD基础上引入仅用干净样本训练的clean teacher，通过跨层注意力矩阵将干净和鲁棒教师特征同时迁移给学生网络，在CIFAR-10/100上提升干净样本分类精度同时保持对抗鲁棒性。

Research relevance: 弱相关（2分）。论文属于泛机器学习/泛视觉领域，聚焦对抗鲁棒性与知识蒸馏，与遥感、卫星、在轨部署、目标检测均无直接关联。唯一可借鉴的是knowledge distillation这一轻量化技术手段，但应用场景（对抗鲁棒性分类）与遥感目标检测差异很大。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向弱相关，仅knowledge distillation技术本身有一定借鉴价值，但应用场景、任务类型和评价指标均不匹配，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像分类（classification）与对抗鲁棒性（adversarial robustness）。应用场景：通用视觉分类，非遥感场景。传感器类型：摘要未说明（使用CIFAR-10/CIFAR-100通用数据集）。目标类别：CIFAR-10（10类）、CIFAR-100（100类）通用物体类别。

Model architecture: 基于Information Bottleneck Distillation (IBD)框架扩展，引入双教师（dual-teacher）结构：一个robust teacher（对抗训练得到）和一个clean teacher（仅用干净样本训练）。通过cross-layer attention matrix将两个教师的特征迁移到student网络。摘要未说明student网络的具体架构（如ResNet等）。

Lightweight method: 采用knowledge distillation（知识蒸馏）作为模型压缩/轻量化手段。核心创新在于双教师蒸馏：在原有robust teacher基础上增加clean teacher，通过跨层注意力矩阵实现特征级蒸馏。但摘要未涉及pruning、quantization、NAS等其它轻量化技术。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗等部署相关信息，也未涉及星上/边缘部署场景。

Datasets and metrics: 数据集：CIFAR-10和CIFAR-100。评价指标：干净样本分类精度（clean accuracy）、对抗样本精度（adversarial accuracy）、以及二者之间的harmonic mean（调和均值）。对比方法包括原始IBD和SOTA方法B-MTARD（dual-teacher distillation框架）。

Experiments: 实验表明：相比原始IBD，所提方法在干净样本上分类精度更高，同时对抗样本精度基本持平；与B-MTARD等SOTA方法相比，在clean与robust精度的harmonic mean上具有竞争力。还分析了不同训练设置对attention module的影响。

Limitations: 1. 仅验证于CIFAR-10/CIFAR-100小规模通用数据集，未在遥感图像、SAR、高光谱等场景验证；2. 任务为分类而非目标检测，未涉及检测框回归、小目标等遥感核心问题；3. 摘要未报告student模型的实际压缩效果（参数量、FLOPs缩减比例）；4. 未讨论部署场景与实时性；5. 双教师+跨层注意力矩阵可能增加训练复杂度，摘要未说明训练开销。

Ideas for my research: 1. 双教师蒸馏思想可借鉴：在遥感目标检测轻量化中，可考虑用"高精度大模型+轻量小模型"双教师蒸馏，或结合不同训练策略（如不同数据增强、不同分辨率）的教师来提升student在遥感小目标上的表现；2. cross-layer attention matrix的特征级蒸馏方式可迁移到遥感检测模型的feature distillation中，帮助student保留多尺度特征信息；3. 但需注意：该方法的蒸馏目标是鲁棒性+精度平衡，而遥感在轨场景更关注精度+速度/能耗平衡，需重新设计蒸馏目标函数。

Abstract: Deep neural networks (DNNs) have achieved remarkable success in classical machine learning problems. However, they are known to be vulnerable to adversarial attacks. Countermeasures proposed in the literature, notably Information Bottleneck Distillation (IBD) introduced by Kuang et al., degrade the classification accuracy on clean inputs while improving the robustness to adversarial inputs. In this work, we extend the IBD framework by introducing an extra teacher model (clean teacher) trained with only clean inputs, into the distillation process from a robust teacher model trained by adversarial training. The features of both clean and robust teachers are transferred to the student through a cross-layer attention matrix. Experimental results on the CIFAR-10 and CIFAR-100 datasets show that the proposed method improves classification accuracy on clean samples compared to the original IBD, while maintaining similar accuracy on adversarial samples. Furthermore, our methods are competitive with state-of-the-art approaches, including the recent dual-teacher distillation framework B-MTARD, particularly in terms of the harmonic mean between clean and robust accuracy. We also analyze the impact of different training settings that have different influences on the attention module.

</details>


### [340] [ROCS: Request-Oriented Compute Sharing for Efficient Large-Scale Recommendation](https://arxiv.org/abs/2607.27744)
*Yuxin Chen,Liang Luo,Buyun Zhang,Jian Jiao,Boda Li,Haoyu Wang,Tongyi Tang,Ao Cai,Zijian Shen,Zhengkai Zhang,Wenyi Xie,Ryan Dick,Han Liu,Neng Shi,Bin Yu,Jianbo Xiao,Shuyao Bi,Hongtao Yu,Yuanwei Fang,Zhuoran Zhao,Sijia Chen,Yang Chen,Shuqi Yang,Qianru Li,Zikun Liu,Wei Ling,Sihan Zeng,Longhao Jin,Jiaxin Lu,Yinbin Ma,Jiawei Li,Yichen Ruan,Yong Ler Lee,Birmingham Guan,Zijian Li,Jianbo Sun,Zhengyu Zhang,Zeliang Chen,Xiaohan Wei,Yuchen Hao,GP Musumeci,Venkatesh Ranganathan,Yantao Yao,Chunqiang Tang,Wenlin Chen,Santanu Kolay,Ellie Dingqiao Wen*

Main category: cs.LG

TL;DR: 提出面向推荐系统推理的请求导向计算共享（ROCS）范式，通过延迟请求-候选交互、隔离候选相关表征、每请求而非每候选评估模型主体，配合广义层掩码（GLM）、深度交叉注意力（DCA）和核内广播优化（IKBO），在保持或提升预测质量的同时显著提升推理效率并降低基础设施成本。

Research relevance: 弱相关（2分）。论文聚焦推荐系统（recommendation systems）的推理效率优化，属于泛机器学习/泛推理加速方向，与遥感、卫星、Earth observation、aerial image、SAR、高光谱等遥感场景完全无关，也不涉及目标检测任务。但其"计算共享"（compute sharing）思想——利用请求侧特征在多个候选间共享的特性来减少重复计算——在推理加速方法论层面有一定借鉴意义，故评为2分（泛机器学习方法，弱相关）。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向弱相关，场景差异大，且未涉及模型压缩技术。但其计算共享/推理加速的范式思想对多尺度检测、多候选检测中的计算复用设计有一定启发，可作为方法论参考而非核心文献阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：推荐系统推理（recommendation inference），包括检索（retrieval）和排序（ranking）阶段。应用场景：广告（ads）和自然内容（organic surfaces）的大规模推荐系统。传感器类型：不适用（非遥感）。目标类别：用户-候选物品交互（user-candidate interaction），非遥感目标检测。

Model architecture: 提出两种核心架构组件：(1) Generalized Layer Masking (GLM)——在特征交互架构中强制实现候选隔离（candidate isolation），使请求侧特征与候选侧特征在层内解耦；(2) Deep Cross Attention (DCA)——将请求导向共享扩展到序列架构（sequence architectures）。整体范式为Request-Oriented Compute Sharing (ROCS)，核心思想是尽可能延迟请求-候选交互（defer request-candidate interactions as late as possible），将模型主体部分每请求评估一次而非每候选评估一次。

Lightweight method: 本文并非传统意义上的模型轻量化（pruning/quantization/distillation/NAS），而是推理范式层面的计算共享优化：通过架构重设计（GLM、DCA）将请求侧共享计算与候选侧独立计算解耦，减少重复计算。配套的In-Kernel Broadcast Optimization (IKBO) 是面向GPU部署的核内广播优化，属于推理加速/部署优化范畴，与轻量化目标（降低计算量、提升吞吐）方向一致，但方法本质是计算复用而非模型压缩。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等具体部署指标。仅报告了生产级工作负载下的吞吐提升：检索模型QPS提升最高3x且无质量损失；短视频排序模型在50% QPS增益下获得0.5%相对LogLoss改进。部署环境为大规模GPU集群（production-scale GPU deployment），非星上/边缘嵌入式环境（FPGA/NPU/TensorRT等均未涉及）。

Datasets and metrics: 数据集：公开基准（public benchmarks）用于跨推荐backbone的质量-效率权衡验证；另使用生产级工作负载（production-scale workloads）验证。评价指标：QPS（每秒查询数，推理吞吐）、LogLoss（排序质量）、质量-效率权衡（quality-efficiency tradeoff）。摘要未说明具体公开数据集名称。

Experiments: 主要实验结果：(1) 在公开基准上，ROCS在多个推荐backbone上一致改善质量-效率权衡；(2) 生产级检索模型上实现最高3x QPS提升且无质量退化；(3) 短视频排序模型上实现50% QPS增益同时LogLoss相对改善0.5%；(4) 已部署于覆盖广告和自然内容、检索和排序阶段、跨两个数量级推理复杂度的超大规模推荐系统，在线收益显著且基础设施成本降低。摘要未说明消融实验细节。

Limitations: (1) 与遥感/卫星/目标检测完全无关，任务场景差异巨大；(2) 计算共享的前提是"每个请求评估多个候选且请求侧特征共享"，这一特性在遥感目标检测中并不天然存在（单张遥感图像通常对应单次推理，不存在多候选共享请求特征的结构）；(3) 摘要未提供模型参数量、FLOPs、延迟、能耗等轻量化关键指标；(4) 部署环境为大规模GPU数据中心，与星上/边缘受限硬件环境差异显著；(5) 未涉及任何模型压缩技术（pruning/quantization/distillation/NAS）。

Ideas for my research: 尽管场景不同，ROCS的"计算共享/计算复用"思想对遥感在轨目标检测轻量化有一定启发：(1) 在遥感图像中，若采用滑窗/多尺度检测策略，相邻窗口或相邻尺度间存在大量重叠特征计算，可借鉴"延迟交互、共享主体计算"的思路减少重复卷积计算；(2) 对于多目标检测场景，backbone特征提取部分可在多个检测头/候选框之间共享，这与ROCS"每请求评估一次而非每候选评估一次"的思想在结构上类似，可启发设计更高效的检测头共享机制；(3) IKBO的核内优化思路可迁移到FPGA/NPU上的算子融合与数据复用设计；(4) 但需注意，ROCS的核心价值在于推荐系统特有的"一请求多候选"结构，直接迁移到单图单次推理的遥感检测场景需重新设计共享粒度。

Abstract: Modern recommendation models gain prediction quality by scaling feature-interaction and sequence modules, but production cost constraints cap how far systems can scale.
  In this work, we propose Request-Oriented Compute Sharing (ROCS), a modeling and inference paradigm that exploits a unique property of recommendation inference: each user request is evaluated against many candidates, while request-side features are shared across candidates. ROCS defers request-candidate interactions as late as possible, isolates candidate-dependent representations, and evaluates substantial portions of the model once per request rather than once per candidate, significantly improving inference efficiency while maintaining or improving prediction quality. To realize this paradigm, we develop Generalized Layer Masking (GLM) to enforce candidate isolation in feature-interaction architectures, and Deep Cross Attention (DCA) to extend request-oriented sharing to sequence architectures. To support efficient GPU deployment, we co-design In-Kernel Broadcast Optimization (IKBO) that significantly accelerates ROCS model execution.
  Experiments on public benchmarks show that ROCS consistently improves the quality-efficiency tradeoff across recommendation backbones. On production-scale workloads, ROCS achieves up to a 3x QPS improvement on retrieval models without quality degradation and a 0.5% relative LogLoss improvement with a 50% QPS gain on a short-form video ranking model. ROCS has been deployed across large-scale recommendation systems spanning ads and organic surfaces, retrieval and ranking stages, and more than two orders of magnitude in inference complexity, delivering significant online gains at reduced infrastructure cost.

</details>


### [341] [DAS-PMVC: A Framework for Partial Multi-View Clustering via Dual Alignment and Structure Enhancement](https://arxiv.org/abs/2607.27761)
*Shubin Ma,Liang Zhao,Chuanye He,Zhenjiao Liu,Liang Zou,Lin Yuanbo Wu,Yu Shao*

Main category: cs.LG

TL;DR: 提出DAS-PMVC框架，通过锚图结构对齐、结构增强特征学习和双重对齐策略解决多视图聚类中的部分视图对齐问题（PVAP）。

Research relevance: 弱相关（2分）。该论文属于多视图聚类（multi-view clustering）领域，是泛机器学习/泛视觉方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然聚类方法在遥感场景理解中可能有间接借鉴价值，但论文本身不涉及遥感数据、目标检测或模型压缩加速。

Reading priority: 1分。该论文属于多视图聚类方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不涉及遥感数据、目标检测或模型压缩加速，阅读优先级极低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多视图聚类（multi-view clustering）；应用场景：通用数据聚类，非遥感场景；传感器类型：摘要未说明；目标类别：无特定目标类别，处理的是多视图样本数据。

Model architecture: DAS-PMVC框架包含三部分：(1) anchor graph structure alignment——基于锚点关系构建样本联合嵌入表示，实现初始视图对齐；(2) structure-enhanced feature learning——通过预训练学习视图结构信息，结合multi-view graph convolutional networks（多视图图卷积网络）提取深层潜在特征；(3) dual alignment strategy——预训练阶段通过锚图进行初始对齐，训练阶段引入contrastive learning loss（对比学习损失）和Hungarian algorithm（匈牙利算法）进一步优化潜在特征对齐。

Lightweight method: 摘要未说明。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等任何部署相关信息，也未讨论星上或边缘部署可行性。

Datasets and metrics: 摘要提到"various datasets"（多种数据集），但未具体列出数据集名称；评价指标为聚类性能（clustering performance），具体指标（如ACC、NMI、ARI等）摘要未说明。

Experiments: 摘要仅说明DAS-PMVC在多种数据集上优于现有state-of-the-art方法，展示了有效性和优越性。具体对比方法、消融实验细节均未在摘要中说明。

Limitations: 摘要未说明局限性。主要缺口包括：未涉及遥感场景、未涉及目标检测任务、未涉及模型轻量化/部署、未报告计算开销和推理效率。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但以下思路可能有间接借鉴价值：(1) anchor graph structure alignment中的锚点图对齐思想，可类比于遥感多传感器（光学+SAR）数据融合中的跨模态对齐问题；(2) contrastive learning loss在多视图特征对齐中的应用，可借鉴到遥感多光谱/高光谱波段特征融合；(3) Hungarian algorithm用于对齐优化，可类比于遥感目标检测中检测框与真值框的匹配策略。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: In recent years, multi-view clustering has attracted widespread research interest. However, due to limitations in data collection devices, data across different views often suffer from misalignment, leading to the partial view alignment problem (PVAP). To mitigate the impact of view asymmetry and irrelevant samples, this paper proposes a framework for partial multi-view clustering via dual alignment and structure enhancement (DAS-PMVC), which leverages view structure consistency and semantic relevance. Specifically, DAS-PMVC includes three parts: \textbf{anchor graph structure alignment}, where sample joint embedding representations with consistent latent space are derived from anchor point relationships for initial view alignment; \textbf{structure-enhanced feature learning}, where the model learns view structure information through pretraining and combines multi-view graph convolutional networks to further extract deep latent features from the aligned graph structure to improve the discriminative power of representations; and \textbf{a dual alignment strategy}, where initial alignment is performed through the anchor graph in the pretraining phase, and contrastive learning loss and the Hungarian algorithm are introduced in the training phase to further optimize the alignment of latent features. Experimental results on various datasets demonstrate that the DAS-PMVC framework outperforms existing state-of-the-art methods in clustering performance, showcasing its effectiveness and superiority.

</details>


### [342] [Train Small, Deploy Large: Zero-Shot GNN Transfer Through Geometric Renormalization](https://arxiv.org/abs/2607.27767)
*Robert Jankowski,Pedro Almagro-Blanco,Marián Boguñá,Melanie Weber,M. Ángeles Serrano*

Main category: cs.LG

TL;DR: 提出一种基于几何重整化（geometric renormalization, GR）的零样本迁移协议，在缩小的图副本上训练GNN，再将权重直接迁移到全分辨率图上，无需重训练即可保持预测性能并显著降低训练成本。

Research relevance: 弱相关（2分）。论文研究的是图神经网络（GNN）在图规模缩放下的零样本迁移问题，属于泛机器学习/图学习方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。但其"在小规模副本上训练、迁移到大规模网络"的思想与知识蒸馏/模型压缩中的"小模型训练后迁移到大模型"或"跨尺度泛化"有一定类比借鉴意义，因此给予2分而非1分。

Reading priority: 2分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于图学习基础研究。仅"跨尺度训练迁移"概念有一定类比价值，可作为背景阅读或灵感来源，不建议作为核心参考文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图神经网络跨尺度零样本迁移（zero-shot transfer）。应用场景：大规模图数据（百万级节点）上的GNN训练与部署。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。摘要未说明具体图数据领域（合成图与真实世界网络）。

Model architecture: 基于GNN（graph neural networks）架构，采用几何重整化（geometric renormalization, GR）对图进行粗粒化（coarse-graining），在缩小的图副本上训练GNN，训练得到的权重直接迁移到原始全分辨率网络。摘要未说明具体GNN层类型（如GCN、GAT、GraphSAGE等）。

Lightweight method: 核心思想是"训练时用小规模图副本，部署时用全分辨率图"，通过权重直接迁移实现训练成本的大幅降低。这与知识蒸馏（knowledge distillation）中"小模型训练、大模型部署"或"teacher-student"范式有概念上的相似性，但并非传统意义上的模型压缩（pruning/quantization/NAS）。摘要未提及剪枝、量化、蒸馏等经典轻量化手段。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署指标。论文关注的是训练成本（training cost）的降低而非推理阶段的计算开销，因此对星上/边缘实时推理部署的直接参考价值有限。

Datasets and metrics: 使用了合成图（synthetic networks）和真实世界网络（real-world networks）两类数据。评价指标为预测性能（predictive performance）和训练成本（training cost）。摘要未说明具体数据集名称和具体指标数值。

Experiments: 实验表明：在GR缩小的副本上训练GNN，能保留原始规模的大部分预测性能，同时显著降低训练成本；学习到的表示（learned representations）和预测轨迹（predictive trajectories）在不同尺度间保持对齐。摘要未给出具体对比方法和消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测场景完全无关，属于图学习基础研究；2. 摘要未说明该方法是否适用于图像/视觉任务，仅针对图数据；3. 未提供部署侧指标（FLOPs、延迟、能耗等）；4. 未说明跨尺度迁移在何种图结构/任务上会失效；5. "scale-equivariant graph architectures"仍处于概念提出阶段，未给出具体实现。

Ideas for my research: 1. 概念类比：该方法"在小规模副本上训练、迁移到大规模网络"的思想，可类比到遥感目标检测中"在低分辨率/降采样图像上训练检测器，迁移到全分辨率图像"的跨尺度训练策略，用于降低训练成本；2. 对轻量化检测器的启发：可探索"小输入分辨率训练+大输入分辨率推理"的零样本迁移协议，减少训练开销；3. 结构相似性（structural similarity）比网络规模更重要的结论，可启发在遥感小目标检测中关注特征结构保持而非单纯增大模型；4. 但整体而言，该方法与在轨目标检测轻量化（剪枝/量化/蒸馏/边缘部署）的直接可借鉴点有限，仅作为跨尺度泛化的理论参考。

Abstract: Graph neural networks (GNNs) can operate on large graphs but become infrastructure-sensitive at the scale of millions of nodes and typically require scalable training techniques for even larger graphs. This raises a central question: when can a model trained on a smaller, scaled-down replica of a graph be deployed on the full-resolution graph without retraining? We introduce a zero-shot transfer protocol in which a GNN is trained on a graph coarse-grained by geometric renormalization (GR), and the resulting weights are transferred directly to the original network. Across synthetic and real-world networks, training on GR scaled-down replicas preserves much of the original-scale predictive performance while significantly reducing training cost. We further find that learned representations and predictive trajectories remain aligned across scales. These findings suggest that structural similarity may be more important than network size in determining GNN transferability, opening a path toward scale-equivariant graph architectures.

</details>


### [343] [Beyond the Best Teacher: Expanding and Compressing the Reasoning Solution Manifold](https://arxiv.org/abs/2607.27770)
*Songshuo Lu,Zhi Chen,Yaohua Tang*

Main category: cs.LG

TL;DR: 提出一种"先扩展后压缩"的多教师策略蒸馏框架，通过RGRPO训练互补教师集合，再用TU-OPD将教师集合压缩到单一学生模型，在数学推理、代码生成和指令跟随任务上超越最强单教师。

Research relevance: 弱相关（2分）。该论文是泛机器学习/LLM领域的方法研究，聚焦于强化学习训练的策略蒸馏与多教师知识融合，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务。虽然"多教师蒸馏"和"模型压缩"思想对轻量化有间接借鉴意义，但论文本身与遥感卫星在轨目标检测方向基本无关。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向弱相关，属于LLM领域的多教师蒸馏方法研究。虽然"多教师互补蒸馏"和"reliability-gated加权"思想对知识蒸馏有借鉴价值，但缺乏遥感场景、目标检测任务和部署指标，优先级较低。若后续在蒸馏策略设计上需要灵感，可选择性阅读相关章节。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：数学推理（mathematical reasoning）、代码生成（code generation）、指令跟随（instruction following）。应用场景：大语言模型（LLM）的强化学习训练与知识蒸馏。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 学生模型为Qwen3-1.7B。框架包含两个阶段：(1) 扩展阶段——Residual Group Relative Policy Optimization (RGRPO) 从共同初始化训练一系列教师策略，每轮将后续训练重定向到尚未被已有教师集合覆盖的样本；(2) 压缩阶段——reliability-gated Teacher-Union On-policy Distillation (TU-OPD) 让学生从自身response前缀学习，仅可靠教师参与贡献，其sampled-token OPD损失按per-example质量加权。另提出Consensus-Residual Decomposition保留胜出教师的excess token偏好，防止专家行为在教师聚合中被抑制。

Lightweight method: 核心方法是知识蒸馏（knowledge distillation）的一种变体——多教师策略蒸馏（multi-teacher policy distillation）。通过将多个互补教师的知识压缩到单一学生模型（Qwen3-1.7B），实现"单模型推理"（single-model inference），从而在推理阶段保持轻量。不涉及剪枝（pruning）、量化（quantization）、NAS或硬件部署。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提到学生模型为Qwen3-1.7B（约17亿参数），并强调"retaining single-model inference"（保持单模型推理），说明推理阶段只需部署一个模型而非多个教师。但该模型是LLM，与星上遥感目标检测的嵌入式/边缘部署场景差异巨大。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标为相对改进百分比：数学推理相对提升2.0%，代码生成相对提升8.3%，指令跟随相对提升6.9%。对比对象为"最强的单个教师"（strongest individual teacher）。

Experiments: 在数学推理、代码生成、指令跟随三个领域进行实验，Qwen3-1.7B学生模型在三个领域均超越最强单教师，相对提升分别为2.0%、8.3%、6.9%。摘要未提供消融实验细节、具体基准数据集或与更多基线方法的对比。

Limitations: (1) 与遥感、卫星、目标检测完全无关，属于LLM强化学习与蒸馏领域；(2) 摘要未说明计算开销、训练成本、蒸馏效率等关键工程指标；(3) 未涉及任何视觉/图像任务，更不用说遥感图像目标检测；(4) 未讨论部署硬件约束（FPGA/NPU/TensorRT等）；(5) 未提供FLOPs、参数量对比、推理延迟等轻量化关键指标。

Ideas for my research: 尽管场景不同，但以下思想可迁移到遥感在轨目标检测模型轻量化：(1) "多教师互补蒸馏"思想——在遥感检测中，可以用不同架构（如不同backbone的检测器）或不同训练策略（如不同数据增强、不同损失函数）训练多个教师，利用其互补性蒸馏到单一轻量学生，避免单教师偏差；(2) "reliability-gated"加权蒸馏——在遥感小目标检测中，不同教师对不同类别/尺度的目标（如舰船、飞机、小目标）可靠性不同，可按per-example质量加权，让擅长小目标的教师主导小目标样本的蒸馏；(3) "Consensus-Residual Decomposition"——防止蒸馏过程中丢失教师对特定难样本的专家偏好，类似地，在遥感检测中可保留教师对难例（如遮挡、密集排列目标）的独特预测能力；(4) 该框架本质上是"先扩展后压缩"，与轻量化中"大模型训练、小模型部署"的范式一致，可借鉴其教师集合构建策略来提升蒸馏学生模型的精度。

Abstract: A single reinforcement-learning run can produce a strong reasoner yet an incomplete teacher: it often amplifies only a subset of the valid solution modes. We argue that reinforcement learning (RL)-trained policies should therefore be viewed as local probes of a multi-basin reasoning solution manifold, rather than as globally reliable supervisors. Based on this view, we propose an expand-then-compress framework that couples teacher construction with multi-teacher policy distillation. In the expansion stage, Residual Group Relative Policy Optimization (RGRPO) trains a sequence of teachers from a common initialization and redirects each later round toward examples not yet covered by the accumulated teacher union. In the compression stage, reliability-gated Teacher-Union On-policy Distillation (TU-OPD) lets the student learn from its own response prefixes. For each example, only reliable teachers contribute, and their sampled-token OPD losses are weighted by their per-example quality. We further introduce Consensus-Residual Decomposition, which preserves a winner teacher's excess token preferences over its reliable peers, preventing specialist behavior from being suppressed during teacher aggregation. Experiments on mathematical reasoning, code generation, and instruction following show that the resulting Qwen3-1.7B student consistently outperforms the strongest individual teacher across all three domains, yielding relative improvements of 2.0%, 8.3%, and 6.9%, respectively, while retaining single-model inference. These results establish a simple but powerful principle: stronger students can be obtained not by selecting a single better teacher, but by deliberately constructing and compressing a complementary teacher union.

</details>


### [344] [RIPPLE: Generating Multi-Channel Phase, Not Recovering It](https://arxiv.org/abs/2607.27775)
*Jaehyuk Lee,Yeajin Lee,Dayeon Shin,Donghun Lee*

Main category: cs.LG

TL;DR: 提出RIPPLE方法，将Griffin-Lim从相位恢复器重新解释为相位先验，用rectified flow在显式通道间相位损失下细化生成多通道波形的相位，在空间音频和地震跨站翻译两个领域优于基于恢复的管线。

Research relevance: 弱相关（2分）。论文涉及的是多通道波形（空间音频、地震图）的相位生成问题，属于信号处理/生成模型领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。仅从"生成模型+物理信息保持"的角度可能对遥感数据生成（如SAR图像仿真）有间接借鉴，但整体相关性很低。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于音频/地震信号生成领域。除非对多通道物理信息保持或生成模型先验设计有特殊兴趣，否则不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务：多通道波形的相位生成（phase generation），而非目标检测。应用场景：first-order ambisonics环境迁移（空间音频）和地震跨站翻译（seismic cross-station translation）。传感器类型：麦克风阵列（ambisonics）和地震仪（seismograms）。目标类别：不涉及目标检测，处理的是波形信号。

Model architecture: RIPPLE框架：将Griffin-Lim算法重新解释为相位先验（phase prior），而非最终估计器。从源相位初始化，该先验携带需保留的通道间结构，然后通过rectified flow在显式inter-channel phase loss下向目标细化。涉及生成模型（generative models）合成幅度谱，相位由RIPPLE生成而非恢复。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS或轻量网络设计等模型压缩/加速方法，也未讨论实时推理或边缘部署。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。论文面向的是信号生成质量而非部署效率。

Datasets and metrics: 数据集：first-order ambisonics环境迁移数据和地震跨站翻译数据（具体数据集名称摘要未说明）。评价指标：coherence metrics（通道间相干性指标）、S-wave polarization error（S波偏振误差，从57.3°随机期望降至33.8°）。摘要未说明具体指标名称如PSNR、SSIM等。

Experiments: 在两个物理上无关的领域测试：ambisonics环境迁移和地震跨站翻译。RIPPLE在coherence metrics上优于基于恢复的管线。地震案例中，跨不同架构的生成器，per-channel recovery的S波偏振误差接近57.3°随机期望，而learned phase将其降至33.8°。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于音频与地震信号处理领域。2. 摘要未说明模型复杂度、推理速度、部署可行性。3. 未讨论在遥感图像或SAR数据上的适用性。4. 未涉及目标检测任务或遥感场景理解。

Ideas for my research: 尽管与遥感目标检测轻量化方向弱相关，但有以下间接可借鉴点：1. "通道间物理信息保持"的思想可类比到多光谱/高光谱遥感图像中波段间相关性的保持——在遥感图像压缩或轻量化模型中，波段间光谱相关性是重要信息，类似论文强调的inter-channel phase coherence。2. 将传统算法（如Griffin-Lim）重新解释为先验而非最终估计器的思路，可类比于将传统遥感处理算法（如CFAR、形态学滤波）作为先验融入轻量检测网络。3. 论文指出"magnitude-based metrics掩盖物理信息丢失"的教训，提醒在遥感轻量化评估中不能只看mAP/FLOPs，还需关注物理保真度指标（如光谱保真、几何精度）。

Abstract: Generative models synthesize magnitude spectra with high fidelity, while phase is delegated to a recovery module---Griffin--Lim, a vocoder, or a latent decoder---applied independently to each channel. For multi-channel waveforms this delegation is costly: the physical content of spatial audio and three-component seismograms lives in the phase relationships between channels, precisely what channel-independent recovery cannot produce. The cost is also invisible, since the magnitude-based metrics common to both fields barely move when inter-channel phase coherence collapses---so a pipeline can discard the physical information in its output while still scoring well. We argue that phase should be generated, not recovered, and present RIPPLE (Rectified Inter-channel Phase with Prior-based LEarning), which reinterprets Griffin--Lim as a phase **prior** rather than a final estimator: initialized from the source phase, this prior carries the inter-channel structure to be preserved, and a rectified flow refines it toward the target under an explicit inter-channel phase loss. Tested on first-order ambisonics environment transfer and seismic cross-station translation---two physically unrelated domains---RIPPLE outperforms recovery-based pipelines on the coherence metrics that downstream analyses consume. The seismic case is decisive: across architecturally distinct generators, per-channel recovery leaves S-wave polarization error near the $57.3^\circ$ random expectation, whereas learned phase reduces it to $33.8^\circ$.

</details>


### [345] [LoRA Scaffolded Policy Optimization (LSPO): A Sampling-Time Low-Rank Scaffold for Recovering Reinforcement-Learning Gradient on Zero-Reward Cliff Prompts](https://arxiv.org/abs/2607.27787)
*Ken Ding*

Main category: cs.LG

TL;DR: 提出 LoRA Scaffolded Policy Optimization (LSPO)，一种采样时机制，通过在 RL 训练中检测"cliff prompts"（所有采样均失败的提示），用 LoRA 适配器做短暂监督微调后重新采样，将成功补全拼回 RL batch 并做重要性采样校正，从而恢复 GRPO 在能力边界提示上丢失的梯度，提升数学推理 RL 性能。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于大语言模型（LLM）的数学推理强化学习（RLVR/GRPO），研究对象是 DeepSeek-R1-Distill-Qwen-1.5B 等语言模型，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等任何遥感相关场景，也不涉及 object detection、小目标检测、旋转框检测等目标检测任务，更不涉及剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署等轻量化/部署方法。唯一与"轻量化"沾边的概念是 LoRA（低秩适配器），但 LoRA 在此处是作为 RL 训练中的采样辅助机制，而非模型压缩/加速手段，且其目的是提升数学推理精度而非部署效率。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于 LLM 数学推理强化学习领域。除非对 RL 训练策略（如困难样本梯度恢复）有跨领域兴趣，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型数学推理的强化学习（RLVR, GRPO）。应用场景：数学推理基准测试（DeepMath-103K, AIME24, AIME26, MATH500）。传感器类型：不适用（文本/语言模型）。目标类别：数学问题求解，非视觉目标检测。

Model architecture: 基础模型为 DeepSeek-R1-Distill-Qwen-1.5B；引入 LoRA 低秩适配器作为采样辅助模块，与基础模型叠加（base-plus-adapter）用于重新采样 cliff prompts；RL 梯度仅作用于 base 模型，adapter 只接收监督梯度并在 checkpoint 时丢弃，最终得到 base-only 模型。训练框架为 GRPO（Group Relative Policy Optimization），对比基线为 DAPO。

Lightweight method: 摘要未说明传统轻量化方法（剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署等均未涉及）。唯一相关概念是 LoRA（Low-Rank Adaptation），但此处 LoRA 并非用于模型压缩或加速，而是作为 RL 训练中恢复梯度信号的采样时辅助机制，属于训练策略而非部署优化手段。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息。虽然基础模型为 1.5B 参数规模，但论文关注的是训练算法改进而非推理部署优化，且场景为数学推理而非在轨遥感目标检测。

Datasets and metrics: 训练数据：DeepMath-103K。评测基准：AIME24, AIME26, MATH500。评价指标：pass@k（pass@1, pass@4, pass@16）。实验设置：n=5 paired seeds per arm，匹配 1000-step reporting horizon，对比基线为 DAPO。

Experiments: LSPO 在全部 16 个 (benchmark, pass@k) 组合上 5-seed 均值匹配或超越 DAPO 基线（15 个严格胜出，1 个平局）。具体增益：AIME24/pass@4 最高 +10.7 分，AIME24 和 AIME26 在 pass@16 上各 +6.7 分，MATH500/pass@1 上 +2.4 分，16 个组合平均提升 +3.8 分。

Limitations: 摘要未说明局限性。但可推断：1）方法针对 LLM 数学推理 RL 设计，与遥感视觉目标检测场景无直接迁移路径；2）LSPO 依赖 ground-truth solutions 做监督微调，在遥感标注稀缺场景下难以直接套用；3）摘要未报告计算开销、训练成本或推理效率；4）仅验证了 1.5B 规模模型，未验证更大或更小模型。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但存在两个可借鉴的抽象思想：1）"cliff prompts"（所有采样均失败的困难样本）概念可类比遥感小目标/遮挡目标等困难样本——在检测模型训练中，对困难样本的梯度信号可能被 group-normalized 归一化稀释，可考虑类似"识别困难样本→针对性增强→重要性采样校正"的训练策略；2）LoRA 作为临时辅助模块、训练后丢弃、仅保留 base 模型的思想，与"训练时用大模型辅助、部署时用轻量模型"的知识蒸馏/辅助训练范式有概念上的相通之处。但这些均为间接启发，不构成直接可复现方法。

Abstract: Reinforcement learning from verifiable rewards (RLVR) for mathematical reasoning suffers from a structural blind spot: on "cliff" prompts-those on which every sampled rollout in a group fails-the group-normalized advantage is identically zero, so GRPO produces no gradient on precisely the prompts at the frontier of the model's capability. We introduce LoRA Scaffolded Policy Optimization (LSPO), a sampling-time mechanism that recovers this lost gradient. Each RL step, LSPO detects cliff prompts, fits a small low-rank (LoRA) adapter by a brief supervised step on their ground-truth solutions, re-rolls the cliffs with the base-plus-adapter model, splices the now-successful completions back into the RL batch with an importance-sampling correction, and takes a GRPO step on the base alone; the adapter receives only the supervised gradient and is discarded at checkpoint, yielding a base-only model. On DeepMath-103K with DeepSeek-R1-Distill-Qwen-1.5B, evaluated over n=5 paired seeds per arm at a matched 1000-step reporting horizon, LSPO's 5-seed mean matches or beats a DAPO baseline on all 16 (benchmark, pass@k) cells (15 strict wins and one exact tie), with gains of up to +10.7 points on AIME24/pass@4, +6.7 points on AIME24 and AIME26 at pass@16, and +2.4 points on MATH500/pass@1; averaged over the 16 cells the improvement is +3.8 points.

</details>


### [346] [Revisiting Predictive Process Monitoring in the Age of Foundation Models: A Comparative Study of Sequence, Tabular, and LLM Approaches](https://arxiv.org/abs/2607.27797)
*Lennart Fertig,Lukas Kirchdorfer,Tobias Sesterhenn*

Main category: cs.LG

TL;DR: 本文系统比较了预测性流程监控（PPM）中三种建模范式——经典序列模型（如LSTM）、大语言模型（LLM）和表格基础模型——在多个数据集和预测任务上的表现，发现序列模型在下一活动预测上最优，表格基础模型在时间类任务上有竞争力，而LLM通常成本高但性能落后。

Research relevance: 与本研究方向基本无关（1分）。论文主题是业务流程监控（Predictive Process Monitoring），属于业务流程管理/过程挖掘领域，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能完全无关；不涉及目标检测、小目标检测、旋转框检测或遥感场景理解；也不涉及轻量化、剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署等模型压缩或部署技术。唯一可能的间接关联是论文比较了不同模型范式的效率与性能权衡，但这属于泛机器学习方法论层面的讨论，对遥感在轨目标检测轻量化课题的借鉴价值极低。

Reading priority: 1分。论文主题为业务流程监控中的预测建模范式比较，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及任何遥感、目标检测或模型压缩/部署技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：预测性流程监控（Predictive Process Monitoring, PPM），包括下一活动预测（next activity prediction）、案例剩余完成时间预测（remaining time）、下一事件时间预测（time to next event）。应用场景：业务流程管理/过程挖掘，非遥感场景。传感器类型：不适用（使用事件日志 event logs）。目标类别：不适用。

Model architecture: 比较了三种建模范式：(1) 经典序列深度模型，如 Long Short-Term Memory (LSTM)；(2) 大语言模型（Large Language Models, LLMs）；(3) 具备 in-context learning 能力的表格基础模型（tabular foundation models）。摘要未说明具体网络结构细节。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等模型压缩或加速方法。仅从成本角度提及LLM"higher cost"，但未给出具体轻量化或部署优化方案。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。仅定性提到LLM成本更高（higher cost），无量化数据。

Datasets and metrics: 摘要提到"multiple datasets and prediction tasks"（多个数据集和多种预测任务），但未列出具体数据集名称。评价指标未在摘要中明确说明（如 accuracy、MAE、RMSE 等均未提及）。

Experiments: 通过受控基准（controlled benchmark）在多个数据集和预测任务上对三种建模范式进行概念性和经验性比较。主要结论：(1) 序列模型在下一活动预测任务上始终表现最佳；(2) 表格基础模型在时间类任务（remaining time、time to next event）上有竞争力；(3) LLM 通常性能落后且成本更高。摘要未给出具体数值结果或消融实验。

Limitations: 摘要未说明局限性。但可推断：(1) 论文聚焦业务流程监控领域，与遥感/卫星/在轨目标检测完全无关；(2) 未提供任何模型压缩、轻量化或部署相关的技术细节；(3) 未给出具体数据集、指标和数值结果，难以评估实验的全面性；(4) 未讨论模型在资源受限环境下的可行性。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的借鉴点非常有限。可能的间接启发：(1) 论文比较不同模型范式（序列模型 vs 基础模型）的性能-成本权衡思路，可类比到遥感目标检测中"轻量模型 vs 大模型蒸馏"的权衡分析；(2) 表格基础模型的 in-context learning 思路或许可启发遥感小样本目标检测的少样本学习方法，但关联较弱；(3) 论文强调"经典模型在特定任务上仍具竞争力"的结论，可类比提醒：在星上资源受限场景下，经典轻量检测模型（如 YOLO 系列轻量变体）可能仍优于复杂大模型蒸馏方案。总体而言，该论文对本课题的参考价值很低。

Abstract: Predictive process monitoring (PPM) leverages event logs to forecast the future of running process instances, for instance, predicting the next activity, the remaining time until case completion, or the time to the next event. While PPM research in recent years has been dominated by deep sequence models trained from scratch, such as Long Short-Term Memory (LSTM) models, foundation-model approaches---particularly large language models (LLMs)---are increasingly explored for PPM. At the same time, tabular foundation models with in-context learning capabilities offer a promising alternative but have not yet been systematically benchmarked for PPM. Thus, it remains unclear whether classical sequence-based models remain competitive in this evolving landscape. This paper compares the three modeling paradigms both conceptually and empirically through a controlled benchmark across multiple datasets and prediction tasks. The results show that sequence models consistently perform best for next activity prediction, whereas tabular foundation models are competitive on temporal tasks, with LLMs usually lagging behind despite higher cost.

</details>


### [347] [Learning-Augmented and Randomized Algorithms for Line Aggregation with Delays](https://arxiv.org/abs/2607.27807)
*Tianhang Lu,Runtian Ren,Shengcai Liu,Ke Tang*

Main category: cs.LG

TL;DR: 本文研究带延迟的线度量空间上的在线聚合问题，提出学习增强的确定性Balance算法和随机化算法，分析其鲁棒性与一致性，并给出竞争比下界。

Research relevance: 与本研究方向基本无关（1分）。论文属于理论计算机科学中的在线算法设计（online algorithms）与学习增强算法（learning-augmented algorithms）研究，讨论的是线度量空间上的在线聚合问题（online aggregation with delays on a line metric），完全不涉及遥感、卫星、地球观测、目标检测或模型轻量化等任何相关主题。

Reading priority: 1分。该论文属于理论计算机科学中的在线算法与学习增强算法研究，与遥感卫星在轨目标检测及模型轻量化方向完全无关，不建议阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：在线聚合问题（online aggregation）的理论算法设计；应用场景：线度量空间上的服务调度/聚合；传感器类型：不适用；目标类别：不适用。摘要未说明任何遥感或视觉相关场景。

Model architecture: 提出确定性学习增强Balance算法和随机化算法，以及结合两者的随机化学习增强算法。核心是算法竞争比（competitive ratio）、鲁棒性（robustness）和一致性（consistency）的理论分析，不涉及神经网络或深度学习模型结构。

Lightweight method: 不涉及。本文的"learning-augmented"指的是利用在线建议（advice）增强算法性能的理论框架，与模型压缩中的剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等轻量化方法完全无关。

Onboard deployability: 不适用。本文是纯理论算法研究，不涉及参数量、FLOPs、延迟、显存、能耗、实时推理或边缘/星上部署等任何部署相关信息。

Datasets and metrics: 无标准数据集。评价指标为竞争比（competitive ratio）、鲁棒性（robustness）和一致性（consistency）。实验为数值实验（numerical experiments）以验证理论分析，摘要未说明具体实验设置。

Experiments: 主要理论结果包括：确定性学习增强Balance算法达到(4/λ+1/λ²)-robust和(4+λ)-consistent；随机化算法在对抗性模型下达到(e+1)-competitive，优于确定性5-competitive的Balance基准；建立了随机化在线算法竞争比e的下界；结合两者得到(e/λ+1/λ²)-robust和(e+λ)-consistent的随机化学习增强算法。摘要未说明具体数值实验结果。

Limitations: 本文与遥感卫星在轨目标检测及轻量化方向完全无关。摘要未涉及任何视觉任务、遥感数据、模型压缩或部署相关内容。其"learning-augmented"概念虽在理论上与"利用先验知识提升算法性能"有抽象关联，但属于在线算法理论范畴，对目标检测模型轻量化没有直接借鉴价值。

Ideas for my research: 借鉴价值极低。唯一可能的抽象启发是"learning-augmented"思想——即利用外部建议/先验信息来提升算法性能并分析鲁棒性与一致性的权衡，这一思路在理论上与"知识蒸馏（knowledge distillation）中教师模型提供软标签作为先验建议"有概念上的相似性，但本文是纯理论在线算法研究，不涉及任何深度学习或遥感内容，不建议作为课题参考。

Abstract: This paper studies learning-augmented and randomized online aggregation with delays on a line metric. We consider advice given as online suggested service lengths, and evaluate the algorithms in terms of robustness and consistency. For each $λ\in (0,1]$, we first propose a deterministic learning-augmented \textsc{Balance} algorithm that is $(4/λ+1/λ^2)$-robust and $(4+λ)$-consistent. We also propose a randomized algorithm for the problem in the classical adversarial model, which is $(e+1)$-competitive against an oblivious adversary, improving over the deterministic $5$-competitive \textsc{Balance} benchmark~\cite{bienkowski2013chain}. Notably, this competitive ratio is even lower than the lower bound of $4$ for deterministic online algorithms. Moreover, we establish a lower bound of $e$ on the competitive ratio of randomized online algorithms, improving the previous lower bound of $e/(e-1)$. Besides, we combine the two ideas and obtain a randomized learning-augmented algorithm that is $(e/λ+1/λ^2)$-robust and $(e+λ)$-consistent. Finally, we conduct numerical experiments to complement our theoretical analysis and evaluate the empirical performance of our algorithms.

</details>


### [348] [Contrastive Concept Importance: Explaining Pairwise Class Decisions Through Automatically Extracted Concept Representations](https://arxiv.org/abs/2607.27904)
*Roel Visser,Isaac Roberts,Barbara Hammer*

Main category: cs.LG

TL;DR: 提出对比概念重要性（CCI）方法，通过将目标类与对照类之间的logit margin归因到自动提取的视觉概念基上，回答"为什么是P而不是Q"的对比性可解释性问题。

Research relevance: 弱相关（2分）。该论文属于泛视觉/泛机器学习领域的可解释性（XAI）研究，聚焦于概念归因与对比性解释，与遥感、卫星、在轨部署、目标检测、轻量化均无直接关联。它既不是遥感场景，也不涉及目标检测模型本身，更不涉及任何模型压缩或加速方法。仅作为泛机器学习方法，对深度学习模型的可解释性分析有一定参考价值，但与"遥感卫星在轨目标检测模型轻量化"方向基本无关。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测模型轻量化"研究方向基本无关，属于泛机器学习可解释性研究。除非对模型可解释性分析有额外兴趣，否则不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：模型可解释性（XAI）中的概念归因（concept attribution）与对比性解释（contrastive explanation）。应用场景：通用视觉分类任务的模型行为分析，非遥感场景。传感器类型：不适用（使用ImageNet自然图像）。目标类别：ImageNet类别对（class pairs），非遥感目标。

Model architecture: 摘要未说明具体模型架构。方法基于CRAFT-style concept bases自动提取视觉概念基，对目标类与对照类之间的logit margin进行归因分解。核心是概念归因框架而非新的检测/分类网络结构。

Lightweight method: 不涉及。论文未提出任何剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计或边缘/星上部署相关的模型压缩与加速方法。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且论文本身不面向任何部署场景。

Datasets and metrics: 数据集：ImageNet类别对（class pairs），使用CRAFT-style concept bases。评价指标：insertion and deletion curves（插入/删除曲线）、logit-wise decomposition analysis（logit分解分析）、semantic class hierarchy（语义类别层级）。

Experiments: 在ImageNet类别对上评估CCI方法，结果显示对比概念重要性能够揭示普通概念重要性无法捕获的类别对特定模型行为；高度对比性的概念可对照语义超类结构评估其影响的是细粒度区分还是宽泛类别证据。

Limitations: 摘要未说明。但可推断：1）仅针对分类任务的解释，未涉及目标检测（object detection）场景；2）仅在ImageNet上验证，未在遥感（remote sensing）数据上测试；3）未讨论计算开销与部署可行性；4）概念基的自动提取质量可能影响归因可靠性。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但可借鉴的点有限：1）对比性归因思想（"为什么是P而不是Q"）可用于分析轻量化模型在遥感小目标检测中误检/漏检的原因，帮助诊断剪枝或量化后模型性能下降的根源；2）logit margin分解方法可用于评估轻量化模型与原始模型在特定类别对上的行为差异，辅助知识蒸馏（knowledge distillation）中教师-学生模型的对齐分析。但这些均为间接借鉴，不构成核心方法支撑。

Abstract: Concept-based explanations are a prevalent way to explain the decisions of complex black-box methods through semantically meaningful, human-interpretable concepts. To attribute the contribution of such concepts to a model's decisions, feature attribution methods are used to quantify how strongly each concept contributes to a model output. These attributions are typically computed for a single output class and therefore answer a non-contrastive "why P?" question. In many situations, however, such as cases of misclassification, class confusion, and low-margin predictions, the more natural question to ask is "why P rather than Q?".
  We introduce contrastive concept importance (CCI), which attributes the logit margin between a target class and a contrast, or foil, class to concepts in an automatically extracted visual concept basis. The resulting scores are signed, indicating whether a concept supports the target over the foil or the foil over the target, and can be decomposed into target-logit and foil-logit effects. This makes it possible to distinguish globally important concepts from concepts that specifically influence a class-pair distinction, including whether their effect is shared, one-sided, or directly contrastive.
  We evaluate the method on ImageNet class pairs using CRAFT-style concept bases, insertion and deletion curves, logit-wise decomposition analysis, and semantic class hierarchy. The results show that contrastive concept importance reveals class-pair-specific model behavior that is not captured by ordinary concept importance alone, and that highly contrastive concepts can be evaluated against semantic superclass structure to assess whether they affect fine-grained distinctions rather than broad category evidence.

</details>


### [349] [Class-Aware Reinforcement Learning for Counterfactual Explanation Generation](https://arxiv.org/abs/2607.27905)
*Muhammad Adil Saleem,Syed Ali Raza,Mary-Anne Williams*

Main category: cs.LG

TL;DR: 本文提出一种类别感知的强化学习（class-aware RL）方法用于生成反事实解释（Counterfactual Explanations, CFEs），通过在RL状态表示中加入实例的预测类别信息，提升黑盒模型可解释性生成的收敛速度、奖励优化和有效性。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于机器学习可解释性中的反事实解释生成，属于XAI（可解释人工智能）与强化学习的交叉领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景，也不涉及目标检测任务，更不涉及任何模型轻量化或边缘/星上部署技术。唯一可能的间接关联是"黑盒模型"概念与深度学习模型相关，但与本课题的遥感在轨目标检测轻量化方向没有实质联系。

Reading priority: 1分。论文主题为机器学习可解释性中的反事实解释生成，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感场景、目标检测任务或任何模型压缩/部署技术，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：反事实解释生成（Counterfactual Explanation Generation）；应用场景：机器学习模型可解释性（XAI）；传感器类型：不适用（非遥感场景）；目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体网络结构。核心方法为基于强化学习（Reinforcement Learning, RL）的CFE生成框架，关键创新在于RL状态表示（state representation）中引入实例的预测类别（predicted class）信息，形成class-aware RL，与不含类别信息的class-blind RL进行对比。

Lightweight method: 不涉及。论文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法，也未涉及实时推理、边缘部署或星上部署。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关指标，且其应用场景（可解释性）与在轨部署无关。

Datasets and metrics: 数据集：使用了7个来自不同领域、规模各异的数据集（摘要未列出具体数据集名称）。评价指标：有效性（validity）、稀疏性（sparsity）、邻近性（proximity）、收敛速度（convergence speed）、奖励优化（reward optimization）、episode长度（episode length reduction），以及SHAP和LIME可解释性分析。

Experiments: 对比实验：class-aware RL vs. class-blind RL。主要结果：(1) 训练阶段class-aware RL在收敛速度、奖励优化和episode长度缩减方面表现更优；(2) class-aware RL生成的CFE有效性显著更高；(3) 通过SHAP和LIME分析，实例的类别特征在RL动作选择中始终排名最具影响力的预测因子之一。

Limitations: 摘要未说明局限性。但从摘要可推断：(1) 仅对比了class-aware与class-blind两种RL变体，缺乏与其他CFE生成方法（如基于梯度、基于搜索的方法）的对比；(2) 未报告计算开销和部署可行性；(3) 未涉及遥感或视觉任务，泛化到高维图像/遥感数据的CFE生成能力未知；(4) 未讨论类别信息引入可能带来的偏差或过拟合风险。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向基本无关，但存在以下微弱的可借鉴点：(1) 强化学习在状态表示中引入类别信息以提升策略优化效率的思路，若未来在遥感目标检测的在线自适应或动态推理策略（如根据场景类别动态调整推理深度/分支）中应用，可能有一定启发；(2) SHAP/LIME用于分析模型决策依据的方法，可用于遥感检测模型的可解释性验证，帮助理解轻量化后模型是否保留了关键判别特征。但整体借鉴价值有限。

Abstract: Counterfactual explanations (CFEs) enhance the interpretability of black-box models by generating alternative instances with adjusted feature values that achieve a contrastive outcome. Reinforcement learning (RL) offers a promising approach for CFE generation, enabling efficient exploration of counterfactual instances while ensuring control over key metrics like validity, sparsity, and proximity. Previous studies have formulated RL states exclusively using features derived from the predictors in the supervised dataset. This study explores the impact of including an instance's predicted class, alongside features derived from the predictors, in the RL state representation for generating CFEs. The hypothesis is that class-awareness enhances exploration efficiency and improves policy optimality. We compare the proposed class-aware RL method with the class-blind RL method, which is similar but excludes the instance's class information from the state representation. The comparison was conducted using seven datasets from diverse domains, varying in size. The results show that during training, class-aware RL offers benefits in terms of convergence speed, reward optimization, and episode length reduction. Moreover, it generates significantly more valid CFEs compared to class-blind RL. Finally, the instance's class-based feature consistently ranks among the most influential predictors in RL's action-selection, as shown by the SHAP and LIME values, underscoring the significance of class-awareness in RL for CFE generation. The impact is heightened clarity, faster learning, improved validity, and more effective counterfactual generation across diverse datasets.

</details>


### [350] [S-CEReBrO: Breaking the Memory Barrier in Continuous EEG Monitoring](https://arxiv.org/abs/2607.27913)
*Glenn Anta Bucagu,Thorir Mar Ingolfsson,Yawei Li,Luca Benini*

Main category: cs.LG

TL;DR: 提出S-CEReBrO，一种面向连续脑电信号(EEG)监测的流式Transformer架构，通过Windowed Alternating Attention机制将注意力计算分解为固定大小的时空窗口，保证KV cache内存恒定，支持比全自注意力长100倍、比低秩线性注意力长3倍的信号处理。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/泛序列建模方向，核心场景是脑电信号(EEG)连续监测，与遥感、卫星、Earth observation、aerial image、SAR、高光谱等遥感场景完全无关。但其中"窗口化交替注意力"（Windowed Alternating Attention）解决长序列下注意力内存瓶颈的思路，以及模型压缩（60%参数减少）和推理吞吐提升的方法，对遥感目标检测模型在长序列/大尺寸遥感图像上的轻量化处理有一定方法学借鉴意义。

Reading priority: 2分。该论文与遥感卫星在轨目标检测方向弱相关，核心场景是EEG信号处理。但其Windowed Alternating Attention在长序列下恒定内存的设计思路，对遥感大图检测中注意力机制的内存优化有一定方法学参考价值。若课题聚焦于遥感图像中Transformer检测头的内存/计算优化，可选择性阅读；若课题聚焦于剪枝/量化/蒸馏等经典轻量化手段，则优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：连续脑电信号(EEG)监测与下游分类任务；应用场景：医疗健康/神经信号连续监测；传感器类型：EEG电极信号（非遥感传感器）；目标类别：11个下游任务（具体类别摘要未说明）。与遥感目标检测场景完全无关。

Model architecture: 基于CEReBrO架构的演进版本S-CEReBrO，核心创新是Windowed Alternating Attention机制：将注意力计算分解为固定大小的时空窗口（spatiotemporal windows），仅活动窗口需要驻留注意力图，从而保证KV cache内存恒定。属于Transformer-based架构。

Lightweight method: 核心轻量化手段是Windowed Alternating Attention——一种注意力计算分解策略，将全局注意力替换为固定窗口内的局部注意力，使KV cache内存与信号时长解耦（恒定内存）。相比低秩线性注意力，内存占用降低至55%，推理吞吐提升2.1倍，参数量减少最多60%。这属于架构层面的高效注意力设计，而非传统剪枝/量化/蒸馏/NAS方法。

Onboard deployability: 摘要未说明星上/边缘部署相关信息。但提供了关键效率指标：可处理比全自注意力长100倍的信号、比低秩线性注意力长3倍的信号；相比低秩线性注意力，内存占用为55%，推理吞吐提升2.1倍；参数量最多减少60%。这些指标反映的是长序列处理能力与内存效率，对遥感大图/长序列推理的部署有一定参考价值，但未涉及FLOPs、延迟、显存、能耗、FPGA/NPU/TensorRT等具体部署信息。

Datasets and metrics: 预训练数据：超过25,000小时、来自12,000+受试者的EEG记录。评估：11个下游任务，在7个任务上达到SOTA。具体数据集名称和评价指标（如准确率、F1等）摘要未说明。

Experiments: 主要实验包括：1) 缩放分析（scaling analysis）验证windowed alternating attention可处理比全自注意力长100倍、比低秩线性注意力长3倍的信号；2) 与低秩线性注意力对比，内存占用为55%，推理吞吐提升2.1倍；3) 在11个下游任务中7个达到SOTA，参数量最多减少60%。具体对比方法、消融实验细节摘要未说明。

Limitations: 1) 场景为EEG信号而非遥感图像，领域差异大；2) 摘要未说明窗口大小选择策略、窗口间信息交互方式（是否会导致长距离依赖丢失）；3) 未提供FLOPs、延迟、显存、能耗等部署关键指标；4) 未说明在图像/视觉任务上的泛化能力；5) 参数量减少60%的具体机制（是注意力分解带来的还是其他压缩手段）摘要未说明。

Ideas for my research: 1) Windowed Alternating Attention的思想可迁移到遥感大尺寸图像的目标检测中——遥感图像通常分辨率极高（如万像素级），全局自注意力在检测头或特征融合阶段存在内存瓶颈，采用固定窗口的交替注意力可降低KV cache内存并支持更大输入；2) 该方法的"恒定内存"设计理念对星上部署有启发——在轨推理时内存资源极其有限，恒定内存的注意力机制可保证推理稳定性；3) 其"参数量减少60%同时性能SOTA"的压缩思路可借鉴到遥感检测模型轻量化中，但需结合遥感场景重新设计；4) 可关注其代码仓库，研究窗口注意力在长序列上的工程实现细节。

Abstract: Foundation models offer a promising paradigm for Electroencephalography (EEG) analysis, leveraging generalizable representations from vast unlabeled datasets. Yet, Transformer-based architectures face a critical bottleneck: global attention mechanisms couple the attention memory state to the signal duration, causing memory overflow during continuous monitoring. To address this, we introduce S-CEReBrO (Streaming CEReBrO), an evolution of the CEReBrO architecture designed for continuous monitoring. Our novel Windowed Alternating Attention mechanism factorizes attention computation into fixed-size spatiotemporal windows, guaranteeing constant KV cache memory as only the active window requires resident attention maps. Empirical scaling analysis confirms that windowed alternating attention can process signals 100X longer than full self-attention and 3X longer than low-rank linear attention. Compared to low-rank linear attention on long contexts, windowed alternating attention requires 55% of the memory while increasing inference throughput by 2.1X. Pre-trained on >25,000 hours of recordings from >12,000 subjects, S-CEReBrO achieves state-of-the-art performance on 7 of 11 downstream tasks, with up to 60% fewer parameters. This work represents a significant step toward the realization of efficient, generalizable, and continuous EEG monitoring. An accompanying code repository is available.

</details>


### [351] [Exact Action Values Are Not Enough: Rollout-Verified Reinforcement Fine-Tuning of a Reasoning Model for Multi-Zone VAV Control](https://arxiv.org/abs/2607.27914)
*Takumi Shioda,Kohei Terashima,Tatsuo Nagai*

Main category: cs.LG

TL;DR: 本文研究多区域变风量（VAV）暖通空调控制，探讨前沿推理LLM（GPT-5）与TD3引导的强化微调（RFT）在无建筑特定训练下实现VAV控制，并审计了critic在rollout验证下的可靠性问题。

Research relevance: 与本研究方向基本无关（1分）。论文主题为暖通空调（HVAC）多区域变风量控制，属于建筑能源管理领域，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测或模型轻量化/部署。虽然涉及强化学习微调（RFT）和模型部署（open-weight model），但这是通用控制领域的知识迁移与部署问题，与遥感在轨目标检测轻量化无直接关联。

Reading priority: 1分。论文主题为HVAC控制与LLM强化微调，与遥感卫星在轨目标检测及轻量化方向基本无关，仅critic审计方法论有微弱参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多区域变风量（VAV）暖通空调控制（HVAC control）。应用场景：建筑能源管理，四区物理仿真模拟器。传感器类型：温度、CO₂浓度传感器（非遥感传感器）。目标类别：不涉及目标检测，控制对象为连续执行器（风阀、送风等）。

Model architecture: 涉及三类模型：(1) 前沿推理大语言模型（GPT-5，LLM with inference-time computation）；(2) TD3（Twin Delayed DDPG）强化学习控制器；(3) 本地可部署的open-weight模型（通过RFT微调）。核心机制包括TD3-guided reinforcement fine-tuning (RFT)、deterministic rollouts、learned critic审计。

Lightweight method: 摘要未明确涉及传统轻量化方法（pruning、quantization、knowledge distillation、NAS等）。但涉及"knowledge transfer"概念——通过RFT将TD3控制知识迁移到本地可部署的open-weight模型，这属于知识迁移/蒸馏的广义范畴，但并非面向遥感模型的轻量化。摘要未说明参数量、FLOPs、延迟等部署指标。

Onboard deployability: 摘要提及"locally deployable open-weight model"（本地可部署的开权重模型），说明关注部署可行性，但未给出参数量、FLOPs、延迟、显存、能耗等具体部署指标。该部署面向建筑控制器而非星上/边缘设备，与在轨部署场景无关。

Datasets and metrics: 数据集：基于物理的四区仿真模拟器（physics-based four-zone emulator），三个夏季日数据。评价指标：HVAC电力消耗（electricity）、温度合规性（temperature compliance）、CO₂合规性、通风裕度（ventilation margin）、采样动作回报（sampled-action return）、五分钟预测误差（five-minute predictions vs persistence）、critic的跨时间相关性（r=0.9998）。基线：Guideline 36。

Experiments: 对比了5个控制器。主要结果：(1) TD3相对Guideline 36基线降低HVAC电力4.5%，同时改善温度和CO₂合规；(2) GPT-5无建筑特定训练即实现最大降耗6.2%，但降低了通风裕度；(3) RFT 200步未产生持续改进，open-weight控制器训练前后用电均高于基线；(4) critic审计发现其跨时间相关性接近完美（r=0.9998）但状态内排序不可靠，仅在10个状态中的5个选出rollout最优候选；(5) GPT-5的转移预测远优于open-weight模型。

Limitations: 摘要未说明：模型参数量、计算资源、部署硬件平台。核心局限：RFT未能有效提升open-weight模型性能，critic存在状态内排序失效问题（尽管跨时间相关性极高），转移误差未改善，提示需先进行transition-focused监督微调。该研究面向建筑HVAC控制，与遥感在轨目标检测场景无直接可迁移性。

Ideas for my research: 尽管场景无关，但有两处方法论可借鉴：(1) critic审计方法——在知识蒸馏/轻量化中，评估teacher或student模型时不能仅看全局相关性（如r=0.9998），需检查状态内/局部排序可靠性，这对遥感检测模型蒸馏后的质量评估有警示意义；(2) RFT失败的分析框架——提示在遥感模型轻量化中，若采用蒸馏或微调，需先确保teacher的转移/特征预测能力足够强，否则student微调难以收敛。但整体借鉴价值有限。

Abstract: Multi-zone variable-air-volume control must balance thermal comfort, indoor air quality, and electricity use across several continuous actuators. Model predictive control and reinforcement learning are widely studied, but deployment typically requires building-specific modeling or training, limiting scalability. We first test whether a frontier reasoning model (an LLM trained to use additional inference-time computation) can achieve competitive VAV control from text without building-specific training. With that capability established, we then test whether TD3-guided reinforcement fine-tuning (RFT) can transfer control knowledge into a locally deployable open-weight model. Five controllers are evaluated over three summer days in a physics-based four-zone emulator. Relative to a Guideline 36-based baseline, TD3 reduced HVAC electricity by 4.5% while improving temperature and CO$_2$ compliance. Without building-specific training, GPT-5 achieved the largest reduction (6.2%) but reduced the ventilation margin. For RFT, deterministic rollouts restore a saved state, apply one candidate, and follow TD3 to score each action. Auditing a learned critic against these rollouts exposed a failure hidden by its near-perfect across-time correlation ($r=0.9998$): within-state ranking was unreliable; the critic selected the rollout-best candidate in only 5 of 10 states. Even with the rollout verifier, 200 RFT steps produced no sustained improvement in sampled-action return; the open-weight controller used more electricity than the baseline before and after training, and its five-minute predictions remained worse than persistence. GPT-5 predicted transitions far better. Exact rollout scores rank sampled actions but reveal neither next-state effects nor an improvement direction. The unchanged transition errors motivate transition-focused supervised fine-tuning before value-based RFT.

</details>


### [352] [Harnessing the Potential of Optimizing Data Mixtures via Bayesian Domain Reweighting](https://arxiv.org/abs/2607.27928)
*Xiang Yuan,Kaiqing Lei,Zhenyu Jin,Jun Shu,Deyu Meng,Zongben Xu*

Main category: cs.LG

TL;DR: 提出一种基于贝叶斯推断的域权重学习方法（Bayesian domain weighting），通过引入Gamma先验从Dirichlet分布推断多域预训练数据的最优混合权重，相比搜索式函数拟合方法更稳定高效且消耗更少数据。

Research relevance: 弱相关（2分）。该论文聚焦于大语言模型（LLM）多域预训练数据的权重分配优化问题，属于自然语言处理/机器学习领域，与遥感、卫星、Earth observation、目标检测、模型轻量化均无直接关联。既非遥感场景，也不涉及目标检测或模型压缩/加速方法，仅其"从数据直接优化权重"的贝叶斯优化思想在方法论层面可能对超参搜索类问题有间接借鉴意义。

Reading priority: 1分。该论文与"遥感卫星在轨目标检测与模型轻量化"研究方向基本无关，属于LLM预训练数据配比优化问题，既非遥感场景、非目标检测任务，也不涉及任何模型压缩或部署技术。仅方法论层面的贝叶斯优化思想有极有限的间接参考价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多域预训练数据的域权重优化（domain weighting for LLM pre-training）。应用场景：大语言模型预训练数据配比。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体网络结构。核心方法为贝叶斯域权重推断：假设域权重服从Dirichlet分布，通过引入Gamma先验（从观测数据学习）来推断最优域权重配置，替代传统的搜索式函数拟合（如rank invariance或scaling laws假设下的代理函数拟合）。

Lightweight method: 不涉及模型轻量化。该论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、实时推理或边缘部署等任何模型压缩/加速技术。其"高效"体现在域权重搜索的数据效率上，而非模型推理效率。

Onboard deployability: 不适用。论文不涉及星上/边缘部署，未报告参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关指标。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅提及实验表明该方法能实现稳定高效的域权重学习，在消耗显著更少数据的情况下识别最优混合配置，相比搜索式函数拟合方法更优。

Experiments: 摘要仅给出定性结论：所提贝叶斯方法在域权重学习的稳定性和效率上优于搜索式函数拟合方法，且数据消耗更少。未提供具体数值结果、对比基线细节或消融实验。

Limitations: 摘要未说明局限性。但可推断：1) 该方法针对LLM预训练数据配比问题设计，其假设（Dirichlet分布+Gamma先验）是否适用于遥感目标检测模型的训练数据配比或超参优化尚待验证；2) 摘要未报告计算开销的具体数值，无法判断其实际效率；3) 未涉及任何视觉任务或遥感场景的验证。

Ideas for my research: 尽管与遥感在轨目标检测轻量化方向弱相关，但可借鉴其方法论思想：1) 在遥感目标检测模型的训练中，多源数据（光学、SAR、多光谱）的配比优化可借鉴贝叶斯域权重方法，替代人工经验配比；2) 在轻量化搜索（如NAS或剪枝率分配）中，若涉及多目标/多约束的权重搜索，可借鉴其"从数据直接优化+先验约束"的思路来稳定优化轨迹；3) 其"消耗更少数据即可找到最优配置"的特性，对遥感标注数据稀缺场景下的超参/结构搜索有一定启发。但整体借鉴价值有限，不建议作为核心参考。

Abstract: The performance of Large Language Models (LLMs) is fundamentally influenced by the distributional composition of multi-domain pre-training data. While manual heuristics were prevalent in early models, they increasingly fail to capture the intricate synergies between domains as data complexity grows. To overcome the issue, a dominant approach seeks to fit a proxy function mapping between domain weights and their corresponding validation losses, and then find the optimal domain weights to minimize validation losses. These methods rely on strong structural assumptions, such as rank invariance or scaling laws, which are often violated, resulting in non-negligible estimation bias. A promising approach is to directly optimize the weighting scheme from data. However, it suffers from unstable optimization trajectory and prohibitive computational overhead, limiting its potential to search better domain weights configurations. This paper presents a Bayesian domain weighting method to infer the weights from a Dirichlet distribution via introducing Gamma prior information learned from observations. Experimental results demonstrate that proposed method could achieve stable and efficient domain weights learning, and identifies optimal mixtures while consuming substantially less data than search-based function-fitting methods, revitalizing optimization-based domain weighting for large-scale applications.

</details>


### [353] [TriShield: Zero-Utility-Loss Defense Against Privacy Backdoors in Federated Language Model Fine-Tuning via Orthogonal Gradient Projection and Optimizer State Entanglement](https://arxiv.org/abs/2607.27940)
*Cheng Wei*

Main category: cs.LG

TL;DR: 提出TriShield，一种三层确定性防御机制，用于抵御联邦微调大语言模型中的NeuroImprint隐私后门攻击，在零模型效用损失和零额外通信轮次下将数据重建率降至0%。

Research relevance: 弱相关（2分）。该论文属于联邦学习（Federated Learning）与大语言模型（LLM）隐私安全领域，核心问题是防御恶意参数服务器对PEFT适配器的隐私后门攻击。论文不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等任何遥感场景，也不涉及object detection、小目标检测、旋转框检测等目标检测任务，更不涉及pruning、quantization、knowledge distillation、NAS等模型轻量化方法。其与本研究方向的关联仅在于"联邦学习"和"模型参数/梯度处理"在概念层面与分布式训练和模型压缩有一定交叉，但缺乏直接可迁移性。

Reading priority: 1分。该论文属于LLM联邦学习隐私安全领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关。既无遥感场景，也无目标检测任务，更无模型压缩/加速方法。除非未来研究涉及联邦学习框架下的遥感模型分布式训练与隐私保护，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：联邦微调（Federated fine-tuning）中的隐私安全防御。应用场景：大语言模型（LLM）的协作训练，防止恶意参数服务器通过PEFT适配器重建客户端训练数据。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 防御框架TriShield包含三层：(1) Parameter Artifact Detector——在本地训练开始前识别分布式模型参数中的记忆神经元签名；(2) Stateful Virtual Iteration——强制Adam/AdamW的momentum状态在虚拟步之间不可逆地纠缠梯度，使NeuroImprint的闭式反演失效；(3) Zero-Utility Orthogonal Projection——通过SVD将本地梯度更新投影到主任务语义子空间，物理消除携带私有记忆的梯度分量。实验模型为GPT-2 (117M)和Llama-Guard-3-1B。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS、轻量网络设计等模型压缩或加速方法。其核心是隐私防御机制，而非模型轻量化。虽然提到"less than 5% additional GPU computation overhead"，但这指的是防御机制引入的计算开销，而非模型本身的轻量化。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。唯一相关的性能指标是防御机制引入的额外GPU计算开销小于5%，但这与在轨部署无关。

Datasets and metrics: 实验在GPT-2 (117M)和Llama-Guard-3-1B上进行。评价指标包括：NeuroImprint数据重建率（reconstruction rate，降至0%）、训练准确率（maintaining or improving）、额外GPU计算开销（<5%）。摘要未说明具体数据集名称。

Experiments: 实验验证TriShield在所有测试的NeuroImprint攻击变体下将数据重建率降至0%，同时保持或提升训练准确率，额外GPU计算开销小于5%。论文还从理论上证明了经过第2、3层后，上传梯度与任何单个训练样本之间的互信息（mutual information）为零。摘要未说明与现有防御方法（如LDP、gradient clipping）的详细对比结果。

Limitations: 1. 论文聚焦于LLM联邦微调的隐私安全，与遥感卫星在轨目标检测及轻量化方向基本无关。2. 防御机制针对的是NeuroImprint这一特定攻击，泛化性到其他攻击类型未在摘要中说明。3. 实验规模较小（117M和1B参数模型），未涉及更大规模模型或真实遥感场景。4. 摘要未说明防御机制对模型收敛速度、通信效率等联邦学习关键指标的影响细节。

Ideas for my research: 尽管论文与遥感在轨目标检测轻量化方向弱相关，但仍有少量概念性借鉴点：1. "Zero-Utility Orthogonal Projection"通过SVD将梯度投影到主任务语义子空间的思想，在模型压缩中可类比为"在低秩子空间中保留主要任务信息"的思路，与低秩分解（low-rank factorization）压缩方法有概念上的相通之处。2. "Stateful Virtual Iteration"中对优化器状态（momentum）的操控，提示在轻量化训练中优化器状态的管理也可能影响模型收敛与压缩效果。3. 联邦学习场景下"参数服务器-客户端"的分布式训练架构，与未来多星协同在轨推理/训练场景有一定概念关联。但整体而言，该论文对本课题的直接借鉴价值有限。

Abstract: Federated fine-tuning of large language models (LLMs) enables collaborative training without exposing raw data. However, a recent attack, NeuroImprint [1] (arXiv:2606.20553), demonstrates that a malicious parameter server can corrupt a PEFT adapter into a privacy backdoor: by assigning a dedicated memorization neuron to each training sample and ensuring each neuron updates at most once, the server can analytically reconstruct 59\%--79\% of client training data with high semantic fidelity. Existing defenses---including local differential privacy (LDP) [8] and gradient clipping---either fail against this attack or impose unacceptable utility degradation. We present \textbf{TriShield}, a three-layer deterministic defense that completely prevents NeuroImprint-style reconstruction with \textbf{zero model utility loss} and \textbf{no additional communication rounds}. TriShield consists of: (1) a \textbf{Parameter Artifact Detector} that identifies memory-neuron signatures in distributed model parameters before local training begins; (2) a \textbf{Stateful Virtual Iteration} mechanism that forces Adam/AdamW's momentum state to irreversibly entangle gradients across virtual steps, invalidating NeuroImprint's closed-form inversion; and (3) a \textbf{Zero-Utility Orthogonal Projection} operator that projects all local gradient updates onto the main-task semantic subspace computed via SVD, physically eliminating any gradient components that carry private memorization. We prove theoretically that after Layers 2 and 3, the mutual information between the uploaded gradient and any individual training sample is zero. Experiments on GPT-2 (117M) and Llama-Guard-3-1B verify that TriShield reduces NeuroImprint reconstruction rate to \textbf{0\%} across all tested attack variants, while maintaining or improving training accuracy, with less than 5\% additional GPU computation overhead.

</details>


### [354] [AutoPref: Automatic Discovery of Task-Specific Preference Objectives for Neural Combinatorial Optimization](https://arxiv.org/abs/2607.27953)
*Shengda Gu,Kai Li,Xinyi Ke,Haobo Fu,Yifan Zhang,Jian Cheng*

Main category: cs.LG

TL;DR: 提出AutoPref，首个利用LLM引导的自动化偏好目标发现框架，用于神经组合优化（NCO），通过将偏好目标分解为成对损失程序和集合感知加权程序，在TSP、CVRP、FFSP和JSSP等组合优化问题上超越手工设计基线。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于神经组合优化（Neural Combinatorial Optimization, NCO），涉及TSP、CVRP、FFSP、JSSP等经典运筹优化问题，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测任务，更不涉及模型轻量化或边缘/星上部署。属于泛机器学习/强化学习方法，与本方向弱相关（2分）。

Reading priority: 1分。论文聚焦神经组合优化中的偏好目标自动发现，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及遥感数据、目标检测任务或任何模型压缩/加速方法。仅在"自动化目标/损失函数设计"和"分阶段搜索策略"层面有极间接的方法论借鉴价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：神经组合优化（NCO）中的偏好目标自动发现；应用场景：组合优化问题求解（TSP、CVRP、FFSP、JSSP）；传感器类型：不适用；目标类别：不适用（组合优化问题而非视觉目标）。

Model architecture: AutoPref框架将偏好目标分解为两个程序化组件：(1) 成对损失程序（pairwise loss program），定义学习信号；(2) 集合感知加权程序（set-aware weighting program），决定每个解对的相对贡献。两者组合形成统一的程序化目标空间（programmatic objective space），包含现有偏好目标作为特例。采用分阶段条件搜索策略（staged conditional search strategy）配合行为门控（behavioral gates）过滤不可行程序。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS或轻量网络设计等模型压缩/加速方法。其核心贡献是自动化目标函数设计，而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗或实时推理等部署相关信息，也未涉及星上或边缘部署场景。

Datasets and metrics: 实验在四个组合优化问题基准上进行：TSP（旅行商问题）、CVRP（带容量约束车辆路径问题）、FFSP（柔性流水车间调度问题）、JSSP（作业车间调度问题）。评价指标为解质量（solution quality），具体指标名称摘要未详细说明。未使用任何遥感或视觉数据集。

Experiments: 在TSP、CVRP、FFSP和JSSP四个问题上，AutoPref在不同问题规模下均一致优于强手工设计基线（strong hand-designed baselines），验证了自动化目标发现方法的有效性和可扩展性。摘要未提供具体数值结果或消融实验细节。

Limitations: 摘要未说明局限性。但可推断：(1) 方法依赖LLM引导的程序搜索，计算开销可能较大；(2) 仅验证于组合优化问题，未涉及视觉/遥感任务；(3) 未报告部署相关指标；(4) 行为门控和分阶段搜索策略的工程复杂度较高。

Ideas for my research: 尽管论文与遥感目标检测轻量化方向弱相关，但存在以下可借鉴的间接思路：(1) "自动化目标发现"的思想可类比于自动化损失函数设计——在遥感小目标检测中，可考虑用类似LLM引导的方式自动搜索更适合小目标/旋转框检测的损失函数（如改进的IoU损失或focal loss变体）；(2) "分阶段条件搜索+行为门控"的搜索策略思想，可迁移到轻量化NAS中，用于在剪枝/量化搜索空间中过滤不可行的候选架构，降低搜索成本；(3) 程序化目标空间（programmatic objective space）的分解思路，可用于将轻量化目标（如FLOPs、延迟）与检测精度目标解耦并联合优化。但整体借鉴价值有限，不建议作为核心参考。

Abstract: Combinatorial optimization problems (COPs) underpin many real-world decisions, but their exponentially large search spaces make high-quality solutions costly to obtain. Neural combinatorial optimization (NCO) learns fast construction policies, typically with reinforcement learning (RL), while preference-based NCO improves sample efficiency by learning from relative solution quality. However, existing preference objectives combine two distinct design choices in manually specified, one-size-fits-all formulations: what learning signal to extract from each solution pair and how to weight each pair relative to the sampled set. We present AutoPref, the first LLM-guided framework for automated preference-objective discovery in NCO. AutoPref factorizes the objective into a pairwise loss program, which defines the learning signal, and a set-aware weighting program, which determines each pair's relative contribution. Their composition forms a unified programmatic objective space containing existing preference objectives as special cases. To make its search tractable, we introduce a staged conditional search strategy with behavioral gates that filter inadmissible programs before short-horizon training and evaluation. Across TSP, CVRP, FFSP, and JSSP, AutoPref consistently outperforms strong hand-designed baselines across problem scales, demonstrating the benefits and scalability of automated objective discovery for NCO.

</details>


### [355] [What Makes Graph Unified? Principles and Generative Sliding-Window Transformer for Graph Foundation Models](https://arxiv.org/abs/2607.27966)
*Dongxiao He,Siqi Liu,Jitao Zhao,Yawen Li,Yi Wang,Di Jin*

Main category: cs.LG

TL;DR: 提出SliGFM，一种基于拓扑感知滑动窗口特征编码和生成式重建的图基础模型，用于跨域图特征统一，解决异构节点特征维度与语义不一致的问题。

Research relevance: 1分，基本无关。该论文属于图基础模型（Graph Foundation Models）领域，聚焦跨域图学习的特征统一问题，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或部署优化。

Reading priority: 1分。该论文属于图基础模型方向，与遥感卫星在轨目标检测及轻量化课题基本无关，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：跨域图学习（cross-domain graph learning）中的特征统一（feature unification）。应用场景：通用图学习，非遥感/卫星场景。传感器类型：不适用。目标类别：不适用。

Model architecture: SliGFM包含：1）拓扑感知滑动窗口特征编码（topology-aware sliding-window feature encoding），按拓扑平滑度对特征维度排序并用共享滑动窗口编码器扫描；2）平滑感知Transformer（smoothness-aware transformer）捕获节点内特征token间的可迁移关系模式；3）生成式重建目标（generative reconstruction objective）保留原始特征信息。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等轻量化方法，也未讨论模型压缩或加速。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息，也未讨论星上或边缘部署可行性。

Datasets and metrics: 摘要未说明。未提及具体数据集名称和评价指标。

Experiments: 摘要未说明。未提供实验对比结果或消融实验细节。

Limitations: 1）论文聚焦图学习领域，与遥感目标检测场景完全无关；2）摘要未提供任何实验验证、数据集或性能指标；3）未讨论计算开销和部署可行性，无法评估其在实际工程中的可用性；4）滑动窗口编码和生成式重建的计算复杂度在摘要中未说明。

Ideas for my research: 尽管整体弱相关，但有两个概念层面的借鉴点：1）"拓扑感知的特征维度排序+滑动窗口编码"思想，若类比到遥感目标检测中，可启发对多光谱/高光谱波段特征进行有序编码的思路，但需要大幅改造；2）"生成式重建目标保留原始信息"的思想，与知识蒸馏中保留教师模型信息的目标有概念相似性，但方法论上差异较大。总体而言，对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值很低。

Abstract: Graph Foundation Models (GFMs) have recently emerged as a promising paradigm for general-purpose graph learning, aiming to learn reusable knowledge that generalizes across diverse graph domains and downstream tasks, reducing the need for specific model development. Achieving this goal requires reconciling the substantial heterogeneity in node features, graph structures, and semantic information across domains. Among them, heterogeneous node features constitute a fundamental input-level barrier, as their dimensionality and semantics vary substantially across datasets. Existing studies typically project or map heterogeneous node features into a fixed-dimensional space, often implicitly equating dimensional uniformity with effective feature unification. Yet dimensional consistency alone does not ensure that the unified features preserve informative semantics and capture transferable patterns that can support cross-domain knowledge transfer. To bridge this conceptual gap, we distill four desiderata for cross-domain graph feature unification: formal uniformity, cross-domain transferability, information preservation, and backbone compatibility. Guided by these principles, we propose SliGFM, a graph foundation model built upon topology-aware sliding-window feature encoding and generative reconstruction. SliGFM orders feature dimensions by topological smoothness and scans the reordered features with a shared sliding-window feature encoder, transforming heterogeneous features into a common space of ordered fixed-dimensional feature tokens. This formulation enables a smoothness-aware transformer to capture transferable relational patterns among feature tokens within each node, while the generative reconstruction objective encourages preservation of the original feature information.

</details>


### [356] [Beyond Binary Rewards: A Comparative Study of Reward Design for Reinforcement Unlearning](https://arxiv.org/abs/2607.27968)
*Efstratios Zaradoukas,Davide Gabrielli,Bardh Prenkaj,Gjergji Kasneci*

Main category: cs.LG

TL;DR: 本文研究机器遗忘（machine unlearning）中的奖励函数设计，提出指数奖励和PageRank启发奖励两种新的奖励函数，在RWKU基准上相比二元奖励实现最高3倍加速的遗忘收敛，同时保持模型通用能力。

Research relevance: 弱相关（2分）。本文属于泛机器学习/大语言模型领域，研究的是machine unlearning（机器遗忘）问题，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然"unlearning"在概念上涉及模型参数的高效更新，但本文聚焦于LLM的隐私遗忘，而非遥感目标检测模型的压缩或加速。其奖励函数设计思想属于泛RL方法，对遥感轻量化课题的借鉴价值有限。

Reading priority: 1分。本文研究LLM机器遗忘，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。除非后续研究涉及RL-based检测器优化或NAS搜索中的奖励设计，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：machine unlearning（机器遗忘），即在已训练的语言模型中选择性移除特定知识。应用场景：隐私合规（GDPR、EU AI Act）。传感器类型：不适用（文本/语言模型）。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。基于Reinforcement Learning with Verifiable Rewards (RLVR)框架，将unlearning重构为强化学习问题，在Reinforcement Unlearning (RUL)框架下研究奖励函数设计。提出两种新奖励函数：指数奖励（exponential reward，基于禁止概念出现次数的分级惩罚）和PageRank启发奖励（按语义重要性加权惩罚）。

Lightweight method: 摘要未涉及剪枝、量化、知识蒸馏、NAS等传统模型压缩方法。本文的"高效性"体现在遗忘收敛速度提升（最高3倍加速），而非模型参数或计算量的轻量化。与遥感在轨部署所需的模型压缩/加速方法无直接关系。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。本文面向的是LLM隐私遗忘场景，与星上/边缘部署无关。

Datasets and metrics: 数据集：Real World Knowledge Unlearning (RWKU) benchmark。评价指标：遗忘性能（forgetting performance）、收敛速度（convergence speed）、通用模型效用（general model utility）。

Experiments: 在RWKU基准上对比了二元奖励（binary reward）与两种新奖励函数（指数奖励、PageRank启发奖励）。结果表明两种新奖励均一致优于二元奖励设置，在达到相似遗忘性能的情况下收敛速度最高提升3倍，同时保持通用模型效用。

Limitations: 摘要未说明局限性。但可推断：1）实验仅在RWKU单一基准上验证，泛化性待考；2）奖励函数设计依赖"禁止概念计数"和"语义重要性"的计算，其计算开销和可扩展性未讨论；3）与遥感目标检测、模型轻量化、在轨部署完全无关。

Ideas for my research: 借鉴价值有限，但有两个间接启发：1）奖励函数设计思想——如果未来在遥感目标检测中引入RL-based的检测器优化或NAS搜索，可借鉴"从稀疏二元奖励到分级连续奖励"的设计思路来加速收敛；2）"语义重要性加权"的概念可类比于遥感目标检测中不同目标类别/尺度的加权损失设计。但整体而言，本文与"在轨遥感目标检测模型轻量化"课题的直接可借鉴点很少。

Abstract: Machine unlearning seeks to selectively remove specific knowledge from trained language models without full retraining, a growing necessity under privacy regulations such as GDPR and the EU AI Act. Recent work has reformulated unlearning as a Reinforcement Learning with Verifiable Rewards (RLVR) problem, where models are optimized against verifiable rewards computed directly from their outputs. However, existing methods rely on sparse binary rewards that provide minimal learning signal, indicating only whether forbidden content was avoided, and limiting convergence speed. In this paper, we study how reward design affects unlearning efficiency within the Reinforcement Unlearning (RUL) framework. We introduce a principled reward decomposition framework that decouples verifiability from sparsity, and propose two new reward functions: an exponential reward that provides graded penalties based on the count of forbidden-concept occurrences, and a PageRank inspired reward that weights penalties by semantic importance. We conduct experiments on the Real World Knowledge Unlearning (RWKU) benchmark, demonstrating that both rewards consistently outperform the binary setting, while reaching similar forgetting performance up to $3\times$ faster and preserving general model utility. Our results show that reward design is a key driver of unlearning efficiency offering a practical path toward scalable and efficient machine unlearning.

</details>


### [357] [TAPO: Transition-Aware Policy Optimization for LLM Agents](https://arxiv.org/abs/2607.27973)
*Cong Li,Peixi Peng,Yisen Zhao,Xinyu Hu,Shudong Liu,Zhan Su,Zhuojian Li*

Main category: cs.LG

TL;DR: 提出TAPO（Transition-Aware Policy Optimization），一种面向LLM智能体的过渡感知策略优化训练框架，通过动作条件下的下一观测预测监督来增强策略优化，在WebShop和ALFWorld上提升任务性能。

Research relevance: 弱相关（2分）。论文属于泛机器学习/强化学习领域，聚焦LLM智能体的策略优化，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可借鉴之处在于其"computationally lightweight, plug-and-play enhancement module"的轻量增强模块设计思路，以及"no additional inference-time overhead"的部署友好理念，但这些与遥感在轨目标检测的模型压缩/加速方法论完全不同。

Reading priority: 1分。论文主题为LLM智能体强化学习策略优化，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在设计理念上有极有限的间接借鉴意义，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体的强化学习后训练（post-training）与策略优化。应用场景：WebShop（网页购物任务）和ALFWorld（具身智能文本环境）。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。

Model architecture: TAPO框架在共享骨干模型（shared backbone model）上交替进行策略优化（policy optimization）和过渡监督（transition supervision）。具体而言，在标准RL更新之外，利用rollout数据施加动作条件下的下一观测预测监督（action-conditioned next-observation prediction supervision）。支持集成不同规模的基础模型（foundation models of various scales）和不同的策略优化算法。

Lightweight method: 论文自述为"computationally lightweight, plug-and-play enhancement module"，即计算轻量的即插即用增强模块。其轻量性体现在：不需要额外专家数据（no additional expert data）、不需要额外采样成本（no extra sampling costs）、无推理时开销（no inference-time overhead）。但这属于训练阶段的计算效率优化，并非模型压缩（pruning/quantization/distillation/NAS）意义上的轻量化方法。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署指标。仅提及"no inference-time overhead"表明该方法不增加推理开销，但未给出任何量化数据。

Datasets and metrics: 数据集：WebShop和ALFWorld。评价指标：摘要未明确列出具体指标名称，仅提及"task performance"。实验设置：集成不同规模的基础模型（foundation models of various scales）与不同策略优化算法（different policy optimization algorithms）进行系统实验。

Experiments: 在WebShop和ALFWorld上，TAPO与纯策略优化基线（pure policy optimization baselines）对比，一致性地提升了任务性能（consistently improves task performance）。摘要未提供具体数值结果、消融实验细节或对比方法列表。

Limitations: 1. 与遥感/卫星/目标检测方向完全无关，属于LLM智能体强化学习领域。2. 摘要未提供量化性能数据（如成功率提升幅度）。3. 仅在文本交互环境（WebShop、ALFWorld）上验证，未涉及视觉或遥感数据。4. 摘要未说明该方法在复杂多步任务上的泛化边界。

Ideas for my research: 1. 借鉴其"plug-and-play enhancement module"的设计哲学——即在不改变主模型结构、不增加推理开销的前提下，通过训练阶段的辅助监督信号提升模型能力。这一思路可迁移到遥感目标检测模型的训练策略设计中，例如在轻量化检测器训练时引入辅助的过渡/上下文预测任务来增强特征表达。2. 其"no inference-time overhead"的约束理念与在轨部署对推理效率的严苛要求一致，可作为设计轻量化方法时的设计准则。3. 但整体而言，该论文对遥感在轨目标检测轻量化的直接借鉴价值有限，不建议深入精读。

Abstract: Recently, Reinforcement Learning (RL) has emerged as a crucial paradigm for the post-training of Large Language Model (LLM) agents. However, existing methods predominantly rely on sparse task rewards for policy optimization, failing to fully exploit another class of inherently dense supervisory signals naturally present during online interaction: environmental feedback following action execution. Recent theoretical studies suggest that generalization in multi-step, goal-oriented tasks hinges on predictive knowledge of environmental consequences. Inspired by this, we propose TAPO: Transition-Aware Policy Optimization for LLM Agents, a unified training framework that alternates between policy optimization and transition supervision. Beyond standard RL updates, TAPO repurposes rollout data to apply action-conditioned next-observation prediction supervision on a shared backbone model. This approach enhances the model's sensitivity to environmental transition dynamics and action consequences while concurrently optimizing the policy. It serves as a computationally lightweight, plug-and-play enhancement module for existing agent RL algorithms, requiring no additional expert data, extra sampling costs, or inference-time overhead. We conduct systematic experiments on WebShop and ALFWorld, integrating foundation models of various scales with different policy optimization algorithms. Empirical results demonstrate that TAPO consistently improves task performance over pure policy optimization baselines.

</details>


### [358] [Generalization Bounds on Optimal Control for Transformer Training and Wasserstein Distributional Robustness](https://arxiv.org/abs/2607.27975)
*Kağan Akman,Naci Saldi,Serdar Yüksel*

Main category: cs.LG

TL;DR: 本文从理论层面推导了基于动态规划递归训练的Transformer的有限样本泛化界，将训练问题建模为有限时域马尔可夫控制问题，并利用量化模型和集中不等式推导泛化界，最后将其与Wasserstein分布鲁棒优化联系起来。

Research relevance: 弱相关（2分）。论文是纯理论性的机器学习泛化分析，研究对象是Transformer架构，与遥感、卫星、目标检测、轻量化部署均无直接关系。虽然涉及"quantized model"（量化模型），但这里的量化是指对状态、动作和测度状态空间的数学量化，用于理论推导泛化界，并非模型压缩中的weight quantization或activation quantization，与工程部署无关。

Reading priority: 1分。论文为Transformer泛化理论分析，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅在"量化误差转移"和"分布鲁棒"两个概念上有极弱的理论启发，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Transformer的泛化理论分析（generalization bounds）。应用场景：非遥感场景，属于泛机器学习理论。传感器类型：不适用。目标类别：不适用。

Model architecture: 研究对象为Transformer架构，采用"doubly lifted, measure-valued formulation"（双重提升、测度值公式化）来描述Transformer动力学，将数据集视为经验输入-输出测度对上的概率律。训练问题被解释为有限时域马尔可夫控制问题（finite-horizon Markovian control problem）。

Lightweight method: 摘要中提到的"quantized model"是对状态、动作和测度状态空间进行数学量化以推导泛化界，属于理论分析工具，并非模型压缩中的量化方法（如weight quantization、activation quantization、低比特部署等）。不涉及剪枝、知识蒸馏、NAS等轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及参数量、FLOPs、延迟、显存、能耗、实时性等任何部署相关信息，属于纯理论分析。

Datasets and metrics: 摘要未说明。论文为理论推导，未提及任何具体数据集或实验评价指标。

Experiments: 摘要未说明。论文主要贡献是理论推导：利用有限度量空间上经验律的集中不等式和值函数的Lipschitz稳定性估计，推导量化模型的有限样本泛化界，并将界转移到基础模型（代价是显式近似误差）。还建立了与Wasserstein分布鲁棒优化的联系。未提及实验验证。

Limitations: 1. 纯理论论文，无实验验证；2. 与遥感、目标检测、工程部署完全无关；3. 摘要中的"quantization"是数学空间量化而非模型压缩量化，容易造成误导；4. 泛化界推导基于特定假设（Lipschitz稳定性、有限度量空间），实际Transformer训练的适用性需进一步验证。

Ideas for my research: 1. 虽然本文与遥感目标检测轻量化无直接关系，但其"量化模型到基础模型的误差转移"思想在理论上与模型量化压缩有概念上的类比——量化引入的近似误差如何影响最终性能，这一理论框架或可启发对轻量化模型（如量化后的检测网络）泛化性能的理论分析；2. 分布鲁棒优化（DRO）思想可用于遥感数据分布偏移（如不同光照、季节、传感器差异）下的鲁棒目标检测模型设计；3. 但整体借鉴价值有限，不建议作为核心参考。

Abstract: We derive finite-sample generalization bounds for Transformers trained with dynamic programming recursions. Building on the doubly lifted, measure-valued formulation of Transformer dynamics, we view data sets as probability laws on pairs of empirical input-output measures, allowing us to interpret the training problem as a finite-horizon Markovian control problem. We then analyze a quantized model, derived by quantizing the state, action, and measure-state spaces, and derive explicit finite-sample generalization bounds using concentration inequalities for empirical laws on finite metric spaces together with a Lipschitz stability estimate for the value function. These bounds are transferred to the base model at the cost of an explicit approximation error. Finally, we show that the same machinery yields a distributionally robust control formulation of the training problem, connecting Transformer generalization to Wasserstein distributionally robust optimization.

</details>


### [359] [It's All Just Vectorization: einx, a Universal Notation for Tensor Operations](https://arxiv.org/abs/2607.27987)
*Florian Fervers,Sebastian Bullinger,Christoph Bodensteiner,Michael Arens*

Main category: cs.LG

TL;DR: 提出 einx，一种通用的张量运算符号表示法，通过向量化（vectorization）统一和简化现有张量框架（如 Numpy、einsum、einops）的复杂 API，使张量编程更简洁、可读且不易出错。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于张量编程范式与符号表示，属于编程语言/科学计算工具层面的工作，不涉及遥感图像、目标检测、模型压缩或边缘部署等任何相关主题。相关性评分：1 分。

Reading priority: 1 分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于张量编程范式/科学计算工具类工作。除非对张量编程符号系统有特别兴趣，否则不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：张量运算编程范式设计（tensor programming paradigm）。应用场景：通用科学计算与深度学习框架开发。不涉及遥感、卫星、Earth observation、SAR、高光谱等任何遥感场景，也不涉及目标检测任务。

Model architecture: 摘要未说明具体模型结构。论文提出的是 einx 张量运算符号系统，基于 vectorization（向量化）概念，将高阶运算分解为低阶运算及其向量化，用声明式、pointful 表达式类比循环记号（loop notation）来表示张量运算。属于编程接口/API 设计层面的工作，而非神经网络模型架构。

Lightweight method: 不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等任何模型压缩或加速方法。论文的"轻量"仅体现在减少张量框架 API 的复杂度（将大型 API 缩减为一小组基本操作），属于代码层面的简化，而非模型层面的轻量化。

Onboard deployability: 摘要未说明。不涉及星上部署、FPGA/NPU/TensorRT、边缘推理、参数量、FLOPs、延迟、显存、能耗等任何部署相关信息。

Datasets and metrics: 摘要未说明。不涉及任何数据集（如遥感数据集）或评价指标（如 mAP、IoU、FPS 等）。

Experiments: 摘要未说明具体实验。仅提及提供了 einx 的 Python 实现并集成到现有张量框架中，附有 GitHub 仓库链接，但未给出性能对比、基准测试或消融实验等结果。

Limitations: 摘要本身未讨论局限性。但从研究角度，einx 作为编程符号工具，其价值在于提升代码可读性和减少 shape errors，对模型推理速度、参数量、能耗等没有任何直接影响；且其适用范围局限于张量运算表达，无法直接用于遥感目标检测或模型轻量化。

Ideas for my research: 对"在轨遥感目标检测模型轻量化"课题的直接借鉴意义非常有限。可能的间接启发包括：(1) 在实现轻量化模型（如剪枝、量化后的网络）时，使用 einx 或类似符号工具可以简化张量维度操作的代码编写，减少 shape error，提升实验开发效率；(2) 其"用统一规则简化复杂操作"的思想，在工程实现层面有一定参考价值，但与算法层面的轻量化无关。

Abstract: Tensor operations represent a cornerstone of modern scientific computing. However, the Numpy-like notation adopted by predominant tensor frameworks is often difficult to read and write and prone to so-called shape errors, i.a., due to following inconsistent rules across a large, complex collection of operations. Alternatives like einsum and einops have gained popularity, but are inherently restricted to few operations and lack the generality required for a universal model of tensor programming.
  To derive a better paradigm, we revisit vectorization as a function for transforming tensor operations, and use it to both lift lower-order operations to higher-order operations, and conceptually decompose higher-order operations to lower-order operations and their vectorization.
  Building on the universal nature of vectorization, we introduce einx, a universal notation for tensor operations. It uses declarative, pointful expressions that are defined by analogy with loop notation and represent the vectorization of tensor operations. The notation reduces the large APIs of existing frameworks to a small set of elementary operations, applies consistent rules across all operations, and enables a clean, readable and writable representation in code. We provide an implementation of einx that is embedded in Python and integrates seamlessly with existing tensor frameworks: https://github.com/fferflo/einx

</details>


### [360] [Building a User Foundation Model for the Open Web](https://arxiv.org/abs/2607.28019)
*Solal Vernier,Ivan Can Arisoy,Merwan Barlier,Blaž Škrlj*

Main category: cs.LG

TL;DR: 提出一个用于开放网络实时竞价（RTB）场景的用户基础模型，通过自监督学习用户浏览历史（masked language modeling + 序列级对比学习）预训练 Transformer encoder，并用 LLM-in-the-loop 优化预训练流程，在点击预测等下游任务上取得显著提升。

Research relevance: 弱相关（1-2分）。该论文属于电子商务/广告推荐领域的用户基础模型（user foundation model），核心任务是 click prediction、CTR ranking、RTB（实时竞价），与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩/轻量化。仅有的间接关联是使用了 Transformer encoder 架构和自监督预训练，这在泛机器学习层面有一定方法借鉴意义，但与本课题方向基本无关。

Reading priority: 1分。论文主题为广告推荐领域的用户基础模型，与遥感卫星在轨目标检测及轻量化方向基本无关，仅在自监督预训练和 LLM 辅助优化层面有极弱的间接方法借鉴价值，不建议投入时间精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：用户基础模型预训练 + 点击预测（click prediction）/ CTR ranking / 竞价胜率预测（bid win-rate）。应用场景：开放网络实时竞价（open-web RTB）、电子商务与社交推荐。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: Transformer encoder，采用 masked language modeling（MLM）和序列级对比学习（sequence-level contrastive objective）进行自监督预训练，随后在下游点击预测任务上微调（fine-tune）。预训练流程通过 LLM-in-the-loop 搜索（LLM-as-optimizer）在可审查的代码级编辑（lifters）目录中优化。

Lightweight method: 摘要未说明。论文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。唯一的"优化"是 LLM-in-the-loop 对预训练 pipeline 的代码级编辑搜索，属于训练流程优化而非模型轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署指标，也未涉及星上/边缘部署。其部署场景是工业广告系统（production RTB），与在轨/边缘部署无关。

Datasets and metrics: 数据集：摘要未明确说明具体数据集名称（为工业生产数据，涉及 open-web RTB 流量）。评价指标：RIG（Relative Information Gain，用于生产 bid win-rate 模型和 CTR ranker）、CTR（点击率）、eCPC（effective cost per click）。实验设置：7天线上 A/B 测试。

Experiments: 主要结果：同一 encoder 表示在 production bid win-rate 模型上 +1.197% RIG，在 production CTR ranker 上 +1.354% RIG；7天线上 A/B 测试确认 +2.13% CTR、-1.13% eCPC（两个指标的 80% CI 均排除零）。摘要未提供消融实验或对比基线细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化完全无关，属于广告推荐领域。2. 摘要未说明模型规模、推理成本、部署资源需求。3. 未提供与轻量化方法的任何对比。4. 用户身份碎片化、隐私约束等场景假设与遥感在轨场景差异极大。

Ideas for my research: 1. 自监督预训练（MLM + 对比学习）在数据稀缺/分布碎片化场景下的有效性，可类比遥感在轨场景中标注数据稀缺的问题——可借鉴"预训练 + 微调"范式用于遥感目标检测骨干网络。2. LLM-as-optimizer / LLM-in-the-loop 自动搜索代码级优化（lifters）的思想，可迁移到自动搜索轻量化算子或剪枝策略，但需注意在轨部署的算力约束。3. 序列级对比学习可用于遥感时序影像（多时相）的自监督特征学习。总体而言，方法层面的迁移价值有限，优先级低。

Abstract: User foundation models have demonstrated strong results in e-commerce and social recommendation, but most industrial deployments assume environments where user identity is stable and persistent. Open-web real-time bidding (RTB) operates on a structurally different data distribution: user identity is fragmented and non-persistent across browsing sessions, and the availability of browsing history depends on user privacy choices. Consequently, a significant portion of traffic carries no historical data, and available records often consist of relatively short, disjointed sessions. As a result, historical signals in this domain are typically represented as aggregated counters and recency buckets, leaving the sequential structure unexploited. To address this limitation, we present a user foundation model that applies self-supervised learning on user browsing histories and show that the learned representation improves multiple downstream production tasks, demonstrating the viability of this approach on the open web. We pre-train a Transformer encoder with masked language modeling and a sequence-level contrastive objective, then fine-tune it on the click prediction task. We optimize the encoder's pre-training pipeline with an LLM-in-the-loop search over a curated catalog of reviewable, code-level edits (lifters), instantiating the LLM-as-optimizer paradigm in an industrial setting. The same encoder representation yields +1.197% RIG on the production bid win-rate model and +1.354% RIG on the production CTR ranker; a 7-day live A/B test confirms +2.13% CTR, -1.13% eCPC (80% CI excluding zero on both metrics).

</details>


### [361] [Flux-OPD: On-Policy Distillation with Evolving Contexts](https://arxiv.org/abs/2607.28022)
*Yuran Wang,Zekun Wang,Bohan Zeng,Ruixu Zhang,Wenxuan Liu,Liu Yang,Yifan Dai,Yang Shi,Bozhou Li,Chengzhuo Tong,Daili Hua,Yuanxing Zhang,Wentao Zhang*

Main category: cs.LG

TL;DR: 提出Flux-OPD，一种在开放式领域大语言模型训练中使用动态演化上下文作为在线监督的在线偏好蒸馏（OPD）范式，通过反向KL目标分解来稳定蒸馏目标并降低上下文教师间的冲突。

Research relevance: 弱相关（2分）。论文属于大语言模型（LLM）训练与在线偏好蒸馏（online preference distillation, OPD）领域，核心关注开放式领域的任务偏好建模与蒸馏目标稳定性，与遥感、卫星、目标检测、轻量化部署均无直接关联。虽然涉及"distillation"（蒸馏）这一与模型压缩相关的概念，但这里的蒸馏是指LLM训练中的偏好蒸馏，而非面向部署的knowledge distillation模型压缩技术，因此借鉴意义有限。

Reading priority: 1分。论文聚焦LLM在线偏好蒸馏训练，与遥感卫星在轨目标检测及模型轻量化方向基本无关。虽然"动态蒸馏目标"和"冲突加权"思想有一定方法论启发，但迁移成本高、关联度低，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：大语言模型（LLM）在线偏好蒸馏训练；应用场景：开放式领域（open-ended domains）的LLM训练；传感器类型：不适用；目标类别：不适用。摘要未说明具体任务场景。

Model architecture: Flux-OPD框架：基于上下文条件教师（context-conditioned teacher）与无上下文教师（context-free teacher）的差异信号（contextual difference signals），将其作为上下文校正注入无上下文教师锚点，并使用冲突项（conflict term）作为权重指标来调节校正强度。核心机制包括：反向KL目标分解、几何均值蒸馏目标、冲突项度量。

Lightweight method: 摘要中涉及的"distillation"是LLM训练中的在线偏好蒸馏（online preference distillation, OPD），用于将上下文条件教师的知识蒸馏到学生模型中，目的是捕获开放式任务偏好，而非面向边缘/星上部署的模型压缩知识蒸馏（knowledge distillation for compression）。摘要未说明任何剪枝、量化、NAS等轻量化方法。

Onboard deployability: 摘要未说明。论文不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息，且其研究对象为LLM训练范式，与星上/边缘部署无关。

Datasets and metrics: 摘要未说明具体数据集名称和评价指标。仅在开放式任务（open-ended tasks）上进行了实验，声称Flux-OPD优于现有OPD范式，但未给出具体数据集、指标名称或数值。

Experiments: 摘要仅提及在开放式任务上Flux-OPD优于现有OPD范式，未给出具体对比方法、消融实验细节或量化结果。

Limitations: 摘要未说明局限性。主要缺口包括：未给出具体实验数据集与指标；未讨论计算开销；未涉及任何部署或轻量化场景；其蒸馏目标面向LLM训练偏好，与视觉目标检测模型的压缩蒸馏在问题设定上差异显著。

Ideas for my research: 尽管论文与遥感目标检测轻量化方向弱相关，但可借鉴其"动态演化上下文作为蒸馏监督"的思想：在遥感目标检测的知识蒸馏中，可考虑让教师模型或蒸馏目标随学生训练进度动态演化，而非使用固定教师输出，以缓解教师-学生能力差距导致的蒸馏效率下降。此外，其"冲突项加权"思想可类比到多教师蒸馏（multi-teacher distillation）中，用于对不同教师（如不同尺度、不同传感器模态的教师）的监督信号进行冲突感知加权。但这些仅为方法论层面的间接启发，需自行迁移到视觉检测蒸馏场景。

Abstract: Large language model training in open-ended domains lacks verifiable rewards, making task preferences difficult to formalize as effective supervision. Contexts can convey such preferences, yet provide little additional supervision once distilled into the student, motivating contexts that evolve with student performance. However, directly using evolving contexts as in-training supervision results in an unstable distillation target and conflicting distributions, requiring mechanisms to stabilize target and downweight conflicts. In this paper, we analyze the effect of contexts through a decomposition of the reverse KL objective, revealing two findings: the student is distilled toward the geometric mean of context-conditioned teachers, and the objective contains a conflict term that measures conflicts among these teachers. Based on this decomposition, we propose Flux-OPD, an OPD paradigm that uses evolving contexts as in-training supervision to capture task preferences in open-ended domains. Flux-OPD treats the differences between context-conditioned and context-free teachers as contextual difference signals, injects them as contextual corrections into the context-free teacher anchor, and weights their correction strength using the conflict term as an indicator. Experiments on open-ended tasks show that Flux-OPD outperforms existing OPD paradigms, highlighting the potential to combine teacher supervision with evolving contexts.

</details>


### [362] [Contrastive Reinforced Policy Optimization via Privileged Self-Distillation](https://arxiv.org/abs/2607.28026)
*Xingjian Wu,Junlin Liu,Xingchen Liu,Xuhang Zhu,Jianing Wang,Linsen Guo,Xiaoyu Li,Xuezhi Cao,Xunliang Cai*

Main category: cs.LG

TL;DR: 提出对比强化策略优化（CRPO），从对比学习视角重构智能体自蒸馏（OPSD），利用预测熵区分正负位置以保留可靠的细粒度优化信号，在13个推理与深度搜索基准上超越现有RL和自蒸馏基线。

Research relevance: 弱相关（2分）。论文聚焦于大语言模型（LLM）的后训练强化学习与自蒸馏方法，属于自然语言处理/智能体推理领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。唯一可借鉴的是其"对比学习+蒸馏"的思想，可能对知识蒸馏（knowledge distillation）方法设计有间接启发，但并非遥感场景。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于LLM训练优化领域。仅"对比学习+蒸馏"的抽象思想有微弱参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM后训练策略优化（RLVR / On-Policy Self-Distillation）；应用场景：多轮智能体（multi-turn agentic）推理与深度搜索；传感器类型：不适用（非遥感）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体网络结构。核心是CRPO算法框架，从对比学习视角重构agentic OPSD，利用predictive entropy区分positive positions（reflective exploration）和negative positions（exposure bias），进行group-wise contrast。

Lightweight method: 涉及On-Policy Self-Distillation（OPSD）自蒸馏思想，但并非面向模型压缩/加速的轻量化蒸馏，而是用于提升LLM训练稳定性与泛化能力的训练策略。不涉及剪枝（pruning）、量化（quantization）、NAS、实时推理或边缘部署。

Onboard deployability: 摘要未说明。论文未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息，且研究对象为LLM训练阶段，与星上/边缘部署无关。

Datasets and metrics: 在13个challenging reasoning和deep-search benchmarks上进行评估，具体数据集名称摘要未说明。评价指标为训练稳定性（training stability）与泛化能力（generalization），具体指标数值摘要未说明。

Experiments: CRPO在13个推理与深度搜索基准上一致优于现有reinforcement learning和self-distillation基线，显著提升长时程交互（long-horizon interactions）中的训练稳定性与泛化。具体对比方法、消融实验细节摘要未说明。

Limitations: 摘要未说明局限性。但可推断：1）该方法面向LLM/智能体场景，与遥感目标检测任务域差异极大；2）未提供计算开销、部署成本等工程指标；3）对比学习+蒸馏的框架在视觉/遥感任务上的迁移性未经验证。

Ideas for my research: 1）其"对比学习视角重构蒸馏"的思想可迁移到遥感目标检测的知识蒸馏（knowledge distillation）中：例如利用预测熵区分teacher网络中"可靠监督位置"与"噪声/偏差位置"，进行group-wise contrast以保留细粒度特征，可能提升蒸馏质量；2）"exposure bias"概念可类比到遥感检测蒸馏中teacher-student特征不对齐问题；3）但整体迁移成本高、收益不确定，建议仅作为蒸馏方法设计的灵感参考，不作为核心借鉴。

Abstract: Recent advances in post-training Large Language Models (LLMs) increasingly rely on Reinforcement Learning with Verifiable Rewards (RLVR) or On-Policy Self-Distillation (OPSD). While OPSD provides dense, logit-level supervision, it inherently suffers from exposure bias due to the privileged information of the self-teacher. In multi-turn agentic settings, this leads to reasoning route convergence and the loss of clear optimization directions. To tackle these challenges, we introduce Contrastive Reinforced Policy Optimization (CRPO), which reformulates agentic OPSD from a contrastive learning perspective. By leveraging predictive entropy to distinguish between positive positions (reflective exploration) and negative positions (exposure bias), CRPO conducts group-wise contrast to preserve reliable, fine-grained optimization signals. Extensive evaluations across 13 challenging reasoning and deep-search benchmarks demonstrate that CRPO consistently outperforms existing reinforcement learning and self-distillation baselines, significantly enhancing training stability and generalization in long-horizon interactions.

</details>


### [363] [Enhancing Irregular Time Series Forecasting with Continuous-Time Modeling Framework](https://arxiv.org/abs/2607.28035)
*Tianen Shen,Zhengyu Li,Yutong Li,Xiangfei Qiu,Xingjian Wu,Bin Yang,Jilin Hu*

Main category: cs.LG

TL;DR: 提出WrapFlow，一个面向不规则多元时间序列预测的连续时间建模框架，通过Continuous-Time Tokenization编码原始观测事件，并采用simulation-free的Residual Flow Matching训练范式，避免ODE数值求解器开销，在多个真实数据集上达到SOTA。

Research relevance: 弱相关（2分）。论文聚焦不规则多元时间序列预测（irregular multivariate time series forecasting），应用场景为healthcare monitoring、human activity recognition、environmental sensing，与遥感/卫星/在轨目标检测无直接关联。虽然时间序列建模方法（continuous-time modeling、flow matching）在理论上可能对遥感时序数据处理有间接借鉴，但论文不涉及遥感图像、目标检测或轻量化部署，属于泛机器学习/时序建模方法。

Reading priority: 2分。该论文属于不规则时间序列预测领域，与遥感在轨目标检测及模型轻量化方向弱相关。仅在连续时间建模和计算开销优化方面有间接方法论参考价值，不建议作为核心阅读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：不规则多元时间序列预测（irregular multivariate time series forecasting）。应用场景：healthcare monitoring（医疗监测）、human activity recognition（人体活动识别）、environmental sensing（环境感知）。传感器类型：时序传感器数据（非图像）。目标类别：不涉及目标检测。

Model architecture: WrapFlow框架包含两部分：(1) 输入侧：Continuous-Time Tokenization，直接编码原始观测事件，通过gap-aware tokens显式建模长未观测间隔，生成continuous-time tokens后送入标准Transformer backbone捕获长程时序依赖；(2) 输出侧：Residual Flow Matching，学习围绕base predictions的条件残差向量场（conditional residual vector fields），采用simulation-free训练范式，避免numerical-solver simulation和训练期backpropagation。

Lightweight method: 摘要未明确提及剪枝、量化、蒸馏或NAS等传统轻量化方法。但框架设计上体现了计算效率考量：采用simulation-free训练范式避免ODE数值求解器开销，推理时仅需少量固定rollout steps（a small number of fixed rollout steps），这属于算法层面的计算开销优化，而非模型压缩/加速的轻量化技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及推理时使用少量固定rollout steps以减少计算开销，但未给出具体数值或硬件平台（FPGA/NPU/TensorRT等）验证。不涉及星上/边缘部署场景。

Datasets and metrics: 摘要提到"multiple real-world datasets"（多个真实世界数据集），但未列出具体数据集名称。评价指标未明确说明，仅称达到state-of-the-art performance。实验设置细节（对比方法、消融实验）摘要未说明。

Experiments: 摘要仅说明在多个真实世界数据集上WrapFlow达到SOTA性能，未给出具体实验结果数值、对比方法列表或消融实验细节。

Limitations: (1) 与遥感/卫星/目标检测方向无直接关联，属于时序预测领域；(2) 摘要未提供模型复杂度、参数量、推理速度等部署相关信息；(3) 未说明在图像/遥感数据上的适用性；(4) 未提及与轻量化技术（剪枝、量化、蒸馏）的结合；(5) 摘要未说明方法在极端不规则采样或大规模数据下的鲁棒性。

Ideas for my research: (1) Continuous-Time Tokenization思想可借鉴：遥感卫星对地观测数据本身具有不规则时间采样特性（重访周期、云遮挡导致的数据缺失），gap-aware tokens建模长未观测间隔的思路可用于遥感时序特征提取；(2) simulation-free的flow matching训练范式可降低连续时间建模的计算开销，若未来在轨场景需要处理连续时间遥感数据流，此方法有理论参考价值；(3) 但整体而言，该论文与"在轨遥感目标检测模型轻量化"课题的直接可借鉴点有限，更多是方法论层面的间接启发。

Abstract: Irregular multivariate time series are widely encountered in applications such as healthcare monitoring, human activity recognition, and environmental sensing. Their core challenges stem from asynchronous observations, non-uniform sampling intervals, and the fact that temporal patterns themselves carry critical dynamic information. Existing approaches either rely on discretization-based preprocessing (e.g., interpolation, imputation, or aggregation), which disrupts the underlying continuous-time semantics, or adopt continuous-time modeling via ODE-based frameworks, which typically require specialized architectures and incur substantial computational overhead due to numerical solvers. To address these limitations, we propose WrapFlow, a continuous-time modeling framework for irregular time series forecasting. On the input side, WrapFlow introduces Continuous-Time Tokenization, which directly encodes raw observation events and explicitly models long unobserved intervals via gap-aware tokens. The resulting continuous-time tokens are then processed by a standard Transformer backbone to capture long-range temporal dependencies. On the output side, we develop a simulation-free training paradigm for Residual Flow Matching, which learns conditional residual vector fields around base predictions while avoiding numerical-solver simulation and backpropagation during training. This design enables high-quality continuous forecasting using only a small number of fixed rollout steps at inference. Extensive experiments on multiple real-world datasets demonstrate that WrapFlow achieves state-of-the-art performance.

</details>


### [364] [Learning features from Newton's algorithm: a way to accelerate nonlinear parametrized PDE solvers](https://arxiv.org/abs/2607.28036)
*Rémy Vallot,Florian de Vuyst,Thibault Dairay,Mathilde Mougeot*

Main category: cs.LG

TL;DR: 提出一种两阶段Newton初值猜测策略，通过参数空间采样和预计算解数据库学习特征，构建解特征空间与修正搜索方向特征空间，用回归模型预测代理解近似并结合GMRES残差最小化修正，为高保真Newton法提供更优初值以加速非线性方程组求解。

Research relevance: 与本研究方向基本无关（1分）。该论文聚焦于数值计算领域中的非线性方程组求解加速问题，属于计算数学/科学计算范畴，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等遥感场景，也不涉及object detection、目标检测、小目标检测等视觉任务，更不涉及pruning、quantization、knowledge distillation、NAS等模型轻量化或部署方法。论文中的"learning"和"regression"仅指对数值解空间的机器学习代理建模，与深度学习视觉模型无关。

Reading priority: 1分。该论文属于计算数学/数值求解领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：非线性方程组（PDE离散后）的数值求解加速。应用场景：大规模科学计算/数值仿真。传感器类型：不适用。目标类别：不适用。摘要未涉及任何遥感或视觉相关任务与场景。

Model architecture: 两阶段Newton初值猜测策略：(1) 通过离散Newton轨迹构建两个互补的降维空间——解特征空间（由收敛状态构建）和修正搜索方向特征空间（由中间Newton增量构建）；(2) 对未见参数用回归模型预测代理解近似，再用基于GMRES的残差最小化方法计算修正量，最终作为高保真Newton法的初值完成收敛。整体属于数值优化与降阶模型（ROM）方法，非神经网络架构。

Lightweight method: 摘要未涉及传统意义上的模型轻量化方法（如pruning、quantization、knowledge distillation、NAS）。论文中的"计算开销小"体现在修正步骤仅需残差评估和小规模最小二乘求解，属于数值计算层面的加速策略，而非深度学习模型压缩或加速。该方法"弱侵入式"（weakly intrusive）的特性与部署有一定类比性，但并非面向边缘/星上部署。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及该方法可减少Newton迭代次数并降低整体CPU时间，在代表性PDE问题上相比独立代理初始化有可量化的加速。但这是CPU上的数值求解加速，与星上/边缘部署（FPGA/NPU/TensorRT）无关。

Datasets and metrics: 数据集：代表性PDE问题（具体问题类型摘要未说明）。评价指标：Newton迭代次数、整体CPU时间、相对独立代理初始化的加速比（speedup）。未涉及任何遥感数据集或目标检测评价指标（如mAP、IoU等）。

Experiments: 在代表性PDE问题上进行了数值实验，对比了独立代理初始化（standalone surrogate initialization），结果显示所提两阶段策略减少了Newton迭代次数并降低了整体CPU时间，观察到显著加速。摘要未给出具体数值结果和消融实验细节。

Limitations: 摘要未说明具体PDE问题类型、规模、加速比数值、方法对问题类型的依赖程度、回归模型的训练数据需求、以及在高维/强非线性问题上的鲁棒性。此外，该方法需要预计算解数据库和脚本化编程接口，工程实现成本未讨论。与遥感在轨目标检测完全无关。

Ideas for my research: 尽管论文与遥感目标检测无关，但"两阶段初值猜测+残差修正"的思想在方法论层面有一定类比启发：(1) 在知识蒸馏或模型微调中，可类比"用低成本代理模型预测初值，再用高保真模型精修"的两阶段推理策略，用于在轨场景下先用轻量模型快速筛选候选框、再用高精度模型精修；(2) "降维特征空间+回归预测"的思路可类比于在轻量化检测器中用低秩分解或子空间投影压缩特征表示。但这些仅是方法论层面的弱类比，实际借鉴价值有限。

Abstract: It is well known that Newton's method converges faster when the initial guess is closer to a root of a system of nonlinear equations. In this paper, a two-stage Newton initial guess strategy is proposed by learning features from a parameter-space sampling and a database of precomputed solutions. The method uses discrete Newton trajectories to construct two complementary reduced spaces: a solution feature space, built from converged states, and a corrective search direction feature space, built from intermediate Newton increments. For an unseen parameter, a regression model is used to predict a surrogate solution approximation. Then, in a second step, a residual-minimizing correction is computed using a dedicated GMRES-based approach. The resulting state is then used as an initial guess for the high-fidelity Newton method, which completes convergence. The corrective step is computationally inexpensive since it only requires residual evaluations and the solution of a small least-squares problem. The methodology is weakly intrusive once the high-fidelity residual fields and a script-based programming interface are available. This strategy reduces the number of Newton iterations and decreases the overall CPU time. Numerical experiments on representative PDE problems show quantifiable speedups compared with standalone surrogate initialization. Significant speedups are observed. This generic approach can be applied to a broad class of large-scale nonlinear problems.

</details>


### [365] [ClawTrack: Towards Trace-Level Evaluation and Improvement of Real-World Autonomous Agents](https://arxiv.org/abs/2607.28037)
*Xingjian Wu,Xuhang Zhu,Xingchen Liu,Junlin Liu,Jianing Wang,Linsen Guo,Xiaoyu Li,Xuezhi Cao,Xunliang Cai*

Main category: cs.LG

TL;DR: 提出ClawTrack，一个针对LLM智能体多步工作流的双维度评估基准，同时衡量任务结果（Task Score）和推理过程质量（Process Score），以区分可靠推理与侥幸成功。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于LLM智能体（LLM-based agents）的评估基准设计，属于自然语言处理/智能体评估领域，不涉及遥感、卫星、目标检测或模型压缩/加速。相关性评分为1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于LLM智能体评估领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM智能体多步工作流的过程级评估（process-level evaluation）。应用场景：通用智能体任务（8个领域、320个任务）。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体模型架构。论文核心贡献是评估框架ClawTrack，包含Task Score和Process Score双维度评估，以及Process Grader评分器（沿goal alignment、efficiency、information utilization、result verification四个维度，基于12,541条rubric items）。评估对象为21个LLM模型。

Lightweight method: 摘要未涉及任何模型压缩或加速方法（如pruning、quantization、knowledge distillation、NAS等）。论文中的"post-training improvements"指基于过程轨迹过滤的数据后训练，并非模型轻量化技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署相关信息。论文不涉及边缘部署或星上部署。

Datasets and metrics: 数据集：ClawTrack基准，包含320个任务、8个领域、25+确定性mock服务、12,541条任务级rubric items。评估指标：Task Score（任务结果得分）和Process Score（过程得分，沿四个维度）。实验：评估21个模型、16,000+次试验。

Experiments: 主要发现：(1) 过程得分能有效将成功/失败归因到具体推理维度，过滤掉仅凭结果评估无法发现的侥幸通过；(2) 四个评估维度互补，其中result verification是系统性瓶颈；(3) 框架对不同judge LLM的选择具有鲁棒性；(4) 基于过程轨迹过滤的后训练在不同模型规模上均带来一致提升。

Limitations: 摘要未说明局限性。但明显与遥感/目标检测/轻量化方向无关，属于LLM智能体评估领域。其评估对象是文本推理过程，不涉及视觉感知、遥感图像处理或模型部署。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是：其"过程级评估"思想（区分结果正确与推理可靠）可类比到目标检测中区分"检测框正确但定位不可靠"与"真正稳健的检测"，即不仅看mAP等最终指标，还可设计过程/中间特征层面的评估维度。但这种类比较为牵强，且论文本身不涉及视觉任务。

Abstract: As LLM-based agents are deployed in complex, multi-step workflows, a critical evaluation gap has emerged: most existing benchmarks judge only final outcomes, unable to distinguish reliable reasoning from lucky success or attribute failures to specific process deficiencies, hindering attribution in long-horizon tasks.
  In this work, we present ClawTrack, a dual-assessment benchmark that simultaneously measures what an agent achieves (Task Score) and how it achieves it (Process Score). ClawTrack comprises 320 tasks across 8 domains with 25+ deterministic mock services. A Process Grader scores each reasoning turn along four dimensions (goal alignment, efficiency, information utilization, and result verification), anchored by 12,541 task-specific rubric items. Evaluating 21 models over 16,000+ trials, we find that: (1) process scores effectively attribute success and failure to specific reasoning dimensions, filtering lucky passes invisible to outcome-only evaluation; (2) the four dimensions are complementary, with result verification as the systematic bottleneck; (3) the framework is robust to evaluator choice across different judge LLMs; and (4) process-based trajectory filtering yields consistent post-training improvements across model scales.

</details>


### [366] [GVR-Coder: A Visual-Feedback Framework for Structured SVG Generation in Complex Document and Meeting Scenarios](https://arxiv.org/abs/2607.28073)
*Yiming Xu,Jihua Kang,Chunsai Du,Qifan Zhang,Wangqiu Zhou,Yiting Wu,Tianqi Li,Qi Song*

Main category: cs.LG

TL;DR: 提出GVR-Coder框架，用于从长文本生成逻辑清晰的SVG图表，通过课程式拒绝采样微调、布局约束、双渲染反馈强化学习和生成-验证-修复智能体循环来提升图表生成质量。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于Text-to-SVG图表生成（自然语言处理+计算机图形学交叉领域），不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱等任何遥感相关元素，也不涉及object detection、小目标检测、旋转框检测等目标检测任务，更不涉及pruning、quantization、knowledge distillation、NAS等轻量化或部署技术。属于泛NLP/图形生成方法，与本课题弱相关（评分1分）。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于Text-to-SVG图表生成领域。除非对"生成-验证-修复"迭代优化或课程式训练策略有跨领域兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：Text-to-SVG图表生成（从长文本生成逻辑清晰的矢量图）。应用场景：专业文档撰写和会议评审场景。传感器类型：不适用（非遥感传感器）。目标类别：不适用（生成对象为SVG逻辑图表，非遥感目标）。

Model architecture: GVR-Coder框架，包含：课程式拒绝采样微调（curriculum-driven rejection sampling fine-tuning）、显式布局约束知识注入、基于双渲染反馈的强化学习（reinforcement learning from dual rendering feedback）、生成-验证-修复智能体循环（generate-verify-repair agent loop）。具体底层模型架构（如Transformer、扩散模型等）摘要未说明。

Lightweight method: 摘要未涉及任何模型压缩或加速方法。未提及pruning、quantization、knowledge distillation、NAS、轻量网络设计、实时推理、边缘部署等轻量化技术。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署相关信息。该任务为离线文本到图表生成，与星上/边缘实时部署场景无关。

Datasets and metrics: 数据集：DocMeetSVG-100K（面向文档撰写和会议评审场景的大规模SVG数据集）。评价指标：摘要未明确列出具体指标名称，仅提及"outperforms competitive baselines"和"logically coherent and visually appealing"等定性描述。

Experiments: 摘要提及"Extensive experiments demonstrate that GVR-Coder outperforms competitive baselines"，但未给出具体对比方法名称、定量结果、消融实验细节。代码和数据已开源（https://github.com/CurryaNa/GVR-Coder）。

Limitations: 摘要未说明局限性。从研究角度看，该工作与遥感目标检测和轻量化方向无直接关联，其方法（如课程式微调、强化学习反馈、生成-验证-修复循环）属于NLP/图形生成范式，难以直接迁移到遥感在轨目标检测任务。

Ideas for my research: 尽管整体弱相关，但个别方法论思想可能有间接借鉴价值：(1) "generate-verify-repair agent loop"的迭代优化思想，可类比于检测模型的自适应后处理或在线校准机制；(2) "curriculum-driven"渐进式训练策略，可借鉴到遥感小目标检测的分阶段训练中（先易后难）；(3) "dual rendering feedback"的强化学习反馈机制，可类比于检测任务中利用检测质量指标作为reward进行策略优化。但这些均为间接启发，不构成直接可复现的技术路线。

Abstract: In demanding professional environments and meeting review scenarios, lengthy text often imposes a high cognitive load. To facilitate efficient information communication, transforming verbose text into logically clear diagrams is essential. Scalable Vector Graphics (SVG) provide an effective representation for this purpose due to their editability and resolution independence. However, current research on Text-to-SVG generation remains hindered by three major challenges: (1) the scarcity of datasets for complex, logic-rich diagrams; (2) the absence of explicit layout priors, which leads to chaotic spatial arrangements; and (3) the lack of fine-grained visual feedback to validate rendered outputs and correct aesthetic defects. To address these challenges, at the data level, we introduce DocMeetSVG-100K, a large-scale SVG dataset tailored for document authoring and meeting review scenarios. At the model level, we propose GVR-Coder, a novel framework designed to generate high-quality logical diagrams from lengthy professional texts. Specifically, we adopt a curriculum-driven rejection sampling fine-tuning to progressively enhance the model's capability in modeling complex structures, while explicitly incorporating layout constraint knowledge during training. In addition, we introduce reinforcement learning from dual rendering feedback, a mechanism that provides implicit feedback through reward signals to jointly optimize structural complexity and visual aesthetics. Furthermore, we design a generate-verify-repair agent loop, which improves generation quality through explicit, fine-grained feedback and targeted refinement. Extensive experiments demonstrate that GVR-Coder outperforms competitive baselines and reliably produces logically coherent and visually appealing diagrams. Code and data are available at https://github.com/CurryaNa/GVR-Coder.

</details>


### [367] [Chem World: A Large-Scale Benchmark and Physics-Informed Framework for Trustworthy Chemical Property Prediction](https://arxiv.org/abs/2607.28079)
*Tianyou Bai,Huan Wang,Mingchen Gao,Fangyue Lin,Pinze Ren,Zhenlin Zhao,Siming Dong*

Main category: cs.LG

TL;DR: 提出Chem World化学性质预测基准（17个数据集、80万+分子样本）和Mixture-PINN物理信息神经网络框架，用于化学、材料科学和药物开发中的性质预测。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦化学性质预测（chemical property prediction），涉及物理信息神经网络（physics-informed neural network, PINN）和化学基准数据集，不涉及遥感、卫星、目标检测或模型轻量化。属于计算化学/材料科学领域的AI方法，与本课题方向弱相关（1分）。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于计算化学领域，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：化学性质预测（chemical property prediction）；应用场景：化学、材料科学、药物开发；传感器类型：不适用（非遥感传感器）；目标类别：分子性质（密度、电导率、溶解度等）。

Model architecture: Mixture-PINN：基于物理信息神经网络（physics-informed neural network, PINN）的预测框架，将化学先验知识（chemical prior knowledge）融入数据驱动学习中。摘要未说明具体网络结构细节。

Lightweight method: 摘要未涉及任何模型轻量化方法（无pruning、quantization、knowledge distillation、NAS等）。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: Chem World基准：整合17个化学数据集，包含超过800,000个分子样本，覆盖密度（density）、电导率（electrical conductivity）、溶解度（solubility）等性质。摘要未说明具体评价指标。

Experiments: 在Chem World上进行广泛实验，与现有方法对比，验证了Mixture-PINN在准确性、鲁棒性和可靠性方面的有效性。摘要未提供具体数值结果或消融实验细节。

Limitations: 摘要未说明局限性。但与本课题方向完全无关：不涉及遥感图像、目标检测、在轨部署或模型压缩加速。PINN方法虽涉及物理先验融入，但与遥感目标检测的物理建模（如几何先验、传感器模型）无直接关联。

Ideas for my research: 借鉴意义有限。唯一可能的间接启发是"将领域先验知识融入数据驱动模型"这一思想——在遥感目标检测中可类比为将卫星传感器成像模型、目标几何约束等先验融入检测网络，但论文本身并未提供可直接复用的遥感相关技术。不建议作为主要参考。

Abstract: Chemical property prediction plays a critical role in accelerating scientific discovery in chemistry, materials science, and drug development. However, existing benchmarks often suffer from limited task diversity, fragmented datasets, and inconsistent evaluation protocols, making it challenging to systematically assess the reliability and generalization of AI models. In this work, we introduce Chem World, a comprehensive benchmark for chemical property prediction that integrates 17 diverse chemical datasets with over 800,000 molecular samples, covering various properties including density, electrical conductivity, solubility, and other molecular characteristics. Chem World provides a unified platform for evaluating AI models across multiple property prediction tasks. Furthermore, we propose Mixture-PINN, a physics-informed neural network based prediction framework that incorporates chemical prior knowledge into data-driven learning, improving the accuracy, robustness, and reliability of chemical property prediction. Extensive experiments on Chem World demonstrate the effectiveness of our approach compared with existing methods. By combining large-scale standardized evaluation with physics-informed learning, Chem World establishes a foundation for developing trustworthy AI systems for computational chemistry and advancing AI-driven scientific discovery.

</details>


### [368] [From Expert Reduction to Behavioral Divergence: Tracing Numerical State through Sparse MoE Inference](https://arxiv.org/abs/2607.28097)
*Tianyang Zhu*

Main category: cs.LG

TL;DR: 本文研究稀疏MoE（DeepSeek-V4-Flash）中数学上等价的专家归约顺序在数值执行层面产生可观测差异的现象，提出将专家操作数转换、累加器精度和归约顺序纳入稀疏MoE运行时与硬件后端的数值兼容性契约。

Research relevance: 弱相关（2分）。论文属于泛机器学习/大模型推理数值稳定性研究，聚焦稀疏MoE（Mixture-of-Experts）的数值执行语义，与遥感、卫星、目标检测、轻量化部署均无直接关联。唯一可能的间接联系是：稀疏MoE的数值兼容性契约对边缘/星上硬件后端的低精度推理有潜在参考价值，但摘要完全未涉及遥感或目标检测场景。

Reading priority: 1分。论文聚焦稀疏MoE大模型数值执行语义，与遥感卫星在轨目标检测及轻量化方向基本无关。仅对低精度推理数值一致性有极间接的参考价值，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：稀疏MoE大语言模型（DeepSeek-V4-Flash）的数值执行语义分析。应用场景：大模型推理的数值稳定性与硬件后端兼容性。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 研究对象为DeepSeek-V4-Flash稀疏MoE模型。核心关注点是MoE层中专家归约（expert-reduction）的聚合语义，区分了四种方案（A/B/C/P32），将操作数表示（operand representation）与累加器精度（accumulator precision）分离。识别出post-mHC为token内边界（intra-token boundary），完整持久状态为跨token延续边界（cross-token continuation boundary）。

Lightweight method: 摘要未说明传统轻量化方法（剪枝、量化、蒸馏、NAS）。但涉及数值精度层面的研究：操作数转换（operand conversion）、累加器精度（accumulator precision）、FP64精确重建、P32（推测为某种32位精度方案）。这些属于低精度数值执行语义研究，与量化推理有间接关联，但并非面向模型压缩或加速的轻量化方法。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。论文关注的是稀疏MoE运行时与硬件后端的数值兼容性契约（numerical compatibility contract），属于数值语义层面，而非工程部署性能层面。对星上/边缘部署的直接可行性信息缺失。

Datasets and metrics: 数据集：摘要未说明具体数据集。实验设置：使用中文prompt（one Chinese prompt）进行测试，50个prompt、192条持久轨迹（persistent trajectories）每方案。评价指标：延续盆地（continuation basins）、结构类别数、token差异数、bitwise匹配、post-mHC状态、路由（routes）、预测与文本的一致性。

Experiments: 主要实验发现：1）720种A-mode归约顺序产生10个延续盆地，720种B-mode顺序形成360个精确结构类别和11个盆地；2）同一中文prompt下B类分裂为202个layoffs、113个hiring、45个other延续；3）最大L-infinity B分支选择可在8/16/32 token尺度上区分12/24/36个prompt；4）P32、A、B方案改变所有native-reference路由轨迹，而C方案保持路由、token序列和文本不变；5）192轨迹C检查与native MoE、post-mHC、next-router、LM状态bitwise匹配；6）FP64精确重建可复现下游301个post-mHC状态、301个持久状态检查点、301条路由、预测和文本。

Limitations: 1）与遥感/卫星/目标检测完全无关；2）摘要明确说明"建立的是受控因果可能性（controlled causal possibility），而非部署发生率（deployment incidence）"；3）C方案的顺序不变性仅限于被评估的六项状态和调度（six-term states and schedules）；4）未提供任何部署性能指标；5）研究对象为通用大模型推理，非遥感在轨场景。

Ideas for my research: 1）数值兼容性契约思想可借鉴：在星上FPGA/NPU低精度推理中，数学上等价的算子重排可能因累加器精度不同产生可观测差异，设计检测模型时需考虑数值语义一致性；2）低精度（如FP16/INT8）量化推理时，累加器精度与归约顺序对检测框回归精度的影响值得关注；3）"identical tokens need not imply identical autoregressive state"这一发现提示：在边缘部署中，相同输入在不同硬件后端可能产生不同中间状态，需建立数值兼容性测试；4）但整体借鉴价值有限，因为论文不涉及目标检测或遥感场景。

Abstract: Mathematically equivalent expert-reduction orders can produce observably different sparse-MoE executions. We isolate this effect in native DeepSeek-V4-Flash by freezing local MoE state and varying only aggregation semantics. Four schemes separate operand representation from accumulator precision. At one layer-5 fork, 720 A-mode orders yield 10 continuation basins; 720 B-mode orders form 360 exact structural classes and 11 basins. Under one Chinese prompt, the B classes split into 202 layoffs, 113 hiring, and 45 other continuations. Maximum-L-infinity B-branch selection separates 12, 24, and 36 of 50 prompts by 8, 16, and 32 tokens. Across 192 persistent trajectories per scheme, P32, A, and B change every native-reference route trajectory, while C preserves routes, token sequences, and texts. A separate 192-trajectory C check matches native MoE, post-mHC, next-router, and LM states bitwise. For one controlled B branch, exact post-mHC endpoint reconstruction reproduces the measured downstream trajectory. At the next decode boundary, exact FP64 reconstruction of the branch's full persistent state yields agreement for 301 downstream post-mHC states, 301 persistent-state checkpoints, 301 routes, predictions, and text over seven steps, given the same naturally generated next input. These controls identify post-mHC as an intra-token boundary and full persistent state as a cross-token continuation boundary. Identical tokens need not imply identical autoregressive state: divergence can survive a token boundary and become visible later. These results make expert operand conversion, accumulator precision, and reduction order part of a numerical compatibility contract for sparse-MoE runtimes and hardware backends. They establish controlled causal possibility, not deployment incidence; C's order invariance is limited to evaluated six-term states and schedules.

</details>


### [369] [Information Bottleneck Learning for Faithful Time Series Forecasting Explanations](https://arxiv.org/abs/2607.28124)
*Xu Zheng,Wei Cheng,Zhuomin Chen,Mo Sha,Jingchao Ni,Dongsheng Luo*

Main category: cs.LG

TL;DR: 提出IB-Forecast，一个内在可解释的多变量时间序列预测框架，通过信息瓶颈和可解释掩码实现高保真解释，同时保持与黑盒模型相当的预测精度。

Research relevance: 弱相关（2分）。论文聚焦于时间序列预测的可解释性（interpretable time-series forecasting），属于泛机器学习/泛AI方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 2分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于时间序列预测可解释性领域。仅信息瓶颈思想可能对特征筛选或蒸馏保真度评估有间接启发，但整体借鉴价值有限，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多变量时间序列预测（multivariate time-series forecasting）及其可解释性（explainability/faithfulness）。应用场景：能源、交通、医疗等领域的预测决策。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。

Model architecture: IB-Forecast框架将预测分解为：1) 学习到的周期分量（learned periodic component）；2) 基于输入token可解释掩码（explainable masks）计算的残差分量（residual component）。采用预算约束的信息瓶颈（budget-constrained information bottleneck）进行端到端优化，使用户能直接控制解释稀疏度（explanation sparsity）。

Lightweight method: 摘要未说明传统轻量化方法（pruning/quantization/distillation/NAS）。但信息瓶颈（information bottleneck）机制通过约束使用的观测数量（仅需14-20%的观测即可实现低误差预测），在推理层面实现了数据效率，且"no additional inference cost"表明解释不增加推理开销，这与部署效率有一定间接关联。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及"no additional inference cost"（解释不增加推理成本）和仅需14-20%观测数据即可低误差预测，暗示推理效率较高，但缺乏具体量化数据。

Datasets and metrics: 摘要未明确列出具体数据集名称。评价指标包括：预测误差（forecasting error，与领先黑盒模型对比）、解释保真度（faithfulness evaluation protocol）、稀疏度预算（sparsity budget）下的解释质量对比（对比gradient-based、occlusion-based、optimization-based基线）。

Experiments: 实验表明：1) IB-Forecast在预测误差上与领先黑盒模型相当；2) 在匹配的稀疏度预算下，其原生解释（native explanations）在所有数据集上一致优于gradient-based、occlusion-based和optimization-based基线；3) 现有可解释预测器的原生解释保真度差，而IB-Forecast保证高解释保真度；4) 仅需14-20%的观测即可实现低误差预测。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于时间序列预测领域；2) 摘要未说明模型参数量、计算复杂度、部署可行性；3) 未涉及任何目标检测任务；4) 未涉及任何模型压缩/加速技术；5) 应用场景（能源、交通、医疗）与在轨遥感场景差异大。

Ideas for my research: 尽管与遥感目标检测轻量化方向弱相关，但以下思路可借鉴：1) 信息瓶颈（information bottleneck）思想——在目标检测模型中引入信息瓶颈约束，可作为一种模型压缩/特征筛选手段，减少冗余特征计算；2) "仅需少量输入即可保持性能"的思想可类比于遥感图像中的稀疏采样或关键区域聚焦，降低在轨推理时的数据处理量；3) 端到端可解释性与推理成本解耦的设计思路，对星上模型的可解释性和可验证性有参考价值；4) faithfulness评估协议的设计思路可用于评估轻量化模型相对原始模型的保真度（如蒸馏后的行为一致性）。

Abstract: As forecasts increasingly drive decisions in fields such as energy, transportation, and healthcare, understanding the historical data behind these predictions has become as crucial as the predictions themselves. Although existing interpretable-by-design forecasters reveal their internal structures, they offer no guarantee that these structures faithfully reflect the underlying evidence driving the predictions. In contrast, while faithfulness-oriented methods explicitly verify model behavior, they are almost exclusively designed for post-hoc classification tasks. To bridge this gap, we propose IB-Forecast, an inherently interpretable multivariate time-series forecasting framework. It decomposes forecasting into a learned periodic component and a residual component computed with explainable masks over input tokens. With a budget-constrained information bottleneck, end-to-end optimization enables users to directly control explanation sparsity. With a rigorous faithfulness evaluation protocol, extensive experiments demonstrate that IB-Forecast matches the forecasting error of leading black-box models while providing faithful explanations at no additional inference cost. Furthermore, under a matched sparsity budget, these native explanations consistently surpass gradient-based, occlusion-based, and optimization-based baselines across all evaluated datasets. Ultimately, whereas the native explanations of existing interpretable forecasters exhibit poor faithfulness, IB-Forecast guarantees high explanation fidelity, requiring only 14-20% of the observations to deliver low-error predictions.

</details>


### [370] [LM-GRASP: Instance-Specific Language Models for Combinatorial Construction via Online Imitation Learning](https://arxiv.org/abs/2607.28135)
*Mohand Mezmaz,Grégoire Danoy*

Main category: cs.LG

TL;DR: 提出LM-GRASP，一种将GRASP元启发式的随机构造阶段重构为在线模仿学习任务的框架，用decoder-only Transformer作为构造策略，通过行为克隆在单个问题实例上从零训练，无需离线预训练或问题特征工程。

Research relevance: 弱相关（2分）。该论文属于组合优化（combinatorial optimization）领域，具体针对流水车间调度问题（PFSP），与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。它属于泛机器学习/元启发式方法，对遥感目标检测方向几乎没有直接借鉴意义。

Reading priority: 1分。该论文属于组合优化/元启发式领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：组合优化（combinatorial optimization），具体为置换流水车间调度问题（Permutation Flow-shop Scheduling Problem, PFSP）。应用场景：生产调度/运筹优化。传感器类型：不适用。目标类别：不适用。

Model architecture: 采用decoder-only Transformer作为构造策略（constructive policy），将GRASP（Greedy Randomized Adaptive Search Procedure）的随机构造阶段重构为在线模仿学习任务。本地搜索（local search）作为专家oracle提供示范轨迹。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。其"轻量"体现在训练方式上——无需大规模离线预训练数据集，而是每个实例在线从零训练，但这是训练策略层面的轻量，而非模型部署层面的轻量化。

Onboard deployability: 摘要未说明。论文未报告参数量、FLOPs、延迟、显存、能耗等部署指标。且该工作面向调度优化而非在轨遥感推理，不具备星上部署可行性分析。

Datasets and metrics: 数据集：Taillard PFSP benchmark（ta51-ta60），这是该基准中最具区分度的块，其中一半最优解未知。评价指标：makespan（完工时间），对比GPU-GRASP平均提升28.4个makespan单位，与GPU加速相对串行执行的增益（27.2单位）相当，但标准差有重叠。

Experiments: 在Taillard PFSP基准的ta51-ta60实例上评估，LM-GRASP相比GPU-GRASP平均提升28.4 makespan单位。作者指出该提升幅度与GPU加速相对串行执行的增益（27.2单位）相当，但统计显著性有限（标准差重叠）。摘要未报告消融实验或更多对比。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于运筹优化领域。2. 统计显著性存疑——28.4 vs 27.2的提升与GPU加速增益相当但标准差重叠，说明提升可能不显著。3. 在线从零训练每个实例的Transformer，推理/训练开销可能很高，未报告计算成本。4. 仅在单一基准（PFSP）上验证，泛化性未知。

Ideas for my research: 1. 该论文的"在线学习+专家oracle"范式（learn-infer-improve循环）可类比到遥感目标检测的持续学习/在线适应场景——例如在轨模型根据新获取的遥感图像在线微调，以本地检测器作为专家提供伪标签。2. "无需离线预训练、实例自适应"的思想对星上模型自适应有一定启发，但需要结合轻量化约束。3. 整体而言，该论文与遥感在轨目标检测轻量化课题的关联非常有限，借鉴价值低。

Abstract: Machine learning for combinatorial optimization typically relies on neural constructors trained via reinforcement learning on large offline datasets for a fixed problem class-incurring high pretraining costs and generalizing poorly outside the training distribution. We propose an alternative: a metaheuristic framework that reformulates the randomized constructive phase of GRASP as an online imitation learning task, trained from scratch on each problem instance. A local search procedure acts as an expert oracle, while a decoder-only Transformer serves as the constructive policy. Unlike classical GRASP, which relies on static, myopic heuristic rules based on localized scalar costs, our approach is fully data-driven: the construction policy emerges from high-quality solutions discovered during the search itself, with no problem-specific feature engineering required.
  We instantiate this as LM-GRASP, a hybrid metaheuristic following an iterative learn-infer-improve cycle, training the policy online via behavioral cloning on a dynamic archive of elite trajectories-no external data or offline pretraining needed. The pipeline interfaces with the domain solely through the objective evaluator used by local search.
  Evaluated on the Taillard PFSP benchmark (ta51-ta60), the most discriminating block due to half its optima being unknown, LM-GRASP outperforms GPU-GRASP by 28.4 makespan units on average-comparable to the gain from GPU acceleration over sequential execution (27.2 units), though with overlapping standard deviations. This suggests instance-specific, online-trained language models are a promising, practical alternative to hand-engineered constructors, especially for landscapes resistant to classical greedy construction.

</details>


### [371] [Search Strategies for Optimal Classification and Regression Trees](https://arxiv.org/abs/2607.28170)
*Jacobus G. M. van der Linden,Mim van den Bos,Emir Demirović*

Main category: cs.LG

TL;DR: 提出一个通用的最优决策树（ODT）算法框架，统一了18种搜索策略，并实证比较了各策略对分类和回归任务性能与运行时间的影响。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于最优决策树（Optimal Decision Trees）的搜索策略与可扩展性，属于经典机器学习/可解释AI领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于可解释机器学习/最优决策树领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：最优决策树（ODT）的全局优化与搜索策略比较。应用场景：通用机器学习分类与回归任务。传感器类型：不适用。目标类别：不适用。

Model architecture: 提出一个通用的ODT算法框架，可实例化此前提出的多种搜索策略（共18种），并支持定义新的搜索策略。框架为不同搜索策略提供了统一的比较视角。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩方法。虽然ODT本身是紧凑可解释模型，但论文关注的是搜索策略的算法效率（scalability），而非模型推理阶段的轻量化部署。

Onboard deployability: 摘要未说明。论文未提供参数量、FLOPs、延迟、显存、能耗等部署相关信息。虽然ODT模型本身具有紧凑可解释的特性，但论文未讨论其在边缘或星上部署的可行性。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标包括：分类任务的anytime performance（任意时刻性能）、回归任务的runtime（运行时间）。实验比较了18种搜索策略。

Experiments: 在18种搜索策略的实证比较中，最佳策略在分类任务上相比state-of-the-art取得了显著更好的anytime performance，在回归任务上运行时间提升超过一个数量级（an order of magnitude）。

Limitations: 摘要未说明具体局限性。主要缺口包括：未涉及遥感场景、未涉及目标检测任务、未讨论模型部署或推理加速、未提供具体数据集和硬件环境细节。

Ideas for my research: 借鉴意义有限。可能的间接启发包括：(1) 搜索策略的系统化比较框架思路，可用于系统比较不同轻量化方法（如剪枝、量化、蒸馏）在遥感目标检测模型上的效果；(2) ODT的紧凑可解释特性提示可解释性在星上决策中的价值，但与本课题核心方向关联较弱。

Abstract: Optimal decision trees (ODTs) are compact, interpretable machine learning models that globally optimize a given objective, but their scalability remains challenging. While recent work has proposed a variety of search strategies to improve scalability, the precise contribution of each strategy remains unclear. To address this gap, we introduce a general algorithmic framework for ODTs that instantiates previously used search strategies and enables the definition of new ones. This provides a common lens through which to understand and compare different strategies, which we use to empirically investigate the effect of 18 search strategies. Compared to the state of the art, the best strategy in our evaluation achieves significantly better anytime performance for classification, and improves runtime by more than an order of magnitude for regression.

</details>


### [372] [Multi-channel Uplift Policy Learning](https://arxiv.org/abs/2607.28182)
*Changjian Liu,Tianyu Wang,Xiaoxuan Deng,WenTao Zhu,Yuwei Xu,Jungqi Jin,Yong Gao,Chuan Yu,Jian Xu,Bo Zheng*

Main category: cs.LG

TL;DR: 提出ReAlloc，一个用于电商平台多渠道营销预算分配的fast-slow因果框架，通过正交教师网络提取无偏局部梯度并蒸馏给学生网络，实现支持感知的保守预算分配决策。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。该论文聚焦电商营销预算分配（marketing budget allocation）和因果推断（causal inference），属于推荐系统/商业智能领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱或空间在轨智能。虽然论文中使用了"distillation"（蒸馏）这一术语，但其含义是知识蒸馏式的信息传递，并非面向模型压缩的轻量化技术，且应用场景与遥感目标检测完全无关。

Reading priority: 1分。该论文属于电商营销优化领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不推荐阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：电商多渠道营销预算分配（marketing budget allocation）的uplift决策问题。应用场景：电商平台（Taobao）。传感器类型：不适用。目标类别：不适用（目标为营销渠道和预算分配）。

Model architecture: ReAlloc框架包含两个核心组件：1) Orthogonal Teacher（正交教师网络），用于从短期日志中提取无偏局部梯度；2) Explanation-Guided Student（解释引导的学生网络），将教师提取的梯度蒸馏为长期时间跨度上的结构化边际场。整体采用fast-slow因果框架设计，在simplex约束空间中进行决策。

Lightweight method: 摘要中提到的"distill"（蒸馏）是知识蒸馏概念，但其目的是将短期无偏梯度信息传递到长期结构化边际场，用于提升决策质量，而非用于模型压缩、剪枝、量化或加速推理。不涉及任何面向边缘部署或星上部署的轻量化技术。

Onboard deployability: 摘要未说明。论文不涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且其应用场景为电商平台在线服务，与星上/边缘部署无关。

Datasets and metrics: 使用了大规模在线A/B测试（on Taobao platform）以及仿真实验（simulations）。评价指标为pay order（支付订单量）和income（收入）的提升。

Experiments: 在Taobao平台上进行了大规模在线A/B测试，结果表明ReAlloc在pay order和income两个指标上同时获得提升。摘要未提供与基线方法的详细对比数据或消融实验结果。

Limitations: 摘要未说明局限性。但可推断：1) 该方法高度依赖电商场景的短期日志和长期收益数据，难以迁移到遥感领域；2) 因果推断框架的假设（如无混淆、支持感知）在遥感场景中不成立；3) 未提供任何计算效率或部署相关指标。

Ideas for my research: 借鉴意义非常有限。唯一可能的概念性启发是"Orthogonal Teacher + Explanation-Guided Student"的教师-学生蒸馏框架设计思路——在遥感目标检测轻量化中，知识蒸馏（knowledge distillation）是常用压缩手段，可以借鉴其"教师提取关键信息、学生结构化吸收"的框架思想，但具体实现（因果推断、uplift建模）与遥感场景无关。此外，其"fast-slow"双时间尺度设计在遥感在轨场景中可类比为"短期快速推理 + 长期模型更新"的架构思路，但仅为概念层面的弱类比。

Abstract: E-commerce platforms must allocate fixed marketing budgets across multiple channels to maximize business utility. However, standard predict-then-optimize (PTO) paradigms fail in this compositional space due to observational confounding and severe extrapolation. We formulate this challenge as a simplex-constrained uplift decision problem and propose ReAlloc, a fast-slow causal framework. Specifically, an agile Orthogonal Teacher extracts unbiased local gradients from short-term logs, while an Explanation-Guided Student distills them into a structured marginal field over long-term horizons. This design enables support-aware, conservative decisions that capture cross-channel substitutions. Extensive simulations and large-scale online A/B tests on Taobao platform demonstrate that ReAlloc achieves simultaneous lifts in both pay order and income.

</details>


### [373] [Persistent Gaussian Perturbations Prevent Oversmoothing in Recurrent Graph Neural Networks](https://arxiv.org/abs/2607.28185)
*Mostafa Haghir Chehreghani*

Main category: cs.LG

TL;DR: 本文从随机动力系统角度研究循环图神经网络（recurrent GNN）中注入高斯噪声以对抗过平滑（oversmoothing）现象，理论上证明了稳态表示不会坍缩到常数流形，Dirichlet energy 存在正下界。

Research relevance: 弱相关（1-2分）。论文主题是图神经网络（GNN）的过平滑问题，属于图表示学习/图深度学习理论方向，与遥感、卫星、目标检测、轻量化部署均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或加速方法。唯一可能的间接联系是GNN在遥感场景理解（如场景图、关系建模）中偶有应用，但本文是纯理论分析，不涉及任何视觉或遥感任务。

Reading priority: 1分。论文主题为GNN过平滑的理论分析，与遥感卫星在轨目标检测及模型轻量化方向基本无关，无直接借鉴价值，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图神经网络理论分析（过平滑问题）。应用场景：通用图学习，非遥感/卫星场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 循环图神经网络（recurrent GNN），在每次传播步骤后注入独立高斯噪声，将网络建模为随机动力系统。基于确定性更新的全局收缩假设，分析隐藏表示的马尔可夫链性质。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS等任何轻量化或模型压缩方法。噪声注入是作为对抗过平滑的正则化/随机机制，而非模型压缩手段。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、延迟、显存、能耗或实时性等任何部署相关信息。本文为理论分析论文，不涉及星上或边缘部署。

Datasets and metrics: 摘要未说明具体数据集。实验仅在"linear and nonlinear recurrent graph neural networks"上进行数值验证，用于匹配理论预测（stationary distribution 的涌现、Dirichlet energy 对噪声强度的依赖），未使用遥感或视觉基准数据集。

Experiments: 数值实验验证了理论结果：在线性和非线性循环GNN上，观测到平稳分布的出现，以及极限Dirichlet energy随噪声强度的变化与理论预测一致。无与现有目标检测或轻量化方法的对比实验。

Limitations: 1. 纯理论分析，缺乏实际任务（如图分类、节点分类）上的下游性能验证；2. 全局收缩假设在真实GNN中未必成立，理论适用性受限；3. 未涉及任何视觉/遥感任务，无法直接评估其实际应用价值；4. 噪声注入可能引入额外计算开销或影响收敛速度，摘要未讨论；5. 与遥感在轨目标检测及轻量化方向基本无交集。

Ideas for my research: 1. 若课题涉及遥感场景图理解或目标关系建模（如GNN用于场景上下文推理），本文的噪声注入抗过平滑思想可作为深层GNN设计的参考；2. 噪声注入作为一种随机正则化手段，理论上可借鉴到轻量化检测模型的训练策略中（如增强小模型泛化能力），但需谨慎评估其计算开销；3. 总体而言，本文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限，仅可作为GNN相关子任务的背景阅读。

Abstract: Oversmoothing is a fundamental limitation of deep graph neural networks (GNNs), where repeated message passing causes node representations to become increasingly similar, eventually collapsing toward a low-dimensional subspace. This phenomenon limits the effective depth of message-passing architectures and motivates the search for mechanisms that preserve representation diversity. In this paper, we study a recurrent graph neural network in which independent Gaussian noise is injected after every propagation step and analyze the resulting architecture as a stochastic dynamical system. Under a standard global contraction assumption on the deterministic update, we prove that the hidden representations form a geometrically ergodic Markov chain admitting a unique invariant probability measure. Our main theoretical result establishes an explicit positive lower bound on the expected stationary Dirichlet energy, proportional to both the noise variance and the spectral gap of the underlying graph. Consequently, the stationary representations cannot collapse onto the constant manifold, providing a rigorous guarantee that asymptotic oversmoothing is prevented in the sense of non-vanishing Dirichlet energy. Our analysis reveals persistent stochastic perturbations as a fundamentally different mechanism for combating oversmoothing, complementing existing deterministic approaches based on residual connections, normalization, and graph rewiring. Finally, numerical experiments on both linear and nonlinear recurrent graph neural networks closely match the theoretical predictions, illustrating the emergence of a stationary distribution and the predicted dependence of the limiting Dirichlet energy on the noise intensity.

</details>


### [374] [TopoFormer: Topology Meets Attention for Graph Learning](https://arxiv.org/abs/2607.28259)
*Md Joshem Uddin,Astrit Tola,Cuneyt Gurcan Akcora,Baris Coskunuzer*

Main category: cs.LG

TL;DR: 提出Topoformer，一个轻量级可扩展的图表示学习框架，通过Topo-Scan模块将图分解为拓扑token序列，用Transformer处理生成图级嵌入，在保持拓扑归纳偏置的同时实现并行化和高效计算。

Research relevance: 弱相关（2分）。该论文是泛机器学习/图表示学习方向的工作，与遥感、卫星、目标检测均无直接关联。虽然标题和摘要中出现"lightweight"一词，但这里的"轻量"指的是图表示学习框架的计算效率，而非模型压缩/轻量化部署（pruning, quantization, distillation等）。与"遥感卫星在轨目标检测模型轻量化"课题基本无关，仅在"轻量高效计算"这一宽泛概念上有间接借鉴意义。

Reading priority: 1分。该论文属于图表示学习/拓扑数据分析方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关。仅在"轻量高效计算"的宽泛概念上有间接启发，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图表示学习（graph representation learning）、图分类（graph classification）、分子性质预测（molecular property prediction）。应用场景为通用图数据，非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: Topoformer由两个核心组件构成：(1) Topo-Scan模块——通过节点或边过滤（node/edge filtrations）将图分解为有序的拓扑token序列，捕获从局部motif到全局组织的多尺度结构模式；(2) Transformer编码器——处理拓扑token序列生成图级嵌入（graph-level embeddings）。与传统persistent homology管线不同，Topo-Scan可并行化，避免昂贵的diagram计算，可无缝集成到标准深度学习架构中。

Lightweight method: 摘要中"lightweight"指框架的计算效率（efficient compute）和可扩展性（scalable），而非传统意义上的模型压缩方法。未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS等轻量化技术。其轻量性体现在：避免传统persistent homology的昂贵diagram计算，通过并行化提升效率。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗等部署相关指标。虽然声称"predictable and efficient compute"，但未给出具体数值，也未讨论边缘部署或星上部署可行性。

Datasets and metrics: 摘要提及图分类（graph classification）和分子性质预测（molecular property prediction）基准数据集，但未给出具体数据集名称（如TU datasets, OGB, MoleculeNet等）。评价指标未明确说明，仅提及"state-of-the-art performance"和与GNN及topology-based baselines的对比。

Experiments: 摘要显示Topoformer在图分类和分子性质预测基准上达到或超过强GNN基线和topology-based基线，并提供了拓扑编码稳定性的理论保证（theoretical guarantees on stability）。但未给出具体实验数值、消融实验细节或对比表格。

Limitations: 1. 与遥感/卫星/目标检测完全无关，属于纯图表示学习领域；2. 摘要未提供具体计算开销数据（FLOPs、参数量、延迟等），"lightweight"和"efficient"缺乏量化支撑；3. 未讨论在图像/遥感数据上的适用性；4. 未涉及目标检测任务；5. 摘要未说明在超大图上的可扩展性边界。

Ideas for my research: 1. 拓扑token序列的思想可借鉴：若未来在遥感场景中处理图结构数据（如场景图、目标关系图），Topo-Scan的多尺度拓扑编码思路可能有参考价值；2. "将结构信息编码为attention-friendly序列"的设计理念，对设计轻量Transformer变体有一定启发；3. 但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限，不建议作为核心参考。

Abstract: We introduce Topoformer, a lightweight and scalable framework for graph representation learning that encodes topological structure into attention-friendly sequences. At the core of our method is Topo-Scan, a novel module that decomposes a graph into a short, ordered sequence of topological tokens by slicing over node or edge filtrations. These sequences capture multi-scale structural patterns, from local motifs to global organization, and are processed by a Transformer to produce expressive graph-level embeddings. Unlike traditional persistent homology pipelines, Topo-Scan is parallelizable, avoids costly diagram computations, and integrates seamlessly with standard deep learning architectures. We provide theoretical guarantees on the stability of our topological encodings and demonstrate state-of-the-art performance across graph classification and molecular property prediction benchmarks. Our results show that Topoformer matches or exceeds strong GNN and topology-based baselines while offering predictable and efficient compute. This work opens a new path for parallelizable and unifying approaches to graph representation learning that integrate topological inductive biases into attention frameworks.

</details>


### [375] [HARGO: Heterogeneity-Aware Reward-Guided Optimization for RL Post-Training of LLMs on HPC Tasks](https://arxiv.org/abs/2607.28301)
*Tiangang Li,Xiangbo Tian*

Main category: cs.LG

TL;DR: 提出HARGO（Heterogeneity-Aware Reward-Guided Optimization），一种面向异构高性能计算（HPC）任务的奖励引导优化方法，通过置信度调制的优势加权来改进LLM的RL后训练对齐质量。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于大语言模型（LLM）在高性能计算（HPC）任务上的强化学习后训练对齐，属于NLP/LLM领域，不涉及遥感、卫星、目标检测或模型轻量化/部署。相关性评分：1分。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于LLM强化学习对齐领域，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM监督微调后的强化学习（RL）后训练对齐；应用场景：高性能计算（HPC）任务，包括data race detection（数据竞争检测）、benchmark question answering（基准问答）、语义生成等；传感器类型：不适用；目标类别：不适用。

Model architecture: 基于大语言模型（LLM）的RL后训练框架，核心创新是HARGO的奖励引导优化机制，包括：group-level reward contrast（组级奖励对比）计算判别信号、reference model log-probabilities（参考模型对数概率）计算置信度信号，并将两者调制到advantage上生成per-response importance weighting（逐响应重要性权重）。不涉及视觉/检测网络结构。

Lightweight method: 摘要未说明。论文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法，也不涉及边缘/星上部署优化。

Onboard deployability: 摘要未说明。论文不涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息，也不涉及星上或边缘部署场景。

Datasets and metrics: 数据集：C/C++ data race样本、MLPerf benchmark问答等HPC任务数据；评价指标：WinRate 54.62%、Data Race F1 91.30%、PLP Similarity 0.8558；对比方法：GRPO等9种方法。

Experiments: 在四个HPC任务和九种方法上进行对比实验，HARGO在三个主要指标（WinRate、Data Race F1、PLP Similarity）上均取得最优结果。消融实验确认了判别信号和置信度信号两者的互补贡献。

Limitations: 与本研究方向完全无关。摘要未涉及遥感图像、目标检测、模型压缩或部署相关内容。该论文属于LLM对齐优化领域，对遥感在轨目标检测轻量化课题无可借鉴的视觉方法或部署技术。

Ideas for my research: 虽然论文主题与遥感在轨目标检测无关，但其"heterogeneity-aware"（异构感知）的思想在方法论层面有一定启发：遥感目标检测中不同目标类别（舰船、飞机、车辆）的尺度、方向、背景复杂度差异极大，类似HPC任务的异构性，若未来在检测头的损失函数或训练策略中引入类似的自适应加权机制（根据样本难度或类别差异调制梯度），可能有助于提升多类别遥感检测的鲁棒性。但这一借鉴非常间接，且不涉及轻量化。

Abstract: Supervised fine-tuning (SFT) can equip large language models (LLMs) with domain knowledge for high-performance computing (HPC) tasks such as data race detection and benchmark question answering. However, knowledge alone does not guarantee task-appropriate behavior: the same SFT model that correctly classifies 88.65\% of C/C++ data race samples produces verbose, imprecise answers to factual queries, with 65.9\% of MLPerf responses exceeding 40 characters. Reinforcement learning (RL) post-training addresses this gap by optimizing for task-specific rewards rather than token-level imitation. Yet HPC tasks exhibit extreme heterogeneity, with binary classification, factual QA, and semantic generation differing by 58x in answer length, spanning three distinct reward distributions, and showing widely varying SFT accuracy. This makes uniform-weight RL methods such as GRPO suboptimal. We propose HARGO, Heterogeneity-Aware Reward-Guided Optimization, which introduces per-response importance weighting via confidence-modulated advantage: computing a discrimination signal from group-level reward contrast and a confidence signal from reference model log-probabilities, then modulating the advantage before computing per-response weights, without requiring task-type labels. Across four HPC tasks and nine methods, HARGO achieves the best performance on all three primary metrics: WinRate 54.62\%, Data Race F1 91.30\%, and PLP Similarity 0.8558. Ablation confirms complementary contributions from both signals. HARGO establishes the best overall alignment quality among compared methods for heterogeneous HPC tasks.

</details>


### [376] [Semi-Supervised Learning for Molecular Graphs via Ensemble Consensus](https://arxiv.org/abs/2607.28304)
*Rasmus Tirsgaard,Laurits Fredsgaard,Marisa Wodrich,Mikkel Jordahn,Mikkel N. Schmidt*

Main category: cs.LG

TL;DR: 提出基于集成共识（ensemble consensus）的半监督学习方法，用于分子科学中的图神经网络属性预测，发现该方法具有类似知识蒸馏的效果，单个集成成员即可超越传统监督训练的完整集成。

Research relevance: 与本研究方向弱相关（2分）。论文属于分子科学/图神经网络领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务。虽然提及了knowledge distillation这一轻量化相关技术概念，但仅作为类比说明，并非用于模型压缩或部署，且应用场景与遥感在轨目标检测完全无关。

Reading priority: 1分。论文属于分子科学/图神经网络半监督学习领域，与遥感卫星在轨目标检测及轻量化方向基本无关。仅"ensemble consensus类似knowledge distillation"这一概念在方法论层面有极间接的启发，但整体阅读优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：分子属性预测（molecular property prediction）、分子模拟。应用场景：分子科学、材料发现。传感器类型：不适用（非遥感传感器）。目标类别：分子/材料，非遥感目标。

Model architecture: 基于图神经网络（Graph Neural Networks, GNN）的架构，具体结构摘要未说明。采用集成（ensemble）训练策略，多个模型成员通过ensemble consensus目标进行半监督训练。

Lightweight method: 摘要提及训练过程表现出与knowledge distillation类似的效果——单个集成成员在ensemble consensus目标下训练后，性能优于传统监督方式训练的完整集成。但这并非主动的模型压缩/蒸馏方法，而是半监督训练带来的副产品。不涉及pruning、quantization、NAS、实时推理或边缘部署。

Onboard deployability: 摘要未说明。不涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息，且应用场景（分子科学）与星上部署无关。

Datasets and metrics: 摘要提到"diverse range of molecular datasets"，但未列出具体数据集名称。评价指标包括预测准确率（predictive accuracy）和校准误差（calibration error）。具体数据集和指标细节摘要未说明。

Experiments: 实验覆盖多种分子数据集、任务类型和图神经网络架构。主要发现：(1) ensemble consensus半监督训练提升预测精度；(2) 该训练方式增强模型鲁棒性；(3) 单个集成成员优于传统监督训练的完整集成；(4) 降低校准误差。具体对比方法和消融实验细节摘要未说明。

Limitations: 摘要未说明局限性。但就本研究方向而言，该论文与遥感、卫星、目标检测、轻量化部署均无直接关联，属于分子科学领域的半监督学习方法研究，借鉴价值有限。

Ideas for my research: 尽管场景不同，但有两个概念可间接借鉴：(1) "ensemble consensus"作为一种半监督训练策略，其"类似knowledge distillation"的效果提示我们：在遥感目标检测模型轻量化中，可以利用teacher-student蒸馏时引入多模型集成共识作为soft target，可能提升蒸馏后小模型的精度和鲁棒性；(2) 该工作表明单模型通过特定训练目标可逼近甚至超越集成性能，这启发我们在遥感在轨检测中，可通过精心设计的训练目标（如consistency regularization）让轻量模型获得接近大模型的性能，减少对重型集成的依赖。但需注意这些只是方法论层面的间接启发，需在遥感目标检测场景中重新验证。

Abstract: Machine learning is transforming molecular sciences by accelerating property prediction, simulation, and the discovery of new molecules and materials. Acquiring labeled data in these domains is often costly and time-consuming, whereas large collections of unlabeled molecular data are readily available. Standard semi-supervised learning methods often rely on label-preserving augmentations, which are challenging to design in the molecular domain, where minor changes can drastically alter properties. In this work, we show that semi-supervised methods that rely on an ensemble consensus can boost predictive accuracy across a diverse range of molecular datasets, task types, and graph neural network architectures. We find that training with an ensemble consensus objective increases robustness in models and exhibits an effect similar to knowledge distillation; an individual member of an ensemble trained this way outperforms a full ensemble trained in a traditional supervised fashion in almost all cases. In addition, this type of semi-supervised training reduces calibration error.

</details>


### [377] [Beyond Geometric Complementarity: Coherent Overlap in Sparse Mixture-of-Experts Routing](https://arxiv.org/abs/2607.28308)
*Huiyuan Tian,Bonan Xu,Shijian Li*

Main category: cs.LG

TL;DR: 本文研究稀疏混合专家（MoE）语言模型中专家子空间几何重叠与路由选择的关系，提出Expert Subspace Separation Index（ESSI）等指标来区分路由一致性、候选质量和候选-上下文交互，揭示"coherent overlap"现象。

Research relevance: 与本研究方向基本无关（1分）。论文研究对象是MoE语言模型（如OLMoE、Mixtral、DeepSeek）的专家路由机制与表示几何，完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据，也不涉及目标检测任务，更不涉及轻量化部署（剪枝/量化/蒸馏/NAS/边缘部署等）。虽然文中提到"pruning value"（剪枝价值），但这是针对MoE专家剪枝的理论分析，与遥感在轨目标检测模型轻量化无直接关联。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于NLP/MoE理论分析。仅当对"基于功能贡献而非几何相似性进行剪枝评估"的方法论感兴趣时，可作为间接参考，但优先级很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：MoE语言模型的专家路由机制分析与几何解释。应用场景：大语言模型（LLM）的稀疏混合专家架构。传感器类型：不适用（非遥感）。目标类别：不适用（非目标检测）。

Model architecture: 研究对象为稀疏MoE语言模型架构，涉及六个MoE架构、OLMoE、Mixtral、DeepSeek等模型。核心分析工具包括：Expert Subspace Separation Index（ESSI）、matched-route residuals、prefix-controlled 2×2 factorial设计、frozen-route interventions、controlled Top-k study。

Lightweight method: 本文并非轻量化方法论文。它讨论的是MoE专家剪枝（pruning）的理论基础——即"几何相似性不能单独决定冗余性或剪枝价值"，属于对模型压缩（专家剪枝）可行性的理论分析，但未提出具体剪枝/量化/蒸馏/NAS方法，也未涉及边缘或星上部署。

Onboard deployability: 摘要未说明。本文不涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署指标，也不涉及FPGA/NPU/TensorRT等硬件加速。

Datasets and metrics: 数据集：摘要未说明具体数据集。评价指标：Expert Subspace Separation Index（ESSI）、matched-route residuals、next-token prediction（下一词预测）。实验设置：六个MoE架构、39个factorial cells（OLMoE、Mixtral、DeepSeek）、frozen-route comparisons（39组）、controlled training study（3个seeds，Top-2 vs Top-1）。

Experiments: 主要发现：1）六个MoE架构中专家子空间重叠显著，但实际路由比匹配替代方案更好地解释token表示；2）在39个factorial cells中，被选中的候选专家比最强的未选中对手解释更多残差表示，但前缀缩小了这一优势（所有交互为负，95%置信区间低于零）；3）几何收窄不意味着功能冗余——39组frozen-route比较中24组显示添加后续专家改善next-token prediction，其余15组不确定；受控训练研究在3个seeds中均偏好Top-2而非Top-1。作者将此模式称为"coherent overlap"（相干重叠）。

Limitations: 1）研究对象为语言模型MoE，与遥感目标检测场景完全无关；2）摘要未说明具体数据集、计算开销或部署指标；3）15/39组frozen-route比较结果不确定，结论存在一定模糊性；4）未提供任何遥感图像、目标检测或边缘部署相关的实验验证。

Ideas for my research: 尽管本文与遥感在轨目标检测无直接关系，但其关于"几何相似性不能单独决定冗余性或剪枝价值"的核心观点对轻量化研究有间接启发：1）在做模型剪枝或结构稀疏化时，不能仅依据表示/权重相似性判断冗余，需结合功能贡献（如对最终检测精度的实际影响）来评估；2）"coherent overlap"概念提示：即使子空间重叠，保留多个专家/分支仍可能有功能价值，这对设计轻量检测模型中的多分支结构有参考意义；3）其"matched-route residuals"和"frozen-route intervention"的实验方法论可迁移到检测模型轻量化中，用于评估被剪枝通道/分支的真实功能贡献。但这些均为方法论层面的间接借鉴，需自行迁移到遥感目标检测场景。

Abstract: Sparse mixture-of-experts (MoE) language models route each token to multiple experts, suggesting a geometric account of their benefit: co-selected experts should contribute distinct representation directions. Existing evidence often conflates route coherence, candidate quality, and candidate-by-context interaction. We distinguish these quantities using an Expert Subspace Separation Index (ESSI), matched-route residuals, and a prefix-controlled $2\times2$ factorial; frozen-route interventions and a controlled Top-$k$ study assess functional value. Three paired contrasts organize the findings. First, across six MoE architectures, expert subspaces overlap substantially, yet actual routes explain token representations better than matched alternatives. Second, across the 39 factorial cells in OLMoE, Mixtral, and DeepSeek, the selected candidate explains more of the residual representation than the strongest unselected rival in every cell, yet the actual prefix narrows this advantage throughout: all interactions are negative, and every 95% confidence interval lies below zero. Third, this geometric narrowing does not imply functional redundancy: adding later experts improves next-token prediction in 24 of 39 frozen-route comparisons, while the other 15 estimates are inconclusive; a controlled training study also favors Top-2 over Top-1 in all three seeds. We call this joint pattern coherent overlap: routing selects token-relevant experts from a shared geometric neighborhood, while useful multi-expert computation persists without disjoint linear coverage. Separating these quantities clarifies why geometric similarity alone cannot determine redundancy or pruning value.

</details>


### [378] [Measuring Distortion in the Empty Regions of Dimensionality Reduction Scatterplots with the Gap Index](https://arxiv.org/abs/2607.28324)
*Jaume Ros,Alessio Arleo,Fernando Paulovich*

Main category: cs.LG

TL;DR: 提出 Gap Index (GI)，一种用于衡量高维数据降维投影在空白区域视觉畸变的质量指标，通过将空间分解为空三角形并比较其高维对应物来计算形变。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文属于可视化/降维投影质量评估领域，不涉及遥感、卫星、目标检测或模型压缩。相关性评分：1分。

Reading priority: 1分。论文属于数据可视化/降维投影质量评估领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不建议深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：高维数据降维投影的质量评估（visualization quality metric）。应用场景：数据可视化、降维投影分析。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体网络结构。核心方法为将2D投影空间分解为"空三角形"（empty triangles），计算每个三角形相对其高维对应物的形变（deformation），聚合为标量值或叠加在投影上可视化区域畸变模式。

Lightweight method: 不涉及轻量化方法。摘要仅提到GI指标"计算快速"（fast to compute），但未涉及剪枝、量化、知识蒸馏、NAS等任何模型压缩或加速技术。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。仅提及GI"计算快速且可解释"，但未给出具体性能数据。

Datasets and metrics: 摘要未说明具体数据集。评价指标为GI（Gap Index），用于衡量投影在空白区域的视觉畸变，与常用的距离/邻域保持类质量指标（如trustworthiness, continuity）进行对比。

Experiments: 摘要未说明具体实验设置。主要结果表明：与主流质量指标相比，GI对具有高视觉影响的小结构形变敏感，且计算快速、可解释。

Limitations: 摘要未说明局限性。从摘要看，该方法仅针对2D投影的空白区域畸变，未涉及目标检测、遥感图像处理或模型压缩。与本研究方向的关联性极弱。

Ideas for my research: 借鉴意义非常有限。可能的间接启发：GI将空间分解为三角形并逐区域评估形变的思路，在概念上类似于将特征图或检测结果进行空间分解评估的思路，但与本课题（在轨遥感目标检测模型轻量化）几乎无直接可借鉴点。

Abstract: Quality metrics play a crucial role in the proper use of dimensionality reduction projections for visual analysis of high-dimensional data. They quantify the degree of distortion of a projection compared to the high-dimensional data and provide a reliable indication of how confident users can be in the structures they see in the resulting layouts. However, most popular metrics focus on capturing direct relationships between points (e.g., distances or neighborhoods) while neglecting distortions in empty areas of the layout, even though these often compose visually relevant features of a 2D layout. In this paper, we introduce the Gap Index (GI), a quality metric for 2D projections that captures visual distortion by measuring spatial distortion in empty areas of a projection. It does so by decomposing the space into empty triangles, which are then compared to their high-dimensional counterparts to compute the deformation. This per-triangle deformation can be aggregated into a single scalar value or overlaid on a projection to visualize regional distortion patterns. Results show that, contrary to popular quality metrics, the GI is sensitive to small structural deformations that have high visual impact. It is also fast to compute and interpretable.

</details>


### [379] [LEDGERMIND: Provenance-Constrained Multimodal Agentic Reasoning with a Structured Evidence Ledger](https://arxiv.org/abs/2607.28374)
*Enjun Du,Hange Zhou,Chenxu Du,Siyi Liu,Zirong Chen,Ziyu Zheng,Yongqi Zhang*

Main category: cs.LG

TL;DR: 提出LedgerMind，一种基于结构化证据账本（Structured Evidence Ledger）的多模态智能体推理框架，通过来源约束状态机、三层接地协议和自适应双路径调度器来提升多模态问答轨迹的忠实度与准确性。

Research relevance: 弱相关（2分）。该论文属于多模态视觉问答（VQA）与智能体推理方向，与遥感、卫星、在轨部署、目标检测、模型轻量化均无直接关联。既非遥感场景，也非目标检测任务，更不涉及任何模型压缩或部署优化方法。仅有的弱关联在于其涉及多模态感知（multimodal perception）和视觉理解，与遥感图像理解在广义视觉层面有间接交集，但方法论上完全不属于目标检测或轻量化范畴。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于多模态智能体推理与VQA评估领域。除非对"自适应计算分配"或"证据可追溯"有跨领域兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：多模态视觉问答（Visual Question Answering, VQA）中的智能体轨迹推理与评估。应用场景：通用多模态推理benchmark，非遥感场景。传感器类型：摘要未说明（推测为自然图像）。目标类别：摘要未说明（非目标检测任务）。

Model architecture: LedgerMind框架包含：1) Structured Evidence Ledger（结构化证据账本）作为轨迹状态；2) Three-Layer Grounding Protocol（三层接地协议）；3) Adaptive Dual-Path Dispatcher（自适应双路径调度器）；4) Event-Triggered Verification-and-Repair engine（事件触发验证修复引擎）。基于backbone MLLM（多模态大语言模型）构建。

Lightweight method: 摘要未涉及任何轻量化方法。不涉及pruning、quantization、knowledge distillation、NAS、模型压缩或加速技术。

Onboard deployability: 摘要未说明。未提及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该方法基于MLLM backbone，计算开销通常较大，与星上/边缘部署的轻量化需求方向相悖。

Datasets and metrics: 摘要提及"multiple multimodal reasoning benchmarks"和"backbone MLLMs"，但未列出具体数据集名称。评价指标包括answer accuracy（答案准确率）和trajectory-level faithfulness（轨迹级忠实度）。具体benchmark名称和指标数值摘要未说明。

Experiments: 摘要仅说明在多个多模态推理benchmark和多种backbone MLLM上，LedgerMind同时提升了answer accuracy和trajectory-level faithfulness。未给出具体数值、对比方法列表或消融实验细节。

Limitations: 1) 与遥感卫星在轨目标检测及轻量化方向基本无关；2) 摘要未提供具体实验数据、benchmark名称和量化结果；3) 基于MLLM的推理框架计算开销大，不适用于资源受限的星上部署场景；4) 未讨论任何模型压缩或推理加速策略。

Ideas for my research: 借鉴意义有限，但可提取以下间接思路：1) "provenance-constrained"（来源约束）思想可类比于遥感目标检测中确保检测结果的可追溯性和可解释性，例如在轻量化检测模型中保留关键证据链；2) "Adaptive Dual-Path Dispatcher"（自适应双路径调度）思想可迁移到遥感场景——根据图像复杂度或目标密度动态选择轻量/重量的检测分支，实现计算资源的自适应分配，这与在轨场景中"简单场景用轻量模型、复杂场景用高精度模型"的动态推理策略有相通之处；3) "Event-Triggered Verification-and-Repair"可类比于检测后处理中的置信度校验与修正机制。但这些均为间接类比，需自行重构为检测框架。

Abstract: Multimodal agents for visual question answering increasingly operate as multi-step trajectories that interleave perception, retrieval, and reasoning, yet evaluation still largely reduces to final-answer accuracy. This aggregate signal cannot tell whether a correct answer was reached through grounded evidence, language priors, or accidental error cancellation. We propose to treat a multimodal agent trajectory as a provenance-constrained state machine: tool outputs are normalized into a Structured Evidence Ledger that serves as the trajectory state, downstream reasoning and decision claims may cite only active ledger entries, grounding is checked at the entity and numeric level, and repair is realized as typed state transitions that cannot introduce content without tool-produced provenance. We instantiate this design as LedgerMind (Provenance-Constrained Multimodal Agentic Reasoning with a Structured Evidence Ledger), augmented by a Three-Layer Grounding Protocol, an Adaptive Dual-Path Dispatcher that matches reasoning depth to question complexity, and an Event-Triggered Verification-and-Repair engine with a formal provenance non-amplification guarantee. We use LedgerMind to target four recurring failure patterns that final-answer accuracy tends to obscure: unsupported intermediate reasoning, citation-backed entity hallucination (Phantom Grounding), over-reasoning on simple queries, and repair-time amplification. Experiments across multiple multimodal reasoning benchmarks and backbone MLLMs show that LedgerMind improves both answer accuracy and trajectory-level faithfulness.

</details>


### [380] [Hierarchical Multilevel Monte Carlo for Order-Optimal Neural Actor-Critic in Average-Reward CMDPs](https://arxiv.org/abs/2607.28390)
*Ankur Naskar,Vaneet Aggarwal*

Main category: cs.LG

TL;DR: 本文提出一种基于分层Multilevel Monte Carlo（MLMC）神经critic的原始-对偶Natural Actor-Critic算法，首次为无限时域平均奖励CMDP（Constrained Markov Decision Process）在一般策略参数化和神经critic下建立了order-optimal收敛保证。

Research relevance: 弱相关（2分）。本文属于强化学习理论（Constrained Markov Decision Process, primal-dual actor-critic, Neural Tangent Kernel），是泛机器学习/强化学习理论方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。仅从"模型压缩/高效估计"角度（MLMC debiasing降低计算成本）可能对轻量化有间接启发，但并非面向遥感场景。

Reading priority: 1分。本文是强化学习理论论文，与遥感卫星在轨目标检测及模型轻量化方向基本无关。除非研究涉及"带约束的强化学习用于星上决策"，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：强化学习中的安全约束优化（CMDP）收敛性理论分析。应用场景：safety-critical reinforcement learning（安全关键强化学习），非遥感场景。传感器类型：不适用。目标类别：不适用。

Model architecture: 提出分层Multilevel Monte Carlo（MLMC）神经critic，用于在trajectory sampling和critic optimization两个维度同时进行debiasing（去偏）。整体框架为primal-dual Natural Actor-Critic算法，采用neural critic（神经critic）和general policy parameterization（一般策略参数化）。

Lightweight method: 摘要未明确涉及传统轻量化方法（pruning/quantization/distillation/NAS）。但MLMC（Multilevel Monte Carlo）方法通过分层采样实现"以对数期望样本成本获得长critic优化运行的偏差"，本质上是一种计算效率/样本效率优化手段，可视为对critic估计计算成本的降低，与轻量化思想有间接关联。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗、实时性等部署指标。本文是理论收敛性分析论文，不涉及实际部署。

Datasets and metrics: 摘要未说明。本文为纯理论分析，未提及任何数据集或实验指标。理论指标为optimality gap和constraint violation，均达到$\tilde{O}(T^{-1/2})$阶。

Experiments: 摘要未说明。本文主要贡献为理论收敛性证明（order-optimal convergence guarantees），未提及实验对比或消融实验。

Limitations: 1. 纯理论论文，无实验验证，实际工程可行性未知；2. 基于NTK（Neural Tangent Kernel）分析，其假设在真实深度网络训练中往往不严格成立；3. 与遥感、目标检测、轻量化部署完全无关；4. 摘要未说明MLMC方法在有限样本/有限算力下的实际收益。

Ideas for my research: 1. MLMC（Multilevel Monte Carlo）分层去偏思想可借鉴：在轻量化模型训练或知识蒸馏中，可通过多层级采样/多分辨率特征来降低训练成本，类似地实现"以更少计算获得相近精度"；2. 原始-对偶（primal-dual）框架在带约束的轻量化优化（如FLOPs/延迟约束下的模型压缩）中可能有借鉴意义，可将部署约束建模为CMDP中的约束条件；3. 但整体而言，本文与遥感在轨目标检测轻量化方向关联度很低，仅作为理论方法储备参考。

Abstract: Constrained Markov Decision Processes (CMDPs) provide a natural framework for reinforcement learning in safety-critical applications, where agents maximize long-term reward while satisfying long-term constraints. Although primal-dual actor-critic methods with linear critics are well understood, extending order-optimal convergence guarantees to neural critics in average-reward CMDPs has remained open. The main challenge is a fundamental bias-cost trade-off in neural critic estimation: under Neural Tangent Kernel (NTK) analysis, reducing critic bias substantially increases critic optimization cost, preventing order-optimal convergence in the primal-dual framework. We resolve this bottleneck by introducing a hierarchical Multilevel Monte Carlo (MLMC) neural critic that performs debiasing simultaneously across trajectory sampling and critic optimization. The resulting estimator attains the bias of a long critic optimization run with only logarithmic expected sample cost. Building on this estimator, we develop a primal-dual Natural Actor-Critic algorithm that achieves both an optimality gap and a constraint violation of order $\tilde{O}(T^{-1/2})$. This establishes the first order-optimal convergence guarantees for infinite-horizon average-reward CMDPs with general policy parameterization and neural critics, while eliminating the need to know the underlying mixing time. Our results are novel even in the unconstrained setting.

</details>


### [381] [Why Are GUI Agents Correct but Late? Decode on the Decision-Time Critical Path, Tested with Pre-Compiled Policy Trees](https://arxiv.org/abs/2607.28399)
*Zihan Dong,Rui Qian,Qishi Zhan,Dongshen Peng,Kaixin Li,Yu Li*

Main category: cs.LG

TL;DR: 提出Adaptive Anticipatory Policy Trees (AAPT)，通过在空闲屏幕期预构建带可观察守卫和分支期限的条件策略树，消除多模态GUI智能体在决策关键路径上的自回归解码延迟，将事件窗口内的成功率从0.50提升到0.79。

Research relevance: 弱相关（2分）。该论文属于泛视觉/多模态GUI智能体领域，核心是解决GUI事件决策延迟问题，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能均无直接关系。不涉及遥感目标检测，也不涉及模型轻量化（剪枝/量化/蒸馏/NAS等）。唯一可借鉴的是其"预计算+轻量观察器"的延迟规避思想，与在轨推理中减少决策时延的思路有间接类比价值。

Reading priority: 2分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于GUI智能体领域。虽然"预计算+轻量观察器"的延迟规避思想有一定类比价值，但缺乏遥感场景、目标检测和模型压缩的任何直接关联，阅读优先级低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：计算机使用智能体（computer-use agent）的GUI事件响应决策。应用场景：桌面/网页GUI交互中的瞬态事件处理。传感器类型：屏幕截图（GUI帧）。目标类别：GUI窗口、按钮、菜单等交互元素。与遥感目标检测无关。

Model architecture: AAPT框架包含：1) 冻结的多模态模型（frozen multimodal model）用于在空闲期构建有界条件策略树（bounded conditional policy tree）；2) 轻量观察器（lightweight observer）用于匹配change-gated frames并执行预授权动作。策略树包含observable guards、pre-authorized actions、branch-specific deadlines，树的大小按模型自身解码延迟（decoding latency）来设定。整体不修改底层模型。

Lightweight method: 摘要未说明传统模型压缩方法（无剪枝、量化、知识蒸馏、NAS）。但包含轻量化思想：通过"预计算策略树 + 轻量观察器"将昂贵的自回归解码从决策关键路径上移除，用轻量匹配替代重解码，属于延迟规避/计算卸载策略，而非模型压缩。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。核心关注点是决策时延（decision-time latency），通过消除关键路径上的自回归解码来降低响应延迟。该思想可类比于在轨场景中"预计算+轻量匹配"的推理加速思路，但未提供任何硬件部署（FPGA/NPU/TensorRT）相关信息。

Datasets and metrics: 实验采用paired trials with pre-registered endpoints，使用exact McNemar tests进行统计检验。主要指标为事件窗口内的成功率（success rate）。在contested decision window中成功率从0.50提升到0.79（p=1.8×10⁻³）。另在独立通用多模态模型上进行了126组配对试验（p=4.9×10⁻¹³），并在外部基准（external benchmark）上对比了reactive baseline。摘要未说明具体数据集名称。

Experiments: 主要结果：1) AAPT在contested decision window内成功率从0.50提升到0.79，且无错误动作；2) open-loop和predict-and-replan基线因仍在执行期解码而成功率为零；3) preparation-time sweep验证了基于延迟的树大小设定规则的有效性；4) 消融实验揭示三个关键需求：fast observer decoding、valid tree planning、accurate branch routing；5) pre-registered oracle probe推翻了初始假设，指出branch routing是因果瓶颈；6) 在外部基准上与reactive baseline整体性能相当，但两者优势互补。

Limitations: 1) 与遥感/卫星/目标检测完全无关，属于GUI智能体领域；2) 无任何模型压缩或轻量化方法（剪枝/量化/蒸馏/NAS）；3) 无部署硬件指标（FLOPs、延迟、能耗、参数量均未说明）；4) 方法依赖"候选动作可预先枚举"的前提，作者也承认当候选动作无法预枚举时reactive执行更强；5) 摘要未说明策略树构建的计算开销和内存占用。

Ideas for my research: 1) "预计算+轻量匹配"的延迟规避思想可类比到在轨遥感目标检测：在卫星空闲时段预计算候选目标区域或预构建检测分支，事件触发时用轻量匹配器快速响应，避免在关键路径上运行重模型；2) 树大小按模型解码延迟设定的规则，可类比为在轨场景中根据模型推理延迟来设计预计算缓存/候选框数量；3) "branch routing作为因果瓶颈"的发现提示：在预计算方案中，路由/匹配模块的准确性往往比预计算本身更关键，值得在轻量化设计中重点关注；4) 消融实验方法论（pre-registered endpoints、McNemar检验、oracle probe）对在轨检测模型的评估设计有参考价值。总体借鉴价值有限，属于间接类比。

Abstract: Computer-use agents often fail on transient GUI events because they produce the correct action only after the relevant window has already closed. We identify the main cause as expensive autoregressive decoding on the decision-time critical path. We propose Adaptive Anticipatory Policy Trees (AAPT), which eliminates this delay without modifying the underlying model. During idle screen periods, the same frozen multimodal model constructs a bounded conditional policy tree with observable guards, pre-authorized actions, and branch-specific deadlines. The tree is sized to cover the model's own decoding latency. When an event occurs, a lightweight observer matches change-gated frames to a prepared branch and immediately executes the corresponding action without generating new text. In paired trials with pre-registered endpoints and exact McNemar tests, AAPT improves the success rate from 0.50 to 0.79 within a contested decision window ($p=1.8\times10^{-3}$), while producing no incorrect actions. Both open-loop and predict-and-replan baselines achieve zero success because they still decode during execution. A preparation-time sweep shows that the gain emerges where the latency-based tree-sizing rule predicts, and ablations reveal three key requirements: fast observer decoding, valid tree planning, and accurate branch routing. A pre-registered oracle probe rejects our initial hypothesis and instead points to branch routing as the causal bottleneck. We further reproduce the effect on an independent general-purpose multimodal model over 126 paired trials ($p=4.9\times10^{-13}$). On an external benchmark, AAPT matches the overall performance of a reactive baseline, although the two methods exhibit complementary strengths. Together, these results suggest that AAPT performs best when candidate actions can be enumerated in advance, whereas reactive execution remains stronger when they cannot.

</details>


### [382] [QAdapt: A Noise-Adaptive Neural Pre-Decoding Framework for Quantum Error Correction](https://arxiv.org/abs/2607.28422)
*Ran Miao,Rui Luo,Xiaohan Shan,Xiaoming Sun*

Main category: cs.LG

TL;DR: 提出QAdapt，一个面向表面码量子纠错的噪声自适应神经预解码框架，通过捕获综合征数据的时空相关性并顺序适应噪声变化，降低逻辑错误率并减少后端解码延迟。

Research relevance: 弱相关（1-2分）。该论文属于量子计算/量子纠错领域，与遥感、卫星、Earth observation、目标检测、模型轻量化均无直接关系。唯一可借鉴的是其"噪声自适应"和"预解码/预筛选"的思想，以及"减少后端计算负载"的架构思路，但这些与遥感在轨目标检测的关联非常间接。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于量子计算/量子纠错领域。虽然"预解码分层架构"和"噪声自适应"思想有一定跨领域启发，但整体借鉴价值很低，不建议优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：量子纠错中的神经预解码（neural pre-decoding）；应用场景：容错量子计算（fault-tolerant quantum computing, FTQC）；传感器类型：不适用（量子比特/综合征数据）；目标类别：不适用。

Model architecture: QAdapt是一个噪声自适应神经预解码框架，用于表面码（surface-code）量子纠错。它捕获综合征数据中的局部时空相关性（local spatiotemporal correlations），顺序适应演化的噪声条件（sequentially adapts to evolving noise conditions），并缓解灾难性遗忘（catastrophic forgetting），将残余综合征（residual syndrome）转发给传统全局解码器（conventional global decoder）。

Lightweight method: 摘要未明确涉及传统意义上的模型轻量化方法（如剪枝、量化、知识蒸馏、NAS）。但"预解码"（pre-decoding）架构本质上是一种计算卸载/负载分担策略——通过轻量神经预处理器过滤部分综合征数据，减少后端全局解码器的计算负担，这与边缘/星上部署中"前端轻量处理+后端重计算"的分层推理思想有一定类比性。

Onboard deployability: 摘要未说明参数量、FLOPs、显存、能耗等部署指标。仅提到在Google Willow基准数据上，后端解码延迟（backend decoding latency）降低了9.32%，但这是量子纠错解码延迟，与遥感在轨部署的延迟指标无关。

Datasets and metrics: 数据集：110个合成分布外（out-of-distribution）噪声配置的旋转表面码存储电路（rotated surface-code memory circuits），以及Google Willow基准数据。评价指标：逻辑错误率（logical error rate）、后端解码延迟（backend decoding latency）。

Experiments: 在110个合成OOD噪声配置上，QAdapt相对神经预解码基线一致降低逻辑错误率。在Google Willow基准数据上，无需目标域微调（without target-domain fine-tuning），逻辑错误率最高降低5.79%，后端解码延迟最高降低9.32%。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关，属于量子计算领域；2. 摘要未说明模型规模、计算开销、训练成本等关键信息；3. 未涉及任何遥感数据、目标检测任务或边缘/星上部署场景；4. 其"噪声自适应"概念虽有趣，但量子噪声与遥感图像噪声/传感器退化问题差异巨大。

Ideas for my research: 1. "预筛选/预解码"分层架构思想：可类比为在轨目标检测中先用轻量网络做候选区域预筛选，再对少量候选区域用高精度模型精检，从而降低整体计算负载；2. "噪声自适应+缓解灾难性遗忘"的在线适应策略：可借鉴到遥感在轨场景中，卫星传感器噪声/光照条件随时间变化，模型需要在线适应新分布而不遗忘旧知识；3. "分布外（OOD）鲁棒性"评估思路：在轨部署中训练数据与真实星上数据存在分布偏移，可借鉴其OOD评估框架来验证轻量化模型的泛化能力。

Abstract: Fault-tolerant quantum computing (FTQC) relies on quantum error correction to suppress physical errors and preserve logical information at scale. In practice, however, performance is constrained not only by physical noise but also by the latency of classical decoders processing rapidly generated syndrome data. This challenge is exacerbated by hardware noise that is strong, heterogeneous, and nonstationary, as well as by the simulation-to-hardware distribution shift that can substantially degrade fixed neural decoders. We present QAdapt, a noise-adaptive neural pre-decoding framework for surface-code quantum error correction. QAdapt captures local spatiotemporal correlations in syndrome data, sequentially adapts to evolving noise conditions while mitigating catastrophic forgetting, and forwards the residual syndrome to a conventional global decoder. Across 110 synthetic out-of-distribution noise configurations for rotated surface-code memory circuits, QAdapt consistently reduces the logical error rate relative to the neural pre-decoding baseline. On Google's Willow benchmark data, without target-domain fine-tuning, it achieves reductions of up to 5.79 percent in logical error rate and 9.32 percent in backend decoding latency on the residual syndrome. These results demonstrate that QAdapt provides a practical and decoder-compatible approach to improving the robustness and backend decoding efficiency of quantum error correction under evolving hardware noise.

</details>


### [383] [Kohn-Sham Spectral Embedding on Sparse Graphs at the Nishimori Temperature for Image Classification](https://arxiv.org/abs/2607.28428)
*V. S. Usatyuk,D. A. Sapozhnikov,S. I. Egorov*

Main category: cs.LG

TL;DR: 提出KSSE（Kohn-Sham Spectral Embedding），一种基于物理启发的能量模型，用稀疏图谱嵌入替代密集CNN分类器，在ImageNet-1000上以21.24M参数达到88.93% Top-1精度，比Swin-L和ViT-H分别减少10倍和30倍模型规模。

Research relevance: 弱相关（2分）。论文完全不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱等遥感场景，也不涉及目标检测任务。它属于泛视觉分类任务（ImageNet图像分类），但其核心贡献——用稀疏图谱嵌入替代密集分类器实现模型压缩（10-30倍参数缩减）——在模型轻量化方法论上对"在轨遥感目标检测模型轻量化"有一定借鉴意义，因此给予2分而非1分。

Reading priority: 2分。论文与遥感/卫星/目标检测方向基本无关，属于泛视觉分类的模型压缩方法。虽然其"稀疏图替代密集分类器"的轻量化思想有一定借鉴价值，但缺乏遥感场景验证、目标检测适配和部署指标，且方法工程复杂度高，对在轨遥感目标检测轻量化课题的直接参考价值有限。建议仅作为轻量化方法论背景阅读，优先级较低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：图像分类（image classification），非目标检测。应用场景：通用视觉分类（ImageNet-1000）。传感器类型：摘要未说明（使用预训练EfficientNet-B4特征，应为自然图像）。目标类别：1000类ImageNet类别，非遥感目标（舰船/飞机/车辆等）。

Model architecture: KSSE架构：将预训练CNN特征（EfficientNet-B4，D=1792维）映射到准循环LDPC（low-density parity-check）图上，构造正则化Laplacian作为Kohn-Sham Hamiltonian，通过FFT在循环块上求解D个独立通道谱问题（O(N log N + k²_mode N)时间），并用低阶Rayleigh refinement（k_mode=5）精化。图拓扑通过"star-domain surgery"优化，构造边移位在码字周围产生局部凸性，同时约束残差frustration ρ(B_γ)≤1+δ。整体替代了密集CNN分类头。

Lightweight method: 核心轻量化思想：用稀疏图谱嵌入（sparse-graph spectral embedding）替代密集CNN分类器。通过将特征映射到LDPC图上并求解谱问题，实现模型参数从Swin-L的197M和ViT-H的632M分别压缩到21.24M（约10倍和30倍缩减）。计算复杂度为O(N log N + k²_mode N)，利用FFT和循环块结构加速。这不是传统的剪枝/量化/蒸馏/NAS方法，而是一种"替代分类头"的模型压缩范式。

Onboard deployability: 摘要未说明具体部署信息。未报告FLOPs、延迟、显存、能耗或实时推理指标。虽然参数规模（21.24M）相对ViT-H（632M）和Swin-L（197M）大幅缩减，但谱求解过程（FFT + Rayleigh refinement）的实际计算开销和硬件友好性（是否适合FPGA/NPU/TensorRT）摘要未说明。此外，该方法依赖预训练CNN特征（EfficientNet-B4），整体推理仍需CNN backbone，端到端部署成本未明确。

Datasets and metrics: 数据集：ImageNet-1000（transductive protocol，使用冻结的EfficientNet-B4特征）。评价指标：Top-1 accuracy（88.93%）。对比基准：Swin-L（197M，86.4-87.3%）、ViT-H/14（632M，88.0-89.5%）。摘要未说明遥感数据集（如DOTA、NWPU VHR-10、SSDD等）或目标检测指标（mAP、IoU等）。

Experiments: 主要结果：KSSE在ImageNet-1000上达到88.93% Top-1精度，使用约21.24M参数，超越Swin-L（86.4-87.3%）并与ViT-H/14（88.0-89.5%）相当，同时模型规模分别缩减10倍和30倍。理论部分证明了六个定理：广义Ihara-Bass恒等式、trapping-set特征值对应、加性通道可分离性（含exchange-correlation界）、surgery定理（frustration界与attractor宽度Ω(1/√d_min)）、准平稳扰动界、不动点收敛定理。摘要未说明消融实验细节。

Limitations: 1. 完全非遥感场景：无任何遥感/卫星/在轨相关实验验证。2. 任务为分类而非目标检测，未涉及anchor、NMS、旋转框等检测组件。3. 依赖预训练CNN特征（EfficientNet-B4），不是端到端轻量化方案。4. 摘要未报告实际推理延迟、FLOPs、能耗等部署关键指标。5. transductive protocol与真实在轨推理的inductive设定存在差异。6. 谱求解的数值稳定性、对硬件（FPGA/NPU）的适配性未说明。7. 方法复杂度较高（涉及FFT、Rayleigh refinement、LDPC图构造），实际工程落地难度可能较大。

Ideas for my research: 1. 借鉴"稀疏图替代密集分类头"的思想：在遥感目标检测中，检测头（classification + regression heads）往往占据一定参数量，可探索用轻量谱嵌入或图结构替代密集全连接检测头。2. 借鉴"预训练特征 + 轻量后处理"的范式：在星上部署中，可考虑冻结轻量backbone（如MobileNet变体）提取遥感特征，再用极轻量的图/谱方法完成检测头，降低星上计算负担。3. 借鉴参数压缩比例思路：KSSE展示了10-30倍参数缩减仍保持精度，可作为遥感检测模型压缩目标的参考基准。4. 注意：该方法依赖FFT和谱求解，在FPGA/NPU上的可实现性需评估，若硬件不支持FFT加速则需谨慎借鉴。5. 可探索将类似"能量模型/图嵌入"思想用于遥感小目标检测的上下文建模，但需重新设计以适应旋转框和密集小目标场景。

Abstract: We introduce Kohn--Sham Spectral Embedding (KSSE), a physics-inspired energy-based model replacing dense CNN classifiers with a sparse-graph spectral embedding evaluated at the Nishimori temperature of an associated Random-Bond Ising Model. By mapping pre-trained features onto quasi-cyclic low-density parity-check graphs and constructing a regularized Laplacian acting as a Kohn--Sham Hamiltonian, we solve $D$ independent channel spectral problems in $\mathcal{O}(N\log N + k^2_{\text{mode}} N)$ time via FFT on circulant blocks (leveraging Pontryagin self-duality of $\mathbb{Z}/p\mathbb{Z}$) and low-order Rayleigh refinement. Graph topology is optimized using \emph{star-domain surgery}: rather than destroying information-carrying codewords by removing frustrated cycles, we construct edge shifts creating local convexity around codewords while bounding residual frustration to $ρ(B_γ)\leq 1+δ$. Multi-scale fractal analysis ($D_2$ spectrum) and fractal learning-rate landscape certifies a landscape transition from rough regimes ($D_2>3$) to star-domain basins ($D_2<1$), enabling Rayleigh refinement with $k_{\text{mode}}=5$ modes. We prove six theoretical results: a generalized Ihara--Bass identity linking belief propagation to the Laplacian; trapping-set eigenvalue correspondence; additive channel separability with an explicit exchange-correlation bound; a surgery theorem bounding frustration with attractor width $Ω(1/\sqrt{d_{\min}})$; a quasi-stationarity perturbation bound; and a fixed-point convergence theorem. In a transductive protocol on ImageNet-1000 with frozen EfficientNet-B4 features ($D=1792$), KSSE achieves \textbf{88.93\%} Top-1 accuracy using $\approx 21.24$M parameters, outperforming Swin-L (197M, 86.4--87.3\%) and matching ViT-H/14 (632M, 88.0--89.5\%) under standard inductive setups, while reducing model footprint by $10\times$ and $30\times$, respectively.

</details>


### [384] [Oracle-Budgeted Molecular Optimization with Short-Term Graph Memory](https://arxiv.org/abs/2607.28437)
*Jiannan Yang,Veronika Thost,Xiang Ling,Tengfei Ma*

Main category: cs.LG

TL;DR: 本文提出一种"短期图记忆"插件模块，利用在线图神经网络代理对候选分子进行预筛选，在固定oracle预算下优先评估预测效用更高的分子，从而提升分子优化性能。

Research relevance: 1分，与本研究方向基本无关。论文主题是分子优化（molecular optimization），属于药物发现/化学信息学领域，完全不涉及遥感、卫星、目标检测或模型轻量化。虽然文中提到"surrogate model"和"online learning"等概念，但这些与遥感在轨目标检测及轻量化没有任何直接关联。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于分子优化/药物发现领域。不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：分子优化（molecular optimization），在有限oracle预算下选择候选分子进行评估。应用场景：药物发现/化学信息学。传感器类型：不适用。目标类别：分子结构。

Model architecture: 提出"short-term graph memory"插件模块，包含一个在线图神经网络（graph neural surrogate）作为代理模型，用于预筛选每轮候选池。该模块保持原有生成器架构和原生更新规则不变，仅作为前置筛选器。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。虽然"surrogate model"在概念上类似代理模型，但用途是oracle预算优化而非模型轻量化。

Onboard deployability: 摘要未说明。不涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 使用标准分子优化基准（standard molecular optimization benchmark），评价指标为mean top-10 score。测试了四种生成器（four generators），在tight budget（一千次oracle调用）下进行实验。

Experiments: 在标准分子优化基准上，该方法在无额外oracle成本下提升了mean top-10 score，且在任何oracle上都不落后于基线。增益在四种生成器上均成立。进一步分析了surrogate-guided selection与不同生成器探索/利用行为之间的交互关系。

Limitations: 摘要未说明局限性。但明显与遥感卫星在轨目标检测及轻量化方向完全无关，属于药物发现领域的专门研究。其"有限预算下选择性评估"的思想虽然有一定通用性，但与遥感目标检测的部署约束（计算资源、功耗、延迟）性质完全不同。

Ideas for my research: 借鉴意义非常有限。唯一可能的概念性启发是"在有限预算下用代理模型预筛选候选"的思想——这类似于在轨场景中，如果计算资源受限，可以用一个轻量代理模型先筛选感兴趣区域（ROI），再对高价值区域运行完整检测模型。但这种"coarse-to-fine"或"cascade detection"思想在遥感目标检测领域已有成熟方案（如两阶段检测器、注意力引导的ROI筛选），本文并未提供遥感场景下的直接可借鉴技术。

Abstract: Molecular optimization is commonly performed under a limited oracle budget, which makes deciding what to evaluate as important as deciding what to generate. We introduce short-term graph memory, a plug-in module that preserves the generator architecture and native update rule while learning from previously evaluated molecules to prioritize subsequent oracle queries. The module maintains an online graph neural surrogate that pre-screens each round's candidate pool, so the fixed oracle budget is spent on molecules with higher predicted utility. Applied to a fragment-based generator on a standard molecular optimization benchmark, it improves the mean top-10 score at no extra oracle cost and never falls behind the base on any oracle; the gain extends to all four generators we tested at a tight budget of one thousand calls. We then analyze how surrogate-guided selection interacts with the exploration and exploitation behavior of different generators. Its benefit at larger budgets is consistent with two properties of the backbone: how broadly it searches, and how effectively its native search already exploits oracle feedback. We provide a simple way to spend a fixed oracle budget more selectively, and evidence on which generators benefit from it.

</details>


### [385] [Cybersecurity Detection Classification with Reasoning-enabled Language Models](https://arxiv.org/abs/2607.28460)
*Amol Khanna,Manu Nandan,Cristian Viorel Popa,Joan Pujol-Roig,Diana Bolocan,Laura Vasilie,Alexandru Apostu,Chase Helwig,Mihaela Gaman,Michael Brautbar,Edward Raff,Chase Midler,Sven Krasser*

Main category: cs.LG

TL;DR: 本文提出一种基于链式思维（CoT）推理的告警分流分类器，用于安全运营中心（SOC）的Windows端点检测告警分流，结合自动提示优化、自训练和带可验证奖励的强化学习训练模型，并额外训练一个校准器来估计推理轨迹的置信度，在真实人工标注数据上达到82.6%的测试准确率。

Research relevance: 弱相关（2分）。该论文属于网络安全/安全运营中心（SOC）领域的告警分流任务，与遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能等遥感方向完全无关；也不涉及目标检测、小目标检测、旋转框检测、舰船/飞机/车辆检测等遥感目标检测任务；更不涉及剪枝、量化、知识蒸馏、NAS、实时推理、边缘部署、星上部署、FPGA/NPU/TensorRT等轻量化或部署技术。它属于泛机器学习/LLM应用方法，仅在"分类器+置信度校准"的方法论层面可能有间接借鉴意义。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于网络安全领域的LLM应用研究。仅在置信度校准和"定向训练优于扩大规模"的方法论层面有极有限的间接参考价值，不建议作为核心阅读文献。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：告警分流（triage）二分类/多分类任务；应用场景：安全运营中心（SOC）的Windows端点检测告警处理；传感器类型：不适用（非遥感传感器）；目标类别：良性（benign）与恶意（malicious）端点检测告警。

Model architecture: 基于大语言模型（LLM）的链式思维（chain-of-thought, CoT）推理分类器；结合自动提示优化（automated prompt optimization）、自训练（self-training）、带可验证奖励的强化学习（reinforcement learning with verifiable rewards）；另训练一个独立的校准器（calibrator）读取完整推理轨迹并估计判定正确的概率。实验涉及30B参数的微调模型。

Lightweight method: 摘要未说明。本文未涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。虽然提到"finetuned 30B model"和"targeted training over scale"的对比，但这属于模型规模与训练策略的权衡，并非轻量化技术。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。且该任务场景为SOC告警分流，与星上/边缘部署场景无关。30B规模的LLM本身也不适合在轨部署。

Datasets and metrics: 数据集：真实人工标注的Windows端点检测告警数据（real, human-labeled Windows endpoint detections）；评价指标：测试准确率（82.6% test accuracy）、良性召回率（benign recall，提升43.0%）、恶意召回率（malicious recall，提升18.3%），以及高置信度工作点（high-confidence operating point）下的性能。

Experiments: 主要结果：系统达到82.6%测试准确率；在高置信度工作点下，相比直接标签（direct-label）LLM分类器，良性召回率提升43.0%，恶意召回率提升18.3%；消融实验表明训练校准器是必要的——未训练的置信度判断器（untrained confidence judge）会使高置信度召回率降为零；对比实验显示微调的30B模型显著优于前沿通用模型（frontier general-purpose models），说明定向训练优于单纯扩大模型规模。

Limitations: 与本研究方向（遥感卫星在轨目标检测与轻量化）基本无关。局限性包括：1）任务场景为网络安全告警分流，非遥感目标检测；2）基于30B规模LLM，计算开销大，不涉及任何轻量化或边缘/星上部署考虑；3）摘要未说明模型推理速度、资源占用等部署指标；4）未涉及遥感图像、SAR、高光谱等传感器数据。

Ideas for my research: 尽管场景不同，本文在方法论层面有少量可借鉴点：1）"置信度校准器（calibrator）"思想——在遥感目标检测中，可考虑对检测框的置信度进行校准，提高高置信度工作点下的精确率，减少虚警，这对在轨资源受限场景下"只上报高置信度目标"的决策策略有参考价值；2）"定向训练优于单纯扩大规模"的结论与轻量化思路一致——在遥感在轨场景中，可通过针对特定目标类别和传感器数据的定向训练来弥补小模型能力不足，而非一味追求大模型；3）CoT推理与置信度解耦的设计思路，可类比于检测模型中"检测头输出与不确定性估计解耦"的设计。但整体借鉴价值有限。

Abstract: A major issue in Security Operations Centers (SOCs) is alert fatigue, as the number of detections reported is more than staff can triage in a given day. Prior work prompts or fine-tunes large language models (LLMs) to emit a triage label directly, but does not train them to reason about whether a detection is a genuine threat. We train a chain-of-thought (CoT) reasoning-enabled triage classifier on real, human-labeled Windows endpoint detections by combining automated prompt optimization, self-training, and reinforcement learning with verifiable rewards. We find that CoT reasoning also degrades the label-token probabilities that automated triage relies on, so we separately train a calibrator that reads the full reasoning trace and estimates the probability that the verdict is correct. Our system reaches 82.6% test accuracy and, at the high-confidence operating point that governs automated triage, improves benign recall by 43.0% and malicious recall by 18.3% over a direct-label LLM classifier. We further show that the trained calibrator is necessary - an untrained confidence judge collapses high-confidence recall to zero - and that a finetuned 30B model significantly outperforms frontier general-purpose models, motivating targeted training over scale.

</details>


### [386] [Stage-Replay Divergence Follows the KV Cache: Fixed-Prefix Precision Controls and Bidirectional Cache Transplantation](https://arxiv.org/abs/2607.28495)
*Alexander Boesgaard Lorup*

Main category: cs.LG

TL;DR: 本文研究大语言模型（Qwen2.5）推理过程中KV cache重放（stage-replay）的诊断方法，探讨在推理阶段边界处保留的live cache与一次性prefill在token级行为上的一致性，属于LLM推理机制与数值精度分析。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关（1分）。论文主题是大语言模型（LLM）推理阶段的KV cache重放与数值精度（BF16 vs FP32）对解码一致性的影响，属于自然语言处理/推理系统领域，不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱、多光谱、空间在轨智能，也不涉及目标检测或任何模型压缩/轻量化方法。

Reading priority: 1分。论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于LLM推理系统研究。除非对"数值精度如何影响模型行为"的归因方法论感兴趣（可类比量化误差分析），否则无需阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：LLM推理阶段的KV cache一致性诊断（stage-replay diagnostics）。应用场景：大语言模型推理系统。传感器类型：不适用（非遥感传感器）。目标类别：不适用（非目标检测任务）。

Model architecture: 基于Qwen2.5-derived系统，涉及48层key/value层（KV cache）的transplantation实验，比较retained live cache与one-shot prefill两种推理路径在token级解码行为上的差异。核心概念包括token prefix、decoder state、KV cache、BF16/FP32精度。

Lightweight method: 摘要未说明。本文不涉及剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计等模型压缩或加速方法。虽然涉及BF16/FP32数值精度对比，但这是关于推理一致性的分析，而非面向部署的量化压缩方法。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗、实时性等部署相关信息。本文讨论的是LLM推理的cache一致性，与星上/边缘部署无关。

Datasets and metrics: 使用200-item匹配实验（matched 200-item experiment），包含166个suffix差异和20个correctness labels差异；采用paired 95% CI [-3.5, +5.5]评估准确率差异；使用95% Wilson upper bound 1.88%评估FP32无解码分歧；另有12/12 rows、24/24、43/43等一致性计数指标。数据集为LLM推理token序列，非遥感数据集。

Experiments: 主要实验包括：(1) 200-item匹配实验对比retained live cache与one-shot prefill，BF16下replicas精确但构造在166个suffix和20个correctness labels上不同，准确率仅差1点；(2) fixed-prefix 2x2实验交叉construction与precision，BF16分歧重现而FP32无解码分歧；(3) prospective bridge使token-by-token incremental与retained live caches在12/12行上bit-exact；(4) all-200 saved-ledger audit复现所有轨迹；(5) 48层KV层双向transplantation使分歧continuation跟随cache donor（24/24和43/43）。

Limitations: 摘要未说明。但可推断：实验仅在Qwen2.5-derived单一系统上验证，样本量较小（200项）；结论局限于特定推理阶段边界；未涉及遥感场景、目标检测或模型轻量化；数值精度对行为表达的影响机制未完全解释。

Ideas for my research: 与本人课题（遥感卫星在轨目标检测模型轻量化）基本无直接借鉴点。唯一可能的间接启发是：本文关于BF16/FP32数值精度对推理行为影响的系统性分析思路，可类比迁移到遥感检测模型量化（quantization）后精度损失的归因分析——即量化误差在哪些层/哪些特征上主导了检测性能退化，类似本文"KV cache是分歧轨迹的因果载体"的思路，可用于定位量化敏感层。但整体相关性很弱。

Abstract: Stage-replay diagnostics reconstruct intermediate token prefixes and treat fresh-prefill continuation as continuation from the decoder state that originally reached the prefix. We audit that assumption at a whole reasoning-stage boundary in a Qwen2.5-derived system. A matched 200-item experiment compares retained live cache with one-shot prefill of identical integer tokens and places an exact replica on both sides. In BF16, replicas remain exact while the constructions differ on 166 suffixes and 20 correctness labels; the accuracy difference is only one point (paired 95% CI [-3.5, +5.5]). A fixed-prefix 2x2 holds all 200 token states constant while crossing construction and precision. The BF16 disagreements recur, whereas FP32 produces no decoded disagreement (95% Wilson upper bound 1.88%). A prospective bridge makes token-by-token incremental and retained live caches bit-exact on 12/12 rows; an all-200 saved-ledger audit reproduces every retained trajectory and comparison fingerprint. Bidirectional transplantation of all 48 key/value layers makes every tested divergent continuation follow its cache donor, both on a selected set at the primary checkpoint (24/24) and an outcome-blind replication at a later checkpoint (43/43). Exact-token replay can therefore be repeatable without preserving live-state fidelity. On the tested states, boundary K/V cache is a causally sufficient carrier of the divergent trajectory, while numerical precision moderates its behavioral expression.

</details>


### [387] [The Role of Causality in Algorithmic Recourse](https://arxiv.org/abs/2607.28497)
*Srikanth Avasarala,Varun Gupta,Shahin Jabbari,Saber Salehkaleybar,Juba Ziani*

Main category: cs.LG

TL;DR: 本文提出一种基于因果执行性框架（causal performative framework）的算法追索（algorithmic recourse）方法，用于在高风险分类场景（如贷款申请）中提供可操作的改进建议，并通过因果结构建模来避免策略性博弈和模型重训后的失效问题。

Research relevance: 弱相关（2分）。该论文属于泛机器学习/因果推断领域，聚焦于算法追索（algorithmic recourse）和因果执行性优化（performative optimization），与遥感、卫星、Earth observation、目标检测、轻量化部署均无直接关联。其核心问题域是信用贷款等高风险分类场景中的公平性与策略性行为建模，属于泛机器学习方法，对遥感在轨目标检测与轻量化方向仅可能有方法论层面的间接借鉴。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于因果推断/算法公平性领域，不涉及任何视觉检测、遥感数据处理或模型压缩技术。除非对因果执行性理论在域自适应中的应用有特殊兴趣，否则不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：算法追索（algorithmic recourse）——为分类模型预测结果提供可操作的输入特征改变建议。应用场景：贷款和抵押贷款申请等高风险分类场景（high-stakes classification）。传感器类型：不适用（非遥感/视觉任务）。目标类别：不适用（非目标检测任务）。

Model architecture: 摘要未说明具体网络结构。核心方法基于结构因果模型（Structural Causal Model, SCM）建模特征间交互及其对真实标签的影响，将追索策略的传播建模为因果响应，并转化为非凸优化问题求解。采用简单迭代动力学（simple iterative dynamics）计算执行性稳定解（performatively stable solutions）。

Lightweight method: 不涉及。摘要中未提及任何模型压缩、剪枝、量化、知识蒸馏、NAS、轻量网络设计或边缘/星上部署相关技术。

Onboard deployability: 不适用。摘要未说明参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。该工作面向信用评分场景，与在轨/边缘部署无关。

Datasets and metrics: 数据集：半合成数据集（semi-synthetic）和真实信用数据集（real credit datasets）。评价指标：摘要未明确列出具体指标名称，但对比了标准经验风险最小化（Empirical Risk Minimization, ERM）方法，评估了预测准确率以及减少重复模型重训需求的能力。

Experiments: 实验表明，所提出的因果追索方法在多个数据集上一致优于标准ERM方法，同时减少了因策略性智能体行为导致分布偏移而需要重复模型重训的次数。摘要未提供具体数值结果或消融实验细节。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向完全无关，属于因果推断与算法公平性领域。2. 摘要未说明模型规模、计算开销、推理效率等部署指标。3. 实验仅覆盖信用评分场景，未涉及视觉/图像任务。4. 摘要未讨论因果结构假设在实际场景中的可获取性与鲁棒性。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向弱相关，但存在以下间接借鉴点：1. 因果执行性框架（causal performative framework）中"模型部署后因环境/数据分布变化而失效"的思想，可类比遥感在轨模型面临的域偏移（domain shift）问题——卫星在轨运行中光照、季节、传感器状态变化会导致模型性能退化，可借鉴其"减少重复重训需求"的思路，考虑在轨自适应或持续学习策略。2. 其"策略性博弈"概念可类比对抗样本/分布外样本对在轨检测模型的干扰，提示轻量化模型需兼顾鲁棒性。3. 迭代动力学求解稳定解的方法论，对在轨模型在线更新策略设计有一定启发。但整体而言，该论文对本课题的直接可借鉴性有限。

Abstract: Algorithmic recourse aims to provide individuals with actionable changes to improve their predicted outcomes in high-stakes classification settings, such as loan and mortgage applications. However, most existing approaches focus only on flipping a model's prediction, without accounting for whether the recommended changes lead to genuine improvement in an individual's true qualifications or merely enable strategic gaming of the classifier. Consequently, deployed recourse policies can induce behavioral responses that degrade predictive accuracy and become ineffective after model retraining.
  In this work, we formalize this failure mode through a causal performative framework for recourse. We model how recourse actions propagate through a structural causal model, capturing interactions among features as well as their effect on the true label. These causal responses induce a non-convex optimization problem, even under standard convex losses. We characterize conditions under which performatively stable solutions exist and can be efficiently computed via simple iterative dynamics. Our analysis reveals that recourse policies that ignore causal structure can induce large, misaligned behavioral responses, whereas causal recourse leads to stable equilibria that reduce incentives for gaming. Experiments on both semi-synthetic and real credit datasets demonstrate that our approach consistently outperforms standard empirical risk minimization while reducing the need for repeated model retraining to accommodate distribution shifts caused by strategic agent behavior.

</details>


### [388] [Same Graph Cross-Task Transfer in GNNs: Protocols and Predictors](https://arxiv.org/abs/2607.28525)
*Neelam Akula,Surbhi Kumar,Murat Kantarcioglu,Baris Coskunuzer*

Main category: cs.LG

TL;DR: 论文研究同一图结构上节点分类（NC）与链接预测（LP）之间的跨任务迁移，提出无泄漏的评估协议和CoTask Score（CTS）指标，发现NC→LP在同质图上稳定有益而LP→NC仅在特定条件下有效。

Research relevance: 弱相关（2分）。论文属于图神经网络（GNN）与多任务迁移学习领域，研究对象是通用图结构上的节点分类和链接预测，与遥感、卫星、地球观测、目标检测、轻量化部署均无直接关联。虽然GNN方法在遥感场景理解中偶有应用，但本文核心关注的是跨任务迁移评估协议，而非遥感任务或模型压缩。

Reading priority: 1分。与遥感卫星在轨目标检测及轻量化方向基本无关，属于图神经网络多任务迁移的纯方法论研究。除非对GNN在遥感场景中的应用或跨任务迁移评估协议有专门兴趣，否则无需优先阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：节点分类（node classification, NC）与链接预测（link prediction, LP）的跨任务迁移；应用场景：通用图数据（非遥感场景）；传感器类型：不适用；目标类别：不适用（图节点/边）。

Model architecture: 使用三种图神经网络骨干：GCN、GraphSAGE、GPS（Graph Transformer + 消息传递混合架构）。核心设计是共享消息传递图（shared message-passing graph），排除被评估的边以避免信息泄漏。

Lightweight method: 摘要未说明。本文不涉及剪枝、量化、知识蒸馏、NAS等模型压缩或加速方法。

Onboard deployability: 摘要未说明。未提供参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。

Datasets and metrics: 摘要未明确列出具体数据集名称。评价指标包括跨任务迁移效果（NC→LP和LP→NC的准确率变化）以及新提出的CoTask Score（CTS），用于衡量共享编码器同时服务NC和LP任务时的联合效用。还使用同质性（homophily）等数据集统计特征来指导机制选择。

Experiments: 在三种骨干（GCN、GraphSAGE、GPS）上验证了跨任务迁移的方向性：NC→LP在同质图（homophilic graphs）上稳定有益；LP→NC在朴素表示复用下可能降低准确率，仅在结构主导（structure-dominant）且LP容易但NC未饱和的场景下可靠为正，表明LP可视为结构预训练（structural pretraining）。

Limitations: 1. 与遥感/卫星/目标检测/轻量化方向基本无关；2. 摘要未说明具体数据集、实验规模、计算开销；3. 结论基于通用图数据，未涉及图像或遥感数据；4. 未讨论任何部署或实时推理问题。

Ideas for my research: 1. 本文提出的"无泄漏评估协议"（leakage-free protocol）思想可借鉴——在遥感目标检测中，训练/测试数据划分需避免空间重叠泄漏（如同一区域的不同切片被分到训练和测试集），这与本文固定节点/边划分的思路有方法论上的类比价值；2. 跨任务迁移的方向性结论（结构预训练思想）可类比思考：在遥感在轨场景中，是否可以用自监督/辅助任务（如场景分类、旋转框回归）作为预训练来提升小目标检测性能；3. CTS联合效用指标的思想可借鉴到"检测精度 vs 模型轻量化"的多目标权衡评估中。整体借鉴价值有限，属于方法论层面的间接启发。

Abstract: Many real-world graphs support multiple predictive tasks over the same underlying structure, creating an opportunity to reuse supervision across node classification (NC) and link prediction (LP). However, existing evaluations often rely on incompatible splits, observed-graph assumptions, and negative sampling rules, making conclusions about same-graph cross-task transfer unreliable. We formalize same-graph NC-LP transfer and propose a leakage-free protocol that fixes node and edge splits, uses a shared message-passing graph that excludes evaluated edges, and employs fixed negatives for LP. Across three backbones (GCN, GraphSAGE, GPS), we find that transfer is strongly directional and predictable: NC $\to$ LP is consistently beneficial on homophilic graphs, while LP $\to$ NC is fragile and can even degrade accuracy under naive representation reuse. LP $\to$ NC becomes reliably positive mainly in a structure-dominant regime where LP is easy but NC is unsaturated, suggesting that LP acts as structural pretraining. Finally, we introduce the CoTask Score (CTS) to summarize joint NC+LP utility when a shared encoder must serve both tasks, and show that simple dataset statistics, especially homophily, can guide mechanism choice and help avoid negative transfer.

</details>


### [389] [APO: Unsupervised Atomic Policy Optimization for 3D Structure Prediction of Atomic Systems](https://arxiv.org/abs/2607.28553)
*Shentong Mo,Yatao Bian*

Main category: cs.LG

TL;DR: 提出一种用于原子系统3D结构预测的无监督对齐框架APO，通过群相对策略优化和双奖励机制替代监督偏好学习，提升晶体与抗体结构预测精度和推理效率。

Research relevance: 1分，与本研究方向基本无关。该论文聚焦材料科学与药物发现中的原子系统3D结构预测（flow-matching模型），属于计算化学/结构生物学领域，与遥感、卫星、目标检测、轻量化部署均无直接关联。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于计算化学/结构生物学领域，不建议投入阅读时间。仅当对无监督对齐或策略优化方法有跨领域兴趣时可作泛读参考。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：3D原子系统结构预测（crystal structure prediction, antibody structure prediction）；应用场景：材料科学、药物发现；传感器类型：不适用（非遥感传感器）；目标类别：晶体相、de novo蛋白质/抗体结构。与遥感目标检测完全无关。

Model architecture: 基于flow-matching模型（如FlowDPO），引入group-relative policy optimization（GRPO）适配3D原子环境，采用双奖励机制：特征值分解样本相似性强化主导潜在结构模态的奖励，以及强制热力学稳定性的奖励。模型通过自纠正机制在采样组内识别物理上合理的构型。

Lightweight method: 摘要提到APO能有效"拉直"概率路径（straightens probability paths），显著提升推理效率（inference efficiency），但未涉及剪枝、量化、知识蒸馏、NAS等传统模型压缩方法，也未涉及边缘/星上部署。轻量化信息仅体现在推理路径优化层面，与遥感模型轻量化方法体系不同。

Onboard deployability: 摘要未说明参数量、FLOPs、延迟、显存、能耗等部署指标。仅提及"improving inference efficiency"，但未给出量化数据，无法评估星上/边缘部署可行性。

Datasets and metrics: 数据集：crystal structure prediction和antibody structure prediction基准数据集（具体名称摘要未说明）；评价指标：match rates（匹配率）、structural fidelity（结构保真度）。

Experiments: 在晶体和抗体结构预测基准上，APO一致优于全监督基线，达到新的SOTA（match rates和structural fidelity）。同时验证了APO能拉直概率路径，提升推理效率。具体对比方法和消融实验细节摘要未说明。

Limitations: 1. 与遥感/卫星/目标检测/轻量化部署方向完全无关；2. 摘要未提供推理效率提升的具体量化指标；3. 无监督对齐在复杂多模态原子系统中的泛化边界未讨论；4. 未涉及任何部署相关硬件或实时性约束。

Ideas for my research: 尽管领域不同，但可借鉴的通用方法论思路包括：(1) 无监督/自监督对齐思想——在遥感目标检测中，如果标注数据稀缺（如特定卫星传感器的新场景），可探索利用物理一致性或几何一致性作为监督信号替代人工标注；(2) 群相对策略优化（GRPO）的奖励设计思路——可类比用于遥感检测模型的自适应训练策略；(3) 概率路径拉直提升推理效率的思想——与轻量化中减少推理步数/加速推理的理念相通。但这些均为间接借鉴，直接可复现性有限。

Abstract: Predicting the 3D structures of atomic systems is fundamental to advancing material science and drug discovery. While flow-matching models (, FlowDPO) have recently shown promise in this domain, their performance relies heavily on alignment with ground-truth coordinates via supervised preference learning. However, obtaining experimental labels for novel crystal phases or de novo proteins is prohibitively expensive, creating a bottleneck for structural modeling in data-scarce regimes. In this work, we propose (Atomic Policy Optimization), a fully unsupervised alignment framework that eliminates the need for ground-truth reference structures. APO adapts group-relative policy optimization to 3D atomic environments, utilizing a novel dual-reward mechanism: (i) a that reinforces the policy's dominant latent structural modes through eigen-decomposition of sample similarities, and (ii) a that enforces thermodynamic stability. Our framework enables the model to ``self-correct'' by identifying physically plausible configurations within sampled groups. Extensive benchmarks on crystal and antibody structure prediction demonstrate that APO consistently outperforms fully supervised baselines, achieving a new state-of-the-art in match rates and structural fidelity. Furthermore, we show that APO effectively straightens probability paths, significantly improving inference efficiency. Our results suggest that intrinsic physical consistency can serve as a superior guide for alignment compared to noisy, supervised coordinate matching.

</details>


### [390] [$β$-OPSD: Deriving with Policy Optimization, Training with Self-Distillation](https://arxiv.org/abs/2607.28582)
*Jiawei Xu,Minghui Liu,Juzheng Zhang,Tom Goldstein,Furong Huang*

Main category: cs.LG

TL;DR: 提出β-OPSD方法，将vanilla OPSD（on-policy self-distillation）推广为带KL惩罚权重β的可控正则化框架，通过logits混合实现从参考策略到教师策略的几何插值蒸馏目标，提升推理语言模型的优化稳定性和推理性能。

Research relevance: 弱相关（2分）。论文聚焦于大语言模型（reasoning language models）的自我蒸馏与策略优化方法，属于泛机器学习/泛AI方法，与遥感、卫星、目标检测、轻量化部署均无直接关联。其核心贡献在LLM推理能力提升，而非视觉目标检测或模型压缩。

Reading priority: 1分。论文与遥感卫星在轨目标检测及模型轻量化方向基本无关，属于LLM推理优化领域。除非对知识蒸馏的理论框架（如蒸馏目标的可控插值）有方法论兴趣，否则无需深入阅读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：推理语言模型（reasoning language models）的自我蒸馏与策略优化。应用场景：数学推理（mathematical reasoning benchmarks）。传感器类型：不适用。目标类别：不适用。

Model architecture: 摘要未说明具体网络结构。核心框架为β-OPSD：将vanilla OPSD视为β=1的特例，引入KL惩罚权重β作为可控正则化参数，最优策略为参考策略与privileged teacher之间的几何插值。实现方式为在token-level logits层面混合参考策略与教师策略的logits，并采用return-to-go credit assignment对齐token更新与序列级目标。

Lightweight method: 涉及知识蒸馏（distillation）思想，但并非面向模型压缩/加速的轻量化方法。本文的蒸馏目标是提升推理性能而非减小模型规模。未涉及剪枝（pruning）、量化（quantization）、NAS、实时推理或边缘部署。

Onboard deployability: 摘要未说明。未涉及参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。且该方法面向LLM推理，与星上/边缘部署场景无直接关联。

Datasets and metrics: 数据集：数学推理基准（mathematical reasoning benchmarks），具体数据集名称摘要未说明。评价指标：优化稳定性（optimization stability）和下游推理性能（downstream reasoning performance），具体指标名称摘要未说明。

Experiments: 实验表明β-OPSD在数学推理基准上一致优于vanilla OPSD，提升了优化稳定性和下游推理性能。具体对比方法、消融实验细节、数值结果摘要未说明。

Limitations: 1. 与遥感/卫星/目标检测/轻量化部署完全无关，属于LLM领域方法。2. 摘要未提供具体实验数据、模型规模、计算开销等关键信息。3. 方法仅验证于数学推理任务，泛化性未知。4. 未讨论该方法在视觉任务或嵌入式/边缘设备上的适用性。

Ideas for my research: 尽管本文与遥感目标检测轻量化无直接关联，但其"将固定超参数转化为可控正则化参数"的思想有一定方法论借鉴意义：1) 在知识蒸馏（KD）用于遥感小目标检测模型压缩时，可借鉴"参考策略-教师策略插值"的思路，将教师logits与学生logits的混合比例作为可调参数，而非固定蒸馏温度或固定soft target权重；2) return-to-go credit assignment思想可类比为在检测任务中按目标重要性（如小目标/难样本）分配蒸馏损失权重，而非对所有目标一视同仁。但整体借鉴价值有限，不建议作为核心参考。

Abstract: On-policy self-distillation (OPSD) is a promising approach to improve reasoning language models, but it remains brittle in practice: making it work reliably often requires substantial engineering effort. We identify a structural source of this difficulty: vanilla OPSD is precisely the $β=1$ member of a broader policy-optimization family, where $β$ weights the KL penalty anchoring the student to a reference policy. This equivalence turns $β$ from an implicit value fixed at one into a controllable regularization parameter, yielding a more general formulation that trades off proximity to a reference policy against privileged teacher guidance. We introduce $β$-OPSD and derive its optimal policy as a geometric interpolation between the reference policy and the privileged teacher. Directly optimizing this objective with reinforcement learning, however, would be costly and high-variance. Rather than optimize the RL objective directly, we turn its closed-form solution into a distillation target. Each value of $β$ selects a target along the reference-to-teacher path, which we implement efficiently by mixing their token-level logits. In this way, inexpensive distillation approximates the solution of expensive policy optimization. Return-to-go credit assignment further aligns token updates with the sequence-level objective while retaining the simplicity of OPSD. Experiments on mathematical reasoning benchmarks show that $β$-OPSD consistently outperforms vanilla OPSD, improving optimization stability and downstream reasoning performance. Our results provide a principled route from self-distillation to policy optimization and back without sacrificing the efficiency that makes OPSD practical.

</details>


### [391] [KAISEN: Reproducible Subgroup Fairness Auditing for Clinical Risk Models](https://arxiv.org/abs/2607.28608)
*Sparsh Roy,Samuel Girmachew,Nishita Chavan*

Main category: cs.LG

TL;DR: KAISEN是一个面向临床风险模型的五阶段审计流水线，用于评估子群分层、差异度量、机制诊断、事后缓解和漂移监测，在合成基准上测试到失败点。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于临床风险模型的公平性审计（clinical risk models, subgroup disparity, equalized-odds），属于医疗AI/公平性机器学习领域，与遥感、卫星、Earth observation、目标检测、轻量化部署均无任何关联。既非遥感场景，也非目标检测任务，更不涉及模型压缩或边缘部署。

Reading priority: 1分。该论文属于医疗AI公平性审计领域，与遥感卫星在轨目标检测及模型轻量化方向无任何直接关联，不建议投入阅读时间。仅方法论层面的"压力测试到失败点"思想有极有限的间接启发。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：临床风险模型的公平性审计（fairness audit）与差异度量；应用场景：医疗健康（Healthy People 2030 社会决定因素）；传感器类型：不适用（非遥感/视觉任务）；目标类别：不适用（非目标检测）。

Model architecture: 摘要未说明具体模型架构。KAISEN是一个五阶段审计流水线（five-phase audit pipeline），包含：subgroup stratification（子群分层）、disparity measurement（差异度量）、mechanism diagnostics（机制诊断）、post-hoc mitigation（事后缓解）、drift monitoring（漂移监测）。涉及的技术组件包括 threshold optimization、Platt scaling（校准器）、CUSUM（累积和控制图，用于漂移监测）。

Lightweight method: 不涉及。摘要中没有任何关于剪枝（pruning）、量化（quantization）、知识蒸馏（knowledge distillation）、NAS、轻量网络设计、实时推理或边缘部署的内容。唯一的"优化"是per-group threshold optimization，属于公平性缓解手段而非模型压缩。

Onboard deployability: 不适用。摘要未说明任何参数量、FLOPs、延迟、显存、能耗或实时性信息。该工作面向临床审计场景，与星上/边缘部署无关。

Datasets and metrics: 数据集：合成基准（synthetic benchmark），包含16个疾病任务（16 disease tasks）、15个来自Healthy People 2030的社会决定因素轴（social-determinant axes）、3个预设交叉（prespecified intersections）。评价指标：equalized-odds difference (EOD)、rank correlation (rho)、paired delta、95% CI、chi-squared p值、CUSUM false alarms / missed shifts。所有结果均为合成数据，具有已知ground truth。

Experiments: 主要发现：(i) 显著性追踪各轴差距与其最小可检测效应的关系，EOD标准化后rank correlation从rho=0.56升至0.78；(ii) per-group threshold optimization在48/48次held-out运行中降低EOD（paired delta=-0.285），而group-wise Platt scaling在19/48次运行中改善EOD，平均效应接近零，说明应报告方差而非均值；(iii) 机制诊断在144/144受控案例中正确分类，但在proxy misspecification下48个模型驱动案例全部无法恢复且无失败信号；(iv) CUSUM失败和误报主要追踪cohort realization而非疾病，27个误报和8个漏检中的7个来自不同seeds（chi-squared p=0.002）。

Limitations: 摘要明确声明所有结果均为合成数据，不建立临床有效性（do not establish clinical validity）。此外，机制诊断在proxy misspecification下完全失效且无失败信号，说明审计组件缺乏压力测试时可能产生虚假信心。该工作与遥感/目标检测/轻量化方向完全无关。

Ideas for my research: 尽管与遥感目标检测轻量化无直接关系，但可借鉴其"压力测试到失败点"（evaluated to the point of failure）的方法论思想——即在评估轻量化模型（如剪枝/量化后的检测器）时，不应只报告平均性能，而应系统性地测试其在极端条件下的失效边界（如极端光照、遮挡、小目标密集场景），并报告方差而非均值。此外，其"审计组件需单独压力测试"的思路可类比为：轻量化方法（剪枝、量化、蒸馏）的每个环节都应单独验证其对检测精度的影响，而非只看端到端结果。但整体借鉴价值有限。

Abstract: Clinical risk models routinely achieve strong aggregate performance while producing materially different error rates across patient subgroups. Audit pipelines have been proposed to catch this, but their components are rarely stress-tested, so it is unclear which parts of an audit can be trusted and under what conditions. We present KAISEN, a five-phase audit pipeline covering subgroup stratification, disparity measurement, mechanism diagnostics, post-hoc mitigation, and drift monitoring, evaluated to the point of failure on a synthetic benchmark of 16 disease tasks, 15 social-determinant axes from Healthy People 2030, and three prespecified intersections. Four findings follow. (i) Significance tracks each axis's gap against its own minimum detectable effect: rank correlation between significance count and raw equalized-odds difference (EOD) across the 15 axes is rho = 0.56, rising to rho = 0.78 once EOD is standardized by that floor. (ii) Per-group threshold optimization reduces EOD in 48 of 48 held-out runs (paired delta = -0.285, 95% CI [-0.313, -0.252]), while group-wise Platt scaling -- the better calibrator -- behaves as a coin flip on EOD (19 of 48 runs improved, 95% CI [0.26, 0.55]) with mean effect near zero, so what an audit should report is the variance, not the average. (iii) The mechanism diagnostic classifies 144 of 144 controlled cases correctly but recovers none of 48 model-driven cases under proxy misspecification, with no signal that it failed. (iv) CUSUM failures and false alarms track cohort realization far more than disease: at the reference threshold, all 27 false alarms and 7 of 8 missed shifts come from different seeds (chi-squared p = 0.002), so a threshold tuned on one cohort fails to transfer. All results are synthetic with known ground truth and do not establish clinical validity. Code, artifacts, and scripts reproducing every number are released.

</details>


<div id='eess.IV'></div>

# eess.IV [[Back]](#toc)

### [392] [Toward Multi-Modal Deep Learning for Pulmonary Disease Classification: A Texture-Based Machine Learning Pilot Study on Public Chest X-Ray Data](https://arxiv.org/abs/2607.27286)
*Yogisri Pujitha Chinthoti*

Main category: eess.IV

TL;DR: 一项针对胸部X光片中COVID-19与肺炎分类的初步研究，使用HOG和GLCM等经典特征描述符配合传统分类器，在COVID-19影像数据集上取得75.4%的准确率，并提出了多模态深度学习架构作为未来方向。

Research relevance: 与本研究方向基本无关（1分）。该论文属于医学影像领域，任务为肺部疾病分类（COVID-19 vs 肺炎），不涉及遥感、卫星、Earth observation、aerial image、SAR、高光谱或多光谱数据；不涉及目标检测、小目标检测或旋转框检测；也不涉及轻量化、剪枝、量化、知识蒸馏、NAS或边缘/星上部署。唯一与本研究方向可能相关的点是使用了HOG和GLCM等经典特征描述符，这些特征在遥感图像处理中也有应用，但论文本身并非遥感场景。

Reading priority: 1分。该论文属于医学影像分类领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，不涉及目标检测、遥感数据或任何模型压缩/部署技术，阅读价值很低。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：医学影像分类（COVID-19 vs 其他类型肺炎）；应用场景：胸部X光片（chest radiograph）诊断；传感器类型：X光影像（posteroanterior/anteroposterior radiographs）；目标类别：COVID-19、其他肺炎。与遥感目标检测无关。

Model architecture: 使用经典特征描述符：HOG（histogram of oriented gradients）和GLCM（gray-level co-occurrence matrix）纹理特征；分类器包括logistic regression、random forest和support vector machine。未来方向提出结合卷积和transformer编码器的多模态深度学习架构，但尚未实现。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（pruning、quantization、knowledge distillation、NAS等均未提及）。使用传统机器学习方法本身计算开销较低，但并非出于轻量化设计目的。

Onboard deployability: 摘要未说明。论文未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时性等部署相关信息。传统特征+分类器方案计算量较小，但论文未从部署角度讨论。

Datasets and metrics: 数据集：COVID-19 Image Data Collection（668张后前位/前后位X光片，来自408名患者）；评价指标：准确率（accuracy）和AUC；实验设置：patient-level 5-fold stratified cross-validation以防止数据泄漏。最佳平均准确率75.4%，AUC 0.755，超过多数类基线71.6%。

Experiments: 对比了HOG和GLCM特征搭配三种经典分类器（logistic regression、random forest、SVM）的性能；最佳结果为75.4%准确率和0.755 AUC，仅略高于71.6%的多数类基线。论文强调透明报告结果及其局限性，并以此论证未来需要更大规模、多机构、伦理合规数据集来支撑多模态深度学习架构的研究。

Limitations: 1. 性能提升有限，仅比多数类基线高约3.8个百分点，说明经典特征在此任务上区分度不足；2. 数据集规模较小（668张影像），且为单中心数据；3. 提出的多模态深度学习架构仅为未来方向，尚未实现和验证；4. 与遥感、卫星、目标检测、轻量化部署完全无关。

Ideas for my research: 1. 论文使用的HOG和GLCM特征在遥感图像中也有广泛应用，可借鉴其作为轻量级特征提取手段，在资源受限的星上环境中作为深度模型的补充或替代方案；2. patient-level交叉验证防止数据泄漏的思路，对遥感数据集中同一目标/同一场景多次成像的划分有借鉴意义；3. 论文"透明报告局限性和基线对比"的做法值得学习，在遥感检测中同样应报告与简单基线的差距；4. 但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Automated classification of pulmonary disease from chest radiographs is a widely studied application of machine learning in medical imaging. This paper presents a pilot study evaluating classical texture- and gradient-based feature representations for distinguishing COVID-19 from other forms of pneumonia using the publicly available COVID-19 Image Data Collection (668 posteroanterior/anteroposterior radiographs from 408 patients). Using histogram of oriented gradients (HOG) and gray-level co-occurrence matrix (GLCM) texture descriptors with classical classifiers (logistic regression, random forest, and support vector machine), evaluated under patient-level 5-fold stratified cross-validation to prevent data leakage, we obtain a best mean accuracy of 75.4% and AUC of 0.755, modestly exceeding the 71.6% majority-class baseline. We report these results transparently, including their limitations, and use them to motivate and scope a proposed multi-modal deep learning architecture -- combining convolutional and transformer-based encoders across imaging modalities -- as a direction for future work requiring access to larger, multi-institutional, ethically sourced datasets.

</details>


### [393] [Endo-NeRF++: Uncertainty-Aware Neural Rendering with Multi-Resolution Hash Encoding for Dynamic Surgical Scene Reconstruction](https://arxiv.org/abs/2607.27825)
*Gousia Habib,Laura Ruotsalainen*

Main category: eess.IV

TL;DR: 提出Endo-NeRF++，一种面向机器人辅助微创手术动态场景重建的神经渲染框架，通过多分辨率哈希网格编码、时序特征融合和不确定性引导的自适应采样提升重建精度与时序一致性。

Research relevance: 与本研究方向基本无关（1分）。论文聚焦于机器人辅助微创手术（robot-assisted minimally invasive surgery）中的动态场景神经渲染（neural rendering / NeRF），属于医学影像与计算机图形学交叉领域。完全不涉及遥感（remote sensing）、卫星（satellite）、地球观测（Earth observation）、航空影像（aerial image）、SAR、高光谱/多光谱等遥感传感器，也不涉及目标检测（object detection）或任何模型轻量化/部署（pruning, quantization, knowledge distillation, FLOPs）方法。

Reading priority: 1分。该论文属于医学手术场景的神经渲染方向，与遥感卫星在轨目标检测及模型轻量化课题基本无关，不涉及遥感、目标检测或任何轻量化/部署方法，不建议投入阅读时间。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：动态手术场景的神经渲染与三维重建（neural rendering / 3D reconstruction）。应用场景：机器人辅助微创手术（robot-assisted minimally invasive surgery）。传感器类型：内窥镜视频（endoscopic video）。目标类别：手术组织、手术工具（tissue, surgical tools），非遥感目标。

Model architecture: 基于EndoNeRF扩展的Endo-NeRF++框架，核心组件包括：多分辨率哈希网格编码（multi-resolution hash-grid encoding）、时序特征融合（temporal feature merging）、不确定性引导的自适应采样（uncertainty-informed adaptive sampling）。用于捕捉内窥镜场景中粗细解剖细节并保证组织形变和工具遮挡下的时序稳定性。

Lightweight method: 摘要未说明。论文未涉及任何模型压缩或加速方法（如剪枝、量化、知识蒸馏、NAS、轻量网络设计等）。多分辨率哈希网格编码本身在NeRF领域常被视为一种加速表示，但摘要中未将其作为轻量化/部署手段讨论，也未报告参数量、FLOPs或推理延迟。

Onboard deployability: 摘要未说明。未报告参数量、FLOPs、显存占用、推理延迟、能耗或实时性指标。该工作面向手术场景的离线/在线渲染质量优化，与星上部署（onboard deployment）、边缘部署（edge deployment）、FPGA/NPU/TensorRT等硬件加速完全无关。

Datasets and metrics: 数据集：机器人手术视频序列（robotic surgical video sequences），具体数据集名称摘要未说明。评价指标：PSNR、SSIM、LPIPS（图像重建质量指标）。对比基线：EndoNeRF。实验结果显示不确定性引导的自适应采样相比EndoNeRF基线：PSNR提升最高1.22 dB（4.3%），SSIM提升最高5.3%，LPIPS降低最高55.1%。

Experiments: 主要实验为在机器人手术视频序列上与EndoNeRF基线进行重建质量对比，验证了不确定性引导自适应采样在PSNR、SSIM、LPIPS三个指标上的提升。摘要未说明消融实验细节、其他对比方法或计算开销对比。

Limitations: 摘要未说明局限性。但可推断：1）仅针对手术内窥镜场景，泛化到其他场景（如遥感）缺乏验证；2）未报告计算开销与部署可行性，NeRF类方法通常计算量大、推理慢，难以直接用于实时在轨推理；3）未涉及目标检测任务，仅做场景重建。

Ideas for my research: 尽管该论文与遥感在轨目标检测轻量化方向基本无关，但仍有少量可借鉴的通用技术思路：1）多分辨率哈希网格编码（multi-resolution hash-grid encoding）作为一种高效特征表示，其"多分辨率特征融合"思想可迁移到遥感小目标检测的特征金字塔设计中，帮助同时捕捉粗细尺度特征；2）不确定性引导的自适应采样（uncertainty-informed adaptive sampling）思想可类比到检测任务中的难例挖掘（hard example mining）或注意力机制，将计算资源集中到高不确定性区域（如小目标、遮挡目标）；3）时序特征融合（temporal feature merging）对视频卫星/多帧遥感目标检测可能有参考价值。但整体而言，该论文对"在轨遥感目标检测模型轻量化"课题的直接借鉴价值有限。

Abstract: Reconstructing dynamic surgical scenes is crucial for robot-assisted minimally invasive surgery; however, it continues to be difficult because of tissue deformation, occlusions, specular reflections, and restricted viewpoints. In this study, we introduce Endo-NeRF++, a neural rendering framework that accounts for uncertainty in the reconstruction of dynamic surgical scenes. Expanding on EndoNeRF, the suggested approach incorporates multi-resolution hash-grid encoding, temporal feature merging, and uncertainty-informed adaptive sampling to enhance reconstruction accuracy and temporal coherence in deformable endoscopic scenes.The multi-resolution hash-grid representation within the framework effectively captures both coarse and fine anatomical details, while temporal feature blending ensures stable reconstruction during tissue deformation and surgical tool occlusions. Additionally, uncertainty-driven adaptive sampling assigns more samples to uncertain areas to enhance rendering quality and geometric coherence. Experiments on robotic surgical video sequences demonstrate that the proposed uncertainty-guided adaptive sampling improves PSNR by up to 1.22\,dB (4.3\%), increases SSIM by up to 5.3\%, and reduces LPIPS by up to 55.1\% compared with the EndoNeRF baseline.

</details>


### [394] [TSOG: A Format For Temporally And Spatially Ordered Gaussians](https://arxiv.org/abs/2607.28049)
*Shady Gmira,Evangelos Alexiou,Emmanouil Potetsianakis,Emmanuel Thomas*

Main category: eess.IV

TL;DR: 提出TSOG格式，将4D Gaussian Splatting动态场景内容高效编码为索引对齐的图像数据，实现超过90%的文件体积压缩且PSNR损失极小。

Research relevance: 与遥感卫星在轨目标检测及轻量化方向基本无关。论文聚焦于4D Gaussian Splatting动态场景的表示与压缩格式，属于计算机图形学/3D视觉内容编码领域，不涉及遥感、卫星、目标检测或模型轻量化。

Reading priority: 1分。论文属于4D Gaussian Splatting内容编码领域，与遥感卫星在轨目标检测及模型轻量化方向基本无关，仅存在非常间接的压缩方法论类比价值。

<details>
  <summary>Details</summary>

Task and scene: 任务：4D Gaussian Splatting动态场景的高效表示与压缩编码。应用场景：4D内容存储、传输与交付（如动态3D场景）。传感器类型：不涉及遥感传感器。目标类别：不涉及目标检测。

Model architecture: TSOG扩展了SOG框架到时间域，为每个Gaussian引入timeline属性和几何/外观属性的时间参数化。每个Gaussian分配唯一索引，属性值编码为索引对齐的图像数据。TSOG是模型无关（model-agnostic）的，兼容离散和连续4DGS表示。

Lightweight method: 论文的"轻量化"体现在文件体积压缩（lossy format），通过将Gaussian属性编码为索引对齐图像数据实现超过90%的文件大小缩减。但这属于内容编码/压缩，而非神经网络模型压缩（pruning/quantization/distillation/NAS等），与深度学习模型轻量化方法论不同。

Onboard deployability: 摘要未说明。未涉及星上/边缘部署、参数量、FLOPs、延迟、显存、能耗或实时推理等部署指标。

Datasets and metrics: 使用PLYs序列作为baseline（简单4DGS表示），FreeTimeGS作为SOTA 4DGS baseline。评价指标：文件大小缩减率（>90%）、PSNR（差异范围-0.42至+0.85 dB）。

Experiments: 与PLYs序列和FreeTimeGS对比，TSOG实现超过90%的文件体积缩减，PSNR差异在-0.42到+0.85 dB之间，表明在极小质量损失下获得显著体积节省。

Limitations: 摘要未说明局限性。但可推断：1）该格式针对4DGS动态场景内容，与遥感目标检测任务无直接关联；2）PSNR差异范围较宽（-0.42至+0.85 dB），在部分场景可能存在质量下降；3）未提供推理速度、内存占用等部署相关指标。

Ideas for my research: 尽管整体弱相关，但可借鉴的间接思路：1）TSOG将高维数据编码为索引对齐图像数据的思路，可类比于将模型权重或特征图进行结构化编码压缩；2）"模型无关（model-agnostic）"的压缩框架设计思想，可迁移到设计不依赖特定检测器架构的通用轻量化方案；3）lossy compression中"以最小质量损失换取大幅体积缩减"的权衡思路，与模型剪枝/量化中精度-压缩比权衡有方法论上的相似性。但这些均为间接类比，直接借鉴价值有限。

Abstract: We propose Temporally and Spatially Ordered Gaussians (TSOG), a format for efficient representation of 4D Gaussian Splatting (4DGS) content. TSOG extends the Spatially Ordered Gaussians (SOG) framework to the temporal domain by introducing a timeline attribute and temporal parameterization of geometry and appearance attributes. Similar to SOG, TSOG is a lossy format that assigns each Gaussian a unique index and encodes attribute values as index-aligned image data. TSOG is model-agnostic, extensible, and compatible with both discrete and continuous 4DGS representations. Evaluation using a PLYs sequence and FreeTimeGS as baselines, serving as simplistic and state-of-the-art 4DGS representations respectively, shows file size reductions exceeding 90%, with PSNR differences ranging between -0.42 and +0.85 dB. These results demonstrate substantial file size savings with minimal quality degradation, enabling efficient representation, storage, and delivery of dynamic scenes for next-generation 4D content.

</details>


### [395] [ReGenVC: End-to-End Real-Time Generative Video Coding at Ultra-Low Bitrate](https://arxiv.org/abs/2607.28144)
*Zheyuan Zhang,Johnson Wu*

Main category: eess.IV

TL;DR: ReGenVC 是一个端到端生成式视频编解码器，通过神经压缩首帧、逐帧姿态关键点和元数据将 talking-head 视频压缩到超低码率（77帧约26 kB），并利用四步蒸馏扩散Transformer在8-GPU系统上实现实时解码（24 fps）。

Research relevance: 弱相关（2分）。该论文属于生成式视频编解码（generative video codec）领域，面向 talking-head 视频压缩与实时解码，与遥感/卫星/在轨目标检测无直接关系。但其"四步蒸馏扩散模型"（four-step distilled diffusion transformer）实现实时推理、模型蒸馏加速、系统级并行优化（sequence parallelism、pipeline）和显存优化等思路，对轻量化与实时推理有一定方法学借鉴意义，但并非遥感场景。

Reading priority: 1分。该论文与遥感卫星在轨目标检测及轻量化方向基本无关，属于生成式视频编解码领域。虽然蒸馏和系统级优化有一定方法学参考价值，但场景差异过大，且依赖多GPU数据中心部署，与星上单卡低功耗约束相去甚远，不建议优先精读。

<details>
  <summary>Details</summary>

Task and scene: 任务类型：端到端生成式视频压缩与实时解码（generative video codec）。应用场景：talking-head 视频的极低码率传输与实时浏览器直播。传感器类型：普通视频（RGB talking-head）。目标类别：人脸/说话人视频帧，非遥感目标。

Model architecture: 编码器：将源片段压缩为紧凑比特流——神经压缩的首帧（neurally compressed first frame）、逐帧姿态关键点（per-frame pose keypoints）和元数据。解码器：四步蒸馏扩散Transformer（four-step distilled diffusion transformer），以传输的姿态和参考帧为条件重建视频，包含Transformer和VAE组件。

Lightweight method: 核心轻量化/加速手段：(1) 四步蒸馏（four-step distillation）将多步扩散采样压缩为4步，使解码达到实时；(2) 八GPU统一序列并行（Ulysses & Ring sequence parallelism）；(3) 空间分割VAE（spatially-split VAE）；(4) 三阶段重叠流水线（three-stage overlapped pipeline）；(5) 混合CPU-GPU部署，将编码器及一次性条件编码器卸载到CPU，降低GPU显存峰值。

Onboard deployability: 部署信息：8-GPU节点上维持24 fps输出（每25帧窗口972 ms，在1000 ms预算内）。混合CPU-GPU部署将每GPU显存峰值从21.1 GB降至约7.7 GB。但这是多GPU数据中心级部署，非星上/边缘单卡部署，能耗和单卡延迟未说明。摘要未说明参数量、FLOPs、单卡推理延迟或能耗。

Datasets and metrics: 摘要未说明具体数据集名称。评价指标：码率（bitrate，约26 kB vs 传统编解码器250-280 kB）、帧率（fps，24 fps）、延迟（每25帧窗口972 ms）、显存峰值（21.1 GB降至7.7 GB）。对比方法：x264/x265传统视频编解码器。

Experiments: 主要结果：相比x264/x265，码率降至约十分之一（26 kB vs 250-280 kB）且重建基本无伪影；在匹配的超低码率下传统编解码器出现blocking artifacts而ReGenVC保持清晰。系统在8-GPU节点上实现24 fps实时解码，支持无帧下溢的实时浏览器流。消融/系统技术验证包括四步蒸馏、序列并行、空间分割VAE、三阶段流水线及CPU-GPU混合部署。

Limitations: (1) 与遥感/卫星/在轨目标检测完全无关，属于视频压缩领域；(2) 依赖8-GPU数据中心级并行，不适用于星上单卡/低功耗约束；(3) 面向talking-head视频，依赖强生成先验（人脸结构），无法直接迁移到遥感目标检测；(4) 摘要未说明参数量、FLOPs、单卡延迟、能耗等对轻量化评估更关键的指标；(5) 未涉及目标检测任务。

Ideas for my research: (1) 四步蒸馏扩散模型实现实时推理的思路可借鉴——若在轨检测采用扩散或生成式模型，可通过多步蒸馏压缩采样步数；(2) 模型蒸馏（distillation）作为轻量化手段，可迁移到遥感目标检测模型的teacher-student蒸馏；(3) 系统级优化（pipeline重叠、并行策略、CPU-GPU异构卸载）对星上异构计算（如CPU+NPU/FPGA）的部署调度有参考价值；(4) 显存优化策略（将部分模块卸载到CPU）对星上有限显存/内存约束下的推理部署有启发；(5) 但整体方法面向视频生成而非检测，借鉴价值有限，仅作为实时推理系统优化的参考。

Abstract: We present ReGenVC, an end-to-end generative video codec that compresses talking-head video to an ultra-low bitrate and decodes it in real time. The encoder reduces a source clip to a compact bitstream -- a neurally compressed first frame, per-frame pose keypoints, and metadata -- totaling about 26 kB for a 77-frame sequence. The decoder is a four-step distilled diffusion transformer that reconstructs the video conditioned on the transmitted pose and reference frame. Compared with x264/x265, ReGenVC reduces the bitrate to roughly one tenth of that required by traditional codecs (about 26 kB vs. 250--280 kB for essentially artifact-free reconstruction); at a matched ultra-low bitrate, conventional codecs collapse into blocking artifacts while ReGenVC stays sharp by exploiting a strong generative prior. The central obstacle to deploying such a codec is decoder latency: multi-step sampling with transformer and VAE components is too slow for interactive use. We make the decoder real-time through four-step distillation and three model-preserving system techniques: (i) eight-GPU unified sequence parallelism (Ulysses & Ring), (ii) a spatially-split VAE, and (iii) a three-stage overlapped pipeline; an analytical timing model characterizes the real-time feasibility region. On an 8-GPU node, the system sustains 24 fps output (972 ms per 25-frame window, within the 1000 ms budget), enabling a live browser stream without observed frame underruns. A hybrid CPU-GPU deployment further runs the encoder on the CPU at 24 fps and offloads the decoder-side one-shot conditioning encoders to the CPU, reducing the per-GPU memory peak from 21.1 GB to about 7.7 GB. To our knowledge, ReGenVC is the first end-to-end generative video codec to combine ultra-low-bitrate encoding with real-time decoding on an 8-GPU system.

</details>
